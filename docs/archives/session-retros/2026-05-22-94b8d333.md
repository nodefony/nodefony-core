---
date: 2026-05-22
session_id: 94b8d333-aa91-4544-89d3-24d27e60b192
focus: Allowlist permissions (anti-prompt) + Volet 2 HMR (DevSupervisor auto-restart) + feedback visibilité
---

# Session retro — 2026-05-22 — 94b8d333

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    39 |
| Edit  |    16 |
| Read  |    12 |
| Write |     3 |
| Skill |     3 |

## Top fichiers Read/Edit

| Fichier                            |   × |
| ---------------------------------- | --: |
| kernel/MEMORY.md                   |   2 |
| kernel/commands/DevCommand.ts      |   2 |
| .claude/settings.json              |   2 |
| (4 mémoires IA éditées 2× chacune) |   2 |
| DevSupervisor.ts (Write)           |   1 |
| Aucun fichier RE-LU 2×+            |   0 |

## Coûts évidents

- **Pollution environnement** : ~5 superviseurs fantômes orphelins (PPID=1) de sessions
  précédentes traînaient — un watchait encore et respawnait un serveur. A causé un **faux
  EADDRINUSE** au 1er test du superviseur → 1 round de diagnostic + nettoyage par groupe avant
  de pouvoir valider à froid. Coût réel mais instructif (= exactement le bug que le code corrige).
- `git index.lock` résiduel (0-octet, laissé par un fantôme tué mid-build) → 1 commande de
  déblocage.
- Sinon **session efficace** : 0 fichier re-lu, build/test une seule fois, validation runtime
  d'un seul jet.

## Recommandations

1. **Hygiène pré-test runtime** (ajoutée à [[feedback-watch-rollup-pitfall]]) : toujours
   `lsof -ti:5151 -ti:5152 -ti:5173 -ti:5177 | xargs kill -9` AVANT un test serveur — des
   superviseurs fantômes peuvent tenir les ports. Le nouveau DevSupervisor (group-kill) empêche
   d'en créer, mais le passif existe.
2. **Feedback visibilité appliqué** ([[feedback-user-visibility]] révisé) : le style « Caveman »
   ultra-court est trop cryptique → narrer l'étape + le but + la suite, donner les transitions.
3. **8b allowlist** : sweep auto OK — rien à ajouter, l'allowlist mise à jour en début de session
   couvre tout. Pattern à reproduire : faire le sweep EN DÉBUT quand le sujet est les permissions.

## Patterns récurrents (déjà gérés)

- ✅ Orphelins Vite / EADDRINUSE → maintenant corrigé dans le code (detached+group-kill+wait-ports).
- ✅ Permissions re-prompt → wildcards sûrs + sweep auto à chaque retex (étape 8b).
- ✅ Décisions archi → toutes en mémoire IA (mises à jour avant clôture).

## Commits produits

| Commit  | Sujet                                                                                      |
| ------- | ------------------------------------------------------------------------------------------ |
| 3ce3fc5 | feat(core): DevSupervisor auto-restart dev — anti-orphelin + anti-EADDRINUSE (volet 2 HMR) |
| 6843b9e | chore(claude): allowlist process sûrs + balayage allowlist auto à chaque retex             |
