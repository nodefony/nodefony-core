---
date: 2026-05-21
session_id: 04b0af9e-4e30-4e4d-b0a2-e11803b6a070
focus: CLEAN des skills — axes 1→4 (dégraissage, descriptions, dédup, périmé) + généralisation wrapper frontend multi-framework
---

# Session retro — 2026-05-21 — 04b0af9e

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    44 |
| Edit            |    18 |
| Read            |    13 |
| Write           |     3 |
| AskUserQuestion |     2 |
| Skill           |     1 |

## Top fichiers Read/Edit

| Fichier                                                                  |         × |
| ------------------------------------------------------------------------ | --------: |
| nodefony-roadmap/SKILL.md                                                | 6 (edits) |
| frontend/docs/index.md                                                   | 2 R + 2 E |
| ts-docs / rfc / nestjs / create-module / create-frontend-module SKILL.md |  2 chacun |

## Coûts évidents

- Session 100 % maintenance skills → **0 restart serveur, 0 build** (coût habituel #1 absent).
- 44 Bash = beaucoup de scans diagnostic (jq/grep/awk) — normal pour un audit de refs.
- 2 faux positifs de vérif coûteux : (1) `md5` comparatif faussé par un `//` double-slash dans `git show HEAD:$d/...` (le glob laisse un `/` final) ; (2) regex `@nodefony/[a-z-]+` tronquant `orm-core`→`orm-`. → 2 itérations de re-vérif.
- 1 `AskUserQuestion` rejeté (clarification du nom de skill) → 1 aller-retour. Le user voulait piloter le nommage.

## Recommandations

1. **Pas de nouveau skill** — la session est ponctuelle (maintenance), aucun pattern ≥3 récurrent à automatiser.
2. **Piège `git show HEAD:$path` en boucle** : toujours `f="${d%/}/SKILL.md"` (le glob `dir/` laisse un slash → `//` casse silencieusement git show, côté gauche vide → faux DIFF). À retenir pour les futurs scripts de vérif diff-vs-HEAD.
3. **Convention description de skill** (figée cette session) : format `description: >`, structure « 1 phrase action + Déclencheurs: … », cible 200–500 car, jamais le détail interne (→ corps). Candidate à inscrire dans la convention skills du CLAUDE.md si on rouvre un chantier skills.

## Patterns récurrents (déjà gérés)

- ✅ Commits FR via heredoc `git commit -F -` (apostrophes) — feedback_commit_fr_apostrophes.
- ✅ Décision design tranchée + justifiée plutôt que QCM (sauf nommage produit où le user pilote).
- ✅ Split mécanique par `sed`/`awk` préservant le contenu (pas de réécriture des gros blocs).

## Commits produits

| Commit  | Sujet                                                                                                                        |
| ------- | ---------------------------------------------------------------------------------------------------------------------------- |
| f2b0d7e | dégraisse create-module (876→184 + reference/templates.md) + généralise wrapper frontend multi-framework (react/vue/angular) |
| 01e86c9 | normalise les 16 descriptions (format + triggering, 0 régression corps)                                                      |
| 4e370e4 | factorise le mécanisme des docs-loaders → règle CLAUDE.md unique                                                             |
| 4b0fd28 | purge le périmé (Vision→Studio, +Angular roadmap, P5/P6=0% corrigé)                                                          |
