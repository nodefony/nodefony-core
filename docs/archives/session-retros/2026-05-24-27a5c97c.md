---
date: 2026-05-24
session_id: 27a5c97c-fd04-41cd-9b3a-95d36b8351db
focus: P14.5 (build prod + renderProdTags + Statics + coquille templatable) + load test révélateur ×23 session
---

# Session retro — 2026-05-24 — 27a5c97c

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |   103 |
| Edit  |    61 |
| Read  |    32 |
| Skill |     4 |
| Write |     3 |

## Top fichiers Edit

| Fichier                      |   × |
| ---------------------------- | --: |
| frontend/FrontendService.ts  |  13 |
| MIGRATION_STATUS.md          |   8 |
| frontend/TemplateHelper.ts   |   7 |
| frontend/IFrontendService.ts |   4 |
| http/server-static.ts        |   3 |

## Coûts évidents

- **Session multi-features non découpée** : P14.5 (renderProdTags/Statics/build) + coquille templatable (renderDocument/Twig/EJS) + exploration charge → 229 tours, cache-read = 74 % du coût. `/clear` entre les 3 blocs aurait coupé le cache porté.
- **Turbo cache stale** (piège connu) : `--filter` a renvoyé un cache-hit alors que la source avait changé → `renderDocument is not a function` au runtime → debug + `--force`. ~3 cycles perdus.
- **index.lock husky stale** rencontré 3× sur les commits (rm manuel à chaque fois).
- **Load test corésident trompeur** : 1ʳᵉ conclusion (session I/O) puis 2ᵉ (corésidence) toutes deux non prouvées avant l'A/B 2-workers décisif. Mesurer avant de conclure aurait évité 2 explications fausses.

## 💶 Coût (€)

- Total **~€107 (≈ $116)** — cache-read $86 (74 %) · output $16 · cache-write 1h $13 · input $1. **Cache-dominé** = contexte porté sur 229 tours. 3 livrables (2 commits feat + 1 doc) → ~€36/livrable, élevé car session longue non clivée.

## Recommandations

1. **`/clear` entre features** — P14.5 ≠ coquille templatable ≠ load test. 3 sessions courtes auraient divisé le cache-read.
2. **Réflexe turbo `--force` (ou build direct) dès qu'un changement en chaîne ne se reflète pas au runtime** — déjà en mémoire (`feedback_turbo_cache_stale_logs`), à appliquer plus tôt.
3. **Load test : mesurer (A/B, curl -w) AVANT de conclure** — ne pas théoriser le goulot. L'A/B 2-workers a tranché net (×23).
4. Nouvelle mémoire `feedback_named_process_titles` créée (exec -a + log nommé, pas `| tail`).

## Patterns récurrents (déjà gérés)

- ✅ index.lock stale → rm (kit `project_retex_improvements_kit` #I — garde-fou husky toujours pas posé, 13ᵉ occurrence).
- ✅ commit FR via `-F` fichier (apostrophes).
- ✅ memory.test obligatoire pipeline (8/8).

## Commits produits

| Commit  | Sujet                                                                              |
| ------- | ---------------------------------------------------------------------------------- |
| c5c8c12 | docs(migration): axe P16.H cluster + vues Studio                                   |
| 2d87361 | feat(frontend): P14.5 page blanche prod résolue (renderProdTags + Statics + build) |
| 2d2a360 | feat(frontend): coquille templatable (renderDocument + helpers Twig/EJS)           |

## Finding marquant (load test)

A/B cluster -w 2, HTTP clair : route AVEC session `/test/index` **210 rps** vs SANS session `/realtime/api/health` **4876 rps** = **×23**. Le client n'était pas le mur (4876 rps mono-core) ; goulot = contention `FileSessionStorage` write/req + race. **Justifie chiffré la migration JWT stateless** (P6/P16). Noté dans `project_security_stateless_http_decision`.
