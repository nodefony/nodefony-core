---
date: 2026-05-21
session_id: e70144c1-274a-45a3-a2ff-479aab31bf51
focus: Debug bar realtime « de rêve » (Core isomorphe nodefony/debugbar + montage Vite/Studio/EJS)
---

# Session retro — 2026-05-21 — e70144c1

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Edit            |    66 |
| Bash            |    64 |
| Read            |    30 |
| Write           |     9 |
| Skill           |     3 |
| AskUserQuestion |     1 |

## Top fichiers Read/Edit

| Fichier                                                    |   × |
| ---------------------------------------------------------- | --: |
| client/debugbar/DebugBar.ts                                |  27 |
| client/debugbar/model.ts                                   |   9 |
| tests/DebugBar.test.ts                                     |   5 |
| studio realtime providers.ts + StudioRealtimeController.ts | 3+3 |
| studio UiStore.ts + AdminLayout.tsx                        | 3+2 |
| frontend TemplateHelper.ts                                 |   3 |
| core rollup.config.ts + package.json                       | 3+2 |
| test DefaultController.ts + index.ejs                      | 3+3 |

## Coûts évidents

- **DebugBar.ts éditée 27×** : feature étoffée en 4 vagues successives (MVP → « de rêve » → realtime/HMR → env/branche → chrome réduire/dock) à chaque nouveau message user. Beaucoup de petits Edits sur le même gros fichier au lieu d'un cahier des charges complet en amont.
- **3 restarts serveur** (chacun pour propager du code serveur : studio backend, test controller). Acceptables (vrais points de mesure) mais le 1er montage EJS aurait pu être testé en 1 cycle si la CSP avait été anticipée.
- 1 aller-retour CSP : `non visible` = inline `<script>` bloqué par `script-src 'self'` sur page EJS — non anticipé (les pages React surchargent la CSP, pas l'EJS).

## Recommandations

1. **Mini-cahier des charges pour une feature « vitrine »** — quand le user dit « de rêve », lister d'emblée les panneaux/contrôles probables (realtime, perf, logs, env/branche, réduire/dock, multi-montage) avant de coder → moins de re-passes sur le même fichier. (cf `feedback_session_hygiene`).
2. **CSP = réflexe pour toute injection `<script>` sur page rendue serveur** : page non-Vite = `script-src 'self'` → jamais d'inline, toujours script externe. Mémorisé dans test/MEMORY.md.
3. **Pattern « subpath client core »** désormais rodé (debugbar) : entry Rollup séparée + export package.json + preserveModules partagé. Réutilisable pour SIP/media (P15).

## Patterns récurrents (déjà gérés)

- ✅ Pas de headless Chrome → curl (transform Vite, @fs, injection) + vérif visuelle user (`feedback_no_headless_chrome`).
- ✅ Build/test impactés lancés + suspecter son diff (CSP, minbar ref, champs Pdu) avant de blâmer le pré-existant.
- ✅ Sparklines SVG maison, pas recharts (`feedback_recharts_react19`).
- ✅ Logs réhydratés en Pdu (même classe Core des 2 côtés) — corrigé suite à la question user « tu as utilisé syslog client ? ».

## Commits produits

| Commit  | Sujet                                                                              |
| ------- | ---------------------------------------------------------------------------------- |
| 80c11b8 | feat(debugbar): debug bar realtime « de rêve » — Core isomorphe + montage Vite/EJS |
| 9f8b0e2 | docs(debugbar): trace la debug bar dans les MEMORY.md (core/studio/frontend/test)  |
