---
date: 2026-05-20
session_id: 3c788b5e-ef52-463a-b785-89648977655c
transcript_size: 2.1 MB / 620 lines
focus: P14.11 G2 — fix bug SSE Pdu listener cleanup HTTP/2 + ANSI parser Logs
---

# Session retro — 2026-05-20 — 3c788b5e

## Tool usage

| Outil            | Calls |
| ---------------- | ----: |
| Bash             |    77 |
| Read             |    26 |
| Edit             |    22 |
| TaskUpdate       |     7 |
| browser_navigate |     7 |
| Write            |     5 |
| TaskCreate       |     4 |
| Skill            |     3 |
| ToolSearch       |     2 |
| browser_snapshot |     1 |
| browser_close    |     1 |
| AskUserQuestion  |     1 |

## Top fichiers Read

| Fichier                                          |   × |
| ------------------------------------------------ | --: |
| `src/nodefony/src/syslog/Syslog.ts`              |   5 |
| `src/nodefony/src/kernel/Kernel.ts`              |   3 |
| `.claude/settings.local.json`                    |   3 |
| `src/packages/@nodefony/http/.../http-kernel.ts` |   2 |
| `src/nodefony/src/Service.ts`                    |   2 |

## Coûts évidents

- **11 restarts serveur** pendant le diagnostic SSE (5 itérations patch debug + 6 autres) — chaque restart = ~12s boot + log volumineux à parser. Itérations nécessaires car overrides JS (`on/off/removeListener`) ne capturaient pas le path réel ; il a fallu poll listenerCount avec `setInterval` pour pinpoint le drop, puis tag les listeners avec `__tag` pour identifier qui retirait notre handler.
- **Syslog.ts lu 5×** pour comprendre les flow `on/off/fire` cross-Service/Event/EventEmitter — candidate pour MEMORY.md du module.
- **MCP browser plante** sur HTML volumineux (1 navigate OK, ensuite ERR_CONNECTION refused puis EOF) — pas réutilisable pour les pages Studio actuelles dans Docker.

## Recommandations

1. **Skill `debug-syslog-listeners`** — toolset prêt-à-l'emploi pour tracer `on/off/removeListener/fire` sur le syslog kernel avec stack traces + poll listenerCount. Réutilisable pour tout bug listener Syslog/Event similaire.
2. **MEMORY.md `@nodefony/core` syslog** — ajouter la map des chemins de log (`Service.log → Syslog.log → fire("onLog") → wrapper init → console`). Évite de relire Syslog.ts/Service.ts à chaque debug listener.
3. **Browser MCP** — éviter pour les pages avec stack traces JSON volumineuses (le MCP container choke). Pour valider Studio, préférer `curl` + assertion ou test manuel browser local.
4. **Strip ANSI dans les debug logs serveur** — les codes ANSI bruts dans `[SYSLOG fire] payload=...` polluaient les greps. Toujours `sed 's/\x1b\[[0-9;]*m//g'` quand on tail `/tmp/nodefony-server.log`.

## Patterns récurrents (déjà gérés)

- ✅ Kill server one-shot — déjà mémorisé `feedback_server_kill_oneshot.md`
- ✅ Permissions skill — migré dans `.claude/settings.json` (commit `98fc302`)
- ✅ HTTP/2 piège `req.on("close")` — leçon généralisée dans `feedback_sse_http2_request_close.md`

## Commits produits

| Commit    | Sujet                                                  |
| --------- | ------------------------------------------------------ |
| `2eae01f` | fix(studio): SSE logs stream — listener cleanup HTTP/2 |
| `dbe32b2` | feat(studio): mini-parser ANSI → React pour Logs       |
| `98fc302` | chore(claude): permissions Claude Code partagées       |

3 commits propres pushés sur `claude-ts`.
