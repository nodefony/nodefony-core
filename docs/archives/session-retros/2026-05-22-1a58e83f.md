---
date: 2026-05-22
session_id: 1a58e83f-f529-4480-b32b-160d20420f95
focus: CI verte + élimination I/O sync du cycle + passe RFC complète (HTTP+WS) + A→E
---

# Session retro — 2026-05-22 — 1a58e83f

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |   213 |
| Edit       |    85 |
| Read       |    70 |
| Write      |     8 |
| Skill      |     3 |
| WebFetch   |     1 |
| ToolSearch |     1 |

## Top fichiers Read/Edit

| Fichier                  | × (R+E) |
| ------------------------ | ------: |
| WebsocketContext.ts      |      18 |
| Controller.ts            |      17 |
| Resolver.ts              |      13 |
| upload-service.ts        |       6 |
| SKILL.md (framework-dev) |       9 |
| Kernel.ts                |       7 |
| Finder.ts                |       7 |
| FileClass.ts             |       4 |

## Coûts évidents

- Session **très longue** (1854 lignes, 5 MO, 1 `/compact`) — 4 chantiers enchaînés (CI, I/O sync,
  cycle RFC, A→E) dans une seule session. Aurait pu être 2-3 sessions (`/clear` entre CI et RFC).
- **213 Bash** dont beaucoup de cycles build→restart→test. Le piège **turbo restaure du dist caché**
  a coûté plusieurs faux diagnostics (route qui hang, header périmé) avant de fixer en `--force`.
- WebsocketContext/Controller/Resolver relus 7× chacun (gros fichiers, multi-passes RFC).

## Recommandations

1. **`/clear` entre chantiers non liés** — CI ≠ I/O sync ≠ cycle RFC. 1 feature = 1 session courte.
2. ✅ **turbo `--force`** désormais en piège structurel du skill §2 + RETEX §11 (ne plus se faire avoir).
3. ✅ **Contrat de réponse RFC** capturé dans skill §4 (auto-JSON/trap, charset JSON, close codes, maxPayload).

## Patterns récurrents (déjà gérés)

- ✅ Spawn detached + kill ports via skill `nodefony-start-server` (pas de SIGHUP).
- ✅ memory.test 8/8 avant commit pipeline.
- ✅ Commits FR via `git commit -F` (apostrophes) + sujet minuscule (commitlint).
- ✅ Vérif RFC EXACTE via skill `nodefony-rfc` (charset JSON, 4xxx privé) — pas de « raisonnable » inventé.

## Commits produits (extrait — passe finale A→E)

<!-- prettier-ignore -->
| Commit | Sujet |
| --- | --- |
| 50d21cf | feat(http) maxPayload WS + close 1009 RFC 6455 §7.4.1 |
| a11a6c1 | test(http) suites RFC headers + forward + stream charge |
| e9dfff3 | docs(bug) clôture CI-001/002 |
| 1669997 | docs(skill) framework-dev v1.2.0 |
| (+ antérieurs même session) | CI-001/002, FileClass async, upload async, Controller async, auto-JSON, throws, WS close codes, helper toWsCloseCode |
