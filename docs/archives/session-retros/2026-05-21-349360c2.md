---
date: 2026-05-21
session_id: 349360c2-257f-4eac-a116-e46ba2e1cc41
focus: profiler.queries (seam ORM via ALS) + P5.9 adapter User Drizzle + 2 skills (externals, security-review)
---

# Session retro — 2026-05-21 — 349360c2

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    61 |
| Edit            |    41 |
| Read            |    33 |
| Write           |     9 |
| Skill           |     3 |
| AskUserQuestion |     2 |

## Top fichiers Read/Edit

| Fichier                            |                                  × |
| ---------------------------------- | ---------------------------------: |
| sequelize/orm-core/SequelizeOrm.ts | 5 (tap global ajouté puis reverté) |
| drizzle/src/user/userTable.ts      |                                  3 |
| http/src/profiler/Profiler.ts      |                                  2 |
| http/src/context/Context.ts        |                                  2 |
| core/runtime/RequestContext.ts     |                                  2 |

## Coûts évidents

- **Aller-retour ALS** : tap profiler d'abord posé en `logging` GLOBAL sur l'instance Sequelize → ALS perdue dans le pool (callback détaché) → reverté + refait en `#prof` PAR REQUÊTE (closure capture le buffer). 5 edits sur SequelizeOrm.ts + 1 probe jetable. Leçon = mémorisée (gotcha pool ALS).
- **Build cassé tardif** : @nodefony/user (peerDep) non externalisée → rollup la bundle → casse sur @node-rs/bcrypt. Détecté seulement au `npm run build` complet, pas avant. → a motivé le skill `nodefony-check-externals`.
- **Contamination cross-test** : `entityRegistry.unregister("User")` sans orm efface le bucket entier → cassait le banc P7.4. Fix = scoper `unregister("User", ORM)`.

## Recommandations

1. ✅ FAIT — `nodefony-check-externals` (audit external↔peerDeps) : aurait évité le build cassé. Aurait pu tourner AVANT le build.
2. ✅ FAIT — `nodefony-security-review` (passe sécu/RFC) : validé en réel, attrape la dette profiler, heuristique corrigée (tagged templates).
3. Mémoriser le gotcha "ALS perdue dans pool ORM → tap PAR REQUÊTE" — fait dans drizzle/sequelize MEMORY + CLAUDE core.

## Patterns récurrents (déjà gérés)

- ✅ Caveman + micro-updates respectés.
- ✅ Décisions archi tranchées sans QCM (sauf rollup.config protégé → AskUserQuestion légitime).
- ✅ Tests dans la même session que le code (profiler 5, user-drizzle 8).
- ✅ ORM par défaut d'abord (Drizzle) — nouvelle préférence mémorisée.

## Commits produits

| Commit  | Sujet                                                             |
| ------- | ----------------------------------------------------------------- |
| c7c57d5 | feat(profiler): populate profiler.queries — tap ORM via ALS       |
| 48fcf9e | feat(drizzle): P5.9 adapter User Drizzle (ORM par défaut)         |
| 9aa5132 | feat(skills): nodefony-check-externals + nodefony-security-review |
| 8734a39 | fix(skills): security-review détecte les tagged templates sql     |
