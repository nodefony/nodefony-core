---
date: 2026-05-23
session_id: ddf3d1e3-122b-44a8-9a2a-b7b27f63089d
focus: Realtime — extraction isomorphe (JsonRpcPeer + IRealtimeTransport + RealtimeController) + actions WS + paire skills 1.6.0
---

# Session retro — 2026-05-23 — ddf3d1e3

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |   114 |
| Edit            |    72 |
| Read            |    51 |
| Write           |    15 |
| Skill           |     3 |
| AskUserQuestion |     1 |

## Top fichiers Edit/Write

| Fichier                                      |   × |
| -------------------------------------------- | --: |
| `RealtimeClient.ts`                          |  12 |
| `nodefony-framework-dev/SKILL.md`            |  10 |
| `studio/CLAUDE.md`                           |   7 |
| `JsonRpcPeer.ts`                             |   7 |
| `client/index.ts`                            |   6 |
| `StudioRealtimeController.ts`                |   5 |
| `ConnectionStore.ts` / `studio-dev/SKILL.md` |   5 |

## Coûts évidents

- **`.git/index.lock` récurrent (husky/lint-staged)** : ~5 commits ont nécessité `rm .git/index.lock` + retry.
  Le 1er essai prenait le lock, husky échouait au stash → lock orphelin. Coût = re-tape du heredoc.
- **commitlint `subject-case`** : 2 commits rejetés car sujet commençait par PascalCase (`JsonRpcPeer…`,
  `IRealtimeTransport…`). Re-commit avec sujet minuscule. → règle à connaître AVANT (évite 2 retries).
- Live tests WS = scripts `.nf-*.mjs` jetables dans le repo (résolution `ws`) ; 1 bug d'instrumentation
  (canal à granularité poussé sur le nom EXACT suffixé → comptais le nom nu → faux « 0 push »).
- Session ÉNORME (1275 lignes transcript) — dialogue archi très dense (user pilote la vision isomorphe).

## Recommandations

1. **Commit : sujet TOUJOURS en minuscule** (commitlint `subject-case`) + `git commit -F` (apostrophes FR).
   → consigné dans `feedback_commit_fr_apostrophes`. Évite le pattern « 2 commits rejetés / session ».
2. **`.git/index.lock` husky** : si commit échoue sur lock, `rm -f .git/index.lock` PUIS re-commit (fichiers
   restent staged). Connu, pas un bug à investiguer.
3. **Test WS live** : un canal à granularité (`x:<ms>`) est poussé sur le canal EXACT souscrit → matcher
   `startsWith(base)`, pas le nom nu (consigné skill studio-dev).

## Patterns récurrents (déjà gérés)

- ✅ Skills auto-améliorés (framework-dev 1.6.0 + studio-dev 1.6.0, paire co-évolutive lockstep).
- ✅ Décisions/vision en mémoire IA (`project_realtime_nodefony_socket_vision`) → pas de perte au /clear.
- ✅ Tests dans la même session que le code (peer 14 / client 26 / controller+transport 19).
- ✅ Allowlist : pas de nouveau gap permission observé (friction = git-lock/commitlint, pas perms).

## Commits produits

| Commit    | Sujet                                                                    |
| --------- | ------------------------------------------------------------------------ |
| `3093850` | refactor(studio): retire le SSE logs/stream mort + cassé HTTP/2          |
| `6fbbcd8` | feat(realtime): actions WS req→rép + discrimination par method (2 côtés) |
| `2b939df` | feat(realtime): JsonRpcPeer — moteur protocole isomorphe                 |
| `def5fa5` | feat(realtime): isole le transport derrière une interface (Win 1)        |
| `d68d4dd` | feat(realtime): base RealtimeController serveur + transport (Win 2)      |
| `f64bbe7` | chore(skill): paire dev polymorphe co-évolutive + version commune 1.6.0  |
