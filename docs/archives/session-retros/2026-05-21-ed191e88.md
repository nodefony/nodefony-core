---
date: 2026-05-21
session_id: ed191e88-6071-46a9-a6e2-5c4b004c62d5
focus: Profiler par requête — Network panel debug bar + waterfall serveur + page Studio
---

# Session retro — 2026-05-21 — ed191e88

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Edit  |    98 |
| Bash  |    94 |
| Read  |    36 |
| Write |    11 |
| Skill |     4 |

## Top fichiers Read/Edit

| Fichier                         |   × |
| ------------------------------- | --: |
| `client/debugbar/DebugBar.ts`   |  50 |
| `src/profiler/Profiler.ts`      |   7 |
| `client/debugbar/network.ts`    |   7 |
| `studio/.../AdminLayout.tsx`    |   5 |
| `http-kernel.ts` / `profile.ts` |   4 |

## Coûts évidents

- **7× « build core + restart »** : itérations visuelles sur la debug bar (taille strip, hauteur panneau, split détail, clic). UI réactive au feedback user → inhérent, mais un **mini-cahier des charges UI en amont** (resize + onglets + responsive + close décidés d'un coup) aurait fusionné ~3-4 cycles.
- **50 edits DebugBar.ts** : réécriture complète (onglets/resize/incrémental) PUIS micro-ajustements CSS un par un. Le gros refactor était justifié ; les retouches CSS auraient pu être batchées.
- **Chasse au bug « clic mort »** longue (~10 échanges) : fausses pistes (proxy, CORS, Angular plugin) avant la vraie cause (HTTP/2 sans x-request-id). Le diag « curl headers HTTP/1.1 vs HTTP/2 » aurait dû venir **plus tôt** (comparer les 2 transports dès le 1er symptôme).

## Recommandations

1. **MEMORY http (FAIT)** — gotcha HTTP/2 `x-request-id` (chemin stream bypasse super.writeHead). Gotcha durable, vérifié.
2. **Réflexe debug headers** — symptôme « header absent côté client » → curl `-D -` sur **les 2 transports** (5151 HTTP/1.1 vs 5152 HTTP/2) en PREMIER. À ajouter au skill `nodefony-tail-error-logs` ou un nouveau réflexe.
3. **Mini-cahier des charges UI** (règle hygiène déjà connue) — pour un widget visuel, lister resize/onglets/responsive/close AVANT de coder → moins de cycles build+restart.

## Patterns récurrents (déjà gérés)

- ✅ Restarts serveur = coût #1 (cf `feedback_session_hygiene`) — confirmé encore (7×).
- ✅ Vérif front = curl + confirmation visuelle user (pas de headless) — respecté.
- ✅ Décision archi tranchée + justifiée (collecteur dans http, namespace profiler dédié, intercepteur header-only) — pas de QCM.
- ✅ Mocha cassé (session.ts TS2322 + tsconfig baseUrl TS5101) → contourné par sonde mémoire manuelle ; runner réel = vitest.

## Commits produits

| Commit    | Sujet                                                                           |
| --------- | ------------------------------------------------------------------------------- |
| `88d21c3` | feat: profiler core (serveur + Network panel + waterfall + page Studio + route) |
| `1b669e9` | feat: sync clic→Studio + UI responsive (panneau + strip)                        |
| `4dcd262` | docs: profiler dans les MEMORY.md (http/core/frontend/studio)                   |
