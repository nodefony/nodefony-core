---
date: 2026-05-21
session_id: cad11fa3-1663-496f-ad60-20a4e9746fc0
focus: P5.6 UserService+BcryptEncoder, AbstractCrudService générique + rétro-fit, ServiceWiring, coverage vitest P5, décisions GraphQL/CLI scaffold
---

# Session retro — 2026-05-21 — cad11fa3

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Edit            |    61 |
| Bash            |    54 |
| Read            |    25 |
| Write           |    17 |
| Skill           |     2 |
| AskUserQuestion |     2 |

## Top fichiers Read/Edit

| Fichier                                  |   × |
| ---------------------------------------- | --: |
| user/MEMORY.md                           |   9 |
| user/package.json                        |   5 |
| user/UserService.ts                      |   4 |
| user/CLAUDE.md                           |   4 |
| MIGRATION_STATUS.md                      |   4 |
| mémoires IA (crud/coverage/MEMORY index) | ~11 |

## Coûts évidents

- **Détour monocart** : lancé un coverage ad-hoc monocart+tsx (3-4 tool calls : 2 configs /tmp + 2 runs) AVANT que le user signale « on utilise vitest ». La convention vitest était déjà dans framework/http (`vitest.config.ts`) + dans la section du skill create-module. → vérifier la convention des modules frères AVANT de sortir un outil ad-hoc.
- **Doc-sync répété** : chaque feature livrée = MAJ MEMORY.md + CLAUDE.md + MIGRATION_STATUS + README module (+ mémoires IA). 61 Edits, dont ~30 de doc. Coût structurel d'une session multi-feature.
- **Renommage events au rétro-fit** : UserService a changé d'events (onUser\* → onCreated/onUpdated…) → re-MAJ des 3 docs + tests. Aurait pu être anticipé en figeant la convention d'events AVANT d'écrire P5.6.

## Recommandations

1. **Réflexe convention-frères** : avant tout outil ad-hoc (coverage, bench, lint), `grep`/`ls` un module déjà équipé (framework/http) pour copier la recette. Évite le détour monocart. → candidate règle CLAUDE.
2. **Bug template skill détecté** : `nodefony-create-module` package.json émettait mocha+monocart alors que sa section disait vitest → dérive de template. Corrigé + figé [[project_cli_scaffold_design]] (source unique CLI↔skill). Pattern à surveiller sur tout skill avec templates dupliqués.
3. **Coverage = vitest partout** (sauf core monocart) : mémoire [[feedback_coverage_modules]] MAJ. Ne plus jamais tenter monocart+tsx sur un module.

## Patterns récurrents (déjà gérés)

- ✅ Tenir MIGRATION_STATUS à jour en continu → audit P5 = **0 écart** fichier/code.
- ✅ Décisions archi tranchées + figées en mémoire (CRUD pattern, GraphQL façade, CLI scaffold, perf DI) sans QCM.
- ✅ peerDep optionnelle pour dep externe (bcrypt, graphql) au lieu d'un module.
- ✅ Commit FR via `git commit -F` (apostrophes).

## Commits produits

| Commit  | Sujet                                                              |
| ------- | ------------------------------------------------------------------ |
| a9bea9e | feat(user): UserService + BcryptEncoder (P5.6)                     |
| 70a1896 | feat(orm-core): AbstractCrudService générique (P5.3b)              |
| abe98a3 | refactor(user): UserService extends AbstractCrudService            |
| ab2f71d | refactor(orm-core): ServiceWiring tuple (fin tunneling ctor)       |
| 85b9d21 | test(orm-core,user): vitest coverage + probe bcrypt + fix scaffold |
