---
date: 2026-05-24
session_id: e1a0f718-f954-4210-8158-66f56eb30587
focus: Cadence adaptative AIMD (lib Core) + intégration Studio + ergonomie « temps réel calme »
---

# Session retro — 2026-05-24 — e1a0f718

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |   117 |
| Edit            |   109 |
| Read            |    46 |
| Write           |     7 |
| Skill           |     5 |
| WebFetch        |     3 |
| AskUserQuestion |     1 |

## Top fichiers Edit/Write

| Fichier                                                                     |   × |
| --------------------------------------------------------------------------- | --: |
| routes/OrmOverview.tsx                                                      |  33 |
| routes/DashboardSupervision.tsx                                             |  15 |
| components/RealtimeHubContent.tsx                                           |  13 |
| skills/nodefony-studio-dev/SKILL.md                                         |   6 |
| stores/UiStore.ts · client/realtime/AdaptiveRate.ts · client/react/index.ts |   5 |

## Coûts évidents

- **Placement du switch « Cadence auto » tâtonné** (le coût #1) : ORM popover → barre d'outils
  ORM → retiré → Hub (console) → Hub chip popover (RealtimeHubContent) → badge ORM re-ajouté.
  ~3-4 allers-retours. Cause : j'ai deviné le « où » au lieu de saisir le modèle mental user
  (la cadence est une affaire de **socket** → vit dans le **Hub**, pas sur une page métier).
  Résolu net par UN `AskUserQuestion` (option « Hub on/off global » choisie) — à faire AVANT.
- **OrmOverview 33 éditions** : feature flux + adaptatif + badge + nettoyage imports, en petites
  passes. Un mini-cahier des charges en amont (où vit le contrôle, quelles briques) aurait
  fusionné des passes.
- **2 restarts serveur** (nouvel export `nodefony/react` → Vite optimizeDeps) ; 1 cycle de
  confusion « switch popover invisible » = serveur pas redémarré (export neuf non chargé).
- Démo AIMD navigateur **non vérifiable en headless** → j'ai dû créer une démo CLI (bénéfice net).

## Recommandations

1. **AskUserQuestion AVANT de placer un contrôle UI dont l'emplacement est ambigu** (surtout
   realtime/global) — le user a un modèle mental fort (socket=Hub). Une question = 0 churn vs 3 passes.
2. **Après ajout d'un export Core consommé par Studio → restart serveur AVANT de demander au user
   de regarder** (Vite optimizeDeps ne voit pas l'export neuf à chaud). Déjà au skill studio-dev,
   à appliquer réflexe.
3. **Pour une mécanique client-driven dure à observer (AIMD) → fournir une démo CLI déterministe**
   (mock + horloge) qui IMPRIME le comportement. Fait : `run.sh aimd` (skill load-test).

## Patterns récurrents (déjà gérés)

- ✅ Granularité 1ʳᵉ classe + AIMD = patron sondes+hub appliqué (mémoires à jour).
- ✅ Ergonomie « temps réel calme » CODIFIÉE (skill studio-dev v1.8.0 : section + passe ergonomie + retex).
- ✅ Vérif front = curl transform Vite + confirmation user (jamais headless) — appliqué.
- ✅ Briques partagées (PageHeader/FlashValue/KpiCard) corrigées une fois → bénéfice tous dashboards.
- ⚠️ Dette : ORM garde des COPIES LOCALES (`KpiCard` + `ensureLivePulseStyle` non-calme) → dédup à venir.

## Commits produits

| Commit  | Sujet                                                                             |
| ------- | --------------------------------------------------------------------------------- |
| dbf98c8 | feat(realtime): granularité 1ʳᵉ classe — convention cadence isomorphe             |
| fd28e2c | feat(realtime): cadence adaptative AIMD client-driven + studio + temps réel calme |
| c89966e | test(load): démo AIMD lisible dans le skill de charge (run.sh aimd)               |
