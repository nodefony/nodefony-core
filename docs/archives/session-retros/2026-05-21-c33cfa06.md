---
date: 2026-05-21
session_id: c33cfa06-a2c6-419f-8f5c-4340cd806c3e
focus: P7.4 Drizzle (adapter + opérateurs riches) → module bootable → session sur Drizzle (IoC) → fix Kernel maxListeners → docs
---

# Session retro — 2026-05-21 — c33cfa06

## Tool usage

| Outil           |                               Calls |
| --------------- | ----------------------------------: |
| Bash            |                                  90 |
| Edit            |                                  74 |
| Read            |                                  48 |
| Write           |                                  30 |
| Skill           |    2 (start-nodefony-server, retex) |
| AskUserQuestion | 2 (driver test, périmètre ORM boot) |

## Top fichiers Read/Edit

| Fichier                                        |                 × |
| ---------------------------------------------- | ----------------: |
| http/.../sessions-service.ts                   | 9 edits / 4 reads |
| drizzle/package.json                           |           7 edits |
| MIGRATION_STATUS.md                            |           6 edits |
| mongoose+sequelize SessionStorage / Repository |        3-4 chacun |
| orm-core CLAUDE/README                         |          3 chacun |

## Coûts évidents

- **Cache turbo → dist sequelize périmé** : après retrait de `@entities([Session])`, `npm run build` (turbo) a resservi l'ancien dist → le boot montrait encore `ENTITY ADD : session` → **confusion user** ("c'est encore sequelize"). Résolu par rebuild DIRECT du module. Piège déjà connu (`feedback_turbo_cache_stale`) mais re-rencontré.
- **~6 cycles stop/start serveur** pour vérifs runtime (drizzle boot, maxListeners, session, sequelize entity). Quelques-uns fusionnables, mais la plupart = vrais points de mesure.
- **Refactor IoC multi-module** : retirer les imports ORM de http a transformé des imports type-only en imports valeur côté ORM → build cassé (cli-color/CJS) → fix externals rollup (sequelize+mongoose). Itération en 2 temps.

## Recommandations

1. **Renforcer le réflexe rebuild-direct** : toute modif `index.ts`/décorateur d'un module dont on veut VÉRIFIER LE RUNTIME → `cd <module> && npm run build` AVANT le boot, jamais `npm run build` racine seul. (Coûte 1 confusion user cette session.)
2. **Sonde rollup type-only→valeur** : ajouter une méthode statique appelée sur un import jusque-là type-only ⇒ penser à externaliser le package dans le rollup.config du module appelant (sinon bundling CJS casse). À garder en tête pour le pattern self-register.
3. Mémoires créées OK : `project_orm_default_positioning`, `project_studio_orm_panel` (+ how-to log SQL), kit P7.4 marqué FAIT. Rien à rajouter.

## Patterns récurrents (déjà gérés)

- ✅ `cd` chemins absolus (cwd persiste) — respecté.
- ✅ commit `-F` heredoc (apostrophes FR) — respecté.
- ✅ memory.test après modif Kernel (8/8) — respecté.
- ✅ AskUserQuestion réservé aux installs/périmètre (better-sqlite3, ORM boot) — respecté.
- ✅ better-sqlite3 natif OK Node 26 (prebuild 12.x) — consigné dans drizzle MEMORY.

## Commits produits

| Commit  | Sujet                                                         |
| ------- | ------------------------------------------------------------- |
| 6e00e44 | P7.4 adapter Drizzle + opérateurs riches (risque #3 ADR-0003) |
| dadb308 | README orm-core — narratif ORM par défaut                     |
| c41a67a | Drizzle module bootable (ORM SQL par défaut)                  |
| 0434241 | fix core Kernel maxListeners 30→config(60)                    |
| 2b52a6d | Session sur Drizzle + mécanisme IoC (registre + events)       |
| e50539a | retire entité session legacy sequelize                        |
| 4e9ff9d | docs/ orm-core + drizzle                                      |
