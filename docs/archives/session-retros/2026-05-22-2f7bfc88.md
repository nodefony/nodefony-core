---
date: 2026-05-22
session_id: 2f7bfc88-cacd-43f9-94ab-d3aa3df38dd2
focus: Studio dashboards par rôle + Realtime Hub (console + log protocole) — session "flow" volontaire (matériau retex)
---

# Session retro — 2026-05-22 — 2f7bfc88

> Session **flow** assumée par le user : demandes en rafale pour apprendre à coder
> Studio en condition réelle + nourrir le skill. ~12 commits, 1 module (Studio) + Core.

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Edit            |   125 |
| Bash            |   108 |
| Read            |    54 |
| Write           |    18 |
| Skill           |     4 |
| AskUserQuestion |     4 |

## Top fichiers Edit

| Fichier                           |   × |
| --------------------------------- | --: |
| DashboardSupervision.tsx          |  14 |
| RealtimeConsole.tsx               |  12 |
| Dashboard.tsx                     |  11 |
| AdminLayout.tsx                   |  11 |
| RealtimeClient.ts (core)          |   9 |
| StudioController.ts / DebugBar.ts |   8 |
| ModuleDetail.tsx / App.tsx        |   7 |

## Coûts évidents

- **Cycles build core + restart répétés** (realtime/debugbar = client Core servi par Vite /@fs)
  : chaque itération hub/socket/frame-log → `cd src/nodefony && npm run build` + `start.sh`.
- **Itérations UI sur le même fichier** (supervision onglets, hub, CSS stable, perf mount) :
  inhérent au mode flow — le design émerge par petites passes.
- **Course `git index.lock`** (husky pre-commit + `git add`) → ~3 commits ont dû `rm -f
.git/index.lock` puis retry. → wildcard allowlist ajouté ce retex.

## Recommandations

1. **Skill `nodefony-studio-dev` enrichi** (FAIT) — section back-end Studio + retex realtime
   (hub patron, socket partagée `RealtimeClient.shared`, frame-log lazy/always-on, dashboards
   par rôle, perf data plane git-per-doc, ConfigView, debug bar height). Le matériau flow y est.
2. **Pattern realtime « 1 socket partagée »** = piège à connaître : 2 mécanismes d'abonnement
   sur un client partagé (ref-compté vs raw emit) se coupent → tout ref-compter. Capturé skill.
3. **`rm -f .git/index.lock`** ajouté à l'allowlist (course husky récurrente).

## Patterns récurrents (déjà gérés)

- ✅ Mesurer un endpoint lent au `curl -w time_total` (page module 80×) — dans le skill.
- ✅ Skeleton loader > spinner ; tabular-nums+nowrap pour boxes stables — skill.
- ✅ HMR (front) vs rebuild+restart (back/core) bien distingués — skill back-end.

## Commits produits (12)

| Commit  | Sujet                                                           |
| ------- | --------------------------------------------------------------- |
| ac06ed0 | dashboards par rôle + GitService core + supervision onglets     |
| b96c673 | page module ~80× + overview + skeleton                          |
| 440d62b | socket WS partagée + logo login                                 |
| f211022 | console Realtime + frame-log                                    |
| 72c37be | ConfigView (config lisible)                                     |
| d8af0b7 | abonnements visibles (barre ref-comptée + console auto-abonnée) |
| 881ba14 | log protocole always-on + protocole par abonnement              |
| a7c742e | hub realtime en HoverCard (aperçu par page)                     |
| e3dce97 | hub taille stable (tabular-nums + nowrap)                       |
| fd5c968 | console montage/démontage fluides                               |
| 58a7d68 | renommage « Realtime Hub » (/nodefony/hub)                      |
| 5e630e0 | accueil lié au rôle + ordre nav                                 |
