---
date: 2026-05-21
session_id: 17eb6855-3c92-4bfd-890d-bb10b617db89
focus: P5.5a + P5.5 — création @nodefony/user (workspace + contrats + base users)
---

# Session retro — 2026-05-21 — 17eb6855

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Write |    19 |
| Read  |    17 |
| Bash  |    13 |
| Edit  |     9 |
| Skill |     2 |

## Top fichiers Read/Edit

| Fichier                                                  |     × |
| -------------------------------------------------------- | ----: |
| MIGRATION_STATUS.md (Read)                               |     2 |
| user/MEMORY.md (Edit)                                    |     3 |
| user/CLAUDE.md (Edit)                                    |     3 |
| orm-core/{tsconfig,rollup,package,IRepository...} (Read) | 1 ch. |

## Coûts évidents

- **Aucun gros coût** : 0 restart serveur, 0 re-build en boucle, 0 re-lecture répétée.
- 2 builds rollup (user) + 1 build orm-core (vérif warnings) — justifiés.
- 1 itération test (test "objet gelé" trop strict) → corrigé en 1 passe.

## Recommandations

1. **RAS skill** — aucun pattern répété ≥ 3×. Le scaffold lib-contrats (calque orm-core) est déjà couvert par `nodefony-create-module`.
2. **Mémoriser le split credential** (`IPasswordAuthenticatedUser`) — décision archi de symbiose user↔security, à ne pas reperdre au `/clear`. → fait dans le state + CLAUDE.md module.
3. **Gotcha test "objet gelé"** — assigner sur frozen ne throw qu'en mode strict ; tester la valeur inchangée. → consigné dans user/MEMORY.md.

## Patterns récurrents (déjà gérés)

- ✅ Calque sur module de référence (orm-core) avant de scaffolder — pattern lib-contrats.
- ✅ Tests dans la même session que le code (`feedback_tests`).
- ✅ Suspecter son diff avant « pré-existant » : warnings http vérifiés reproductibles sur orm-core seul → artefact monorepo confirmé.
- ✅ Décision design tranchée + justifiée (split credential, IRole/IPermission différés) sans QCM (`feedback_session_hygiene`).
- ✅ Commit FR via heredoc `git commit -F` (`feedback_commit_fr_apostrophes`).

## Commits produits

| Commit  | Sujet                                                                               |
| ------- | ----------------------------------------------------------------------------------- |
| 3911004 | feat(user): scaffold workspace @nodefony/user (P5.5a)                               |
| 25398e2 | feat(user): contracts IUser + BaseUser/AnonymousUser + encoder/provider/repo (P5.5) |
