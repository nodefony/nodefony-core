---
date: 2026-06-01
session_id: 690029d6-7ba2-44ed-ac1d-44a4e991b682
focus: fix doublon JSONL (double initializeLog) + fusion Profiler → Suivi de requête
---

# Session retro — 2026-06-01 (RESUME) — 690029d6

## Focus

Reprise post-`/clear`. Deux livrables : (1) fix du **doublon JSONL** (bug item 1 du `_state`),
(2) **fusion du Profiler autonome dans le Suivi de requête** + onglet Profiling de la page Logs (item 2bis).

## Fait

1. **`6814c05` fix(core)** — `initializeLog()` appelé 2× au boot re-montait un 2ᵉ `FileTransport`
   (dédup `addTransport` par référence inopérante) → chaque log écrit 2× dans le JSONL (constaté live :
   1335 l / 669 uid). Fix = `Kernel._mountedLogTransports` (lazy) retiré en tête d'`initializeLog` avant
   remontage + getter `Syslog.transportCount` + test d'idempotence des transports. Validé : 323/323 (0 doublon).
   memory.test HTTP+WS 9/9 (serveur frais), core 1517 passing. **Poussé.**
2. **`1d4ed01` feat(studio)** — fusion Profiler → Suivi de requête : TraceView gagne fetch profil best-effort
   - onglet **Timing** (`computeWaterfall(phases)`) + onglet **ORM** refondu (vraies SQL profiler, fallback logs)
   - Accueil enrichi ; page Logs gagne onglet **Profiling** (`ProfilingTab`, liste `recent` → clic = Suivi) ;
     briques partagées extraites dans `routes/logs/profileVisuals.tsx` ; page Profiler autonome **supprimée**
     (route + nav) ; deep-link debug bar → Suivi. Front-only, pas de bump lockstep. typecheck 0, Vite 200. **Poussé.**
3. **`docs(migration)`** — MAJ ligne P3.11 (fix doublon + fusion Profiler).

## Frictions du jour (détail dans RETEX.md)

- **Le `_state` se trompait sur la cause du bug** : il disait « switch `setActiveLogDriver` re-tap » ;
  vraie cause = double `initializeLog`. La note RETEX de la session passée portait la même hypothèse fausse →
  **corrigée**. Leçon : re-prouver une cause (code + empirique) au RESUME, ne pas copier le diagnostic du `_state`.
- **memory.test contaminé par serveur dev pas frais** : 2 fails (44.8/35, 10.7/10) → disparaissent sur serveur
  fraîchement booté (9/9). Isolation = vérité (déjà gradué, skill `nodefony-debug`).
- **`routes/logs/` gitignoré** : nouveaux fichiers → `git add -f` ; 1er commit « lint-staged git error » →
  retry après `pkill lint-staged` + `rm .git/index.lock` OK.

## Coûts évidents

- 2 restarts serveur (fix core → rebuild+restart ; memory.test isolation). Front-only = HMR, 0 restart.
- Le bug doublon : ~6 commandes Bash de diagnostic empirique (jq sur JSONL vivant) — bien investi (vérité terrain).

## Commits produits

| Commit  | Sujet                                                                                 |
| ------- | ------------------------------------------------------------------------------------- |
| 6814c05 | fix(core): initializeLog idempotent sur les transports — fin du doublon JSONL         |
| 1d4ed01 | feat(studio): fusion du Profiler dans le Suivi de requête + onglet Profiling des Logs |
