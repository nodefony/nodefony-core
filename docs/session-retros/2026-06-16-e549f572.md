---
date: 2026-06-16
session_id: e549f572-ed2a-4272-9e8a-0f2b749f3688
focus: J5 cœur P6 — CSRF + CORS + en-têtes de sécurité + DX complétion config
---

# Session retro — 2026-06-16 — e549f572

## Fait (cœur P6 BOUCLÉ — J0→J9 + J5)

- `ebe7e915` **CSRF** : `Csrf` Fetch Metadata (`Sec-Fetch-Site`) + repli Origin/Referer, flag
  `strictSameSite`, liste `trustedOrigins` (alias multi-domaine ≠ cors.origins). `enforceCsrf` global.
  Banc live 11/11.
- `0724a8c6` **CORS** : `Cors` Fetch Standard (preflight 204 + reflet origine + Vary, `*`+creds
  interdit boot). `handleCors` câblé EN TÊTE de `handleHttp` (avant routing). Banc live 6/6.
- `f4f9ead8` **En-têtes + DX** : `SecurityHeaders` applicatif (CSP/Referrer/COOP/COEP/CORP/Permissions),
  séparation transport(http)/applicatif(security). `referrerPolicy`→enum + `declare module
NodefonyModuleConfig` (complétion clés+valeurs). Banc live 7/7.
- Gates : security unit 303, http intég 502, mémoire 9/9, 0 régression, security-review 0 blocker.

## Frictions (→ RETEX.md)

- **`npm run build` en BACKGROUND pendant serveur live → 404 fantômes** (DevSupervisor restart sur dist
  à demi-écrits). Re-violation du CLAUDE.md, masquée par `run_in_background`. Coût : panique 404 user +
  1 restart de récupération + faux négatif non-régression.
- **CORS preflight : câblé au mauvais étage** (handleFrontController → 405 au pré-match avant). Tranché
  par **log temporaire** (présent GET / absent OPTIONS). Déplacé en tête de handleHttp.
- **« source unique » freinée par le user** → vérif terrain : http pose le socle à `onHttpRequest`
  (couvre statics/erreurs) → séparation transport/applicatif, pas unification.

## Coûts évidents

- ~4 cycles build+restart (dont 1 de récupération 404). Le 1 évitable = le build background concurrent.
- Bancs live RÉELS = la bonne dépense : ont attrapé le 405 preflight et le mismatch Host:port (CSRF)
  que les unit ne voyaient pas.

## Recommandations

1. Le rebuild concurrent serveur live est gradué 3×+ → candidat **graduation `feedback_*`** au CONSOLIDATE.
2. DX `NodefonyModuleConfig` : security l'avait oublié (CLAUDE.md le dit OBLIGATOIRE) → audit des autres
   modules (`http`/`framework`/`realtime`) au passage.

## Commits produits

| Commit   | Sujet                                                          |
| -------- | -------------------------------------------------------------- |
| ebe7e915 | feat(security): CSRF Fetch Metadata + repli Origin             |
| 0724a8c6 | feat(security): CORS native preflight + reflet origine         |
| f4f9ead8 | feat(security): en-têtes applicatifs + complétion config typée |
