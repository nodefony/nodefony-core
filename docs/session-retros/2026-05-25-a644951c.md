---
date: 2026-05-25
session_id: a644951c-e73c-40b0-95d2-ca62e957172e
focus: P16.H.7 — santé pod ORM + erreurs par worker (back + front + skills)
---

# Session retro — 2026-05-25 — a644951c

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    54 |
| Edit            |    37 |
| Read            |    33 |
| Write           |     7 |
| Skill           |     5 |
| AskUserQuestion |     1 |

## Coûts évidents

- Restarts serveur : 3 (dev→cluster→dev) — justifiés (preuve cluster réel puis démo UI dev). Pas de gaspillage.
- 1 `git stash` risqué pour prouver une erreur pré-existante → `.git/index.lock` orphelin a fait échouer le
  pop (edits coincés dans le stash). ~3 calls perdus à diagnostiquer/récupérer. **Garde-fou husky #I à poser.**
- 5 Skill (session/framework-dev/check-memory-health/start-server/studio-dev) — tous pertinents (feature full-stack).

## 💶 Coût (€)

- Total ~**€84.7** (≈ $92.1) sur 142 tours — **cache-dominé ~88 %** (cache-read 35,6M × 1.5 ≈ $53 +
  cache-write 1h 966k × 30 ≈ $29) ; output 120k × 75 ≈ $9 ; input 44k ≈ $0.7. Le coût = le CONTEXTE porté
  (3 gros skills framework-dev/studio-dev + CLAUDE.md + fichiers relus), pas la production. Feature full-stack
  + 3 commits livrés → ratio sain. Levier : `/clear` entre back et front aurait allégé le cache (les 2 gros
  skills coexistaient).

## Recommandations

1. **Garde-fou `index.lock` (husky)** — vécu 2× cette session ; le + rentable ([[project_retex_improvements_kit]] #I).
2. **`/clear` entre couches back↔front** d'une même feature — les 2 kits (framework-dev + studio-dev) chargés
   ensemble = cache lourd. Couper aurait réduit le coût.
3. **Prouver une erreur pré-existante sans `git stash`** (lock/hook husky) → `git worktree` ou `tsc` sur le commit parent.

## Patterns récurrents (déjà gérés)

- ✅ Généraliser un agrégat opaque = enrichir le colis (pas l'agrégateur) — capturé skill framework-dev §11.
- ✅ Seam core pour dép propre (framework ⊥ orm-core) — capturé.
- ✅ Sonde additive front = champs optionnels + rendu conditionnel + `normalize` propage — capturé studio-dev §11.
- ✅ Erreur pré-existante (SessionStorage drizzle TS2345) prouvée et non corrigée (hors scope).
- ✅ Mémoire 8/8 avant commit pipeline core (Syslog hot path).

## Commits produits

| Commit    | Sujet                                                            |
| --------- | --------------------------------------------------------------- |
| `aa9b6fc` | feat(cluster): santé pod ORM + erreurs par worker (backend)     |
| `7ab9219` | feat(studio): cartes ORM + erreurs par worker (page Cluster)    |
| `bf30ebb` | chore(skills): framework-dev + studio-dev 1.12.0 (lockstep)     |

## Prochaine étape

Dashboard ORM cluster-aware + verdict « Santé ORM » 3 états (« comme cluster », GO user) →
kit [[project_orm_dashboard_cluster_kit]].
