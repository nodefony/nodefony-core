---
date: 2026-06-27
session_id: 7cca116c-f44f-4903-8d02-6b9b91d293b1
focus: Webhooks P6.13 Slice C #1 data plane + #2 stores ORM (drizzle/mongoose) + e2e live + push
---

# Session retro — 2026-06-27 (4ᵉ) — Webhooks Slice C #1 + #2

## Focus

Reprise Slice C webhooks. Data plane admin (#1) + stores ORM drizzle/mongoose + wiring (#2),
prouvés par e2e live + suite complète. 5 commits poussés.

## Fait (5 commits POUSSÉS `8fea78ea`→`a9305e64`)

- **#1 data plane** `8fea78ea` : `WebhookAdminApi.ts` (fichier dédié) composé dans le broker
  `SecurityAdminApi` → 7 endpoints `/nodefony/security/api/webhooks` (RBAC `ROLE_NODEFONY_ADMIN`
  - audit + duplex gratuits). `adminAudit.ts` extrait (anti-cycle). `NF_WEBHOOK_KEY` câblé.
- **#2 stores** : `DrizzleWebhookStore`+entité `e8823e6d` · `MongooseWebhookStore`+schéma `5b494f1a`
  · wiring app `NF_WEBHOOK_STORE=drizzle` opt-in fail-loud `bb803934`.
- **banc e2e** `a9305e64` : `webhooks-dataplane-e2e.mjs` config-agnostic + self-clean.

## Vérification

- Unit : security 759 · drizzle 95 (sqlite réel 10) · mongoose 79 (Mongo réel 9).
- **e2e live 25/25** avec `NF_WEBHOOK_STORE=drizzle` : 3 strates RBAC + CRUD + SSRF + **durabilité
  prouvée** (ligne SQL `webhook_endpoint`, `secretEnc=gcm1.…` chiffré at-rest).
- Régression 68/68 (dont `users-admin-factors` 13/13 → `SecurityAdminApi.ts` édité non régressé).
- **Suite complète : 35/35 turbo, ~4100 tests, 0 échec.**

## Frictions / leçons (→ RETEX.md)

1. **e2e live ≠ unit** : 2 défauts trouvés en live (dans le banc) — driver `orm` pas `drizzle`,
   hôte de test non résolvable rejeté par SSRF. Banc committé = config-agnostic + self-clean.
2. **Admin = producteur broker, pas un controller** : kit disait « calque ApiKeyController » mais
   celui-ci est par-user sans garde ; le broker donne RBAC fail-closed gratuit. Fichier dédié OK.
3. **Restart serveur = impactant** (déconnexion Studio) → demander avant. Backup `.env.local`.

## Décisions

- driver "orm" = backend SQL/document (classe concrète dans `store`) — calque apikeys.
- sqliteTable DUR (comme user/token/session/webauthn) → +1 au backlog multi-dialecte post-P6.

## Commits

8fea78ea (data plane) · e8823e6d (drizzle) · 5b494f1a (mongoose) · bb803934 (wiring) · a9305e64 (e2e)

## ➡️ Next

Slice C #3 page Studio — ⚠️ **EN 1ER : fix coverage `@nodefony/security` cassé dans Studio** (bug user).
