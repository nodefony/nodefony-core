---
date: 2026-09-05
session_id: bfdb356c-1226-4f19-97a9-3b12f10b48c9
focus: Trous de couverture du temps réel soldés, typecheck du cœur inversé, CI réparée (2 causes), pilotes de base symétriques
---

# Session retro — 2026-09-05 — bfdb356c

## Focus

Reprise sur #202/#203 (enfants de #197), puis la CI rouge signalée par le user,
puis #201, puis une passe de pilotage, puis #19.

## Fait

- **#202** — la preuve du ticket était fausse (le banc existait, atteint via
  `RedisBackplane`). Trois trous réels comblés : ordre du jeté prouvé par
  contenu, **hystérésis** que rien n'éprouvait, et le silence de `publish`
  énoncé comme décision. L'instrument de couverture, qui avait fabriqué ce
  ticket, mesure désormais l'atteinte indirecte.
- **#203** — `RealtimeError` retirée : exportée, annoncée par le README, levée
  par zéro ligne. `errorSurface.test.ts` porte la règle, avec auto-contrôle.
- **#201** — strictitude du typecheck du cœur **inversée** : le geste spontané
  couvre les tests, la restriction porte un nom (`tsconfig.src.json`).
- **#197** — parent fermé, ses 5 enfants clos.
- **#204** — décor des bancs ORM rendu réentrant (ouvert et fermé dans la foulée).
- **CI** — deux causes empilées : clé d'exception `nodefony.check` vs
  `nodefony.doctor`, puis `doctor` sortant en 1 sur « RIEN À SIGNALER ».
- **#19** (non fermé) — les trois pilotes de base symétriques, en pair
  optionnelle + import dynamique ; le scaffold pose celui du dialecte choisi.
- **Pilotage** — 186 items du tableau repositionnés (actifs par jalon puis
  ordre, 120 terminés en bas) ; #187 et #175 redescendus de « In Progress ».

## Coûts évidents

- **Trois instruments ont accusé le produit à leur place** : le relevé de
  couverture (2 faux → 2 tickets), le banc devkit (clé jamais lue), le smoke
  (secrets non posés → « migrations non appliquées »).
- **J'ai pollué le run du user** en lançant le même banc drizzle pendant le
  sien, puis en le tuant : 3 rouges présentés comme les siens. Décor partagé.
- **Un aller-retour complet sur Vite** : fait le geste que le ticket prescrit,
  puis annulé en lisant le code qui portait la décision inverse, mesurée.
- Un `replace` qui a dupliqué sa ligne d'ancre → fichier de test non parsable.

## Recommandations

1. **Avant de suivre un ticket, chercher qui porte déjà la décision** — le cas
   Vite a coûté un aller-retour que trois lignes de commentaire évitaient.
2. **Demander si une suite tourne avant d'en lancer une** — les décors sont
   partagés, et la sortie ne le dit pas.
3. **Un débranchement large masque ce qu'il ne couvre pas** : débrancher la
   garde PRÉCISE que le cas vise.

## Patterns récurrents (déjà gérés)

- ✅ Voir rouge chaque test neuf, par un débranchement ciblé.
- ✅ Prouver sur l'artefact reçu (tarball, `dist` bâti, image Docker).
- ✅ Nommer ce qui n'a pas été lancé.

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `2da4322d` | test(realtime): ordre, hystérésis et silence de la file bornée          |
| `db48bf7f` | refactor(realtime)!: retirer RealtimeError                              |
| `1e177e21` | fix(devkit-bench): exception de dialecte sous la clé que le produit LIT |
| `2579ba42` | build(core)!: le typecheck spontané couvre les tests                    |
| `12c96f8a` | fix(doctor): un contrôle NON DEMANDÉ ne condamne plus `--deep`          |
| `77c6a5f2` | test(orm): décor partagé des bancs de migration rendu réentrant         |
| `149a729d` | refactor(drizzle)!: trois pilotes symétriques et optionnels             |
