---
date: 2026-07-24
session_id: 5eebe48d-2fbd-4b58-a779-3822f66f7c97
focus: Bancs (opt-in + cluster) puis lot C du registre soldé — F18 WebAuthn, F40 ordre du pipeline, F28 webhooks multi-pod
---

# Session retro — 2026-07-24 — 5eebe48d

## Fait

- **(a) 4 bancs opt-in** repassés, chacun sur son propre serveur : ratelimit, handshake WS,
  cap de connexions, webhooks. Décor entièrement posé **par variables d'env** — dont
  `NF__SECURITY__WEBHOOKS__DENYPRIVATEIPS=true`, qui remplace une édition de `nodefony.config.ts`
  qu'il fallait penser à reverter.
- **(b) 5 bancs cluster** réparés : ils importaient des symboles passés de `@nodefony/framework`
  à `@nodefony/realtime`. Repère du skill retrouvé (~300k pub/s @256B → mesuré 343 800).
- **F18** — `webauthn login/options` ne cible plus les credentials depuis le corps de la requête.
- **F40 🔴 + F64** — le controller n'est plus instancié avant l'autorisation (HTTP).
- **F28** — le cache d'endpoints webhooks a une fraîcheur bornée : un pod voit enfin ce que les
  autres créent. **Lot C du registre SOLDÉ.**

## Coûts évidents

- **12 lancements du banc A/B** (3 paires × 2 cibles + vérifs) : chacun = rebuild + boot + 3× wrk.
  Le premier jeu a été fait sur la MAUVAISE cible (`livez`) et a dû être refait sur la route dédiée.
- **6 redémarrages serveur** liés au décor opt-in (un serveur par configuration) — incompressible,
  c'est la définition du décor, mais ça se planifie (grouper les bancs par décor).
- Beaucoup de lectures ciblées du pipeline (http-kernel/Resolver) avant de toucher quoi que ce soit :
  c'est ce qui a permis de trouver que le chemin paresseux d'instanciation existait DÉJÀ.

## Recommandations

1. **Le protocole du banc A/B doit commencer par « quelle est la cible dédiée ? »** — corrigé dans
   le skill `nodefony-load-test` (cible figée + flag posé par le script), à ne plus re-perdre.
2. **Gate doc dans le même geste que le diff de code** : `anchor-check` après toute modif d'un
   fichier ancré, sinon la page ment silencieusement (16 ancres fausses ici).
3. **Un commentaire qui promet une garantie de sécurité mérite son test externe** — le motif est
   assez fort pour mériter une graduation si on le revoit (cf RETEX § garantie locale).

## Patterns récurrents (déjà gérés)

- ✅ Contrôle négatif systématique avant de déclarer un gate opérant (3 fois cette session :
  tests unitaires F18 rouges avant fix, mouchard F40 rouge avant fix, garde F28 retirée → 3 rouges).
- ✅ Verdict de perf honnête : « dans le bruit » écrit tel quel plutôt qu'un gain revendiqué.
- ✅ Décor de banc e2e (RÈGLE N°2 du skill) — appliquée d'emblée, zéro faux « KO » cette fois.

## Commits produits

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `b76fec1e` | bancs cluster rebranchés sur `@nodefony/realtime` + décor opt-in par env |
| `8bebe3f7` | webauthn `login/options` — plus de ciblage depuis la requête (F18)       |
| `9ce69934` | pipeline — instanciation après l'autorisation (F40 🔴 + F64)             |
| `403b6162` | doc des deux sondes du module test                                       |
| `939541a3` | webhooks — fraîcheur bornée du cache d'endpoints (F28)                   |
