---
date: 2026-06-11
session_id: 02ad9225-62e0-44f2-a096-0bee90446c02
focus: étape 4 index de routes (+15,3 % RPS net) + lot RFC Allow 405 agrégé — chantier fast path CLOS, backlog routing vide
---

# Session retro — 2026-06-11 — 02ad9225 (5e du jour)

## Fait

- **Index de routes** (`c533efa`) : partition littérales (Map lowercase) / dynamiques,
  merge ordonné par position dans `resolve()` — jamais de court-circuit du match.
  A/B mono prod : **+15,3 %** RPS (pos 134/222, 2 paires, séparation totale),
  **+24,9 %** (littérale pos 151). Coût résolution O(dynamiques) au lieu de O(N).
- **Lot RFC Allow 405** (`bc88444`) : pass 2 = chemin unique du 405 HTTP (agrégat
  host-aware) + hostname AVANT methods dans `Route.match` (anti-fuite cross-vhost,
  la D3 du banc passait par chance d'ordre) + pseudo-méthode WEBSOCKET exposée sur
  path duplex (décision figée). Banc 25 → 28 invariants.
- Docs : `0f62320` + `bc84050` (MIGRATION_STATUS), MEMORY.md framework ×2, kit fastpath.

## Frictions

- **ENOSPC fantôme harness** sur capture stdout des grep (3 To libres réels) →
  contournement : redirection fichier + `Read`. → RETEX.md.
- **cd persistant** a re-cassé 2× le chemin relatif de `bench-ab-mono.sh` — friction
  DÉJÀ graduée (`feedback_cd_startsh_relative_path`), re-frappée quand même : penser
  `cd <racine> &&` systématique avant tout script skill.
- Profil mono-route trompeur (« scan 0,9 % ») → leçon position-dépendance au RETEX.md.

## Patterns récurrents (déjà gérés)

- ✅ A/B structurel via stash+rebuild par flip (RETEX 2026-06-11, appliqué sans accroc).
- ✅ Flip policy `@nodefony/test` + REVERT avant commit (appliqué).

## Commits produits

| Commit    | Sujet                                                                                        |
| --------- | -------------------------------------------------------------------------------------------- |
| `c533efa` | perf(framework): index de routes littérales — étape 4 fast path, +15,3 % RPS A/B             |
| `0f62320` | docs(migration): index de routes étape 4 livré — chantier fast path clos                     |
| `bc88444` | fix(framework): allow 405 agrégé RFC 9110 §15.5.6 — hostname avant methods, anti-fuite vhost |
| `bc84050` | docs(migration): conformité allow 405 RFC 9110 livrée — dernier écart routing clos           |
