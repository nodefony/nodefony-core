---
date: 2026-09-21
session_id: 5aab42b6-19fb-4531-b89b-d953aba959c9
focus: Licence de l'application générée, signal des alertes de code, et dev déporté — trois tickets ouverts puis soldés dans la même séance
---

# Session retro — 2026-09-21 — 5aab42b6

Troisième temps de la journée. Entrée par une question du user sur le `package.json`
généré (« les scripts sont écrits deux fois, pourquoi ? »), qui a fait tomber deux
angles morts, puis reprise du jalon alpha.

## Fait

- **#452** (`a29a819a`) — `create app --license <spdx>` : `UNLICENSED` (défaut inchangé),
  `MIT`, `Apache-2.0`, `BSD-3-Clause`, `ISC`. Le `LICENSE` quitte le layer `base` pour un
  dossier par identifiant. Le job de release génère la vitrine sous MIT.
- **#453** (`55840581`) — `ticket:lint` nomme les alertes d'analyse de code ouvertes
  (`ALERTE-CODE`), lecture tolérante et paginée. Au passage : table des contrôles recalée
  (11 listés pour 18 rendus) et gate d'appariement table ↔ script.
- **#442** (`7ded2b77`) — `remoteDevListenAdvice` : plateforme distante détectée + écoute de
  boucle locale ⇒ WARNING nommant les DEUX causes (`devHost`, puis `trustedHosts`).
- **Alerte CodeQL 207** écartée avec son motif — 10ᵉ instance du cas déjà tranché en #385.

## Frictions

- **Un gate né faux vert.** L'appariement table ↔ script lisait le fichier ENTIER : la prose
  qui explique un code le cite deux lignes sous la table. Ligne retirée à l'essai → vert.
  Vu uniquement parce que le débranchement est systématique. Versé au sas.
- **Un critère de fin chiffré se périme.** #385 fermé sur « le compte rend `0` », faux sept
  jours plus tard — le verdict avait été posé site par site. Versé au sas.
- **Le défaut d'un générateur a deux publics.** `UNLICENSED` est juste pour une app privée et
  un contresens pour la vitrine publique issue du même gabarit. Versé au sas.
- **Typecheck rattrapé après coup** : `readJson` des tests rend `Record<Record>>`, les champs
  scalaires (`license`, `private`) ne s'y lisent pas. Vitest efface les types — la suite était
  verte, seul `npm run typecheck` l'a dit.
- **Deux commits refusés par les hooks** : `prefer-array-find` du linter, puis fiches de skill
  périmées (`npm run skills:doc`). Sans coût réel, mais deux allers-retours.

## Coûts évidents

- Un cycle serveur complet (build + start + stop, ×2) pour prouver le câblage de #442 contre un
  serveur réel — justifié : c'est la seule preuve qui porte sur le point d'appel.
- Le `ticket:lint` rejoué cinq fois (dates, gate, fermetures) — bon marché.

## Recommandations

1. Le débranchement systématique a payé **deux fois** cette séance (gate faux vert, garde
   d'écoute). Rien à changer, tout à maintenir.
2. Le `ticket-verify --touched-by HEAD` a rendu des ancres voisines déjà fausses avant ce
   diff (`package.json.tpl:52-58`, `:11`) — c'est #359, ne pas le refaire à la main.

## Patterns récurrents (déjà gérés)

- ✅ Un test neuf vu ROUGE avant d'être cru — appliqué 9 fois ici.
- ✅ Une capacité non atteignable n'existe pas — l'aide et `--describe-json` dérivent de la spec.
- ✅ Prouver sur l'artefact reçu — app générée, pas le gabarit.

## Commits produits

| Commit     | Sujet                                                                      |
| ---------- | -------------------------------------------------------------------------- |
| `6652d2e9` | chore(pilotage): ouvrir la licence de la vitrine et le signal des alertes  |
| `a29a819a` | feat(scaffold): choisir la licence de l'application à sa création          |
| `55840581` | ci(securite): dire quand une alerte d'analyse de code est revenue          |
| `7ded2b77` | fix(frontend): dire pourquoi la page de dev pointe une machine injoignable |
| `1737cef0` | chore(pilotage): rafraîchir l'empreinte après la fermeture de #442         |
