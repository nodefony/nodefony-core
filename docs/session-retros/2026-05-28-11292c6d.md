---
date: 2026-05-28
session_id: 11292c6d-bf09-40f4-93e8-a8b6663e4011
focus: P13 Bloc B étape 8 — docker compose infra Redis (setup distribué)
---

# Session retro — 2026-05-28 — 11292c6d

## Tool usage

| Outil                         | Calls |
| ----------------------------- | ----: |
| Bash                          |    14 |
| Read                          |     5 |
| Write                         |     3 |
| ToolSearch                    |     3 |
| Skill                         |     1 |
| mcp\_\_MCP_DOCKER\_\_mcp-find |     1 |
| Edit                          |     1 |

## Top fichiers Read/Edit

| Fichier                            |   × |
| ---------------------------------- | --: |
| project_p13_realtime_finish_plan   |   1 |
| project_session_2026-05-28_state   |   1 |
| redis/config.ts + service/redis.ts |   2 |
| MIGRATION_STATUS.md (Edit)         |   1 |

## Coûts évidents

- 1 cycle git perdu : lock orphelin a fait échouer `git add` silencieusement → safe-commit a
  commité un staging vide. Refait proprement. Coût ~1 Bash.
- Sinon session très efficace : 1 seul livrable net, 0 restart serveur, docker up du 1er coup.

## Banc charge/fan-out (à la demande user, jetable)

- `redis-benchmark` : ~100k rps simple, ~490k-930k rps pipeliné. Infra OK.
- Fan-out **Redis pub/sub vs IPC** (loopback, RTT, N pods) : IPC p50 0,06→0,82ms (×14 avec N,
  master=goulot, event-loop sature) vs Redis 1,6→3,2ms (×2, fan-out délégué serveur). IPC gagne
  petit cluster, Redis décharge le master à grand N. Banc séquentiel = mesure latence, pas débit max.
- ⚠️ Leçon : j'aurais dû proposer ce test de charge **spontanément** en posant l'infra (le user l'a
  réclamé 2×). Poser une brique d'infra → la stresser dans la foulée.

## 💶 Coût (€)

- Total ~€8,42 (≈ $9,15) — cache write 1h $4,05 + cache read $3,39 = **81 % cache** ; output
  seulement $1,55. Cache-dominé classique (contexte CLAUDE.md + MEMORY.md + skill session).

## Recommandations

1. **allowlist `docker compose`/`docker ps`/`docker exec`** — ajouté ce retex (Bloc B/C en auront
   besoin à chaque session distribué).
2. **MCP_DOCKER ≠ pilotage conteneurs** — à mémoriser : c'est Playwright + gestion serveurs MCP +
   catalogue (docker-docs/dockerhub). Piloter Redis/Kafka = CLI docker (Bash). Évite de re-chercher.
3. safe-commit : le lock orphelin retiré N'A PAS re-staged → toujours vérifier `git status` après un
   retrait de lock avant de conclure.

## Patterns récurrents (déjà gérés)

- ✅ Commit FR via heredoc `-F` (apostrophes) — [[feedback_commit_fr_apostrophes]]
- ✅ safe-commit lock orphelin — [[feedback_git_index_lock]]
- ✅ MAJ MIGRATION_STATUS + commit avant fin — [[feedback_migration_status_uptodate]]

## Commits produits

| Commit  | Sujet                                                           |
| ------- | --------------------------------------------------------------- |
| 436b0b4 | feat(realtime): docker compose infra Redis — P13 Bloc B étape 8 |
