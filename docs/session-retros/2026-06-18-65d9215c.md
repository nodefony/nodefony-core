---
date: 2026-06-18
session_id: 65d9215c-d201-473e-8320-4897961504bf
focus: P6 — @Csp per-route + CSRF étape 2 (@CsrfProtect/@CsrfExempt) + fix flush multi-cookie
---

# Session retro — 2026-06-18 — 65d9215c

## Focus

Deux features P6 sur `refactor/p6-security`, poussées : `@Csp` per-route puis CSRF
étape 2. Un vrai bug framework corrigé en route (flush multi-cookie).

## Fait

- **`@Csp` per-route** (`eba01a03`) : décorateur dual classe+méthode → directives CSP
  additionnelles fusionnées (merge additif W3C CSP3) dans le CSP de la réponse,
  uniquement sur routes décorées. `applySecurityHeaders` déplacé APRÈS le resolve.
  e2e `security-headers.test.ts` 10/10.
- **CSRF étape 2** (`0a8fb192`) : `@CsrfProtect` (synchronizer token double-submit
  signé HMAC-SHA256 stateless — `CsrfTokenManager`) + `@CsrfExempt` (opt-out auth
  conservée). Câblage = moule `@Csp` (meta route → `ctx` au resolve → `enforceCsrf`).
- **FIX `Response.setCookies()`** : n'émettait qu'1 `Set-Cookie` (Node `setHeader`
  REMPLACE) → tableau = N lignes. Session + csrf-token coexistent. 0 test avant.
- Gates : security 332, framework 290, http unit 462, http intég 514 (+1 skip),
  mémoire 9/9, e2e csrf 30/30. P6 → 65 % (P6.7 ✅).

## Frictions (détail → RETEX.md)

- User a flairé le bug d'infra (« il n'y a pas de test double cookies !!! ») alors
  que je voulais contourner (token header-only). → corriger l'infra, pas dégrader le
  design ; signaler le bug découvert.
- Seam `Resolver.match` (pose sessionIntent) = moule canonique des décorateurs de
  policy (réutilisé 2× : @Csp + @Csrf\*).
- security ne peut pas `new Cookie()` (import runtime http interdit) → http pose le
  cookie depuis `context.csrfToken`, security mint seulement.
- Coverage : reporter TEXTE v8 masque un fichier 100 % (csrfToken.ts) → lire
  json-summary ; e2e en process séparé = non instrumenté (% firewall sous-estimé).

## Commits produits

| Commit     | Sujet                                                 |
| ---------- | ----------------------------------------------------- |
| `eba01a03` | feat(security): @Csp per-route                        |
| `0a8fb192` | feat(security): csrf étape 2 + fix flush multi-cookie |
