---
date: 2026-07-06
session_id: 380c8dfc-c45a-4b00-bdc6-ebfae4d21375
focus: Chantier stores — sqlite par défaut + kill file JSON + MemorySessionStorage + switch global NF_STORE=memory
---

# Session retro — 2026-07-06 (2ᵉ) — 380c8dfc

## Fait (7 commits `81999145`→`ca9c4c32`)

- Lot 1 `kernel.varDir` + `location` + colonne Studio ; fix idempotency available.
- Lot 3a `DrizzleTotpSecretStore` (comble gap 2FA SQL) + banc e2e 2FA ops-réelles.
- Lot 3b **sqlite par défaut** (resolveAutoStore préfère backend local persistant) + **SUPPRESSION 3 File\*Store JSON** + WAL.
- Lot 2 `MemorySessionStorage` + suppression `FileSessionStorage` (session available=[memory,drizzle]).
- Switch global **`NF_STORE=memory`** → prouvé : 7 briques + user → memory.

## Décision structurante

**sqlite = backend MONO-NŒUD** (dev → prod localhost, modèle Rails 8/Laravel 11), memory = charge
(NF_STORE=memory), pg/redis/mongo = multi-nœud (infra déclarée). File JSON tué (blob RAM, doublon sqlite).

## Frictions / leçons

- **Tests SÉRIEUX ≠ tests d'adapter isolé** (reproche user 2×) : pour une brique sécu (2FA, session),
  tester le VRAI flux à travers le store (enroll→login→anti-rejeu persisté), pas juste le CRUD.
- **Vérifier la couverture AVANT de supprimer** : le gap « totp sans adapter SQL » était un landmine —
  tuer file l'aurait fait régresser en memory volatil. Terrain avant plan (devise).
- **commitlint `subject-case`** : un sujet qui COMMENCE par un mot PascalCase (`DrizzleTotpSecretStore`)
  est rejeté → toujours commencer le sujet en minuscule.
- **hook `.env.example`** : ajouter une var à `env.ts` → `npx tsx scripts/gen-env-example.ts` + `git add`.
- **Boot serveur lent** (DevSupervisor « Vérification du framework turbo ») après un `--force` : le boot
  peut dépasser 25 s ; poller `nodefony status` plutôt que le timeout du start.sh.

## Coûts évidents

- Serveur redémarré ~4× (mesures runtime des résolutions de stores) — inévitable (preuve terrain par brique).

## Commits produits

| Commit   | Sujet                                    |
| -------- | ---------------------------------------- |
| 81999145 | lot 1 varDir + location + colonne Studio |
| cf731756 | fix idempotency available                |
| f0d62b03 | lot 3a DrizzleTotpSecretStore            |
| c4dc7607 | lot 3a e2e 2FA ops-réelles               |
| ad570647 | lot 3b sqlite défaut + kill file JSON    |
| f8cb168a | lot 2 MemorySessionStorage + kill files  |
| ca9c4c32 | switch global NF_STORE=memory            |
