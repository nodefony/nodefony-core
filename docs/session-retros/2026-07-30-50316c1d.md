---
date: 2026-07-30
session_id: 50316c1d-0a5e-441b-8583-e6741fe1a25f
focus: "`nodefony check` porte sur l'APPLICATION, pas sur le dossier où l'on a tapé"
---

# Session retro — 2026-07-30 — 50316c1d

Session courte, un seul objet : la priorité 1 laissée par la session `07-30d`
(« `check` ne lit le bilan que depuis `process.cwd()` »). Le banc de sécurité
devkit — le vrai sujet — n'a pas été entamé.

## Fait

- `028ac723` — `check`/`doctor` remonte à la racine de l'app (`findProjectRoot`,
  la même définition que le lanceur et les scaffolds), ANNONCE cette racine
  quand elle diffère du départ, accepte `--cwd <path>`, porte `root` en `--json`,
  et sort en 64 sur une option inconnue au lieu de l'ignorer.
- Même geste dans `docsReader` du framework (4 sites) : `.ai/symbols.json`,
  `node_modules` hissés, docs du core, dépôt git — l'app lancée depuis un
  sous-dossier vidait les onglets Docs et API du plan d'administration.
- 7 tests neufs, dont 3 vus ROUGES par débranchement de la remontée.
- Doc : `cli/CLAUDE.md`, `docs/cli.md`, `kernel/MEMORY.md`, gabarit `AGENTS.md.tpl`.

## Coûts évidents

- **La mesure « avant/après » sur le binaire n'a rien coûté mais n'a rien prouvé** :
  un `nodefony-dev-supervisor` vieux de 2 h 16 rebâtissait le core à chaque `Edit`,
  donc l'« avant » lisait déjà l'après. Trois tours perdus à comprendre pourquoi
  l'ancien code se comportait bien — heureusement passés à chercher la cause
  plutôt qu'à conclure « le trou n'existe pas ».
- **Un `rg -n` exploratoire sur `MIGRATION_STATUS.md` a rendu 35 KB d'un coup**
  (ligne 150 = 34 KB sur une seule ligne). Friction déjà connue, revue ici sous
  une autre forme que celle consignée → passée à `2×` dans `RETEX.md`.

## Recommandations

1. **Le débranchement reste le seul contraste fiable** quand un daemon rebâtit
   en continu : il ne dépend d'aucun artefact bâti. À préférer d'emblée à un
   avant/après sur le binaire dès qu'un superviseur tourne.
2. **`ps` avant toute mesure par contraste** — savoir ce qui tourne dans le
   projet fait partie du décor de la mesure, au même titre que l'empreinte de
   l'arbre git.

## Patterns récurrents (déjà gérés)

- ✅ `cd` relatif refusé par le hook (`feedback_bash_cwd_drift`) — repris au vol.
- ✅ Test neuf vu rouge avant d'être cru (`feedback_gate_must_bite`).
- ✅ Preuve sur l'artefact reçu (`feedback_prove_on_received_artifact`) — c'est
  précisément la règle qui a fait chercher pourquoi le binaire disait autre chose.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `028ac723` | `check` porte sur l'APPLICATION, pas sur le dossier où l'on a tapé |
