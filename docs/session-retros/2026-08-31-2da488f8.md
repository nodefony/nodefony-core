---
date: 2026-08-31
session_id: 2da488f8-631c-410a-82b8-95e3a6f386ce
focus: Grappe #54 rouverte et réordonnée, #127 fermé (abonnements rejoués au welcome), vitrine Vue réparée par isolation Vite
---

# Session retro — 2026-08-31 — 2da488f8

## Fait

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `e5933cf8` | grappe **#54** rouverte, réordonnée sur les dépendances, #127 rattaché   |
| `7161699f` | **#127** — rejouer les abonnements au welcome, pas à l'ouverture (fermé) |
| `110a0b88` | **Vue** — isoler la vitrine dans sa propre instance Vite                 |

Jalon **10.0.0 : 43 ouverts / 48 fermés**. **#131** ouvert (10.1) — duplication de la
composition des plugins Vite.

## Frictions

- **Mon instrument a menti deux fois avant que je le suspecte.** `curl` sur un module servi par
  Vite ne mesure pas ce que la page reçoit : la **baseline** rendait déjà `0` alors que la page
  cassait. Deux « succès » étaient des artefacts. Ce qui a tranché : mesurer la baseline d'abord
  (deux valeurs identiques = instrument non discriminant), puis mesurer **par la page**, décor
  identique à chaque tirage.
- **Une heure passée sur un fichier jamais exécuté.** Les plugins Vite sont composés à deux
  endroits ; seul `ViteConfigGenerator` sert en développement. Le fichier généré était pourtant
  sur le disque, lisible en une commande.
- **Le correctif évident ne marchait pas** (`exclude` de `@vitejs/plugin-react`, sans effet
  jusqu'à `[/./]`). Retiré plutôt que gardé « au cas où » — une garde qui ne mord pas ment.
- **Rouvrir une issue `Done` puis éditer ses champs la referme** (workflow Projects). Poser
  `Todo` d'abord. Vu, corrigé, contrôlé stable après 20 s.
- **Mon commit de board a fait monter 4 tickets en « In Progress »** sans qu'aucun travail ne les
  concerne (le hook réagit à toute citation `#N`). Redescendus au END.

## Coûts évidents

- ~8 cycles `build → purge des prébundles → redémarrage` (≈ 2–3 min chacun) pour la chasse Vue,
  dont **4 avant d'avoir un instrument valide** — c'est le poste le plus cher de la session, et il
  était évitable en mesurant la baseline au premier tirage.
- Deux relances complètes de la suite d'intégration `@nodefony/http` (675 tests) — justifiées :
  le premier correctif touchait le socle partagé par les quatre liaisons.

## Recommandations

1. **Mesurer la baseline AVANT le correctif**, systématiquement, quand on ne connaît pas encore la
   fiabilité de l'instrument. Coût : un tirage. Gain ici : quatre.
2. **Chercher qui produit l'artefact exécuté** avant de conclure qu'une correction n'a pas d'effet
   (fichier généré, dist, tarball) — la règle existe déjà (`feedback_prove_on_received_artifact`),
   elle n'a pas mordu parce que je cherchais un défaut de LOGIQUE, pas d'artefact.
3. Le hook `ticket-progress` gagnerait à ignorer les commits `chore(board)` — ils citent tous les
   tickets d'une grappe par construction.

## Patterns récurrents (déjà gérés)

- ✅ Gate vu rouge avec preuve du débranchement (les deux correctifs).
- ✅ Sauvegarde hors git avant de débrancher — la garde du dépôt a refusé mon `git checkout` sur
  arbre sale, à raison.
- ✅ Console d'administration vérifiée en marche avant de fermer un ticket client/serveur.

## Commits produits

| Commit     | Sujet                                                  |
| ---------- | ------------------------------------------------------ |
| `e5933cf8` | chore(board): rouvrir la grappe de la socket cliente   |
| `7161699f` | fix(realtime): rejouer les abonnements au welcome      |
| `110a0b88` | fix(frontend): isoler Vue dans sa propre instance Vite |
