---
date: 2026-06-08
session_id: 78aaa9af-4fb6-4d1a-b5f6-e8d7aa748f53
focus: Chantier statics « préfixe natif /<module>/ » + assetBaseUrl (CDN) + assets:publish
---

# Session retro — 2026-06-08 — 78aaa9af

## Focus

Vider le backlog statics/CDN du chantier proxy : préfixe natif statique configurable,
base CDN des assets, commande d'assemblage CDN-ready.

## Fait

- **Préfixe natif `/<module>/`** (`e735544`) : `server-static.mountModulePublics()` auto-monte
  `public/` de chaque module sous `/<basename>/` (configurable `module.options.publicMount` =
  `false`|`{publicPath,dir}`|auto). Skip app root + frontend-managed + sans public. Migration
  `@nodefony/test` : `git mv public/test/* → public/`, retrait `statics.test`, fix `/media` (chemin direct).
- **assetBaseUrl + asset()** (`29203c1`) : config `frontend.assetBaseUrl` (CDN) préfixe `base` Vite +
  `renderProdTags` + helper `asset()` (Controller locals, Twig/EJS). `assetUrl()` + `IFrontendService`.
- **assets:publish** (`6918f89`) : commande qui assemble la carte préfixe→dossier (mounts natifs +
  bundles frontend) dans `dist-assets/` + manifest. Provider-agnostic (0 dep cloud). Planner pur testé.

## Frictions (→ RETEX.md)

- Course de listeners `onReady` (proxy:generate vs montage) → appel idempotent explicite (auto-suffisance).
- Kernel console CLI = modules PROD only → `/test/` absent de proxy:generate = correct, pas un bug.
- Convention test http = chai + globals (pas import vitest) → écrit faux au 1er jet.
- Préfixe `/test/` était accidentel (sous-dossier) → rendu structurel (nom module).

## Efficacité

- Override `publicMount` prouvé runtime puis révoqué proprement (0 diff). Tous gates verts du 1er coup
  après chaque build. Discussion design en prose (pas de QCM) — user a piloté la config (publicMount/assetBaseUrl).

## Commits

| Commit    | Sujet                                                             |
| --------- | ----------------------------------------------------------------- |
| `e735544` | feat(http) préfixe natif statique /<module>/ configurable         |
| `29203c1` | feat(frontend) assetBaseUrl (CDN) + helper asset()                |
| `6918f89` | feat(http) commande assets:publish (CDN-ready, provider-agnostic) |

## Gates

tsc 0 (http/frontend/framework) · http unit 436 · frontend unit 35 · framework unit 190 ·
intégration monorepo 23/23 · E2E assets:publish (studio 127 fichiers + manifest).
