---
date: 2026-08-28
session_id: 5f96dc8c-9a86-4218-ae06-80becbe30c16
focus: #98 sur les trois dialectes, un défaut de catalogue PostgreSQL, puis la réparation d'une forge rouge depuis six commits
---

# Session retro — 2026-08-28 — 5f96dc8c

## Focus

Reprise de #98 : rendre le banc des commandes `orm:migrate*` multi-dialecte, comme la matrice de
preuve du ticket l'exige. Le banc a fait tomber un défaut qui touchait **toute** application
PostgreSQL.

## Fait

- **`34cdb235`** — `migrate-cli.e2e.test.ts` paramétré par dialecte : 16 cas verts sur sqlite,
  PostgreSQL et MySQL/MariaDB (il n'exerçait que sqlite).
- **Défaut produit corrigé** : `to_regclass('User')` plie l'identifiant en minuscules → la table
  `User` du framework était déclarée absente → verdict `divergent` permanent et sonde de
  disponibilité qui retient le pod en mode `fail`. Le catalogue compare désormais des chaînes,
  comme `columnsOf` juste en dessous.
- **Garde de non-régression** posée dans un banc OUVERT (`NF_PG_URL` suffit), pas derrière
  `NF_RUN_CLI_BOOT` — vue rouge sur PostgreSQL, verte sur MySQL, par débranchement réel.
- Deux preuves qui n'existaient nulle part : `divergent` provoqué par un `ALTER` fait à la main,
  et `NF_MIGRATE_DATABASE_URL` qui détourne `orm:migrate` seul.
- **#105 ouvert** : le verdict `divergent` ne nomme ni table ni colonne, alors que
  `compareToDeclared()` les connaît.
- #104 redescendu en `Todo` (rien ne l'a fait avancer depuis son ouverture).

## Coûts évidents

- **Un `&&` coupé** : `ls <fichier absent> && python3 …` — le débranchement n'a jamais eu lieu, la
  suite est sortie verte, et j'ai failli conclure que mon gate ne mordait pas. Il a fallu tout
  refaire, cette fois en AFFICHANT le code actif.
- **Décor sale que j'ai fabriqué** : trois schémas de diagnostic (`nf_dbg*`) laissés en base ont
  fait rougir deux bancs voisins à la passe complète. Nettoyés, puis 1153/1153.
- **Dérive du répertoire courant** : deux lancements de vitest partis de la racine au lieu du
  module (« no tests », puis `describe is not defined`).
- Diagnostic du `divergent` à la main (psql, comparaison de schémas migré/dérivé) : ~30 min, pour
  une information que le produit calcule déjà — d'où #105.

## Seconde moitié — la forge était rouge depuis six commits

Le user l'a signalé : `nodefony-core` rouge depuis 10:11, `Code généré` depuis 13:42. Cinq
sessions avaient clôturé dessus sans la regarder.

- **`91b626b2`** — un exemplaire dont la mise en service est déjà retenue ne meurt plus.
  Le nœud était circulaire : `orm:migrate` boote un kernel, le cycle applicatif tape `User`,
  l'échec est fatal en production, la commande meurt avant de s'exécuter. Mesuré `exit=70` →
  `exit=0`. Décor de production (forge + gabarit) : migrations appliquées AVANT tout le reste.
- Défaut de gabarit trouvé au passage : la couche `base` du générateur ne recevait pas `hasOrm`,
  donc l'étape ajoutée disparaissait du fichier RENDU — vert à l'écriture, absent chez
  l'utilisateur. Vu sur une application réellement générée.
- **#106 ouvert** (P0) : éprouver les migrations dans une application générée — « si ça ne marche
  pas là, c'est comme si on n'avait rien fait ». **#101 recalé** (deux ancres soldées, un site
  manqué : `AGENTS.md.tpl:495` enseigne encore le geste d'avant, devenu faux).
- **Laissé rouge en connaissance de cause** : la tâche `Code généré`. Une application n'a aucun
  moyen d'obtenir le schéma de ses propres entités en production (#102). Le banc passe de « ne
  démarre pas » à « démarre et rend 500 ». Le rouge dit vrai ; le maquiller aurait coûté plus cher.

## Coûts évidents — seconde moitié

- Deux tentatives de dérogation par variable d'environnement, toutes deux inopérantes : la
  première écrasée par un `...process.env` posé après, la seconde sans effet parce qu'une
  surcharge `NF__*` n'écrit que sur un chemin déjà présent. Vérifiées trop tard, sur la présence
  de la ligne au lieu de la valeur effective.
- Deux lancements de vitest partis de la racine au lieu du module (dérive du répertoire courant),
  encore.

## Recommandations

1. Exiger la **preuve d'état** d'un débranchement (afficher le code actif), jamais la déduire du
   résultat du test — versé dans `RETEX.md`.
2. Varier la **forme** des fixtures d'un banc (ici : la casse des noms de table) — un corpus
   homogène ne prouve que sa propre forme.
3. Nettoyer son décor de diagnostic AVANT de lancer une passe complète : c'est le voisin qui
   rougit, et on soupçonne son propre diff.

## Patterns récurrents (déjà gérés)

- ✅ « le décor SALE fabrique les faux verdicts » — mémoire `feedback_stale_decor_poisons_verdicts`.
- ✅ « le répertoire courant dérive » — mémoire `feedback_bash_cwd_drift`.
- ✅ « un gate qu'on n'a jamais vu échouer n'est pas un gate » — appliqué, et c'est ce qui a
  révélé le `&&` coupé.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `34cdb235` | voir les tables au nom capitalisé, et éprouver les commandes sur 3 dialectes |
| `91b626b2` | ne pas tuer un exemplaire dont la mise en service est déjà retenue           |
