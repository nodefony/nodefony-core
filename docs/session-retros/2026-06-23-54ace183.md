---
date: 2026-06-23
session_id: 54ace183-fb24-4b13-9538-c5334d2d84c3
focus: Audit sécu Studio (post-P6) → lot sécu + modèle de rôles 2 échelles + dette doc
---

# Session retro — 2026-06-23 — 54ace183

## Focus

Parti d'un simple « MAJ MIGRATION_STATUS.md » → dérive en **audit sécurité complet du module
Studio** (codé avant P6) : confronter la doc/code, fermer les oublis, durcir, puis corriger la
dette doc dangereuse. + naissance du modèle de rôles 2 échelles (sur alerte multi-tenant du user).

## Fait

- **Audit Studio** (5 sous-agents parallèles, matrice actif×chemin) : backend controllers / RBAC data
  plane / info-leak-secrets / gardes front / dette technique. Verdict : P6 SOLIDE (mocks supprimés,
  RBAC fail-closed, redactions OK), 5 oublis mineurs + 1 grosse dette doc.
- **Lot sécu** `b1281aca` : #1 `kernel:gc`/`kernel:ping` WS gatés (DoS, **red→blue** frame lock 56/56,
  câblage hub singleton tracé) · #2 `/studio/api/stats`→ROLE_SUPERVISOR + `/documentation/api/*`→
  [ROLE_DEV,SUPERVISOR] (montés hors broker) · #3 `/studio/api/info` anonyme réduit · #4 `test/run`
  séparateur `--`+rejet `-` · #6 code mort Bearer retiré. #5 (URL profiler) écarté (théorique).
- **Modèle de rôles 2 échelles** : roleHierarchy serveur réconciliée (NODEFONY*ADMIN couvre
  DEV/SUPERVISOR/ADMIN/AUDITOR→USER) ; frontière `ROLE_NODEFONY*_`=plateforme / `ROLE\__`=tenant.
- **Roadmap** `ddd94a87` : 4e chantier post-Studio (store back préférences) ajouté.
- **Dette doc** `bcf567d0` : 6 messages UI + CLAUDE/MEMORY/docs Studio (« auth mock » → P6 branché) +
  stub mort supprimé. **Restes** `883cea38` : exports package.json (sans types, intentionnel) +
  Mantine v9 + **smoke live** (boot OK, /info réduit, stats/doc 401).

## Frictions / leçons (→ RETEX.md)

- Gater un namespace WS = distinguer homonymes (`ping` heartbeat ≠ `kernel:ping` action) + vérifier
  usage front + PROUVER le câblage (fix pur ≠ protection si le seam ne l'invoque pas).
- Gater une page front avec un rôle tenant exige que NODEFONY_ADMIN le couvre (sinon 403 admin).
- Doc IA d'un module ment après un branchement majeur (P6) → resync les CONSOMMATEURS, pas que le
  module modifié. Un « écart au template » flaggé par un auditeur peut être une exception documentée.

## Coûts évidents

- 5 sous-agents general-purpose (audit) = gros mais justifié (couverture matrice). 0 restart serveur
  (DevSupervisor a rechargé seul → smoke live direct, pas de boot manuel). Plusieurs « File not read »
  sur Edit après commits prettier (re-Read obligatoire) — friction connue.

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `b1281aca` | fix(security) lot sécu Studio + hiérarchie de rôles 2 échelles |
| `ddd94a87` | docs(migration) audit journalisé + 4e chantier post-Studio     |
| `bcf567d0` | fix(studio) dette doc (auth « mock » → P6)                     |
| `883cea38` | chore(studio) exports package.json + Mantine v9 + smoke live   |

(+ `9cfe4423` docs(migration) profil self/PATCH, début de session.)
