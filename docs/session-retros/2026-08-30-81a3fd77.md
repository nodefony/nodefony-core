---
date: 2026-08-30
session_id: 81a3fd77-f964-405c-afa4-0752c7773b34
focus: adopter une base qui n'a jamais eu de migrations (#118) — et la preuve du ticket était périmée
---

# Session retro — 2026-08-30 — 81a3fd77

## Fait

- `orm:migrate:baseline --from-database` : la migration de référence est LUE sur la base,
  décommentée, inscrite sans rien exécuter. `orm:generate` refuse le schéma initial sur une base
  qui porte déjà ces tables (`NF_GENERATE_DATABASE_NOT_ADOPTED`).
- Le décor de la tâche 33 du banc réparé aux deux niveaux : `set -e` dans le LANCEUR, et la ligne
  témoin RELUE au lieu de se fier à un code de sortie de `curl`.
- Doc : skill npm `nodefony-migrate-schema` (+ `references/verdicts.md`), `docs/migrations.md`,
  `MEMORY.md` du module, carte des phases.

## Coûts évidents

- **Instruire AVANT de coder a été le meilleur investissement de la session** : un test de 20 lignes
  a montré que l'affirmation centrale du ticket était fausse pour son décor, et évité de corriger un
  composant sain.
- Trois allers-retours de conception sur `tablesFilter` (liste positive → exclusion → constat des
  extras) : le multi-moteur a invalidé deux fois une conception qui passait sur SQLite. Le coût est
  réel, mais il est ARRIVÉ AU BON MOMENT — avant la publication, pas après.
- Une régression auto-infligée non détectée par le typecheck (`name` résolu sur `Window.name`) :
  attrapée à la relecture du diff.

## Recommandations

1. **Reproduire l'affirmation d'un ticket avant de coder dessus** — quand elle cite un run daté,
   la confronter au `git log` du chemin concerné. Coût : minutes. Gain : une session.
2. **Deux passes MySQL, toujours** — MariaDB et MySQL Community ne sont pas le même produit, et le
   dépôt le sait déjà (`MYSQL_COMMUNITY_GATE`). Un test qui bifurque doit constater le SERVEUR.
3. **Exécuter la `nextAction` que rend un refus** — c'est le seul moyen de voir un enfermement,
   qu'aucune garde prise isolément ne révèle.

## Patterns récurrents (déjà gérés)

- ✅ Vu-mordre par débranchement, avec l'assertion NOMMÉE (pas le code de sortie).
- ✅ Le gate d'infra du dépôt a attrapé un skip silencieux que la lecture ne voyait pas.
- ✅ Décor sale de banc (tables résiduelles) : contourné par un nom de table propre au banc.

## Non fait — énoncé

- La tâche 33 du banc n'a PAS été rejouée (#99) : c'est le critère de fin de #118, qui reste ouvert.
- La commande CLI `--from-database` n'a jamais tourné sur un BOOT RÉEL — seules ses briques sont
  éprouvées. Le dépôt ne déclare aucune entité applicative.
- Suite complète sur MySQL Community non jouée (seul le fichier d'adoption, 8/8).

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `949d5bcb` | feat(orm): adopter une base qui n'a jamais eu de migrations |
