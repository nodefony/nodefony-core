---
date: 2026-06-05
session_id: b8c2a82b-d98c-4f1c-8ada-4c720fca5a3f
focus: P14.11 core isomorphe CLOS + suppression totale mocha→vitest (5/6 workspaces)
---

# Session retro — 2026-06-05 — b8c2a82b (3e du jour)

## Fait

- **P14.11 — core isomorphe CLOS à 100%** : shim browser `node:events` était incomplet (`rawListeners` +
  `prepend*` manquants) alors que `Event.emitAsync`/`emitAsyncGuarded` (bus `fireAsync`) l'appelle → `TypeError`
  runtime browser **masqué par un tsc vert** (shim typé permissif). Complété + test régression `clientShimEvents.test.ts`.
  Audité au passage : le canal syslog isomorphe (pduProtocol + useNodefonySyslog) est safe (consomme via RealtimeClient,
  n'instancie pas Syslog browser). `f41bb23`.
- **Suppression totale mocha → vitest — 5/6 workspaces** :
  - core (`4106303`) : 57 fichiers, **1558 tests parité exacte**, 101 `done`→Promise (codemod), 2 BUGS RÉELS
    débusqués (typeOf `arguments.callee` strict-mode ; NODE_ENV='test' sticky → resolveRuntimeEnv).
  - mediasoup (`82cc83a`, 22 tests) · frontend (`1a9b912`, 42) · framework (`899924a`, 235).
  - docs/skills/CLAUDE.md/mémoire (`01e3a93` + mémoire `d3901b4`) — refs CORE corrigées, refs http laissées exactes.

## Décisions

- **`test → production` CONSERVÉ** (pas touché `resolveRuntimeEnv`) : `isTest` est l'axe orthogonal ; production =
  défaut sûr 12-factor ; c'était déjà le défaut effectif sous mocha. Tests d'env rendus explicites (delete NODE_ENV scopé).
- **`NODE_ENV='test'` est BON** (vitest le pose) — ne PAS le supprimer globalement (hack de parité) ; le bug était dans
  des tests qui supposaient NODE_ENV absent. Cf [[feedback_test_framework_vitest]].
- **« Suppression totale » ≠ purge aveugle des docs** : tant que http garde le gate mocha, ses refs (skills/CLAUDE.md)
  restent EXACTES → ne purger que le workspace migré, sinon la doc ment.
- monocart **déprécié** → coverage `@vitest/coverage-v8` partout.

## Reste — prochaine étape

1. **Priorité 1 : migrer `@nodefony/http` vers vitest** — DERNIER détenteur de mocha, **sensible** : porte le gate
   mémoire/charge (`memory.test.ts` + `.mocharc.load.json`, blocker perf). Faire en session DÉDIÉE : `nodefony-check-memory-health`
   AVANT (baseline seuils) → migrer unit + porter le gate load → check APRÈS (parité seuils). Alors `npm audit` clean sur mocha.
2. **Mise à jour de tous les packages outdated** (demandé user en clôture).
3. Puis reprise boussole : durcissement ORM [[project_orm_hardening_kit]].

## Commits (6)

| Commit  | Sujet                                                    |
| ------- | -------------------------------------------------------- |
| f41bb23 | feat(core): shim node:events complet → P14.11 isomorphe  |
| 4106303 | test(core): migration mocha → vitest 4                   |
| 01e3a93 | docs(test): refléter migration core (skills + CLAUDE.md) |
| 82cc83a | test(mediasoup): mocha → vitest                          |
| 1a9b912 | test(frontend): mocha → vitest (intégration)             |
| 899924a | test(framework): mocha → vitest (intégration)            |
