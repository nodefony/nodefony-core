---
date: 2026-06-15
session_id: 868cec33-4a25-47e7-a8f5-82fec4916423
focus: P6 J8 — débloquer la garde @IsGranted côté WS en JWT (vraie cause = dual-package) + résilience tests mongoose + fix unhandled rejection CLI
---

# Session retro — 2026-06-15 — 868cec33

## Focus

Clôturer P6 J8 (volet b JWT) resté bloqué : prouver « 1 garde = N transports ET N modes d'auth »
sur la socket. Puis fiabiliser les tests (mongoose flaky sous turbo, unhandled rejection CLI).

## Fait

- **J8 volet b débloqué + prouvé** (`9f322a06`) : banc JWT `ws-isgranted-jwt` 2/2 (admin GRANT / user 403),
  cookie 8/8 non-régression, WS intég 105/105, mémoire 9/9, security 195, security-review 0 blocker.
- **Vraie cause trouvée (≠ kit)** : pas la sécu JWT, mais un **dual-package hazard** — `src/modules/test` ne
  déclarait pas `@nodefony/realtime` (peerDep + `external` rollup) → 2ᵉ `RealtimeHub` dupliqué → authenticator
  WS invisible → garde 403. Fix = externaliser realtime (3 lignes), 0 changement de code sécu.
- **Tests mongoose fiabilisés** (`7ba4783c`) : 1 `mongod` partagé (`globalSetup`+`inject`) + `fileParallelism:false`
  - `describe.skipIf` (skip gracieux, exit 0 si infra absente). Suite 59/59 ; skip prouvé.
- **Fix core unhandled rejection** (`9417ebdf`) : `Command.ts` retourne la promesse d'action → `Cli.test.ts` 93/93,
  0 unhandled rejection.
- MIGRATION_STATUS P6 → 62 % (J8 ✅).

## Frictions / leçons (→ RETEX.md)

- Cause racine d'un kit peut être 100 % fausse → prouver au terrain (devise contrôle).
- DIAG qui ne fire pas = code instrumenté ≠ code exécuté → suspecter module dupliqué.
- Dual-package hazard sur singleton (hub) si module non externalisé.
- Échec seulement sous turbo = contention de parallélisme, pas un bug ; reproduire dans le bon contexte.
- Isoler une régression suspecte par `git stash` A/B (les 2 fails CliIntegration = pré-existants).

## Restes / non couverts

- **CliIntegration.test.ts** : 2-4 fails flaky PRÉ-EXISTANTS (timeout boot CLI réel) — hors scope, à traiter à part.
- **Matrice E2E incomplète** : `subscribe × auth` (canaux protégés) seulement en unit, pas en banc E2E ;
  cellule WS-JWT-anonyme sans assertion explicite.

## Commits produits

| Commit     | Sujet                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| `9f322a06` | feat(security): garde @IsGranted WS cookie+JWT (J8) + fix dual-package realtime |
| `7ba4783c` | test(mongoose): mongod partagé (globalSetup) + skip gracieux                    |
| `9417ebdf` | fix(core): propage la promesse d'action CLI → plus d'unhandled rejection        |
