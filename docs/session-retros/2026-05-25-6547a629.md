---
date: 2026-05-25
session_id: 6547a629-e432-48d5-a591-16503ebc2aba
focus: Audit migration phase-par-phase + session « santé » supervision (worker/pondération/heap) + Vite spawn direct + titres ps
---

# Session retro — 2026-05-25 — 6547a629

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |   111 |
| Edit            |    65 |
| Read            |    41 |
| Skill           |     5 |
| AskUserQuestion |     2 |
| Write           |     1 |

## Top fichiers Read/Edit

| Fichier                          |   × |
| -------------------------------- | --: |
| MIGRATION_STATUS.md (R16/E35)    |  51 |
| DashboardSupervision.tsx (R8/E8) |  16 |
| ProcessGraphGrid.tsx (R5/E8)     |  13 |
| ViteProcessSupervisor.ts (R2/E7) |   9 |

## Coûts évidents

- **Friction no-HMR cluster** : ~6 cycles build:front + restart cluster pour « voir »
  la santé worker (cluster sert le buildé, manifest caché au boot → 404 stale → restart).
- **`index.lock` husky** : a bloqué 2 commits (retiré 2× à la main). Piège récurrent.
- **2 commits commitlint/header > 100c** rejetés → raccourcis.
- MIGRATION_STATUS.md relu 16× (audit 17 phases, normal pour cet exercice).

## 💶 Coût (€)

- Total ~**€158** (≈ $172) — **cache-read ~$128 (74 %)** + output ~$25 + cache-write-1h ~$17.
  Cache-dominé (contexte porté sur 268 tours : CLAUDE.md + skills + gros .tsx relus). ~€32/commit.

## Recommandations

1. **Garde-fou `index.lock` husky** (kit `project_retex_improvements_kit` #I) — bloqué 2× cette
   session, 5+ fois cumulé. Le plus rentable. Pré-commit qui `rm -f` un lock orphelin (âge + 0 process git).
2. **Chantier dev-cluster HMR** — la friction « voir le multi-process » coûte 6 rebuild/restart/session.
   Tant qu'il n'existe pas, valider en cluster PUIS basculer dev (debug bar).
3. **Leçon `lsof`** : pour traquer un serveur orphelin → `lsof -sTCP:LISTEN` (pas `lsof -ti` qui
   compte les connexions navigateur → faux positif, vécu cette session).

## Patterns récurrents (déjà gérés)

- ✅ Titres process nommés ([[feedback-named-process-titles]], étendu `claude-` pour mes spawns).
- ✅ Commits FR via heredoc `-F -` ([[feedback-commit-fr-apostrophes]]).
- ✅ Décider+expliquer plutôt que QCM (arbitrages techniques tranchés inline).

## Commits produits

| Commit    | Sujet                                                                                |
| --------- | ------------------------------------------------------------------------------------ |
| `ef2edc1` | docs(migration): audit phase-par-phase vérifié code                                  |
| `1939c70` | feat(studio): santé worker + pondération card + fix heap repos + branche ROLE_DEV    |
| `76fa254` | fix(frontend): spawn Vite direct (node vite.js) au lieu de npx — 0 shim, kill propre |
| `1936966` | feat(frontend): titre ps lisible par instance Vite (argv0 = familles servies)        |
