---
date: 2026-05-20
session_id: 384a7e53-b087-4617-ae4d-418d61d8d268
focus: Presets Vite vue3 + angular (3 frameworks/1 supervisor) + fix warnings TS2307 cross-module
---

# Session retro — 2026-05-20 — 384a7e53

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Bash  | 92 |
| Edit  | 51 |
| Read  | 36 |
| Write | 24 |
| AskUserQuestion | 2 |
| Skill | 1 |

Transcript : 862 lignes / 3.0M. Tool results : 207 events, 197k chars (avg 950).

## Coûts évidents
- **8× start.sh** (restarts serveur) : multi-framework + 1 restart "gratuit" car le module Angular n'était pas dans `MODULE ADD` au 1er boot → **dist root périmé** (start.sh skip le build du root app ; le watch Rollup le reconstruit ~12s après boot). Variante du piège `feedback_watch_rollup_pitfall`.
- **14× build** dont plusieurs pour découvrir des échecs (rollup `external` manquant pour `@analogjs`/`@angular`).
- **4× npm install** : ERESOLVE Angular (TS6 vs `@angular/build`<6.0) → `--legacy-peer-deps` ; puis re-add `@vitejs/plugin-react` (legacy ne réinstalle pas les peers optionnels).
- **Cache turbo trompeur** : 43 "faux" TS2307 rejoués depuis le cache (changer le package.json de framework n'invalide pas le cache de http) → temps perdu avant de comprendre. Confirmé via build direct/`--force`.
- **23× curl** : vérif transform Vite (pas de headless — `feedback_no_headless_chrome`). Normal.
- **Bruit hook** : `generate-symbols` crache ~50 lignes "homonym" × 3 commits dans le contexte. Pur gaspillage de tokens.

## Recommandations
1. **MAJ start.sh OU CLAUDE.md** : quand `index.ts` racine change (ajout `@modules`), rebuilder le root app avant boot — sinon 1er boot rate le module (dist root périmé). → nouvelle mémoire `feedback_root_dist_stale_modules`.
2. **Mémoire `feedback_turbo_cache_stale_logs`** : turbo rejoue les logs cachés ; un fix dans une dep non-déclarée n'invalide pas le cache du consommateur → vérifier via `turbo run build --force` ou build direct. Et `turbo run test --force` provoque une race build/test sur le dist de http (échec résolution transitoire) → tester sans `--force`.
3. **Silencer le hook generate-symbols** (homonym warnings → `>/dev/null` ou flag quiet) : 150+ lignes de bruit par session de commits.

## Patterns récurrents (déjà gérés)
- ✅ Pattern multi-framework Vite = **3 touchpoints** (preset + ViteBuilder + ViteConfigGenerator), TemplateHelper seulement si injection HTML spéciale → mémoires `project_frontend_vue_multiframework` + `project_frontend_angular_plan`.
- ✅ Gotchas Angular (TS6/`--legacy-peer-deps`, external rollup, tsconfig scoping, HMR=reload) → mémoire + MIGRATION_STATUS.
- ✅ Vérif front = curl transform + hard-reload user (pas de headless) → appliqué.
- ✅ AskUserQuestion avant action impactante (install Angular lourd, conflit peer TS6) → bon réflexe.

## Commits produits
| Commit | Sujet |
| ------ | ----- |
| f8f6937 | preset vue3 + validation multi-framework (React+Vue) |
| 55d0c67 | preset angular (Angular 21) — 3 frameworks/1 Vite |
| 669e085 | fix warnings TS2307 cross-module (exports.types → source) |
