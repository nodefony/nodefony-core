---
date: 2026-05-20
session_id: 384a7e53-b087-4617-ae4d-418d61d8d268
focus: Presets Vite vue3+angular (3 fw/1 supervisor) → nettoyage 0 warning TS → audit migration phase-par-phase → skill migration-audit
---

# Session retro — 2026-05-20 — 384a7e53 (session complète)

## Tool usage (session entière)
| Outil | Calls |
| ----- | ----: |
| Bash  | 142 |
| Edit  | 91 |
| Read  | 56 |
| Write | 35 |
| AskUserQuestion | 2 |
| Skill | 1 (retex) |

Transcript 1556 lignes / 4.0M. Tool results : 327 events, 350k chars (avg 1071). **13 commits.**

## Arc de la session (4 blocs)
1. **Presets Vite** vue3 + angular → 3 frameworks (React/Vue/Angular) sous 1 superviseur Vite, validés runtime (HMR + autocannon). Pages démo unifiées + rename routes /react|/vue|/angular.
2. **Nettoyage build TS → 0 warning** : TS2307 (exports.types→source), TS4114 (override), TS7016 (.d.ts locale ×6 modules), TS2339 (getter WebsocketContext), TS2532 (`this.context!`), TS2591 (types:["node"] llm).
3. **Audit migration phase-par-phase** vérifié dans le code (P0→P16) → recale détail + remplace le résumé par-composant (périmé) par un comptage **par phase** = source unique. État réel : 164 tâches, 29%.
4. **Skill `migration-audit`** créé + 4 itérations (revue interactive, synthèse graphique, tableau parfait déps/prio/temps, encadré « prochaine étape »).

## Coûts évidents
- **19× `turbo run build/test --force`** — énorme. Causé par le cache turbo qui rejoue d'anciens logs (un fix dans une dep non-déclarée n'invalide pas le cache du consommateur) → on a dû forcer pour voir le vrai décompte TS à chaque catégorie. ~30-40s × 19 = ~10 min de rebuild + tokens de logs.
- **9× start.sh** (restarts) + **24× curl** (vérif transform Vite, pas de headless) — attendu pour le multi-framework.
- **5× npm install** dont le `--legacy-peer-deps` Angular (conflit TS6 vs @angular/build<6.0) qui a viré `@vitejs/plugin-react` (peer optionnel) → re-add.
- **Bruit hook generate-symbols** : ~50 lignes "homonym" × 13 commits = ~650 lignes de bruit pur dans le contexte.

## Recommandations
1. **Réduire les `turbo --force`** : au lieu de forcer le monorepo entier pour vérifier 1 fix, builder le workspace directement (`cd <pkg> && npx rollup ...`) ou cibler `--filter`. Cf mémoire `feedback_turbo_cache_stale_logs` (créée cette session).
2. **Silencer le hook generate-symbols** (homonym → `>/dev/null` ou flag quiet) — récurrent (3e retex à le noter). ~650 lignes/session gaspillées. **Action concrète à faire.**
3. **Mémoire `feedback_root_dist_stale_modules`** (créée) : rebuild root app après ajout `@modules()` sinon 1er boot rate le module.
4. **Skill `migration-audit`** (créé) capitalise l'audit phase-par-phase + synthèse graphique → réutilisable.

## Patterns récurrents (déjà gérés)
- ✅ Pattern multi-framework Vite = 3 touchpoints (preset+builder+generator). Mémoires vue/angular.
- ✅ Vérif front = curl transform + hard-reload (pas de headless).
- ✅ AskUserQuestion avant action impactante (install Angular lourd, conflit peer TS6).
- ✅ Fix type-only (override/`!`/import type/.d.ts) = runtime inchangé → pas de memory.test requis.
- ✅ Ne jamais dire « pré-existant » sans vérifier vs commit parent (les 59 fails framework = serveur absent, pas régression).

## Commits produits (13)
| Commit | Sujet |
| ------ | ----- |
| f8f6937 | preset vue3 + multi-framework (React+Vue) |
| 55d0c67 | preset angular (3 fw/1 Vite) |
| 669e085 | fix TS2307 cross-module |
| 12ef051 | retex (partiel) |
| 83ed147 | fix TS4114 |
| 33c55c4 | fix TS7016 + TS2339 |
| 34eb90a | fix TS2532 + TS2591 (0 warning TS) |
| 6088d73 | audit migration détail recalé |
| 57e9a56 | skill migration-audit (revue) |
| d4d5bdc | résumé migration → par phase |
| 48ba8c4 | skill : synthèse graphique |
| 3b48610 | skill : tableau parfait (déps/prio/temps) |
| 72b454d | skill : graphe + encadré prochaine étape |
