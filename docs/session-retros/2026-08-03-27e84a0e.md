---
date: 2026-08-03
session_id: 27e84a0e-2f4c-42b3-84de-1c11a69fc3bd
focus: les compteurs suivent la recherche, puis `$like` gagne sa clause ESCAPE
---

# Session retro — 2026-08-03 — 27e84a0e

9ᵉ séance du 3 août. Deux gestes, tous deux hérités de reliquats nommés la veille.

## Fait

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `463a4869` | les COMPTEURS cessent de figer quand on cherche (serveur + 2 gates) |
| `6efbfb0a` | la barre de recherche emmène les CARTES avec elle (Studio)          |
| `848026db` | `$like` gagne sa clause ESCAPE — un motif portable dit UNE chose    |
| `f3f3e4c4` | deux avertissements oxlint qui bloquaient `npm run lint`            |

## Coûts évidents

- **4 rebuilds complets** (~30 s chacun) : trois étaient des points de mesure
  légitimes (débranchements successifs sur un serveur qui charge le `dist`), le
  quatrième était le retour à l'état sain. Difficile à compresser ici — le banc
  du catalogue tape un serveur live.
- **Un faux vert payé deux fois** : heredoc rejeté par un hook → banc inchangé →
  14 verts crus, puis un run depuis la racine qui les a confirmés. ~3 outils
  perdus, et surtout un verdict faux tenu pendant deux tours.
- L'exploration du terrain avant de coder (≈ 10 lectures ciblées) a **supprimé
  les trois quarts du chantier annoncé** : c'est le meilleur ratio de la séance.

## Frictions (rangées dans `RETEX.md`)

- Un gate de paire liste↔compteurs doit tester la DÉCLARATION **puis** l'EFFET.
- Un double de test qui filtre par deux chemins reproduit le défaut qu'il juge.
- Un contrat portable qui se tait a autant de sémantiques que de moteurs.
- Un hook qui refuse une commande refuse aussi le heredoc qu'elle portait.

## Ce que les débranchements ont attrapé

Six, tous vus rouges : le passage de `q` au comptage (users, webhooks), la
publication de `search`, la garde anti-banc-vide (`proven=0` a crié), la lecture
de l'échappement au socle, et le `LIKE` sans clause — ce dernier avec la
démonstration la plus nette de la séance : **SQLite tombe, PostgreSQL passe**.

## Non lancé / à savoir

- redis (aucun décor ; aucun store Redis n'interprète `$like`).
- Gate mémoire non joué — le pipeline HTTP n'a pas été touché.
- `MongooseWebAuthnCredentialStore` échappe encore en regex native : hors du
  contrat portable, inoffensif.
- Conteneurs `nodefony-postgres`, `nodefony-mariadb`, `nodefony-mongo` et le
  serveur dev laissés DÉMARRÉS.

## Suites rejouées

drizzle 990 (3 dialectes réels) · mongoose 193 · orm-core 114 · security 929 ·
user 192 · http 744 + 597 intégration · framework 128 · studio 77 · lint vert.
