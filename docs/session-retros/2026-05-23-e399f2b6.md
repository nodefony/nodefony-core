---
date: 2026-05-23
session_id: e399f2b6-7df9-4cf7-925a-f48cc5c317c1
focus: Tests de charge combinés + fix B (signaler la famine realtime sous charge) + 2 idées archi notées
---

# Session retro — 2026-05-23 — e399f2b6

## Tool usage (approx.)

| Outil | Usage                                                                               |
| ----- | ----------------------------------------------------------------------------------- |
| Bash  | élevé (start server, 3 runs stress en bg, polling output, curl data plane, git)     |
| Edit  | 8 (DashboardSupervision.tsx fix B) + 2 (skill Retex)                                |
| Write | 3 mémoires (orm-flow-probe, MAJ realtime-granularity, MAJ session state) + ce retex |
| Read  | ciblé (DashboardSupervision sections, drizzle #prof/probe, ui kit signatures)       |

## Coûts évidents

- **3 runs de stress** (dont 2 coupés/relancés) : 1er trop mou (0 rupture), 2e bloqué à l'ouverture
  WS (HTTP_STEP trop haut affame les handshakes), 3e équilibré OK. → leçon paramétrage notée.
- **Polling manuel** de l'output bg (Read + Bash tail répétés) au lieu d'un Monitor ciblé.
- `.git/index.lock` stale (hook pre-commit symbols) → 1 retry commit.
- Le vrai blocage « pas vu le badge » = navigateur user non hard-reloadé / granularité 10 s, pas le code.

## Recommandations

1. **Élargir le critère de rupture du stress** (`supervision-stress.mjs`) : ajouter lag event-loop /
   cadence realtime, pas que `%err` (0 % err alors que l'app était inutilisable). — friction réelle vue.
2. **`ORM_PATH=/nodefony/orm/api/counts`** pour un vrai stress lecture DB (≈410 COUNT, session 104k).
   Documenter dans le skill `nodefony-load-test`.
3. **Purger la table `session`** (104 720 lignes de déchet load-test) — GC sessions / vacuum.
4. Pour suivre un bg long avec rupture → `Monitor` (filtre PALIER|RUPTURE|err) plutôt que polling.

## Patterns récurrents (déjà gérés)

- ✅ Famine realtime sous charge = inhérent single-thread → SIGNALER (fix B), pas masquer.
- ✅ Latence applicative (ping ORM) à lire à l'aune de l'event-loop lag.
- ✅ Vérif front = curl + demander hard-reload au user (pas de headless).

## Commits produits

| Commit  | Sujet                                                                     |
| ------- | ------------------------------------------------------------------------- |
| ea8acd2 | feat(studio): supervision — badge « retard » famine realtime + ⓘ ping ORM |
