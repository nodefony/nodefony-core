---
date: 2026-05-23
session_id: 6e0d2ca9-e693-41e1-87ca-02d6b375461575
focus: Supervision realtime (patron sondes+hub) + connecteurs ORM + debug bar live + load test combiné
---

# Session retro — 2026-05-23 — 6e0d2ca9

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |   105 |
| Edit  |    84 |
| Read  |    49 |
| Skill |     6 |
| Write |     5 |

## Top fichiers Read/Edit

| Fichier                                               |   × |
| ----------------------------------------------------- | --: |
| `studio/frontend/src/routes/DashboardSupervision.tsx` |  54 |
| `nodefony/src/client/debugbar/DebugBar.ts`            |  12 |
| `studio/nodefony/realtime/providers.ts`               |   5 |

## Coûts évidents

- **~8 restarts serveur** + 1 rebuild complet `--force` (~40s). Cause récurrente = la **trappe dist périmé** :
  - orm-core servait un **dist turbo-caché** SANS `connection/health` → `orm:health` muet → faux « ping échec ».
  - `framework`/`http` dist **manquants** → crash boot `ERR_MODULE_NOT_FOUND` au restart.
  - **front HMR en avance sur le back** (endpoint/canal renommés non rebuildés) → « widgets fantômes ».
- `DashboardSupervision.tsx` édité **54×** : design UX très itératif piloté par interruptions user (cards ORM-like, OFF par défaut, masquage widgets, 3-états, ⓘ alertes…). Inévitable en design interactif, mais un mini-cahier des charges en amont aurait réduit la casse.
- `rm -f .git/index.lock` **6×** : husky/lint-staged + hook generate-symbols laisse un `index.lock` quand un commit est interrompu → friction.

## Recommandations

1. **Réflexe « endpoint/canal renommé ne s'affiche pas au runtime » → suspecter le DIST (turbo cache), pas le code.** Vérifier `grep` dans le `dist/` + comparer mtime ; rebuild **direct rollup** (bypass turbo) ou `npm run build -- --force`. Déjà en mémoire ([[feedback_turbo_cache_stale_logs]], [[feedback_dist_*]]) mais a coûté cher → renforcer le réflexe.
2. **Changement back d'un module + serveur tournant = rebuild CE module + restart**, et si incohérence dist → `--force`. (Le `start.sh` « SKIP build dist à jour » ne rebuild QUE le module test.)
3. **MAJ skill `nodefony-studio-dev`** (FAIT cette session) : nouvelles briques + patterns OFF/live, 3-états, ⓘ alertes, debug bar live toggle.

## Patterns récurrents (déjà gérés)

- ✅ Patron sondes+hub répliqué (supervision) — [[project_studio_probes_hub_vision]].
- ✅ Canal paramétré `:<ms>` (granularité) — décision « par canal », à remonter dans la lib ([[project_realtime_granularity_clientlib]]).
- ✅ Revue sécu avant commit (skill) ; 0 any/secret.

## Commits produits

| Commit  | Sujet                                                                |
| ------- | -------------------------------------------------------------------- |
| 4a2ab77 | supervision realtime — sondes process riches + flash + granularité   |
| 1b985b2 | supervision — connecteurs ORM live + UX OFF/alertes + canal clarifié |
| e46fec1 | load-test — scénario stress COMBINÉ HTTP+WS+ORM                      |
| 0703b69 | canal debug bar dédié + ping « en attente » (≠ échec)                |
| 852ae6a | debug bar — bouton temps réel opt-in                                 |
| c4b6aab | debug bar — figer le graphe frames/s quand OFF                       |
