---
date: 2026-06-02
session_id: 3ecfac6b-466a-43ff-9af1-0662df0998f1
focus: Boot de rêve dev (BootReporter) + purge 57 deps mortes + fix help no-arg + audit sync
---

# Session retro — 2026-06-02 — 3ecfac6b

## Fait

- **Purge 57 deps mortes** core (`5b064db`) : clui/node-emoji/rxjs/shelljs/pug/@babel×4/@rollup-plugin-replace
  (+ @types). Code mort retiré (spinner/progress/sparkline, getEmoji, wrappers shell, champ `prompt` Rx).
- **BootReporter « boot de rêve »** (`7fea7e1`) : checklist boot dev par phase (spinner animé TTY / `✓`
  statiques debug & non-TTY), dev-only, backplane-safe, banner consolidé `Kernel.printDevHeader` sous l'ASCII.
- **fix** `npx nodefony` nu → help propre exit 0 (whitelist `commander.help`, `a32690d`).
- **docs migration P11.6** (`0cbfd77`) — barre P11 22→33 %, backlog 3 idées CLI.
- **Audit sync** (demande user) : corrigé dérives mémoire spinner (→ LIVRÉ) + pm2 (retiré C6 ≠ Phase 16).

## Coûts évidents / frictions

- Itération UX TTY très serrée (le user teste en live) → beaucoup d'allers-retours sur le banner (ordre,
  double ASCII = scrollback, ligne Version, debug). Header déplacé reporter→Kernel devSplash (3 itérations).
- Audit dep auto par regex = faux positifs/négatifs (reflect-metadata side-effect, lodash/terser indirects).
- commitlint subject-case (sujet majuscule rejeté) — déjà connu, re-vu.

## Recommandations

1. Avant purge dep : re-vérif `import "x"` + scripts/ + rollup.config (pas juste `from "x"`).
2. Afficher tout header/banner CLI au plus tôt (Kernel devSplash), jamais via un hook tardif (`console.log` ≠ sink).
3. END : MAJ la mémoire de la feature LIVRÉE, pas que le `_state` (sinon dérive « PROCHAINE » périmée).

## Commits produits

| Commit  | Sujet                                              |
| ------- | -------------------------------------------------- |
| 5b064db | chore(core): purge 57 deps mortes                  |
| 7fea7e1 | feat(core): BootReporter (boot de rêve + banner)   |
| a32690d | fix(cli): npx nodefony nu → help exit 0            |
| 0cbfd77 | docs(migration): P11.6 boot UX + backlog idées CLI |
