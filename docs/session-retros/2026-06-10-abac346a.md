---
date: 2026-06-10
session_id: abac346a-a6db-4a61-b38d-24e676236ca4
focus: durcissement cycle requête VAGUE 2 complète (V2.1 gate events + V2.2 requirements pré-compilés + V2.3 micro P8)
---

# Session retro — 2026-06-10 (3ᵉ du jour) — abac346a

## Fait

- **V2.1 (P1)** `55405ff` — gate boot-time des logs d'events lifecycle (`Context.logEvent`) :
  en prod 0 Pdu d'event construit (~5/req HTTP + 3/frame WS supprimés). Events JAMAIS gatés
  (listeners/sondes/realtime intacts). `setLifecycleEventLogging()` exporté = ancrage « Audit
  à chaud ». Fusion `lifecyclePromoted`+`eventSeverity` → 1 flag.
- **A/B mesuré** (4 runs alternés, médianes wrk) : old 6441/6383 → new 6611/6499 RPS ≈ **+2,2 %**,
  séparation nette 2/2 mais < bruit ±3 % → gardé pour la pression ring/GC (règle du plan).
- **V2.2 (P3a)** `4592679` — `Route.compileRequirements()` : `methodsSet`/`methodsAllow`/`varRegexp`
  pré-compilés au boot → 0 alloc + 0 `new RegExp` par match ; fail-fast config (RegExp invalide
  throw à la création de route). Config brute préservée (hash/introspection).
- **V2.3 (P8)** même commit — `ANSI_REGEX` const, `toLowerCase` header, fix `getContentType`
  multi-espaces, 3 dead-checks `"prod"` retirés (vérifié `resolveRuntimeEnv` ∈ {development,production}).

## Frictions

- 2 full builds turbo quand des builds ciblés suffisaient (« le build est long !!! ») → RETEX
  build-turbo incrémenté `[2×]`, recette ciblée notée (module + `npx rollup -c` racine).
- `Edit` refusé ×2 : fichier lu via `sed` Bash ≠ `Read` outil → nouvelle entrée RETEX Shell.
- Piège banc évité : `process.env` par event aurait faussé le run new → const module-level
  (nouvelle entrée RETEX perf).

## Gates

unit framework 190/190 · unit http 445/445 · intégration 425/425 (+1 skip) · mémoire 9/9.
Vérif consommateurs AVANT code (warning user) : realtime 0 couplage Pdu events, bridge
`syslog:stream` dev inchangé, sondes tickers indépendants.

## Commits produits

| Commit    | Sujet                                                                                |
| --------- | ------------------------------------------------------------------------------------ |
| `55405ff` | perf(http): gate boot-time des logs d'events lifecycle (V2.1 — P1)                   |
| `4592679` | perf(framework,http): requirements pré-compilés au boot + micro hot path (V2.2/V2.3) |
