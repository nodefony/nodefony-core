---
date: 2026-07-22
session_id: 7f43c1a7-e3cc-47bc-b2ee-6ea108e860d6
focus: Assainir docs/ + réparer doc-lint (2 régimes) + requalifier les 102 écarts doc↔code + veille normative agents → kit devkit requalifié
---

# Session retro — 2026-07-22 (3ᵉ) — 7f43c1a7

## Fait

- **`BUILDER.md` supprimé** (`13352784`) — spec de build de mai 2026 exécutée puis dépassée
  (43 mentions de Rollup, checklist de 14 tâches « à faire » toutes faites), doublon menteur de
  `build-bundling.md`. 3 index réparés au passage, tous faux depuis ADR-0001.
- **`doc-lint` : 2 défauts corrigés** (`fa0197a8`) — les liens **cités en exemple** comptaient
  comme morts (fences + code inline désormais masqués) ; les `README.md` d'index étaient jugés au
  gabarit d'une page de contenu (4ᵉ régime). Rapport désambiguïsé (basenames dupliqués → chemin).
- **`doc-lint` : régime ADR** (`223eba2c`, skill 2.2.0 → 2.3.0) — 5ᵉ régime : frontmatter ADR,
  `status` dans un cycle de vie fermé, 4 sections Nygard. **Rien à archiver ni supprimer** : un ADR
  garde sa valeur quand sa décision tombe. Corpus 71/85 → 78/85, index 3/7 → 6/7.
- **Requalification INTÉGRALE du registre** (mémoire `54395ee`) — 102 constats, 7 lots parallèles,
  lecture seule : **85 tiennent · 16 corrigés · 1 périmé · 0 invérifiable**. Rapport HTML triable
  `tmp/requalif-ecarts-doc-code.html` (non commité, c'est une photo).
- **Veille normative** (`tmp/specs-agents/`, 11 fichiers) — Agent Skills + AGENTS.md + MCP + A2A,
  tous sous l'Agentic AI Foundation (Linux Foundation).
- **Kit `@nodefony/devkit` requalifié + plan en 8 lots** (mémoire `2701367`), enrichi par une
  analyse « du point de vue de l'agent » (Fable 5).

## Frictions notables

- **J'ai extrapolé un taux de péremption depuis un échantillon saillant** (50 % annoncé, 16 % réel).
  L'échantillon mesurait l'attention déjà reçue, pas la survie. → gradué dans `RETEX.md`.
- **Une décision d'archi figée sur une connaissance périmée** (« les skills sont propriétaires »)
  a contaminé le kit devkit du 14/07 — corrigée seulement parce que le user a posé la question.
- **Le rapport HTML a perdu un item en silence** (`| **F9**  |` a deux espaces) : 101 au lieu de 102.
- **`git add` + `curl` refusé une fois** (reprise immédiate après « pardon continue »).

## Commits produits

| Commit     | Sujet                                                                          |
| ---------- | ------------------------------------------------------------------------------ |
| `13352784` | docs(architecture) : retirer BUILDER.md — une spec exécutée n'est plus une doc |
| `fa0197a8` | fix(doc-lint) : ne plus crier sur l'exemple, ni juger un index comme une page  |
| `223eba2c` | feat(doc-lint) : régime ADR — un texte immuable se vérifie autrement           |
| _mémoire_  | `54395ee` requalif des 102 · `2701367` devkit requalifié + plan                |
