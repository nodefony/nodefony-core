---
date: 2026-09-26
session_id: 74a611c6-eb78-4309-a474-78735ca5800d
focus: CI rouge de la veille réparée, cliquet no-unnecessary-condition étendu au kernel (#498)
---

# Retex 2026-09-26 — 74a611c6

- **Fait** : b7de98259 fix(realtime) — faux kernel du test e2e sans `modules` (CI rouge 6/6 unit
  depuis f72b95bea) ; ed51e3a7a fix(inspect) — `inspect toString` pris pour un sujet
  (`Object.hasOwn`) ; b96791fa3 build(lint) — cliquet étendu au kernel, 114 sites jugés, helper
  `runtime/isTerminal.ts`. #498 commenté. Unit de tous les paquets verts, gate mémoire 11/11.
- **Frictions** :
  - la CI rouge d'hier n'a été vue qu'à la reprise : la clôture précédente n'avait ni lancé les
    suites des paquets appelants, ni lu la CI avant d'écrire le `_state` ;
  - le même piège (faux kernel casté) a mordu une 2ᵉ fois dans la session, attrapé cette fois
    parce que TOUS les paquets ont été lancés ;
  - un `git stash` refusé a emporté l'écriture d'un test dans la même commande (vu au compte 4/5) ;
  - `isTTY === true` puis `Boolean(isTTY)` : chaque forme déclenche une AUTRE règle en
    avertissement tant que le type ment → helper unique au type honnête.
- **Décisions** :
  - rapport doctor relu d'une autre version : `Partial` au POINT DE LECTURE, pas sur l'interface
    (le rapport produit est complet ; l'interface élargie cassait 30 assertions pour rien) ;
  - `Injector.injectables` passe en `Partial` (API publique, 10.0.0 non publiée) — README recalé ;
  - le rouge mémoire Windows de la veille = aléa du runner (vert à la repasse).
