---
date: 2026-06-29
session_id: 7849c48c-95b6-48a9-b470-e7e3518f22a4
focus: Fix « bug WS ~30s »/déconnexion Studio (sonde auth → logout destructif) + config live étendue + ConfigExplorer
---

# Session retro — 2026-06-29 — 7849c48c

## Fait

- `fa78b9b4` **fix déconnexion Studio aléatoire** (chassé 6 sessions) : cause CLIENT =
  `ApiClient.onUnauthorized`→`auth.logout()` POST destructif sur TOUT 401, y compris la sonde
  `/auth/me` (401 = réponse normale « pas connecté »). Un `me` 401 transitoire détruisait une session
  VALIDE + cookie → déconnexion permanente. Fix : exempter `/auth/*` + `clearLocalSession()` non
  destructif. Confirmé live.
- `33cfd1d2` **config live étendue** (http 4→12 champs `runtimeMutable` upload/HSTS, prouvé live) +
  **`<ConfigExplorer>`** partagé (ModuleDetail ↔ page globale).

## Frictions / coûts

- **Rebuild-par-instrumentation = coût #1** : chaque log backend = ~4 min turbo + sessions vidées +
  re-repro d'un bug aléatoire. A prolongé la chasse. → chantier debug-runtime.
- **turbo « FULL TURBO » sans rebuilder un fichier édité** → `--force` + vérifier le marqueur dans le dist.
- **J'ai instrumenté une hypothèse (pont WS) avant le goulot (store session)** → cycles perdus.
- **Mémoire disait « bug ~30s prouvé sain »** → FAUX. Ne pas confondre hypothèse et preuve.

## Enseignements gradués (mémoire IA)

- `feedback_debug_instrument_choke_point` — instrumenter le GOULOT d'abord, log négatif, question métier user.
- `project_runtime_debug_toggle_kit` — chantier debug à chaud par-module (NEXT).

## Décision / avis demandé

- Kernel isomorphe pour debug front : bon sur le PRINCIPE (Core déjà isomorphe), à faire APRÈS migration,
  en INCRÉMENTAL (bus de debug isomorphe, pas un mini-kernel complet). Détail dans le `_state`.

## Commits produits

| Commit     | Sujet                                                    |
| ---------- | -------------------------------------------------------- |
| `fa78b9b4` | fix(studio) 401 sonde auth ≠ logout destructif           |
| `33cfd1d2` | feat(config) live étendue (upload+HSTS) + ConfigExplorer |
