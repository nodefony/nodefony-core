---
date: 2026-06-08
session_id: f04b5d07
focus: Durcissement WebSocket complet (G2 heartbeat + G1 backpressure + G3 fragmentation/latence)
---

# Session retro — 2026-06-08 (S6) — durcissement WebSocket

## Fait

- **G2 heartbeat keep-alive** (`6babbe7`) : `ws@8` n'a 0 keep-alive natif (les knobs `keepalive*` étaient
  un reliquat de l'ancienne lib `websocket` theturtle32 → config menteuse). Helper `wsHeartbeat.ts` =
  1 `setInterval`/serveur (jamais 1/conn, ≠ ancienne lib), `terminate()` half-open. + toutes les
  `ServerOptions` ws@8 déclarées+câblées. Half-open live 36s.
- **G1 backpressure sortante** (`081b8b3`) : `Response.send()`/`broadcast()` gatent `decideSend(ws,max,policy)`
  AVANT `client.send()` (0 alloc sous seuil). Config `maxBackpressure` 4 MiB + `backpressurePolicy`
  `drop`(déf)/`close` 1013. Démo live : flood 17.6 MiB à un lecteur paused → drop à 4 MiB, reste OPEN, 0 OOM.
- **G3 fragmentation/latence/audit** (`13552db`, 0 code prod) : test fragmentation RFC §5.4 + ping interjeté ;
  banc latence p50/p95/p99 ; audit deflate (maxPayload borne la décompression → zip-bomb mitigé).
- **3/3 trous WS fermés.** Gates : unit ws 9, WS intég 90/90, mémoire 9/9, load 27/29. 8 commits poussés
  (3 WS + 5 ORM antérieurs) `11bd923..13552db`.

## Décisions

- Backpressure `drop` par défaut (aligné « superviser ≠ tomber la prod ») ; `close` 1013 en opt-in ;
  `coalesce` renvoyé à la **couche canal realtime** (pas transport, le framework ne connaît pas le payload).
- Zombie = `terminate()` (pas `close()` : pair mort, pas de handshake possible).

## Frictions (→ RETEX.md)

- **Branche** : chantier WS committé/poussé sur `refactor/orm-hardening` (ORM) → signalé par le user.
  Leçon : START doit brancher si le chantier ≠ nom de branche. Non réécrit (déjà poussé, même cible merge).
- `ws.Sender.frame` absent du default ESM + de `@types/ws` → `import * as ws` + cast. Echo route JSON-encode
  - envoie `{handshake:true}` d'abord → consommer le handshake, fragmenter un objet JSON.
- `vitest run` silence `console.log` → asserter une borne (p99 < N), pas grep la mesure.
- Flood demo gonfle `/tmp/nodefony-server.log` (5 MiB) + ENOSPC transitoire capture → `truncate -s 0`.

## Commits produits

| Commit  | Sujet                                                      |
| ------- | ---------------------------------------------------------- |
| 6babbe7 | feat(http): heartbeat keep-alive WS (G2) + options ws@8    |
| 081b8b3 | feat(http): backpressure WS sortante (G1) drop/close       |
| 13552db | test(http): durcissement WS G3 fragmentation/latence/audit |
