---
date: 2026-07-12
session_id: 314aadd0-5c30-45e3-a17a-d5c63f3183da
focus: scaffold create app v3/v4 (3 fronts, frontend react/vue/angular, --link) + résilience dev durcie par le banc user tmp/mon-app
---

# Session retro — 2026-07-12 — 314aadd0

## Fait

11 commits (`414d30f5` → `7c2f7e19`) — deux fils entremêlés :

1. **Scaffold v3/v4** : `--link` (npm install réel sans release), outillage
   complet généré (compose docker, tests vitest unit+e2e, eslint+prettier),
   architecture 3 fronts (spec déclarative JSON-able + moteur pur eta +
   interactif readline défaut TTY), choix frontend react|vue|angular (layers),
   presets complete|minimal, coquille index.html templatable, home react
   design Studio (logo, slogan, 3 piliers, cards interactives).
2. **Résilience issue du banc réel** (le user a cassé l'app en vrai, chaque
   crash = un fix) : garde fail-loud install/build au boot, superviseur
   standalone sans turbo, multi-projet (scoping cwd — l'app 2 TUAIT l'app 1),
   readiness any-port (3 occurrences du même faux négatif), Ctrl+C ≠ crash
   Vite, rappel build post-boot, dedupe react (2 node_modules --link = page
   blanche), debugbar fs.allow.

## Frictions / leçons (versées au RETEX.md)

- App `--link` = 2 node_modules = classe de bugs invisible dans le repo.
- Liste de ports sondés = convention, pas topologie (ready = ≥1 port).
- Page démo sans interaction = perçue cassée.
- Log détaché : vérifier l'exit du launch avant de grepper.
- cwd Bash dérive : gradué en [[feedback_bash_cwd_drift]] (4×).
- La console navigateur départage en 1 passe ce que 10 curls ne voient pas
  (Invalid hook call invisible côté serveur).

## Méthode gagnante

Le user teste EN RÉEL pendant que je code : chaque retour terminal/console
était un bug objectif reproductible. Cadence fix→preuve→commit (11×) sans
aucun faux diagnostic une fois la console navigateur obtenue.

## Commits

| Commit   | Sujet                                                     |
| -------- | --------------------------------------------------------- |
| 414d30f5 | create app v3 — outillage complet + --link                |
| afac3046 | scaffold 3 fronts — spec + moteur + interactif + frontend |
| f97fa3e5 | garde fail-loud boot (install/build)                      |
| 1896f4c0 | superviseur standalone sans turbo                         |
| 423da772 | multi-projet — scoping cwd (jamais tuer l'autre app)      |
| 33ece54d | readiness any-port + READY honnête                        |
| 369b19eb | ctrl-C ≠ crash Vite                                       |
| 8f23f451 | rappel build post-boot + guide policies                   |
| 2764c58d | debugbar 403 + coquille index.html                        |
| 15e8965a | dedupe react/vue/angular (--link 2 node_modules)          |
| 7c2f7e19 | home react design Studio                                  |
