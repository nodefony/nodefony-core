---
date: 2026-08-02
session_id: 254d5ee4-d45b-45e8-af8a-8544aa5b32ca
focus: Surface npm de Studio (190 Mo de front en dépendances d'exécution) puis remise à niveau des dépendances périmées
---

# Session retro — 2026-08-02 — 254d5ee4

## Fait

- **D1 — `02799504`** : les 23 paquets d'interface de Studio passent de `dependencies` à
  `devDependencies`. Le front est construit au `prepack` et les sources ne sont pas publiées :
  aucun n'est résolu à l'exécution. 190 Mo de dépendances directes en moins pour qui installe,
  259 paquets basculés en `"dev": true` dans le lock. Prouvé sur le tarball (0 fichier
  `frontend/`, 250 assets et `index.html` toujours là). Un test verrouille la règle, écrit en
  implication — vu rouge en remettant `react`.
- **Dépendances périmées** : 12 paquets montés (1 MAJOR de rattrapage `@types/better-sqlite3`
  7→9 pour un runtime en 13, 3 MINOR, le reste en PATCH). `typescript` laissé en 6.0.3.
- Cadrage de **R1.2** : plus petit que craint, le dialecte n'est figé nulle part dans le code
  généré (`NF_DATABASE_URL`, dialecte déduit du scheme).

## Coûts évidents

- Un `npm install` complet par correction de manifeste (3 passes) — la première n'ayant monté
  que 3 des 4 sites déclarant `tsx`, la deuxième que ce que le lock voulait bien, la troisième
  après le signalement de `turbo` par le user.
- Le rapport du sous-agent a dû être recontrôlé : la commande dictée était aveugle à la racine.

## Recommandations

1. **Un gate générique « dépendance d'exécution jamais importée »** dans
   `kernel/checks/packageDeps.ts` — il servirait aussi `nodefony check` dans les applications.
   Le test posé cette session ne couvre que Studio, or la faute s'est produite deux fois en deux
   jours (ici et dans les gabarits de `create app`).
2. **Relever tous les sites déclarants avant d'éditer le premier** — vaut pour toute montée de
   version dans un dépôt à espaces de travail.
3. **Ne pas dicter la commande à un sous-agent sans avoir vérifié son périmètre** — il rend un
   rapport exhaustif sur un périmètre amputé, sans que rien ne le signale.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact reçu (tarball) et non sur le manifeste — `feedback_prove_on_received_artifact`.
- ✅ Un test neuf vu rouge en débranchant — `feedback_gate_must_bite`.
- ✅ Le modèle du sous-agent annoncé dans le champ `description` — `feedback_subagent_model_in_label`.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `02799504` | fix(studio): 190 Mo de front partaient chez qui installe le paquet |
