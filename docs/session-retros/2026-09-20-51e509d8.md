---
date: 2026-09-20
session_id: 51e509d8-bbfd-4f27-9cc2-7bf265e1129b
focus: #60 livré, trois bugs réels trouvés par la campagne de test, CI remise au vert
---

# Session retro — 2026-09-20 — 51e509d8

## Focus

Reprise sur #60 (rôles Studio lus du serveur), puis campagne de test complète sur demande du
user — les six étages, jusqu'au backplane multi-pods. La campagne a trouvé trois bugs réels que
les suites vertes ne montraient pas.

## Fait

- **#60 fermé** — `Users.tsx` lit la hiérarchie du serveur, `STUDIO_ROLES` retiré du navigateur.
- **#159 détaché de #155**, **#155 fermé** (4 sous-tickets porteurs soldés). Jalon alpha 4 → 2.
- **#159 corrigé** sur quatre écarts, dont une preuve d'absence **fausse à l'écriture**.
- **Trois bugs réels corrigés et poussés** (détail ci-dessous).
- **Campagne complète** : CI 8/8 verte, étages A / A+ / B / C1 / C2 / C3 / E / F joués.
- **Rapport** `tmp/campagne-test.html` (58 Ko, autonome).

## Les trois bugs — aucun visible d'une suite verte

1. **2FA self-service morte** (`d674d76f`). Un `ROLE_USER` authentifié recevait **403** sur
   `totp/enroll`. Le durcissement de zone de `e8b2e200` exige `ROLE_NODEFONY_ADMIN` par défaut sur
   tout le plan de données ; la famille self-service était nommée dans le TSDoc d'`areaRoleExempt`
   (`me`, `sessions/mine`, `keys`) — TOTP manquait au balayage.
2. **`nodefony see --help` plantait** (`ab037beb`). Le drapeau pris pour un chemin de page →
   `https://127.0.0.1:5152--help`. Le banc qui l'exige existait et nommait le trou.
3. **Deux mots de passe de développement** (`3aef2985`). `secret-de-dev-42` (persistant) contre
   `secret` (mémoire), selon un réglage invisible. Écran de connexion faux, 7 recettes du skill
   navigateur et 5 scripts de charge cassés — sans qu'un seul test tombe.

## Frictions

- 🔴 **Trois verdicts faux, tous de MON instrument** : un moniteur qui lisait 6 workflows sur 7
  (« tous ont rendu » sur liste tronquée), un motif `rg` trop étroit (4 fichiers manqués, deux
  passes), deux bancs lancés sans décor (`verify-runtime` sans `--keep` ; suite de charge privée
  du serveur qu'elle réclame → 7 fichiers en `ECONNREFUSED`).
- 🔴 **Un rouge était mon diff** : le filet de zone voyait `totp/status` s'ouvrir à un `ROLE_USER`.
  Garde-fou légitime — déclaré dans `JOIGNABLES_SANS_ROLE`, pas contourné.
- ⚠️ **`clusterIpc.e2e` / `redisCluster.e2e`** échouent systématiquement en passe complète, passent
  systématiquement seuls. Incompatibilité structurelle, pas un flake — à ticketer.
- ✅ Ce qui a marché : l'arbre de décision du skill `nodefony-test-campaign`. Sur 24 rouges,
  **3 appartenaient au produit** ; l'arbre a écarté les 21 autres sans les imputer au code.

## Mesures

| Mesure                               | Valeur                                    |
| ------------------------------------ | ----------------------------------------- |
| Passe la plus large (dialectes)      | 12 549 ✓, 0 ✗, 230 sautés                 |
| WS — plafond propre                  | 6 520 msg/s (décrochage au burst 200 000) |
| WS — aller-retour micro-frames       | p50 0,27 ms · p99 0,86 ms                 |
| Backplane Redis — fan-out cross-pod  | 5 974 msg/s · p99 49 ms                   |
| Multi-pods — livraisons              | 50 000 / 50 000, perte 0 %                |
| Multi-pods — latence hors saturation | p50 3 ms                                  |

## Non fait

- ~24 bancs du catalogue `nodefony-load-test` · chaîne de publication (`nodefony-release`).
- Installation pnpm / yarn / bun → **#294**, jalon beta.
- Ticket pour l'isolement des bancs cluster.

## Commits produits

| Commit                 | Sujet                                                                      |
| ---------------------- | -------------------------------------------------------------------------- |
| `a2a4111c`             | fix(studio): lire la liste des rôles depuis le serveur (#60)               |
| `3aef2985`             | fix(dev): n'avoir qu'UN mot de passe de fixture, et le prouver             |
| `d674d76f`             | fix(security): rendre à l'utilisateur la gestion de SON second facteur     |
| `b11873c6`             | test(realtime): porter les deux échelles de rôle, comme la fixture réelle  |
| `ab037beb`             | fix(cli): faire répondre `nodefony see --help` au lieu de le faire planter |
| `ec1cfd6a`, `fb9400db` | chore(pilotage): empreintes                                                |
