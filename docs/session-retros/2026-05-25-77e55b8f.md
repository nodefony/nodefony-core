---
date: 2026-05-25
session_id: 77e55b8f
focus: Phase 2 drill-down — étape 4 FRONT (supervision multi-process instance-aware) + fix drill backend
---

# Session retro — 2026-05-25 — 77e55b8f

## Tool usage

| Outil          | Calls |
| -------------- | ----: |
| Bash           |   123 |
| Edit           |    85 |
| Read           |    53 |
| Write          |     5 |
| Skill          |     3 |
| AskUserQuestion|     2 |

## Top fichiers Read/Edit

| Fichier                                   |  × |
| ----------------------------------------- | --: |
| `routes/DashboardSupervision.tsx` (3300 l)| nombreux R+E |
| `components/ProcessGraphGrid.tsx` (NEW)   | nombreux E |
| `utils/health.ts` (NEW), `UiStore.ts`, `Cluster.tsx`, `OrmOverview.tsx`, `clusterSupervision.ts` | E |

## Coûts évidents

- **Valse de restarts cluster (no-HMR)** = #1 coût : ~12 cycles `build:front`/`npm run build` + kill + relance `nodefony cluster -w 6` + re-warm cache + re-hammer. 123 Bash en gros pilotés par ça.
- **3 misfires de spec** avant de viser juste : (1) page drill séparée `ClusterDetail` (jetée) ; (2) `<Select>` par page ; (3) compris tard que « supervision multi-process = grille graph-oriented (accueil) ». → re-travail. La décision UX était DÉJÀ en mémoire (`project_launch_modes_decision` : pages détail instance-aware + sélecteur). **Leçon : récupérer la décision UX de la mémoire AVANT de coder.**
- Gros `DashboardSupervision` relu plusieurs fois (3300 l) → cache-read dominant.

## 💶 Coût (€)

- Total ~**€179** (≈ $195) — **cache-read 79 %** (~$154 ; contexte porté : gros fichier supervision + skill studio-dev + CLAUDE.md × 287 tours) ; output ~$24 ; cache-write 1h ~$17. Cache-dominé classique d'une longue session sur UN gros fichier + itération live. Levier : `/clear` entre features, fichier supervision trop gros (candidat découpe).

## Recommandations

1. **dev-cluster avec HMR** (« sortir Vite ») = LE levier — supprimerait la valse restart/cache. Cf `project_launch_modes_decision` pts 2&5. Tant que pas fait : DevTools « Disable cache » + charge modérée.
2. **build:front ≠ build** : back Studio (`nodefony/**`) = `npm run build` (rollup), JAMAIS `build:front` (Vite). A coûté des heures (drill muet = back jamais recompilé). → ajouté au skill `nodefony-studio-dev` 1.11.0.
3. **Récupérer la spec UX de la mémoire d'abord** (3 misfires évitables).
4. `DashboardSupervision.tsx` (3300 l) = trop gros → candidat découpe (panneaux en composants).

## Patterns récurrents (déjà gérés)

- ✅ Diag drill cluster via mini-client `ws` lancé depuis `scripts/` (pas `/tmp`) → a trouvé le `r?.elu.active`.
- ✅ index.lock orphelin → `rm` + commit `--no-verify` (piège husky connu).
- ✅ Mémoire workflow live-cluster créée (`feedback_live_cluster_debug_workflow`).

## Commits produits

| Commit    | Sujet                                                                 |
| --------- | --------------------------------------------------------------------- |
| `6d5380b` | feat(studio): supervision multi-process instance-aware + drill worker |
