---
date: 2026-09-25
session_id: d06ea516-80b8-4ce0-8daf-5cba676f97af
focus: ré-audit doc G15/G16 + security, #275, préparation alpha.9
---

# Retex 2026-09-25 — d06ea516

- **Fait** : `82537edb0` docs G15/G16 (3 routes documentation, Vue isolée, 5 presets, 8 skills
  devkit, 37 ancres) · `c0eba2f5d` fix(release) #275 FERMÉ (`perimetrePublie`) · `4fee72653`
  docs(security) 17 pages (~330 ancres, 6 faits faux dont `markUsed` qui trace bien IP/agent) ·
  `fa9e55916` skill release. Estampille alpha.9 préparée puis REMISE à HEAD, changelog réécrit
  gardé dans `tmp/release-alpha9-changelog.md`.
- **Frictions** : `doc:anchors` tolère un décalage de fenêtre → ~330 ancres fausses vues « OK » ;
  applicateur d'ancres fautif deux fois (chaîne de remplacements, séparateur `|`) — versées au sas
  sous 🎯.
- **Décisions** : #275 = `files` + sources compilées (hors tests/réglages de build), pas `files`
  seul — sinon tout `feat(http)` sur `nodefony/**/*.ts` serait écarté. Un export ajouté puis retiré
  dans la même fenêtre de release n'est pas une rupture. `config` est bien émise (KernelAdminApi) :
  trois catégories d'audit réservées, pas quatre.
