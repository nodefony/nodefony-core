---
date: 2026-07-25
session_id: cb355883-57d4-44b4-8580-36c3fabd20ca
focus: les 3 bugs terrain du devkit, l'index des colonnes de jointure, et l'ouverture du kit « entité User à l'app »
---

# Session retro — 2026-07-25 — cb355883

## Fait

- **Les 3 bugs signalés en livrant devkit S4** (`c83e3b39`) : nom d'entité réservé
  refusé avant écriture, `drizzle-orm` déclaré par l'application, readiness
  `--detach --wait` instruite. Le garde d'identité existait déjà mais n'était
  prouvé par aucun test ; la vraie cause du « 404 partout » était ailleurs — le
  glissement de ports `portPolicy: "auto"` était MUET.
- **Trou de sécurité fermé, sur alerte du user** : la reconnaissance d'un runtime
  cherchait son titre DANS la ligne de commande → un `tail` innocent classé
  serveur de production, que `stop --all` aurait tué. Titre ancré en tête,
  seconde preuve exigée pour `--all`, filet shell du skill aligné (un listener
  tiers survit désormais, il était tué avant).
- **Une colonne de relation naît indexée** (`ea8c6ced`) — c'est par elle que
  passent `?include=` et les jointures. L'index n'est pas la FK : un `JOIN`
  n'exige aucune contrainte.
- **La colonne `user` des sessions était filtrée sans index** (`32a26a6c`) alors
  que le TSDoc promettait un « WHERE indexable » ; l'écran d'administration
  balayait la table à chaque ouverture. Preuve rejouée sur les 3 dialectes.
- **L'AGENTS.md généré apprend les utilisateurs et les droits** (`d55b0857`) :
  quatre gestes nommés (`@IsGranted`, `@CurrentUser`, `roleHierarchy`,
  `security:user:add`) + le voter comme point d'extension.
- **Kit ouvert** `project_user_entity_roles_kit` + dette inscrite au dashboard
  (`1bcd441a`), et mémoire périmée corrigée : **les voters EXISTENT**.
- **Rapport HTML** `tmp/dettes.html` — 33 dettes ouvertes lues dans le dashboard,
  avancement devkit, trous restants.

## Frictions (détail dans RETEX.md)

- Une preuve négative FAUSSE par persistance de la base (PG/MySQL gardent leurs
  tables entre les runs) — elle constatait un index de la veille.
- Le geste de débranchement lui-même inopérant en silence (`DROP` via `all()`).
- Un gate ancré sur un mot qui apparaît ailleurs dans le texte : vert malgré le
  débranchement.
- L'API d'une lib maison supposée au lieu d'être lue : onglets vides, cellules
  non échappées — et un script qui « réussit ».
- 515 tests drizzle skippés faute d'infra : c'est le user qui a demandé « on est
  sûrs ? ». Infra montée → 900 verts sur les 3 dialectes.

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `c83e3b39` | les 3 bugs terrain du devkit (+ identification des runtimes)        |
| `ea8c6ced` | une colonne de relation naît indexée                                |
| `32a26a6c` | l'index manquant sur `session.user` (+ docs IA des modules touchés) |
| `d55b0857` | l'AGENTS.md généré apprend les utilisateurs et les droits           |
| `1bcd441a` | dashboard : trous devkit fermés + dette « l'entité User »           |
