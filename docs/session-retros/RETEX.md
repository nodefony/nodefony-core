# RETEX.md — digest des retours d'expérience (SAS, lu à chaque début de session)

> **Rôle** : ce fichier est le **sas** entre les retex bruts (`docs/session-retros/<date>-<id>.md`,
> jamais relus seuls) et les leçons durables (mémoires `feedback_*` indexées dans `MEMORY.md`).
> Il porte les **frictions récentes pas encore confirmées** (vues 1-2×). Le skill `nodefony-session`
> le **lit au START/RESUME** et le **met à jour au END** (ajout de 3-5 bullets du jour, par thème).
>
> **Règle anti-doublon (CRITIQUE)** : une leçon est **soit** ici (sas, non confirmée), **soit** en
> `feedback_*` (graduée, prouvée). **JAMAIS les deux.** Quand une friction atteint **3×** → mode
> CONSOLIDATE la promeut en `feedback_*` et la **retire d'ici**. Sinon dérive garantie (cf l'anti-pattern
> « liste dupliquée » que dénonce `nodefony-check-externals`).
>
> **Taille bornée** : ce fichier ne grossit jamais. Deux sorties (gérées par CONSOLIDATE, tous les
> 10-20 retex) : (a) friction ≥3× → graduée en `feedback_*` puis retirée ; (b) retex bruts vieillis →
> `archive/` + 1 ligne de résumé ici. Cible : ~1 écran. Format = bullet `[N× — date courte]` par thème.

---

## 📦 Dépendances / upgrade

- `[1× — 2026-07-10]` **Un « minor » d'outil peut casser la COMPILATION sans casser une assertion.** `vite 8.0.16 → 8.1.4` tire `rolldown 1.1.5`, dont `OxcOptions` **omet `tsconfig`** → notre `experimentalDecorators`/`emitDecoratorMetadata` n'est plus lu, `decorator.legacy` retombe sur `false`, les décorateurs sortent **bruts** et Node lève `SyntaxError: Invalid or unexpected token`. Symptôme trompeur : **fichiers morts en COLLECTE, 0 test failed** (`Test Files 2 failed | 65 passed` / `Tests 1705 passed`). Réflexe : un `Test Files failed` avec `0 Tests failed` = échec de transformation, PAS de logique → lire la sortie transformée (`server.transformRequest()` via l'API vite), pas la stack. Correction = `oxc: { decorator: { legacy: true, emitDecoratorMetadata: true } }` (source unique `vitest.oxc.ts` racine), **pas** un gel de version.
- `[1× — 2026-07-10]` **Vérifier `emitDecoratorMetadata`, pas seulement `legacy`.** L'injector résout par `design:paramtypes` en fallback ⇒ si la metadata n'est pas émise, l'auto-injection casse **silencieusement** (param `undefined`, aucune erreur). Toujours prouver la présence de `design:paramtypes` **avec le vrai type** dans la sortie compilée, jamais se contenter de « les tests passent ».
- `[1× — 2026-07-10]` **Un bump de MAJOR peut être un changement de NATURE du paquet.** `typescript@7` (port Go) ne publie plus d'API JS (`main: null`, pas de `lib/typescript.js`, `ts.createProgram === undefined`) → `@rollup/plugin-typescript`/`typescript-eslint`/`typedoc` cassent. Avant d'évaluer un major d'outillage : `jq '{main,types,exports}' node_modules/<p>/package.json` + un `import()` réel, AVANT de lire le changelog.
- `[1× — 2026-07-10]` **Réécrire des pins par `String.replace()` sur le texte du package.json touche la PREMIÈRE occurrence**, qui peut être dans `peerDependencies` et pas `devDependencies` (vécu : peer bumpé, devDep laissé → divergence). Réécrire par **bloc** (`npm pkg set "devDependencies.x=v"`), et auditer après coup avec `jq` bloc par bloc contre `git show HEAD:<f>`.
- `[1× — 2026-07-10]` **`npm outdated` ne voit pas les `peerDependencies` pinnées exactes** et `npm update` ne bouge pas un pin exact. Le lot « tout à jour » peut laisser des peers en arrière. Inventorier les 3 blocs.
- `[1× — 2026-07-10]` **Un alias npm ne renomme PAS le binaire.** `npm i -D tsgo@npm:typescript@7` a écrasé `node_modules/.bin/tsc` avec TS 7 (silencieux : `npx tsc --version` → 7.0.2). Puis `npm uninstall tsgo` a **délogé notre `typescript@6.0.3` hoisté** et exposé un `5.7.3` transitif. Deux règles : (a) pour deux versions du même outil, prendre un paquet au **binaire distinct** (`@typescript/native-preview` → `tsgo` ; `@typescript/typescript6` → `tsc6`) ; (b) **retirer un paquet de test par `git checkout package.json package-lock.json && npm install`, jamais `npm uninstall`**. Vérifier APRÈS install : `npx tsc --version` ET `require("typescript").createProgram`.
- `[1× — 2026-07-10]` **Un outil peut MASQUER un défaut du source.** rolldown a révélé que `@nodefony/http` **s'importe par son propre nom** (`Request.ts:28`) → il avale son `dist` (65→120 fichiers). Rollup ne bronchait pas. Corollaire : changer d'outil = **audit gratuit**. Externaliser systématiquement le nom propre du paquet.
- `[1× — 2026-07-10]` **`declarationDir` du tsconfig ÉCRASE `--outDir` de la CLI** : un `tsc --emitDeclarationOnly --outDir /tmp/x` a écrit dans le VRAI `dist/client/types` du repo. Passer `--declarationDir` explicitement pour toute génération de `.d.ts` hors-build.

## 🔧 Migration d'outillage (exécution)

- `[1× — 2026-07-17]` **« Consommé en SOURCE » ne vaut que pour les TYPES — le runtime vient du `dist`.** `exports.types = "./index.ts"` (pattern anti-race du CLAUDE.md) fait croire qu'éditer orm-core suffit : le typecheck voit tout de suite le nouvel opérateur, mais `isFieldOperators` est une **fonction**, résolue par `main: ./dist/index.js`. Sans `npm run build` d'orm-core, l'ancien `OPERATOR_SET` tourne → `{ $null: true }` n'est plus reconnu comme opérateur, part en égalité, et drizzle tente de binder un objet : `RangeError: Too few parameter values were provided` — une erreur qui ne parle **jamais** du vrai problème. Réflexe : toucher un helper RUNTIME d'un paquet dépendant = rebuild ce paquet, même s'il est « consommé en source ».
- `[1× — 2026-07-17]` **Avec `preserveModules`, `dist/index.js` n'est qu'un barrel** : `grep '$null' dist/index.js` → 0, alors que le code est dans `dist/nodefony/src/criteria.js`. Vérifier un dist = `grep -rl` sur `dist/`, jamais sur le seul `index.js` (j'ai failli rebuilder deux fois pour rien).
- `[1× — 2026-07-10]` **Purger une dep = grep TOUS les blocs `scripts`, pas des clés devinées.** Le sweep lot 5 a couvert `build`/`dev`/`rollup` mais raté `build:force` (`rollup -c` → `command not found`, trouvé par le user au premier build). Réflexe : `jq '.scripts | to_entries[] | select(.value | test("<dep>"))'` sur les 21 package.json, puis grep global hors node_modules.
- `[1× — 2026-07-10]` **`npm pkg delete` est inconsistant en workspaces** (certains dirs silencieusement non modifiés dans une boucle). Pour une purge de masse fiable → `jq 'with_entries(select(.key|test(...)|not))'` directement sur les fichiers, puis UN `npm install` de resync.
- `[1× — 2026-07-10]` **Retirer un fichier d'un `include` tsconfig peut déplacer la racine commune** (TS5011 tsgo) : `tsconfigClient` sans `rollup.config.ts` → racine `./src` → layout `dist/client/types` cassé vs exports map. Fix = `rootDir` explicite. Vérifier le LAYOUT émis, pas juste l'exit 0.

## 🧪 Méthode de comparaison de builds

- `[1× — 2026-07-10]` **Comparer deux builds : la surface exportée par IMPORT RÉEL, pas le nombre de fichiers.** Le compte de `.js` ment (chunks vides, granularité de tree-shaking : rolldown 951 fichiers vs Rollup 854 dont 76 VIDES ; frontend 40→104 fichiers mais 290→237 Ko). Ce qui fait foi = `Object.keys(await import(dist/index.js))` avant/après. ⚠️ **Dans des PROCESS ISOLÉS** : charger deux builds du même paquet dans un process explose sur les registres globaux (`EntityRegistry: entity "session" already registered`).
- `[1× — 2026-07-10]` **Ne pas conclure « X est meilleur » sur un grep mal borné.** Deux faux positifs en une session : `grep -rl "node:fs" dist/client` matchait les `.d.ts` (pas les `.js`) → fausse « fuite de builtin » ; et `xargs` mange les guillemets → `import * as React from react` (code invalide) alors que la sortie était correcte. Toujours restreindre l'extension ET relire la ligne brute.
- `[1× — 2026-07-10]` **Un `grep` de plugins dans un `rollup.config.ts` compte le code COMMENTÉ** (`copy(`/`terser(` désactivés) et les faux amis (`replace(` = `String.prototype.replace`). Pour savoir ce qui est vivant : filtrer les lignes `^\s*//` et grepper les **imports**, pas les appels.

## 🧪 App générée `--link` (banc de test réel)

- `[1× — 2026-07-14]` **Un générateur se teste en EXÉCUTANT ce qu'il génère, pas en testant le générateur.** `create entity` : 85 tests verts sur le scaffold… et **5 bugs** sortis en exerçant le code produit dans une vraie app (`@HttpCode(204)+return null` qui PEND jusqu'au timeout · `IResourceService.find()` sans options → pagination impossible · import `default` sur la 2ᵉ entité → build KO · `--connector` non porté par l'entité → tables créées dans la MAUVAISE base en silence · fermeture de table avalée par le commentaire d'un champ `ref` en dernier). Aucun n'était visible côté générateur. **Gate d'un scaffold = typecheck de l'app générée + boot + CRUD réel + tests générés**, jamais les seuls tests du moteur.
- `[1× — 2026-07-14]` **Appliquer un VRAI schéma tiers est le meilleur banc** : WordPress (12 tables, 94 colonnes, relation en dernière position, `--no-timestamps`) a déclenché 3 des 5 bugs ci-dessus, qu'aucun exemple maison n'atteignait (nos exemples avaient toujours des horodatages après le dernier champ). Un schéma réel a des formes qu'on n'invente pas.
- `[1× — 2026-07-14]` **Ne JAMAIS déléguer le wiring d'un artefact à celui d'un AUTRE** : `wireEntitiesDecorator` réutilisait `wireDecoratorList` (écrit pour les controllers → import **default**) alors qu'un descripteur d'entité est une **const nommée** → `MISSING_EXPORT "default"` sur la 2ᵉ entité seulement (la 1ʳᵉ passait par un chemin dédié, correct). Deux artefacts = deux conventions d'export = deux wirings.

- `[1× — 2026-07-12]` **Une app `--link` a DEUX node_modules (app + checkout) = classe de bugs INVISIBLE dans le repo self-hosted** (node_modules unique hoisted). Vécu 2 fois le même jour : react en double (« Invalid hook call », page blanche → `resolve.dedupe`) et debugbar en 403 (realpath du paquet hors `fs.allow`). Réflexe : toute feature front se teste AUSSI depuis une app générée `--link`, pas seulement dans le repo.
- `[4× — 2026-07-13]` **Une liste de ports sondés est une CONVENTION, pas la topologie de l'app.** 4ᵉ occurrence (readiness `--wait`, `reportReady` superviseur, rapport READY, puis : la readiness `--detach` sondait `[5151,5152]` et le **state file n'était PAS publié en production** → une app sur `NF_PORT=8080` bootait très bien, la sonde ne voyait rien, épuisait son plafond de 120 s et **group-killait un serveur sain**). Sémantique juste : ready = AU MOINS UN port + **le serveur PUBLIE ses ports effectifs** (canal `runtime.json`, tous environnements), les lecteurs les prennent à la source. **≥3× → À GRADUER au prochain CONSOLIDATE** (règle : ne jamais SUPPOSER une topologie qu'on peut se faire DIRE).
- `[1× — 2026-07-12]` **Une page démo sans interaction se lit comme une page cassée.** Le user a conclu « react ne marche pas » devant une page qui MARCHAIT (h1 + JSON) mais n'avait ni bouton ni mouvement. Une vitrine générée doit prouver qu'elle est vivante : action utilisateur (bouton, input WS), pas juste du texte statique.
- `[1× — 2026-07-12]` **Ne jamais grepper le log d'un run détaché sans avoir vérifié l'exit du lancement** (sortie redirigée → un launch refusé laisse l'ANCIEN log en place → faux diagnostic « le fix ne marche pas », vécu). `rm` le log avant relance OU vérifier `exit=0` d'abord.
- `[2× — 2026-07-12 soir]` **Le runtime résout l'APP depuis process.cwd() → tout lancement depuis un sous-dossier boote un « projet fantôme »** (le package http pris pour l'app : 1 module, bind ::1, config défauts, ZÉRO warning). Vécu 2× dans la même session (start.sh puis `--write` de security:secrets qui a créé un `.env.local` dans le package). Réflexe : `cd <racine> &&` dans le MÊME appel avant tout `node bin/nodefony` ; le `cd "$ROOT"` est maintenant DANS start.sh.
- `[2× — 2026-07-13]` **Un filet de secours doit copier la RÈGLE EXACTE du chemin principal, pas une approximation** : 1ʳᵉ occurrence = filet `kill -9` par port non scopé (serveur d'une AUTRE app tué) ; 2ᵉ = filet « corrigé » mais par PRÉFIXE `"$ROOT"/*` alors que `splitByProject` compare en ÉGALITÉ stricte → l'app IMBRIQUÉE `tmp/cci-app` (contrôle scaffold du user) classée « autre projet » par `stop` puis SIGKILLée par le filet. Deux implémentations d'une même règle de scoping = dérive garantie (anti-pattern liste dupliquée, version kill). Fixé : filet strict + rapport `stop` visible (il partait dans /dev/null) + message superviseur « signal EXTERNE » actionnable.
- `[2× — 2026-07-14]` **eta `autoTrim: "nl"` mange le newline après CHAQUE tag** → 3 lignes `KEY=<%= v %>` sortent sur UNE ligne. Un bloc de lignes générées = UN tag avec `join("\n")` **ET un `\n` FINAL** : sinon la ligne SUIVANTE du template (`});`) remonte sur la dernière ligne rendue — anodin tant qu'elle finit par une virgule (`…notNull(),});` reste valide, donc **invisible**), fatal dès qu'elle finit par un commentaire (`// → User.id …});`). 2ᵉ occurrence. ⚠️ Et `<%-` **n'est PAS une interpolation** en eta (c'est un contrôle d'espaces — il n'affiche RIEN) : le tag est `<%=` (non échappé si `autoEscape: false`).
- `[2× — 2026-07-13]` **`$PIPESTATUS` n'existe pas en zsh (c'est `$pipestatus`)** → `${PIPESTATUS[0]:-$?}` évalue silencieusement `$?` du DERNIER maillon (exit toujours 0). Mesurer un exit code = run SANS pipe, puis `echo $?`.
- `[2× — 2026-07-14]` **Un cache qui n'a pas d'invalidation ment POUR TOUJOURS.** Le manifest de complétion (clé = version du paquet, **identique d'un build à l'autre en dev**) servait les built-ins d'un boot vieux de 2 jours : `create <TAB>` ne proposait pas `module` alors que le binaire, lui, le connaissait (signalé par le user, invisible en test). **Règle** : ce qui est lisible dans le CODE (0 boot — les built-ins) ne vient JAMAIS d'un cache ; le cache ne porte que ce que **seul un boot** peut connaître (les commandes de module). Corollaire de la 1ʳᵉ occurrence (« tester `__complete` après purge ») : si le fix impose une purge, c'est que la source est mal choisie.
- `[1× — 2026-07-12 nuit]` **`create app` ne buildait pas l'app → le premier `npm run dev` du README mourait en CRITIC « NON CONSTRUIT »** (trouvé par le user, pas par le banc). Le banc onboarding doit dérouler le happy-path EXACT du README généré (`cd app && npm run dev`), pas un flux équivalent qui buildait à la main.
- `[1× — 2026-07-12 nuit]` **`@types/react(-dom)` manquaient en devDeps du preset react → `npm run typecheck` de l'app générée cassé DEPUIS LE DÉBUT.** La gate `tsgo --noEmit` de l'app générée fait partie du banc scaffold (oxc/esbuild ne type-checke pas) — la lancer sur CHAQUE preset×frontend, pas seulement le build.
- `[1× — 2026-07-12 nuit]` **Une fixture de test doit reproduire la DONNÉE RÉELLE, pas la donnée d'entrée** : `describe().zones[].pattern` = `RegExp.source` (V8 échappe les slashes `^\/nodefony\/…`) → le classement `startsWith("^/nodefony")` ne matchait JAMAIS en réel alors que mes tests (fixtures = pattern config non échappé) étaient verts. Attrapé par la sortie collée par le user. Fixture juste = `new RegExp(p).source`.
- `[1× — 2026-07-12 nuit]` **Prouver un shape par le RUNTIME, pas par un DTO cousin** : `IUser` porte `identifier` (l'anonyme = `AnonymousUser` identifier `"anon."`, JAMAIS null en zone) ; le `username` de `/auth/me` est un RENOMMAGE du DTO. `who` restait « anonyme » même loggé — vu au curl réel, pas au typecheck (cast structurel silencieux).

## 🎨 Front / Studio (chaud)

- `[1× — 2026-07-13 soir]` **Mantine v9 : `Grid` n'a plus `gutter` → `gap`** (TS2322 au typecheck, pas au transform Vite). Réflexe : sur une prop Mantine douteuse, lire le `.d.ts` du composant (`node_modules/@mantine/core/lib/components/X/X.d.ts`), pas la mémoire des versions antérieures.
- `[1× — 2026-07-13 soir]` **Une modif front Studio ne se voit dans une app `--link` (ui static) qu'après `npm run build:ui`** — le `dist/frontend` est l'UI servie, le HMR ne concerne que le repo self-hosted. Vérif : `grep <symbole> dist/frontend/assets`.

## 🧪 Ce que le compilateur voit et que les tests ne voient pas

- `[1× — 2026-07-14]` **Les tests n'étaient type-checkés NULLE PART** (tous les `tsconfig.json` les excluent) → 201 erreurs dormantes, **0 dans le code de prod**, mais **5 assertions MORTES** : `assert.isFulfilled` (inexistant → `undefined !== undefined` toujours faux → la branche restante partait en **promesse flottante jamais attendue** : vert même si l'appel rejetait) ; deux `.types.test.ts` **entièrement inertes** (`declare const` illégal dans un corps de fonction → tout le bloc rejeté → **aucun `@ts-expect-error` ne s'appliquait**) ; `syslog._events.onLog.length` qui mesurait l'**arité du callback** au lieu du nombre de listeners (Node stocke la fonction NUE quand il n'y en a qu'un) ; un test « typage littéral préservé » qui **échouait** faute d'`as const`. ⚠️ Les 3 `tsconfig.tests.json` qui existaient étaient **MORTS** : `extends` hérite de l'`exclude` du parent → ils excluaient les tests qu'ils prétendaient couvrir, et **aucun script ne les appelait**. Réflexe : un fichier de config qui n'est **invoqué par aucun script** ne prouve rien — le vérifier, pas le supposer.
- `[1× — 2026-07-14]` **Un mock incomplet ne fait pas rougir une suite, il fabrique des NaN.** Un `drops` absent d'un mock de backpressure → `drops += undefined` → **NaN silencieux** dans l'agrégation cluster (aucune assertion ne le regardait). Idem : `implements IRepository` sans 6 méthodes du contrat, un espion `storage.write` avec un 3ᵉ argument fantôme (JS ignore un argument surnuméraire), `@UseSession({ context })` verrouillant une clé qui n'a **jamais existé**. Le typecheck des tests est le seul outil qui voit ça.
- `[1× — 2026-07-14]` **Une union TS sans contrainte SQL ne protège rien**: des catégories d'audit hors union (`"admin"`, `"burst"`) étaient persistées **depuis des mois** — colonnes `text $type<>` **sans `CHECK`**. Le type était le seul garde-fou… et personne ne le passait sur les tests.
- `[1× — 2026-07-17]` **Lancer `npm run typecheck` AVANT d'interpréter un échec de test.** Un test rouge sur MySQL 8.4 mais vert sur MariaDB m'a lancé dans un faux diagnostic « collation » ; la vraie cause était mon `order: { age: "asc" }` au lieu de `[["age","ASC"]]` — le tri partait SANS `ORDER BY`, et l'ordre venait d'une PK UUID aléatoire (donc 50/50 par backend). Le typecheck le disait en 2 s : `TS2353 'age' does not exist in type '[string, "ASC"|"DESC"][]'`. La gate qui l'attrape a justement été soldée le 07-14 — encore faut-il la lancer. Réflexe : test rouge + code fraîchement écrit → **typecheck d'abord**, hypothèse ensuite. (A aussi révélé une dette : une option `order` mal formée est **silencieusement ignorée**, là où un critère inconnu lève `UnknownCriteriaField`.)

## 🕸️ Le serveur qui écrit dans ses propres sources

- `[1× — 2026-07-14]` **Un scaffold lancé DEPUIS le serveur se fait tuer par son propre watcher.** Le `DevSupervisor` surveille `nodefony/` + `index.ts` — exactement là où `create module` écrit → rebuild + restart **au milieu du job** → le `npm install` (process ENFANT du serveur) est tué net → `node_modules` à moitié écrit. Trouvé par le user (« le watcher ne watche pas trop de fichiers ? »), prouvé par le PID du serveur qui change pendant le job. **Fix = suspension NOMMÉE et générique** (`suspendSupervisor(root, raison)` / `resumeSupervisor`), pas une rustine par feature — le superviseur avait DÉJÀ `#building`/`#pending` (même patron : « opération en cours → diffère »), et migration/install auront le même besoin. Le rechargement est **différé, pas annulé** (les fichiers restent dans `#dirty`) → le code généré est bien chargé à la levée. ⚠️ **Le danger est SYMÉTRIQUE** : un verrou jamais levé muselle le rechargement pour toute la session, sans explication. D'où **fail-safe** (PID vivant + TTL + verrou mort supprimé au passage) et `resume` dans le point de sortie **UNIQUE**. Et la **raison est obligatoire** : un rechargement qui ne part pas SANS le dire est un mystère.

## 🕵️ Un front ne DEVINE pas l'état du serveur

- `[1× — 2026-07-14]` **`FRONT_CAPABILITIES = { hasCheckout: false }` écrit en dur côté React → une option SUPPRIMÉE EN SILENCE.** La question `link` du scaffold (`askIf: "hasCheckout"`) n'était jamais posée — or c'est elle qui réécrit les deps `@nodefony/*` en `file:<checkout>`. Sans elle, l'app générée tire des paquets **non publiés sur npm** → `npm install` meurt en **404**. Trouvé par le user **en cliquant**, pas par un test (le typecheck était vert : le mensonge était sémantique). Règle : une capacité qui dépend de l'**état du disque serveur** se **demande au serveur** (`caps` dans le data plane), et l'absence de réponse retombe sur le défaut **le plus restrictif**, jamais sur une invention. Corollaire d'ergonomie : une option nécessaire au FONCTIONNEMENT se **pré-coche + s'explique**, et si l'utilisateur la décoche → **avertir AVANT**, pas pendant.
- `[1× — 2026-07-14]` **Une garde de chemin, ça s'ATTAQUE — ça ne se suppose pas.** Explorateur de dossiers pour `create app` : testé `../../../../etc`, chemin absolu, encodage URL, remontée au milieu, racine forgée, octet nul, **liens symboliques** (segments irréprochables → chemin réel ailleurs : d'où le contrôle sur `realpathSync`) et le **piège du préfixe** (`/home/dev-secrets` passe pour un enfant de `/home/dev` avec un `startsWith` nu). Principe qui rend le tout tenable : **le client n'envoie JAMAIS un chemin** (id de racine + segments validés + nom) → le serveur RECOMPOSE. Il n'y a rien à traverser.

## 🔇 Une suite VERTE peut être MUETTE (le pire des faux verts)

- `[1× — 2026-07-19]` **Un test vert ne prouve une garde que si on l'a vu ROUGIR sans elle.** Le TSDoc de `listPage` (idempotence) promettait d'exclure les entrées échues ; en retirant le filtre `expiresAt > now`, AUCUN test ne rougissait — ni le banc partagé, ni le test sqlite d'origine, ni mémoire/Redis. La promesse tenait par chance depuis toujours. Même famille le même jour : `decodeCursor` (Redis) jurait tolérer un curseur « malformé » et laissait passer n'importe quelle chaîne vers `SCAN` → une consultation admin levait. **Réflexe : neutraliser la garde AVANT de croire le vert** ; et quand un banc partagé ne peut pas exercer un cas (il faut vieillir une horloge), passer par une **capacité optionnelle du harness** (`expireSeeded`) → le cas est _skippé et annoncé_, jamais silencieusement absent.

- `[1× — 2026-07-18]` **Un DOUBLE de test trop permissif rend la suite VERTE mais MUETTE sur un bug d'API réel — et « pas prouvable » se dit rarement, « pas diagnostiqué » souvent.** Le `FakeRedis` typait `scan(cursor: number)` → il acceptait ce que le VRAI node-redis v6 refuse (un `number` en argument de commande → erreur `encodeCommand`). Le store passait donc un curseur `number` dans `listPage` **ET** `listAll` : bug latent invisible depuis toujours, révélé au 1ᵉʳ run contre un vrai serveur. J'ai d'abord gravé « pas prouvable contre un vrai serveur (dette) » — FAUX : c'était prouvable, il y avait un bug à corriger (curseur = **string** opaque de bout en bout). Réflexes : (a) un fake doit MENTIR comme le vrai (types au plus près de l'API réelle, sinon il masque) ; (b) un test réel qui échoue = suspecter un VRAI bug AVANT de conclure « impossible/dette » ; (c) ne jamais écrire « pas prouvable » sans avoir lu l'erreur exacte ; (d) l'infra était UP (`docker ps` : pg/mysql/redis) — il manquait juste les creds (`grep REDIS_PASSWORD docker-compose`), pas « pas d'infra ».
- `[4× — 2026-07-17]` **Un test qu'on ne LANCE pas n'existe pas — gaté par une env, OU vivant dans un script qu'on n'appelle jamais.** Deux formes vues le même jour. (a) **Gates env** : `npm test` (35/35 tâches, ~4 900 tests, exit 0) ne teste **AUCUN dialecte réel** — 119 e2e drizzle partent en `skipped`, sans un mot ; en activant tout : realtime 318→325, redis 42→43, core 2046→2081, et le `RedisTokenStore` contre un vrai Redis (`REDIS_TEST_URL`) n'avait **JAMAIS** tourné depuis son écriture (il échouait sur `NOAUTH` : le compose exige un mot de passe). (b) **Script non lancé** — trouvé par le USER : `turbo run test` n'exécute QUE le script `test` ; les suites **`test:integration`** (http/framework/frontend) sont une AUTRE commande, et elles tapent le serveur en HTTP réel. **683 tests hors du compte** quand j'annonçais « tout passe » — et le serveur n'avait jamais été démarré de la session, parce qu'aucun de MES tests n'en avait besoin. Réflexes : (a) lire le `| M skipped`, toujours ; (b) `jq '.scripts | keys'` AVANT d'affirmer « tout passe » — `npm test` n'est pas « tous les tests » ; (c) un gate dont personne ne connaît la valeur = un test mort ; (d) **prouver qu'un gate est réellement emprunté par l'ÉCHEC ATTENDU** — un banc en double backend (fake par défaut / vrai serveur si l'env est posée) passe au vert dans les deux cas, donc le vert ne dit PAS lequel a tourné : relancer une fois avec des creds fausses, le banc DOIT casser (vécu 2026-07-18 sur le banc Redis des passkeys ; `dbsize` après `flushDb` ne prouve rien, il vaut 0 dans les deux cas). **≥3× → À GRADUER au prochain CONSOLIDATE.** **5ᵉ le 2026-07-19**, deux fois dans la même session : suite Redis lancée avec `REDIS_URL` alors que le banc comportemental se gate sur `REDIS_TEST_URL` (14 tests muets, vert), puis un relevé de COUVERTURE lancé sans les variables (+3 points au lieu de +32, tests gatés skippés). → outillé le jour même : `vitest.gates.ts` (racine) + `gateReporter` nomme en fin de suite les cibles non exercées, leur commande docker et le compte non exécuté. ⚠️ L'outil ne suffit pas : je l'ai écrit le matin et je n'ai pas LU son rapport l'après-midi.
- `[2× — 2026-07-14]` **Un fichier de test qui meurt À LA COLLECTE ne compte AUCUN test échoué.** 2ᵉ occurrence du piège `oxcDecorators` (1ʳᵉ : bump vite/rolldown) — mais cette fois la cause était une **config jamais alignée** : `vitest.config.ts` de mongoose et `vitest.config.load.ts` de drizzle n'importaient pas `oxcDecorators` (drizzle `vitest.config.ts`, lui, l'avait) → tout fichier portant `@entity` levait `SyntaxError` au chargement. Symptôme lisible : `Test Files 2 failed | 7 passed` avec `Tests 68 passed` — **aucun test failed**, donc le regard glisse. **24 tests dormaient** (mongoose 68→84, banc load drizzle 0→8, dont le banc de charge ORM ENTIER). Réflexe : `Test Files failed` + `0 Tests failed` = échec de TRANSFORMATION, jamais de logique → lire l'erreur de collecte. Et : **une option de config qui conditionne la collecte doit être partagée** (le `vitest.oxc.ts` racine existe pour ça — vérifier que TOUTES les configs qui transforment des décorateurs l'importent, y compris les configs `load`/`integration` séparées).

## 🔎 Prouver une ABSENCE (le grep dit ce qu'il cherche, pas ce qui est)

- `[1× — 2026-07-19]` **Chercher le MOT au lieu de l'APPEL fabrique de faux trous — et je les ai annoncés au user comme des faits.** Audit de couverture des 9 stores : `grep -c "listPage" <banc>.ts` → 0 sur session/webhook/totp, j'en ai conclu « banc de pagination absent, 3 stores non couverts ». FAUX : l'appel s'écrit `runSessionPaginationContract(...)`, qui ne contient pas le mot `listPage`. Les trois étaient couverts sur les 3 dialectes depuis toujours. Une absence de match prouve l'absence du MOTIF, jamais l'absence du COMPORTEMENT. **Réflexe : prouver par l'EXÉCUTION** — `vitest run --reporter=verbose` puis lire les `describe` réellement déroulés ; c'est la seule source qui ne ment pas sur ce qui tourne. Corollaire : avant d'annoncer un trou, chercher l'APPELANT (`run*Contract(`), pas le vocabulaire interne du banc.
- `[1× — 2026-07-19]` **Deux bancs différents ne peuvent pas porter le même NOM.** L'enveloppe locale de drizzle s'appelait `runSessionStoreContract`, comme le banc partagé de `http` : l'importer a fait s'appeler la fonction elle-même → `RangeError: Maximum call stack size exceeded`, sans le moindre indice de collision (la stack ne montre que le fichier local, répété). Convention du repo = préfixer l'enveloppe par son backend (`runDrizzleSessionStoreContract`, cf `runDrizzleAuditPagination`).

## 🗄️ Concurrence & atomicité (ce que le dialecte ne dit pas)

- `[1× — 2026-07-17]` **Un pool FROID masque les races — chauffer AVANT de mesurer.** Un test « 10 `put` concurrents » passait SANS le fix, 3 fois sur 3 (pas flaky : structurel). Les connexions `pg` s'ouvrent à la demande → le 1ᵉʳ écrivain, seul à tenir une connexion chaude, boucle son SELECT+INSERT pendant que les 9 autres attendent leur TCP+auth ; ils lisent tous APRÈS lui, voient la ligne, et la course ne se produit JAMAIS. En prod le pool est chaud, donc le test était un **faux négatif**. Réflexe : avant tout test de concurrence sur un pool, forcer l'ouverture (`Promise.all` de N `count()`), et **prouver le test rouge sans le fix**. C'est la sonde d'entrelacement (tracer `start/select/insert` par tâche) qui l'a montré, pas le raisonnement.
- `[1× — 2026-07-17]` **`ON CONFLICT (x)` n'arbitre QU'UN index — une 2ᵉ contrainte unique lève.** `access_token` a `id` PK **et** `secretHash` UNIQUE : deux INSERT concurrents du même record font lever le perdant (PG `23505` sur `secretHash`) malgré `ON CONFLICT (id) DO UPDATE`. SQLite (connexion unique, ligne déjà committée) passait → **divergence de dialecte**. Réflexes : (a) avant de « corriger » avec un upsert, compter les uniques de la table ; (b) reproduire en **SQL pur, hors framework**, pour savoir si c'est notre code ou le SGBD ; (c) une table à deux uniques réellement disputées demande un autre remède (réservation en 2 instructions, cf `reserveIdempotencyKeyMysql`).
- `[1× — 2026-07-17]` **MySQL n'a ni `RETURNING` ni `WHERE` sur `ON DUPLICATE KEY UPDATE`** → tout design « upsert conditionnel » y coûte 2-3 requêtes, donc une course : on soigne une race avec une race. Quand un upsert doit être conditionnel, mettre la condition **dans la valeur écrite** (`GREATEST(col, ?)` / `MAX(col, ?)` / `$max` Mongo) = 1 instruction atomique partout. Généralisable : _un upsert ne porte pas de condition, une valeur si_.
- `[1× — 2026-07-17]` **Un upsert reste un INSERT qui BASCULE en UPDATE** : son INSERT doit être valide (toutes les colonnes `NOT NULL` fournies) même quand on SAIT que la ligne existe.
- `[1× — 2026-07-17]` **Porter un banc sur un NOUVEAU backend est un audit gratuit** (cousin de « changer d'outil = audit gratuit », rolldown). Le premier run mysql des stores a sorti 2 bugs en 2 minutes, invisibles depuis 6 mois : `put` qui ÉCRASE le jeton légitime sur collision de `secretHash` (ODKU sans `target` arbitre TOUTES les uniques), et `signCount` en int32 alors que le W3C le définit uint32 (pg `22003`, mysql `ER_WARN_DATA_OUT_OF_RANGE`, sqlite passe). Corollaire : une couverture « 1 backend » ne prouve pas le code, elle prouve le code SUR CE BACKEND.
- `[1× — 2026-07-17]` **Tester des valeurs JOUETS ne prouve rien sur le type de la colonne.** Un seuil à `1000`/`9000` passe sur n'importe quoi ; c'est `1_775_000_000_123` (epoch ms réel, 13 chiffres) qui prouve le `bigint`, et `INT32_MAX` qui trouve la borne. Mettre au banc au moins UNE valeur de production par colonne numérique.

## 🧭 Le bon remède — borner à la SOURCE plutôt que paginer la lecture

- `[1× — 2026-07-18]` **Une pagination ne borne pas ce qui doit être borné à l'écriture — et appliquée par réflexe, elle MASQUE le trou.** Le chantier disait « paginer `IWebAuthnCredentialStore` ». Or `findByUser` ne PEUT pas être paginé : il sert `allowCredentials`, qui est complet ou faux (WebAuthn n'a pas de « page 2 » — le navigateur reçoit une liste unique ; une clé en page 2 = connexion impossible). Le vrai défaut était l'**absence de plafond d'enrôlement**, avec une amplification exploitable : `login/options` est `bypassFirewall` **par design** et prend un `username` en entrée → un compte enrôle N passkeys, puis n'importe quel **anonyme** poste cet identifiant et force une lecture O(N) + la sérialisation de N entrées, à chaque requête. Remède = borner à la source (`passkeys.maxPerUser`, `countByUser` natif, 409 posé APRÈS la vérif crypto et AVANT le `save` — car `register/verify` est postable sans passer par `register/options`). **Réflexe à garder : avant de paginer une lecture, demander (a) cette lecture peut-elle être bornée à l'écriture ? (b) qui peut la déclencher SANS être authentifié ?** Une liste « petite en pratique » n'est bornée que si quelque chose la borne.

## 🧹 Supprimer un doublon

- `[1× — 2026-07-17]` **Un « doublon » peut couvrir un chemin que son remplaçant ignore — le relire AVANT de le supprimer.** En retirant `session-postgres.e2e` au profit du banc partagé, j'ai failli perdre `storage.gc(1)` : le banc ne testait que `gc()` sans argument. En allant vérifier, découverte que `gc(idleSeconds?, absoluteSeconds?)` porte DEUX bornes et que **la seconde n'était testée nulle part** — l'âge absolu (`createdAt` jamais prolongé) est pourtant l'invariant NIST qui fait mourir une session rafraîchie en continu, donc ce qui borne un vol de cookie. Réflexe : `grep -oE "\w+\.\w+\(" <fichier-à-supprimer>` et cocher chaque APPEL dans le remplaçant — pas chaque `it()`, dont le libellé ment plus facilement que le code. Sinon on réduit la couverture en croyant la rationaliser.

## 🧪 Écrire le test

- `[2× — 2026-07-18]` **Un DOUBLE valide toujours ce qu'un vrai serveur refuse — il doit imiter les CONTRAINTES du réel, pas les assouplir.** 2ᵉ occurrence (1ʳᵉ = le FakeRedis typant le curseur SCAN en `number` alors que node-redis exige une string). Cette fois : `SCAN COUNT` est un **indice d'effort, pas un plafond** — Redis rend plus de clés que demandé sur un petit keyspace (listpack) → la page dépassait `limit` et violait `IPage`. Le fake, lui, faisait un `slice(start, start+count)` bien élevé : vert partout. Sorti **uniquement** sur serveur réel. Corollaire : le même bug dormait dans `RedisTokenStore` livré la veille — **un bug trouvé dans un store doit être cherché immédiatement chez ses frères** (même protocole = même piège).
- `[1× — 2026-07-18]` **Introduire un SEUIL (taille de page, batch, buffer) sans test qui le DÉPASSE = le code au-delà n'est jamais exécuté.** Les tests d'orchestration des sessions travaillaient sur 3 items alors que la page interne vaut 200 : toute la boucle de pagination (avance d'offset, suivi de curseur, drain) était **structurellement inatteignable** — verte sans jamais tourner. Un parc de 450 l'a franchie et a immédiatement sorti un vrai défaut (`destroyByUser` laissait 50 sessions sur 60 : détruire pendant un parcours décale l'offset ET un curseur faible). Réflexe : tout seuil constant mérite un test à `N > seuil`, et un « déconnecter partout » se prouve par **ce qui reste**, jamais par le compteur qu'il renvoie.
- `[2× — 2026-07-18]` **Vert en isolation + rouge en suite = ressource PARTAGÉE, pas régression du code.** Le pire symptôme, parce qu'il fait suspecter le diff. 1ʳᵉ occurrence : deux fichiers purgeaient (`flushDb`) la même base Redis en parallèle. 2ᵉ : deux bancs aux **connecteurs distincts** (`tokens_pg` / `token_pagination_pg`) — donc « isolés » en apparence — retombaient sur la MÊME table physique `access_token` de la même base ; la purge de l'un effaçait le seed de l'autre et son `COUNT(*)` comptait les lignes du voisin. Réflexe qui fait gagner 20 min : relancer le fichier SEUL, puis **prouver** en lançant ensemble les deux suspects SANS le diff en cours (ici : 3 bancs token → 11 échecs, aucun fichier du diff impliqué). ⚠️ **Nuance qui corrige le remède noté en 1ʳᵉ occurrence** : « une ressource par fichier » n'est pas toujours possible — l'utilisateur applicatif ne peut pas créer de base sous MySQL/MariaDB (`ERROR 1044`), alors qu'il le peut sous PG. Une isolation qui ne marche que sur la moitié des dialectes = deux règles pour un invariant = la dérive qu'on cherchait à éviter. Là, **sérialiser la suite** (`fileParallelism:false`) est le bon choix : UNE règle, valable partout, qui protège d'office les bancs futurs. Coût mesuré et assumé (62 s au lieu de 21 s) contre une gate qui ment.
- `[1× — 2026-07-18]` **Un COMPTE est absolu — ajouter une assertion d'un TYPE nouveau peut exiger une isolation que les tests voisins n'exigeaient pas.** Deux tests `countByUser` ajoutés à un banc mongoose existant : rouges (4 ≠ 2). Réflexe correct appliqué (suspecter son propre diff) → c'était bien le TEST, pas le code. Le banc ne purge qu'au `beforeAll`, et ça suffisait jusque-là parce que tous ses cas vérifient la **présence d'ids connus** (tolérant aux résidus des cas précédents) ; un `count`, lui, voit tout. Réflexe : en greffant un cas sur un banc existant, regarder ce que les cas voisins TOLÈRENT avant de supposer que l'isolation en place suffit — et purger localement (`beforeEach` dans le seul `describe` concerné) plutôt que de durcir tout le fichier.
- `[1× — 2026-07-18]` **Un banc qui ÉCRIT dans un store persistant sans nettoyer n'est jouable qu'UNE fois.** `apikey-flow.test.ts` créait des PAT sans jamais les révoquer : ils s'accumulaient d'un run à l'autre jusqu'au plafond `maxPerSubject`, après quoi toute création répondait `409` — 5 tests rouges accusant le code, des mois après que le banc eut cessé d'être rejouable. Invisible tant qu'on ne relance pas la gate deux fois de suite. Réflexe : tout banc d'intégration qui crée des ressources plafonnées ou uniques doit poser son ardoise propre en `beforeAll` (pas seulement nettoyer en `afterAll` — un run interrompu laisse le terrain sale), et **se prouver par deux runs consécutifs**, jamais un seul.

- `[3× — 2026-07-17]` **Un test neuf ne vaut que PROUVÉ non complaisant — et il faut parfois 3 essais.** 3ᵉ occurrence → **À GRADUER au prochain CONSOLIDATE.** Vécu sur le scaffold : il m'a fallu **trois** assertions pour en obtenir une capable de rougir. (1) `include(svc, '"blog"')` : vert malgré le `super()` cassé — il matchait **le commentaire que je venais d'ajouter au template**. (2) `/super\(\s*"blog",/` : vert aussi, même cause (la prose contient littéralement `super("blog", …)`). (3) `/super\(\s*"blog",\s*module\.container/` — ancré sur ce que la prose ne contient PAS. Leçon nouvelle : **documenter du code dans un fichier généré rend les assertions naïves inopérantes** (la prose ressemble au code) — vaut pour tout test qui grep un template/snapshot/doc. Et : mesurer la PORTÉE réelle d'un garde-fou plutôt que la supposer (mon anti-`<%`/`%>` n'attrape PAS une variable mal nommée : Eta rend `<%= it.typo %>` en chaîne VIDE, sans lever — écrit dans le test pour qu'on ne lui prête pas une garantie qu'il n'a pas).
- `[2× — 2026-07-17]` **Un test neuf ne vaut que PROUVÉ non complaisant** (2ᵉ occurrence, cf `[1× — 2026-07-16]` plus bas) : retirer le fix (`git stash push <fichier>`) → il DOIT rougir. Vécu 2× ce jour : un test de concurrence `revoke` acceptait « logout OU manual » → il passait sans le fix ; il ne discrimine qu'en exigeant le **1ᵉʳ arrivé** (l'ordre est déterministe en sqlite : connexion unique + microtasks FIFO). Et un test de race PG passait sans le fix à cause du pool froid. **Un test de concurrence qui ne rougit pas sans le fix ne teste rien.**
- `[1× — 2026-07-17]` **Le seul chemin testé peut être le seul qui marche — chercher l'angle mort par une MATRICE verbe × dialecte.** `transaction()` (sqlite-only, `throw` en prod PG/MySQL) tenait parce que ses tests étaient tous en `:memory:` et que les 12 e2e réels ne l'appelaient jamais. En croisant l'API et le banc, la MÊME classe est ressortie ailleurs : les 13 verbes d'`IRepository` étaient couverts sur 3 dialectes, mais **`IOrm` ne l'était NULLE PART hors sqlite** → `probe()` rendait `{}` en prod (panneau Studio muet). Réflexe : après un bug « marche en dev, mort en prod », lister le CONTRAT ENTIER et cocher ce que le banc exerce vraiment (`grep -oE "orm\.<verbe>\("`) — un adapter qui ne route pas par dialecte est un `transaction()` en puissance.
- `[1× — 2026-07-17]` **Une sonde/métrique se prouve par l'ÉTAT, pas par la FORME.** `typeof p.pool.size === "number"` passe sur des compteurs figés à 0. Faire agir le système puis exiger que la sonde le voie (ici : ouvrir une vraie transaction → `borrowed >= 1`).
- `[1× — 2026-07-17]` **La CONCURRENCE est un angle mort structurel des bancs : ils sont séquentiels.** Un `Promise.allSettled` de 15 a sorti un bug qu'aucun test ne pouvait voir (sqlite = connexion unique → 2 `BEGIN` → « cannot start a transaction within a transaction », soit 2 requêtes HTTP simultanées = un 500). Il faut TENIR le travail ouvert (`setTimeout` dans la closure), sinon les tâches se sérialisent d'elles-mêmes et le chevauchement — donc le bug — ne se produit jamais. Corollaire : tester aussi que sérialiser ne MÉLANGE pas (un rollback n'emporte pas ses voisines).
- `[1× — 2026-07-17]` **Un banc de PARITÉ ne teste que ce qui est IDENTIQUE — une divergence de dialecte y est un faux échec.** Deux fois en une session : la visibilité avant commit depuis un repo non lié (sqlite = connexion unique → il voit ; pg/mysql → non), et le tri d'un texte (collation MySQL 8.4 ≠ MariaDB). Formuler la règle PORTABLE (« la sonde n'est jamais vide », « seul `withTransaction` entre dans la tx ») et documenter la divergence à côté, sans la tester. Et : **ne jamais trier sur du texte quand la PK est un UUID** (ordre physique aléatoire → flake 50/50 qui passe sur un backend et pas l'autre).
- `[1× — 2026-07-16]` **Un test qui CONSTATE un bug tombera au fix — c'est un signal, PAS une régression.** `CliKernel.test.ts` affirmait « environment = `NODE_ENV` TEL QUEL, AUCUNE normalisation » : un constat honnête du défaut, écrit exprès (07-14) après que l'ancienne rédaction (`=== "production" || === "development" || === "test"`) eut fait CROIRE à un contrôle de validité. Au fix, il a rougi. Réflexe : **lire le test avant de le « réparer »** — s'il documente le défaut, il doit énoncer le contrat inverse ; s'il défend un comportement voulu, c'est le fix qui est faux. Ne jamais le verdir sans trancher lequel des deux a raison.
- `[1× — 2026-07-16]` **Un test neuf ne vaut que PROUVÉ non complaisant** : remettre le bug 30 s → il DOIT rougir. Vécu : cast brut rétabli → 5 tests tombent (dont le contournement `delete process.env.NODE_ENV`, devenu inutile). Sans cette passe, on ne sait pas si on teste ou si on décore — cousin direct des 5 assertions mortes du 07-14. Vaut aussi pour une sonde de type (`Eq<A,B>` : la faire échouer une fois).
- `[3× — 2026-07-13 nuit]` **Deux implémentations d'une même RÈGLE = dérive garantie** (3ᵉ occurrence : après le filet `kill` par préfixe vs égalité stricte, et la liste `external`/peerDeps). Ici : deux regex pour « Vite dit-il que le port est pris ? » — l'une cherchait `Port X is in use`, or Vite écrit `Port X is **already** in use` → le retry de port ne s'est JAMAIS déclenché et la 2ᵉ app perdait tout son frontend, alors que la résilience était codée. **≥3× → À GRADUER au prochain CONSOLIDATE** (règle : une décision = une fonction nommée, jamais deux tests inline).
- `[2× — 2026-07-17]` **Une fixture de test doit porter le CONTRAT du vrai objet — sinon chaque ajout au core casse des tests qui ne testent pas le core.** 2ᵉ occurrence, et cette fois **4 fois dans la MÊME journée** : quatre faux kernels (`{ get: … }` sans `set`, `makeKernelStub` sans `serviceBootErrorFatal`) ont rougi à chaque fois que le DI a utilisé une capacité que le VRAI kernel possède depuis toujours. À chaque fois le réflexe « j'ai cassé quelque chose » était faux : c'est le mock qui ne représentait qu'une tranche. Signal de fond : **le contrat qu'un kernel doit honorer est implicite et non typé** (des objets littéraux castés `as any`) → aucun compilateur ne dit à un stub qu'il est incomplet. Piste : un `makeKernelStub()` PARTAGÉ et typé (`satisfies` sur une interface minimale) plutôt que N littéraux ad hoc. **≥3× → À GRADUER** (compter les 4 occurrences comme une).
- `[1× — 2026-07-14]` **Une fixture de test doit porter le CONTRAT du vrai objet, pas une approximation.** Le faux `ctx` de `realtimeControllerPaths.e2e` (objet littéral casté en `ContextType`) n'avait pas les nouvelles méthodes du contexte → 5 tests rouges, qu'on peut prendre pour une régression du code alors que c'est le MOCK qui ment. Réflexe (déjà vu avec `RegExp.source`) : quand un diff ajoute une méthode au contrat d'un objet transverse, **grep les mocks** — et faire porter au mock le chemin PROD par défaut (ici `beginFrame → null`), ce qui teste gratuitement « le code marche sans profiler ».
- `[1× — 2026-07-13 nuit]` **Tester une INVARIANTE, pas une valeur absolue.** J'assertais « 0 écouteur après le bind » ; un `http.Server` naît avec 1 écouteur `listening` interne (Node) → faux échec. La bonne assertion est **relative à la ligne de base** (« rien de plus qu'un serveur neuf ») — c'est d'ailleurs la vraie propriété visée : aucune ACCUMULATION au fil des replis. Un test qui échoue mérite d'abord un diagnostic (repro Node de 5 lignes), pas une supposition.

## 🧭 Lever une contrainte = auditer les gardes qui la présupposaient

- `[1× — 2026-07-13 nuit]` **En rendant les ports auto, TROIS gardes sont devenus des faux positifs** — tous écrits quand « 2 apps ne peuvent pas coexister » était une vérité : (a) le superviseur `exit(UNAVAILABLE)` si les ports sont pris par un autre projet ; (b) `status` criait « empilement anormal » en comptant les process de TOUS les projets ; (c) `stop` rapportait « 5151 encore occupé » en montrant les ports du VOISIN après un arrêt impeccable. Réflexe à graver : quand on lève une contrainte, **grepper tout ce qui la supposait vraie** — un garde qui se déclenche sur le cas nominal n'est plus lu du tout (anti-pattern « au loup »).
- `[1× — 2026-07-13 nuit]` **Un signal ≠ une preuve d'identité.** La readiness sondait « un port écoute → prêt ». Avec 2 apps, ce port peut être celui du VOISIN → faux READY sur un enfant même pas booté. Il faut une preuve que c'est bien NOTRE process (state file + pid), pas juste un signal observable de l'extérieur. Vaut pour toute sonde « ça répond donc c'est moi ».

## 👻 Le MIROIR : une option (ou un décorateur) qu'on POSE mais que rien ne LIT

- `[1× — 2026-07-17]` **La doc faisait écrire deux fantômes — dont un qui RASSURE.** (a) `@injectable({ singleton: true })` : `singleton` n'existe pas dans `InjectableOptions` (`{ name?, scope? }`) — le décorateur lit `.scope`, jamais `.singleton` → **accepté et ignoré en silence**. Le plus toxique des deux : il fait CROIRE qu'on a demandé un singleton, alors que le scope n'en était pas un — il a probablement contribué à ce que la dette DI reste invisible aussi longtemps. (b) `@Service({ singleton: true })` sur un Module : **n'existe pas du tout** (le barrel exporte `injectable`, `inject`, `services`) → le code ne compile même pas. Les deux étaient dans le CLAUDE.md du kernel, le CLAUDE.md de l'injector, `docs/injection.md`, `docs/kernel.md`, un skill ET un TSDoc du code. Réflexe symétrique du thème ci-dessous : une option **lue** doit être déclarée ; une option **documentée** doit être lue — vérifier l'API AVANT de recopier un exemple (`grep` la clé dans l'interface). Trouvé parce que le USER a demandé « `@injectable` n'a plus d'argument ? ».

## 🕳️ Une option que le code LIT mais que rien ne permet de POSER

- `[1× — 2026-07-13]` **`timing.enabled` était inatteignable en production.** Le `Context` le lisait (`options.timing?.enabled`), mais la clé n'existait ni dans le schéma Zod ni dans `defaultAppConfig` — or les overrides `NF__APP__*` n'écrivent **que sur des chemins DÉJÀ présents**. Résultat : allumer la sonde de phases sur un pod pour diagnostiquer était **impossible**, sans le moindre message. Réflexe : une option lue par le runtime doit être **déclarée** (schéma + défaut matérialisé) — sinon elle est morte, et son absence est SILENCIEUSE. Corollaire de conception : un défaut « pas d'avis » se matérialise (`enabled: null`), il ne s'omet pas — omettre la clé, c'est la rendre non-surchargeable.

## 🧬 TypeScript / JS — pièges de langage

- `[1× — 2026-07-18]` **Une méthode `#privée` (hard ECMAScript) installe un contrôle du receveur à l'EXÉCUTION** : elle rejette tout objet qui n'est pas passé par le constructeur → `TypeError: Receiver must be an instance of class X` dès qu'un test instancie par `Object.create(X.prototype)` (pratique courante ici pour isoler une surface d'un constructeur lourd). `private` TS est effacé à la compilation et n'a pas ce contrôle : mêmes garanties au compilateur, pas de brand check. **Regarder ce que le fichier fait DÉJÀ avant de choisir** — `sessions-service.ts` avait tranché de longue date (`private static readonly storages`, raison écrite en commentaire : `#` statique + décorateur de classe = TS18036). Un diff qui introduit la forme minoritaire crée un faux bug à débugger.

## 🧰 Outils du rename (mécanique)

- `[1× — 2026-07-19]` **Réécrire du code par script : `perl -pi` interpole `${…}`, et découper par index de chaîne casse le fichier.** Deux dégâts dans la même session : `perl -pi -e 's/…`s-${i}`…/'` a produit `` `s-` `` (le `${i}`mangé) — le test passait quand même, donc **un faux vert écrit de ma main** ; et un`python3`qui coupait un bloc par`str.index()`a laissé un fragment orphelin (fichier non parsable). Réflexes : (a) pour du code, préférer l'outil`Edit`(ancre exacte, échec bruyant) ; (b) après tout script de réécriture, relire le`git diff`**et** relancer le test — un test vert après réécriture ne dit pas que la réécriture est juste ; (c)`git checkout <fichier>` est le retour arrière, pas une nouvelle passe de script.

- `[1× — 2026-07-14]` **Renommer un champ d'INTERFACE = laisser le compilateur faire l'inventaire.** `IEntity.orm` → `connector` : renommer la propriété d'abord, puis `tsgo -p <pkg>/tsconfig.json` par paquet liste les oublis à la ligne près (0 erreur = 0 oubli sur le code typé). ⚠️ Mais le typecheck ne couvre PAS : les **tests** (pas de script `typecheck` sur orm-core/drizzle/mongoose → les oublis n'apparaissent qu'à l'exécution), les **templates de scaffold** (`.tpl`, texte), les **messages d'erreur** (assertions `/multiple ORMs/` devenues fausses) et les **query params** d'API. Les 4 ont mordu.
- `[1× — 2026-07-14]` **BSD sed (macOS) ne connaît pas `\b`** → `s/\borm: string/…/` ne remplace RIEN (silencieux, exit 0). Écrire `sed -E 's/(^|[^A-Za-z_])orm: string/\1connector: string/g'`. Et TOUJOURS relire le `git diff` du sed avant d'enchaîner : un motif trop étroit (`orm: "x"` / `orm: CONST` mais pas `orm: identifiant`) laisse un survivant qui ne sort qu'au run des tests.
- `[4× — 2026-07-18]` **`rg -r` = `--replace`, PAS « récursif »** — déjà gradué [[feedback_rg_no_replace_flag]], revenu **deux fois le 18 au matin**, puis **une 4ᵉ fois le soir** (`rg -rn "implements IWebAuthnCredentialStore"` → toute la sortie mutilée en `n`). Conséquence la plus grave observée : `rg -rn "listPage" <fichiers>` donnait à lire `ISessionStorage.n?()` / `users.n(...)` — une **fausse vérité sur le nom d'une méthode publique**, à deux doigts de partir en décision de nommage. Constat net : **une mémoire graduée n'empêche pas la faute de frappe réflexe** (4 récidives malgré la règle écrite) → le seul garde-fou qui marche est en AVAL : **relire la sortie avant de conclure** (un identifiant d'une seule lettre dans du code Nodefony = signal d'alarme, relancer sans `-r`).
- `[1× — 2026-07-18]` **`git stash -u` peut ne stasher QUE les fichiers non suivis** quand un lock git traîne : les modifiés restent en place, les nouveaux disparaissent, et la « baseline » obtenue n'est ni l'avant ni l'après — ici 3 fichiers de test en échec de COLLECTE (import d'un banc absent), diagnostic faussé le temps d'un cycle. Le message d'erreur du lock passe inaperçu au milieu de la sortie. Réflexe : après TOUT `git stash`, relire `git status --short` **et** `git stash list` avant d'interpréter le moindre résultat de test.

## 🔤 Un NOM DE PAQUET n'est pas un NOM DE RÔLE

- `[2× — 2026-07-14]` **DEUX noms pour UN concept = un bug qui attend.** L'entité déclarait `orm: "wordpress"` pendant que la config déclare `connectors: { wordpress }` — le MÊME objet (une connexion nommée), deux mots. C'est exactement là que le bug s'est logé : `--connector` posait le nom dans le service mais pas dans l'entité, donc les tables naissaient dans la base de l'app pendant que le service les cherchait ailleurs, **en silence**. Le mot juste est celui du RÔLE (`connector` — l'ORM, lui, c'est le driver : Drizzle/Mongoose). Vu par le user à la lecture de la config, pas par un test. **Soldé** (`96bf4cac`, 85 fichiers, avant publication npm). Corollaire à retenir : le coût du renommage est LINÉAIRE avant publication, BREAKING après — la dette de vocabulaire se paie tout de suite ou jamais.

- `[1× — 2026-07-13]` **Le watch dev était AVEUGLE sur tout `@nodefony/frontend`** (la dette « watch muet », enfin diagnostiquée). La règle d'exclusion ignorait tout segment de chemin nommé `frontend` — visant les sources SPA (`studio/frontend/`, `modules/x/frontend/`, qui ont le HMR Vite) — mais `frontend` est AUSSI le nom du **paquet serveur** (le builder Vite) : on l'éditait, rien ne se passait, jamais. Réflexes : (a) un filtre par NOM DE SEGMENT doit se demander si ce nom peut aussi être un paquet/module ; (b) un prédicat inline dans une option de lib (`ignored:` de chokidar) est **invérifiable** → l'extraire en **fonction pure exportée** et la tester (c'est ce qui a rendu le bug trouvable ET verrouillable).

## 🖥️ CLI / tests e2e process

- `[1× — 2026-07-13]` **Un chemin « après réponse envoyée » qui throw = dégradation SILENCIEUSE garantie** : le complete() d'@Idempotent throwait (structure circulaire) APRÈS le 201 déjà parti → abort() effaçait la clé, personne ne voyait rien, chaque rejeu ré-exécutait la mutation. Règle : tout code post-response doit être fail-safe + fail-loud (log), jamais laisser l'exception filer dans le vide. Et tester les DEUX formes de retour d'action (payload brut ET renderJson).
- `[1× — 2026-07-13]` **Vérifier le DESIGN avant de déclarer un bug sécu** : « @RequireScope laisse passer l'anonyme » semblait un bug — le ScopeVoter documente que les scopes ne bride nt QUE les jetons machine (humain/anonyme → GRANT, l'autorisation humaine = rôles). Le TSDoc de la brique est la 1ʳᵉ source, pas l'intuition OWASP.
- `[2× — 2026-07-13]` **Une capacité prouvée sur UN chemin n'existe pas forcément sur le chemin voisin** : (1) la dédup de clé d'idempotence marchait au data plane admin, mais via le pont `api.request` les controllers user n'avaient NI `@Body` NI la porte `@Idempotent` → rejeu `socket.mutate` = doublon SILENCIEUX ; (2) LE LENDEMAIN, même pont : le banc duplex ne testait que le retour NU — une action `renderJson` via le pont écrivait une frame NUE hors protocole + retournait la `WebsocketResponse` circulaire → stringify du peer cassé → unhandledRejection + timeout client (trouvé par le user au Playground, `6f8a4177`). La leçon d'hier disait DÉJÀ « tester les DEUX formes de retour (payload brut ET renderJson) » — appliquée à l'idempotence HTTP mais PAS généralisée au pont. Un template/saveur qui promet un comportement = un banc qui le prouve côté USER, pour CHAQUE forme de retour × CHAQUE transport.
- `[1× — 2026-07-13]` **`RealtimeClient` en Node = `nodefony/client`** (l'entrée `.` du core ne l'exporte qu'en condition `browser`) ; et l'API WebSocket standard (undici) n'accepte PAS de headers → cookie de session au handshake = transportFactory custom sur `ws`. En zone firewall `security:true`, le pont `api.request` REFUSE l'anonyme (frameAuthorizer) même si la zone accepte l'anonyme en HTTP — c'est le design (surface d'invocation), documenté dans le template duplex.

- `[3× — 2026-07-13 ×2]` **Un e2e qui spawne le binaire valide le DIST, pas le source.** (3ᵉ occurrence, attrapée par le USER en live : les TEMPLATES scaffold sont lus sur DISQUE mais le moteur tourne en DIST → désync possible en checkout — `it.pkg` inconnu du vieux dist = crash create. Règle : tout edit engine/templates scaffold = rebuild core AVANT test binaire. ≥3× → À GRADUER au prochain CONSOLIDATE.) Le filet CLI (`node bin/nodefony …`) a été lancé APRÈS le refacto mais AVANT `npm run build` → 12 verts… sur l'ANCIEN code. Réflexe : tout test qui spawn un binaire/dist = **rebuild d'abord**, sinon le vert ne prouve rien. (Cousin du « dist périmé » mais côté VALIDATION, pas boot.)
- `[1× — 2026-07-14]` **Un échec sous `turbo run test` n'est pas un échec du test — c'est peut-être la CONCURRENCE.** Les bancs qui spawnent un binaire (`CliIntegration`, `completion` zsh, harnais HOME jetable) et les bancs de charge (seuils de perf) échouent quand 13 suites tournent en parallèle, et passent 2× de suite en isolation. Réflexe avant de « corriger » : rejouer la suite SEULE (`turbo --filter=<pkg>` ou `npx vitest run <fichier>`). Ne jamais toucher au code sur la foi d'un rouge de run parallèle.
- `[1× — 2026-07-10]` **La sortie vitest se termine par ~40 lignes blanches** → un `cmd | tail -N` après le run rend une sortie VIDE (2× dans la session, on croit à un échec silencieux). Fiable : rediriger vers un fichier puis `grep -E "Test Files|Tests"` dessus.
- `[1× — 2026-07-10]` **Une mémoire de dette d'archi peut être PÉRIMÉE côté « déjà réglé »** : la dette CLI listait le double-boot prod/cluster comme ouvert alors que `e51af263` l'avait corrigé (asserts boot-count=1 VERTS). Avant d'auditer une dette mémorisée, croiser chaque point avec `git log`/le code — la devise vaut aussi pour les mémoires IA.
- `[1× — 2026-07-10]` **Un script shell GÉNÉRÉ se valide en l'EXÉCUTANT dans le vrai shell, pas en le parsant.** `zsh -n` était vert alors que 2 bugs cassaient la complétion en réel : offset `${words[@]:2}` (l'**offset d'expansion zsh est 0-based** contrairement à l'indexation 1-based → `:1` pour sauter le 1er mot) + `compdef` introuvable sans `compinit` (échec SILENCIEUX → TAB retombe sur les fichiers). Les 2 trouvés par le retour user, pas par les tests. Harnais qui marche : `zsh -f` HOME jetable (vérifier `_comps[cmd]`) + faux binaire `./node_modules/.bin` qui capture ses args.

> ♻️ Gradué 2026-07-12 : « le cwd Bash dérive entre appels » (4×) → [[feedback_bash_cwd_drift]].

## 🕵️ Un rapport d'audit ANCRE — il ne PROUVE pas

- `[2× — 2026-07-17]` **Un diagnostic VÉRIFIÉ peut désigner la mauvaise CAUSE — vérifier le symptôme ne vérifie pas l'explication.** Suite directe du bullet ci-dessous : « le scope `singleton` n'est pas mémoïsé » était **exact sur le symptôme** (0 `kernel.set`, ancrage `injector.ts:100-112` juste) et **faux sur la cause**. La vraie racine : **deux annuaires et un seul nom** — `@injectable` indexe des CLASSES, `super(nom, …)` des INSTANCES, et le décorateur ne PEUT pas connaître la clé (chargement vs construction). Le fix « évident » (mémoïser) aurait été un **placebo** : `argsClass` est propagé du parent aux dépendances, donc rien n'était mémoïsable. Le dashboard a recopié la mauvaise cause pendant un jour, et elle orientait vers un fix inutile. Réflexe : dans un doc de vérité, séparer **ce qu'on a OBSERVÉ** (le symptôme, ancré) de **ce qu'on en DÉDUIT** (la cause, à instruire). Coût de l'instruction : 2 sondes parallèles + 1 mesure runtime. Coût de l'avoir sautée : un fix faux, mergé.
- `[1× — 2026-07-16]` **Un audit d'agent a raison ET tort dans le MÊME document.** L'audit du 07-14 (`tmp/audit-nodefony.html`) a sorti 2 bugs **critiques réels**, vérifiés au code et irréfutables : `transaction()` SQLite-only (`#client` assigné dans le seul `#connectSqlite`) et le scope `singleton` du DI non mémoïsé. Le même document affirme « 0 `@ts-expect-error` » → il y en a **14**. Règle : un ancrage `fichier:ligne` est un **point de départ**, jamais un fait. Dans un doc de vérité, ne graver que ce qu'on a relu au code ; le reste va dans un tableau explicitement **« NON VÉRIFIÉ »** (sinon on blanchit une erreur en la recopiant). C'est la devise appliquée à un livrable d'IA.
- `[1× — 2026-07-16]` **Le bug survit là où le seul chemin testé est le seul qui marche.** Les tests de `transaction()` sont TOUS en `:memory:` (sqlite) et les **12 bancs e2e PG/MySQL réels n'appellent jamais `transaction()`** → un `throw` en production, invisible depuis des mois, dans la brique qui est la _condition de release_. Réflexe : pour une capacité annoncée « multi-X », lister les appels de la capacité **par dialecte/variante**, pas par fichier de test.

## 📏 MESURER (méthode) — une mesure fausse est pire qu'aucune mesure

- `[1× — 2026-07-16]` **Compter des tâches sans exclure les CADUQUES donne un chiffre faux** : P7 sortait à 45 % contre 75 % déclaré — 4 lignes `⏭️` (Sequelize supprimé, MikroORM abandonné) comptées « à faire ». Le dénominateur = les tâches **vivantes**. Ici le fichier avait raison et mon compteur avait tort : avant d'accuser une source, vérifier sa propre formule (la recette du skill `nodefony-migration-audit` exclut `⏭️` — la relire plutôt que la ré-improviser).
- `[1× — 2026-07-16]` **Le code de sortie rapporté n'est pas celui de la commande qui compte** : `npm test > log 2>&1; echo "EXIT=$?"` lancé en tâche de fond → le harness annonce « exit 0 » (celui du `echo`) alors que turbo avait échoué. Cousin du `$pipestatus` zsh. Capturer l'exit **immédiatement**, et le corroborer dans le log (`Failed:` / `Tasks: N successful, M total`).

- `[1× — 2026-07-14]` **`ps -o time` NE MESURE PAS la boucle d'événements** : il additionne TOUS les threads du process (GC parallèle de V8, pool libuv). Il donnait « 227 µs de CPU par message, donc 4 400 msg/s max » alors que **9 782 msg/s passaient réellement** — incohérence flagrante, donc mesure rejetée. La métrique juste pour un process Node = l'**ELU** (`performance.eventLoopUtilization`) échantillonnée PENDANT la charge : `plafond = débit observé ÷ ELU`. Bonus : elle corrige le biais du client (ELU 0,2 → le banc est le goulot, pas le serveur). ⚠️ La sonde `/studio/api/stats` échantillonne sur une fenêtre GLISSANTE de 150 ms → une soustraction avant/après y donne des valeurs aberrantes (« ELU 117 % »).
- `[1× — 2026-07-14]` **Une mesure de perf UNIQUE ne vaut rien.** Un run isolé a donné « ws clair 35 % plus lent que wss » — conclusion absurde (le chiffrement rendrait plus rapide ?), invalidée dès la 2ᵉ série alternée : à petit payload les deux sont équivalents, et dès 1 Ko le clair reprend l'avantage. Réflexe : **médiane de N runs + dispersion PUBLIÉE** avec le chiffre. Un chiffre sans son incertitude est un piège — et la taille de payload change le classement, donc mesurer avec la taille RÉELLE des messages métier.
- `[1× — 2026-07-14]` **Un delta mémoire avant/après est noyé dans le bruit du GC** : « RSS par socket = **−24 Ko** » (négatif !). La méthode juste = **paliers croissants + régression linéaire** : la pente est le coût unitaire, l'ordonnée à l'origine absorbe le bruit constant, et le **R² dit si la mesure est exploitable** (< 0,9 → on écrit « non exploitable », on n'invente pas un chiffre).
- `[1× — 2026-07-14]` **Un banc qui suppose les routes du module `test` ne peut mesurer QUE le dev** : ce module est `policy: "dev"` → **absent en production** (404). Or c'est la prod qu'il faut dimensionner. Mesuré : **prod = 2,4× le dev** (4 902 rps vs ~2 000 ; RSS repos 217 Mo vs 1 233). Le profiler + le timing + le watcher ne sont pas gratuits. → tout banc de capacité doit avoir un mode qui cible ce que la PROD expose (ici : hub Studio + `/studio/api/health`).

## 🔤 JS — le backtick dans un commentaire ferme le template literal

- `[3× — 2026-07-14]` **Un commentaire contenant des backticks, écrit À L'INTÉRIEUR d'un template literal, casse le fichier.** Vécu **3 fois dans la même session** (CSS inline, JS inline du deck, commentaire d'impression) : `/* … \`opacity: 0\` … */`dans un`const CSS = \`…\``→`SyntaxError: Unexpected identifier`. Le backtick n'a aucune valeur de citation en JS : il TERMINE la chaîne. Règle : dans une string template (CSS/JS/HTML inline), **jamais de backtick dans les commentaires** — écrire `opacity:0` nu. **≥3× → À GRADUER au prochain CONSOLIDATE.**

## 🧾 Rapports & livrables (HTML pour l'humain)

- `[1× — 2026-07-14]` **Un `<details>` fermé n'est PAS imprimé** — absent du PDF, dans les 3 moteurs, et **aucun CSS n'y remédie**. Idem pour un onglet `hidden`. Sans hook `beforeprint` (déplier tout, restaurer après), un rapport perd la moitié de son contenu **en silence**. Même famille : `break-after: avoid` est un **no-op en Firefox ET Safari** (seul `break-inside: avoid` est portable) ; une animation jamais jouée laisse `opacity: 0` → invisible au papier.
- `[1× — 2026-07-14]` **Le validateur W3C attrape ce que l'œil ne voit pas** : `role="button"` sur un `<th>` est INVALIDE (bonne forme = un `<button>` DANS le `th` + `aria-sort`), et un `<div>` ne peut pas vivre dans un `<p>` (le deck s'insérait dans un `<p>` → `</p>` orphelin). Réflexe : POSTer le HTML généré à `validator.w3.org/nu/?out=json` — 10 s, et ça a trouvé 2 vrais bugs.
- `[1× — 2026-07-14]` **Un asset dupliqué diverge toujours** : le logo aurait pu être recopié en base64 dans le skill → on l'aurait changé dans Studio sans que les rapports suivent, pendant des mois, sans que personne ne le voie. Il est LU à sa source (`NodefonyLogo.tsx`) avec repli **annoncé** si introuvable. Le repli a servi immédiatement (chemin relatif faux d'un niveau) — c'est le message console qui l'a dit, au lieu de livrer une page sans logo.

## 🏎️ Perf / bancs A/B

> ♻️ CONSOLIDATE 2026-06-12 : les patterns A/B (mono-route ment / verdict 3 issues / stash+rebuild
> par flip / banc concurrent bench-frameworks) sont **gravés dans `nodefony-load-test` SKILL.md**
> (niveau 3) — retirés d'ici (anti-doublon). Restent les leçons non couvertes :

- `[1× — 2026-07-08]` **Un ORM réseau (PG) ne bat pas sqlite en local si le POOL par défaut n'est pas dimensionné.** Banc `auth/me` en PG réel : c=25 → **696 RPS** (≈ sqlite 689), mais c=50 → **294 RPS** (chute). Ce n'est PAS une limite de Postgres : le pool `pg` par défaut = **10 connexions** → 50 req concurrentes font la queue derrière 10 conns. sqlite (sync, mono-connexion) est PLAT quelque soit la concurrence ; PG async DEVRAIT scaler MAIS son avantage n'apparaît que si le pool ≥ concurrence. + round-trip localhost = latence que sqlite in-process n'a pas. Réflexe : avant de conclure « PG ne bat pas sqlite », vérifier `pool.max` (follow-up : exposer la taille du pool dans la config drizzle). Présenter les 3 régimes (c=10/25/50) et nommer le goulot (pool), jamais un point unique.
- `[1× — 2026-06-28]` **Un RPS ne prouve PAS le « gain » d'une session de cleanup/refacto hors hot-path — et un gain runtime de VERSION Node ≠ un gain de CODE.** Session « tirer parti de Node 24/26 » (deps→natif, `using`, `RegExp.escape`) : 0 changement dans le pipeline requête → débit steady-state inchangé. Le seul levier throughput (AsyncContextFrame = ALS rapide) dépend de la **version Node**, or on tourne sur 26 depuis le début → déjà actif AVANT les commits, un RPS « après » ne le compare à rien. Méthodo honnête : (1) RPS = référence de capacité, pas un avant/après ; (2) le gain dep-cleanup se voit au **boot + RSS/heap** (paquets en moins) ; (3) le gain ALS se mesure **même code Node 22 vs 26**. Ne JAMAIS présenter un RPS comme « le gain de la session » (devise : ne pas annoncer un gain non prouvé). Le user a validé : « tu dis que ce n'est pas objectif » → test abandonné.
- `[1× — 2026-06-25]` **Débit cluster : optimum = nombre de cœurs PHYSIQUES, sur-fork = contre-productif** (courbe live wrk,
  livez, machine 6 phys/12 logiques) : mono **6683** → 3w **17114** → **6w 19076 (pic = cœurs phys)** → 10w **15562 (−18 % vs 6w,
  p99 ×2.5)**. Au-delà des cœurs, les workers se battent → context-switch/cache-thrash → débit ↓ + p99 explose. L'HT (12 log)
  ne repousse PAS le plafond. → `--workers auto` (cgroup-aware ≈ cœurs) = le bon défaut. **Le vrai gain cluster = la LATENCE p99**
  (mono p99 **285ms** sur 100 conns → cluster **18-47ms**, ÷6-15), pas tant le débit.
- `[1× — 2026-06-25]` **Client de charge CO-LOCALISÉ bride le scaling mesuré** : wrk sur la MÊME machine que les workers leur
  vole des cœurs → 6w = ×2.85 du mono (pas ×6 attendu). Un client DISTANT révélerait le vrai plafond (≈×6). Toujours dire
  « débit bridé par le co-location » quand client+serveur partagent la machine — ne pas conclure « ça ne scale pas ». Idem
  node-client (http-load.mjs) ÷ wrk : node lui-même CPU-bound bride (mesuré : node+TLS 9583 vs wrk H1 17114 = +78 % juste en changeant de client/transport).

## 🧩 Concevoir un contrat/standard PARTAGÉ (avant de le coder)

- `[1× — 2026-07-17]` **Un contrat partagé multi-module vit dans le plus petit dénominateur commun des DÉPENDANCES, pas dans le module « thématiquement » proche.** J'avais posé `Page`/`PageQuery` dans `orm-core` ; mais `http` et `security` (Sessions/tokens) ne dépendent QUE de `nodefony` core, pas d'orm-core — et le user refusait de les coupler à l'ORM (« Redis pas dans le scope ORM »). Réflexe : avant de choisir l'emplacement d'un type partagé, `jq '.dependencies+.peerDependencies' ` sur TOUS les consommateurs visés → le point commun. Ici = le core. orm-core ré-exporte + étend (`PageQuery extends IPageQuery` avec `criteria`).
- `[1× — 2026-07-17]` **Auditer l'EXISTANT avant de poser un « nouveau » contrat, sinon on crée un doublon divergent.** Le contrat de page à curseur existait DÉJÀ en prod (`IAuditStore.query` : `events`/`nextBefore`/`before`). Poser mon `IPage` à côté = deux paginations (l'anti-pattern « liste dupliquée »). Le standard doit ENGLOBER l'existant (events→items, nextBefore→nextCursor, before→cursor). Réflexe : `grep -rEn "page|cursor|limit|listAll|query" ` sur tous les contrats du domaine AVANT de dessiner le type.
- `[1× — 2026-07-17]` **Ne pas se fier aux symptômes VISIBLES pour cadrer l'exhaustivité** : j'ai compté 4 stores (ceux qui font `listAll()`/`.find()` dans une façade admin) ; le user en voyait 8+. La vérité = `find … -name "I*Store.ts" -o -name "I*Repository.ts"` → 11 contrats, puis classer chacun (liste O(N) / déjà curseur / lookup-only). Un chantier « partout » exige l'inventaire des CONTRATS, pas des appels fautifs qu'on a sous les yeux.
- `[1× — 2026-07-17]` **Un « simple swap » cache souvent un filtre non-portable.** `UserAdminApi` semblait un `find()`→`findPage()` trivial ; en réalité il filtre par `role` (containment JSON array), `isActive()` (dérivé `enabled && !locked`, le « false » est un OR) et `q` (substring) — deux ne sont PAS exprimables en `Criteria` générique → vrai fix = méthode `listPage(filters)` NATIVE par backend (patron `findBySocialProvider`). Lire le corps du handler AVANT d'estimer l'ampleur.
- `[2× — 2026-07-18]` **Un banc de contrat multi-backend = UN SEUL banc chez le PROPRIÉTAIRE du contrat, importé cross-package ; UN caller par PACKAGE, jamais par dialecte.** J'avais éparpillé la pagination (fichier sqlite séparé + wrapper dupliqué dans drizzle + append dans les e2e pg/mysql + describe inline mongoose) → le user, 2×: « un seul banc, pas un par dialecte ». Cible : `runXPaginationContract(harness)` vit dans le module qui POSSÈDE le contrat (`@nodefony/user`, `@nodefony/security`), l'adapter fournit juste `{ store, clear, mode }` ; UN fichier `X-pagination.test.ts` par package déroule ses dialectes (`skipIf` URL). Le test-import **cross-package** (`../../../<pkg>/tests/support/…`) tient au typecheck ET au runtime vitest — pattern NOUVEAU, validé, à réutiliser. Les modes (offset vs curseur) se paramètrent dans le harness, pas en dupliquant les assertions.

## 🧪 git — un `git add` multi-chemins échoue en BLOC si un pathspec manque

- `[1× — 2026-07-17]` **`git add <a> <b> .claude/x.md` où `x.md` a été `git rm` → `fatal: pathspec did not match` et AUCUN des fichiers n'est stagé** → le commit qui suit ne prend que ce qui était déjà dans l'index (la suppression), pas les modifs. Vécu : commit « 1 file changed » au lieu de 9, rattrapé par `--amend` après relecture du `--stat`. Réflexes : (a) ne pas re-`git add` un fichier déjà supprimé par `git rm` (il est DÉJÀ stagé) ; (b) TOUJOURS relire `git show --stat` après commit et compter les fichiers attendus.

## 🗂️ Thèmes archivés (CONSOLIDATE 2026-07-10)

> Les leçons `[1×]` antérieures au ~2026-06-28 (jamais re-vécues) et les thèmes entiers ci-dessous
> vivent dans [`archive/RETEX-snapshot-2026-07-10.md`](archive/RETEX-snapshot-2026-07-10.md) —
> rien n'est perdu, mais le SAS ne porte que le chaud. Thèmes archivés : **Shell/environnement**
> (les ≥3× sont gradués : [[feedback_rg_no_replace_flag]], [[feedback_shell_no_unquoted_multipath]]),
> **Git/commit** (doublons de [[feedback_commit_fr_apostrophes]] purgés), **Front/Studio/UX**,
> **Red-team/délégation IA**, **Conception deps & arbitrage**, **Archi/isomorphisme**,
> **Redis/portabilité**, **Build/dist/boot**, **Conception/vocabulaire**.
> Gradués aussi : [[feedback_bench_isolate_session_store]] (3× mesures+profiling) ; « batcher les
> edits backend pendant test live front » (3×) = déjà gravé CLAUDE.md racine §Hygiène n°4.
> Consolidation détaillée : [`CONSOLIDATION-2026-07-10.md`](CONSOLIDATION-2026-07-10.md).

## 📚 Corpus doc / travail par agents

- `[1× — 2026-07-19]` **Le gate mécanique attrape ce que la relecture rate.** 3 preuves en 1 session : l'exemple config documentait une API INVENTÉE (`areas` en tableau — Zod l'aurait rejetée au boot), le bloc « qui compile » citait `IUser.username` (inexistant, c'est `identifier`), la prose Tests disait « 35 cas » (68 réels). Gates ×3 gravés (doc-lint + anchor-check + tsc du Démarrage rapide) : une page n'est « faite » que verte aux trois.
- `[1× — 2026-07-19]` **1 cellule-journal de 10 KB → prettier padde TOUT le tableau à sa largeur → fichier ×8** (MIGRATION_STATUS 376 KB pour ~45 KB réels, récidive de l'obésité 06-05). Tuer la cellule géante tue le padding — vérifier `awk length` avant d'accuser le contenu.
- `[1× — 2026-07-19]` **Un agent tué (quota session) ne perd PAS son travail écrit** : les 4 agents de la vague 1 sont morts sur la limite, mais les 4 pages étaient ÉCRITES — les gates ont permis de valider/committer un travail d'agent mort. Architecture résiliente = write early + gates mécaniques indépendants de l'agent.
- `[1× — 2026-07-19]` **Une ancre `fichier:ligne` vieillit en jours, pas en mois** : 19/283 ancres décalées 48 h après écriture (le code bouge sous la doc). Ancre SYMBOLIQUE (`matchPath()` + ligne) = le nom survit, la ligne se recale ; anchor-check en gate.

## 📘 Doc — le rendu qu'on valide n'est pas celui qu'on lit

- `[1× — 2026-07-19c]` **Vérifier dans le RENDU QUE L'UTILISATEUR REGARDE, jamais dans son propre outil.** J'ai validé la navigation d'un corpus entier (fil d'Ariane, retours au hub, cards) sur `build-preview` — mon aperçu HTML maison — et déclaré « fait ». Dans Studio, RIEN ne marchait : `MarkdownDoc.tsx:426` n'acceptait que des liens PLATS (`/^\.?\/?([a-z0-9._-]+)\.md$/`), donc toute remontée `../index.md` tombait en ancre morte ; les cards n'étaient rendues nulle part (0 occurrence côté React) ; `index.md` n'avait aucun statut et se triait entre `headers` et `lexique`. Trois défauts invisibles depuis l'aperçu. Corollaire : un outil d'aperçu maison **valide sa propre implémentation**, pas la cible.
- `[1× — 2026-07-19c]` **Un test qui scanne PLUS que le code testé ment par optimisme.** `corpusLinks.test.ts` parcourait tout le dépôt et déclarait 0 lien mort ; le service, lui, ne scannait que les modules **chargés** (8 sur 13) → 6 liens du hub global pointaient dans le vide, test vert. Aligner le banc sur la source réelle, ou corriger la source (fait : scan des paquets **installés**, real-path pour que le lien de workspace pointe vers la source).
- `[1× — 2026-07-19c]` **Un gate écrit avant une évolution REFUSE l'évolution.** `doc-lint` exigeait un catalogue en `### \`nom\``, une section H2 « Par où commencer » et un hub nommé `index.md` : la page d'accueil refondue (cards déclaratives, parcours déplacés) échouait sur trois règles à la fois. Assouplir en gardant l'INTENTION (un hub doit orienter) plutôt qu'en gardant la FORME.
- `[1× — 2026-07-19c]` **Des backticks dans un commentaire à l'intérieur d'un template literal FERMENT le literal** (`/* bloc \`\`\`nodefony-cards */`dans un`s.textContent = \`…\``). Symptôme trompeur : `TS1005 '}' expected`à la DERNIÈRE ligne du fichier, accolades pourtant équilibrées.`npx esbuild <fichier>` donne la position exacte en une seconde, là où tsc désigne la fin du fichier.

## 🗣️ Lever l'ambiguïté d'un mot AVANT de dépenser

- `[1× — 2026-07-19c]` **« la page d'accueil studio » = l'accueil DU PORTAIL DOC (dans Studio), pas l'accueil DE Studio.** J'ai lancé un agent complet (cahier des charges, endpoints sondés) sur le mauvais écran. Quand un mot désigne à la fois un contenant et un contenu (« studio », « menu », « accueil »), **reformuler la cible en une phrase avant d'engager un agent** — pas après.
