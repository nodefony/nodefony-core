# RETEX.md — digest des retours d'expérience (SAS, lu à chaque début de session)

> **Rôle** : ce fichier est le **sas** entre les retex bruts (`docs/session-retros/<date>-<id>.md`,
> jamais relus seuls) et les leçons durables (mémoires `feedback_*` indexées dans `MEMORY.md`).
> Il porte les **frictions récentes pas encore confirmées** (vues 1-2×). Le skill `nodefony-session`
> le **lit au START/RESUME** et le **met à jour au END** (ajout de 3-5 bullets du jour, par thème).
>
> **Règle anti-doublon (CRITIQUE)** : une leçon est **soit** ici (sas, non confirmée), **soit** en
> `feedback_*` (graduée, prouvée). **JAMAIS les deux.** Quand une friction atteint **3×** → mode
> CONSOLIDATE la promeut en `feedback_*` et la **retire d'ici**. Sinon dérive garantie (cf l'anti-pattern
> « liste dupliquée » que dénonce `nodefony-check-externals`).
>
> **Taille bornée** : ce fichier ne grossit jamais. Deux sorties (gérées par CONSOLIDATE, tous les
> 10-20 retex) : (a) friction ≥3× → graduée en `feedback_*` puis retirée ; (b) retex bruts vieillis →
> `archive/` + 1 ligne de résumé ici. Cible : ~1 écran. Format = bullet `[N× — date courte]` par thème.

---

## 📦 Dépendances / upgrade

- `[1× — 2026-07-10]` **Un « minor » d'outil peut casser la COMPILATION sans casser une assertion.** `vite 8.0.16 → 8.1.4` tire `rolldown 1.1.5`, dont `OxcOptions` **omet `tsconfig`** → notre `experimentalDecorators`/`emitDecoratorMetadata` n'est plus lu, `decorator.legacy` retombe sur `false`, les décorateurs sortent **bruts** et Node lève `SyntaxError: Invalid or unexpected token`. Symptôme trompeur : **fichiers morts en COLLECTE, 0 test failed** (`Test Files 2 failed | 65 passed` / `Tests 1705 passed`). Réflexe : un `Test Files failed` avec `0 Tests failed` = échec de transformation, PAS de logique → lire la sortie transformée (`server.transformRequest()` via l'API vite), pas la stack. Correction = `oxc: { decorator: { legacy: true, emitDecoratorMetadata: true } }` (source unique `vitest.oxc.ts` racine), **pas** un gel de version.
- `[1× — 2026-07-10]` **Vérifier `emitDecoratorMetadata`, pas seulement `legacy`.** L'injector résout par `design:paramtypes` en fallback ⇒ si la metadata n'est pas émise, l'auto-injection casse **silencieusement** (param `undefined`, aucune erreur). Toujours prouver la présence de `design:paramtypes` **avec le vrai type** dans la sortie compilée, jamais se contenter de « les tests passent ».
- `[1× — 2026-07-10]` **Un bump de MAJOR peut être un changement de NATURE du paquet.** `typescript@7` (port Go) ne publie plus d'API JS (`main: null`, pas de `lib/typescript.js`, `ts.createProgram === undefined`) → `@rollup/plugin-typescript`/`typescript-eslint`/`typedoc` cassent. Avant d'évaluer un major d'outillage : `jq '{main,types,exports}' node_modules/<p>/package.json` + un `import()` réel, AVANT de lire le changelog.
- `[1× — 2026-07-10]` **Réécrire des pins par `String.replace()` sur le texte du package.json touche la PREMIÈRE occurrence**, qui peut être dans `peerDependencies` et pas `devDependencies` (vécu : peer bumpé, devDep laissé → divergence). Réécrire par **bloc** (`npm pkg set "devDependencies.x=v"`), et auditer après coup avec `jq` bloc par bloc contre `git show HEAD:<f>`.
- `[1× — 2026-07-10]` **`npm outdated` ne voit pas les `peerDependencies` pinnées exactes** et `npm update` ne bouge pas un pin exact. Le lot « tout à jour » peut laisser des peers en arrière. Inventorier les 3 blocs.
- `[1× — 2026-07-10]` **Un alias npm ne renomme PAS le binaire.** `npm i -D tsgo@npm:typescript@7` a écrasé `node_modules/.bin/tsc` avec TS 7 (silencieux : `npx tsc --version` → 7.0.2). Puis `npm uninstall tsgo` a **délogé notre `typescript@6.0.3` hoisté** et exposé un `5.7.3` transitif. Deux règles : (a) pour deux versions du même outil, prendre un paquet au **binaire distinct** (`@typescript/native-preview` → `tsgo` ; `@typescript/typescript6` → `tsc6`) ; (b) **retirer un paquet de test par `git checkout package.json package-lock.json && npm install`, jamais `npm uninstall`**. Vérifier APRÈS install : `npx tsc --version` ET `require("typescript").createProgram`.
- `[1× — 2026-07-10]` **Un outil peut MASQUER un défaut du source.** rolldown a révélé que `@nodefony/http` **s'importe par son propre nom** (`Request.ts:28`) → il avale son `dist` (65→120 fichiers). Rollup ne bronchait pas. Corollaire : changer d'outil = **audit gratuit**. Externaliser systématiquement le nom propre du paquet.
- `[1× — 2026-07-10]` **`declarationDir` du tsconfig ÉCRASE `--outDir` de la CLI** : un `tsc --emitDeclarationOnly --outDir /tmp/x` a écrit dans le VRAI `dist/client/types` du repo. Passer `--declarationDir` explicitement pour toute génération de `.d.ts` hors-build.

## 🔧 Migration d'outillage (exécution)

- `[1× — 2026-07-10]` **Purger une dep = grep TOUS les blocs `scripts`, pas des clés devinées.** Le sweep lot 5 a couvert `build`/`dev`/`rollup` mais raté `build:force` (`rollup -c` → `command not found`, trouvé par le user au premier build). Réflexe : `jq '.scripts | to_entries[] | select(.value | test("<dep>"))'` sur les 21 package.json, puis grep global hors node_modules.
- `[1× — 2026-07-10]` **`npm pkg delete` est inconsistant en workspaces** (certains dirs silencieusement non modifiés dans une boucle). Pour une purge de masse fiable → `jq 'with_entries(select(.key|test(...)|not))'` directement sur les fichiers, puis UN `npm install` de resync.
- `[1× — 2026-07-10]` **Retirer un fichier d'un `include` tsconfig peut déplacer la racine commune** (TS5011 tsgo) : `tsconfigClient` sans `rollup.config.ts` → racine `./src` → layout `dist/client/types` cassé vs exports map. Fix = `rootDir` explicite. Vérifier le LAYOUT émis, pas juste l'exit 0.

## 🧪 Méthode de comparaison de builds

- `[1× — 2026-07-10]` **Comparer deux builds : la surface exportée par IMPORT RÉEL, pas le nombre de fichiers.** Le compte de `.js` ment (chunks vides, granularité de tree-shaking : rolldown 951 fichiers vs Rollup 854 dont 76 VIDES ; frontend 40→104 fichiers mais 290→237 Ko). Ce qui fait foi = `Object.keys(await import(dist/index.js))` avant/après. ⚠️ **Dans des PROCESS ISOLÉS** : charger deux builds du même paquet dans un process explose sur les registres globaux (`EntityRegistry: entity "session" already registered`).
- `[1× — 2026-07-10]` **Ne pas conclure « X est meilleur » sur un grep mal borné.** Deux faux positifs en une session : `grep -rl "node:fs" dist/client` matchait les `.d.ts` (pas les `.js`) → fausse « fuite de builtin » ; et `xargs` mange les guillemets → `import * as React from react` (code invalide) alors que la sortie était correcte. Toujours restreindre l'extension ET relire la ligne brute.
- `[1× — 2026-07-10]` **Un `grep` de plugins dans un `rollup.config.ts` compte le code COMMENTÉ** (`copy(`/`terser(` désactivés) et les faux amis (`replace(` = `String.prototype.replace`). Pour savoir ce qui est vivant : filtrer les lignes `^\s*//` et grepper les **imports**, pas les appels.

## 🧪 App générée `--link` (banc de test réel)

- `[1× — 2026-07-14]` **Un générateur se teste en EXÉCUTANT ce qu'il génère, pas en testant le générateur.** `create entity` : 85 tests verts sur le scaffold… et **5 bugs** sortis en exerçant le code produit dans une vraie app (`@HttpCode(204)+return null` qui PEND jusqu'au timeout · `IResourceService.find()` sans options → pagination impossible · import `default` sur la 2ᵉ entité → build KO · `--connector` non porté par l'entité → tables créées dans la MAUVAISE base en silence · fermeture de table avalée par le commentaire d'un champ `ref` en dernier). Aucun n'était visible côté générateur. **Gate d'un scaffold = typecheck de l'app générée + boot + CRUD réel + tests générés**, jamais les seuls tests du moteur.
- `[1× — 2026-07-14]` **Appliquer un VRAI schéma tiers est le meilleur banc** : WordPress (12 tables, 94 colonnes, relation en dernière position, `--no-timestamps`) a déclenché 3 des 5 bugs ci-dessus, qu'aucun exemple maison n'atteignait (nos exemples avaient toujours des horodatages après le dernier champ). Un schéma réel a des formes qu'on n'invente pas.
- `[1× — 2026-07-14]` **Ne JAMAIS déléguer le wiring d'un artefact à celui d'un AUTRE** : `wireEntitiesDecorator` réutilisait `wireDecoratorList` (écrit pour les controllers → import **default**) alors qu'un descripteur d'entité est une **const nommée** → `MISSING_EXPORT "default"` sur la 2ᵉ entité seulement (la 1ʳᵉ passait par un chemin dédié, correct). Deux artefacts = deux conventions d'export = deux wirings.

- `[1× — 2026-07-12]` **Une app `--link` a DEUX node_modules (app + checkout) = classe de bugs INVISIBLE dans le repo self-hosted** (node_modules unique hoisted). Vécu 2 fois le même jour : react en double (« Invalid hook call », page blanche → `resolve.dedupe`) et debugbar en 403 (realpath du paquet hors `fs.allow`). Réflexe : toute feature front se teste AUSSI depuis une app générée `--link`, pas seulement dans le repo.
- `[4× — 2026-07-13]` **Une liste de ports sondés est une CONVENTION, pas la topologie de l'app.** 4ᵉ occurrence (readiness `--wait`, `reportReady` superviseur, rapport READY, puis : la readiness `--detach` sondait `[5151,5152]` et le **state file n'était PAS publié en production** → une app sur `NF_PORT=8080` bootait très bien, la sonde ne voyait rien, épuisait son plafond de 120 s et **group-killait un serveur sain**). Sémantique juste : ready = AU MOINS UN port + **le serveur PUBLIE ses ports effectifs** (canal `runtime.json`, tous environnements), les lecteurs les prennent à la source. **≥3× → À GRADUER au prochain CONSOLIDATE** (règle : ne jamais SUPPOSER une topologie qu'on peut se faire DIRE).
- `[1× — 2026-07-12]` **Une page démo sans interaction se lit comme une page cassée.** Le user a conclu « react ne marche pas » devant une page qui MARCHAIT (h1 + JSON) mais n'avait ni bouton ni mouvement. Une vitrine générée doit prouver qu'elle est vivante : action utilisateur (bouton, input WS), pas juste du texte statique.
- `[1× — 2026-07-12]` **Ne jamais grepper le log d'un run détaché sans avoir vérifié l'exit du lancement** (sortie redirigée → un launch refusé laisse l'ANCIEN log en place → faux diagnostic « le fix ne marche pas », vécu). `rm` le log avant relance OU vérifier `exit=0` d'abord.
- `[2× — 2026-07-12 soir]` **Le runtime résout l'APP depuis process.cwd() → tout lancement depuis un sous-dossier boote un « projet fantôme »** (le package http pris pour l'app : 1 module, bind ::1, config défauts, ZÉRO warning). Vécu 2× dans la même session (start.sh puis `--write` de security:secrets qui a créé un `.env.local` dans le package). Réflexe : `cd <racine> &&` dans le MÊME appel avant tout `node bin/nodefony` ; le `cd "$ROOT"` est maintenant DANS start.sh.
- `[2× — 2026-07-13]` **Un filet de secours doit copier la RÈGLE EXACTE du chemin principal, pas une approximation** : 1ʳᵉ occurrence = filet `kill -9` par port non scopé (serveur d'une AUTRE app tué) ; 2ᵉ = filet « corrigé » mais par PRÉFIXE `"$ROOT"/*` alors que `splitByProject` compare en ÉGALITÉ stricte → l'app IMBRIQUÉE `tmp/cci-app` (contrôle scaffold du user) classée « autre projet » par `stop` puis SIGKILLée par le filet. Deux implémentations d'une même règle de scoping = dérive garantie (anti-pattern liste dupliquée, version kill). Fixé : filet strict + rapport `stop` visible (il partait dans /dev/null) + message superviseur « signal EXTERNE » actionnable.
- `[2× — 2026-07-14]` **eta `autoTrim: "nl"` mange le newline après CHAQUE tag** → 3 lignes `KEY=<%= v %>` sortent sur UNE ligne. Un bloc de lignes générées = UN tag avec `join("\n")` **ET un `\n` FINAL** : sinon la ligne SUIVANTE du template (`});`) remonte sur la dernière ligne rendue — anodin tant qu'elle finit par une virgule (`…notNull(),});` reste valide, donc **invisible**), fatal dès qu'elle finit par un commentaire (`// → User.id …});`). 2ᵉ occurrence. ⚠️ Et `<%-` **n'est PAS une interpolation** en eta (c'est un contrôle d'espaces — il n'affiche RIEN) : le tag est `<%=` (non échappé si `autoEscape: false`).
- `[2× — 2026-07-13]` **`$PIPESTATUS` n'existe pas en zsh (c'est `$pipestatus`)** → `${PIPESTATUS[0]:-$?}` évalue silencieusement `$?` du DERNIER maillon (exit toujours 0). Mesurer un exit code = run SANS pipe, puis `echo $?`.
- `[2× — 2026-07-14]` **Un cache qui n'a pas d'invalidation ment POUR TOUJOURS.** Le manifest de complétion (clé = version du paquet, **identique d'un build à l'autre en dev**) servait les built-ins d'un boot vieux de 2 jours : `create <TAB>` ne proposait pas `module` alors que le binaire, lui, le connaissait (signalé par le user, invisible en test). **Règle** : ce qui est lisible dans le CODE (0 boot — les built-ins) ne vient JAMAIS d'un cache ; le cache ne porte que ce que **seul un boot** peut connaître (les commandes de module). Corollaire de la 1ʳᵉ occurrence (« tester `__complete` après purge ») : si le fix impose une purge, c'est que la source est mal choisie.
- `[1× — 2026-07-12 nuit]` **`create app` ne buildait pas l'app → le premier `npm run dev` du README mourait en CRITIC « NON CONSTRUIT »** (trouvé par le user, pas par le banc). Le banc onboarding doit dérouler le happy-path EXACT du README généré (`cd app && npm run dev`), pas un flux équivalent qui buildait à la main.
- `[1× — 2026-07-12 nuit]` **`@types/react(-dom)` manquaient en devDeps du preset react → `npm run typecheck` de l'app générée cassé DEPUIS LE DÉBUT.** La gate `tsgo --noEmit` de l'app générée fait partie du banc scaffold (oxc/esbuild ne type-checke pas) — la lancer sur CHAQUE preset×frontend, pas seulement le build.
- `[1× — 2026-07-12 nuit]` **Une fixture de test doit reproduire la DONNÉE RÉELLE, pas la donnée d'entrée** : `describe().zones[].pattern` = `RegExp.source` (V8 échappe les slashes `^\/nodefony\/…`) → le classement `startsWith("^/nodefony")` ne matchait JAMAIS en réel alors que mes tests (fixtures = pattern config non échappé) étaient verts. Attrapé par la sortie collée par le user. Fixture juste = `new RegExp(p).source`.
- `[1× — 2026-07-12 nuit]` **Prouver un shape par le RUNTIME, pas par un DTO cousin** : `IUser` porte `identifier` (l'anonyme = `AnonymousUser` identifier `"anon."`, JAMAIS null en zone) ; le `username` de `/auth/me` est un RENOMMAGE du DTO. `who` restait « anonyme » même loggé — vu au curl réel, pas au typecheck (cast structurel silencieux).

## 🎨 Front / Studio (chaud)

- `[1× — 2026-07-13 soir]` **Mantine v9 : `Grid` n'a plus `gutter` → `gap`** (TS2322 au typecheck, pas au transform Vite). Réflexe : sur une prop Mantine douteuse, lire le `.d.ts` du composant (`node_modules/@mantine/core/lib/components/X/X.d.ts`), pas la mémoire des versions antérieures.
- `[1× — 2026-07-13 soir]` **Une modif front Studio ne se voit dans une app `--link` (ui static) qu'après `npm run build:ui`** — le `dist/frontend` est l'UI servie, le HMR ne concerne que le repo self-hosted. Vérif : `grep <symbole> dist/frontend/assets`.

## 🧪 Écrire le test

- `[3× — 2026-07-13 nuit]` **Deux implémentations d'une même RÈGLE = dérive garantie** (3ᵉ occurrence : après le filet `kill` par préfixe vs égalité stricte, et la liste `external`/peerDeps). Ici : deux regex pour « Vite dit-il que le port est pris ? » — l'une cherchait `Port X is in use`, or Vite écrit `Port X is **already** in use` → le retry de port ne s'est JAMAIS déclenché et la 2ᵉ app perdait tout son frontend, alors que la résilience était codée. **≥3× → À GRADUER au prochain CONSOLIDATE** (règle : une décision = une fonction nommée, jamais deux tests inline).
- `[1× — 2026-07-14]` **Une fixture de test doit porter le CONTRAT du vrai objet, pas une approximation.** Le faux `ctx` de `realtimeControllerPaths.e2e` (objet littéral casté en `ContextType`) n'avait pas les nouvelles méthodes du contexte → 5 tests rouges, qu'on peut prendre pour une régression du code alors que c'est le MOCK qui ment. Réflexe (déjà vu avec `RegExp.source`) : quand un diff ajoute une méthode au contrat d'un objet transverse, **grep les mocks** — et faire porter au mock le chemin PROD par défaut (ici `beginFrame → null`), ce qui teste gratuitement « le code marche sans profiler ».
- `[1× — 2026-07-13 nuit]` **Tester une INVARIANTE, pas une valeur absolue.** J'assertais « 0 écouteur après le bind » ; un `http.Server` naît avec 1 écouteur `listening` interne (Node) → faux échec. La bonne assertion est **relative à la ligne de base** (« rien de plus qu'un serveur neuf ») — c'est d'ailleurs la vraie propriété visée : aucune ACCUMULATION au fil des replis. Un test qui échoue mérite d'abord un diagnostic (repro Node de 5 lignes), pas une supposition.

## 🧭 Lever une contrainte = auditer les gardes qui la présupposaient

- `[1× — 2026-07-13 nuit]` **En rendant les ports auto, TROIS gardes sont devenus des faux positifs** — tous écrits quand « 2 apps ne peuvent pas coexister » était une vérité : (a) le superviseur `exit(UNAVAILABLE)` si les ports sont pris par un autre projet ; (b) `status` criait « empilement anormal » en comptant les process de TOUS les projets ; (c) `stop` rapportait « 5151 encore occupé » en montrant les ports du VOISIN après un arrêt impeccable. Réflexe à graver : quand on lève une contrainte, **grepper tout ce qui la supposait vraie** — un garde qui se déclenche sur le cas nominal n'est plus lu du tout (anti-pattern « au loup »).
- `[1× — 2026-07-13 nuit]` **Un signal ≠ une preuve d'identité.** La readiness sondait « un port écoute → prêt ». Avec 2 apps, ce port peut être celui du VOISIN → faux READY sur un enfant même pas booté. Il faut une preuve que c'est bien NOTRE process (state file + pid), pas juste un signal observable de l'extérieur. Vaut pour toute sonde « ça répond donc c'est moi ».

## 🕳️ Une option que le code LIT mais que rien ne permet de POSER

- `[1× — 2026-07-13]` **`timing.enabled` était inatteignable en production.** Le `Context` le lisait (`options.timing?.enabled`), mais la clé n'existait ni dans le schéma Zod ni dans `defaultAppConfig` — or les overrides `NF__APP__*` n'écrivent **que sur des chemins DÉJÀ présents**. Résultat : allumer la sonde de phases sur un pod pour diagnostiquer était **impossible**, sans le moindre message. Réflexe : une option lue par le runtime doit être **déclarée** (schéma + défaut matérialisé) — sinon elle est morte, et son absence est SILENCIEUSE. Corollaire de conception : un défaut « pas d'avis » se matérialise (`enabled: null`), il ne s'omet pas — omettre la clé, c'est la rendre non-surchargeable.

## 🔤 Un NOM DE PAQUET n'est pas un NOM DE RÔLE

- `[1× — 2026-07-14]` **DEUX noms pour UN concept = un bug qui attend.** L'entité déclare `orm: "wordpress"` pendant que la config déclare `connectors: { wordpress }` — le MÊME objet (une connexion nommée), deux mots. C'est exactement là que le bug s'est logé : `--connector` posait le nom dans le service mais pas dans l'entité, donc les tables naissaient dans la base de l'app pendant que le service les cherchait ailleurs, **en silence**. Le mot juste est celui du RÔLE (`connector` — l'ORM, lui, c'est le driver : Drizzle/Mongoose). Vu par le user à la lecture de la config, pas par un test. **Renommage `IEntity.orm` → `connector` = P1 de la prochaine session (38 fichiers), AVANT publication npm** (après, c'est un breaking change).

- `[1× — 2026-07-13]` **Le watch dev était AVEUGLE sur tout `@nodefony/frontend`** (la dette « watch muet », enfin diagnostiquée). La règle d'exclusion ignorait tout segment de chemin nommé `frontend` — visant les sources SPA (`studio/frontend/`, `modules/x/frontend/`, qui ont le HMR Vite) — mais `frontend` est AUSSI le nom du **paquet serveur** (le builder Vite) : on l'éditait, rien ne se passait, jamais. Réflexes : (a) un filtre par NOM DE SEGMENT doit se demander si ce nom peut aussi être un paquet/module ; (b) un prédicat inline dans une option de lib (`ignored:` de chokidar) est **invérifiable** → l'extraire en **fonction pure exportée** et la tester (c'est ce qui a rendu le bug trouvable ET verrouillable).

## 🖥️ CLI / tests e2e process

- `[1× — 2026-07-13]` **Un chemin « après réponse envoyée » qui throw = dégradation SILENCIEUSE garantie** : le complete() d'@Idempotent throwait (structure circulaire) APRÈS le 201 déjà parti → abort() effaçait la clé, personne ne voyait rien, chaque rejeu ré-exécutait la mutation. Règle : tout code post-response doit être fail-safe + fail-loud (log), jamais laisser l'exception filer dans le vide. Et tester les DEUX formes de retour d'action (payload brut ET renderJson).
- `[1× — 2026-07-13]` **Vérifier le DESIGN avant de déclarer un bug sécu** : « @RequireScope laisse passer l'anonyme » semblait un bug — le ScopeVoter documente que les scopes ne bride nt QUE les jetons machine (humain/anonyme → GRANT, l'autorisation humaine = rôles). Le TSDoc de la brique est la 1ʳᵉ source, pas l'intuition OWASP.
- `[2× — 2026-07-13]` **Une capacité prouvée sur UN chemin n'existe pas forcément sur le chemin voisin** : (1) la dédup de clé d'idempotence marchait au data plane admin, mais via le pont `api.request` les controllers user n'avaient NI `@Body` NI la porte `@Idempotent` → rejeu `socket.mutate` = doublon SILENCIEUX ; (2) LE LENDEMAIN, même pont : le banc duplex ne testait que le retour NU — une action `renderJson` via le pont écrivait une frame NUE hors protocole + retournait la `WebsocketResponse` circulaire → stringify du peer cassé → unhandledRejection + timeout client (trouvé par le user au Playground, `6f8a4177`). La leçon d'hier disait DÉJÀ « tester les DEUX formes de retour (payload brut ET renderJson) » — appliquée à l'idempotence HTTP mais PAS généralisée au pont. Un template/saveur qui promet un comportement = un banc qui le prouve côté USER, pour CHAQUE forme de retour × CHAQUE transport.
- `[1× — 2026-07-13]` **`RealtimeClient` en Node = `nodefony/client`** (l'entrée `.` du core ne l'exporte qu'en condition `browser`) ; et l'API WebSocket standard (undici) n'accepte PAS de headers → cookie de session au handshake = transportFactory custom sur `ws`. En zone firewall `security:true`, le pont `api.request` REFUSE l'anonyme (frameAuthorizer) même si la zone accepte l'anonyme en HTTP — c'est le design (surface d'invocation), documenté dans le template duplex.

- `[3× — 2026-07-13 ×2]` **Un e2e qui spawne le binaire valide le DIST, pas le source.** (3ᵉ occurrence, attrapée par le USER en live : les TEMPLATES scaffold sont lus sur DISQUE mais le moteur tourne en DIST → désync possible en checkout — `it.pkg` inconnu du vieux dist = crash create. Règle : tout edit engine/templates scaffold = rebuild core AVANT test binaire. ≥3× → À GRADUER au prochain CONSOLIDATE.) Le filet CLI (`node bin/nodefony …`) a été lancé APRÈS le refacto mais AVANT `npm run build` → 12 verts… sur l'ANCIEN code. Réflexe : tout test qui spawn un binaire/dist = **rebuild d'abord**, sinon le vert ne prouve rien. (Cousin du « dist périmé » mais côté VALIDATION, pas boot.)
- `[1× — 2026-07-10]` **La sortie vitest se termine par ~40 lignes blanches** → un `cmd | tail -N` après le run rend une sortie VIDE (2× dans la session, on croit à un échec silencieux). Fiable : rediriger vers un fichier puis `grep -E "Test Files|Tests"` dessus.
- `[1× — 2026-07-10]` **Une mémoire de dette d'archi peut être PÉRIMÉE côté « déjà réglé »** : la dette CLI listait le double-boot prod/cluster comme ouvert alors que `e51af263` l'avait corrigé (asserts boot-count=1 VERTS). Avant d'auditer une dette mémorisée, croiser chaque point avec `git log`/le code — la devise vaut aussi pour les mémoires IA.
- `[1× — 2026-07-10]` **Un script shell GÉNÉRÉ se valide en l'EXÉCUTANT dans le vrai shell, pas en le parsant.** `zsh -n` était vert alors que 2 bugs cassaient la complétion en réel : offset `${words[@]:2}` (l'**offset d'expansion zsh est 0-based** contrairement à l'indexation 1-based → `:1` pour sauter le 1er mot) + `compdef` introuvable sans `compinit` (échec SILENCIEUX → TAB retombe sur les fichiers). Les 2 trouvés par le retour user, pas par les tests. Harnais qui marche : `zsh -f` HOME jetable (vérifier `_comps[cmd]`) + faux binaire `./node_modules/.bin` qui capture ses args.

> ♻️ Gradué 2026-07-12 : « le cwd Bash dérive entre appels » (4×) → [[feedback_bash_cwd_drift]].

## 📏 MESURER (méthode) — une mesure fausse est pire qu'aucune mesure

- `[1× — 2026-07-14]` **`ps -o time` NE MESURE PAS la boucle d'événements** : il additionne TOUS les threads du process (GC parallèle de V8, pool libuv). Il donnait « 227 µs de CPU par message, donc 4 400 msg/s max » alors que **9 782 msg/s passaient réellement** — incohérence flagrante, donc mesure rejetée. La métrique juste pour un process Node = l'**ELU** (`performance.eventLoopUtilization`) échantillonnée PENDANT la charge : `plafond = débit observé ÷ ELU`. Bonus : elle corrige le biais du client (ELU 0,2 → le banc est le goulot, pas le serveur). ⚠️ La sonde `/studio/api/stats` échantillonne sur une fenêtre GLISSANTE de 150 ms → une soustraction avant/après y donne des valeurs aberrantes (« ELU 117 % »).
- `[1× — 2026-07-14]` **Une mesure de perf UNIQUE ne vaut rien.** Un run isolé a donné « ws clair 35 % plus lent que wss » — conclusion absurde (le chiffrement rendrait plus rapide ?), invalidée dès la 2ᵉ série alternée : à petit payload les deux sont équivalents, et dès 1 Ko le clair reprend l'avantage. Réflexe : **médiane de N runs + dispersion PUBLIÉE** avec le chiffre. Un chiffre sans son incertitude est un piège — et la taille de payload change le classement, donc mesurer avec la taille RÉELLE des messages métier.
- `[1× — 2026-07-14]` **Un delta mémoire avant/après est noyé dans le bruit du GC** : « RSS par socket = **−24 Ko** » (négatif !). La méthode juste = **paliers croissants + régression linéaire** : la pente est le coût unitaire, l'ordonnée à l'origine absorbe le bruit constant, et le **R² dit si la mesure est exploitable** (< 0,9 → on écrit « non exploitable », on n'invente pas un chiffre).
- `[1× — 2026-07-14]` **Un banc qui suppose les routes du module `test` ne peut mesurer QUE le dev** : ce module est `policy: "dev"` → **absent en production** (404). Or c'est la prod qu'il faut dimensionner. Mesuré : **prod = 2,4× le dev** (4 902 rps vs ~2 000 ; RSS repos 217 Mo vs 1 233). Le profiler + le timing + le watcher ne sont pas gratuits. → tout banc de capacité doit avoir un mode qui cible ce que la PROD expose (ici : hub Studio + `/studio/api/health`).

## 🔤 JS — le backtick dans un commentaire ferme le template literal

- `[3× — 2026-07-14]` **Un commentaire contenant des backticks, écrit À L'INTÉRIEUR d'un template literal, casse le fichier.** Vécu **3 fois dans la même session** (CSS inline, JS inline du deck, commentaire d'impression) : `/* … \`opacity: 0\` … */`dans un`const CSS = \`…\``→`SyntaxError: Unexpected identifier`. Le backtick n'a aucune valeur de citation en JS : il TERMINE la chaîne. Règle : dans une string template (CSS/JS/HTML inline), **jamais de backtick dans les commentaires** — écrire `opacity:0` nu. **≥3× → À GRADUER au prochain CONSOLIDATE.**

## 🧾 Rapports & livrables (HTML pour l'humain)

- `[1× — 2026-07-14]` **Un `<details>` fermé n'est PAS imprimé** — absent du PDF, dans les 3 moteurs, et **aucun CSS n'y remédie**. Idem pour un onglet `hidden`. Sans hook `beforeprint` (déplier tout, restaurer après), un rapport perd la moitié de son contenu **en silence**. Même famille : `break-after: avoid` est un **no-op en Firefox ET Safari** (seul `break-inside: avoid` est portable) ; une animation jamais jouée laisse `opacity: 0` → invisible au papier.
- `[1× — 2026-07-14]` **Le validateur W3C attrape ce que l'œil ne voit pas** : `role="button"` sur un `<th>` est INVALIDE (bonne forme = un `<button>` DANS le `th` + `aria-sort`), et un `<div>` ne peut pas vivre dans un `<p>` (le deck s'insérait dans un `<p>` → `</p>` orphelin). Réflexe : POSTer le HTML généré à `validator.w3.org/nu/?out=json` — 10 s, et ça a trouvé 2 vrais bugs.
- `[1× — 2026-07-14]` **Un asset dupliqué diverge toujours** : le logo aurait pu être recopié en base64 dans le skill → on l'aurait changé dans Studio sans que les rapports suivent, pendant des mois, sans que personne ne le voie. Il est LU à sa source (`NodefonyLogo.tsx`) avec repli **annoncé** si introuvable. Le repli a servi immédiatement (chemin relatif faux d'un niveau) — c'est le message console qui l'a dit, au lieu de livrer une page sans logo.

## 🏎️ Perf / bancs A/B

> ♻️ CONSOLIDATE 2026-06-12 : les patterns A/B (mono-route ment / verdict 3 issues / stash+rebuild
> par flip / banc concurrent bench-frameworks) sont **gravés dans `nodefony-load-test` SKILL.md**
> (niveau 3) — retirés d'ici (anti-doublon). Restent les leçons non couvertes :

- `[1× — 2026-07-08]` **Un ORM réseau (PG) ne bat pas sqlite en local si le POOL par défaut n'est pas dimensionné.** Banc `auth/me` en PG réel : c=25 → **696 RPS** (≈ sqlite 689), mais c=50 → **294 RPS** (chute). Ce n'est PAS une limite de Postgres : le pool `pg` par défaut = **10 connexions** → 50 req concurrentes font la queue derrière 10 conns. sqlite (sync, mono-connexion) est PLAT quelque soit la concurrence ; PG async DEVRAIT scaler MAIS son avantage n'apparaît que si le pool ≥ concurrence. + round-trip localhost = latence que sqlite in-process n'a pas. Réflexe : avant de conclure « PG ne bat pas sqlite », vérifier `pool.max` (follow-up : exposer la taille du pool dans la config drizzle). Présenter les 3 régimes (c=10/25/50) et nommer le goulot (pool), jamais un point unique.
- `[1× — 2026-06-28]` **Un RPS ne prouve PAS le « gain » d'une session de cleanup/refacto hors hot-path — et un gain runtime de VERSION Node ≠ un gain de CODE.** Session « tirer parti de Node 24/26 » (deps→natif, `using`, `RegExp.escape`) : 0 changement dans le pipeline requête → débit steady-state inchangé. Le seul levier throughput (AsyncContextFrame = ALS rapide) dépend de la **version Node**, or on tourne sur 26 depuis le début → déjà actif AVANT les commits, un RPS « après » ne le compare à rien. Méthodo honnête : (1) RPS = référence de capacité, pas un avant/après ; (2) le gain dep-cleanup se voit au **boot + RSS/heap** (paquets en moins) ; (3) le gain ALS se mesure **même code Node 22 vs 26**. Ne JAMAIS présenter un RPS comme « le gain de la session » (devise : ne pas annoncer un gain non prouvé). Le user a validé : « tu dis que ce n'est pas objectif » → test abandonné.
- `[1× — 2026-06-25]` **Débit cluster : optimum = nombre de cœurs PHYSIQUES, sur-fork = contre-productif** (courbe live wrk,
  livez, machine 6 phys/12 logiques) : mono **6683** → 3w **17114** → **6w 19076 (pic = cœurs phys)** → 10w **15562 (−18 % vs 6w,
  p99 ×2.5)**. Au-delà des cœurs, les workers se battent → context-switch/cache-thrash → débit ↓ + p99 explose. L'HT (12 log)
  ne repousse PAS le plafond. → `--workers auto` (cgroup-aware ≈ cœurs) = le bon défaut. **Le vrai gain cluster = la LATENCE p99**
  (mono p99 **285ms** sur 100 conns → cluster **18-47ms**, ÷6-15), pas tant le débit.
- `[1× — 2026-06-25]` **Client de charge CO-LOCALISÉ bride le scaling mesuré** : wrk sur la MÊME machine que les workers leur
  vole des cœurs → 6w = ×2.85 du mono (pas ×6 attendu). Un client DISTANT révélerait le vrai plafond (≈×6). Toujours dire
  « débit bridé par le co-location » quand client+serveur partagent la machine — ne pas conclure « ça ne scale pas ». Idem
  node-client (http-load.mjs) ÷ wrk : node lui-même CPU-bound bride (mesuré : node+TLS 9583 vs wrk H1 17114 = +78 % juste en changeant de client/transport).

## 🗂️ Thèmes archivés (CONSOLIDATE 2026-07-10)

> Les leçons `[1×]` antérieures au ~2026-06-28 (jamais re-vécues) et les thèmes entiers ci-dessous
> vivent dans [`archive/RETEX-snapshot-2026-07-10.md`](archive/RETEX-snapshot-2026-07-10.md) —
> rien n'est perdu, mais le SAS ne porte que le chaud. Thèmes archivés : **Shell/environnement**
> (les ≥3× sont gradués : [[feedback_rg_no_replace_flag]], [[feedback_shell_no_unquoted_multipath]]),
> **Git/commit** (doublons de [[feedback_commit_fr_apostrophes]] purgés), **Front/Studio/UX**,
> **Red-team/délégation IA**, **Conception deps & arbitrage**, **Archi/isomorphisme**,
> **Redis/portabilité**, **Build/dist/boot**, **Conception/vocabulaire**.
> Gradués aussi : [[feedback_bench_isolate_session_store]] (3× mesures+profiling) ; « batcher les
> edits backend pendant test live front » (3×) = déjà gravé CLAUDE.md racine §Hygiène n°4.
> Consolidation détaillée : [`CONSOLIDATION-2026-07-10.md`](CONSOLIDATION-2026-07-10.md).
