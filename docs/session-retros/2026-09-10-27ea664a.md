---
date: 2026-09-10
session_id: 27ea664a-b269-4351-a7bc-6bfc02c6cc21
focus: publier 10.0.0-alpha.4 (#311) — trois défauts de la chaîne, deux faits de npm, deux alertes de sécurité
---

# Session retro — 2026-09-10 — 27ea664a

## Fait

- **`10.0.0-alpha.4` PUBLIÉE** — 15 paquets, canal `alpha` à jour, 14 `latest` recalés au poste
  (`nodefony` protégé sur son `latest` stable `7.0.2`). #311 fermé avec compte rendu.
- **Trois défauts de la chaîne de publication**, tous hors du code publié, tous découverts par les
  épreuves successives — quatre runs avant le vert.
- **Deux faits de npm** transformés en code : le registre accepte avant de servir ; le second
  facteur expire au milieu d'un lot.
- **Deux alertes du scan de code** fermées, plus les quatre vulnérabilités d'une app générée.
- **#313 et #314 ouverts** sur deux défauts trouvés en éprouvant l'app publiée.

## Coûts évidents

- **Quatre tags posés pour une seule version.** Chaque échec d'épreuve coûtait ~15 min de forge.
  Le troisième a été évité en rejouant le scénario EN LOCAL avant de retaguer — ce que le skill
  prescrit précisément dans ce cas (« la forge a déjà rougi »), et que je n'ai fait qu'au 3ᵉ tour.
- **Une fausse alerte de lot partiel** — annoncée sur une lecture unique du registre, démentie
  deux minutes plus tard. Coût : la confiance, et le temps de lire un journal de 1799 lignes.
- **Dix-huit lignes de `jq` écrites pour une faute de casse supposée** — que le source a démentie
  (le champ n'est jamais rempli avec `--format`). C'est une remarque du user qui a arrêté ça.

## Recommandations

1. **Ne pas ouvrir de thème neuf pour les frictions du jour** — les trois versées ont trouvé leur
   maison dans deux thèmes existants ; c'est ce qui fait mordre le seuil de graduation.
2. **Trois thèmes sont MÛRS** (9/6/5 frictions) — à graduer au prochain CONSOLIDATE, pas au END.
3. **Le `prepack` de `@nodefony/studio` est désormais inerte** mais toujours déclaré : le retirer
   touche `package.json`, il attend un feu vert.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact REÇU — l'app générée depuis npm a fermé les trois critères de #311.
- ✅ Un gate vu mordre — `attw` rouge→vert, le test du répertoire piège tombé sur `EISDIR`.
- ✅ La commande du dépôt fait autorité — l'entrée standard a été câblée DANS `release.mjs`,
  pas dans un script à côté.

## Commits produits

| Commit     | Sujet                                                                            |
| ---------- | -------------------------------------------------------------------------------- |
| `c1dd2fac` | chore(release): 10.0.0-alpha.4                                                   |
| `be2c9745` | fix(release): empêcher un hook de pack de défaire la préparation du tarball      |
| `1b762855` | fix(release): viser la déclaration du module, pas la première mention de son nom |
| `ae2e456a` | fix(ci): constater le login docker là où le CLI l'écrit, pas par `--format`      |
| `2eaa882a` | fix(cli): fermer deux alertes du scan de code                                    |
| `08e8e8a3` | fix(release): attendre que le registre SERVE ce qu'il vient d'accepter           |
| `85c7ccda` | feat(release): demander le second facteur sur l'entrée standard                  |
| `4a2048cf` | docs(release): basculer l'accueil sur la préversion 10.0.0-alpha.4               |
| `44b3a819` | fix(scaffold): ne plus annoncer quatre vulnérabilités à la première installation |
