---
date: 2026-09-07
session_id: 09f81112-ee96-4f3c-bf05-3142423b8f2e
focus: #258 soldé au registre, la porte d'entrée éprouvée pour la première fois, et six défauts que la génération d'une app a révélés
---

# Session retro — 2026-09-07 — 09f81112

## Fait

- **#258 fermé, prouvé au registre** : 14 dist-tags `latest` recalés sur la `10.0.0-alpha.2`
  (`nodefony` épargné à `7.0.2`), 16 paquets historiques dépréciés. `npm create nodefony` ne sert
  plus l'alpha.1 — le défaut ACTIF est éteint.
- **Deux modes rejouables ajoutés à la chaîne** : `--dist-tags` (le constat n'existait qu'à chaud,
  pendant un `--publish`) et un `--deprecate` qui CONSTATE au lieu de rejouer sa liste à l'aveugle.
  `--otp` sur les deux gestes que le trusted publishing ne couvre pas.
- **#47, cran alpha** : les trois endroits qui affirmaient « rien n'est sur npm » disent la vérité
  sans rien promettre ; l'étape de bascule est entrée dans le « RESTE À FAIRE » du script.
- **La porte d'entrée éprouvée pour la première fois**, poste vierge, cache jetable :
  `npm create nodefony@alpha` → `npm start` → `GET /` **200**, `nodefony@10.0.0-alpha.2`.
- **Sept tickets ouverts** (#261 → #267), 28 ancres vérifiées VRAIES par un contrôle délégué.

## Frictions

- **Mon propre instrument a rendu un FAUX VERT** : `npm view <nom> --json` enveloppe sa réponse dans
  un tableau, `doc["dist-tags"]` valait `undefined`, et le mode annonçait « rien à recaler » sur
  quatorze paquets qui l'étaient tous. Gravé en test (`lireVueNpm`), sur la forme réelle.
- **`latestsRestesEnArriere` n'avait AUCUN test** — la règle qui empêche de servir une alpha à tout
  `npm i` de la terre n'était prouvée par rien. Banc écrit, vu rouge en la débranchant.
- **Une règle appliquée à un endroit, oubliée au voisin** : `wiring.ts` porte le commentaire « le
  corps est relu SANS ses commentaires », l'applique l. 522, et ne l'applique pas l. 582 — d'où un
  `@IsGranted` lu dans un TSDoc et un `verify` rouge sur toute app neuve.
- **Un piège que le skill DOCUMENTE, et dans lequel je suis tombé** : `items(first:100)` tronque
  sans le dire ; ma requête a rendu 1 ticket sur 7 avec l'air d'un inventaire complet.
- **Trois remèdes proposés avant d'avoir le message d'erreur** — la cause tenait dans une capture
  d'écran : la 2FA du compte est une clé de sécurité, qui ne produit aucun code.
- **Un wrapper shell rendait 0 pendant que `verify` sortait en 1** — la capture explicite de
  l'`EXIT` a seule évité le faux vert.

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `7561d0d2` | rendre REJOUABLE le constat du dist-tag « latest » resté en arrière |
| `40cbacae` | nommer le piège de la clé de sécurité face à `--otp`                |
| `f2ebcbd6` | faire CONSTATER au mode `--deprecate` ce qui reste à poser          |
| `4421681a` | dire à l'accueil que l'alpha est en ligne, et inscrire la bascule   |
