---
date: 2026-09-12
session_id: 6494e957-4eae-4b32-b5c1-389eee2889f1
focus: Publication de la 10.0.0-alpha.5, garde de CI avant publication, et second essai réel de découverte
---

# Session retro — 2026-09-12 — 6494e957

## Focus

Deux tickets de documentation d'agent (#361, #342), puis la **publication de la
10.0.0-alpha.5**, puis trois défauts que cette publication a révélés, puis un
**second essai réel de découverte** par un agent tiers sur une application générée.

## Fait

- **#361 et #342 fermés.** Section « Voir un écran » 8 359 → 1 584 octets (le geste
  versé dans le skill livré avant la coupe) · ligne MCP périmée corrigée · `doctor`
  2 364 → 1 620 o · jumeau carte de visite ↔ gabarit aligné sur `npm run verify` ·
  une application « Minimal » nomme désormais ses briques absentes et le geste qui
  les pose. Blocs payés intacts, prouvé par hachage de section.
- **10.0.0-alpha.5 PUBLIÉE** — 15 paquets, run de forge vert de bout en bout.
- **Garde de CI avant publication** (`garde-main` + `verdictCiDuCommit`) : le tag ne
  publiera plus sur une CI rouge.
- **Trois défauts réparés**, tous révélés par la publication elle-même.
- **#362 et #363 ouverts**, **#348 étendu**, **#360 confirmé au terrain**.

## Coûts évidents

- `test:all` joué DEUX fois (6 rouges à la première passe, tous verts en isolé puis
  à la relance : flake de charge). ~12 min de machine, nécessaires — on ne publie pas
  sur un doute.
- Une heure de va-et-vient sur la séquence de branches (préparer depuis `dev`,
  publier depuis `main`) que le produit ne disait pas — c'est précisément ce que la
  correction de ce jour supprime pour les prochaines fois.

## Recommandations

1. **CONSOLIDATE** — deux thèmes du sas mûrs depuis trois sessions (🤝 à 7, 📐 à 6).
   Le seuil ne sert à rien s'il ne déclenche jamais.
2. **Publier une alpha.6** — le défaut de `THIRD-PARTY-NOTICES.md` est dans l'alpha.5
   publiée : toute application créée avec `npm create nodefony@alpha` échoue son
   propre `npm run verify` à la première minute. Le correctif est prouvé.
3. **Jouer la tâche 0 de #348** — l'étage « premier contact » reste le seul non exercé.

## Patterns récurrents (déjà gérés)

- ✅ Un test neuf vu ROUGE en débranchant, débranchement prouvé par `diff` (5 fois ce jour).
- ✅ Prouver sur l'artefact REÇU — le rendu d'application, pas le gabarit ; le fichier
  réel de la vitrine, pas une reconstitution.
- ✅ Suspecter sa propre sonde avant l'agent qu'elle juge (trois fois ce jour).

## Commits produits

| Commit     | Sujet                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------- |
| `816abede` | docs(cli): retirer d'AGENTS.md ce qu'un skill porte, dire ce que l'app n'a pas — #361, #342 |
| `5a8596da` | chore(pilotage): empreinte — #361 et #342 fermés                                            |
| `cff8ef97` | fix(release): dire dès la RÉPÉTITION que le commit n'est pas sur la branche de publication  |
| `addf480b` | fix(release): se taire quand on EST déjà sur la branche de publication                      |
| `d46a2b65` | chore(release): 10.0.0-alpha.5                                                              |
| `8175ebdd` | feat(release): exiger que la CI du commit tagué soit VERTE avant de publier                 |
| `41f3e2fe` | fix(release): réparer trois défauts que la publication de l'alpha.5 a révélés               |
| `c63a36ce` | chore(pilotage): empreinte — #362 et #363 ouverts, #348 étendu                              |
