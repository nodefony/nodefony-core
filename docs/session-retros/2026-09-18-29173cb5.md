---
date: 2026-09-18
session_id: 29173cb5-8af1-45f0-b96f-d7dbdf9ce218
focus: #382 et #358 — une seule rédaction du message de semis, et un contrôle d'image atteignable depuis une application
---

# Session retro — 2026-09-18 — 29173cb5

## Focus

Deux tickets du jalon `10.0.0-alpha`, tous deux sur ce que le générateur LIVRE :
la duplication d'un message d'erreur dans un gabarit (#382), puis un contrôle de
sécurité qui vivait hors du paquet publié (#358).

## Fait

- **#382 fermé** (`5c1c1ca5`) — le gabarit du semis importe de nouveau
  `describeSeedFailure` et n'a plus de rédaction en propre. La condition qui
  l'avait fait retirer est levée : `10.0.0-alpha.6` est publiée et exporte le
  symbole (vérifié dans le tarball, pas dans le dépôt).
- **#358 fermé** (`0cca01a2`) — la règle et la lecture des couches déménagent
  dans le produit ; commande `nodefony image:check` (standalone, zéro boot) ; le
  workflow GitHub généré construit l'image et la soumet au contrôle ; les scripts
  du dépôt cessent d'en porter une copie et l'appellent.
- **#418 ouvert** — la chaîne GitLab générée, laissée hors périmètre avec son
  motif (job dans `node:24`, sans client docker ; décor `dind` non éprouvable
  ici).
- **#316 recalé** — une de ses preuves (`rg -c 'docker build' … → 0`) est devenue
  fausse ; commentaire posté avec ce qui reste vrai.

## Coûts évidents

- Deux `docker build` d'une application générée (~2 min chacun) pour la preuve en
  réel. Non évitables : le ticket demandait une image, pas un inventaire.
- Un aller-retour sur l'élagage des ré-exports du bundler (11 tests rouges) —
  réglé en constatant les exports du `dist` au lieu de les déduire du source.
- Une enquête sur un contrôle qui rendait 0 là où le ticket promettait un refus.
  Coût réel, mais c'est elle qui a trouvé le VRAI trou (`.env.production`).

## Recommandations

1. Ne pas graduer les 4 thèmes mûrs au END — c'est un travail de CONSOLIDATE,
   mais il ne se programme que s'il se voit (signalé au user).
2. `npm run check:lang` est ROUGE hors de mon diff (`engine.ts:2390,2396`, deux
   identifiants français). À solder : le gate est déjà en place, il n'est
   simplement pas vert.
3. Le gate `release:image-gate` n'est appelé par AUCUN workflow — seulement à la
   main. Un gate que personne ne lance ne garde rien.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact REÇU (tarball npm dépaqueté, image `docker save`,
  application générée puis installée depuis le registre).
- ✅ Débrancher pour voir mordre — deux fois, avec restauration vérifiée.
- ✅ Une règle, une implémentation : la copie du dépôt retirée, pas doublée.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `5c1c1ca5` | refactor(scaffold): faire rédiger le message du semis par le framework |
| `0cca01a2` | feat(cli): donner à une application un contrôle de sa propre image     |
