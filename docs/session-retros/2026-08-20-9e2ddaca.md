---
date: 2026-08-20
session_id: 9e2ddaca-d63e-4123-99ef-a41c8f0e31f5
focus: Le document RFC 9728 monté génériquement — puis dégraissage des tableaux markdown
---

# Session retro — 2026-08-20 — 9e2ddaca

Seconde session du jour (la première : `60f8a79e`, P6.9d tranché + 2 failles fermées).
Reprise sur la Priorité 1 du `_state` : le défi RFC 9728 pointait vers un 404.

## Fait

- **`fde3d850`** — `ProtectedResourceMetadataController` + `mountProtectedResourceRoutes` dans
  `@nodefony/framework` : un document par ressource déclarée, source = les zones du pare-feu
  (`Firewall.publishedProtectedResources()`), `authorization_servers` dérivés de
  `resourceServer.issuers`. Garde d'autorité extraite en fonction pure PARTAGÉE avec le document
  d'émetteur (`onDeclaredAuthority`).
- **`71228441`** — devkit DÉCLARE au lieu de publier ; son `OAuthMetadataController` retiré. Le
  montage balaie le conteneur (`collectProtectedResources`) plutôt qu'un service nommé.
- **`18958a57`** — MIGRATION_STATUS + MEMORY de framework / security / devkit.
- **`d63f0af9`** — ligne P6.9 scindée en 12 lots (cellule de 19 600 car.).
- **`37a67492`** — 3 pavés sortis du tableau des dettes (ils n'y avaient qu'une colonne sur trois).
- **`4175391f`** — 176 tableaux déséquilibrés dé-paddés dans 109 fichiers, 451 Ko d'espaces.

## Coûts évidents

- **Une course de build a fait mesurer l'ancien binaire.** Mon `turbo --force` a `rimraf`é le dist
  du cœur pendant que le DevSupervisor buildait `http` → « build en échec — serveur courant
  conservé », et le banc restait rouge après rebranchement. Trois cycles perdus avant de lire les
  lignes `[dev]` du journal.
- Un `cd` relatif a fait tourner une suite depuis la racine : 448 fichiers « en échec » qui
  n'étaient que du ramassage hors périmètre.
- 2 redémarrages complets du serveur (build complet + stop/start), justifiés : course de build.

## Recommandations

1. **Sonde de décor ≠ objet du test** — à ajouter au patron de banc live (déjà au RETEX).
2. **Lire QUI rend un 404** (corps du routeur vs corps du controller) — diagnostic en 1 seconde.
3. La règle « annoncer le compte de rouges AVANT de couper » a servi une 4ᵉ fois, et a cette fois
   révélé une erreur d'analyse à MOI (5 annoncés, 3 obtenus → deux tests purement négatifs).

## Patterns récurrents (déjà gérés)

- ✅ `feedback_bench_probe_false_verdicts` — la sonde est le premier suspect (variante neuve ajoutée).
- ✅ `feedback_test_discriminant_or_dead` — test sans donnée discriminante (2ᵉ occurrence du jour).
- ✅ `feedback_gate_must_bite` — débranchement systématique, 4 fois dans la session.
- ✅ `feedback_bash_cwd_drift` — la garde a mordu, une fois de plus.
- ✅ La garde anti-`git checkout` sur arbre sale a mordu **sur moi** : elle allait effacer 110
  fichiers non commités. Elle fonctionne.

## Commits produits

| Commit     | Sujet                                                  |
| ---------- | ------------------------------------------------------ |
| `fde3d850` | le refus nomme un document, et ce document existe      |
| `71228441` | un module DÉCLARE ce qu'il protège                     |
| `18958a57` | doc : où le document est monté, et pourquoi            |
| `d63f0af9` | scinde P6.9 — une cellule de 19 600 car. en faisait 12 |
| `37a67492` | trois pavés sortent du tableau des dettes              |
| `4175391f` | 451 Ko d'espaces retirés — tableaux déséquilibrés      |
