---
date: 2026-08-27
session_id: ad112fb3-080c-457d-8228-f87585dc43de
focus: trancher la validation du corps (#58), et découvrir qu'un contrôle rangé dans un skill était muet depuis la migration rolldown
---

# Session retro — 2026-08-27 — ad112fb3

## Fait

- **#58 tranché et fermé** — la validation du corps reste à l'appelant, mais au SERVICE
  (`beforeCreate`/`beforeUpdate`, `await`és, communs à REST + socket + CLI), pas dans `@Body`.
  L'aval était déjà livré et inatteignable : une `ZodError` qui remonte devient un 422 avec
  `error.fields`. TSDoc, section de doc, 15 ancres recalées, gabarit `create entity` typé
  `CreatePost`/`UpdatePost`, 2 tests d'intégration du trajet complet.
- **#82 ouvert ET fermé dans la foulée** — l'audit `external` remis en état de marche.
- **Pilotage** — le statut d'un ticket se dérive du commit qui le cite (`.githooks/post-commit`),
  `npm run ticket:open` inscrit au tableau de bord, le mode END redescend les statuts fossilisés.
- **8 tickets ouverts** — parent #83 (communication sortante, REFONTE et non migration) + veille
  #89 en tête + 5 sous-tickets marqués « périmètre provisoire », et #90 (auditer le module généré
  comme l'application générée).

## Coûts évidents

- 3 rebuild + 3 redémarrages du serveur pour un seul trajet de test (débranchement compris) — le
  prix, assumé, de voir le test rouge une fois.
- 2 commits refaits : hooks contournés (`.husky`), puis scripts orphelins refusés par le gate.
- Le fil principal (#58, ~2 h annoncées) a dérivé sur l'outillage pendant l'essentiel de la
  session — dérivation JUSTIFIÉE (deux paquets publiés embarquaient une copie), mais non prévue.

## Recommandations

1. **Un contrôle vit dans le dépôt, jamais dans un skill** — appliqué à `externals`, reste à
   vérifier si d'autres skills portent des blocs de shell qui prétendent contrôler quelque chose.
2. **Suspecter un rapport qui se contredit** — c'est la contradiction interne qui a révélé le
   défaut de ma propre sonde, pas une relecture.
3. **Ne jamais chaîner `git log && git push` après un commit qui peut échouer** — `git log`
   réussit sur l'ANCIEN HEAD.

## Patterns récurrents (déjà gérés)

- ✅ 404 sur un banc = dist chargé par le serveur, pas le dist bâti → redémarrage.
- ✅ Un test neuf se voit rouge une fois, empreinte à l'appui.
- ✅ La garde du dépôt refuse un `reset` sur arbre sale — elle a mordu, correctement.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `f9e2a18d` | docs(framework): dire où se valide le corps d'une requête — closes #58       |
| `cf27e918` | fix(build): rétablir le contrôle des dépendances avalées par le bundler      |
| `9f340db1` | style: formater et délinter ce que mes deux commits précédents ont contourné |
| `06ca54ef` | feat(pilotage): dériver le statut d'un ticket du commit                      |
