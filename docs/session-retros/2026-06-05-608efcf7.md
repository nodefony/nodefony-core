---
date: 2026-06-05
session_id: 608efcf7-e59a-43fd-8c76-ab03cfd9ac73
focus: Durcissement core/http avant ORM+P6 — registre controllers + lot P2.8/P2.9/P3.7
---

# Session retro — 2026-06-05 — 608efcf7

## Focus

Établir le périmètre « 0 dette core/http/framework » avant ORM+P6, puis livrer le lot durcissement
fondations : P2.8 (backpressure) + P2.9 (`@Body({stream})`) + P3.7 (trace verbose). Démarrée par un
fix CLAUDE.md > 40k.

## Fait (3 commits poussés origin/claude-ts)

- **`e9bf9b0`** refactor(core) : `Module.controllers` **module-scopé** (`${module}:${ClassName}`) → 2 modules
  tiers homonymes coexistent sans collision ; + Kernel 2× `@ts-ignore`→cast, CliKernel bloc mort retiré ;
  CLAUDE.md 41.2k→37.7k. Test anti-collision red→green.
- **`311dbab`** feat(http) : **P2.8** backpressure (`write()===false`→`once("drain")`, RFC 9112) + **P3.7**
  trace verbose phases (`logPhasesVerbose`, perf-gate triple, 0 coût off). +9 unit.
- **`39e3f1c`** feat(framework,http) : **P2.9** `@Body({stream})` — flux brut `Readable`, route-match **hissé
  avant le parse**, parse sauté. +13 tests (8 framework, 5 e2e HTTP/1+2 dont 1 Mo).

## Périmètre établi (point migration)

core/http/framework/studio = **0 dette bloquante hors-ORM**. Reste = scope ORM (résidu `kernel/orm/*`,
session `any`) + realtime (toléré user). Audit vérité 2026-06 frais ; dashboard déjà assaini.

## Garde-fous P2.9 (demande user, tous tenus)

HTTP intrusif **minimal** (match déplacé, pas dupliqué) · WS **isolé** (`handleWebsocket` ne parse pas de
body) · P6 ordre `beforeResolve→firewall` **préservé** · perf **A/B 0 régression** · RFC 9112 §6.1/§7.1 +
doc Node `stream` validés.

## Frictions (→ RETEX.md)

- commitlint `subject-case` (sujet minuscule) — **5× cumul**, DÛ graduation.
- A/B RPS maison **bruité** : warmup + variance > écart → 3 runs/côté, comparer les plages, jeter le warmup.
- champ optionnel ajouté à un metadata partagé (`ParamMeta.stream`) casse les `deep.equal` → ne poser que si truthy.

## Validation

Build 20/20 (0 TS) · framework 185 ✅ · intégration http 400 ✅ · gate mémoire 9/9 ×2 · +22 tests unit.

## Commits produits

| Commit  | Sujet                                                             |
| ------- | ----------------------------------------------------------------- |
| e9bf9b0 | refactor(core): registre controllers module-scopé + dette TS core |
| 311dbab | feat(http): backpressure (P2.8) + trace verbose (P3.7)            |
| 39e3f1c | feat(framework,http): @Body({ stream }) (P2.9)                    |
