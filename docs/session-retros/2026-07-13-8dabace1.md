---
date: 2026-07-13
session_id: 8dabace1-f27a-4c76-a9f7-71e629f2fb72
focus: contrôle lot 3a par le user → @CurrentUser templates, redécoupage saveurs (example/rest/duplex), 2 fix framework pont api.request, fix SIGKILL trans-instance, GO playground Studio
---

# Session retro — 2026-07-13 (après-midi) — 8dabace1

## Fait (5 commits `4920dc69` → `63a5f5b2` + docs)

- `4920dc69` templates controller : `@CurrentUser()` au lieu de `RequestContext.getUser()`
  brut (question user « comment RequestContext a le context ?! » → pédagogie ALS, puis
  le décorateur idiomatique partout).
- `aa1060b7` saveurs redécoupées sur retour user : `example` (vitrine) · `rest` (CRUD
  production pur) · `duplex` (REST + socket via pont `api.request`). Banc client réel 9/9.
- `e0d93760` 2 fix framework débusqués par le banc duplex : `@Body` ← corps ALS du pont ;
  porte `@Idempotent` sur mutations du pont (`methodOverride` + `executeActionGuarded`,
  rejeu socket.mutate ne crée plus de doublon). Rouge/vert prouvé par stash.
- `63a5f5b2` fix SIGKILL : le filet start.sh (préfixe) divergeait de `splitByProject`
  (strict) → serveur de l'app imbriquée `tmp/cci-app` du user tué, superviseur zombie.
  Filet aligné strict + rapport stop visible + message superviseur « signal EXTERNE ».

## Frictions / leçons (→ RETEX.md, fait en session)

- Filet ≠ règle exacte du chemin principal → 2× → graduée dans RETEX.md.
- Capacité prouvée sur UN chemin (data plane admin) ≠ chemin voisin (controllers user
  via pont) — seul le banc bout-en-bout côté user l'attrape.
- `RealtimeClient` en Node = `nodefony/client` (entrée `.` = browser-only) ; cookie au
  handshake = transportFactory `ws` (API WebSocket standard sans headers).
- Zone `security:true` : le pont refuse l'anonyme même si le HTTP l'accepte (design).

## Décisions

- Playground Studio (GO user) : page générique data-driven, kit
  `project_studio_playground_kit` — PROCHAINE SESSION.
- Studio 2 vies (dev cockpit / admin prod assumée) ; playground dev-only dans Studio.

## Commits

| Commit   | Sujet                                                                    |
| -------- | ------------------------------------------------------------------------ |
| 4920dc69 | feat(cli): templates @CurrentUser                                        |
| e0d93760 | fix(framework): pont api.request complet (@Body ALS + porte @Idempotent) |
| aa1060b7 | feat(cli): saveurs example/rest/duplex                                   |
| 63a5f5b2 | fix(dev): filet strict + message superviseur signal externe              |
