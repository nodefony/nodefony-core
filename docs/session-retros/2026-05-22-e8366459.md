---
date: 2026-05-22
session_id: e8366459-f3ee-46b7-9a82-093bca170a76
focus: Debug CI (build/test-unit/test-integration) + hooks git + configs racine + fix superviseur orphelins
---

# Session retro — 2026-05-22 (soir) — e8366459

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |   106 |
| Read            |    23 |
| Write           |    15 |
| Edit            |    15 |
| AskUserQuestion |     4 |

## Top fichiers Read/Edit

| Fichier                |       × |
| ---------------------- | ------: |
| Kernel.ts              | 4R / 1E |
| node.js.yml (workflow) |      3R |
| DevSupervisor.ts       | 1R / 5E |
| BUG_REPORT.md          | 2R / 2E |

## Coûts évidents

- **`rm -f .git/index.lock` répété ~6×** avant chaque commit (dette husky : hook lent + crash terminal initial laisse le lock). Fait perdre un cycle à chaque commit.
- **Debug CI multi-cycles** (~3 runs × 5-8 min) : inévitable car le `terminate:0` integration est **CI-only** (non reproductible local, même `CI=true`). Le diag via `terminate()` instrumenté a finalement localisé `CliKernel.js:155`.
- **Machine saturée par 16 superviseurs `nodefony` orphelins** (2-7h uptime, accumulés sessions antérieures) + mes lancements `production`/`test:integration` en background sans cleanup. ~4 commandes de nettoyage.
- Repro serveur local 2× (production --no-daemon) — discipline kill respectée.

## Recommandations

1. **`gh run watch --exit-status` n'est pas fiable** (a conclu exit 0 sur un run failure 2×) — toujours revérifier via `gh run view --json conclusion`.
2. **Skip perf-tests en CI = pattern** : root hook mocha (`.mocharc.cjs` + `perf-skip.cjs`) sur titre « < N ms ». Réutilisable pour tout module avec seuils temporels.
3. **Discipline background** (déjà mémorisée [[feedback_watch_rollup_pitfall]] volet C) : ne JAMAIS lancer serveur/superviseur en background sans cleanup dans la même tâche.
4. **typecheck = gate distinct du build rollup** : `tsc --noEmit` rejette ce que rollup avertit (TS18036). Désormais hook pre-push.

## Patterns récurrents (déjà gérés)

- ✅ Dist périmé → clean+build (non rencontré, hooks ok)
- ✅ Superviseurs orphelins → fix single-instance livré ([[feedback_watch_rollup_pitfall]] volet C)
- ✅ Commits FR apostrophes → `git commit -F` ([[feedback_commit_fr_apostrophes]])

## Commits produits (claude-ts, poussés jusqu'à 6a4b26c)

| Commit  | Sujet                                              |
| ------- | -------------------------------------------------- |
| 6db77ba | fix(ci) production --no-daemon (sans PM2)          |
| d669932 | fix(dev) DevSupervisor single-instance (orphelins) |
| 211bf51 | docs(readme) réécriture histoire 2017→v10          |
| 5b787dd | fix(http) typecheck CI TS18036 (#storages→private) |
| e5d3344 | perf(hooks) pré-filtre pre-commit                  |
| 20b9006 | chore(tooling) hooks git + configs racine          |
| 05ce331 | fix(tests) skip perf en CI (test-unit)             |
| 6a4b26c | chore(diag) trace terminate() [À RETIRER]          |
