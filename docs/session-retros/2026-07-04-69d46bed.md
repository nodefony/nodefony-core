---
date: 2026-07-04
session_id: 69d46bed-811d-45c1-b5b9-1c250bd00eab
focus: Phase 0.8 lot 3 — infra déclarée (database/cache/logs) + stores "auto" + fix dialecte Drizzle
---

# Session retro — 2026-07-04 — 69d46bed (session 4 du jour)

## Fait

- **Lot 3 LIVRÉ** (`e1e3da5a`, 37 fichiers) : core `src/config/infra.ts` (`resolveInfra` pure +
  `resolveAutoStore` borné aux backends enregistrés, 21 tests) · `kernel.infra` + `ctx.infra` ·
  `store:"auto"` défaut sur 7 briques (chaque consommateur logge choix + provenance) · fix
  `DrizzleService.#connectOne` (dialect/url propagés, `DRIZZLE_DB_FILE` supprimé) · garde
  fail-loud entité non portée (message actionnable vs TypeError) · logs URL⇒driver · redis
  `when: !!ctx.infra.cache` · provisionUsers branche mongoose.
- **Renommage à chaud « rôle » → « infra »** sur recadrage user (collision `ROLE_*` security) —
  avant commit, 0 coût API.
- Preuve terrain : 3 boots (solo / sqlite / postgres) + banc postgres cross-pod 7/7.

## Frictions

- Vocabulaire « rôle » : collision non détectée avant d'écrire l'API → RETEX (grep du candidat).
- Gates incomplètes annoncées : le user a rappelé les e2e (realtime/frontend/doc/user/orm-core/
  framework-intégration/banc pg) → RETEX (gates d'un lot transverse = TOUTES les suites).
- `start.sh` TIMEOUT 25 s après clean build (faux négatif connu, boot à ~95 s) → RETEX incrémenté.
- commitlint : header > 100 chars rejeté → sujet raccourci. `index.lock` husky orphelin (connu).

## Commits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `e1e3da5a` | feat(config): infra déclarée + stores auto + fix dialecte Drizzle (0.8 lot 3) |
