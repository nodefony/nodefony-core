---
date: 2026-08-28
session_id: ad04dc59-c7a7-4c04-8e7b-e05fa3ee7df9
focus: brancher schemaDiff (rattrapage dev + verdict divergent), livrer la page des migrations, et corriger tout ce qui niait encore les migrations
---

# Session retro — 2026-08-28 — ad04dc59

## Fait

- **`94b3f1e2`** — `schemaDiff.ts`, code mort depuis la veille, branché **des deux côtés** :
  rattrapage additif au démarrage en mode `auto`, et verdict `divergent` en `migrate`/`none`.
  Lecture du catalogue extraite en implémentation unique (`catalog.ts`), partagée par les trois
  pilotes de migration ET par l'ORM.
- **`08cc9f3f`** — `drizzle/docs/migrations.md` (patron d'orchestrateur, droits du compte qui
  migre, expansion/contraction, pourquoi PAS de sauvegarde automatique) + **7 documents** qui
  niaient encore les migrations, dont le gabarit figé dans chaque application créée.
- **`205de867`** — `CLAUDE.md` + `MEMORY.md` du module recalés.
- **Ticket [#103](https://github.com/nodefony/nodefony-core/issues/103)** ouvert (10 ancres
  vérifiées) : `nodefony status` ne nomme pas qui retient la mise en service.

## Ce que seule l'EXÉCUTION a montré

1. **Une colonne manquante ne donnait pas une erreur à la requête : elle TUAIT le démarrage.**
   Le `CREATE INDEX` dérivé la référence et échoue avant tout diagnostic. Sorti à la **première
   maquette**, avant d'avoir écrit une ligne de production — la contre-mesure de la session
   précédente a fonctionné, pour la première fois.
2. **Le cas « fréquent » l'est moins qu'annoncé** : sur les entités du framework, la plupart des
   colonnes sont `NOT NULL` → elles tombent en `blocking`. Le message compte autant que le geste.
3. **Le banc de boot réel a rendu un rouge JUSTE** : après une suppression de colonne assumée, le
   verdict passe de « à jour » à « divergent ». Le test avait raison AVANT que cette source
   existe — c'est l'attente qu'il fallait corriger, pas le code.

## Frictions

- Sept documents périmés trouvés seulement parce que le user l'a demandé — la question « qui
  d'autre AFFIRME ce que je viens de rendre faux ? » n'a pas été posée au moment du commit.
- Une action rendue par un refus proposait `orm:generate`, **qui n'existe pas** : brancher une
  capacité rend soudain visible tout ce qu'elle disait dans le vide.
- Deux affirmations fausses dans le brouillon du ticket (une ancre, un compte) — rattrapées en
  lançant ce que le ticket annonce.
- `grep` sans `-a` s'est tu sur `DrizzleMigrator.ts` : gotcha déjà écrit dans le `CLAUDE.md` du
  module, appliqué seulement après l'échec.

## Preuves

- drizzle **1151 verts / 3 dialectes** · suite globale **9105 verts, 0 rouge** (4 infras exercées)
- boot réel **7/7** (`NF_RUN_CLI_BOOT=1`) · gate mémoire **9/9**
- **Les deux gardes vues MORDRE** : rattrapage débranché → le crash d'origine revient à
  l'identique ; troisième source débranchée → le `divergent` retombe.
- Gates doc : doc-lint 2/2, ancres 7/7, ancres internes 2/2, code-check 1 bloc compile.

## Non fait

- le `hint` dans le corps d'erreur (§3.7 c) — vit dans `@nodefony/http`, autre module
- les e2e **des commandes** sur PostgreSQL/MySQL : le banc de boot réel est sqlite seul
- `test:all --load` et `--dialects` (MySQL Community) non lancés

## Commits produits

| Commit     | Sujet                                                   |
| ---------- | ------------------------------------------------------- |
| `94b3f1e2` | rattrapage de schéma au démarrage + verdict `divergent` |
| `08cc9f3f` | page des migrations + 7 documents qui les niaient       |
| `205de867` | docs IA du module recalées                              |
