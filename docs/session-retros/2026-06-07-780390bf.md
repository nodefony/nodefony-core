---
date: 2026-06-07
session_id: 780390bf-6416-425f-b7a4-b2b125295d6d
focus: Audit + refonte service certificates (RFC 5280/6125) + banc TLS haproxy + proxy:generate
---

# Session retro — 2026-06-07 (3e) — certificats + banc TLS + proxy:generate

Reprise pour banc TLS ; pivot demandé par le user (« on n'a jamais audité ce service »)
→ audit RFC du service `certificates` (verdict : bancal), refonte complète, puis banc TLS
et `proxy:generate`. Discussion design dense (statiques, prod, CDN/S3).

## Fait

- **Durcissement cert** (`65f7e41`) : SHA-256 (node-forge `sign()` sans digest = SHA-1, piège
  silencieux), serial `randomBytes` 128b (était `01`), privkey 0600, SAN/IP RFC 6125, SKI,
  backdate, lazy `node-forge` (0 charge en prod explicit), CLI `certificates`, ancre CA client
  (requête vérifiée sans `-k`), **purge clé privée commitée** (sept. 2024).
- **Nettoyage** (`b1d02d1`) dirs vides + **`isCertAdequate` SAN** (`00d3dd2`, régénère si SAN change).
- **Banc TLS haproxy** (`0f302c3`) : re-encrypt `verify required`+`verifyhost`+`sni`, frontend TLS
  `:8443` proto=https E2E, `build-haproxy-pem.sh`, README. E2E prouvé (200 pas 502).
- **`proxy:generate`** (`6ac8562`, P11.7 ✅) : générateurs purs nginx/haproxy de l'introspection ;
  nginx résout le trou statics multi-modules (chaîne `try_files`) ; 12 tests.
- Mémoire `feedback_design_dialogue_prose` créée (user rejette le widget QCM 2×).

## Frictions

- **Clé privée commitée** trouvée via `git ls-files | grep pem` : gitignore avec slash = ancré
  racine, ne couvre pas les sous-modules → `**/`. (→ RETEX Git/commit.)
- **node-forge** : `cert.sign(key)` sans 2e arg = SHA-1 ; `extend(true, defaultOptions,…)` mute la
  constante partagée (cible `{}`). (→ MEMORY.md http.)
- **AskUserQuestion rejeté 2×** : le user veut clarifier en prose, pas catégoriser dans un QCM
  (le 2e cas, son besoin n'était PAS du mTLS mais « requête TLS vérifiée sans -k »). → gradué.
- Session **très longue** (scope ajouté en continu : audit → refonte → banc → proxy:gen → design CDN).

## Commits produits

| Commit    | Sujet                                                                                  |
| --------- | -------------------------------------------------------------------------------------- |
| `65f7e41` | feat(http): durcis le service certificates (SHA-256/serial/SAN, lazy node-forge) + CLI |
| `b1d02d1` | refactor(http): retire les dossiers cert vides                                         |
| `00d3dd2` | fix(http): isCertAdequate vérifie le SAN pour les deux stratégies                      |
| `0f302c3` | test(docker): banc TLS haproxy — re-encrypt validé + frontend TLS proto=https E2E      |
| `6ac8562` | feat(http): commande proxy:generate (nginx\|haproxy) dérivée de l'introspection        |

## Prochaine étape

Chantier **statics « préfixe natif »** (impactant, flaggé, pas codé) : module `public/` auto-monté
sous `/<module>/` via `addMount` (0 symlink, 0 sous-dossier manuel, 0 collision) ; + config
`assetBaseUrl`/CDN + commande `assets:publish` (S3 sync depuis la carte préfixe→dossier). PUIS
boussole = **durcissement ORM**.
