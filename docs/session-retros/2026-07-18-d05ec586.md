---
date: 2026-07-18
session_id: d05ec586-cc7b-41c4-8a69-2dd0d4218936
focus: WebAuthn — plafond d'enrôlement (faille trouvée) puis listing paginé (8e store)
---

# Session retro — 2026-07-18 — d05ec586

4ᵉ session de la journée. Reprise sur « WebAuthn = prochain store à paginer ».

## Fait

- **`1947e85f`** — plafond d'enrôlement des passkeys. Trou trouvé en vérifiant AVANT
  de coder : `verifyRegistration` faisait `save()` sans compter, `passkeys` n'avait
  pas de limite, et `login/options` (`bypassFirewall` par design) prend un `username`
  → amplification déclenchable par un anonyme. `passkeys.maxPerUser` (20) +
  `countByUser` natif × 4 backends + `WebAuthnError` 409 + mapping controller.
- **`63b4033a`** — listing paginé (8ᵉ store du standard). `listPage`/`countCredentials`,
  vue sans `publicKey`, offset natif × mémoire/Drizzle 3 dialectes/Mongoose, curseur
  SCAN composite en Redis (`countCredentials` = -1), data plane `webauthn/list`.
  Banc de contrat UNIQUE chez le propriétaire + banc Redis en double backend.

## Ce qui a bien marché

- **Vérifier le terrain avant de coder** (devise du CLAUDE.md) : la question du user
  « pourquoi on pagine pas findByUser ? » a mené à l'audit du plafond — la faille
  était là, pas dans le listing. Le sujet annoncé n'était pas le vrai sujet.
- **Non-complaisance systématique** : garde neutralisée → 6/6 rouges ; bornage du
  `limit` neutralisé → rouge. Deux fois, pas une supposition.
- **Preuve du gate par l'échec attendu** : mauvaises creds Redis → banc cassé, donc
  le vrai serveur était bien emprunté. Un `dbsize` à 0 n'aurait rien prouvé.

## Coûts / frictions

- `rg -r` **4ᵉ récidive** (sortie mutilée en `n`) malgré la mémoire graduée.
- 2 tests mongoose rouges au 1ᵉʳ jet : isolation du banc (un COMPTE est absolu, les
  cas voisins toléraient les résidus). Diagnostic correct du 1ᵉʳ coup (mon test),
  mais un coup d'œil aux cas voisins l'aurait évité.
- 2 démarrages/arrêts serveur (bancs HTTP réels + gate mémoire) — nécessaires,
  le controller et le data plane étaient touchés.

## Gates

security 829 · drizzle 743 (3 dialectes réels, 0 skipped) · mongoose 145 ·
redis 81/81 (vrai serveur, 0 skipped) · framework 463 + 96 · http 580 + mémoire 9/9 ·
typecheck code ET tests sur 5 modules · route vérifiée en réel (401 ≠ 404).

## Commits produits

| Commit     | Sujet                                              |
| ---------- | -------------------------------------------------- |
| `1947e85f` | plafond d'enrôlement des passkeys (fix sécurité)   |
| `63b4033a` | listing paginé des passkeys — 8ᵉ store du standard |
