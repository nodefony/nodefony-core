---
date: 2026-06-01
session_id: 961eb178-70a2-4162-84d6-58d52541265d
focus: Gate couleur ANSI boot-time — payloads de log bruts hors TTY (JSONL/pipe propres)
---

# Session retro — 2026-06-01 — 961eb178

## Focus

Gate couleur ANSI résolue **une fois au boot** : retirer l'ANSI baké dans les
`payload`/`msgid` des Pdu (qui polluait le `.jsonl` queryable + le pipe stdout
prod), sans `.replace()` par log (refusé, hot path).

## Fait (1 commit `7e68b05`, local sur `claude-ts`)

- **Helper `logColor`** (`src/nodefony/src/syslog/logColor.ts`, isomorphe) : slots de
  fonctions (13 combos clc) **mutés une seule fois** via `setLogColor(bool)` ; OFF =
  `identity`, ON = `cli-color`. 0 test booléen/log, chaînage `clc.x.y` hors hot path.
  `ColorFn = (string|number)=>string` (statusCode brut). Export barrel `nodefony`.
- **Câblage boot** : `Kernel.initializeLog → setLogColor(process.stdout.isTTY)`.
- **Consommateurs gatés** : core (Kernel events + banner), http (Context events,
  request-logger, pretty-request-logger, http-kernel DEBUG, WebsocketContext close/error),
  security (msgid firewall — ANSI hardcodé `\x1b[36m` éliminé). Console (`wrapper`/`inspect`)
  sur le même flag.
- Doc : `syslog/MEMORY.md` + MIGRATION_STATUS (LB → gate ✅).

## Décisions (POURQUOI)

- **Critère = `isTTY`** (terminal interactif → couleur ; pipe/fichier/non-TTY → brut). Choix
  tranché : le cas qui compte = prod (stdout→Loki, détaché, CI) = non-TTY → propre. Le dev
  **TTY foreground** garde un JSONL coloré (sous-produit assumé) ; strip viewer Studio =
  micro-tâche froide séparée si besoin. Pas d'option config ajoutée (chirurgical).
- **Slots rebindés > proxy par appel** : 0 coût/log (lookup propriété V8-inlined + appel),
  vs un proxy trap par expression couleur. Allocation-neutre (OFF retourne l'entrée, _moins_
  d'allocs qu'avant qui créait une string colorée).

## Frictions (→ RETEX.md)

- `grep $'\x1b'` ne matche pas l'ANSI dans un JSONL (JSON encode ESC en texte ``).
- DevSupervisor rebuild+restart à chaque save → baseline before/after par mtime ment ;
  preuve = raisonnement sur le mécanisme. Memory-test flake sous charge cumulée (déjà gradué).

## Gates

build 3 modules 0 erreur TS · core 1493 · http unit 321 · intégration 720/0 fail ·
memory OK isolé (échecs rotatifs sous charge = flake GC) · runtime : JSONL 0 ANSI.

## Commits produits

| Commit    | Sujet                                                               |
| --------- | ------------------------------------------------------------------- |
| `7e68b05` | feat(syslog): gate couleur ANSI boot-time — payloads bruts hors TTY |
