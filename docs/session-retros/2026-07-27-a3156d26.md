---
date: 2026-07-27
session_id: a3156d26-24f5-4ace-9260-2eb690011f3e
focus: Combler les trous du banc de schéma, exiger la chaîne complète — et un bug du scaffold trouvé par la mesure
---

# Session retro — 2026-07-27 (9ᵉ) — a3156d26

## Ce qui a été fait

Trois commits, tous poussés sur `claude-ts` :

- `51828c36` — **le décor du banc** : app témoin hors du dépôt + paquets installés
  depuis les tarballs de `pack-all.mjs`, isolation **constatée** avant l'agent
  (arrêt si le constat échoue). Auto-contrôle du juge PostgreSQL (14 verdicts
  nommés + une table au DDL écrit à la main). Transcript écrit au fil de l'eau.
  Corrige au passage `pack-all.mjs`.
- `693ca6c4` — **la chaîne complète** : la consigne exige une ressource REST
  servie, paginée, refusée à l'anonyme ; un second juge démarre le serveur et
  frappe l'API. `mission.json` trace la consigne avec le run. Le rapport cherche
  les tables sous un autre nom au lieu d'annoncer « 0 colonne ».
- `d2598386` — **bug du scaffold** : le générateur déduisait le dialecte du
  fichier de config alors que l'app déclare sa base par URL. Branché sur
  `resolveInfra` (source unique). Deux tests neufs, vus rouges. Plus
  `docs/guides/eprouver-loutillage-agent.md`.

## Le résultat qui compte

Le décor a **inversé le verdict** : 0 appel au générateur dans le checkout,
**11 appels** hors du checkout. Aucun mot ajouté nulle part.

Et la mesure a livré une liste de travail chiffrée : 6 tables sur 6 au mauvais
nom (`tableName()` pluralise sans échappatoire), 32 longueurs perdues, 18
identifiants dégradés — ces deux derniers dus au bug de dialecte, corrigé.

## Coûts évidents

- Deux montages de décor complets avant d'obtenir un `npm install` vert
  (manifeste de tarballs corrompu par le build Vite de studio).
- Un correctif cru inopérant pendant plusieurs minutes : le shim délègue au CLI
  de l'application, pas au checkout.

## Recommandations

1. Corriger les deux trous restants du générateur (`--table`, colonnes SQL)
   **avant** de rejouer calcom/ghost — sinon on remesure le même trou trois fois.
2. Le juge HTTP doit être vu **échouer** (débrancher la protection de la route)
   avant d'être cru.

## Patterns récurrents (déjà gérés)

- ✅ décor de banc qui ment (thème RETEX « Mesure & bancs »)
- ✅ juge non éprouvé → auto-contrôle à chemin indépendant + `--prove`

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `51828c36` | le banc mesurait un agent mieux servi que l'utilisateur réel  |
| `693ca6c4` | le banc exige la chaîne complète, et cesse de mentir          |
| `d2598386` | le générateur voyait sqlite pendant que l'app tournait sur pg |
| `ae847829` | retex + 5 frictions au SAS                                    |
| `a1f38a56` | le nettoyage inter-runs visait des noms que rien ne portait   |
| `3390ceec` | le juge arrête ce qui traîne, et cesse de compter `/dev/null` |

## Ce qui n'a PAS été fait, et qu'il faut dire

- Le **juge HTTP n'a toujours jamais tourné en entier** : son premier run est
  mort sur des ports tenus par l'agent lui-même. Corrigé, mais le correctif
  n'est **pas éprouvé** — il n'y avait pas de décor sale sous la main.
- Les chiffres de TYPE du dernier run (18 uuid dégradés, 32 longueurs perdues)
  sont **ceux du run précédent** : le nettoyage ne supprimait rien. La mesure
  est à refaire. Seul `29 → 5` éditions à la main est fiable (lu au transcript).
- Les deux trous du générateur (`--table`, colonnes SQL) sont **diagnostiqués et
  chiffrés, pas corrigés**.
- `calcom` et `ghost` : toujours aucun run réel.
