---
date: 2026-06-02
session_id: 1f1261c4-c352-482c-bc4a-6b7a2406be2a
focus: help global enrichi (idée b) + refonte KernelType → IRunProfile ; décisions REPL/introspection, isTTY, virage ORM
---

# Session retro — 2026-06-02 — 1f1261c4

## Focus

Suite « boot de rêve » : idée (b) help global qui liste les commandes de module, puis refonte du
binaire `KernelType` (SERVER/CONSOLE) en profil d'exécution déclaratif `IRunProfile`. Beaucoup de
discussion design (REPL ≠ feature mais substrat d'introspection, TTY, virage ORM).

## Fait

- **`1bd6118`** `feat(cli)` — help global (`nodefony`/`--help`/`-h`) liste AUSSI les commandes de
  module : `isGlobalHelpRequested()` + `dispatchGlobalHelp()` (boot CONSOLE jusqu'à `onPreRegister`
  → commander connaît les commandes module → `showHelp` + `terminate(0)`). `--version` non concerné.
  2 tests intégration. Idée (b) du backlog P11.6 livrée.
- **`d1a3bc9`** `refactor(core)` — `KernelType` (`"console"|"server"|"CONSOLE"|"SERVER"`) → `IRunProfile
{servers, lifetime, interactive}` déclaratif. `setType`→`setRunProfile`, `isConsole()`=`!servers`,
  8 consommateurs rebranchés (CliKernel/Orm core/mongoose+sequelize orm+SessionStorage). Scope A =
  0 comportement changé. Gates : core 1511 ✓, filet CLI 1517 ✓, memory 9/9 ✓.
- Docs kernel (CLAUDE/MEMORY/README) + MIGRATION (P11.6 idée b, P7 virage ORM) à jour.

## Décisions (le POURQUOI qui survit au /clear)

- **Le REPL n'est pas une feature, c'est un substrat** → viser la **surface d'introspection du Kernel
  live** (routes/services/config/état) dont REPL + IA (P12) + Studio + audit sont des clients. Mémoire
  `project_kernel_runmodes_introspection`.
- **`KernelType` quasi-inerte** : le montage serveur = `kernelEvent` + présence `HttpKernel` ; le
  rester-en-vie = park. `type` ne pilotait que 4 gates de log → refonte = nettoyage de modèle, pas
  de comportement. 3 axes orthogonaux (servers/lifetime/interactive) au lieu d'un binaire.
- **`isTTY` = volet ENVIRONNEMENT** (capacité) qui complète `interactive` (besoin déclaré) →
  `interactif = runProfile.interactive && kernel.isTTY` ; résoudre 1× au boot (comme gate ANSI),
  surfacer dans le banner (rappel). Cloud-native → toujours false → jamais de hang.
- **🔀 VIRAGE ORM** : Sequelize SUPPRESSION COMPLÈTE ; Mongoose REFAIT NEUF sur modèle Drizzle
  (`extends Service`) ; `kernel/orm/{Orm,Connector,Entity}` core → MORT, à retirer (le core ne
  connaît pas l'ORM). Mémoire `project_orm_hardening_kit`.

## Frictions (→ RETEX.md)

- Refonte field largement consommé : le BUILD TS est le filet (grep rate des formes), `?.`/`?? true`
  défensifs pour préserver le no-crash.
- `memory.test` exige serveur lancé (ECONNREFUSED sinon) + conflit ports avec le filet CLI → séquencer.
- `tsx` transpile-only laisse passer un test qui lit un field supprimé.
- commitlint subject-case (3× → à graduer).

## Commits produits

| Commit    | Sujet                                                             |
| --------- | ----------------------------------------------------------------- |
| `1bd6118` | feat(cli): help global liste aussi les commandes de module (b)    |
| `d1a3bc9` | refactor(core): remplace le binaire KernelType par un IRunProfile |
