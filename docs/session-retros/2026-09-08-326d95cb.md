---
date: 2026-09-08
session_id: 326d95cb-5e41-4905-b6dc-d53dc5c3a7ca
focus: la troisième sortie du cycle des retex (leçon → porteur), puis la doctrine des connexions appliquée à Redis
---

# Session retro — 2026-09-08 — 326d95cb

## Fait

- **Le chaînon manquant du cycle des retex** (`ac173467`, `804e1847`, `128f75cc`). Question du user :
  « à quoi servent ces retex ? un jour un retex a-t-il amélioré le produit avec du code ? ». Réponse
  mesurée, et rendue REPRODUCTIBLE par `npm run lessons:carriers` : sur 115 leçons durables,
  **8 sont portées par du code livré**, 30 par un automate, 77 relues seulement, 0 inerte.
  `--recos` ferme la boucle la plus ancienne — **937 recommandations dans 310 retex archivés**,
  dont 30/36 mémoires nommées écrites et 16/27 skills créés.
- **16 ancres mortes soldées** — dont deux mémoires qui décrivaient un mécanisme DISPARU comme la
  vérité courante (`nodefony/config/schema.ts`, absent partout ; le root-hook mocha `perf-skip.cjs`,
  supprimé avec Mocha). `--strict` sortait en 1, il sort en 0.
- **3 thèmes mûrs gradués** (`8c1a6f23`) — sas 721 → 526 lignes. Deux sur trois avaient déjà leur
  maison ; une seule mémoire neuve (`feedback_written_rule_needs_reread`).
- **La doctrine `runNeedsExternalServices` appliquée à Redis** (`f8502a1c`, ticket #279 ouvert et
  fermé). Redis était le seul service à connecter au boot sans demander si le run ouvre des
  connexions : un `nodefony inspect` dans un pod portant `NF_REDIS_URL` y ouvrait trois sockets.

## Frictions du jour

- **Le user a arrêté deux réflexes de pilotage** : brancher un outil neuf au END (déjà jugé trop
  long), et ouvrir un ticket pour un travail déjà commité. Les deux étaient des rituels.
- **Mon propre `✓` a menti** : une édition avait disparu de l'arbre, vue seulement au `git diff --stat`.
- **Mon `cd` a lancé vitest depuis la racine deux fois** → « 529 fichiers en échec », un chiffre
  alarmant issu d'un run sans décor, qu'il ne fallait surtout pas lire comme une régression.
- **Ma garde ne gardait rien** : `RedisService` passe `null` en 3ᵉ argument de `Service`, donc
  `this.kernel` y est TOUJOURS nul. Ni le typecheck ni la suite ne le disaient — c'est le banc qui
  l'a trouvé, en débranchant.
- **Un vert aveugle** : `RedisBackplane.test.ts` crée ses clients au client `redis` NU, donc ses
  11 verts ne disaient rien du chemin modifié. Le user a demandé ce banc ; c'est ce qui a révélé
  que le chemin du backplane n'était éprouvé nulle part.
- **`rc=127`** : `timeout` absent sur macOS — l'instrument, pas le code.

## Recommandations

1. Lancer `npm run lessons:carriers -- --recos` au prochain CONSOLIDATE — la liste des
   recommandations non suivies n'est PAS un backlog, c'est une lecture de ce qui a été accepté
   par le silence.
2. Ne jamais conclure d'un run vitest lancé depuis la racine sans décor — le nombre de rouges y est
   un artefact de collecte.

## Patterns récurrents (déjà gérés)

- ✅ `feedback_prove_the_target_not_the_verdict` — le run mal ciblé, le `rc=127`, le vert aveugle.
- ✅ `feedback_gate_must_bite` — la garde vue mordre en débranchant.
- ✅ `feedback_bash_cwd_drift` — le `cd` qui déplace la cible.

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `ac173467` | tracer QUI porte chaque leçon — la troisième sortie du cycle des retex  |
| `804e1847` | refermer la boucle recommandation → action sur les retex archivés       |
| `128f75cc` | la liste des recommandations non suivies n'est pas un backlog de skills |
| `8c1a6f23` | graduer les 3 thèmes mûrs et solder les ancres mortes du sas            |
| `f8502a1c` | fix(redis) : ne pas se connecter quand le run ne déclare pas son besoin |
