---
date: 2026-09-04
session_id: af56c47f-66e6-4125-9b37-63f21ae930a4
focus: doctor --live (#182) livré ; le pilotage des tickets devient un automate ; deux CI rouges réparées, dont une de ma main
---

# Session retro — 2026-09-04 — af56c47f

## Fait

- **#182 fermé** — `nodefony doctor --live` : l'étage 2 sort du fast-path, boote en console
  (`onPostReady`, zéro port) et interroge `orm/migrations` et `security/firewall` par le plan
  d'administration. Rien n'est recalculé ; `summary` et `nextActions[0]` sont rendus tels quels.
- **`board-lint.mjs`** — neuf constats à verdict binaire sur le tableau de bord, branché au RESUME
  et au END. Répond à la demande du user : « je te reprends à chaque fois sur la priorisation ».
- **Deux CI rouges réparées** — la première héritée de la veille (tests qui héritaient de leur
  décor), la seconde de ma main (« non demandé » condamné comme « empêché »).
- Tableau recalé : #187 inscrit, six statuts menteurs redescendus, quatre estimations posées,
  ordre de la grappe doctor remis sur les dépendances réelles (#183 avant #184).

## Coûts évidents

- **Deux allers-retours CI complets** (~25 min chacun) pour des défauts qu'un test de CHAÎNE aurait
  attrapés en trois secondes. C'est le poste dominant de la session.
- Le hook pre-commit a rendu **code 0 en bloquant**, deux fois (fiches de skills périmées) — le
  commit n'avait pas eu lieu et seul `git log -1` le disait. Piège déjà consigné, repayé.
- `tsc -p tsconfig.json` ne lit AUCUN fichier de test (`tsconfig.tests.json` est séparé) : quatre
  décors incomplets compilaient « vert » chez moi, le hook pre-push les a arrêtés.

## Recommandations

1. **Un test de chaîne pour toute règle qui lit l'environnement** — deux fois la même faute dans la
   même session, la seconde APRÈS en avoir tiré la leçon. La brique pure ne prouve pas l'appelant.
2. **Avant d'ajouter un membre à un type union, chercher ses énumérations littérales** (`rg` sur
   deux valeurs existantes) : elles compilent toujours après, donc elles se taisent.
3. **Ne jamais déléguer une borne de mot à `--grep`** : `\b` n'est pas de l'ERE. Dégrossir avec
   l'outil, trancher en code éprouvé.

## Patterns récurrents (déjà gérés)

- ✅ Commit en arrière-plan + vérification par `git log -1`, jamais par le code de sortie.
- ✅ Débranchement systématique des gardes neuves (4 lots vus rouges), restauration par empreinte.
- ✅ Délégation des journaux > 2 000 lignes à `nodefony-run-log-report` (2 runs, `haiku`).

## Commits produits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `9f364da8` | feat(pilotage): contrôler le tableau de bord au lieu d'espérer qu'on y pense  |
| `4ebab3fa` | fix(tests): que les tests de doctor ÉNONCENT leur décor au lieu d'en hériter  |
| `25ef9abb` | feat(cli): doctor demande à l'application, pas seulement à ses fichiers       |
| `726119d9` | refactor(cli): nommer en anglais la constante ajoutée par doctor --live       |
| `6039aa48` | fix(pilotage): que le contrôle du tableau cesse de nier des commits existants |
| `7a32d582` | fix(cli): que doctor ne condamne pas ce qu'on ne lui a PAS demandé            |
