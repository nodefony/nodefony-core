---
date: 2026-08-25
session_id: bbc4de2a-7502-4e53-93e2-f5d48205215d
focus: orm.yml rouge depuis deux jours — et ce que sa fermeture a révélé (pod tué en transaction, seuils mémoire décoratifs, Windows ouvert)
---

# Session retro — 2026-08-25 — bbc4de2a

## Focus

Reprise sur le seul rouge de la forge : `orm.yml`, rouge depuis le 2026-08-23 sans lecteur.
Fermé — puis la levée de chaque rouge en a découvert un autre, jusqu'à deux défauts
produit sérieux et un gate mémoire qui ne gardait rien.

## Fait

- **`orm.yml` VERT**, ses trois jobs disaient trois choses différentes (faux rouge
  d'instrument sur Redis · deux vrais défauts ORM · décor de gate inapplicable).
- 🔴 **Une base qui tombe PENDANT une transaction tuait le pod** (`pg-pool` retire son
  auditeur `error` du client tant qu'il sert) — le défaut déjà fermé sur le pool, par le
  chemin resté ouvert.
- 🔴 **En MySQL, la coupure n'émet AUCUN événement** — 30 s d'aveuglement contre quelques
  ms sur `pg`. Le socket sert désormais de signal, et fait trancher un battement anticipé.
- **`memory.yml` et `e2e-autonomes` ouverts aux 3 systèmes** (12 étapes sans `shell:`
  fermées d'abord). Verdict immédiat : mémoire verte partout, Windows rouge sur e2e — un
  kill de GROUPE qui ne tue rien là-bas et empoisonnait le banc suivant.
- **Les seuils du gate mémoire sont 55 à 572× trop larges** — invisible jusqu'ici, le
  delta n'étant publié qu'en échec. La marge s'imprime maintenant à chaque passage.
- **Verdict `ViteProcessSupervisor` sous Windows** : 6 cas verts, sans `skipIf` de
  plateforme (spawn réel, dont l'auto-restart après SIGKILL).
- **5 gates neufs, chacun vu ROUGE avant d'être cru** : variables de gate vs workflows ·
  passes filtrées · actions locales déclarées · kill portable dans les bancs · marge
  mémoire publiée.

## Frictions

- **Quatre défauts, une seule faute** : une règle appliquée à un seul frère (gate Redis,
  `attendreServeur`, `NF_GATES_ALLOW`, `consumeHandshake`). Coût : deux allers-retours de
  forge, dont un où le rouge suivant était MASQUÉ par celui que je venais de fermer.
- **Deux fois, un banc a redeviné une primitive que le cœur publie** (`signalProcessGroup`,
  après `besoinDeShell` la veille).
- **Ma recommandation de retirer le banc fan-out était FAUSSE** — le user a tenu bon : le
  banc utilise `node:cluster`, le test vitest `child_process.fork()`. Deux choses
  différentes. C'est en vérifiant sa remarque que les 5 déclencheurs muets sont sortis.
- **Mon premier gate n'a pas mordu au débranchement** (il comptait une présence dans le
  fichier, pas par bloc `paths:`) — corrigé, puis revu rouge.
- **Le user a demandé « tu as fait les tests pour ça ? »** sur un correctif que j'avais
  vérifié mais pas testé. Il avait raison : le gate est né de sa question.
- **`tac` et `timeout` n'existent pas sur macOS** — deux commandes perdues.

## Recommandations

1. **Resserrer les seuils mémoire** sur les chiffres des 3 plateformes (l'instrumentation
   est en place) — sans quoi le gate reste décoratif.
2. **Chercher les frères AVANT de commiter** : `rg` sur le MOTIF corrigé, pas sur le
   fichier. C'est le poste de coût n°1 de cette session.
3. **Avant d'écrire une primitive système dans un banc**, chercher qui la porte dans le
   barrel `nodefony`.

## Commits produits

| Commit     | Sujet                                                           |
| ---------- | --------------------------------------------------------------- |
| `e6374174` | gate Redis exigeait un nom que la forge ne posait plus          |
| `e595ef36` | MySQL : la coupure n'émettait aucun événement — le socket parle |
| `32e7f574` | 🔴 base qui tombe PENDANT une transaction tuait le pod          |
| `e9f74308` | un rouge en masquait un autre — les deux passes filtrées        |
| `7d228d70` | liste blanche : 5 dépendances muettes                           |
| `fa53f53c` | WS : attente muette rendait « timed out »                       |
| `62319343` | memory + e2e-autonomes aux TROIS systèmes (12 étapes muettes)   |
| `30f5ede4` | kill de GROUPE inopérant sous Windows — résidu volant les ports |
| `7f88e22e` | gate : le kill deviné ne peut plus revenir dans un banc         |
| `54e2653a` | seuils mémoire 55 à 572× trop larges — la marge est publiée     |
