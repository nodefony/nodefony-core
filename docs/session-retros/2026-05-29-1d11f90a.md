---
date: 2026-05-29
session_id: 1d11f90a-8d9a-4385-881c-d77cb19311f8
focus: Audit profond du pipeline @nodefony/http — P2.3/4/5 + P4.2 + 3 bugs body-parsing/listener + champs morts
---

# Session retro — 2026-05-29 — 1d11f90a

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |   123 |
| Read       |    50 |
| Edit       |    37 |
| TaskUpdate |    12 |
| Write      |     6 |
| TaskCreate |     6 |
| Skill      |     2 |
| ToolSearch |     1 |

## Top fichiers Read/Edit

| Fichier                   | Read | Edit |
| ------------------------- | ---: | ---: |
| http/HttpContext.ts       |    5 |    4 |
| http/Request.ts           |    3 |    5 |
| http-kernel.ts            |    4 |    — |
| framework/Resolver.ts     |    4 |    — |
| http/parser.ts            |    — |    3 |
| tests/unit/parser.test.ts |    3 |    2 |

## Coûts évidents

- **Session XXL non découpée** : 4 fils distincts (P2 lot / P4.2 / audit pipeline / HTTP/2) en UNE session, 244 tours, 0 `/clear` → cache-read = 60,7 M tokens.
- **Turbo cache empoisonné = ~30 min ×2 perdus** : dist périmé à mtime FRAIS sur module test (404 fantômes) PUIS dist http (faux `@Body` cassé). Faux signal « régression ». Résolu par rebuild direct `rm -rf dist && npm run rollup`.
- Nombreux restarts serveur (gate mémoire à chaque modif pipeline) — justifiés mais coûteux.
- `.git/index.lock` orphelin 2× (commits husky interrompus) → rm manuel.

## 💶 Coût (€)

- Total **~€113 (≈ $123)** sur 244 tours — décompo : **cache-read $91 (74 %)**, output $16, cache-write 1h $15, input $0,5.
- Enseignement net : le coût est **dominé par le CONTEXTE relu** (session longue portée 244×), PAS par l'output. €113/9 commits ≈ €12,6/commit. **Lever #1 raté : `/clear` entre les 4 fils** aurait coupé le cache-read de moitié+. Une session = un fil.

## Recommandations

1. **Découper** : 4 fils = 4 sessions courtes avec `/clear`. Vu le cache-read à 74 %, c'est LE levier (déjà dans CLAUDE.md hygiène, pas appliqué ici car l'user enchaînait « continue »).
2. **Turbo dist poisoning** → mémoire [[feedback_turbo_cache_stale_logs]] durcie : vérifier un fix par `grep` du dist (jamais mtime), `rm -rf dist && npm run rollup` en debug actif.
3. **Pattern champ-mort-à-effet-de-bord** = mine d'or : 3 passes ont donné 6 champs morts dont 2 bugs réels (`dataSize`, race body). Réflexe à garder : grep `.champ` hors `=` → 0 lecture = suspect.

## Patterns récurrents (déjà gérés)

- ✅ memory.test flaky GC (crashes) → isolation = vérité (vu 4× cette session, toujours vert isolé).
- ✅ `.git/index.lock` orphelin → `pgrep` puis `rm -f` (cf [[feedback_git_index_lock]]).
- ✅ « suspecter son propre diff avant pré-existant » → appliqué (le @Body cassé = turbo, pas mon diff).

## Commits produits

| Commit    | Sujet                                                                         |
| --------- | ----------------------------------------------------------------------------- |
| `010f57d` | feat P2.3/P2.4/P2.5 — 499 interne, timeout→AbortSignal, error boundary init() |
| `bf6c45e` | refactor — flag mort `timeoutExpired`                                         |
| `6480525` | **fix body parsing — race `dataSize` + drain Qs/Xml** (2 bugs réels)          |
| `c1cea18` | test P4.2 — décorateurs réponse × param (pipeline réel)                       |
| `0167a95` | refactor — leak listener jumeau `ended()` + 3 champs morts                    |
| `911af01` | test — cleanup listener `ended()`                                             |
| `df8ea97` | refactor — retire `Response.ended` (mort)                                     |
| `d3e6f81` | test — robustesse abort mid-stream HTTP/2 (write-after-end)                   |

Final : 256 unit / 641 intég / memory 9/9. HTTP/2 write-after-end audité = solide (0 crash/leak sur 15 aborts mid-stream).
