---
date: 2026-09-18
session_id: 957e55f4-2feb-4560-9bf7-3a522d7636d9
focus: Campagne devkit pré-alpha.7 — référence recalée, 4 rouges instruits, 3 tickets ouverts et fermés
---

# Session retro — 2026-09-18 — 957e55f4

## Focus

Rejouer le banc de découvrabilité avant la publication `alpha.7`, INSTRUIRE ses rouges
plutôt que les repayer, et corriger dans la foulée ce qu'ils désignent.

## Fait

- **Campagne complète** — 33 tâches × 3 répétitions, 7 h 28, source `depot`, MCP `eteint`.
  Référence recalée sur `f39d79d3`.
- **La tâche 0 ÉCARTÉE, et c'est le user qui l'a vu** : son énoncé porte
  `npm create nodefony@alpha`, donc elle installe depuis le registre PUBLIC quoi que vaille
  `NF_DEVKIT_BENCH_SOURCE`. Elle aurait mesuré l'alpha.6 — l'inverse de ce qu'un test
  pré-alpha.7 doit prouver. Le premier lancement, sans `--task`, la jouait.
- **4 rouges instruits sans repayer un run** → 3 tickets ouverts, corrigés et fermés le
  jour même (#415, #416, #417). La tâche 16 : aucun ticket, aléa d'exécution assumé.
- **4 chutes analysées** (1, 8, 26, 28) : aucune régression — le produit n'avait changé que
  sur `kernel/checks/wiring.ts` entre les deux références.
- **Dependabot** : PR #414 fermée (visait `main`), `devalue` monté en 5.9.2 sur `dev`, et le
  piège inscrit dans `dependabot.yml`.
- **#195 recalé** — il citait `runCheck.ts`, fichier disparu, et deux ancres décalées par ce
  diff.

## Frictions

- 🔴 **`task-<n>.gates.json` lu comme s'il portait le verdict.** Il ne porte que les sondes
  `gate` ; `code` et `transcript` comptent aussi. J'ai annoncé la tâche 17 « remontée à 2/3 »
  (elle est à **0/3**) et fondé dessus un argument de #416 — corrigé à sa place, avec un
  commentaire qui dit l'erreur.
- 🔴 **Mon test unitaire validait sa propre fiction.** `openSiblingRoutes` éprouvé sur des
  `@route` écrits à la main passait au vert ; le générateur réel ne trouvait rien — le préfixe
  d'un controller vit dans `@controller("…")`, les `path:` lui sont relatifs. Seul le test qui
  FAIT TOURNER le générateur l'a montré. → [[feedback_prove_on_received_artifact]]
- **Trois instruments shell faux d'affilée** : `grep -c` qui rend `0` ET sort en 1 (le
  `|| echo 0` doublait), zsh qui ne découpe pas une variable sur les espaces, et compter une
  MENTION au lieu d'un appel. → [[feedback_shell_false_diagnostics]]
- **`suivre-run.mjs` déclaré muet à tort** : `| head -25` + kill à 20 s ne rendent rien d'un
  outil qui suit un fichier. J'en avais conclu que l'agent n'écrit pas de journal.
- **Le live coûte au LECTEUR, pas à la machine** — branché sur une campagne de 99 tâches, il
  déverse des heures de gestes pour une matière qui ne rend aucun verdict. Règle inscrite dans
  le skill.

## Ce que les gardes du dépôt ont attrapé

Cinq refus, tous justes : un renvoi mort vers `lib/transcript-dialectes.mjs` (le chemin est
`scripts/lib/`), deux imports orphelins après déplacement (invisibles au typecheck), des fiches
de skills périmées, et un caractère de contrôle dans une clé de dédoublonnage.

## Recommandations

1. **CONSOLIDATE** — 4 thèmes MÛRS (🪤, 🧊 à 6, 🩹, 📏 à 6), sas à 715 lignes.
2. **Rejouer la référence de la tâche 34** — #417 change son empreinte (règle 3bis).
3. **Arbitrage à rendre** : faire de `dev` la branche par défaut du dépôt, seul vrai remède aux
   pull requests de sécurité qui visent `main`.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `26cf0164` | fix(deps): monter devalue en 5.9.2 — alerte DoS                  |
| `98a7c4b4` | docs(ci): dire que les alertes Dependabot ignorent target-branch |
| `6b5c8b20` | chore(devkit-bench): recaler la référence sur le code courant    |
| `fe456d11` | feat(cli): signaler une base d'un autre moteur que les entités   |
| `66f4d00d` | fix(devkit-bench): prouver la cause d'une création refusée       |
| `3180243a` | feat(cli): signaler les routes sœurs qu'un rôle laisse ouvertes  |
| `edb1461e` | chore(pilotage): rafraîchir l'empreinte du tableau de bord       |
