---
date: 2026-07-30
session_id: a299898d-52ad-4245-849c-9a215ae4b48a
focus: Angular BUG-2 + superviseur Vite Windows + plan de distribution + devkit 3 (bilan de boot)
---

# Session retro — 2026-07-30 — a299898d

## Fait

| Commit     | Objet                                                                                   |
| ---------- | --------------------------------------------------------------------------------------- |
| `a0e8e676` | test(frontend) — la délégation du kill d'arbre Vite est enfin pincée (3 cas, vue rouge) |
| `9bbd0578` | fix(deps) — Angular 22.1.0, le lock imbriquait l'arbre au lieu de le hisser             |
| `1757eae5` | docs(release) — §10 plan de distribution (D1-D7, lots R0-R6)                            |
| `653339be` | chore(deps) — `@angular/build` 22.1.2 + mongoose 9.9.0, les peers remontent à la racine |
| `4a56b638` | feat(kernel) — `var/last-boot.json`, le bilan du dernier démarrage (**devkit 3**)       |

## Coûts évidents

- **Un chantier repris pour rien** : le superviseur Vite Windows était corrigé depuis le 27/07,
  dans un commit intitulé `fix(syslog)`. Une délégation entière est partie le refaire ; elle a
  fini par livrer le test manquant, donc le run n'est pas perdu — mais la relecture du kit avant
  de déléguer l'aurait évité.
- **Deux allers-retours sur Angular** : le premier correctif (hissage manuel dans `node_modules`)
  a tenu jusqu'au patch suivant, qui l'a immédiatement ré-imbriqué. La vraie cause était en amont.
- **Un gate mémoire rouge pour rien** : serveur arrêté juste avant. 1 relance.
- **Une exécution annulée** : 18 `package.json` modifiés avant que le user demande un plan
  plutôt qu'une exécution. Annulé proprement (script conservé, rejouable).

## Recommandations

1. `git log -S <symbole> -- <chemin>` **avant** de reprendre un item de « RESTE » — le sujet d'un
   commit cache régulièrement ce qu'il contient.
2. Un lock npm ne se prouve que par `npm ci` + le build RÉEL, jamais par `npm ls`.
3. Quand une résolution npm résiste : comparer avec ce que `create app` écrit — c'est la
   configuration de référence, et le dépôt en divergeait.

## Patterns récurrents (déjà gérés)

- ✅ échec d'intégration → 1ʳᵉ hypothèse serveur éteint (recette C de `nodefony-debug`)
- ✅ le sous-agent PROPOSE, l'agent principal APPLIQUE et VÉRIFIE
- ✅ voir le gate ROUGE avant de le déclarer acquis (fait 2× aujourd'hui)

## Commits produits

5 commits sur `claude-ts`.
