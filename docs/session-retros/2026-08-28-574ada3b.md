---
date: 2026-08-28
session_id: 574ada3b-d154-40e9-bfa4-c4d451976446
focus: "#97 — l'applicateur de migrations : historique, verrou natif, trois dialectes"
---

# Session retro — 2026-08-28 — 574ada3b

Un lot, un ticket, deux commits. `DrizzleMigrator` livré : il planifie, valide, applique,
adopte une base existante et répare — sous verrou natif, sur les trois dialectes.

## Fait

- **`c4998acb` — #97** (fermé). `nodefony/src/migrator/` : `DrizzleMigrator` (status/migrate/
  baseline/repair), table `nodefony_migrations` auto-amorcée, trois pilotes à **connexion unique**,
  empreinte `sha256:` normalisée, sources en espace de noms ouvert, verdicts `NF_MIGRATE_*`.
- **`6ed26de9`** : `MIGRATION_STATUS.md` recalé (S5b livré, reste S5c et S5d).
- Preuves : 14 cas sqlite · 5 e2e PostgreSQL · 5 e2e mysql (MariaDB 11.4 **et** MySQL 8.4) ·
  9 contrats gravés. Suite du module **1090 passés / 0 échec**. Typecheck et build verts.
- Doc : `CLAUDE.md` + `MEMORY.md` du module — les pièges gravés au moment où le code les rend vrais.

## Coûts évidents

- **Trois lots de débranchement** pour voir les gardes mordre (12 des 14 cas sqlite, les deux
  verrous). C'est le poste le plus cher de la session, et le seul qui ait transformé « 13 verts du
  premier coup » en preuve. Sans lui, la session aurait livré des tests complaisants.
- **Un aller-retour perdu sur un faux « rien trouvé » de `grep`** (fichier accentué pris pour du
  binaire) : j'ai cru que mon propre patch avait supprimé une méthode qui était là.
- Un premier jet de `#findOutOfOrder` écrit en brouillon (boucles mortes `void`) que le typecheck
  a accepté sans broncher — réécrit avant les tests, mais il aurait pu passer.

## Recommandations

1. **Lire la ligne `Errors` de vitest, pas seulement `Tests N passed`** — cinq verts portaient deux
   crashs, et le défaut était réel (process tué au lieu d'une erreur rendue).
2. **Un test de verrou affirme l'état INTERMÉDIAIRE**, sinon il reste vert sans verrou.
3. **Confronter les prescriptions d'une conception entre elles**, pas seulement au code : celle-ci
   se contredisait à deux paragraphes d'écart (pool vs verrou de session).

## Patterns récurrents (déjà gérés)

- ✅ Débrancher pour voir rouge (`feedback_gate_must_bite`) — appliqué systématiquement ici.
- ✅ Nommer ce qui n'a pas été prouvé — les deux bases MySQL réelles, dites dans le ticket et le code.
- ✅ Ne rien lancer sur une base pendant qu'une autre suite y travaille (leçon 08-28f) — respecté.

## Commits produits

| Commit     | Sujet                                                                      |
| ---------- | -------------------------------------------------------------------------- |
| `c4998acb` | feat(orm): appliquer les migrations avec historique et verrou — closes #97 |
| `6ed26de9` | docs(migration): l'applicateur pose désormais les migrations sous verrou   |
