---
date: 2026-06-05
session_id: 2d461fc6-6aa8-4bdd-a9e7-665c1bfe79b9
focus: session perf #2/#4/#5 — query.* morts +3.2% ; différé audit & saveSession rejetés (mesure) ; banc versionné
---

# Session retro — 2026-06-05 — 2d461fc6 (7e, PERF micro-optims)

## Fait

- **`2bc852b`** perf(http) : retrait des `setParameters("query.*")` MORTS de `HttpContext.handle()` (+ 2 dans
  WebsocketContext). @Query/@Param/@Body lisent `ctx.request.queryGet` direct → le scope DI n'était lu par PERSONNE.
  **+3,2 % RPS** (A/B atomique mono prod). Gates : intégration 404 verts + mémoire 9/9.
- **`bd8873d`** chore(load-test) : banc perf A/B mono prod versionné (`scripts/bench-ab-mono.sh` + SKILL.md niveau 3) —
  fini les scripts `/tmp` éphémères (demande user).
- **Mesuré PUIS REJETÉ** (discipline A/B, 0 commit) : (a) #2 audit « différer le JSON.stringify » (objet au Pdu) =
  **−5,3 %** (le ring buffer/Syslog paie un objet plus cher qu'une string) ; (b) saveSession-skip = **+0,4 % bruit**.

## Diagnostic structurant (survit au /clear)

- **Pipeline post router-first = pas de gros poisson.** 2 hypothèses « malignes » sur 3 ont donné 0/négatif à la mesure.
  Le seul gain franc = **supprimer du travail MORT** (clés DI jamais lues), pas micro-optimiser l'async/alloc du cœur.
- **#2 audit n'a pas de free lunch** : seul levier = sampling (`log.requestLogger.sampleRate`, déjà câblé) = trade-off
  observabilité → reste une OPTION, défaut inchangé (0 log perdu). Validé avec le user.
- **Perf micro-optim = CLOSE.** Le vrai prochain levier = « fast path » (sauter par requête l'inutilisé) = chantier dédié,
  PAS du grattage #4/#5 (ROI faible + risque ctor Context/DI core). User : « je veux plus revenir sur ça ».

## Frictions (coûts)

- Mesures baseline prises machine FROIDE puis comparées après 3 gros builds (machine chaude) → fausse régression
  apparente → leçon : **A/B atomique en paires alternées** sur le MÊME build (toggle env), jamais comparer entre fenêtres.
- `client-abort-499` rouge en fin de suite = artefact de banc (j'avais lancé le serveur sur `/tmp/nf-dev.log` au lieu du
  `/tmp/nodefony-server.log` attendu) — PAS mon code. Relancé sur le bon fichier → vert.

## Commits produits

| Commit  | Sujet                                                        |
| ------- | ------------------------------------------------------------ |
| 2bc852b | perf(http) retrait setParameters("query.\*") morts +3.2% RPS |
| bd8873d | chore(load-test) banc A/B mono prod versionné + doc          |

## ➡️ Prochaine

Durcissement **ORM** (boussole [[project_orm_hardening_kit]]) → API souveraine → **P6 security**. Perf close.
