---
date: 2026-08-28
session_id: 3dac6b93-73c1-48f1-b843-3756ca6627ef
focus: Les deux dettes du front ouvertes hier, puis #37 — et quatre défauts trouvés en les éprouvant
---

# Session retro — 2026-08-28 — 3dac6b93

## Fait

- **Dette (a) — rien ne BÂTISSAIT le front.** Trois gates, à trois portées : `node.js.yml` bâtit
  les 4 vitrines + Studio sur les TROIS systèmes (8 s) ; le banc du code généré fait naître son
  témoin `--frontend vue` **plus** une étape `frontend:build --force` ; `create.test.ts` refuse une
  URL d'asset en dur dans les quatre fronts. Les trois vus ROUGE.
- **Dette (b)** — `LiveTickerController` → `LiveSalonController`, et les trois commentaires qui
  citaient `live:ticker` recalés sur `live:events` / `live:dire`.
- **#37 fermé** — `nodefony/vue` : plugin + 12 composables, la MÊME surface que `nodefony/react`
  (pas le minimum du « Fini quand »), consommé par la vitrine ET le gabarit, doc publiée.
- **Deux défauts d'outillage corrigés** (demande du user) : le banc en mode `--link` n'était jamais
  allé au bout ; `create app` rendait 0 sur un build raté.
- **`npm run test:all` VERT** : 8904 passés, 0 échec, sur infra complète (PG, MySQL, Redis, Mongo).

## Coûts évidents

- **Le cwd, deux fois.** Deux sabotages « concluants » qui n'avaient rien lancé (`Tests no tests`).
  Chaque occurrence coûte un aller-retour complet — et surtout, elle a failli faire conclure FAUX.
- **Le banc du code généré, joué trois fois** (~4 min chacun : `--link` cassé, puis isolé rouge,
  puis isolé vert). Les deux premiers runs étaient nécessaires — ils ont trouvé les défauts.
- **`test:all` joué deux fois** (~5 min chacun) : le second pour vérifier que les deux plafonds
  relevés suffisaient. Justifié — sans lui, je livrais « probablement vert ».

## Recommandations

1. **Le décor d'un banc se choisit comme un chemin de production** — sous un dossier ignoré, il
   hérite de règles écrites pour autre chose, et le gate se tait. Vaut au-delà d'oxlint.
2. **Un producteur dit son échec par son CODE DE SORTIE**, jamais seulement en prose : c'est la
   seule chose qu'un automate lit.
3. **Faire naître le décor le plus RICHE que le produit permet** — le témoin du banc naissait sans
   front, donc trois gabarits sur quatre portaient un défaut de lint que personne ne voyait.

## Patterns récurrents (déjà gérés)

- ✅ « Vert en isolation, rouge en suite » = contention, pas régression — mais le vérifier AVANT de
  conclure, et mesurer son propre diff (mon gate ajouté : 146 ms, suite scaffold 12,35 s → 11,48 s).
- ✅ Un test neuf se voit ROUGE une fois : appliqué aux 5 gates de la session.
- ✅ Déléguer la lecture d'un journal de 6 460 lignes (`nodefony-run-log-report`, `haiku`).

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `e01b2654` | fix(ci): bâtir le front pour de bon, et nommer le salon par ce qu'il fait |
| `1478ad23` | feat(client): ajouter les composables Vue 3 du temps réel                 |
| `2e96b32e` | fix(devkit): un décor de banc sous un chemin ignoré, et un create muet    |
| `b33ec1eb` | test(core): deux cas qui mesuraient la CHARGE, pas le code                |
