---
date: 2026-07-26
session_id: 9a2a1400-681b-4553-bc3c-e4d3e1089d90
focus: Solde des avertissements de lint — 263 → 109, et quatre défauts réels que le compteur cachait
---

# Session retro — 2026-07-26 — 9a2a1400 (5ᵉ du jour)

## Fait

Suite directe de la session `d` (lint 27 erreurs → 0). Cible : les 263
avertissements restants, dans l'ordre de valeur prévu par le `_state`.

| Lot                       | Avant → après | Nature                                             |
| ------------------------- | ------------: | -------------------------------------------------- |
| `no-multiple-resolved` 24 |     263 → 239 | **2 défauts réels** + 10 réécritures de lisibilité |
| `any` structurels 30      |     239 → 209 | dérogations motivées, zéro édition de typage       |
| `any` de dette 24 (http)  |     209 → 185 | alignement sur les interfaces + 1 défaut réel      |
| `no-new` 37               |     185 → 148 | 37/37 en tests, 0 en prod → zone                   |
| `always-return` 38        |     148 → 110 | `ignoreLastCallback` + 1 chaîne redressée          |
| retour du filet           |     110 → 115 | `no-multiple-resolved` réactivée en prod           |
| `no-shadow` 6             |     115 → 109 | **6 masquages d'import** en production             |

## Défauts trouvés (aucun n'était visible autrement)

1. `docsReader` — un `child_process` en échec émet `error` **puis** `close` :
   deux verdicts pour une promesse, le second écrasé en silence, écouteurs
   jamais retirés. Garde `settle` idempotente.
2. `ViteProcessSupervisor.doStop` — **ReferenceError en zone morte** quand
   `child.kill()` lève : le nettoyage lisait un `const` déclaré plus bas.
   L'arrêt du superviseur échouait. 2 tests neufs, **vus rouges** sur le code
   d'avant avec l'erreur attendue.
3. `SessionsService.addContextSession` — appelait `storage.open(context)` alors
   que l'interface ne prend aucun argument ; zéro appelant. Supprimée.
4. `Kernel.ts` — directive `eslint-disable-next-line` inerte depuis oxlint.

## Coûts évidents

- **1 faux diagnostic à 3 min** : `turbo --filter` sur 3 workspaces → 3 tests
  d'intégration CLI rouges, attribués d'abord à mon diff. Baseline stashée +
  build complet → innocent. (→ RETEX, thème décor de test.)
- 2 dérives de `cwd` interceptées par le hook du dépôt (règle déjà graduée).
- 1 aller-retour évitable : hypothèse « `resolve(); return;` calme la règle »
  testée puis annulée — le linter ne suit pas ce flux-là.

## Recommandations

1. **Ne pas prolonger sur `no-map-spread` (15) dans la foulée** — la correction
   suggérée par l'outil (`Object.assign`) **mute l'objet source** : lot à traiter
   tête reposée, jamais en fin de session longue.
2. Le `CLAUDE.md` d'un module ne doit pas CHIFFRER un inventaire que l'outil sait
   compter (celui de `framework` annonçait 6 `any`, il y en avait 22).
3. Une désactivation de règle se motive par écrit sur « et le prochain cas ? ».

## Patterns récurrents (déjà gérés)

- ✅ Preuve négative avant de croire un test neuf (`feedback_gate_must_bite`).
- ✅ Suspecter son propre diff avant de qualifier un échec de pré-existant.
- ✅ Annoncer ce qui n'a PAS tourné (drizzle : 517 tests skippés, signalés).

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `2e963543` | deux verdicts pour une seule promesse, et un minuteur lu trop tôt    |
| `0b9224a9` | les `any` que TypeScript impose cessent de compter comme de la dette |
| `76bfa5fe` | les types que les interfaces annonçaient déjà (@nodefony/http)       |
| `882a10ed` | deux règles qui ne parlaient qu'aux tests, et une chaîne redressée   |
| `81380a1c` | le filet qui avait attrapé le double verdict est remis en place      |
| `a5236808` | six variables locales qui masquaient un import                       |
