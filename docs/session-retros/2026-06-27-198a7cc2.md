---
date: 2026-06-27
session_id: 198a7cc2-8eb7-41b4-96e0-80513140bfd1
focus: Overhaul des skills de dev (back framework-dev v2 + front frontend-dev v1) + nettoyage users/profil + assainissement migration
---

# Session retro — 2026-06-27 — 198a7cc2

## Focus

Refonte des 2 skills de dev en référence AUTOSUFFISANTE tout-LLM (best practices Anthropic),
précédée d'un nettoyage Studio (users/profil) et d'un assainissement du dashboard migration.

## Fait (commits LOCAUX sur `refactor/p6-security`)

- `79469cc0` refactor(studio): badge statut compte unifié (StatusBadge partagé, a11y).
- `025397a8` docs(migration): dashboard 300K→80K + bloqueurs recadrés (MVP sécu LEVÉ · config+CLI = MVP · ORM/multi-dialecte = RELEASE).
- `f636fd74` + `277afa0b` refactor(skill): **framework-dev v2.0** — progressive disclosure (SKILL 271 l. + reference/ 1-fichier/module miné) + **bundle offline 5,8 Mo** (38 RFC + 16 Node.js + OWASP + cloud-native).
- `0bb0282d` feat(skill): **frontend-dev v1.0** — NOUVEAU skill front shippé (la brique manquante) + reference/ miné + 8 docs best-practice offline (WCAG/ARIA/NN-g/web.dev/MDN).
- `238122b4` framework-dev §0 jumeau = frontend-dev · `e03b4145` studio-dev dérive de frontend-dev.

## Décisions structurantes (survivent au /clear → kit `project_dev_skills_overhaul_kit`)

- Skill = **vérité courante** (0 changelog/retex daté ; git = histoire ; leçon → gotchas).
- **Autosuffisance** : en projet consumer le core source/MEMORY sont absents → tout dans le skill + bundles offline.
- Paire full-stack shippée = **framework-dev (back) ⇄ frontend-dev (front)** ; studio-dev = app interne qui en dérive.
- P6/roadmap/phases = **MIGRATION_STATUS uniquement**, jamais dans un skill.

## Frictions (→ RETEX.md : Shell, Refonte)

- `grep` cassé toute la session → awk/sed/rg ; un audit sous-agent au grep = **faux négatifs** (confirmer au source).
- Bash `run_in_background` = **pas de réseau** + `sleep` foreground bloqué → download par petits lots foreground.
- `.git/index.lock` orphelin (lint-staged) → `rm -f`.
- Méthode overhaul skill rejouable (progressive disclosure + mining sous-agents // + consolidation 1-fichier/module + bundle offline).

## Rien perdu (prouvé)

Ancien framework-dev (2017 l.) récupérable en git ; recettes relocalisées (marqueurs présents) ; 0 fichier non-commité.

## Reste (session suivante)

1. Slim complet `studio-dev` (sortir changelog/retex, extraire le général → pointer frontend-dev, ToC).
2. Réconcilier framework-dev Partie A (périmés Twig/`update`) ↔ B ; assainir les MEMORY modules.
3. Chantier session NIST · ORM bloqueur release (multi-dialecte).

## Commits

| Commit              | Sujet                                          |
| ------------------- | ---------------------------------------------- |
| 79469cc0            | studio: StatusBadge unifié                     |
| 025397a8            | migration: dashboard assaini + bloqueurs       |
| f636fd74 / 277afa0b | framework-dev v2.0 + 1-fichier/module + bundle |
| 0bb0282d            | frontend-dev v1.0 (nouveau)                    |
| 238122b4 / e03b4145 | §0 jumeau + studio-dev dérive                  |
