---
date: 2026-07-30
session_id: 2c481e48-86cd-488b-8333-edac49610dd9
focus: rejeu de T14 → la variance invalide la méthode ; puis assainissement complet de la CI et 404 RFC 9110
---

# Session retro — 2026-07-30 — 2c481e48

## Focus

Rejouer T14 après la correction de la veille. Le rejeu a échoué, ce qui a ouvert trois
chantiers en cascade : le bruit de la CI (216 609 lignes sur un job), la variance du banc,
et un 500 là où la RFC prescrit un 404.

## Fait

- **T14 rejouée 4×** — 2 PASS / 2 FAIL sur gabarit identique. La sonde de contenu, elle,
  est verte 4/4 (0/1 avant correction) : c'est la seule qui conclut sur un run unique.
- **Les 3 façades de fichier hissées en tête de l'`AGENTS.md`** — l'agent lit `head -20`
  puis `limit:100` ; elles étaient ligne 142/403. Échange à coût nul avec le paragraphe méta.
- **Fuite de 7 listeners `process` par `Cli`** fermée → log CI Windows 216 609 → 4 074 lignes.
- **`Event` : `nbListeners: 0` avalé par sa véracité** ; TSDoc annonçant un défaut jamais appliqué.
- **`TURBO_CONCURRENCY=4`** — `0xC0000142` sur rolldown ET tsgo, 3 runs Windows. Atténuation
  raisonnée, énoncée comme telle, avec la façon de l'invalider.
- **`GcScheduler` en horloge simulée** — 3 ms de marge en temps réel faisaient accuser un
  commit innocent. Un test à 3 périodes devient gratuit.
- **Échec du superviseur Windows rendu bavard** (journal du décor joint à l'assertion).
- **`npm test` de l'app générée annonce les suites non exercées.**
- **Juge de T14 qui nomme sa cause** (5 sorties) + garde de port avant boot.
- **Effort de l'agent dans le rapport** + variables `NF_DEVKIT_BENCH_*`.
- **Fichier introuvable → 404** (RFC 9110 §15.5.5), les 3 façades d'un coup, fuite de chemin fermée.

## Coûts évidents

- 4 runs de banc (~40 min chacun décor compris) : ~3,5 $ d'agents, incontournable pour la variance.
- 3 cycles build + restart serveur pour prouver le 404 bout-en-bout — justifiés : le test
  unitaire ne dit rien de ce que reçoit le client.
- Une enquête sur un log de décor **déjà écrasé** par mon propre essai : perdue, et c'est
  précisément ce qui a motivé la remontée de `CAUSE=` dans le rapport.

## Recommandations

1. **Ne jamais conclure d'un run unique sur un banc à verdict binaire** — la variance est
   mesurée, elle est de ±20 % sur les tours et bascule le verdict.
2. **Preuve négative systématique sur les instruments** : trois des miens étaient faux, aucun
   ne se voyait à la relecture.
3. **CONSOLIDATE** — `RETEX.md` dépasse 800 lignes ; plusieurs leçons du jour sont mûres.

## Patterns récurrents (déjà gérés)

- ✅ Faux rouge d'une sonde qui recale un agent ayant fait juste — 5ᵉ occurrence, connue du skill.
- ✅ `timeout` absent sur macOS, et ne pas conclure sur une sortie vide : le piège a resservi.
- ✅ Dérive du `cwd` après un `cd` — repris en chemins absolus.

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `3fd34344` | fraîcheur des tarballs aveugle aux gabarits                         |
| `0e7c277d` | les 3 façades en tête de l'`AGENTS.md`                              |
| `2493f7b2` | un `Cli` ne laisse plus 7 listeners sur `process`                   |
| `5dc2f0ef` | concurrence turbo alignée sur les cœurs du runner                   |
| `e765214d` | l'échec du superviseur nomme sa cause                               |
| `271eeddb` | `npm test` dit ce qu'il n'a pas exercé                              |
| `37fe7fa6` | l'ordonnanceur se teste sur une horloge simulée                     |
| `3a33f967` | le second but du banc — le nombre de tours                          |
| `f9aeed9f` | l'effort entre dans le rapport, et se préfixe `NF_`                 |
| `950a7bde` | le juge de T14 nomme sa cause ; la variance écrase l'écart d'un run |
| `109976c0` | fichier introuvable → 404 (RFC 9110 §15.5.5)                        |
