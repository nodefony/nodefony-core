---
date: 2026-07-06
session_id: efcc79ad-3cac-4997-a9f3-91099dfbf815
focus: Lot 6 Studio « Stores » — état runtime de la persistance (clôt le chantier 0.8 = fin Phase 0)
---

# Session retro — 2026-07-06 — efcc79ad

## Focus

Dernier lot du chantier 0.8 (config stores) : écran Studio « Stores » = version RUNTIME de la
matrice brique×backend, branchée sur le vrai serveur. Session de **co-design interactif** (le user a
affiné l'écran sur ~8 échanges : questions « pourquoi X pas Y » + ajouts chip / onglet transport /
provenance-source / tooltip durabilité / norme cards).

## Fait (commit `a6ec2dec`, 28 fichiers, +1419)

- Core : registre `kernel.storeResolutions` (lazy, boot-time) + `IStoreResolution` +
  `registerStoreResolution` (IKernel) + `deriveStoreBackend`.
- 7 briques enregistrent leur résolution au boot (security ×5, framework idempotency, http session).
- Endpoint `/kernel/api/stores` : registre × infra redactée × **source de champ** (default/app/env via
  `resolveStoreSource`, croise `configPath` avec la couche config `computeConfigEntry`).
- User : contrat `UserAdminApi` driver/store INVERSÉ corrigé (`store`=backend, `repository`=classe).
- Studio : page 3 onglets (Stores / Flux & transport / Utilisation & aide) + puce `BrickStoreChip`
  (header Sessions/Users/ApiKeys/Webhooks/Audit) + dualité store/driver (onglet transport).
- Gates : build 19/19, typecheck studio, memory.test http 9/9, endpoint prouvé au curl.

## Frictions / leçons (→ RETEX.md thème Front/Studio)

- Écran runtime > matrice statique (révèle les gaps d'implémentation) ; « pourquoi X pas Y » = grep, pas spéculation.
- `available` porté par le registre → casse le cycle framework→security/http.
- Provenance lookup insensible à la casse (`tokenStore`→`tokenstore`, bug tokens null).
- Norme cards Studio (DocHint/WarnHint typés, pas InfoHint brut ; valeurs en Text stylé) — oubliée au 1er jet, signalée par le user.

## Coûts évidents

- 3 restarts serveur (backend endpoint #3 + fix casse) — le reste en HMR (front). Bon ratio.
- 1 agent Explore (cartographie terrain runtime) — évité de coder sur des mémoires de 10 jours périmées (totp/user).

## ⚠️ Note maintenance

`RETEX.md` = 1738 lignes (>> ~1 écran cible). **CONSOLIDATE largement dû** (graduer les patterns ≥3×, archiver).

## Commits produits

| Commit     | Sujet                                                                              |
| ---------- | ---------------------------------------------------------------------------------- |
| `a6ec2dec` | feat(studio,config) écran Studio « Stores » — état runtime persistance (0.8 lot 6) |
