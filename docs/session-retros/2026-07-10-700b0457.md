---
date: 2026-07-10
session_id: 700b0457-2f13-43d7-a37c-798e5ac78956
focus: migration Rollup → rolldown EXÉCUTÉE (lots 0-5, Rollup supprimé) + resync migration 68 %
---

# Session retro — 2026-07-10 (4ᵉ) — 700b0457

## Fait

Migration rolldown **complète en 1 session** (plan prévoyait des lots séparés) :
lot 0 défauts (self-import http, zod frontend) → socle `rolldown.shared.ts` + gate
`compare-exports.mjs` → core 4 bundles → 18 packages + app racine → DevSupervisor →
**suppression totale** (20 configs, deps, scripts, skills, docs). Directives user
intégrées en cours de route : ES2024, TS 6 racine seule, suppression franche sans
fallback legacy, exigence bundler d'app publiable (`nodefony/bundler`, release Ph.2).
Puis **resync migration** (65 → 68 %, P6.18/P10.4/10.5/10.15 sous-vendus, P7 90→75 %
sur-vendu, cellule P7.10 dégraissée 5 099→1 110 car.).

## Frictions

- `build:force` raté par le sweep scripts (user l'a trouvé) → grep TOUS les blocs scripts.
- `npm pkg delete` inconsistant en workspaces → jq direct.
- 2× faux TIMEOUT `start.sh` (25 s) → 285 fails intégration faux (serveur pas prêt).
- TS5011 : retirer un include déplace la racine commune → rootDir explicite.
- oxc per-file : 2 défauts masqués par Rollup (self-import, `import type` manquant).

## Commits produits

| Commit                | Sujet                                                       |
| --------------------- | ----------------------------------------------------------- |
| `c600ae79`→`d295121b` | lots 0-5 rolldown (7 commits, gates complètes à chaque lot) |
| `42ca103c`            | MIGRATION_STATUS bandeau build rolldown                     |
| `766d4572`            | fix build:force + scripts analyze morts                     |
| dernier               | resync migration 2026-07-10 (68 %)                          |

## Recommandations

1. Follow-up `start.sh` : élargir la fenêtre boot (gravé au kit rolldown).
2. `nodefony/bundler` subpath = livrable release Ph.2 (gravé plan + kit).
3. CONSOLIDATE toujours dû (~60 retex).
