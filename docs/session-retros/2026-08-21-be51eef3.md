---
date: 2026-08-21
session_id: be51eef3-b80c-43fe-86f7-0e4b6625dbc8
focus: run large leviers actifs (21/29) · CI reverdie (NBSP + 8.3) · fix create module · nodefony menu (palette)
---

# Session retro — 2026-08-21 — be51eef3

## Fait (fil de la session)

1. **Run large `--repack`** : probe T9 d'abord (demande user) — PASS 12 tours, skills 5/5 +
   MCP `connected` constatés à l'init → run complet lancé. Coupé par le QUOTA à la T30 ;
   complément `--task 30,31,32` (3/3 PASS) + agrégat : **21/29 PASS réels**,
   FAILS {9,10,14,16,22,24,25,26}. MCP : 4 appels sur T9, 0 ailleurs.
2. **Axe A (transcripts)** : T10/T12/T24 = l'agent haiku ne lit pas AGENTS.md, ne typecheck
   jamais, écrit à la main. Prouvé : `claude -p` injecte le CLAUDE.md du cwd (sonde BANANE42)
   → le pointeur porte le réflexe `npm run verify`.
3. **T28 instruite** → 2 défauts : `create module` ne construisait que le module (fix : build
   RACINE, reproduit/fermé hors banc) ; le juge lisait `git status` (aveugle au commité).
4. **CI reverdie** : NBSP dans bearer.ts (témoin vu rouge) + `realpathSync.native` (forme 8.3
   Windows, prouvé par le job Windows) + 2 patchs deps. Analyse deps : 1 seul vrai retard
   (typescript 7 — bloqué P3 design:paramtypes), le reste = planchers de peerDeps sains.
5. **`nodefony start` → `nodefony menu`** (audit user) : composition PURE + palette `search`
   (filtre à la frappe, accents ignorés), groupes aérés, résumés commander tronqués à la
   largeur, conseils d'usage, scripts npm du projet, commandes de module du manifest (noms ET
   alias exclus), `start` = alias de `production` (npm/next), `nodefony` nu en TTY = menu,
   Ctrl+C sobre (quiet propagé jusqu'à Kernel.terminate, IKernel à jour), écran reset au
   lancement. Prouvé au pty (script(1)) à chaque étape. 10 descriptions harmonisées FR.

## Frictions (détail au RETEX.md)

- Quota : coupe aussi l'agent du banc ; l'agrégat retient un transcript tronqué (T30 FAIL non
  opposable) — défaut `--analyze-only` à corriger.
- Ctrl+C propre = 2 couches (throw post-terminate, quiet perdu) ; preuve pty obligatoire.
- perl en octets a cassé l'UTF-8 (NBSP coupé en deux) ; od -c pour vérifier.
- stream-json ne montre pas le contexte injecté — sonde discriminante avant de conclure.
- cwd drift → vitest « 0 test » (×2, déjà gradué).

## Commits produits

| Commit   | Sujet                                   |
| -------- | --------------------------------------- |
| 2ec0f698 | pointeur CLAUDE.md porte verify         |
| aa303106 | fix CI : NBSP + realpathSync.native     |
| 30cf8de7 | deps : busboy 3.2.2, svelte 5.56.10     |
| 3718590d | create module construit l'application   |
| 228af967 | menu : refonte + start→production       |
| b4f1d783 | menu : palette, filtre, sorties propres |
| 144e3dac | juge module-local lit le disque         |
