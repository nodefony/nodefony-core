---
date: 2026-09-11
session_id: 6d2322b1-fd3c-45ac-ae15-4806c42001c4
focus: lever les bloquants de l'alpha.5 — banc de publication, étiquettes OCI, fichiers d'agents, gabarits menteurs
---

# Session retro — 2026-09-11 — 6d2322b1

## Fait

- **#353** — le banc de publication était ROUGE et ne pouvait plus passer : (b2) effaçait
  `public/dist` dans un conteneur où le code appartient à root. `--tmpfs` masque le dossier ;
  l'image garde son ENTRYPOINT, donc on mesure celle qu'on publie.
- **#354** — l'image officielle partait avec `version="0.1.0"`, `revision=""`. La forge passe les
  `--build-arg`, et `image-labels-gate.mjs` refuse AVANT le push.
- **#350** — cibles `copilot` et `cursor` (canal `fichier-agent`), pointeur
  `.github/copilot-instructions.md`. Périmètre RÉÉCRIT avant d'être fait : 23 outils lisent
  `AGENTS.md` nativement, et Windsurf est déprécié au profit de `.devin/rules/`.
- **#356** / **#357** — trois commentaires faux du Dockerfile généré, et la clé JWT qui ne restait
  hors de l'image que par coïncidence de chemins.
- Deux gates réparés en passant : celui des skills (aveugle aux cibles-dossier) et celui de la
  langue, qui m'a repris sur 13 identifiants français.
- **#340 remonté en alpha** avec #343 et #346 : le parent était en beta pendant que ses enfants
  étaient déjà en alpha.

## Coûts évidents

- **Trois passes de CI rouges** avant d'avoir la bonne : gate de langue (héritée), gate de langue
  (la mienne), gate des skills. Chacune coûte un push et une attente.
- **Deux runs complets du banc de publication** (~8 min chacun) — le second uniquement pour voir la
  garde mordre. Incompressible : c'est ce que le ticket exigeait.
- Le renommage des 13 identifiants a été fait motif par motif, parce qu'un remplacement global
  aurait emporté la prose française. Lent, mais c'était le seul geste sûr.

## Recommandations

1. **Lancer `npm run check:lang` AVANT le commit, pas après le rouge de la forge** — trois commits
   d'affilée l'ont enfreint alors que la règle est au `CLAUDE.md`. Le pre-commit ne le porte pas.
2. **Thèmes MÛRS au sas** : 🤝 « le terrain qu'on donne à un délégué » et 📐 « composer une assertion
   de chemin » sont tous deux à 5. À graduer au prochain CONSOLIDATE.
3. Le banc de publication n'a pas été rejoué après #354 et #356, qui touchent pourtant le Dockerfile
   généré — à faire avant de publier.

## Patterns récurrents (déjà gérés)

- ✅ « suspecter son propre diff » — le test `cursor` et les deux défauts de `writeAgentPointers` /
  `targetsToDeclare` sont sortis de là.
- ✅ « prouver sur l'artefact reçu » — image réelle bâtie, app réellement générée.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `2aa37756` | fix(devkit): l'identifiant qui a mis la forge au rouge               |
| `21667665` | fix(release): masquer le front au lieu de l'effacer — **#353**       |
| `782442f1` | fix(release): estampiller l'image officielle — **#354**              |
| `608e8d8b` | feat(cli): les fichiers que VS Code, Copilot et Cursor lisent — #350 |
| `10244b65` | fix(skills): un runner de tests prend un DOSSIER                     |
| `b544c189` | fix(scaffold): trois commentaires du Dockerfile — **#356**           |
| `43d9ab41` | fix(security): avertir quand le trousseau JWT sortira — **#357**     |
| `6897aa1f` | fix(cli): écrire en anglais les identifiants ajoutés                 |
