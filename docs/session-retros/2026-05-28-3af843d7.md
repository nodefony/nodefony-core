---
date: 2026-05-28
session_id: 3af843d7-6f61-4a71-ab2f-8caaf084e52b
focus: Vitrine doc Socket complète (5 graphes live) + merge POC → portail /nodefony/documentation
---

# Session retro — 2026-05-28 — 3af843d7

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    48 |
| Edit            |    31 |
| Read            |    25 |
| TaskUpdate      |    16 |
| TaskCreate      |     8 |
| Write           |     6 |
| Skill           |     4 |
| ToolSearch      |     1 |
| AskUserQuestion |     1 |

## Top fichiers Read/Edit

| Fichier                                     |               × |
| ------------------------------------------- | --------------: |
| `pages.ts` (registry LIVE_GRAPHS)           |              14 |
| `useSocketLiveData.ts` (5 mappings purs)    |               5 |
| `DocumentationController.ts` (cleanup back) |               4 |
| `Documentation.tsx` (refonte 842L → 352L)   | 3+6 (Read+Edit) |
| `BackplaneLiveGraph.tsx`                    |               3 |
| `ProtocoleLiveGraph.tsx`                    |               2 |
| `App.tsx`                                   |             2+3 |

## Coûts évidents

- **Build depuis cwd repo root vs cwd module Studio** (vécu 1× cette session)
  → 1 restart serveur INUTILE + 5 commandes diag (`grep mtime`, `grep dist`, etc.) avant de
  comprendre que `npm run build` depuis la racine ne touchait pas Studio (turbo cache).
  Pattern déjà documenté dans le retex `nodefony-studio-dev` (« Trappe dist » + « build:front
  ≠ build »). **À renforcer en réflexe** : tout edit dans `src/packages/@nodefony/<mod>` →
  `cd src/packages/@nodefony/<mod> && npm run build` AVANT le restart.
- `pages.ts` édité 14× — un edit par enregistrement de graphe dans `LIVE_GRAPHS`. Normal (chaque
  ajout est une feature isolée testée immédiatement). Pas un coût évitable, plutôt itératif.

## 💶 Coût (€)

- Total **~€47.55** (≈ $51.68 USD) — **cache 1h + read = 98 %** du coût (cache write 1h 388k tok +
  cache read 21.4M tok). Output 106k tokens = **14 %**. Input « fresh » 149 tok = négligeable.
- **Lecture** : session **cache-dominée** (contexte porté = CLAUDE.md racine 1500L + studio/CLAUDE.md
  - skills `nodefony-studio-dev` 1100L + `nodefony-session` 250L + `nodefony-start-server` 80L +
    les 7 MD `docs/realtime/socket/*` lus 1×). Le levier d'économie #1 reste un contexte plus mince
    (skills load-on-demand fonctionne déjà bien — sans eux le cache serait pire).
- **Coût/livrable** : 5 composants + refonte 800→350L + helper + cleanup back = 47€ / ~5 livrables
  ≈ **€9 par livrable**. Acceptable pour une session productive sans re-lecture massive.

## Recommandations

1. **Renforcer le réflexe « build Studio = cd module »** dans la mémoire `feedback_turbo_cache_stale_logs`
   ou créer une mémoire dédiée. Vu 2× (cette session + 2026-05-25). Symptôme caractéristique : le
   warning `HelmetOptions` (root) apparaît dans le tail du build — c'est le ROOT qui build, PAS Studio.
2. **Pattern « registry isomorphe + helper de mapping »** à capter dans le retex de `nodefony-studio-dev` :
   un registry front court (`vue-ensemble`) consommable depuis 2 mondes (POC HMR + portail backend
   long slug) via UNE fonction `findXxxLiveGraph(slug)` qui accepte les 2 formats. Pattern réutilisable
   pour toute future migration POC → prod (ne pas dupliquer le registry).
3. **`MarkdownDoc.onInternalLink` doit être systématique** dans toute page qui rend du MD multi-fichier.
   Quand on rewrite des liens `.md` vers des slugs portail, le préfixe « dossier » = `slug.replace(/[^~]+$/, "")`
   est l'idiome propre. À noter dans `MarkdownDoc.tsx` docstring (TODO si pas déjà fait).

## Patterns récurrents (déjà gérés)

- ✅ Suppression branche `if (isSocket)` géante → cohérent avec le \_state précédent qui notait
  « refactor Documentation.tsx (798L, supprimable une fois la vitrine MD complète) ».
- ✅ Pattern « 0 ticker quand OFF » respecté sur les 5 nouveaux graphes (LiveBranch séparé qui
  appelle `useSocketLiveData()`, statique n'a pas d'abonnement).
- ✅ Type miroir local pour `RealtimeHealth` (frontière isomorphe — pas d'import serveur).
- ✅ AskUserQuestion utilisé 1× et à bon escient (choix produit garder/virer `/socket-poc`, pas un
  arbitrage technique). Cohérent avec hygiène §5.

## Commits produits

**Aucun commit pendant cette session** — toutes les modifs sont staged/unstaged. Récap des fichiers :

| Fichier                                               | Statut |
| ----------------------------------------------------- | ------ |
| `frontend/src/App.tsx`                                | M      |
| `frontend/src/layouts/navConfig.ts`                   | M      |
| `frontend/src/realtime/socket/pages.ts`               | M      |
| `frontend/src/realtime/socket/useSocketLiveData.ts`   | M      |
| `frontend/src/routes/Documentation.tsx`               | M      |
| `frontend/src/routes/SocketPocPage.tsx`               | D      |
| `nodefony/controller/DocumentationController.ts`      | M      |
| `frontend/src/realtime/socket/ActionsLiveGraph.tsx`   | ??     |
| `frontend/src/realtime/socket/BackplaneLiveGraph.tsx` | ??     |
| `frontend/src/realtime/socket/FanOutLiveGraph.tsx`    | ??     |
| `frontend/src/realtime/socket/ProtocoleLiveGraph.tsx` | ??     |
| `frontend/src/realtime/socket/SondesLiveGraph.tsx`    | ??     |

Commit recommandé (à faire à l'ouverture de la prochaine session ou maintenant si le user souhaite).
