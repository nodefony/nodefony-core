---
date: 2026-07-26
session_id: 1ef17fee-7df5-4c28-b015-523da671c2b4
focus: La CI cessait de dire la vérité — lint 263→0 verrouillé, build débloqué après 2 mois, sécurité fermée, Windows révélé
---

# Session retro — 2026-07-26 — 1ef17fee (6ᵉ du jour)

## Focus

Finir le compteur de lint, puis — sur signalement du user — réparer une CI qui
n'exécutait pas ce qu'on croyait qu'elle exécutait.

## Fait

- **Lint 263 → 0**, et surtout **verrouillé** (`--deny-warnings` en CI, gate vu ROUGE
  avant d'être posé). 41 masquages éteints par ZONE, 15 renommés en production.
- **Build CI débloqué après deux mois** : `src/modules/test/index.ts` importait en dur une
  fixture que le même commit avait mise au `.gitignore`.
- **Sécurité** : 5 alertes HIGH → 0 (`overrides` sur `brace-expansion`) ; CodeQL déclenché
  pour la première fois sur la branche de travail, verdict `success`.
- **Audit du workflow** validé contre la documentation GitHub : `permissions: contents:
read`, jobs découplés, Bun retiré (plus aucun `bun test`), `npm audit` et `skills:check`
  posés en gates, job `test-cli` créé (le binaire n'était éprouvé sur aucune plateforme).
- **Windows** : un test corrigé, et l'ampleur réelle mesurée — ~27 tests rouges, 4 familles.

## Frictions

- **QUESTION ZÉRO enfreinte** : agent lancé pour inventorier 56 `no-shadow` **déjà triés la
  veille**, dont la répartition tenait dans un `grep`. Le user l'a vu ; agent arrêté.
- **Six suites rejouées en local** avant un push, que la CI relançait de toute façon.
  Reproche explicite du user → gradué en `feedback_ci_is_free_dont_double_it`.
- **Un agent a surveillé un run annulé** pendant 18 minutes, faute d'avoir été redirigé
  quand un nouveau push a supplanté sa cible.
- **Rapports d'agents à qualifier, jamais à croire** : « le serveur s'arrête sans raison »
  alors que la cause était deux lignes plus haut dans le log qu'il avait lu.

## Recommandations

1. **Chantier Windows en session dédiée** — kit écrit (`project_windows_support_kit`).
2. **Ne plus déléguer un tri déjà fait** — relire le `_state` de la veille avant de lancer
   un agent d'inventaire.
3. **Rediriger ou tuer un agent dès que sa cible devient obsolète** (push supplantant).

## Patterns récurrents (déjà gérés)

- ✅ Preuve NÉGATIVE avant de poser un gate (sonde lint, filet Windows, helper neutralisé).
- ✅ Annoncer ce qui n'a PAS été lancé (drizzle 517 skippés, `LogSink` non reproductible).
- ✅ Interdiction de l'index git dans chaque prompt de délégation.

## Commits produits

| Commit     | Sujet                                                    |
| ---------- | -------------------------------------------------------- |
| `d469091f` | lint `no-map-spread` — 15 sites, 0 vrai défaut           |
| `5ca2d3de` | build CI débloqué — fixture absente de tout clone        |
| `fafb2354` | option CLI disparue + chemins Windows du générateur Vite |
| `a29f26aa` | les 33 avertissements non qualifiés                      |
| `d22909fe` | lint → 0                                                 |
| `f7e353c4` | 5 alertes HIGH → 0 + CodeQL déclenché                    |
| `492c2c81` | audit du workflow                                        |
| `4c47f06e` | test Windows + job `test-cli`                            |
| `26b8ae76` | clés de production générées en CI                        |
| `b29924a5` | `LogSink` (fichier partagé) + job renommé                |
