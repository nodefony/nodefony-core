---
date: 2026-09-06
session_id: 3d4a6dc1-5887-472f-97ee-3208bd252ee8
focus: dépendances hissées avant l'alpha, et le résidu de base qui faisait rougir un test depuis des jours
---

# Session retro — 2026-09-06 — 3d4a6dc1

## Fait

- **`npm run deps:check`** : 19 écarts sur 147 pins, **6 actionnables**, 5 pris.
  `jose` 6.2.12 et `@simplewebauthn/server` 14.0.1 (security), `mongoose` 9.9.5,
  `playwright` 1.63.0 et `lint-staged` 17.5.0 (outillage).
- **`typescript` 7.0.2 écarté avec preuve** : bloqué en amont (`@angular/build` et
  `@angular/compiler-cli` exigent `>=6.0 <6.1`, `typedoc` plafonne à `6.0.x`) — `npm ci`
  échouerait avant tout test.
- **#253** — le test drizzle `migrator-postgres.e2e` mesure un DELTA de schémas au lieu d'un absolu.
- **#254** — `db-outage-pod.mjs` se donne sa propre base (`nf_bench_outage`), au lieu de migrer
  dans la base de travail partagée.

## Coûts évidents

- **Deux passes `npm run test:all` complètes** (~6 min chacune) : la première pour le lot de deps,
  la seconde pour armer le piège DDL. La seconde était nécessaire — c'est elle qui a innocenté la
  suite entière.
- **Quatre lancements de la suite drizzle** (1 518 tests) pour éprouver le correctif dans les deux
  sens. Justifié : chaque run répondait à une question distincte.
- **Un rejeu perdu sur un mauvais mot de passe** (`nodefony` au lieu de `nodefony-dev`) : les cinq
  tests sont tombés sur la connexion, le run ne prouvait rien. Le `gateReporter` affichait pourtant
  l'URL exacte, en fin de sortie.

## Recommandations

1. **Armer un déclencheur d'événement DDL est l'instrument de choix pour trouver un producteur
   inconnu** — 140 créations capturées en un run, verdict immédiat. À préférer à l'essai des
   suites une par une dès qu'un état partagé est sali par un inconnu.
2. **Lire l'`include`/`exclude` du tsconfig qu'on invoque** avant de croire un typecheck vert.
3. **Défaire un débranchement par `git diff`**, jamais par le remplacement inverse.

## Patterns récurrents (déjà gérés)

- ✅ Le code de sortie masqué par un wrapper (`feedback_shell_false_diagnostics`) — revu ici sous
  une forme neuve : la notification de tâche de fond annonçait « exit code 0 » alors que la passe
  était rouge, le vrai code étant en dernière ligne du journal capturé.
- ✅ Troncature muette d'un client en ligne de commande — `gh project item-list --limit 120` a
  omis #220 et #215 sans un mot ; le skill `nodefony-ticket` porte déjà le piège.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `f8817b19` | chore(deps): hisser les 5 dépendances actionnables avant l'alpha     |
| `b60719c2` | test(drizzle): borner au schéma le contrôle de la table d'historique |
| `5b4e6f35` | test(bench): donner au banc de coupure sa propre base                |
