---
date: 2026-09-11
session_id: 2fde98f2-3c46-4b9e-a278-a6d18f561817
focus: Autopsie d'un essai réel de découverte (Copilot CLI sur une app générée) — 11 tickets, frise redatée
---

# Session retro — 2026-09-11 — 2fde98f2

## Focus

Le user a fait générer une application par `nodefony create app`, puis a demandé à GitHub
Copilot CLI d'y créer un module de chat. 89 minutes, résultat jugé « catastrophique ».
Session d'AUTOPSIE : aucun code produit, du diagnostic et du pilotage.

## Fait

- Analyse de l'application de test `/Users/cci/repository/chat-app` (preset `minimal` confirmé
  par les dépendances du commit de bootstrap : 5 paquets, ni ORM ni sécurité ni Studio).
- **Découverte du transcript Copilot** (2 112 événements, 5,1 Mo) — sauvegardé hors de
  `~/.copilot` (volatile) vers `memory/core-dev/transcripts/`.
- 11 tickets ouverts (#340 parent + 8 enfants, #349, #350, #351), 2 recadrés après le transcript.
- 57 tickets redatés (Début/Cible), alpha + beta, relus un par un sur le tableau : 57/57.

## Ce que le transcript a démenti — trois fois

| Diagnostic rendu                    | Réalité                                                             |
| ----------------------------------- | ------------------------------------------------------------------- |
| « le compagnon a improvisé »        | 4 skills chargés spontanément, `--dry-run` avant d'agir             |
| « les générateurs lui ont résisté » | 3 refus, chacun nommant cause + geste, corrigés du 1ᵉʳ coup en 25 s |
| « AGENTS.md est trop long »         | il l'a lu ; aucun échec ne s'y rattache                             |

Les trois ont été énoncés au user AVANT d'ouvrir la source. C'est le user qui a demandé
« tu as le transcript ? ».

## Les murs réels, datés

| Coût                                                                                             | Durée           |
| ------------------------------------------------------------------------------------------------ | --------------- |
| `403` puis `realtime:denied` — aucune identité pour appeler ce qui vient d'être protégé          | **33 min / 89** |
| Worktree sans `node_modules` : toutes les commandes `npx nodefony` échouaient sans dire pourquoi | 6 min           |
| 20 `rg`/`find` dans les sources du framework pour trouver comment lire des données               | 3 min           |
| Socket isomorphe et client Svelte non trouvés — soufflés par le user                             | 2 messages      |

## Coûts évidents

- Trois analyses partielles successives, chacune suivie d'une correction publique : le coût n'est
  pas l'analyse, c'est d'avoir conclu avant d'avoir la source.
- Le format de sortie `gh api graphql --slurp` incompatible avec `--jq` (2 essais perdus).

## Recommandations

1. **Analyser le reste du transcript dans une session dédiée** — 119 appels `view` (la carte de ce
   qu'un agent ouvre pour comprendre le framework), 174 messages assistant, les 40 dernières
   minutes, 2 échecs de notre propre MCP `nodefony_docs`. Gisement plus riche que l'exploité.
2. **Graduer les deux thèmes mûrs** du sas (🪟 à 17 frictions, 🧾 à 5) — travail de CONSOLIDATE.
3. **Un contrôle qui refuse une frise dont le départ est dans le passé** — aucun des neuf
   contrôles de `board-lint` ne regarde les dates.

## Patterns récurrents (déjà gérés)

- ✅ `ticket:lint` a mordu 3 fois (PARENT-SOMME ×2, PRIORITE-ORDRE ×2) — le pilotage tient parce
  qu'un automate le relit.
- ✅ `ticket-effort.mjs` a confirmé la convention de datation (1 séance médiane / ticket, n=93).

## Commits produits

| Commit       | Sujet                                |
| ------------ | ------------------------------------ |
| (aucun code) | session de diagnostic et de pilotage |
