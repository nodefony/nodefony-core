---
date: 2026-05-23
session_id: 5f4095ce-d755-4b1a-b05b-d6c375461575
focus: Schéma Dolibarr (410 tables) + DataGrid réutilisable + Dashboard ORM (sondes + hub realtime)
---

# Session retro — 2026-05-23 — 5f4095ce

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Edit            |   279 |
| Bash            |   185 |
| Read            |    79 |
| Write           |    18 |
| AskUserQuestion |     4 |
| Skill           |     3 |

## Top fichiers Write/Edit

| Fichier                                                       |   × |
| ------------------------------------------------------------- | --: |
| studio/frontend routes/OrmOverview.tsx                        |  71 |
| studio/frontend components/ui/DataGrid.tsx                    |  65 |
| studio/frontend routes/Database.tsx                           |  50 |
| /tmp/gen-dolibarr.mjs                                         |  15 |
| orm-core OrmAdminApi.ts / ConnectionMonitor.ts / IOrmGraph.ts | ~18 |
| studio realtime providers.ts + StudioRealtimeController.ts    |   8 |

## Coûts évidents

- **Itération UI visuelle longue** : OrmOverview édité 71×, DataGrid 65× — beaucoup de micro-allers-retours sur le rendu (resize handle, filtres, flash CSS, KPIs) faute de feedback visuel direct (pas de browser). Normal pour du front piloté par retours user, mais coûteux.
- **Régression dist périmé** : `routes/page` du framework absent du dist (turbo a rejoué un cache d'avant l'ajout pendant les builds ORM) → page Routes 404. Rebuild framework direct = fix. (Piège connu `feedback_turbo_cache_stale_logs` / `feedback_session_pitfalls`.)
- **Plusieurs rebuild+restart** (orm-core/drizzle/mongoose/studio/framework) pour le backend sondes/hub — inhérent (backend ≠ HMR), mais batchés correctement en fin.
- **Bug unicode** : `С` cyrillique dans un nom d'interface (ConnectionMonitor) — réécrit. Vigilance copier-coller.

## Recommandations

1. **Skill `nodefony-studio-probe-panel`** (ou recette dans `nodefony-studio-dev`) — le patron sondes+hub est maintenant figé (cf §Patterns) ; la prochaine application (supervision dashboard) doit suivre la même recette déterministe. Déjà inscrit dans le skill `nodefony-studio-dev` (section PATRON) + mémoire `project_studio_probes_hub_vision`.
2. **Réflexe « build framework si routes admin 404 »** — quand un endpoint data plane (`/nodefony/<m>/api/...`) renvoie 404 alors que la source l'a → dist périmé, rebuild le module propriétaire DIRECT (pas turbo). Ajouté implicitement au pattern.
3. **Vérifier l'existant AVANT de coder** (règle #1 skill) — appliquée cette session (TanStack pour DataGrid, IconJson vérifié installé). À garder systématique.

## Patterns récurrents (déjà gérés)

- ✅ Patron **sondes back + abonnement hub** → mémoire `project_studio_probes_hub_vision` + skill `nodefony-studio-dev` (section PATRON).
- ✅ Studio reste générique : provider realtime invoque l'endpoint admin via le **broker** (pas de dép module).
- ✅ Per-instance cloud-native (`instanceId`=pid), pas d'agrégation in-process.
- ✅ Vérif sans browser : `typecheck` + transform Vite `/@fs/...` + curl data-plane + client `ws` de test.

## Commits produits

| Commit                           | Sujet                                                       |
| -------------------------------- | ----------------------------------------------------------- |
| (aucun — à committer en clôture) | dashboard ORM sondes/hub + DataGrid + Dolibarr (gitignored) |
