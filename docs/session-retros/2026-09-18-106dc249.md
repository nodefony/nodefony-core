---
date: 2026-09-18
session_id: 106dc249-a004-48f7-abad-ac30574177ca
focus: "#420 — l'image de l'application passe sur node:24-alpine, et le choix est ÉCRIT là où on le subit ; #421 ouvert et fermé (le banc ne rangeait son décor que lorsqu'il réussissait)"
---

# Session retro — 2026-09-18 — 106dc249

## Focus

Bascule de la base de l'image générée (`node:24-slim` → `node:24-alpine`), demandée par
mesures dans #420, et surtout : **écrire le choix là où on le subit** — le user a demandé
que les candidates, leurs chiffres et la ligne à remplacer soient dans le `Dockerfile`, le
README de l'application et la page Docker Hub, « un peu au-dessus du help de Docker Hub ».

## Fait

- **#420 avancé, pas fermé** (`141201ca`) — les deux `FROM` sur Alpine ; bloc de décision en
  tête du gabarit (3 candidates, poids, CVE, libc, shell, Node embarqué, et les lignes de
  remplacement) ; section « Sur quelle base elle est bâtie » sur `docker/hub-overview.md` ;
  section courte dans le README de l'application ; test qui fige la base, **vu rouge**.
- **#421 ouvert, corrigé, fermé** (`582e5aff`) — le scénario `edge` du banc désarmait le
  nettoyage final : les volumes ne partaient que sur le chemin de l'ÉCHEC.
- **Le guide cloud-native portait une COPIE du Dockerfile**, dérivée en silence (plus
  d'utilisateur non-root, plus de sonde, `CMD` en forme shell). Remplacée par un renvoi.
- **Le README de l'application annonce l'identité admin**, qu'il était seul à taire (le log
  du premier boot, le `.env.local` et le TSDoc le disaient, pas lui).
- **#160 documenté** : aucun gabarit d'issue GitHub n'existe, et en poser un créerait une
  seconde copie de la règle du skill.

## Frictions

- 🔴 **J'ai annoncé un mécanisme de cause comme établi sans l'avoir reproduit.** Le run
  suivant, parti AVEC le volume résiduel, est passé vert : le mécanisme était plausible et
  ne mordait pas sur CE rouge. Corrigé auprès du user et dans le corps du ticket.
- **La commande de preuve de #420 ne jouait pas le geste du produit** : elle installait sans
  `--ignore-scripts`, là où l'image installe avec — et c'est ce drapeau qui décide du
  binaire prébâti. Rejoué dans les conditions réelles : vert, mais par chance.
- **Un assert que rien ne pouvait faire tomber** : j'avais ajouté un contrôle de parité des
  deux étages, impliqué par les deux égalités qui le précèdent. Retiré plutôt que gardé.
- **Trois quarts d'heure sur un rouge de décor** faussement imputable à la bascule.

## Gardes du dépôt qui ont mordu sur mon travail

- `format:scaffold` — une ligne de citation coupée dans le README rendu.
- `commitlint`, `check:filenames`, `check:nul`, `oxlint`, `prettier` au pre-commit.
- Le banc lui-même : c'est lui qui a révélé le défaut de nettoyage.

## Recommandations

1. Rejouer #420 sur les 3 moteurs SQL serveurs et la suite e2e DANS Alpine — scripts prêts
   dans le scratchpad de session, à reprendre (`banc-dialectes.sh`, `e2e-alpine.sh`).
2. Ne pas fermer #420 avant ces deux points : son « Fini quand » les nomme.
3. Corps de #420 périmé sur ses chiffres — les mesures qui font foi sont dans son commentaire
   du 18/09 et désormais dans la documentation.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `582e5aff` | fix(release): ranger le décor du banc docker même quand il réussit |
| `141201ca` | build(docker): bâtir l'image de l'application sur node:24-alpine   |
