---
date: 2026-06-01
session_id: def24b9f-a081-4bd0-af51-4a8411099816
focus: Test ultime Docker/charge Log Backplane → bug doublon JSONL cluster-file fixé + dette double-boot CLI identifiée
---

# Session retro — 2026-06-01 — def24b9f

## Focus

« Test ultime » du Log Backplane sous charge (Docker Loki + cluster), puis correction d'un bug
réel découvert pendant le test (doublon JSONL en cluster-file), puis diagnostic d'une dette CLI
sous-jacente (double boot complet dev+prod par worker).

## Fait

- **Test ultime mono dev** : 5 axes coupables à chaud sous charge (200/`changed:true`, 0 crash),
  fan-out **Loki réel** prouvé, perf stable (1249 RPS p99 62ms sur 1,55 M req ; 1796 tout coupé),
  memory 9/9 serveur frais, `logDir` relatif maintenu.
- **Test ultime cluster prod -w4** : `isCluster` + round-robin 4 pids, toggles **403** (DEV-only),
  per-worker (memory) vs **`cluster-file`** (agrège les 4 workers).
- **BUG trouvé + fixé** (`0fb1046`) : doublon JSONL en cluster-file (ratio ~2.0). Cause prouvée
  par instrumentation runtime : worker = **2 boots complets dev→prod** (4 `initializeLog`),
  syslog partagé entre les 2 Kernels, `_mountedLogTransports` (état Kernel) ne traque pas le
  transport du cycle précédent. Fix = `Syslog.addTransport` dédup par **`name`** (remplace),
  invariante portée par le syslog. Test anti-régression + memory 9/9 + ratio runtime 1.00.
- **`./tmp` nettoyé** : 1200 `<uuid>.txt` (résidus `memory.test` 200 uploads × runs).

## Frictions (→ RETEX.md)

- Mon « test ultime vert » a **raté le doublon** : je validais l'AGRÉGATION (pids vus) sans
  l'INTÉGRITÉ (`total == unique`). Un test d'agrégateur cluster DOIT asserter l'unicité.
- `memory.test` (« 200 multipart uploads ») pollue `./tmp` comme `upload.test.ts` (déjà fixé là)
  → régression du même pattern, à corriger (snapshot + cleanup du diff).
- L'instrumentation runtime (stderr + rebuild) a tranché là où la lecture de code spéculait
  (la mémoire disait kernel #1 « orphelin jamais booté » = FAUX, il boote complètement en dev).
- `bc` absent sur la machine → `awk` pour les ratios.

## 💶 Coût

- Non mesuré (END léger). Session longue : test ultime + fix core + gros diagnostic CLI.

## Commits produits

| Commit    | Sujet                                                                  |
| --------- | ---------------------------------------------------------------------- |
| `0fb1046` | fix(core): doublon JSONL en cluster — addTransport déduplique par name |

## Reste — prochaine session

1. **Chantier CLI double-boot** (tests intégration d'abord → bump commander 15 → refacto registry).
2. Corriger `memory.test` pour ne plus polluer `./tmp`.
3. Option B : broadcast realtime de l'état backplane (feature).
