---
date: 2026-07-24
session_id: 6ce9130e-1e6b-4245-82a2-ef21bf2ab228
focus: devkit S1 — scaffold transactionnel, dry-run, mode machine, mutualisations, socle Studio
---

# Session retro — 2026-07-24 — 6ce9130e

7ᵉ session du jour. Vague `devkit S1` exécutée en entier (Opus 4.8 ; le kit
prévoyait Sonnet — le user a tranché en ouverture).

## Fait

- **Le trou dangereux du kit est fermé** : `create controller` / `create entity`
  rendaient leurs fichiers AVANT la garde anti-doublon. Test d'intégrité écrit
  d'abord (il montrait `tests/post.test.ts` réécrit avec les champs du second
  appel), puis **transaction** `ScaffoldWriter` — écritures ET lectures en
  mémoire, versement par le scaffold racine seul. Ferme tous les refus tardifs,
  pas seulement les deux repérés.
- **`--dry-run`** : dérivé gratuitement de la transaction (`diffLines` LCS au
  moteur, pour que CLI et Studio décrivent le même changement).
- **Mode machine** : `--describe-json` (catalogue, cibles, capacités) +
  `--answers-json <fichier|->`. Clé hors spec = refus 64, pas un silence.
- **Une source par artefact** : 6 `main.*` → `shared/front-entry`, registrar
  d'entry en fichier dédié partout, contrôleur d'accueil rendu par le gabarit
  `controller/hello`, feuille de style des 3 vitrines partagée (accent en
  variables CSS).
- **Socle Studio** : `preview()` + RPC `nodefony:scaffold:preview`, étapes npm
  remontées au core, 6 premiers tests sur un chemin qui n'en avait aucun.
- **Doc** : page publique `docs/guides/generer-du-code.md` (4 gates verts), MEMORY
  du cli, CLAUDE+MEMORY de Studio (le générateur n'y était documenté nulle part),
  gouvernance d'Agent Skills corrigée (Anthropic, pas l'AAIF — source primaire).

## Frictions

- **Un grep vide n'est pas une absence** : `grep --include` (ugrep) a rendu 0
  résultat là où `rg` en trouvait 10 ; j'allais conclure « tout est corrigé ».
- **Ce qui sort du dépôt n'est pas testé par le dépôt** : l'import CSS partagé
  cassait le `typecheck` de l'app GÉNÉRÉE — 2145 tests verts, typecheck racine
  vert. Trouvé en générant une vraie app témoin.
- **Angular renomme les `@keyframes` de `styles: [...]`** — l'animation nommée
  par une variable CSS serait devenue introuvable, sans erreur.
- `code-check` valide la compilation d'un bloc de doc, pas sa correspondance au
  rendu réel : `vue-ensemble.md` montrait des noms de route périmés, gate vert.
- Deux `index.lock` résiduels après un `commit` refusé par commitlint
  (sujet en Start-Case).

## Preuves terrain

App témoin `--link` (React + preset complete) : `npm install` → build backend →
`typecheck` → `nodefony frontend:build`. CSS bundlé en un fichier de 2,82 kB,
variables d'accent présentes. Mode machine éprouvé au binaire réel (stdin,
`--describe-json`, clé inconnue → 64). Chaque nouveau gate cassé exprès une fois.

## Commits produits

| Commit     | Sujet                                             |
| ---------- | ------------------------------------------------- |
| `ab117cd8` | un refus n'écrit plus rien (transaction)          |
| `03076434` | `--dry-run`                                       |
| `bd4c4e80` | mode machine `--describe-json` / `--answers-json` |
| `04c95cc7` | une seule source par artefact généré              |
| `8109ff76` | Studio : préview avant génération                 |
| `51396e5b` | gouvernance Agent Skills + doc de la vague        |
| `a7327748` | `MIGRATION_STATUS` — devkit S1 fait               |
| `e85904a0` | page publique `docs/guides/generer-du-code.md`    |
