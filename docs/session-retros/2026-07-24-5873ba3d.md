---
date: 2026-07-24
session_id: 5873ba3d-355b-4f8d-b7c0-f1bb5b605e39
focus: Registre doc↔code clos (F93/F177/F4/F58/F47/F87) puis unification de la contre-pression WS
---

# Session retro — 2026-07-24 — 5873ba3d

## Fait

Le registre des écarts doc↔code passe de **113/120 avec 3 ouverts** à **119/120, 0 ouvert,
1 partiel**. Les trois derniers items étaient des décisions d'architecture, tranchées une
par une avec le user :

- **F93** — un canal appartient à qui fournit son provider. `hub.listen()` = écoute PASSIVE ;
  la fabrique du controller est rejouée à l'arrivée d'une connexion cliente. Deux dégradations
  silencieuses fermées (un service inventait un canal pour tout le monde ; un client était
  branché sur un flux que rien ne produisait).
- **F177** — streaming RPC **retiré** plutôt que publié à moitié : `requestStream` vivait dans
  un contrat public alors qu'aucun serveur n'émettait de frame `stream`. Inscrit en P12 avec
  son cahier des charges (annulation, erreur en cours de flux, débit régulé).
- **F4** — déjà résolu au terrain ; le kit était périmé.
- **F58**, **F47** (le trou réel était le champ `types` RACINE, absent du tarball — et le
  packager ne tournait plus depuis son déplacement dans le skill), **F87**.

Puis, sur relance du user, la contre-pression WebSocket a été reprise en profondeur :
**deux implémentations** de la même règle cohabitaient et divergeaient déjà (4 Mio côté
`http`, 1 Mio dans une copie chez `realtime`). Unifiées dans `@nodefony/http`, avec un
défaut réel corrigé : la fermeture reposait sur un second seuil d'octets que le drop rend
inatteignable.

## Frictions

- **Un test qui ne quitte pas sa brique ne prouve pas le câblage** — vécu 3 fois. Débrancher
  le point de câblage et vérifier que quelque chose tombe est devenu le réflexe.
- **Le banc réel a échoué 3 fois pour 3 raisons de décor** (canal homonyme d'une action, dist
  non rebuildé, mauvais serveur WS configuré), jamais pour le mécanisme testé.
- **Compter côté client ce que le serveur décide ne prouve rien** — il a fallu une sonde
  serveur pour distinguer « frame jetée » de « frame pas encore lue ».
- **Ma première correction du défaut était fausse aussi** ; seule la mesure l'a montré.
- `anchor-check` constatait sans réparer → chaque session refaisait le recalage à la main.

## Recommandations

1. `anchor-fix.mjs` versé dans `nodefony-documentation` — 123 ancres périmées recalées d'un
   coup sur le module realtime, avec les deux pièges du recalage automatique documentés.
2. Le banc `ws-backpressure-e2e.mjs` et son décor (endpoint + sonde serveur) sont versionnés :
   la contre-pression a désormais une preuve rejouable en conditions réelles.
3. `test:all` ne lance **aucun** des ~30 bancs `.mjs` du skill load-test, et son rapport ne le
   dit pas — alors que tout son principe est de nommer ce qu'il n'a pas testé. À traiter.

## Patterns récurrents (déjà gérés)

- ✅ dist périmé après modification d'un module (revécu, diagnostiqué en une minute)
- ✅ `cd` relatif qui fait dériver le cwd (2 commandes perdues, hook a mordu)
- ✅ un vert ne couvre que le diff qui l'a produit

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `ce7eb849` | un canal appartient à qui fournit son provider (F93)                    |
| `796ba7f9` | `anchor-fix` — réparer les ancres de doc au lieu de les constater       |
| `acea1b32` | tracer la dette « politique inopérante sur canal dynamique »            |
| `0cac14c1` | retirer le streaming RPC, publié à moitié (F177)                        |
| `bdf42744` | recaler l'ancre décalée par le fix F93                                  |
| `b6117eed` | le court-circuit Zero Trust vaut pour HTTP, pas le handshake WS (F58)   |
| `01eacd77` | dire qu'un canal dynamique n'est couvert par aucune politique           |
| `78b145e6` | publier les types aux DEUX champs + réparer la racine du packager (F47) |
| `7618cf96` | rendre la back-pressure WS réglable (F87)                               |
| `e7f10722` | décor + banc de contre-pression sur socket réelle                       |
| `0d295d42` | une seule contre-pression WS, et une fermeture qui se déclenche         |
