---
date: 2026-09-01
session_id: f3ab3911-657c-4a12-af49-4b88894bb2be
focus: réparer les bancs cassés par le déménagement de l'entité User (#152, #151), et faire naître une application avec sa migration (#146)
---

# Session retro — 2026-09-01 — f3ab3911

## Focus

Session de RÉPARATION, demandée explicitement en fin de session précédente : « corriger tout ça,
les bancs, tout ». Trois tickets visés — #152 (banc d'adoption), #146 (banc de publication),
#151 (flake macOS).

## Fait

- **#152 fermé** — le banc d'adoption écrivait dans `migrations/` du dépôt, devenu un artefact
  COMMITÉ. Détourné vers un abri temporaire par l'override générique `NF__DRIZZLE__MIGRATIONS__DIR`
  (le mécanisme du produit, ADR-0006 D3). Deux décors voisins réparés dans la foulée.
  **CI verte** : `Bancs sur infrastructure réelle` sur `9028e49b`.
- **#151 fermé** — semis du banc d'échelle par lots de 500 : 1 221 → 311 ms (×3,9), comptes
  rigoureusement identiques. Délai du hook déclaré (120 s) et VU mordre à 1 ms.
- **Défaut produit trouvé en chemin** — sur MySQL, l'adoption écrivait `tinyint(1)` là où le code
  déclare `boolean` : toute application MySQL restait bloquée au premier champ ajouté après
  adoption. Corrigé dans `normalizeIntrospection` ; banc dédié écrit, vu rouge puis vert.
- **#146 à moitié** — `create app` écrit désormais sa migration initiale (après le build, avant le
  premier commit). Prouvé sur une application RÉELLE : fichier + journal dans le commit initial,
  puis `up-to-date · sources: [framework 1, app 1]` sur base vierge en production.

## Coûts évidents

- **Trois passes du banc d'adoption** avant le vert : chaque correction révélait la suivante
  (assertions comptant un dossier vierge, puis `--up-to` qui inscrit TOUT jusqu'au tag).
- **Un push refusé** par la barrière du dépôt : mon `tsc -p tsconfig.json` ne couvrait pas
  `tsconfig.tests.json`, le second projet — le seul qui lit les bancs.
- **Une variable d'environnement créée puis annulée** : le produit portait déjà l'override
  générique. Découvert en lisant un banc voisin, pas en cherchant.
- **Une voie entière annulée sur #146** : générer la migration dans le conteneur est impossible
  (`drizzle-kit` est une devDep). Seul le banc réel l'a dit.
- **Deux faux rouges de décor local** : tables résiduelles de six jours de bancs sur MySQL et
  MariaDB partagés — j'ai failli les imputer à mon diff.

## Recommandations

1. Le banc de publication reste rouge : la séquence qui tient est écrite sur #146 (installer côté
   hôte depuis les tarballs, générer, puis construire l'image). Coût à arbitrer : un `npm install`
   de plus par scénario.
2. Vérifier les deux `create app --no-install` de `release.yml` (l. 287, 333) — même cas.
3. Le décor MySQL des bancs n'est pas isolé de ses voisins : une table laissée par une autre suite
   fait refuser le banc suivant, avec un message qui accuse le produit.

## Patterns récurrents (déjà gérés)

- ✅ « La commande du DÉPÔT est l'autorité » — rattrapé par la barrière de poussée, pas par moi.
- ✅ « 1 RÈGLE = 1 implémentation » — la variable dupliquée a été annulée avant le commit.
- ✅ « Prouver sur l'artefact REÇU » — le tarball a été ouvert pour vérifier qu'il portait bien la
  modification que le banc prétendait éprouver.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `5037e901` | fix(orm) : booléen MySQL rendu à sa forme déclarée quand la base est adoptée |
| `22d95b31` | test(orm) : banc de migration isolé des migrations commitées du dépôt (#152) |
| `698c17e8` | test(orm) : semis par lots dans le banc d'échelle (#151)                     |
| `9028e49b` | fix(orm) : narrowing du tag du décor pour le contrôle de types des bancs     |
| `9e70e420` | feat(cli) : une application naît avec sa migration, pas seulement son entité |
