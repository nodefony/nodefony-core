---
date: 2026-09-26
session_id: 2d548e9a-2053-4f9b-8d45-65213a249889
focus: "#498 — require-await coupée, no-confusing-void-expression allumée"
---

# Retex 2026-09-26 — 2d548e9a

- **Fait** : e840bc26a build(lint) couper `require-await` (557 sites, tous des contrats
  `Promise` : `async` y fait d'une exception un rejet) ; d8c472c7f build(lint) allumer
  `no-confusing-void-expression` avec `ignoreArrowShorthand` + `ignoreVoidReturningFunctions`
  (1 343 → 24 sites de production réécrits, identiques à l'exécution), gabarit aligné, bouchon
  `redirect` d'`authFactorAudit` corrigé. #498 commenté (avancé).
- **Frictions** (versées au sas, thème 🎯) : « gabarit non concerné » affirmé sur le seul lint —
  le gate de parité vit dans `npm test` et tombait ; suppression `sed` par numéro de ligne décalé
  qui a effacé les appels `close/drop` WS (rattrapé par le lint, vu par le user) ; bouchon qui
  rendait une valeur que la production ne rend jamais.
- **Décisions** : une règle qui ne signale que des implémentations correctes d'un contrat se
  COUPE avec motif écrit, plutôt que de tordre 557 sites ; les exemptions d'idiomes lisibles
  gardent la règle sur le seul vrai défaut (retour inféré `X | void` dans le `.d.ts`).
