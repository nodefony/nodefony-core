---
date: 2026-07-16
session_id: 2d2ec5a4-8f61-4a46-ac2a-d5462a16517f
focus: Solder les dettes (llm hors gates, typage realtime, Cli NODE_ENV) puis croiser les mémoires/l'audit avec le dashboard — 2 bugs critiques trouvés
---

# Session retro — 2026-07-16 — 2d2ec5a4

## Fait (10 commits)

- **`@nodefony/llm` était hors des DEUX gates** (`130f7386`) : seul workspace sur `bun test` (bun absent
  de la machine → `npm test` racine en **exit 127**), et pas de script `typecheck` (son `lint` n'est
  appelé par rien). Migré vitest (25 tests), `tsconfig.tests.json` prouvé non mort (`--listFiles`),
  `types` réparé (pointait un `.d.ts` que le build ne produit pas).
- **Garde typecheck client** (`7a460b11`) : `tsconfigClient.json` (`types: []` = zéro node-ism) n'était
  compilé qu'au `build:types`. 1 ligne, 2 s.
- **3 flakes matés** (`8b6d13bc`) : tests à spawn (`zsh`+`compinit`, `lsof`) sous timeout 5 s par défaut.
- **Typage realtime** (`3f6c5de1`) : TROU 1 (index signature qui fuit → tout nom inconnu passait, branche
  « hors map » morte, wildcard mal typé) + TROU 2 (`ActionParams` sur `in?`). TROU 3 laissé ouvert exprès.
- **`Cli.ts:181`** (`234f1208`) : cast `NODE_ENV as EnvironmentType` → `toEngineEnvironment` (+10 tests).
- **Dashboard** (`35993786`) : section « Dettes transverses » + 2 bugs critiques vérifiés + 3 titres recalés.
- Fichiers de la session desktop commités : README (6 chiffres faux corrigés), skill report (mode
  présentation réparé), banc Express équitable.

## Frictions (→ RETEX.md)

- Un audit d'agent a raison ET tort **dans le même document** → n'acter que ce qu'on a relu au code.
- Un test qui CONSTATE un bug tombe au fix : signal, pas régression.
- Compteur de tâches sans exclusion des `⏭️` → chiffre faux (le fichier avait raison, ma formule non).
- Exit code d'un `cmd; echo $?` en background = celui du `echo`.

## Coûts évidents

- 4 `npm test` complets (~2-3 min chacun) — dont 2 évitables : le 1ᵉʳ tronqué par un `tail -60`, le 2ᵉ
  cassé par un `dist` absent après `npm install` (piège #1 du repo, non anticipé).
- 3 tentatives de commit rejetées par commitlint (header > 100, puis `subject-case` majuscule en tête).

## Recommandations

1. **Transactions Drizzle avant la publication npm** — publier un framework dont les transactions lèvent
   `not connected` hors SQLite = offrir l'échec à la 1ʳᵉ vraie app. L'audit le classait #1/10.
2. **Le test manquant EST la dette** : ajouter `transaction()` au banc de contrat multi-dialecte, sinon
   le bug reviendra (12 bancs e2e PG/MySQL ne l'appellent jamais).
3. Vérifier les 10 points de l'audit encore marqués NON VÉRIFIÉ (TRUSTPROXY, `@inject` parent,
   `ClusterManager` signaux…) avant de les traiter — ils sont ancrés, pas prouvés.

## Patterns récurrents (déjà gérés)

- ✅ Sentinelles de type : elles ont cassé d'elles-mêmes au fix, 6 sessions plus tard, sans mémoire humaine.
- ✅ `dist` périmé/absent après `npm install` → `npm run build` (piège connu, retombé dedans quand même).
- ✅ `rg -r` = replace (piège évité de justesse — mémoire `feedback_rg_no_replace_flag`).

## Commits produits

| Commit     | Sujet                                                           |
| ---------- | --------------------------------------------------------------- |
| `130f7386` | fix(llm) : le seul module IA du README hors des deux gates      |
| `7a460b11` | fix(core) : garantie « zéro node-ism » vérifiée par aucune gate |
| `8b6d13bc` | test(core) : 3 tests à spawn flakaient sous charge              |
| `abfcc1b2` | fix(skill) : mode présentation « 1 / 0 »                        |
| `e79bf2a0` | feat(skill) : Express « équitable » au banc                     |
| `3186a351` | docs(readme) : six chiffres se vérifiaient en secondes          |
| `3f6c5de1` | fix(realtime) : les maps fuyaient leur index signature          |
| `234f1208` | fix(cli) : un axe libre ne se caste pas en mode moteur          |
| `35993786` | docs(migration) : dettes transverses + 2 bugs critiques         |
