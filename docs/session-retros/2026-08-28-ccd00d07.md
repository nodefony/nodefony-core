---
date: 2026-08-28
session_id: ccd00d07-c846-4d78-8c37-9deb39afebb4
focus: "#95 — un composant peut retenir la mise en service ; trois lecteurs à accorder, et mon propre outil qui accusait à tort"
---

# Session retro — 2026-08-28 — ccd00d07

## Fait

- **`dba85ded` — #95, le registre de disponibilité** (lot S5-R du chantier #17, fermé). Registre au
  CŒUR (`readinessRegistry.ts` + `Kernel.setReadiness/clearReadiness/readinessBlocked/
readinessReport`, publiés par `IKernel`), lu par `/readyz` (`http-kernel.ts:978`). Verdict DÉJÀ
  calculé, registre `null` tant que personne ne s'inscrit et libéré au retrait du dernier.
- **Deux vérités concurrentes alignées** (au-delà de l'énoncé) : `livez.ready` du plan
  d'administration valait `booted` ; `nodefony status` affichait « 2/2 ports UP » sur un pod hors
  service. Les trois lecteurs rendent désormais le même verdict — la sonde n'interroge jamais un
  port tenu par un AUTRE projet.
- **`f2938a05` — le compte rendu de fermeture** (`ticket-close.mjs` + §7 du skill `nodefony-ticket`,
  renvoi depuis `nodefony-session`) : commits, preuves, **ce qui a débordé de l'énoncé**, **ce qui
  n'a pas été fait**. Réparé dans la foulée : il n'avait jamais rien trouvé.

Gate mémoire 9/9 (1000 GET → 1,17 MB / seuil 35). `test:all` : 8973 passés, intégration 820/820.

## Coûts évidents

- **Deux `test:all` complets jetés** : le premier tournait sur un arbre où j'avais écrasé une suite,
  le second a dû être relancé après réparation. ~12 min de mur, évitables par un `ls` avant `Write`.
- **Trois cycles build/restart perdus** en course avec le superviseur de développement — qui rebuild
  seul sur la modification de source. Mes builds manuels ont fait échouer les siens.
- Quatre débranchements de garde (nécessaires, eux) : chacun coûte un cycle rebuild + relance de banc.

## Frictions (versées dans `RETEX.md`)

- `Write` sur un nom déjà pris par un AUTRE concept → 11 cas écrasés ; le retour de l'outil le disait
  en un mot (« updated »), `git status` l'a confirmé (`M` au lieu de `??`).
- Build manuel concurrent du superviseur → le serveur servait l'ancien code, un banc « passait » en
  prouvant l'inverse.
- Trois lecteurs du même verdict, un seul inventorié — le troisième nommé par le user.
- Mon propre script accusait à tort : `--grep='#95\b'` ne mord pas (git = ERE POSIX, pas de borne
  de mot).

## Recommandations

1. **`ls` avant tout `Write` de fichier neuf**, et lire le verbe du retour d'outil (created/updated).
2. **Ne jamais lancer `turbo build` à la main quand le superviseur tourne** — il rebuild seul ; le
   contrôle est `[dev] ✓ build OK` dans le journal.
3. **Un script de pilotage neuf se lance sur un cas dont on connaît la réponse** avant d'être livré.

## Patterns récurrents (déjà gérés)

- ✅ Garde neuve vue mordre : 4 débranchements, 9 rouges constatés au total.
- ✅ Sortie complète capturée dans un fichier avant tout filtrage.
- ✅ La garde anti-`git checkout` sur arbre sale a refusé mon geste, à juste titre — restauration
  faite par `git show HEAD:<fichier>`, sans toucher à l'index.

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `dba85ded` | feat(http): laisser un composant retarder la mise en service — closes #95 |
| `f2938a05` | fix(pilotage): réparer le compte rendu de fermeture, qui n'a rien trouvé  |
