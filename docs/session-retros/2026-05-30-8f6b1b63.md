---
date: 2026-05-30
session_id: 8f6b1b63-34ed-4f7d-990f-da0891e4bf5e
focus: Kit cycle requête — L3 (sampling audit) + L4 (aplatir new Promise async) + A/B prod + doc S1 realtime
---

# Session retro — 2026-05-30 — 8f6b1b63

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    46 |
| Read            |    18 |
| Edit            |    18 |
| Write           |     3 |
| Skill           |     2 |
| AskUserQuestion |     1 |

## Top fichiers Read/Edit

| Fichier                                    |   × |
| ------------------------------------------ | --: |
| http/service/http-kernel.ts (Read+Edit)    | 5+6 |
| memory/project_request_cycle_perf_plan_kit | 4 E |
| http/service/audit-logger.ts               | 2+1 |
| realtime/server/RealtimeHub.ts             | 2 R |

## Coûts évidents

- **A/B RPS raté = coût #1.** 1er essai en `node -e` `detached:true` + `process.exit(0)` → pipe log cassé + **process prod zombies** (38200/38275 vivants, PID 36810 sur 5151) → mesure « avant L4 » servie par un binaire incertain → distributions chevauchées suspectes → **tout refait** en protocole atomique. ~12 Bash perdus + diagnostic.
- **Cycles serveur nombreux** : 2 restarts dev (L3, L4) + 4 boots prod (A/B raté + atomique ×2 côtés). Incompressible pour un A/B honnête, mais le raté a doublé les boots prod.
- http-kernel.ts (gros) relu 5× — justifié (4 conversions L4 + revert A/B + signatures).

## 💶 Coût (€)

- Total ~**€41,5** (≈ $45, 89 tours). Décompo : **cache read 54 % + cache write-1h 19 % = 73 %** ; output 26 % ; input 1 %. Cache-dominé classique. ~€14/commit (3 commits) — gonflé par le bench A/B raté + re-mesure.

## Recommandations

1. **Mémoire/kit : bench A/B = protocole ATOMIQUE** (clean→boot tracé `$!`→wait health→bench→kill, dans UN SEUL shell). Jamais `detached`+`exit` inter-commandes (zombies + binaire servi incertain). → ajouté au kit perf §5. Réutilisable pour tout A/B.
2. **Mesurer AVANT de committer un levier perf.** L4 commité avec « gain non chiffré » → le user a (à raison) exigé l'A/B après coup. Pour un commit `perf(...)`, faire l'A/B dans la même passe (ou annoncer explicitement « neutre attendu, non benché »).
3. Pattern « 1 fichier = 1 levier isolé » pour l'A/B (`git checkout <sha> -- <file>` + rebuild) → isole proprement L4. Bon, à garder.

## Patterns récurrents (déjà gérés)

- ✅ memory.test flake (1000-GET ET sync-crashes) → isolation = vérité (innocentés 2/2 chacun).
- ✅ `new Promise(async executor)` anti-pattern → capturé MEMORY.md http (gotcha L4).
- ✅ Décision impactante (S1 touche contrat public + prémisses kit fausses) → AskUserQuestion légitime → user a tranché « skip ».
- ✅ Commits FR à apostrophes → `git ci -F` fichier.

## Commits produits

| Commit  | Sujet                                                              |
| ------- | ------------------------------------------------------------------ |
| 7a7d740 | perf(http) L3 — audit sampling opt-in (-94 % poste audit)          |
| a545d64 | perf(http) L4 — aplatir 3 new Promise(async) (-33 l, A/B neutre)   |
| 3d10ae6 | docs(realtime) — plan S1 mutualisation stringify fan-out (reporté) |
