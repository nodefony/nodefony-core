---
date: 2026-06-25
session_id: 271ff8b3-3bd8-4f4e-8765-4c3efe70e651
focus: Introspection process multi-mode (dev/prod/cluster) + gardes anti-collision
---

# Session retro — 2026-06-25 — 271ff8b3

> 2ᵉ session du 2026-06-25 (la 1ʳᵉ = red-team scope DI + page Runtime + bench perf, session `b076cabc`).

## Focus

Bug signalé par le user : `nodefony status` ne dit pas qu'un runtime tourne en **prod** (juste « ports
occupés »), et `nodefony dev` démarre par-dessus un prod → incohérence. Demande : couvrir **tous les
cas, cluster compris**. Avant ça : validation visuelle de la page Runtime en dev (reprise du \_state).

## Fait

- **Validation page Runtime en dev** : serveur relancé en dev, 3 strates vertes (status source de
  vérité · endpoint `/kernel/api/processes` 401 = gate RBAC · transform Vite `Runtime.tsx` 200).
- **Chantier introspection multi-mode** (`1bd57a7d`, poussé `refactor/p6-security`) :
  - `devProcess.ts` : champ `mode` (dev/prod/cluster), `classify` élargi (master/worker/prod-server),
    helpers purs `detectRuntimeMode`/`runtimeModes`/`findRuntimeConflict`.
  - `status` : libellé + rôles + synthèse + warnings **selon le mode** (workers cluster ≠ empilement).
  - `stop` : arrête **tout** runtime Nodefony (group-kill racines).
  - **Gardes anti-collision** fail-loud (exit 69, jamais de kill cross-mode auto) : `DevSupervisor`
    refuse sur prod/cluster (ne tue que les résiduels dev) ; `launchTopology` refuse l'inverse.
  - `start.sh` : phase KILL délègue à `nodefony stop` (l'ancien `pkill -f "nodefony development"`
    ratait le superviseur `nodefony-dev-supervisor`).
  - doc IA kernel (CLAUDE.md/MEMORY.md) + MIGRATION_STATUS P14.15 actualisés.
- **Réponse archi au user** : pourquoi Vite est enfant de `server` et pas du superviseur (le module
  frontend = `@Service` dans le serveur ; group-kill propre → 0 orphelin).

## Frictions (→ RETEX.md)

- `timeout N cmd` n'existe pas sur macOS (exit 127) ; cwd persiste entre Bash → re-`cd` relatif casse ;
  `${PIPESTATUS[0]}` après pipe peu fiable → exit code via fichier.
- `start.sh` TIMEOUT 25s < boot 32s après gros rebuild turbo → pas un crash, attendre en fond.
- `pkill -f <argv>` rate un process renommé par `process.title` → tuer par `ps`+titre (`nodefony stop`).
- Garde prouvé dans les 2 sens (live exit 69 + tests purs) — un garde n'est fermé que sur ses 2 faces.

## Gates

- devProcess unit **20/20** · core **1662 passed / 0 failed** · build core+framework+studio verts.
- Prouvé LIVE : status cluster (master+2 workers), `nodefony development` sur cluster → refus exit 69,
  stop cluster propre.

## Commits produits

| Commit     | Sujet                                                                                  |
| ---------- | -------------------------------------------------------------------------------------- |
| `1bd57a7d` | fix(core): introspection process multi-mode (dev/prod/cluster) + gardes anti-collision |
