---
date: 2026-09-01
session_id: 5fff32db-4ff4-4a16-9975-209571347ef3
focus: Grappe documentation (#53) — 6 tickets, puis refonte du portail Studio sur retour user
---

# Session retro — 2026-09-01 — 5fff32db

## Focus

Nuit autonome sur la grappe documentation, puis — au réveil du user — refonte du
menu, du plein écran et de la recherche du portail Studio, sur ses retours
successifs.

## Fait

- **6 tickets fermés** : #45, #48, #49, #52, #46, #44. Jalon 10.0.0 : 32/65.
- **Corpus** : doc-lint 92/92 (exit 0), 235 ancres recalées, 285 → 69 SUSPECT,
  0 LINE_OUT, 0 FILE_NOT_FOUND. Page neuve `testing.md`. 64 `navTitle` + gate.
- **Portail Studio** : menu refait sur un modèle (périmètre, ordre pédagogique,
  actif unique, familles lisibles, `mode="page"`), plein écran cohérent,
  **recherche plein texte** avec extraits en cartes.
- **5 affirmations fausses corrigées** après audit `fable`, dont une écrite par
  moi la nuit même.

## Coûts évidents

- **7 cycles rebuild + restart** sur la fin de session (chaque modif du service
  documentation touche le back). Batcher davantage aurait économisé ~4 cycles.
- La grammaire de `NF_BROWSER_ACTIONS` bute sur un `=` dans un sélecteur CSS :
  4 tentatives perdues avant d'écrire un script Playwright direct.
- Un sous-agent `haiku` à ~33 k tokens pour un rapport inutilisable (1/6 exact).

## Recommandations

1. `NF_BROWSER_ACTIONS` — accepter un sélecteur contenant `=` (échappement ou
   séparateur distinct). Le contournement actuel est un script à part.
2. Un test comparateur entre `ROOT_GROUPS` (`DocumentationService.ts`) et
   `PUBLIC_DIRS` (`build-docs-site.mjs`) : la règle est dupliquée de part et
   d'autre d'une frontière de paquets, et rien ne la garde alignée.
3. Le gate `doc-lint` n'est lancé par AUCUN workflow — il vit dans un skill.

## Patterns récurrents (déjà gérés)

- ✅ « prouver la cible avant de croire le verdict » — repris deux fois ce jour.
- ✅ « une capacité qu'on n'atteint pas n'existe pas » — `navTitle` s'arrêtait au
  scanner.
- ✅ La garde du dépôt a refusé un `git checkout` sur arbre sale : elle a bien
  travaillé, contre moi.

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `30013024` | #45 — cinq pages fantômes et « mocha + bun »                  |
| `61d908f9` | #48 — l'architecture après le geste                           |
| `47e32fad` | #49 — un backend de session qui n'existe pas                  |
| `28610bbe` | #49 — une colonne entière inventée dans la matrice des stores |
| `3507a99b` | #49 — trois pages renvoyant à l'intérieur du dépôt            |
| `a6484d77` | #49 #52 — les 11 pages conformes, 91/91                       |
| `deaf3c4f` | #49 — 235 ancres recalées                                     |
| `6feb2cf2` | #46 — page `testing.md`                                       |
| `1c9346aa` | générateur : les exemples de liens ne sont plus des liens     |
| `a10635e9` | #44 — 64 `navTitle` + gate vu rouge                           |
| `1671b42a` | menu de la doc refait sur un modèle                           |
| `7e32e35e` | plein écran cohérent                                          |
| `74eb21a7` | recherche plein texte + 3 défauts visuels                     |
| `87d839ea` | cinq affirmations que le code dément                          |
| `343d3cdb` | les écrans nommés reçoivent leur chemin                       |
