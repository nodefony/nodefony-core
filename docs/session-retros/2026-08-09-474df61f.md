---
date: 2026-08-09
session_id: 474df61f-4617-446b-a355-4680ce11b345
focus: rôle serveur de ressource OAuth 2.1 (RFC 9728/6750/8707) au cœur, branché sur la porte MCP
---

# Session retro — 2026-08-09 — 474df61f

## Fait

- **Norme MCP figée hors ligne** (`723ded21`) : la révision `2026-07-28` entière (30 pages +
  `schema.ts`/`schema.json` + 150 exemples) et les RFC OAuth 9728/6750/8707, dans
  `nodefony-rfc/references/` — 758 Ko, arborescence identique à l'amont. Motif : la même spec avait
  été téléchargée 3× dans la journée.
- **Rôle _serveur de ressource_ OAuth 2.1 au CŒUR** (`6296fdaa`) : `nodefony/src/oauth/` —
  métadonnées RFC 9728, chemin bien connu par INSERTION (§3.1), défi RFC 6750, audience RFC 8707,
  contrat `IAccessTokenVerifier`. Fonctions pures, **hors de `mcp/`**. `bearerToken` remonté au
  cœur (`runtime/bearer.ts`), security n'en garde qu'un point d'entrée.
- **Porte MCP branchée** (`d7e075ec`) : `mcp.authorization` (éteint par défaut),
  `OAuthMetadataController`, `caller` littéral remplacé par le verdict, `503`+`CRITIC` si le rôle
  est déclaré sans vérificateur.

## Coûts évidents

- **Session en parallèle d'un banc devkit** — build du cœur différé ~1 h pour ne pas contaminer
  1 h 30 de mesure. Bonne décision, mais elle a bloqué toute validation du devkit pendant ce temps :
  le travail a été écrit « à l'aveugle » puis prouvé d'un bloc.
- **3 restarts serveur** (débranchement + restauration) — le DevSupervisor ne voit pas un rebuild de
  `dist/` d'un package, il faut relancer explicitement.
- Un débranchement inopérant a coûté un cycle rebuild+restart complet pour rien.

## Recommandations

1. **Règle de doc d'ABSENCE** — quand une capacité arrive, `rg` sur sa justification d'absence AVANT
   d'écrire la nouvelle doc (5 fichiers étaient devenus faux d'un coup). Versée au SAS.
2. **Prouver le débranchement** — `git diff --stat` est vide sur un fichier neuf ; la preuve est le
   rouge lui-même. Versé au SAS.
3. **Test dont la réponse attendue est la réponse par défaut** (404, `null`, `[]`) — exhiber le
   témoin discriminant, sinon il passerait aussi sans le code. Versé au SAS.

## Patterns récurrents (déjà gérés)

- ✅ `cd` relatif → dérive du cwd : le garde-fou du harnais a mordu ([[feedback_bash_cwd_drift]]).
- ✅ Décider + justifier au lieu d'un QCM (généricité `oauth/` vs `mcp/`, périmètre du lot suivant).
- ✅ Nommer ce qui n'a PAS été lancé avant de dire « livré ».

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `723ded21` | docs(skills) : la norme MCP cesse d'être une requête réseau  |
| `6296fdaa` | feat(core) : une ressource sait dire qu'elle est protégée    |
| `d7e075ec` | feat(devkit) : la porte MCP peut exiger un jeton, et dire où |

## Preuves

| Suite              | Résultat                                               |
| ------------------ | ------------------------------------------------------ |
| Cœur               | 2711 verts (26 neufs, vus rouges : 5 au débranchement) |
| devkit             | 117/117, **0 sauté** (banc HTTP inclus, 1 rouge joué)  |
| security           | 929/929                                                |
| Gate mémoire       | 9/9                                                    |
| Build · typechecks | 21/21 · devkit + security verts                        |

**Non lancé** : `npm run test:all` (ORM, docker, intégration HTTP).
