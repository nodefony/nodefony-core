---
date: 2026-05-30
session_id: 400f8187-ae07-487b-8361-ba4dc03ce8f2
focus: Profiling plafond RPS (TLS réfuté) → plan cycle requête L1-L5 + volet sondes → L1+L2 (+6,2%) + S2 backpressure WS livrés
---

# Session retro — 2026-05-30 — 400f8187

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |    71 |
| Edit       |    33 |
| Read       |    32 |
| Write      |     5 |
| WebFetch   |     2 |
| Skill      |     2 |
| ToolSearch |     1 |

## Top fichiers Edit (volume produit)

| Fichier                                                        |   × |
| -------------------------------------------------------------- | --: |
| memory/project_request_cycle_perf_plan_kit.md (créé + enrichi) |  10 |
| @nodefony/realtime/.../WsConnectionTransport.ts (+ test)       |   7 |
| @nodefony/realtime/.../IRealtimeProbe.ts                       |   3 |
| @nodefony/http/.../trace.ts                                    |   2 |

## Coûts évidents

- ~6 boots serveur (prod baseline/prof/0x/after + dev pour memory.test) — nécessaires (A/B + gates), pas du gaspillage.
- 1 faux négatif memory.test « requires server » (ECONNREFUSED après kill ports) → relancé serveur UP. Pattern connu (nodefony-debug).
- 1 grep mocha cassé : `--grep "a\|b"` (pipe échappé = littéral) → 0 test → relancé sans `\`.
- Kit édité 10× : artefact construit incrémentalement (le volet sondes a été ajouté EN COURS à la demande user) — peu évitable.

## 💶 Coût (€)

- Total **~€63,4** (≈ $68,9) — cache **78 %** (cw1h $17,8 + cr $36,2) vs output **21 %** ($14,4). 116 tours, 3 livrables → ~€21/livrable.
- Output élevé (21 % vs ~10 % habituel) : beaucoup de code produit (S2 = 8 fichiers) + plan dense. Session productive, pas de re-lecture chère.

## Recommandations

1. **Candidat skill `nodefony-profile`** (futur, si ≥3 usages) : workflow profiling prod (0x flamegraph + `node --prof` + A/B clair/TLS + parse Summary). 1ʳᵉ fois ici → noter, pas créer.
2. **Gotcha `--prof` macOS** : 64 % des ticks natifs s'effondrent sur un faux symbole snapshot (`BlobSerializerDeserializer`) → C++ inexploitable sans dtrace. Capturé dans le backlog.
3. **Mocha `--grep`** : c'est une regex JS → pipe = `|` (jamais `\|` qui devient littéral).

## Patterns récurrents (déjà gérés)

- ✅ memory.test « requires server » = serveur down (nodefony-debug, 1ʳᵉ hypothèse).
- ✅ flaky « 1000 GET < 35 MB » → innocenté en isolation (MEMORY.md http).
- ✅ commit FR apostrophes via `-F` fichier ; hook pre-commit régénère symbols.json.
- ✅ dist root périmé → rebuild avant boot prod.

## Commits produits

| Commit    | Sujet                                                                              |
| --------- | ---------------------------------------------------------------------------------- |
| f534b5f   | perf(http) L1+L2 : log DEBUG gardé + pool entropie traceparent — +6,2 % RPS        |
| bc803ea   | feat(realtime) S2 : back-pressure WS drop/close 1013 — trou OOM superviseur comblé |
| (mémoire) | profiling + kit cycle requête + règle observabilité (repo nodefony-ai-memory)      |
