---
date: 2026-06-22
session_id: efaf73a7-84f1-4067-8b75-db475cf51566
focus: Studio archi rôles + Users CRUD + fix élévation de privilège socket WS figée
---

# Session retro — 2026-06-22 (3ᵉ) — efaf73a7

## Fait

- **`d0d2d764` fix(security,realtime,studio)** — **élévation de privilège par socket WS figée**.
  Une WebSocket grave l'identité au handshake et SURVIT à sa session (singleton partagé navigateur) →
  après logout admin puis login d'un autre compte, le pont `api.request` rejouait un GET data plane
  avec l'identité ADMIN (le user voyait TOUS les users en ROLE_USER ; contourné par F5). Défense 2 couches :
  BACK `IRealtimeToken.isValid()` re-valide la session BFF au pont (`invokeApiRequest`→401, fail-closed) ;
  FRONT `RootStore` reconnecte la socket au changement d'identité. **Tests 55/55** (`realtimeFrameLock.test.ts` +5).
- **`b064f4ac` feat(studio,user)** — archi rôles front (`auth/roles.ts` source unique + hooks + `RoleGate`,
  dédup `ADMIN_ROLE`, nav Firewall/Audit gatés admin) · console Users **create/update** (modals, back déjà
  complet, `patchAbsolute`) · fenêtres Modal **deux-tons** `light-dark()`.
- Les 2 commits POUSSÉS sur `refactor/p6-security`. Mémoire IA poussée (`a8cb8fe`).

## Frictions / leçons

- **« ça marche après F5 » = diagnostic en or** : pointe direct vers une socket qui re-handshake →
  identité figée de la socket. Écouter le symptôme user tôt (a évité un forage long).
- **Confusion d'identité ≠ révocation différée** : l'asymétrie de révocation WS est documentée/assumée
  (état de l'art) ; un compte B qui HÉRITE de la socket de A (navigateur partagé) = élévation, inacceptable.
- **`light-dark()` pour un deux-tons** : `default`/`default-hover` s'INVERSENT clair↔sombre (en clair
  `default`=blanc) → figer le sens explicitement, sinon « tout blanc » en clair (corrigé en 2 itérations).
- **Entité User minimale** (vérifié) : identité = `identifier`+`roles` ; profil étendu = `metadata`
  (pas de firstName/lastName de 1er niveau). Le modal CRUD est fidèle au modèle, pas un oubli.
- **Gating front = AFFICHAGE** : la vraie défense reste serveur (le fix socket le prouve : le data plane
  refuse, le front ne fait que cacher/reconnecter).

## 💶 Coût

- Session longue full-stack (Studio + realtime + security) ; non mesuré (END léger).

## Reste — prochaine session

- **MODE USER** (specs user figées) → kit `project_studio_mode_user_kit` : couper les appels admin 403 en
  user · page profil self (rôles lecture + mdp zone danger, endpoint self ≠ admin) · cleaner menu Security
  · nom/prénom via metadata.
- Dette : graduer ces leçons vers les skills `nodefony-studio-dev` (briques roles.ts/RoleGate/reconnect/
  light-dark) + `nodefony-framework-dev`/`security-review` (re-validation token WS au pont = surface
  « token ALS figé »).

## Commits produits

| Commit   | Sujet                                                                          |
| -------- | ------------------------------------------------------------------------------ |
| d0d2d764 | fix(security,realtime,studio): élévation de privilège socket WS figée          |
| b064f4ac | feat(studio,user): archi rôles front + console Users CRUD + fenêtres deux-tons |
