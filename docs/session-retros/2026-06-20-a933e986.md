---
date: 2026-06-20
session_id: a933e986-e9c1-4dec-a138-92df46595058
focus: DevSupervisor DX — nodefony status/stop standalone (hors trunk) + 2 parades résilience
---

# Session retro — 2026-06-20 — a933e986

## Focus

Chantier DevSupervisor DX (kit étape A + B) : `nodefony status` + `nodefony stop` en
commandes SYSTÈME **standalone** (zéro boot, marchent hors trunk), + 2 parades « pas de
dégradation silencieuse » (post-vérif dist au boot ; superviseur lit livez).

## Fait (6 commits)

- `21742b67` status · `73ae92ee` parade 1 (post-vérif dist) · `7f254830` standalone hors-trunk
- `5e43c48a` parade 2 (livez degraded) · `981a5add` stop · `2354e425` skill start-server

## Frictions / leçons

- **Locale FR → `ps -o pcpu` formate `%CPU` avec une VIRGULE** (`0,0`) → regex `[\d.]` ne
  matchait pas → 0 process (faux « aucune instance »). Fix : `LC_ALL=C` au spawn + parse `,`.
- **« Vert mais cassé » EN CASCADE** : `security/dist` absent → fail-soft security → `test`
  (l'importe) tombe → `test:batch` invisible → test CLI rouge. Visible SEULEMENT en `-d`.
  `turbo build` exit 0 ≠ outputs présents → vérifier le terrain (`missingWorkspaceDists`).
- **Suspecter son diff PUIS prouver** : 3 fails « de mon commit » = 2 transitoires (watch
  rebuildait pendant les tests) + 1 état repo (`git status` module = vide → hors mon code).
- **Outillage de process ≠ booter le kernel** → fast-path standalone avant `new Kernel`.
- **Démo réelle** : `mv security/dist` → turbo a fait `cache miss` + rebuild (165 s, toute la
  chaîne security→… invalidée), PAS le cache-hit trompeur attendu → parade 1 reste le filet.

## Validé runtime

stop (arbre tué proprement, ports libérés) · status (repo / `$HOME` / `/tmp`) · parade 1
(security réparé) · parade 2 (`framework ready`) · serveur relancé 4 process sains.
Gates : memory 9/9, dispatch 8/8, devProcess 12/12, --help 5/5, build vert.

## Commits produits

| Commit     | Sujet                                               |
| ---------- | --------------------------------------------------- |
| `21742b67` | nodefony status — introspection process dev         |
| `73ae92ee` | parade 1 — vérif dist sur disque (anti cache-turbo) |
| `7f254830` | status standalone (marche hors trunk)               |
| `5e43c48a` | parade 2 — fail-loud sur dégradation (livez)        |
| `981a5add` | nodefony stop — group-kill (remplace pkill -9)      |
| `2354e425` | skill start-server alimenté (status/stop)           |
