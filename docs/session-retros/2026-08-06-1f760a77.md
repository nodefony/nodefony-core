---
date: 2026-08-06
session_id: 1f760a77-a56c-4103-9df9-22f068aa1e89
focus: preuve d'équité express-fair rejouée sur HEAD — équité confirmée, harnais versionné
---

# Session retro — 2026-08-06 — 1f760a77

## Fait

- Preuve d'équité express-fair sur HEAD, décor exact du banc mono prod
  (`NODE_ENV=production NF_LOG_DRIVER=null NF_BENCH_ROUTE=1`) : 1 000/1 000 → 200,
  0 `Set-Cookie`, 0 commit sqlite (`PRAGMA data_version` stable, connexion readonly
  persistante + 10 s de repos témoin), deltas 0 sur 6 tables framework, data plane
  profiler → 404, timing noop vérifié au code (`Context.ts`, non forcé par la config).
- Instrument vu ROUGE avant d'être cru : écriture témoin → data_version 2→3.
- Harnais versionné dans le skill (`bench-frameworks/express-fair-proof.mjs`, exit ≠ 0
  sur toute dérive), catalogué ; en-tête d'`express-fair.mjs` recalé (ancienne preuve à
  200 req antérieure aux lots C/D) ; kit perf + MIGRATION_STATUS à jour.

## Frictions

- Header commitlint > 100 chars au premier commit (re-commit, 30 s) — mineur.
- Aucune autre : session courte (~10 min de terrain), zéro restart superflu, decor posé
  du premier coup.

## Leçons (portées au SAS)

- `PRAGMA data_version` (connexion readonly persistante) = sonde exhaustive « 0 écriture,
  toutes tables » ; deux invocations CLI ne se comparent pas ; fenêtre de repos témoin
  pour discriminer un écrivain périodique.

## Commits produits

| Commit     | Sujet                                                                                               |
| ---------- | --------------------------------------------------------------------------------------------------- |
| `e68c4e59` | test(perf): preuve d'équité express-fair rejouée sur HEAD — 0 Set-Cookie, 0 commit db, profiler 404 |
