---
date: 2026-05-20
session_id: f9271beb-bcad-4f3d-a41a-8114204b885f
focus: Studio modules — Docs/API/Coverage/Tests, Mermaid, carte Core, migration vitest, dashboard UX, deps versions/outdated, sécu path
---

# Session retro — 2026-05-20 — f9271beb (session longue, 16 commits)

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Bash | 222 |
| Edit | 211 |
| Read | 75 |
| Write | 24 |
| TaskCreate/Update | 14 |
| Skill | 4 · AskUserQuestion 3 · ToolSearch 2 |

Transcript 2405 lignes / 7 Mo. Tool results 555 events, ~703k chars (avg 1267, sain). **16 builds + 16 restarts serveur.**

## Top fichiers édités
| Fichier | × |
| ------- | -: |
| studio/.../routes/ModuleDetail.tsx | 42 |
| studio/.../routes/Modules.tsx | 27 |
| framework/.../KernelAdminApi.ts | 25 |
| framework/.../docsReader.ts | 12 |
| framework/package.json | 11 |

## Coûts évidents
- **16 cycles rebuild+restart serveur** : chaque modif backend (KernelAdminApi/docsReader) = stop→build→start (~30 s). Les modifs frontend, elles, passaient en Vite HMR (0 restart) — bon réflexe.
- **2 faux bugs « path absolu / liste périmée »** = cache React (état non rechargé). Le user a dû hard-reload. Coût : allers-retours inutiles.
- **Mis-attribution d'une régression** : 21 échecs http qualifiés « pré-existants » alors que c'était MON fallback SPA générique (`/{section}/{page}`) masquant `/nodefony/test/*`. Trouvé seulement parce que le user a poussé sur les tests.
- **Rabbit-hole coverage** (monocart×mocha×tsx) : long mais a abouti (vitest). Bien tracé.
- **Régression en cascade dans la même session** : la sécu « path relatif » a cassé `categoryOf` (qui matchait `/src/modules/` avec slash) → modules test classés framework.

## Recommandations
1. **Après une modif frontend → curl le transform Vite + dire au user de HARD-RELOAD** (cache React = 2 faux bugs cette session). Cf [[feedback_no_headless_chrome]] (vérif front).
2. **Refactor d'un champ exposé (ex path absolu→relatif) → grep ses consommateurs AVANT** (`categoryOf`, affichages). Une sécu a introduit une régression downstream → mémoire `feedback_refactor_grep_consumers`.
3. **Suspecter son propre diff d'abord** avant « pré-existant » (déjà en [[feedback_spa_fallback_literal]] — l'appliquer).
4. **Sessions longues (7 Mo) → /compact périodique** (cf [[feedback_token_economy]]).

## Patterns récurrents (déjà gérés)
- ✅ Watch Rollup → stop avant édit backend (skill start-nodefony-server).
- ✅ Coverage = vitest, runner hook-free, lcov pour le core ([[feedback_coverage_modules]]).
- ✅ Fallback SPA = préfixe littéral, pas générique ([[feedback_spa_fallback_literal]]).
- ✅ Pas de Chrome headless ([[feedback_no_headless_chrome]]).
- ✅ Frontend HMR (pas de rebuild pour les .tsx).

## Commits produits (16)
48e2d12 Docs+API+Core+Mermaid · 055e44d @Options/@Head/@All + fix 404 régression · 8fb4b15/5be6456 runner tsx · e19f038/c74c0c0 coverage vitest+lcov · e69a5a5/22a24dc onglet Tests (async) · 9a4a745/bef7345/b5e557c/d4e1a9a/f8e0666/1543406 dashboard UX + deps versions/outdated + sécu path/config + catégories.
