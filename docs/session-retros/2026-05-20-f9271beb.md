---
date: 2026-05-20
session_id: f9271beb-bcad-4f3d-a41a-8114204b885f
focus: Kit docs modules Studio (onglets Docs+API) + carte Core + schémas Mermaid + fix 404 deep-link SPA
---

# Session retro — 2026-05-20 — f9271beb

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Bash | 71 |
| Edit | 61 |
| Read | 40 |
| Write | 9 |
| TaskUpdate | 9 |
| TaskCreate | 5 |
| Skill | 4 |
| ToolSearch | 2 |
| AskUserQuestion | 2 |

Transcript : 850 lignes, 3,0 MB. Tool results : 204 events, ~507k chars, avg 2484/event (sain).

## Top fichiers Read/Edit
| Fichier | Read | Edit/Write |
| ------- | ---: | ---: |
| studio/.../routes/ModuleDetail.tsx | 1 | 9 |
| framework/.../KernelAdminApi.ts | — | 9 |
| studio/MEMORY.md | 2 | 9 |
| framework/.../docsReader.ts | — | 5 |
| framework/.../Route.ts | 3 | — |
| src/nodefony/docs/container.md | 2 | 3 |

Re-lectures faibles (max Route.ts ×3 pour confirmer wildcard/requirements) → lecture ciblée OK.

## Coûts évidents
- **Vérif navigateur via Chrome headless CDP → a BLOQUÉ la machine du user.** Approche rejetée explicitement. ~6 calls Bash + 2 scripts CDP gaspillés. Le rendu Mermaid était pourtant validé (svg, screenshot) avant le blocage.
- **3 cycles stop→build→start serveur** (protocole watch Rollup) — incompressible mais coûteux (~30s × 3). 5 `npm run build` au total.
- **Édition itérative ModuleDetail.tsx** (9 Edit + réécritures Write) : 4 vagues de demandes UX successives (fluidité → onglets vides → pleine hauteur → fullscreen → mermaid). Chaque vague = re-build mental du composant.
- Investigation routing (Route.ts ×3, router.ts, routerDecorators.ts) pour le fix 404 — nécessaire (compréhension wildcard `*` magic + ordre Pass 1 + type RouteRequirements).

## Recommandations
1. **NE PAS lancer Chrome headless CDP sur cette machine** (bloque le user) → mémoire `feedback_no_headless_chrome`. Vérif frontend = curl transform Vite (compile) + curl endpoints backend + **demander au user de confirmer visuellement**.
2. Le pattern « valider backend via curl sur serveur lancé » a très bien marché (deep-link 200, data plane non masqué, docs/symbols) → continuer, c'est la bonne granularité de preuve sans navigateur.
3. Demander **toutes les contraintes UX d'un coup** avant de coder un gros composant (ici 4 allers-retours sur ModuleDetail) — proposer un mini-cahier des charges (hauteur, onglets, fullscreen, schémas) en 1 question.

## Patterns récurrents (déjà gérés)
- ✅ Watch Rollup → stop avant édition backend (skill start-nodefony-server).
- ✅ Doc hybride ADR-0001 + git mv (mémoire `feedback_doc_placement`).
- ✅ Mermaid edge-labels forme `|"..."|` (consigné studio MEMORY).
- ✅ Deep-link 404 / fallback SPA profondeur (consigné studio CLAUDE+MEMORY).

## Commits produits
| Commit | Sujet |
| ------ | ----- |
| 48e2d12 | feat(studio): onglets Docs+API + carte Core + Mermaid + fix 404 deep-link (poussé sur claude-ts) |
