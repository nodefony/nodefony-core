---
date: 2026-08-05
session_id: c6904c78-2b2b-4f79-8672-6c34f16e921e
focus: Chantier perf pipeline HTTP — session MESURE (profilage runtime, zéro code touché)
---

# Session retro — 2026-08-05 — c6904c78 (6e du jour)

## Fait

- Baseline rejouée : **9 813 RPS** médian (9 341/9 813/9 933) — cohérente avec les 9 347 du 23-07.
- 2 cpuprofiles fenêtrés (60 s + 20 s) + `--trace-gc` + tick processor + sonde de comptage
  par requête (`--import`, compteurs recoupés wrk à 0,1 %) + bottom-up par appelant.
- Verdicts : en-têtes ≈13 % (levier n°1, sous-estimé 5× en statique) · GC RÉFUTÉ (0,93 %,
  3 instruments) · nonce CSP/req DÉCOUVERT (~1,7 %) · 3→2 `setTimeout`/req PROUVÉ causal ·
  listeners = 94 % node structurel · ~69 % du temps est natif (réduire les APPELS, pas le JS).
- 22 ancrages du rapport statique vérifiés par `@agent-nodefony-check-claims` (14 vrais,
  5 déplacés, 2 faux — dont « 3 gardes kernel » qui n'en est qu'une).
- Livrables : `tmp/perf-profilage-2026-08-05.html` (données embarquées) + kit
  `project_perf_pipeline_p2_kit` (lots A–D, fichier:ligne, gates, protocole).

## Frictions / leçons

- **Machine PAS calme au départ** : mongo idle = 64 % CPU (healthchecks) — Docker coupé sur ordre
  user. → RETEX.md 📏.
- **Conclusions publiées trop tôt** : le user a dû challenger (« tu es sûr ? contrôle au lieu
  d'inventer », « les websocket aussi ? ») pour obtenir fenêtrage boot, bottom-up des validations,
  config lue sur pièces, 2ᵉ profil, tick processor, et l'énoncé « WS non mesuré ». Instance de
  [[feedback_announce_gaps_first]] : les limites se déclarent AVANT la conclusion.
- `--cpu-prof` couvre le boot → fenêtrer (faux suspect `package_json_reader`). → RETEX.md 📏.
- Contre-épreuve causale par override `NF__…` = la preuve qui manquait au compteur. → RETEX.md 📏.
- Faux symbole macOS du tick processor : nouveau nom sous node 26 → mémoire
  `reference_perf_profiling_method` éditée en place.
- Sonde compteur : `new URL` aveugle aux imports `node:url` (limite énoncée au rapport) ; le
  `once` interne de node repasse par `on` (double comptage interprété, pas corrigé à la sonde).

## Commits produits

Aucun commit de code (session mesure pure, voulu). Repo : MIGRATION_STATUS (ligne chantier perf),
RETEX.md, ce retex. Mémoire IA : kit P2 + MAJ méthode profilage + état de reprise.
