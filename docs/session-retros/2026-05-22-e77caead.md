---
date: 2026-05-22
session_id: e77caead-c321-4ae2-8894-e608d2475897
focus: Studio frontend — UI kit + revue + gate tsc + bindings realtime nodefony/react + skill nodefony-studio-dev
---

# Session retro — 2026-05-22 — e77caead

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Edit | 74 |
| Bash | 55 |
| Read | 41 |
| Write | 24 |
| Skill | 3 |
| AskUserQuestion | 3 |

## Top fichiers Read/Edit
| Fichier | × |
| ------- | -: |
| routes/Dashboard.tsx | 10 |
| skills/nodefony-studio-dev/SKILL.md | 10 |
| stores/ConnectionStore.ts | 6 |
| routes/ModuleDetail.tsx | 6 |
| memory/project_studio_page_playbook.md | 6 |
| routes/System.tsx | 5 |
| client/react/index.ts | 4 |

## Coûts évidents
- **3 restarts serveur** (start.sh) après changements frontend/core — légitimes (vérif).
- **git `index.lock` résiduel** rencontré 3× → `rm -f` manuel (friction récurrente, process git crashé plus tôt).
- **`npx tsc` cwd parasite** (sandbox) → fallback binaire `node_modules/.bin/tsc` (désormais allowlisté).
- **SKILL.md édité 10×** : co-construction itérative (rename → IA → retex → W3C → sécu → frontière isomorphe → debug bar) au fil des demandes user empilées. Normal mais coûteux ; un mini-cahier des charges initial l'aurait condensé.
- **Coquilles ` ``` ` parasites** en fin de 2 Write (skill/mémoire) → corrigées après coup.
- **Amend git incomplet** (1×) : `git add` n'avait pas capté les éditions de contenu (lock) → re-add + re-amend + contrôle `git show HEAD:`.

## Recommandations
1. **Vérifier `git show HEAD:<file>`** après un `--amend` quand un lock a été retiré — le `git add` peut n'avoir staged que partiellement.
2. **`node_modules/.bin/tsc -p`** > `npx tsc` (cwd parasite) — allowlisté ce retex.
3. **Avant un Write de gros doc** : ne pas terminer par des fences ` ``` ` orphelines (relire la fin).
4. Pour un livrable co-construit en plusieurs allers-retours, **proposer le plan complet en 1 question** avant d'éditer N fois.

## Patterns récurrents (déjà gérés)
- ✅ Gate tsc frontend = `customConditions:["browser"]` (mémoire `project_studio_frontend_conventions`).
- ✅ Skill = foyer d'une méthode de dev (vs mémoire = rappel) ; source de vérité unique.
- ✅ Balayage allowlist auto à chaque retex (étape 8b).
- ✅ Frontière isomorphe : `nodefony` côté front = build client (jamais le serveur).

## Commits produits
| Commit | Sujet |
| ------ | ----- |
| 5dffad7 | feat(core): nodefony/react — bindings React temps réel + subscribe ref-compté |
| 4305ca8 | feat(studio): UI kit + refit pages + gate tsc + realtime sur nodefony/react |
| 8e40d0e | feat(skill): nodefony-studio-dev — kit IA dev Studio (oblig.) + retex/sécu/a11y + debug bar |
