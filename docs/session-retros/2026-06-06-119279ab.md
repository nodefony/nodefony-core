---
date: 2026-06-06
session_id: 119279ab-e0b6-41d0-a420-44c0bc882880
focus: Studio — Supervision migrée en bureau + taxonomie de tags + catalogue à facettes
---

# Session retro — 2026-06-06 (3e) — Supervision en bureau + tags + facettes

## Fait (front-only, branche refactor/studio-workspace, HMR 0 restart)

- 3 widgets de détail (`supervision.memory`/`handles`/`errors`) sur la sonde riche `dashboard:supervision`.
- Taxonomie de tags `workspace/tags.ts` (domaine hiérarchique + nature saisis ; capacités cluster-ready /
  temps-réel **dérivées**) ; 24 blocs tagués.
- Catalogue refait : 16/9 + plein écran, facettes (Chip.Group domaine/sous/nature + toggles capacités),
  recherche tolérante, aperçu LIVE au survol (`useBlockSource`+`BlockBody` lazy).
- `WorkspacePreset.layout?` (positions exactes) + templates Système/Config/Logs/Mémoire/Erreurs ;
  Supervision = layout exact (`ws_mq2fpy02`) ; `Superviseur` supprimé.
- Fixes : en-tête sticky UNIFIÉ (marginTop négatif tuait le sticky) ; `isolation:isolate` sur WidgetGrid
  (z des fenêtres) ; `returnFocus={false}` + `userSelect:none` (renommage menu).

## Frictions du jour (→ RETEX.md)

- Sticky qui défile = `marginTop` négatif sort de la zone sticky → copier la recette du frère (PageHeader).
- z-index enfants qui dépasse un voisin = stacking context manquant → `isolation:isolate` sur le conteneur.
- `Menu` Mantine rend le focus au trigger à la fermeture → `returnFocus={false}` pour un input ouvert depuis un Menu.Item.
- Malentendu « topbar » (PageHeader vs bandeau) : 2 tours perdus → lever le vocabulaire AVANT de coder.
- Classement : capacités DÉRIVÉES (jamais saisies) ; template fidèle = `layout` exporté du localStorage user.

## Coûts évidents

- ~2 allers-retours sur l'ambiguïté « topbar » (j'ai corrigé le mauvais élément 2×). Leçon : reformuler la cible.
- Itérations live pilotées par le user (JSON de bureau fourni 2×) — sain, peu coûteux (HMR).

## Commits produits

| Commit     | Sujet                                                                          |
| ---------- | ------------------------------------------------------------------------------ |
| (voir git) | feat(studio): Supervision en bureau + taxonomie de tags + catalogue à facettes |
