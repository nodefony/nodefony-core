---
date: 2026-08-09
session_id: e3fee379-0a64-4666-ac2a-2f987a423963
focus: P6.9 — l'IAuthenticator firewall pour jetons tiers (external-jwt), + e2e sur le fil
---

# Session retro — 2026-08-09 — e3fee379

## Fait

- **`eedd746e`** — `ExternalJwtAuthenticator` (zone `external-jwt`) : un jeton émis par un serveur
  d'autorisation tiers ouvre le pare-feu. `subjectPolicy: "require"` (défaut) | `"ephemeral"`.
- Contrat neuf **`IAuthenticator.validateArea?(area)`** — conditions d'emploi contrôlées au BOOT,
  sans que le pare-feu connaisse un seul nom en dur.
- **`peekIssuer`** (lecture non vérifiée bornée à 8 Ko) : `jwt` et `external-jwt` s'aiguillent par
  `iss` → l'ordre de la zone ne décide plus de rien.
- **`UnverifiableTokenError` (503)** + cas spécial dans `Firewall` (patron `ThrottledError`/429).
- Banc live `http/external-jwt.test.ts` (6) sur zone `test-external` du module test, émetteur
  `.invalid` (RFC 2606) → panne déterministe, aucun IdP à démarrer.

## Coûts évidents

- **4 débranchements + 2 rebuild/restart serveur** pour voir les gardes rouges (dont 2 sur le fil,
  qui exigent le cycle `turbo --force` → `stop` → `start`). Incompressible : c'est ce qui a prouvé
  les gardes. Le débranchement unitaire, lui, coûte 2 s.
- **Un typecheck faux** au départ (cwd dérivé) → relancé + prouvé par `--listFiles`. ~3 commandes.
- L'e2e n'était **pas prévu** : c'est le user qui l'a demandé. Il a rapporté le seul défaut réel de
  la session — donc le coût était du gain.

## Recommandations

1. **Quand un TSDoc affirme qu'une valeur ne fuite pas, écrire le test dans le même geste** — la
   phrase d'intention a survécu à 26 unitaires verts et n'est tombée qu'au banc live.
2. **Prouver la cible d'un outil avant de croire son verdict** (`tsc --listFiles | grep -c`).
3. **Pour une brique câblée dans un pipeline, l'e2e n'est pas optionnel** : le statut sur le fil
   (503 vs 401) est exactement ce qu'aucun unitaire ne peut établir.

## Patterns récurrents (déjà gérés)

- ✅ Un gate jamais vu rouge n'est pas un gate → 4 débranchements, 8 rouges ciblés, 0 résidu (`rg`).
- ✅ Suspecter son propre diff → suites relancées après chaque geste, y compris après le formateur.
- ✅ Question zéro : « est-ce couvert par les bancs ? » tranchée par `rg -c "Bearer"`, pas par un run.

## Commits produits

| Commit     | Sujet                                                    |
| ---------- | -------------------------------------------------------- |
| `eedd746e` | feat(security): un jeton émis ailleurs ouvre le pare-feu |
