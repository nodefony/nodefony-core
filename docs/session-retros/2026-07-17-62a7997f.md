---
date: 2026-07-17
session_id: 62a7997f-a84d-420b-bece-3dcd05fa4e0b
focus: Transactions Drizzle réparées sur les 3 dialectes — et 2 autres bugs de la même classe sortis dans la foulée (sonde ORM muette en prod, concurrence sqlite cassée)
---

# Session retro — 2026-07-17 — 62a7997f

## Fait

- **`transaction()` portée aux 3 dialectes** (`3dabb5ae`) — le bug #1 du dashboard. `#client`
  (better-sqlite3) était `null` en pg/mysql → `throw not connected` en PROD. Fix ≠ rustine : sur un
  pool, une transaction exige une connexion **dédiée** (`BEGIN` sur le pool = 0 atomicité + `BEGIN`
  orphelin recyclé) → `DrizzleTransaction` découplée du driver (`ITxDriver`
  exec/quoteIdent/release), fabrique par `#connectX`. 2ᵉ bug de dialecte trouvé par le banc :
  `SAVEPOINT "x"` = CHAÎNE en mysql (hors ANSI_QUOTES) → `quoteIdent`.
- **Sonde ORM routée par dialecte** (`e9faaa00`) — même angle mort : `probe()` lisait `#client` →
  `{}` en prod → **panneau Studio ORM muet**. Trouvé en répondant à « les tests sont suffisants ? ».
  Pool pg (API publique) / mysql2 (internes narrowés, aucune API publique).
- **Concurrence sqlite** (`789a0dba`) — le pire des 3 : 2 requêtes HTTP simultanées = 2 `BEGIN` sur
  la connexion unique → « cannot start a transaction within a transaction ». `#sqliteTxGate` = la
  file d'attente que le pool fait déjà en pg/mysql (mesuré : 15 tx simultanées sur pool 10 = 15/15).
- **Banc de contrat 19→26 cas** × sqlite + PG 17 + MariaDB 11.4 + MySQL 8.4 réels. Couvre désormais
  `IOrm` (dont test **anti-fuite de credential**) + les transactions + la **concurrence**.
- **Dashboard** (`af1e3ffd`, `ffdd7e34`) — dette #1 rayée ; ajoutées : `options.order` ignorée en
  silence, **9 check-then-act** des stores sécurité + leur cause racine (`IRepository` sans `$null`
  ni upsert conditionnel).

## Coûts évidents

- **Faux diagnostic « collation » (~15 min)** : test rouge sur MySQL 8.4 / vert sur MariaDB → j'ai
  accusé `utf8mb4_0900_ai_ci`. Vraie cause : mon `order: { age: "asc" }` (mal formé → tri ignoré) +
  PK UUID = ordre aléatoire = flake 50/50. **`npm run typecheck` le disait en 2 s** (TS2353).
- **`git stash push` échoué à mi-course** (index.lock husky) → tests dans `stash@{0}`, working tree
  nettoyé. Récupéré, mais 2 tours perdus.

## Recommandations

1. **Typecheck AVANT d'interpréter un test rouge** — la gate soldée le 07-14 ne sert que si on la
   lance (→ RETEX §« ce que le compilateur voit »).
2. **Matrice contrat × dialecte** pour chasser les angles morts — c'est elle qui a sorti `probe()`
   après `transaction()` (→ RETEX §« Écrire le test »).
3. **La concurrence ne se teste pas toute seule** : `Promise.allSettled` + tenir le travail ouvert,
   sinon les tâches se sérialisent et le bug ne sort jamais.

## Patterns récurrents (déjà gérés)

- ✅ `index.lock` husky → mémoire `feedback_git_index_lock` (variante `stash push` ajoutée).
- ✅ « Suspecter son propre diff » (devise CLAUDE.md) → appliqué 3× (test d'isolation, tri texte,
  option `order`) : c'était le test 3 fois sur 3, jamais le fix.
- ✅ « Un rapport d'audit ancre, il ne prouve pas » → les 2 agents check-then-act ont été
  re-vérifiés au code (put/denyJti/revoke/gc lus à la main).
- ✅ Preuve négative systématique (stash du fix → le banc rougit) sur les 3 bugs.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `3dabb5ae` | toute transaction levait « not connected » hors SQLite — en production |
| `af1e3ffd` | la dette #1 est soldée — et le diagnostic en a sorti une autre         |
| `e9faaa00` | le panneau ORM de Studio était plein en dev et vide en prod            |
| `789a0dba` | deux requêtes simultanées, et SQLite en refusait une                   |
| `ffdd7e34` | 9 courses dans les stores de sécurité — et le contrat qui les impose   |
