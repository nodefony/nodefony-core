---
date: 2026-05-20
session_id: 6ac4c458-0b54-4c97-b3de-19dd0f53d90c
focus: Fix ALS (BUG-001/002) + audit cycle de vie scopes DI → BUG-003/004 + séparation tests de charge
---

# Session retro — 2026-05-20 — 6ac4c458

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Bash | 86 |
| Edit | 64 |
| Read | 23 |
| Write | 9 |
| TaskUpdate | 9 |
| TaskCreate | 5 |
| ToolSearch / Skill / AskUserQuestion | 1 each |

Transcript : 844 lignes / 3.0M. Tool results : 200 events, ~209k chars, **avg 1043 chars/event** (bonne hygiène logs : sed ANSI + grep ciblés).

## Top fichiers Read/Edit/Write
| Fichier | × |
| ------- | -: |
| BUG_REPORT.md | 11 |
| lifecycle-als.test.ts | 7 |
| AlsController.ts | 7 |
| memory/* (MEMORY, bug004, als_ws_bug) | 15 cumulé |
| WebsocketContext.ts | 5 |
| http-kernel.ts | 4 |

## Coûts évidents
- **23× restart serveur** (start.sh) — inhérent : dev charge le dist au boot, chaque modif src = rebuild+restart. Skill conditionnel limite déjà le coût.
- **3× piège `cd` + start.sh** : après `cd src/packages/@nodefony/http`, `bash .claude/skills/.../start.sh` échoue (chemin relatif). Le working dir Bash persiste entre appels.
- **Heap inexploitable en dev** : `process.memoryUsage().heapUsed` ≈ 1.1 GB (Rollup watch in-process) → mesures delta bruitées. Pivot vers comptage direct `container.scopes` = la vraie preuve (12× sondé).
- **11× édition BUG_REPORT** : itérations de doc (open→résolu) — normal vu 4 bugs.

## Recommandations
1. **Mémoire friction `cd`/start.sh** — toujours lancer start.sh en chemin absolu OU `cd` racine avant. Évite 3 retries/session. → créée ce tour.
2. **Mémoire technique leak detection** — pour scopes/cycle de vie, compter `container.scopes` (route diag `/als-test/scopes`), pas le heap (pollué Rollup watch dev). Capturé dans [[project_als_ws_bug]].
3. **Tests delta > absolu** — assertions scope en delta (before/after) robustes au résidu inter-suites. Pattern à réutiliser.

## Patterns récurrents (déjà gérés)
- ✅ start.sh consolidé (skill), build conditionnel, spawn detached
- ✅ séparation tests charge/non-régression (.mocharc.load.json) — consigne user appliquée + mémoire
- ✅ tests-first (rouge avant fix) respecté pour BUG-001/002/003/004
- ✅ logs filtrés (sed ANSI + grep) — avg 1k chars/result

## Commits produits
| Commit | Sujet |
| ------ | ----- |
| 6a09cdf | BUG-001/002 ALS (AsyncResource.bind WS + onAfterResponse) |
| 72de86d | garde-fou cycle de vie ALS (dedup/cleanup) |
| 814f769 | BUG-003 leak scope WS erreur + séparation tests charge |
| 07414a6 | BUG-004 leak scope WS+session (onFinish saveSession) |
| f080cc4 | stabilise flaky memory "50 WS echo" (settle + seuil) |
