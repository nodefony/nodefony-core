---
date: 2026-05-23
session_id: a18122d3-52ec-4136-ad26-36a86d39e417
focus: Double ORM Sequelize (orm-core) + describeConnection + retrait legacy + AuditLog + doc + test multi-vendor
---

# Session retro — 2026-05-23 — a18122d3

## Tool usage

| Outil           |                                               Calls |
| --------------- | --------------------------------------------------: |
| Bash            |                                                  90 |
| Edit            |                                                  29 |
| Read            |                                                  21 |
| Write           |                                                  11 |
| Skill           |                                                   2 |
| AskUserQuestion | 1 (clarifiée, puis refusée → user préfère trancher) |

## Top fichiers Write/Edit

| Fichier                       |   × |
| ----------------------------- | --: |
| sequelize/.../SequelizeOrm.ts |   5 |
| index.ts (app)                |   5 |
| sequelize/index.ts            |   4 |
| modules/test/index.ts         |   4 |
| SequelizeService.ts           |   3 |

## Coûts évidents

- **19 cycles start/stop + 26 builds** : coût #1, dont une grosse part en **chasse au dist stale** (pas de logique).
- **Turbo resert un dist périmé** — frappé ≥3× : (1) `@services([orm,SequelizeService])` perdu → seul legacy ; (2) drizzle `describeConnection` sans `safeTarget` (chemin absolu fuité) ; (3) **même `npm run clean && npm run build` a resservi stale**. Cause finale : `npx rollup -c` **échoue par intermittence** à charger un `rollup.config.ts` (`export named 'Plugin'`, exit 1) → n'écrit rien → le dist turbo stale reste. Fix fiable = `npm run build` DU package (rimraf + `rollup --config rollup.config.ts --configPlugin typescript`).
- **Bug singleton orm-core** : entité enregistrée dans un module non-externalisant → `@nodefony/orm-core` bundlé en double → 2 instances du registre → entité invisible. Fix = ajouter `@nodefony/orm-core` aux `external` du module.
- **git index.lock résiduel** (husky/lint-staged) : `rm -f .git/index.lock` avant commit, répété ~4×.
- **Session très choppy** : nombreuses redirections user (Post→AuditLog→Post→AuditLog, place mediasoup) → plusieurs allers-retours implémentation/revert.

## Recommandations

1. **Skill/règle build** : quand un dist semble stale après refacto cross-module → `cd <pkg> && npm run build` (jamais `npx rollup -c` seul ; jamais se fier au turbo). À ajouter au skill `nodefony-framework-dev` (gate build).
2. **Pattern placement de test cross-module** : décider le HOME AVANT d'écrire (mediasoup=domaine drizzle, refusé). Confirmer scope quand le besoin touche plusieurs modules.
3. **Confirmer la cible AVANT de coder** quand le user explore (Post/AuditLog) → éviter implémente/revert. Mémoire : trancher placement+nommage en 1 question au lieu de coder puis défaire.

## Patterns récurrents (déjà gérés)

- ✅ Turbo cache stale → `clean && build` (mais ici insuffisant ! cf coût ci-dessus) — [[feedback_turbo_cache_stale_logs]] à enrichir.
- ✅ SÉCU info-leak FS : chemin relativisé + credentials rédactés dans describeConnection (rétro-fix drizzle).
- ✅ Config getter lazy (kernel deref) respecté.

## Commits produits

| Commit            | Sujet                                                                   |
| ----------------- | ----------------------------------------------------------------------- |
| 9aded67           | feat adapter Sequelize orm-core + describeConnection (14 tests)         |
| 3881228           | feat AuditLog Sequelize + retrait Post                                  |
| 5c0a2f1           | refactor legacy viré + config 1 bloc + AuditLog→module test + demo→user |
| 0a9b896           | docs tuto entité débutant (orm-core)                                    |
| 68c221e → b0366f9 | test multi-vendor ajouté (mediasoup) puis retiré (hors scope)           |
