---
date: 2026-06-10
session_id: 49aa68df-824b-4b03-97e3-61634474556c
focus: POC API souveraine — Phase 1 (socle convergence REST/WS)
---

# Session retro — 2026-06-10 — 49aa68df

## Focus

POC « API souveraine » : cadrage (6 phases minimales, décisions groupées Phase 6) + **Phase 1 livrée**
en branche `poc/api-souveraine` — prouver qu'une action écrite une fois est joignable REST **et** WS sans réécriture.

## Fait

- Cadrage : point archi, recentrage « le mieux est l'ennemi du bien » → périmètre minimal par phase ;
  branche **évolutive** (pas jetable) = briques de prod gardées, échafaudage jeté ; mediasoup → option Phase 6.
- Phase 1 (commit `71fcfe9`) :
  - `Router.resolve(ctx, cleanPathOverride?)` — route un path porté par un message WS (hot path inchangé).
  - `Resolver.executeAction()` — **seam** découplant « exécuter → valeur » de « rendre » ; `callController =
executeAction + rendu` (iso-comportement).
  - Cobaye jetable `src/modules/test/nodefony/poc/` (service mémoire + controller REST + pont WS-RPC + `proof.mjs`).
  - **Preuve** : `GET /poc/books/by-author/42` ≡ `socket {id,path}` → `result` identique.
- Gates : framework **190/190**, http intégration **419/420** (1 skip), **mémoire 9/9**, 0 régression.

## Frictions / leçons (→ RETEX.md)

- POC sur pipeline = scope imprévisible : « 1 ligne » annoncée → 2 briques (`executeAction` découvert en codant).
  C'est la valeur du POC, mais annoncer le scope comme provisoire.
- Client/preuve WS standalone = `WebSocket` **global natif** (Node ≥ 22), pas le package `ws` (non résolvable
  depuis `src/modules/*/nodefony/poc/`). API WHATWG (`addEventListener`, pas `.on`).
- `cd <module> && bash .claude/skills/.../start.sh` casse le chemin relatif du script (cwd persiste).

## Décision marquante

`callController` **n'est pas buggé** : il EXÉCUTE+REND par design (mono-transport). Le multi-transport impose de
**découpler** → `executeAction`. Couplage exécution/rendu = la vraie trouvaille archi de la Phase 1.

## Reste

Phases 2→6 (prochaine = **ResourceController**, risque héritage prototype). Cf `project_session_2026-06-10_state`.

## Commits produits

| Commit    | Sujet                                                                                 |
| --------- | ------------------------------------------------------------------------------------- |
| `71fcfe9` | feat(poc): socle API souveraine — convergence REST/WS (resolveByPath + executeAction) |
