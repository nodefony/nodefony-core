---
date: 2026-09-24
session_id: 1a4f2a5f-84a6-4af4-a068-b0119e9eb8f7
focus: bugs chauds ORM — journal perte/reprise, migrations d'un secondaire, connecteur publié, schéma refusé
---

# Retex 2026-09-24 — 1a4f2a5f

- **Fait** : `9659c2fb` fix(orm) #475 (ORM construit sans container → journal orphelin) ·
  `3d04e8dd` fix(drizzle) #473 (seul `default` possède les migrations ; refus
  `NF_MIGRATE_SECONDARY_CONNECTOR`) · `5703e527` feat(stores) #474 (`connector` publié) ·
  `21e32bc7` fix(orm) #461 (verdict `rejected`, compositeur unique) · `625796fc` fix(stores) brique
  `user` + invariant d'intégration + banc 42804 sur PostgreSQL réel. Tickets #475 #473 #474 #461 FERMÉS.
- **Frictions** : `user` oubliée par #474, vue seulement au serveur réel sur PG ; superviseur de dev
  qui rebâtit les `dist` pendant les tests (faux rouge, contre-épreuve sans morsure) — versées au
  sas sous « 🎯 Une règle vérifiée sur UN décor ».
- **Découvertes hors ticket** : #475 rendait muets aussi le CRITIC d'écart de schéma et l'audit
  d'index Mongoose ; #461 : `readCode` lisait le `code` numérique MongoDB avant `codeName` → aucun
  refus d'authentification MongoDB réel n'était reconnu.
- **Décisions** : migrations par connecteur NON supportées (le dossier décrit UNE base) — refuser
  plutôt qu'écrire au mauvais endroit ; le connecteur d'une brique se publie depuis `from(orm)`, seul
  point qui connaît l'ORM ; le message d'échec se rédige en UN endroit (`describeConnectFailure`).
