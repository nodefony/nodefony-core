---
date: 2026-06-13
session_id: 2137d18e (soir — suite de 662e0de9)
focus: P6 J3b Étapes 1+2 — fermer le data plane HTTP (aire + bypass + matchPath) + nettoyage sessionContext
---

# Session retro — 2026-06-13 (soir) — J3b Étapes 1+2

## Fait

- **J3b Étape 1 `f6f169c9`** : contrat de zone (`area.realtime`, `requireMfa`→false honnête,
  `studio.authenticators` retiré, tests pattern data plane vs inventaire réel).
- **J3b Étape 2 `7995f481`** : **data plane HTTP FERMÉ** — aire `nodefony-admin` portée par
  framework, `bypassFirewall` plumbing câblé (trou trouvé), `matchPath` source unique HTTP/WS,
  login/logout/me en bypass. Prouvé runtime (anonyme→401, login→200). Bancs : admin-dataplane
  réécrit (27), session-bff 7, memory 9, unit 87+263. Handshake WS fermé en bonus.
- **`sessionContext` retiré** (sur-ingénierie ; legacy `checkChangeContext` non porté = 0 conso ;
  isolation admin = RBAC P6.8, aligné OWASP/RFC 6265). Nouvelle mémoire `session_context_strategy_gap`.
- **Pédago** (le user apprend le vocabulaire) : MFA vs 2FA, hot/cold path, SPA, bucket=casier →
  lexique global + security. **Devise** « la confiance n'exclut pas le contrôle » en tête CLAUDE.md.

## Frictions / leçons (détail dans RETEX.md §Vérification)

- Un kit « béton » n'est pas le terrain : 3 ancrages périmés (bypass câblé en aval mais pas depuis
  les options → deadlock latent). Contrôler la CHAÎNE complète, pas les 2 bouts.
- Suspecter son diff (bug login partagé = mon `sessionContext`, pas l'existant) ; prouver par 1 test ciblé.
- Mesurer l'effet de bord avant commit (handshake WS fermé → api-souverain-bridge skippé).
- « Au cas où » = sur-ingénierie : faire le point des consommateurs + valider contre l'état de l'art.

## Coûts évidents

- Session TRÈS longue (énormément d'échanges pédago vocabulaire + 1 grosse découverte de design en
  cours de route → sessionContext ajouté Étape 1/2 puis retiré). Cache lourd. → END + /clear recommandé.
- 3 restarts serveur (preuves runtime + après revert) — justifiés (points de mesure).

## Reste — prochaine étape

1. **J3b Étape 3** — verrou WS frame (`api.request`/`subscribe`) : `SessionRealtimeAuthenticator` +
   `setFrameAuthorizer` + réécrire `api-souverain-bridge` (skippé). Voir `project_p6_j3b_plan_kit` §Étape 3.
2. J3b Étape 4 (security-review + preuves WS). Puis J4 JWT.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `f6f169c9` | feat(security) : contrat de zone J3b (realtime + défauts honnêtes) |
| `7995f481` | feat(security,framework) : J3b Étape 2 — data plane HTTP fermé     |
