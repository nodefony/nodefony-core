---
date: 2026-08-02
session_id: 3d75528e-4dd6-445c-b4b0-39c7c3e35881
focus: R1.2 (compose conditionnel au dialecte) livré, puis cadrage de l'unification de la pagination sur IPageQuery
---

# Session retro — 2026-08-02 — 3d75528e

## Fait

- **R1.2 livré** (`475e6d5f`) — question `database` + `--database <sqlite|postgres|mariadb|mysql>` ;
  le `compose.yaml` porte LA base retenue, sans `profiles:` ; `.env` porte l'URL active ;
  `DATABASE_PARAMS`/`resolveDatabase` = source unique service/port/URL des 3 gabarits.
  Nouveau `askWhen: {key, equals}` (condition sur une réponse précédente) appliqué par les TROIS
  fronts : moteur, récap CLI, formulaire Studio.
- **Cadrage pagination** — kit mémoire `project_pagination_unification_kit` gravé et poussé.

## Coûts évidents

- Le rendu eta s'est vérifié **trois fois** (deux allers-retours dus au recollement de lignes sur
  des gabarits non couverts par le contrôle de forme, limité aux `.ts`).
- Un typecheck Studio faussement rouge (dist du core périmé) a coûté un rebuild + une relance ;
  la chaîne `&&` masquait en plus le maillon front.

## Recommandations

1. **Étendre le contrôle anti-recollement eta aux gabarits non-`.ts`** (YAML, `.env`, Markdown) —
   la classe de défaut ne dépend pas de l'extension.
2. **Un gabarit LIVRÉ se relit comme du code** : ses commentaires partent chez l'utilisateur
   (cas vécu : affirmation fausse sur `parseEnv`).
3. **Recompter à l'automate tout relevé de sous-agent** avant de le répercuter (4 annoncés / 44
   réels) — déjà gradué en `feedback_inventory_needs_crosscheck`, appliqué ici avec succès.

## Patterns récurrents (déjà gérés)

- ✅ Gate vu ROUGE avant d'être cru (deux débranchements distincts sur R1.2).
- ✅ Preuve sur l'artefact REÇU (app générée par `bin/nodefony`, YAML relu au parseur).
- ✅ Délégation d'une liste d'affirmations à `nodefony-check-claims` en `haiku`.

## Commits produits

| Commit     | Sujet                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| `475e6d5f` | feat(scaffold): le compose ne porte plus trois bases quand l app en utilise une |
