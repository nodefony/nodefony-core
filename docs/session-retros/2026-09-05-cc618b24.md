---
date: 2026-09-05
session_id: cc618b24-c85d-4d60-a014-f89a1362ef44
focus: Gate de langue éprouvé par un banc, CI réparée (3 causes), mécanique d'attente unifiée, SPINNER retiré
---

# Session retro — 2026-09-05 — cc618b24

## Focus

Priorité 1 tenue (banc du gate de langue + extension `.mjs`/`.js`/bash), puis la
forge est passée au rouge et l'a emporté : trois causes distinctes, toutes
instruites par la preuve. Enfin la demande du user — une mécanique d'attente
générique — qui a fait tomber le défaut le plus intéressant de la journée.

## Fait — 11 commits, tous poussés

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `d10516bd` | banc du gate de langue — trouve un faux positif au 1ᵉʳ run          |
| `4cc0771c` | le dictionnaire est une DONNÉE → JSON (2 880 → 1 147 lignes)        |
| `082bc6db` | le gate lit le shell, avec son PROPRE automate                      |
| `13a1a96a` | 49 scripts npm rangés + `check:lang*` branchés                      |
| `9b69d00d` | 2 instruments accusaient à tort (décor de test, contrat figé)       |
| `f03fb8bd` | le scaffold n'installe plus depuis le registre en décor isolé       |
| `8131f60d` | `cli/progress.ts` — une seule mécanique d'attente, doctor s'en sert |
| `924600dd` | l'animation ne POUVAIT pas tourner — `spawnSync` bloquait la boucle |
| `a22ba0d6` | le second `spawnSync` figeait 30 s de plus                          |
| `7b87896b` | retrait de `SysLogSeverity.SPINNER` (+ doc `progression.md`)        |

## Coûts évidents

- **La CI a mangé la moitié de la session** — mais elle était rouge sur du
  travail d'hier soir jamais poussé, pas sur le travail du jour. Pousser plus
  tôt aurait coupé la boucle : 16 commits accumulés hors forge.
- **Trois allers-retours pour un même défaut d'animation** (spinner figé →
  `spawnSync` #1 → `spawnSync` #2). Le premier tour de boucle aurait pu chercher
  TOUS les appels bloquants au lieu du premier.
- **Un `$?` après un pipe** a rendu un faux verdict sur mon propre banc.

## Recommandations

1. **Pousser à chaque groupe de commits cohérent**, pas en fin de session : la
   forge est le seul juge de ce qui n'est pas éprouvable en local (4 plateformes,
   3 moteurs), et 16 commits en attente rendent l'instruction plus chère.
2. **Chercher tous les sites d'un défaut avant de corriger le premier** — la
   règle `--no-install` vivait dans 1 banc sur 3, et `spawnSync` dans 2 fonctions
   sur 2.
3. **Rejouer une suite sous `CI=true` avant de pousser** dès qu'un comportement
   dépend de l'environnement. A sauvé un push cette fois.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff — les 3 rouges venaient bien de nos commits.
- ✅ Prouver sur l'artefact REÇU — artefact CI téléchargé plutôt que log tronqué.
- ✅ Voir un test ROUGE avant de le croire — garde de corpus, routage shell,
  retrait de `SPINNER`.

## Ce qui n'a PAS été fait

- **`doctor --deep` s'arrête-t-il tout seul ?** Signalé par le user en clôture,
  NON tranché. Mesure lancée en fond, résultat non lu.
- Le typecheck du cœur exclut `src/tests` — ticket à ouvrir.
- Les 154 constats de langue de `scripts/` et 875 de `.claude/` (#187).
