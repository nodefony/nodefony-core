---
date: 2026-09-04
session_id: bc5e3d3f-4f5d-4a87-8b1b-29f7ea5df95a
focus: instruire deux rouges du banc a livré trois défauts produits — puis doctor est devenu une vraie commande
---

# Session retro — 2026-09-04 — bc5e3d3f

## Focus

Reprise sur l'arbitrage de la référence du banc, puis instruction des rouges 33 et 24.
Chacun a livré des défauts PRODUITS, pas des fautes d'agent. La session a dérivé — au bon
sens — vers `doctor`, qui gagne son nom, son rapport et son plan.

## Fait

- **Référence du banc enregistrée** depuis le run large (voie mixte tranchée par le user) :
  comparabilité rétablie à coût zéro, verdicts à `runs: 1`. Dette des 3 runs d'unanimité
  écrite et rendue EXIGIBLE par une étape neuve du mode END.
- **CI réparée** — `npm audit` ne rendait pas d'erreur, il PENDAIT 5 min par essai
  (défaut `fetch-timeout` de npm, mesuré) ; trois essais dépassaient la limite du job.
- **Tâche 33 instruite** → trois défauts produits fermés : le générateur écrivait un SQL
  inapplicable sans le dire, la doc prescrivait un remède faux pour un champ unique, deux
  refus prescrivaient un script inexistant chez leur destinataire.
- **#177** — `stateless` était un drapeau INERTE. Deux gardes posées, quinze affirmations
  recalées.
- **`doctor`** devient le nom principal (`check` en alias), gagne le contrôle de fraîcheur
  du build (#179), et un rapport conçu pour être lu en panne.
- **Tâche 24 instruite** → l'agent avait suivi le patron que le gabarit nommait
  « service partenaire ». Rejouée après correction : **PASS** (remontée à confirmer).

## Frictions

- Le code de sortie d'un commit lancé en arrière-plan ment — deux fois de suite.
- `sed \b` inopérant sur macOS, silencieusement.
- `this.timeout()` mocha → « no tests », qui ressemble à un succès.
- Un débranchement trop large ne prouve rien de précis : 1 cas sur 4 éprouvait vraiment la garde.
- Un `python3` heredoc resté bloqué 40 min en arrière-plan, découvert au `ps` de fin de session.

## Ce que la session a trouvé en PLUS de l'énoncé

- `stateless` : déclaré, validé, stocké, affiché dans Studio — **jamais lu**. `git log -S`
  le dit en dix secondes.
- Le récit des gabarits était faux SÉPARÉMENT du code : un « 401 intermittent » sans
  mécanisme, qui a orienté un agent du banc.
- Le hook du dépôt a refusé un commit pour « le cœur n'est pas construit » — exactement ce
  que le contrôle ajouté par ce même commit détecte, mais en amont.

## Commits produits

| Commit     | Sujet                                                     |
| ---------- | --------------------------------------------------------- |
| `eebe6f64` | référence du banc enregistrée + dette de nuit             |
| `f46a8762` | borner l'attente de `npm audit`                           |
| `9cf2f2f3` | dire ce qui ne peut pas s'appliquer sur une table peuplée |
| `f4e82396` | #177 — faire agir le drapeau `stateless`                  |
| `4a0168ca` | `doctor` nom principal, `check` alias                     |
| `df32fe8e` | #179 — voir le build en retard et le plancher Node        |
| `69307823` | un rapport de `doctor` qu'on lit en panne                 |
