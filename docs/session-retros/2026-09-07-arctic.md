---
date: 2026-09-07
session_id: 11648209-6d0c-480d-bb66-96ffdf192d74
focus: "#262 — retirer arctic et écrire la face cliente d'OAuth 2.0 ; audit fable ; passe de jalons"
---

# Session retro — 2026-09-07 — arctic

## Fait

- **#262 fermé** : `arctic` retiré, remplacé par RIEN. Trois fichiers neufs sans dépendance
  (`oauth2Client.ts`, `metadata.ts`, `httpJson.ts`), la découverte RFC 8414 remplaçant les ~50
  classes de fournisseurs par zéro. 59 tests OAuth, dont 30 neufs.
- **Audit `fable`** sur la conception : il a trouvé une faute réelle — j'avais REDÉCLARÉ la RFC 8414
  que le cœur porte déjà, et que `security/MEMORY.md:119` interdit explicitement de redéclarer. Les
  deux copies divergeaient sur quatre points. Factorisé.
- **Politique `iss` à trois états** (`issuerPolicy`), l'ID token contrôlé sur `iss`/`aud`/`exp`,
  et six durcissements (redirections refusées, lecture bornée pendant le flux, délais, single-flight,
  callback journalisé).
- **Grappe Keycloak ouverte** : #268 parent + #269 #270 #271 #272. Plus #273 (ouvrir les signatures
  avant gel des `exports`).
- **Passe de jalons sur la sémantique des préversions** : alpha = on construit · beta = gel
  fonctionnel · 10.0.0 = le geste de publication. 24 → 1 ticket dans `10.0.0`.
- **Deux outils du dépôt corrigés** : `ticket-open.mjs` lisait le tableau par une liste tronquée ;
  le serveur MCP n'était nommé nulle part, donc jamais appelé.

## Fait — seconde moitié (après le premier retex)

- **La vue Roadmap du tableau de bord était vide** : aucun champ de type DATE. Deux champs posés
  (`Début`, `Cible`), échéance de `10.1.0` au 15 décembre, et les 16 tickets de l'alpha datés en
  séquence depuis le 8 septembre. Ils finissent le **29** contre une échéance au **19** — huit
  jours ouvrés d'écart, constatés sans rien estimer.
- **`ticket-effort.mjs` refondu** pour répondre à « combien coûte vraiment un ticket ». Réponse
  mesurée sur 97 tickets fermés : **une séance, quelle que soit la taille estimée**. C'est le
  NOMBRE de tickets ouverts qui prédit le reste-à-faire, pas la somme de leurs jours.

## Coûts évidents

- **Quatre passes complètes de `test:all`** (~7 min chacune) : la première pour l'état, deux pour
  qualifier des rouges de contention, la dernière sur le code final. Deux auraient suffi si j'avais
  gardé la machine calme dès le départ.
- **J'ai fabriqué un faux rouge** en laissant mon serveur de vérification Studio sur 5151 pendant
  une passe : le banc navigateur de devkit est tombé dessus. Décor sale, verdict faux.
- Attente de l'audit `fable` (13 min) pendant laquelle je n'ai rien pu éditer des fichiers qu'il
  lisait — coût réel, mais l'audit a payé son prix en une seule trouvaille.

## Recommandations

1. **Graduer les deux thèmes mûrs du sas** (6 et 5 frictions) — travail de CONSOLIDATE.
2. **Arrêter tout serveur de vérification avant `test:all`** — trois faux rouges cette session,
   tous rejoués verts en isolé.
3. **Nommer un outil là où sa règle vit, sinon il n'existe pas** — vérifié deux fois aujourd'hui
   (le MCP, et le piège `item-list` que le skill documentait sans que son script l'applique).

## Patterns récurrents (déjà gérés)

- ✅ Décor sale → faux verdicts (`feedback_stale_decor_poisons_verdicts`) — repayé aujourd'hui.
- ✅ Prouver sur l'artefact REÇU (`feedback_prove_on_received_artifact`) — appliqué : `dist` et
  `node_modules` contrôlés, pas seulement les sources.
- ✅ Un gate qu'on n'a jamais vu échouer ne garde rien — 8 rouges provoqués puis rétablis.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `740c6d19` | fix(security) — retirer arctic, écrire la face cliente d'OAuth 2.0 |
| `72629d2b` | fix(pilotage) — cesser de lire le tableau par une liste tronquée   |
| `8d6913ca` | docs(inspect) — nommer le serveur MCP                              |
| `c44b5ef8` | docs(ticket) — porter les dates du tableau de bord                 |
| `7e4d5540` | feat(pilotage) — mesurer le travail d'un ticket en SÉANCES         |
