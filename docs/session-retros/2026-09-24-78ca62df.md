---
date: 2026-09-24
session_id: 78ca62df-be3e-4772-baf6-7759a6a1aedf
focus: CI « bizarre » (accrochages Windows), preuves de jetons en production, #471
---

# Retex 2026-09-24 — 78ca62df

- **Fait** : `2a7668f44` ci — preuves de jetons exigées en production (`NF_JWT_ISSUER` posé,
  audiences du banc sur `ctx.devModules`, `TOKEN_PROOFS`) ; `39cde50be` feat(cli) — champs d'une
  entité composés un par un (#471 fermé) ; `58fc3ae50` ci — accents graves exécutés par bash dans
  les commentaires du `node -e "…"`. Ticket #478 ouvert (clé de signature partagée entre pods, beta).
- **Frictions** :
  - Recensement des accrochages CI tronqué par `gh run list --limit 100` (s'arrêtait au 17/09) —
    famille déjà graduée « l'outil altère sa sortie ». Seconde passe nécessaire.
  - Enquête CI poussée au-delà de ce que le user voulait payer : il fallait proposer la décision
    (instrumenter ou non) dès le premier constat, pas après trois passes.
  - Explication du point #3 d'abord jargonneuse : le user a dû demander « pour les nuls ».
  - Mon commentaire YAML portait le défaut qu'il documentait (accents graves → commandes) — famille
    graduée « le remède porte le défaut ». Vu en lisant le journal de la CI, pas avant.
  - Preuves de jetons sautées en production depuis toujours → versé au sas (🎯 décor).
- **Ajout en fin de session** : `04e488fd7` — lanceur `scripts/ci/run-watched.mjs` (#479 ouvert jusqu'à ce qu'un journal nomme le détaché). Friction : deux remplacements `perl -pe s/…\s*$/` ont avalé un saut de ligne — `\s*$` mange le `\n` en mode ligne.
- **Décisions** :
  - Accrochages Windows : diagnostic consigné au kit CI (#12), remède (lanceur de surveillance)
    d'abord reporté (coût), puis FAIT sur demande du user en fin de session (#479).
  - #478 en beta, pas en alpha : l'alpha n'est pas déployée multi-pods ; la beta doit l'être.
  - #471 : `compose` déclaré par la SPEC (le dialogue ne décide rien) ; Studio garde sa copie,
    confrontée à l'écrivain du cœur.
