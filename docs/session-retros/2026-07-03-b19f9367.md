---
date: 2026-07-03
session_id: b19f9367-5232-445d-ae81-d6318e02b411
focus: Phase 0.5 release — ADR-0007 ClientKernel isomorphe (design) + volet code 10.0.0 (types + façade)
---

# Session retro — 2026-07-03 (après-midi) — b19f9367

## Fait

- **ADR-0007 ClientKernel isomorphe** rédigé et commité (`docs/adr/0007`) : contrat runtime
  client gelé avant publication — 11 décisions (périmètre infra-jamais-la-vue, contrat d'abord,
  pas de DI décorateurs client, façade named unique, lifecycle navigateur, opt-in strict,
  observabilité full-stack, sécurité d'identité structurelle, budgets bundle, convergence
  debug-client→Studio). Implémentation différée Phase 3.2.
- **Volet pré-release appliqué** : `IClientKernel.ts` publié types-only + façade
  `class Nodefony`/`export default` supprimée du barrel client + legacy `api/Storage` /
  `transport/websocket` sortis de la surface et du bundle.
- Gates : build clean 0 TS, core 1761 tests verts, typecheck Studio vert, bundle 49,4 KB gzip (−0,6).
- Journal release 0.5 clôturé.

## Frictions / coûts

- **Leçon déjà graduée non appliquée** (coût : 1 commit rejeté) : sujet commitlint commencé par
  « ADR-0007 » → rejet `subject-case` (upper-case). La règle « sujet en minuscule » est dans
  `feedback_commit_fr_apostrophes` (8×+) — la relire AVANT le premier commit d'une session, pas après le rejet.
- Vérif terrain pré-ADR très rentable (2 leçons → RETEX.md thème 🔎) : elle a fourni les
  arguments D4/D9 de l'ADR, pas seulement corrigé des ancrages.

## Recommandations

1. Phase 3.2 : suivre l'ordre D11 de l'ADR (ApiClient core → kernel → debug-client → Studio).
2. Gate `size-limit` : à câbler dans le chantier pipeline release (avec la bascule `exports.types`).
3. Suppression physique de `api/Storage.ts` + `transport/websocket.ts` : demander l'accord user
   (fichiers morts hors bundle depuis `8928c558`).

## Commits produits

| Commit     | Sujet                                                                                    |
| ---------- | ---------------------------------------------------------------------------------------- |
| `cb186179` | docs(adr): gel du contrat runtime client — ADR-0007 ClientKernel isomorphe (design only) |
| `8928c558` | feat(core): contrat IClientKernel types-only + façade client named-only                  |
| `45b6dc5a` | docs(release): journal — Phase 0.5 clôturée                                              |
