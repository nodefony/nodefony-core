---
date: 2026-06-26
session_id: 2f6506e1-dc07-4f08-8ef5-764328610154
focus: DrizzleIdempotencyStore (axe 3) + Slice 0 portabilité multi-dialecte Drizzle + quick-win activable
---

# Session retro — 2026-06-26 (soir) — 2f6506e1

## Fait

- **`cc8dd9ed` (POUSSÉ)** `DrizzleIdempotencyStore` — `IIdempotencyStore` SQL, `begin` atomique
  `ON CONFLICT DO UPDATE … WHERE expiré RETURNING` (= `SET NX PX` SQL), résolveur db lazy, approche B,
  12 tests SQLite.
- **`5bcd2d7f` (LOCAL)** Slice 0 **portabilité multi-dialecte** : `connector.dialect` + `DrizzleOrm`
  dialect-aware (driver `pg` lazy optionalDep) + factory `createIdempotencyTable(dialect)` + store
  `#table` injectable. **e2e PG cross-pod 7/7** (2 pods × 20 rounds × 10 begin → 1 fresh/round, 0 race).
  Service postgres docker-compose.
- **`6b596f90` (LOCAL)** quick-win : idempotence Drizzle **activable** (`NF_IDEMPOTENCY_STORE=drizzle`)
  - **fix ordre boot** (résolveur ORM lazy). Prouvé : log `Idempotency store → "drizzle" (distributed)`
  - banc userland 5/5.
- 2 CHANTIERS tracés (mémoire) : ORM multi-dialecte + config clarté (avec vision user + règle d'or).

## Leçons (→ RETEX.md)

- **Câblage de fabrique cross-module = PROUVER PAR UN BOOT, pas le typecheck.** L'ordre de boot
  (framework résout le store avant que l'ORM connecte) cassait silencieusement (fallback memory) ;
  le build était vert. La devise a évité de committer un trou. Fix = résolveur ORM lazy.
- **Drizzle dialect-spécifique ≠ Sequelize** : entités FRAMEWORK doivent être portables → factory par
  entité. User perte de confiance ORM → recadrage (périmètre fini, patron prouvé, comparatif à proposer).
- **Config : Zod = SEULE source des défauts** (security duplique `timeCost` 3 vs 2 → la confusion config).

## Décisions

- Garder `5bcd2d7f` + `6b596f90` LOCAUX (user). Chantier ORM en pause après P6.
- NEXT = **finir P6** (post-Studio GC session/store prefs, Webhooks P6.13, 2FA).

## Coûts évidents

- 2 boots dev complets pour prouver le quick-win (le 1er a révélé le bug d'ordre → justifié, pas gaspillé).

## Commits produits

| Commit   | Sujet                                                 | État   |
| -------- | ----------------------------------------------------- | ------ |
| cc8dd9ed | DrizzleIdempotencyStore (axe 3 P6.8)                  | poussé |
| 5bcd2d7f | portabilité multi-dialecte Drizzle (Slice 0) + e2e PG | local  |
| 6b596f90 | idempotence Drizzle activable + fix ordre boot        | local  |
