---
date: 2026-07-27
session_id: 701bb63d-9027-44f4-ba4f-1d03058c6ede
focus: Windows soldé, puis tout ce que la CI ne lançait pas — dialectes, socket distribuée, charge, preuves e2e
---

# Session retro — 2026-07-27 — 701bb63d (3ᵉ de la journée)

## Focus

Reprise sur le verdict CI du chantier Windows. Les deux derniers rouges soldés, le
chantier s'est déplacé de lui-même vers une question plus large : **qu'est-ce que
cette CI ne lance pas ?** Réponse : beaucoup, et rien ne le disait.

## Fait

- **Windows soldé** — `keyset.json` : le mode 600 est désormais CONSTATÉ après
  écriture et au chargement (NTFS l'ignore, comme un montage FAT ou NFS) ; le banc
  `Builder` copiait l'arbre de tests du dépôt, seul endroit portant des liens
  symboliques, et en boucle. **16/16 jobs verts** — premier run complet du chantier.
- **Dialectes de production** — un workflow filtré par chemin monte PostgreSQL,
  MariaDB et Redis en service containers : **183 + 183 cas** qui ne tournaient nulle
  part. Redis démarre par `docker run` (le mot-clé `services` ne sait pas passer
  `--requirepass`, exit 125 constaté).
- **Socket distribuée** — `RUN_CLUSTER_E2E` posé : fan-out cross-process (IPC) et
  cross-pod (backplane Redis) éprouvés à chaque changement.
- **Câblage du 429** — les deux cas rendus muets la veille sont rejoués sur un
  second décor armé, sélectionnés par nom, avec un filet qui exige le COMPTE.
- **Charge + gate mémoire** — `test:load` (7 fichiers : WebSocket, scopes DI,
  sessions, flux, heap) entre en CI avec `--expose-gc`.
- **Preuves e2e autonomes** — cluster realtime, sonde de pod, santé, surcharge de
  config par l'environnement : 4/4 vertes au premier run.
- **`buildDevStatus`** — deux appels système pour la même réponse. 5,5 s → 2,8 s.
- **Ancrages** — la règle « le modèle du sous-agent s'écrit dans le champ
  `description` » gravée dans le `CLAUDE.md` (je l'avais mise dans le prompt :
  invisible). Dette « les preuves e2e appartiennent à leur paquet » au
  `MIGRATION_STATUS`, après release.

## Frictions

- **Le vert par défaut est un silence** : 517 tests sautés sur 901, exit 0.
- **Mon filet comptait mal** → conclusion fausse (« 1 seul cas cross-pod » alors
  qu'il y en avait 7, dont la matrice F83). Un filet qui compte mal RENSEIGNE de
  travers, il ne se contente pas de laisser passer.
- **`--reporter=json` seul** efface la sortie lisible — reproduit **3×** dans la
  session, après l'avoir corrigé le matin sur `turbo --continue`.
- **Accuser un chemin sans chercher son jumeau** : j'ai désigné le crash sync
  alors que le crash `TypeError`, synchrone aussi, passait.
- **Une fiche d'inventaire non exécutée ment** (`idempotency-cluster` classé
  autonome, exige en fait le serveur dev).

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `d7530ce9` | mode 600 constaté, pas supposé                                 |
| `121617c8` | un test de copie qui copiait le dépôt éprouvait le dépôt       |
| `44bc3929` | le contrôle du 429 avait cessé de tourner                      |
| `22c3bc70` | 517 tests sautés sur 901, et la suite sortait verte            |
| `6c63ca9b` | redis a besoin de sa commande, pas d'une option docker         |
| `a1891c05` | la socket distribuée n'était éprouvée nulle part               |
| `922df226` | la règle mémoire du dépôt n'était appliquée par rien           |
| `b3355273` | un gate qui échoue sans pouvoir dire de combien                |
| `5a6c5b3f` | le rapport d'état demandait deux fois la même chose au système |
| `7ba43430` | le filet ne comptait qu'un banc Redis sur deux                 |
| `f733913f` | six bancs de charge stables qui ne tournaient nulle part       |
| `a7b138db` | des preuves qui assertaient sans que personne ne les lance     |
| `b118436d` | une fiche de banc qui n'avait jamais été exécutée              |

## Reste

- **`gateReporter` doit ÉCHOUER en CI**, pas seulement avertir — position du user :
  non lancé = rouge. Remplacerait les filets `jq` ad hoc.
- Cas 2 du backplane (contre-pression Redis réelle) : conçu, pas écrit.
- 4 preuves à décor opt-in ; MongoDB, Loki, OpenSearch jamais joints.
- `idempotency-cluster` et `graceful-shutdown` non prouvés en CI.
