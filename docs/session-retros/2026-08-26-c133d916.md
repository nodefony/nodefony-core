---
date: 2026-08-26
session_id: c133d916-ec34-4f3b-ac69-beed87be679e
focus: le soak acquittait à tort, quinze paquets ne distribuaient pas leur licence, et un taux de couverture qui ne dit pas ce qu'on croit
---

# Session retro — 2026-08-26 — c133d916

## Focus

Reprise sur la traque RSS. Le soak devait être rejoué machine libre ; il a surtout
révélé que **l'instrument acquittait le produit à tort**. De là, les dettes de
l'audit externe ont toutes été soldées, et la couverture a reçu sa commande — avec
ce qu'elle ne mesure pas, écrit noir sur blanc.

## Fait

- **`soak.mjs` ne peut plus rendre un faux « pas de fuite »** (`59a150a1`). Run coupé
  à la 37ᵉ fenêtre sur 180, deux heures pendu, verdict `"clean"` et exit 0 sur 17 %
  du travail commandé. Quatre trous fermés : verdict comparé à la durée DEMANDÉE,
  délais sur `wrk` et la sonde, sonde qui NOMME sa panne et réessaie, hyperviseur
  constaté. Vu rouge en débranchant pour de vrai.
- **La licence voyage enfin** (`81a5ee01`). Les quinze paquets déclaraient une licence
  qu'aucun tarball ne portait — `LICENSE.txt` vit à la racine du MONOREPO, hors de
  tout paquet. Constaté sur l'artefact reçu (`@nodefony/http` : 154 fichiers, zéro
  licence) ; quatre paquets sans champ `license` auraient affiché « UNLICENSED ».
  Garde dans le cœur pur, aux DEUX points d'entrée.
- **Dettes de l'audit externe : toutes soldées.** Revendication de marché du README
  reformulée (`72add44f`), politique de compatibilité écrite (`df52aebc`), licence
  prouvée. Vérifié en plus : C8 (le discours du README ne déborde plus) et C10 —
  que l'audit avait laissé « NON VÉRIFIÉ » — `check --json` code 0, `env --json`
  9 clés, `inspect` répond.
- **Dashboard corrigé** (`5668002f`) : il se contredisait sur le plateau RSS, citait
  107 tests là où il y en a 114. Passe d'automate sur ses 108 chemins : aucun
  mensonge, 15 abrégés.
- **`npm run coverage`** (`99b283ae`) : les 16 modules, avec le décor dérivé de
  `vitest.gates.ts` — et le comptage de ce que le taux SURESTIME (24 suites
  d'attaque) et SOUS-ESTIME (136 scripts de banc hors module). Le POURQUOI est en
  `docs/guides/integration-continue.md` § 6 bis (`9e13790f`), là où l'outil renvoyait
  déjà sans que la section existe.

## Frictions

- **Le premier instrument qui ACQUITTE à tort.** Huit précédents accusaient le
  produit ; celui-ci l'innocentait. Un faux vert ferme la question au lieu de la poser.
- **Mon débranchement n'a rien débranché** — `pkill -f` sur un process renommé, et un
  `;` au lieu d'un `&&` qui imprimait « serveur tué » quand même. Le gate est passé
  vert sans avoir été éprouvé.
- **J'ai attribué à Docker un écart de débit de −40 %**, puis ma propre mesure
  suivante l'a réfuté (11 000 rps hyperviseur allumé). Cause toujours inexpliquée —
  et dite comme telle.
- **`timeout` n'existe pas sur macOS** : code 127 lu comme « la commande n'existe pas ».
- **Un kit de mémoire m'a envoyé refaire une tâche finie depuis dix jours.**
- **Importer un SCRIPT l'exécute** : `test-all.ts` importé pour une fonction relançait
  l'infra, le build et la batterie entière.

## Commits produits

| Commit     | Sujet                                                                 |
| ---------- | --------------------------------------------------------------------- |
| `59a150a1` | soak : un run coupé au tiers rendait « pas de fuite » et sortait en 0 |
| `81a5ee01` | quinze paquets déclaraient une licence sans en distribuer le texte    |
| `72add44f` | README : une revendication qu'un seul contre-exemple démolissait      |
| `df52aebc` | politique de compatibilité — écrire ce qu'on tient vraiment           |
| `5668002f` | le dashboard se contredisait sur le plateau RSS                       |
| `99b283ae` | une commande de couverture pour les 16 modules                        |
| `9e13790f` | le renvoi de `npm run coverage` pointait une page muette              |
| `7190664b` | les rapports de couverture n'entrent pas dans le dépôt                |
