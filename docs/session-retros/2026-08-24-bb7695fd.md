---
date: 2026-08-24
session_id: bb7695fd-7374-457d-860f-c255e8138626
focus: Les deux trous CONSTANTS du banc devkit soldés, `nodefony/testing` publié, et la référence enfin figée (26/30)
---

# Session retro — 2026-08-24 — bb7695fd (08-24d)

## Fait

- **T4 « commande CLI » (0/3 → PASS 2/2)** — défaut du BANC : l'agent faisait un sans-faute
  (`create command`, zéro Write/Edit) et une sonde le recalait sur un **commentaire de notre propre
  gabarit**. La garde anti-commentaire tombait sur le `+` du diff. Matière dépouillée
  (`lignesDuDiff`, 5 sites, 1 implémentation), 4 motifs recalés, garde `refuserLesAncresDeDiff` au
  lancement, 141 échantillons du selftest remis en cohérence.
- **T6 « configuration par l'environnement » (0/3 → PASS 2/2)** — défaut PRODUIT : `nodefony env`
  accusait de faute de frappe les variables que le framework pose lui-même. `reservedEnv.ts` = 23
  variables, source unique, section dédiée dans le rapport, gate anti-dérive dans les deux sens.
- **T8** — le `--dry-run` rend enfin « ce que l'exécution dirait » et nomme le connecteur.
- **T24** — le gabarit dit comment AJOUTER une route à une zone fermée sans l'ouvrir.
- **T13 → `nodefony/testing`** (décision produit prise par le user) : `createTestModule()` publié,
  `create service` génère son test unitaire. La résolution PAR LE CONTENEUR est documentée comme
  structurellement hors de portée (`Nodefony.getKernel()` singleton), pas contournée.
- **Page `/performance/`** : le prix MESURÉ des garanties (équiper Express = −23,5 %), un
  calculateur pour le travail applicatif, et ce qui n'est PAS mesuré, dit comme tel. En ligne.
- **Référence figée** : 30 tâches, 26 PASS, 2 runs, unanimité, décor `MCP eteint`.

## Coûts évidents

- **Le banc coupé à 18 h** par un `403 Unable to verify organization membership` (fournisseur,
  transitoire) — une passe et demie perdue, ~1 h 40. Reprise par 2 passes neuves.
- **J'ai modifié des gabarits PENDANT la campagne** (T24 à 16 h 17, `create service` à 21 h) : la
  référence date de `af71b9fa` et le code a bougé depuis. Erreur méthodologique — un dépistage
  sera nécessaire sur T13 et T24.
- Deux commandes composées **refusées par un hook** ont fait conclure sur un état non acquis
  (un test jamais écrit, une sauvegarde jamais faite).

## Recommandations

1. **Ne jamais toucher aux gabarits pendant une campagne de référence** — la règle vaut pour tout
   ce que l'agent LIT, pas seulement l'`AGENTS.md`.
2. **Une assertion de chaîne sur un artefact généré ne prouve rien** — passer par le banc de vérité
   avant de dire « fait » sur un gabarit.
3. Après un refus de hook, **relire l'état** plutôt que supposer la première moitié passée.

## Patterns récurrents (déjà gérés)

- ✅ La sonde d'un banc est le premier suspect, pas l'agent qu'elle juge.
- ✅ Prouver sur l'artefact REÇU (tarballs) et non sur ce qu'on a écrit.
- ✅ Un gate qu'on n'a jamais vu échouer n'est pas un gate — tout a été débranché puis rebranché.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `62f5a4fd` | les deux trous CONSTANTS du banc — dont un que le banc se faisait à lui-même |
| `e07ee0e0` | les variables que le framework pose ne sont plus une accusation              |
| `f29f3957` | la zone protégée dit comment lui AJOUTER une route sans l'ouvrir             |
| `d1f9806f` | trois frictions du jour, versées sous des thèmes existants                   |
| `af71b9fa` | écarter la référence du 08-09, dont le décor n'existe plus                   |
| `d221b6dd` | `nodefony/testing` — éprouver un service SEUL                                |
| `0ebbc2a9` | le conteneur d'un module de test se CONSTATE                                 |
| `3e8205c3` | les 92 % disent le prix des GARANTIES                                        |
| `b186a401` | la RÉFÉRENCE est figée — 26/30, la tâche 6 redevient jugeable                |
