---
date: 2026-09-02
session_id: 5dcea8c7-4214-4199-8c51-417297232894
focus: Sécurité (Dependabot, code scanning, dépendances) — et le refus de #144 qui ne refusait rien
---

# Session retro — 2026-09-02 — 5dcea8c7

## Focus

Passe de sécurité demandée par le user : vulnérabilité Dependabot, mise à jour des dépendances,
analyse de code, état de la CI, merge dans `main` — plus le trou de preuve laissé par #144
(contrôlé en sqlite seulement, aucun test du chemin complet dans une application générée).

## Fait

- **Dependabot #704 fermée.** L'esbuild vulnérable venait d'un paquet MORT : `drizzle-kit` déclare
  encore `@esbuild-kit/esm-loader` sans qu'aucune ligne de son code ne le charge. Override **ciblé**
  sur le seul chemin fautif — un override global aurait fait redescendre les esbuild 0.28 de vite 8
  et `@angular/build`. `npm audit` : 0, toutes sévérités.
- **Dépendances montées** via `npm run deps:check` (l'inventaire du dépôt ; `npm outdated`
  sous-compte) : `@types/node` 26.4.1 sur 21 sites, `@xyflow/react` 12.11.6, `oxlint` 1.81.0.
  typescript 7 reste bloqué en amont par Angular — nommé, pas forcé.
- **6 alertes d'analyse de code fermées** : 3 par le premier merge (elles pointaient `main`, en
  retard de 65 commits), 3 corrigées — dont deux ReDoS _révélées_ par ce merge même.
- **#144 rouvert dans les faits, puis fermé pour de bon.** Le refus « au démarrage » était absorbé
  par la politique de résilience du kernel. Corrigé par `BootConfigurationError`.
- **#154 ouvert** : flake du décalage de port Vite sur agents macOS (3 rouges / 1 vert), pré-existant.
- **3 merges dans `main`** ; `main` = `claude-ts` = `e963d2a2`.

## Frictions (versées dans RETEX.md, 6 bullets, 0 thème neuf)

- Une même expression portait DEUX chemins quadratiques ; corriger l'un rouvre l'alerte sous un
  autre numéro. Le message de l'outil ÉNUMÈRE les cas — le lire.
- Un seul corpus hostile pour deux ambiguïtés : le banc reste vert sur la moitié non corrigée.
- Le refus de démarrage absorbé par le fail-soft du kernel — la frontière existait déjà au cœur.
- Deux fois, le banc n'a jamais atteint l'étape écrite (`--link` sans `better-sqlite3` ; le `build`
  d'une app démarre un kernel).
- Trois rouges lus comme « permanent », le quatrième vert — écrit dans un ticket, donc cru.
- Un TSDoc affirmait « rend un objet vide » ; elle LÈVE. Justification inventée, déjà recopiée.

## Preuves

- Banc du code généré : **18/18** en décor ISOLÉ (l'étape neuve comprise).
- Contrat de colonnes : 13/13 sur 3 dialectes, vu ROUGE deux fois (5 rouges sans le routage par
  dialecte, 3 sans le contrôle).
- ReDoS : deux cas vus ROUGES séparément (1779 ms et 1083 ms contre un budget de 1000).
- `bearer` : garde vue mordre sur l'ANCIEN motif après durcissement (×4,05 / ×4,02 / ×4,12).
- Suites `user` 204/204, `documentation` 71/71 ; CI verte sur les trois systèmes.

## Non fait, et su

- `@nodefony/mongoose` garde son accroche non fatale (`critical = false`, décision existante).
- Signalement upstream `npm/cli#9837` — demande l'accord du user avant publication sur un dépôt tiers.
- #154 ouvert (le flake Vite n'a pas été instruit, seulement qualifié et documenté).

## Commits produits

| Commit     | Sujet                                                                 |
| ---------- | --------------------------------------------------------------------- |
| `07507c93` | chore(deps): écarter la version d'esbuild signalée, monter trois pins |
| `4743e69f` | fix(user): faire REFUSER pour de vrai un utilisateur incomplet (#144) |
| `3fda1fb2` | fix(documentation): retirer deux expressions au temps quadratique     |
| `456cee3c` | test(security): débruiter la mesure de complexité du porteur          |
| `356d738f` | fix(documentation): fermer le SECOND chemin quadratique du même motif |
