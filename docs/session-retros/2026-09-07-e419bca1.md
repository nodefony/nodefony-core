---
date: 2026-09-07
session_id: e419bca1-45e3-4f1b-afa3-1d100a269795
focus: la grappe alpha soldée (#47 #264 #265 #267 #273) — plus trois défauts trouvés par le user, dont un blocage sans fin et un client OAuth que rien ne faisait passer sur un socket
---

# Session retro — 2026-09-07 — e419bca1

## Fait

- **#47** — l'accueil montre `npm create nodefony@alpha`, le `git clone` requalifié. Ticket laissé
  OUVERT : il se reporte aux crans beta et release.
- **#264 #265** — le doctor lit le CODE (nettoyage remonté à la lecture, onze lecteurs d'un coup) et
  reconnaît les entités que le générateur écrit (garde qui était MORTE).
- **#267** — le doctor s'affiche en fin de génération, après avoir été rendu véridique sur le
  frontend non construit.
- **#273** — les signatures du client OAuth passent en objets, `clientAuthMethod` déclarée. Rupture
  assumée, dans la fenêtre prévue pour ça.
- **Hors ticket** : un délai de garde sur les connexions ORM — une commande n'attend plus sans fin.
- **Hors ticket** : les gabarits de tests en anglais, deux `no-shadow` levés, l'import de feuille de
  style qui ne fait plus crier le linter.
- **#274 ouvert** : le gate de langue lit les `.tpl` et n'y voit pas les identifiants français.
- **Après le END, sur question du user** : les deux bancs live OAuth RETROUVÉS (j'avais affirmé
  qu'ils n'existaient pas) et lancés — 8/8, plus la suite d'intégration HTTP entière à 679. Puis le
  trou réel comblé : `oauth2ClientOverTheWire.test.ts`, qui fait passer le client sur un vrai
  socket au lieu d'un `fetch` injecté.

## Coûts évidents

- **Quatre générations d'application complètes** (avec `npm install`) pour éprouver le terrain — le
  poste le plus lourd de la séance, et le seul qui prouvait quelque chose.
- **Deux mesures refaites** parce que la première portait sur la mauvaise cible : les 200 du serveur
  du dépôt pris pour ceux de l'app générée, et le refus immédiat pris pour le blocage signalé.
- Le `pre-push` construit tout le dépôt : deux pushes = deux builds complets.

## Frictions (versées dans `RETEX.md`)

- 🕳️ **Quatre gardes rassurantes dans la même journée** — `check:lang` aveugle à un gabarit qu'il
  lit, « Entités et dialecte » exercé sur rien, « Fraîcheur du build » qui ignore le frontend,
  `tsc -p tsconfig.json` d'un paquet qui exclut ses tests. Toutes trouvées en DÉBRANCHANT.
- 🎭 **Un banc qui appelle la fonction ne prouve pas le BRANCHEMENT** — six cas verts, l'appel retiré,
  toujours six verts. Même défaut que la veille, une case plus loin.
- 📖 **Le user a redit la même chose parce que ma réponse ne répondait pas à sa question** — je
  mesurais un refus immédiat, il décrivait une attente sans fin.
- 🕳️ **J'ai affirmé une ABSENCE sur une recherche qui cherchait au mauvais endroit**, et je l'ai
  gravée dans un commit et une fermeture de ticket. Une absence se prouve par une commande qu'on
  montre, jamais par une recherche muette.
- 🎭 **Un test qui rejette pour la MAUVAISE raison passe pour un test** — mon cas « redirection
  refusée » restait vert avec la garde désarmée.

## Recommandations

1. **Publier l'alpha.3** — six correctifs de cette séance ne sont livrés à personne tant qu'elle
   n'est pas publiée, et le décor `alpha2-nodefony` du user continue de porter les défauts corrigés.
2. **#274 avant la suite** — un gate aveugle a déjà laissé passer un identifiant français dans une
   version publiée ; le prochain partira aussi.
3. **Le protocole de preuve tient** : voir rouge avant vert a attrapé les quatre gardes mortes de la
   journée. C'est ce qui a coûté le plus de temps, et c'est le seul poste qui a produit des faits.

## Patterns récurrents (déjà gérés)

- ✅ Prouver la CIBLE avant de croire le verdict (les 200 venaient du serveur du dépôt).
- ✅ Un gate qu'on n'a jamais vu échouer n'est pas un gate.
- ✅ Poser les variables d'infra avant de conclure « vert » (drizzle : 770 → 1449 verts).

## Commits produits

| Commit     | Sujet                                                                            |
| ---------- | -------------------------------------------------------------------------------- |
| `5ac00827` | docs(release): l'accueil montre la commande publiée, pas seulement le dépôt      |
| `a2677bd2` | fix(doctor): le contrôle lit le CODE, et reconnaît les entités du générateur     |
| `c6bcf875` | feat(cli): montrer l'état de l'application à la fin de sa génération             |
| `3735307a` | fix(orm): une commande n'attend jamais sans fin une base qui ne répond pas       |
| `2e3c1ca1` | fix(scaffold): les gabarits de tests s'écrivent en anglais                       |
| `a3d86639` | refactor(security)!: ouvrir les signatures du client OAuth avant qu'elles gèlent |
| `c4abc481` | docs(session): retex 09-07h                                                      |
| `bb4b0100` | test(security): éprouver le client OAuth sur un vrai socket                      |
