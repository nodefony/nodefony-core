---
date: 2026-07-25
session_id: b8b335e7-93c8-46ef-bf49-a7547d82f534
focus: SECURITY.md + audit BUG_REPORT + versions + banc devkit 9/9 joué + coût réel mesuré
---

# Session retro — 2026-07-25 — b8b335e7

## Fait

- **Banc devkit rejoué en entier** (`tmp/devkit-bench/2026-07-25T13-29-26`) — premier verdict
  honnête : **6 PASS / 3 FAIL**, 139 à 1 121 échanges par tâche (garde `runTask` validé). Les
  tâches 2 et 3, non rejouées depuis que l'`AGENTS.md` avait doublé, **passent** → pas de dilution.
- **`SECURITY.md`** (`8d992260`, corrigé `a226f974`) — canal privé, périmètre, délais solo.
- **`BUG_REPORT.md` 540 → 30 lignes** (`fe7558e8`) : 8/8 résolutions vérifiées AU CODE, un faux
  « OUVERT » retiré, ancrages périmés supprimés, fichier ramené à sa fonction de boîte de blocage.
- **Versions alignées** (`62a0560c`) : racine `10.0.0`, 5 maquettes IA en `0.0.0`.
- **`CLAUDE.md`** : devise dépouillée de son anecdote périmée (`5b87608c`), délégation sur la
  NATURE de la tâche (`5b87608c`), question zéro « faut-il un modèle ? » (`89339cf2`), et la règle
  d'hygiène adossée à sa mesure (`3e7baea3`).
- **Coût réel mesuré** — `scripts/session-cost.mjs` : 32 682 réponses, juin + juillet, ventilation
  par mois et par modèle. **72 % relecture de contexte / 10 % production.**
- **Historique scanné** (`gitleaks`, 2 020 commits) : aucun secret ; 44 résultats tous expliqués.
- **`docs/session-retros/README.md`** (`2f5d8a89`) : avertissement anti-« règles mortes ».

## Coûts évidents

- **Deux scans faux avant un bon.** `rg` sur `git log -p` a abandonné sur octet nul (« 0 hit »),
  puis le vrai scan a demandé `gitleaks`. Le contrôle de validité était gratuit et disponible.
- **Mes propres scans ont tué le banc** (exit 137) : `docker pull` + `gitleaks` à 663 % CPU pendant
  un run qu'on attendait. Le rapport avait été écrit avant le kill — mesure sauvée par chance.
- **`index.lock` résiduel** d'un hook interrompu → deux commits perdus, à refaire un par un.

## Recommandations

1. Brancher `gitleaks` en gate `pre-commit` **avec allowlist** (RFC bundlées, `tests/unit`) — sinon
   44 faux positifs le feront désactiver dans la semaine.
2. Corriger les 3 FAIL du banc par l'**interdit accroché au chemin** (« n'écris jamais dans
   `nodefony/command/` à la main »), pas par une ligne de plus dans un tableau.
3. Ne pas lancer de charge lourde pendant un run de banc.

## Patterns récurrents (déjà gérés)

- ✅ Vérifier au code plutôt que croire le journal (devise) — a payé 8 fois sur `BUG_REPORT`.
- ✅ Automate avant modèle (question zéro) — 44 résultats triés en deux `jq`, zéro token.

## Commits produits

| Commit     | Sujet                                                     |
| ---------- | --------------------------------------------------------- |
| `8d992260` | `SECURITY.md` — par où signaler une faille                |
| `fe7558e8` | `BUG_REPORT` — un journal clos cachait un faux bug ouvert |
| `5b87608c` | devise + délégation sur la nature de la tâche             |
| `62a0560c` | versions racine et maquettes IA                           |
| `a226f974` | le canal « recommandé » n'existe pas sur un dépôt privé   |
| `89339cf2` | question zéro : chercher l'automate avant le modèle       |
| `3e7baea3` | la règle d'hygiène porte le chiffre qui la motive         |
| `2f5d8a89` | les retex disent qu'ils ne sont pas de la documentation   |
