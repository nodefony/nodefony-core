---
date: 2026-07-03
session_id: 0c2cba93-5b67-424e-b81f-0aa0befb6d66
focus: Phase 0.6 — volet DoS/limites realtime (F5 handshake, F6a caps, F2 plancher, F3/F9)
---

# Session retro — 2026-07-03 (soir 2) — 0c2cba93

## Focus

Finir le volet **DoS/limites** de la revue realtime 0.6 (suite de F4/F7/F1 du même jour).
Banc de charge threat-first → fix → preuve → 3 modules touchés (http, realtime, security).

## Fait (4 commits)

- **F5** `870ddca6` (http) — handshake WS soumis au rate-limit IP (même compteur que HTTP,
  IP forwarded-aware, close 1013 avant scope/ALS). Banc versionné `ws-handshake-ratelimit-e2e.mjs`.
- **F6a + F9** `02406ac9` (realtime) — cap canaux/connexion (256 configurable, `null`=illimité,
  refus `realtime:denied`) + `slowConsumer.bytes` (clé morte) câblée à la sonde.
- **F2 + F3** `2db12d35` (security) — plancher irréductible `authenticated` sur namespaces
  réservés (config ne peut plus ouvrir `security:`/`syslog:` à l'anonyme) + docstring corrigé.
- Journal release `3b46f1ef`. **F6b rayé** (design), **F6c** reste (optionnel).

## LA décision structurante

**F6b (throttle messages/connexion) ABANDONNÉ sur pushback user.** Un socket authentifié qui
pousse fort = la North Star, pas une attaque. La défense DoS realtime est au **périmètre**
(handshake F5 + cap connexions/IP), jamais au débit d'un socket établi. Mon banc AVANT-fix
(200 sockets anonymes → p95 HTTP 57 s) a prouvé le point du user : le flood ne marche que
parce que le périmètre est ouvert.

## Frictions

- `rg -rn` = REPLACE (pas récursif) — refait 2× cette session malgré la note RETEX (→ ≥3×,
  graduation `feedback_*` due).
- Kit ancrages : `frameAuthorizer.ts` était dans **security**, pas realtime (kit disait realtime).
  Devise confirmée (kit ≠ code) — vérifié avant d'éditer.
- F9 clé de config morte (`slowConsumer.bytes` ne pilotait rien) = famille du knob `watch`.
- Downgrade safeguard Fable→Opus en pleine session sur du défensif pur (faux positif).
- Erreur de date (07-04 au lieu de 07-03) rattrapée avant push.

## Gates

http unit 530 · intégration 560 · memory 9/9 · realtime 312 · security 781 · build 0 TS.

## Commits

| Commit   | Sujet                                               |
| -------- | --------------------------------------------------- |
| 870ddca6 | fix(http) rate-limit handshake WS (F5)              |
| 02406ac9 | feat(realtime) cap canaux + slowConsumer (F6a/F9)   |
| 2db12d35 | fix(security) plancher réservés + docstring (F2/F3) |
| 3b46f1ef | docs(release) journal 0.6 volet DoS                 |
