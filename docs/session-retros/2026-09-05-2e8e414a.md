---
date: 2026-09-05
session_id: 2e8e414a-1712-4ef4-8bb6-ba6f799735f5
focus: instruire les rouges d'une nuit de banc — 7 faux sur 11 — puis solder doctor (renommage, framework construit, --deep)
---

# Session retro — 2026-09-05 — 2e8e414a

## Fait

- **Banc de nuit lancé puis ARRÊTÉ à mi-course** (82 passes sur 99) : le décor était faux
  pour toutes les tâches, pas seulement pour celles qui rougissaient. Arrêté avant que
  `--enregistrer-reference` ne grave une mesure inutilisable ; `baseline.json` intacte.
- **11 rouges instruits, 7 FAUX** — aucun imputable à l'agent mesuré.
- **9 commits**, tous avec suite verte et preuve sur l'artefact.
- **4 tickets ouverts** (#197 parent, #198 inventaire, #199 welcome filtré, #200 e2e du welcome),
  **#187 mis à jour** avec un chiffre mesuré.

## Frictions

- **La brique éprouvée, la chaîne jamais — commis le jour même où je l'écrivais dans un commit.**
  Le reporter de progression de `doctor --deep` : écrit, testé, branché nulle part. Aucun test
  ne peut voir qu'un paramètre optionnel n'est jamais fourni. Seul `stderr` à 0 octet l'a dit.
- **Suspecter son propre diff avant de dire « pré-existant ».** Neuf manquements de surface npm
  sont réapparus sur un commit qui ne touchait aucun des manifestes accusés : renommer la clé
  `nodefony.check` avait fait perdre à `check-package-deps.mjs` ses exceptions. Son commentaire
  annonçait « source UNIQUE » alors qu'il y a deux lecteurs.
- **`for f in $VAR` ne découpe pas en zsh** : un renommage sur 26 fichiers n'a touché aucun
  fichier, et la seule trace était un `File name too long` noyé dans la sortie.
- **Se placer dans le dossier d'un artefact fait mesurer une autre version** : le binaire a
  délégué au `nodefony` installé là-bas, qui annonçait encore un alias supprimé.
- **`npx vitest run` à la racine** : 165 fichiers rouges d'un coup, mode non supporté — rappel
  déjà noté au RESUME du matin, refait quand même.

## Ce qui a débordé de la demande

- Un **menu proposant `check`** avec un test vert (il vérifiait la présence, pas la destination).
- Un **test qui polluait `var/last-boot-console.json`** du dépôt : le user a lu, dans son propre
  `doctor`, un boot en échec fabriqué par ma suite. Garde posée et prouvée par empreinte.
- Un **4ᵉ site d'émission** de `realtime:denied` côté serveur, et le **client qui jetait** le
  champ — trouvés par le banc e2e, invisibles à l'unitaire.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `103d688a` | `create app` : sautée et ratée sont deux états, pas un booléen   |
| `25283e81` | realtime : dire POURQUOI un canal est refusé, hors production    |
| `c4ee8bec` | banc : un COMPTE ne se prouve pas avec deux instruments          |
| `3fcdbc26` | retirer l'alias `check` — la commande s'appelle `doctor`         |
| `c3bc9b37` | doctor voit si le FRAMEWORK est construit                        |
| `8eb5f8f1` | `doctor --deep` — LANCER ce que le projet déclare                |
| `e85af488` | mesurer les identifiants français du code de production (#187)   |
| `741e2873` | `doctor --deep` annonce ce qu'il fait, et fait TOUT              |
| `515276ab` | nommer les gardes armées — un compte seul se lit comme un quitus |

## Non fait

- Rien n'est **poussé**.
- Le **banc n'a pas été relancé** avec le décor corrigé.
- `--deep` n'a jamais été mené à terme **sur ce dépôt** (il lance la suite entière) — prouvé sur
  un projet minimal et sur l'app témoin.
- Les **9 problèmes de surface npm** (types hors de `files`) restent ouverts : ils bloquent la
  publication, à rattacher à #175.
- Le script de langue ne traite **ni `.mjs`, ni `.js`, ni bash** — décidé pour la session suivante.
