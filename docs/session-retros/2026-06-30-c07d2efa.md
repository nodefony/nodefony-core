---
date: 2026-06-30
session_id: c07d2efa-8b67-4897-8a7f-1647d46cb940
focus: Phase 0.2 (bases nickel) + 3 bugs Studio (TraceView crash, trace vide, refonte ergo Documentation) + désamorçage Dependabot
---

# Session retro — 2026-06-30 — c07d2efa

## Focus

Reprise (RESUME) → **Phase 0.2 « bases nickel »** du plan release, PUIS le user a
enchaîné sur 3 bugs Studio vus en revue live, PUIS panique Dependabot « 145 ».

## Fait (6 commits poussés `claude-ts` `f811dc38..20aba051`)

- `cc2924d9` **chore(cleanup) Phase 0.2** : convention CLI (`network`→`http:network`,
  `certificates`→`http:certificates`, typo `Outated`→`Outdated`) · route 204
  `/nodefony/test/nocontent` + test RFC 9110 §6.4.1 dé-skippé (prouvé runtime) ·
  suppression `api-souverain-bridge.test.ts` (100% skipped, accord user) · 2
  `console.log` commentés retirés. Gates : build 19/19 · framework 96 · http 560.
- `491982be` **fix(studio) crash `useStore` TraceView** : `StoreContext` épinglé
  `globalThis` (HMR/multi-bundle dédoublaient le contexte). + `44568f1f` gotcha gravé.
- `9a0bbc3f` **fix(studio) trace vide** : recherche par requestId lit le journal
  PERSISTANT (`scope=trace` → driver `file` JSONL) au lieu du ring mémoire évincé +
  état vide honnête. Reproduit (flood>2000 : memory 0 / file 11).
- `17237300` **fix(studio) refonte ergo Documentation** : `DocLayout` flexbox 3
  colonnes largeur fixe + portail en `mode=container` (scroll indépendant). +
  `20aba051` skill documentation 1.3.0.

## Frictions / leçons (→ RETEX.md)

- **Dependabot 145 ≠ 145 vulns** (2× confirmé) : 93 dev/46 runtime, 3 micro-utils =
  52 % (×24/manifeste), déjà patchés. Méta : ne pas exécuter aveuglément une
  remédiation que J'AI proposée — vérifier le terrain d'abord (j'ai recommandé
  CONTRE mes propres `overrides`).
- **Bug « souvent vide » = reproduire l'éviction** (flood > capacité) bat le
  raisonnement « ça ne peut pas être l'éviction ».
- **`createContext` en dev = épingler `globalThis`** (sinon contexte dédoublé HMR).
- **Layout docs-site = container + flex largeur fixe** > page+sticky (fragile).

## Coûts évidents

- 2 restarts serveur (back framework modifié : 204 route + scope=trace). Front =
  HMR 0 restart (StoreContext, DocLayout). Flood ×2 (~450 curl) pour prouver l'éviction.

## Patterns récurrents (déjà gérés)

- ✅ Devise « confiance n'exclut pas le contrôle » : ancrages kit vérifiés au code
  (3 ancrages Phase 0.2 précisés avant édition) ; remédiation Dependabot réfutée par inspection.
- ✅ Curl authentifié admin (login `admin`/`secret` → cookie jar) pour prouver un data plane.

## Commits produits

| Commit   | Sujet                                                  |
| -------- | ------------------------------------------------------ |
| cc2924d9 | chore(cleanup): phase 0.2 bases nickel                 |
| 491982be | fix(studio): StoreContext globalThis (crash TraceView) |
| 44568f1f | docs(skill): studio-dev gotcha createContext           |
| 9a0bbc3f | fix(studio): trace scope=trace (journal persistant)    |
| 17237300 | fix(studio): refonte ergo Documentation (flexbox)      |
| 20aba051 | docs(skill): documentation 1.3.0                       |
