---
date: 2026-06-28
session_id: e51deeb9-8568-4f57-8466-f6b2187e643f
focus: Chantier config unifiée (ADR-0006) — 1 source + NF__* override Docker + provenance Studio
---

# Session retro — 2026-06-28 — e51deeb9

## Focus

Reprise → audit migration « vérité » → chantier config unifiée (ADR-0006, Slices 1-4).

## Fait

- Audit vérité migration (4 sous-agents) + resync dashboard 62→65 % (`ab299794`).
- **ADR-0006** config unifiée + guide enrichi (`7716c75a`).
- **S1** security : 1 source, bug `timeCost` 3/2 mort (`bb70cf14`).
- **S2** drizzle pilote fusion `schema.ts`→`config.ts` (`bd801677`) + skill `create-module` aligné (`ed9e1346`).
- **S3** `NF__<MODULE>__<CHEMIN>` + secrets `*_FILE` (override Docker, validé Zod) (`507ea243`+`13dc6fa2`).
- **S4** provenance Studio (badge défaut/app/env) (`525560c9`).
- 8 commits poussés `claude-ts`. Gates : core 1694 · framework 400 · security 765 · drizzle 95 · Studio tsc 0.

## Frictions (→ RETEX.md)

- Pédagogie convention `NF_`/`NF__` + jargon « churn/cosmétique » : ré-expliqués 2-3× → analogie+tableau+exemples dès la 1ère fois, anticiper la zone de recouvrement.
- Devise plan≠code : le kit disait « double-source partout », le code = security SEUL + `schema.ts` anti-cycle justifié → sonder avant un refacto large.
- `git commit && git push` combiné = timeout (push lent, digest Dependabot) → séparer.
- Preuve E2E config : `config:show` absent (dette doc), `--help` s'arrête avant validation → test d'intégration Kernel versionné.

## Décisions

- 2 couches env : catalogue `NF_` (nommé : secrets/logique/config app) vs générique `NF__` (adresse d'un champ de module, 0 code). Distinction = nb d'underscores.
- Fusion cosmétique : drizzle pilote OUI ; 6 modules sains NON (cycle + churn 0 bug) ; skill garantit le futur. `schema.ts`→`config.ts` (nœud bas, pas `define`).
- Provenance Studio re-dérivée à l'introspection (0 instrumentation du merge).

## Reste — prochaine étape

1. `NF__APP__*` (config app log/domain/servers) → config « impeccable ». Kit `project_config_nf_app_kit` (piège : validation AU `resolve()`). + EN PREMIER : test fail-closed `NF__*` modules.

## Commits produits

| Commit     | Sujet                                    |
| ---------- | ---------------------------------------- |
| `ab299794` | audit vérité + dashboard 62→65 %         |
| `7716c75a` | ADR-0006 config unifiée + guide          |
| `bb70cf14` | S1 security 1 source (bug timeCost mort) |
| `bd801677` | S2 drizzle fusion schema→config          |
| `ed9e1346` | skill create-module aligné ADR-0006      |
| `507ea243` | S3 NF__* + *_FILE                        |
| `13dc6fa2` | intégration Kernel NF__* + guide livré   |
| `525560c9` | S4 provenance Studio                     |
