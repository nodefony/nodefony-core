---
date: 2026-09-15
session_id: b328d7dc-3cae-49e2-a06a-9bb44aa749a0
focus: L'écart du banc ORM était une double instance de drizzle dans le camp témoin — pas du code
---

# Session retro — 2026-09-15 — b328d7dc

## Fait

- **La cause du +46 % trouvée, et ce n'est pas le code** : deux instances de `drizzle-orm` dans le
  process témoin. `is()` perd son chemin rapide sur chaque colonne de chaque ligne, sans une erreur
  (`entityKind` est un `Symbol.for`, le résultat reste juste). Rapport corrigé **145,9 % → 90,9 %**.
- **Biais mesuré camp contre lui-même** : +58,8 % (SQLite), **+83,4 %** (PostgreSQL).
  Le ×1,07 PostgreSQL est RETIRÉ — son témoin tournait à 54,5 % de son débit.
- **La thèse de l'escalier tombe** : 90,1 % sur route triviale, 90,9 % sur cycle applicatif.
  L'écart est stable à ~10 %, il ne « fond » pas.
- **Garde d'équité** posée dans les 3 camps ORM (identité, pas version), **vue mordre** (exit 2).
- **Soak 90 min** joué en décor propre → 🔴 **RSS +108,6 MB/h, R² 0,99, sans plateau**, tas plat.
  Le coût normalisé passe de **0,55 à 2,62 MB/million de requêtes** : **régression ×4,8** depuis août.
- **Verdict du banc corrigé** (`rssSuspect` entrait dans le calcul mais pas dans le verdict) et
  **trois affirmations en dur du rapport** remplacées par des lectures.
- Tickets : #402 et #397 **fermés** (comptes rendus complets), #403 et #398 **recalés**.
- CI : rouge de langue réparé, fastify sorti du périmètre, site rendu de nouveau.

## Coûts évidents

- **Deux lancements de banc ORM** (le premier inconclusif : série 1 refusée à 3,5 %, machine partie
  froide). Warmup doublé au second → 4 séries valides. ~50 min de machine, inévitables.
- **Un soak de 90 min relancé** après 10 min : Docker Desktop tournait (hyperviseur, 8 vCPU sur 12)
  et une faute de frappe dans `--out` créait un dossier hors dépôt. Les deux attrapés avant la fin.
- **Trois commits refusés par les hooks** avant de passer (imports inutilisés, script non cité,
  fiches de skill périmées). Chacun demandait un geste évident — mais lus seulement après l'échec.

## Recommandations

1. **Chercher la régression RSS par bissection** entre le 26 août et le 15 septembre — la grandeur
   à comparer est `rssMbPerMillionReq`, jamais les MB/h. Le kit `project_rss_leak_hunt_kit` porte
   la commande et le décor. C'est la prochaine session.
2. **Composer la liste des surfaces à relire après tout recalage de chiffre** — trois surfaces
   périmées trouvées par hasard dans la même session (hub, README du dossier, README racine).
   Un `rg` sur le CHIFFRE, pas une relecture de mémoire.
3. **Un gate de liens morts entre pages** manque : `doc:anchors` ne voit que les ancres
   `fichier:ligne`, et six liens morts ont survécu dans `docs/performance/README.md`.

## Patterns récurrents (déjà gérés)

- ✅ Serveur résiduel renommé (`nodefony server`, PPID 1) purgé par PORT avant mesure.
- ✅ Sortie de run capturée en entier puis filtrée ; délégation au-delà de ~2 000 lignes seulement.
- ✅ Garde neuve vue ROUGE avant d'être crue, avec preuve que le débranchement a eu lieu.

## Commits produits

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `7b8a944f` | rendre au camp témoin ORM l'instance de drizzle dont vient son schéma    |
| `8c8813fb` | retirer le renversement du banc ORM, qui était un défaut de mesure       |
| `3599435f` | mesurer le biais de la double instance, et retirer le ×1,07 qu'il fausse |
| `7640cc16` | rafraîchir l'empreinte du tableau après la fermeture de #402             |
| `1fdf8c6a` | écrire en anglais la variable locale du banc applicatif                  |
| `3fe215f6` | sortir Fastify du périmètre, et réparer la pancarte du dossier           |
| `4bd792f6` | faire entrer le RSS dans le verdict du soak, et faire LIRE le rapport    |
