---
date: 2026-07-12
session_id: 56265619-078b-417c-b02d-a60ff6df277e
focus: Studio statique prod (pattern générique module npm avec UI) + sécurité guidée du create app
---

# Session retro — 2026-07-12 (nuit) — 56265619

## Fait (11 commits `0a69c665`→`b366a168`)

- **Studio STATIQUE prod LIVRÉ** : molette `ui: auto|static|vite` + `PrebuiltUi`
  dans @nodefony/http (générique tout module npm avec UI — exigence user dès le
  design). Banc app `--link` production SANS Vite/@nodefony/frontend : page,
  asset, SPA fallback, login → 200, CSP strict. Studio = 1er consommateur.
- **Sécurité guidée** : zone `main` (^/api, session→anonymous) ACTIVE dans l'app
  générée + zone `secure` (^/api/secure, session seule) + démo home login/logout
  → « Bonjour admin » ; ligne **Firewall** au bilan de boot (tableau
  ZONE/MODULE/PATTERN/AUTH/ACCÈS, gabarit Process, demande user).
- **Fixes onboarding** : `create app` build l'app (1er `npm run dev` OK) ·
  @types/react manquants (typecheck app générée cassé depuis le début) ·
  warning WS StrictMode home react · favicon Studio · bilan Process = tableau
  de `nodefony status` (renderProcessTable partagé) · override module absent
  WARNING→INFO si source module.

## Frictions (→ RETEX.md, 4 bullets)

- Banc onboarding ≠ happy-path README (build manquant trouvé par le user).
- Gate typecheck app générée jamais lancée (@types manquants invisibles).
- Fixtures ≠ donnée réelle (`RegExp.source` échappé) — tests verts, réel cassé.
- Shape par le runtime, pas par un DTO cousin (`identifier` vs `username`).
- Récidive outillage : `rg -rln` utilisé 2× (leçon DÉJÀ graduée) — mémoire
  `feedback_rg_no_replace_flag` à renforcer.

## Commits

| Commit   | Sujet                                                         |
| -------- | ------------------------------------------------------------- |
| 0a69c665 | feat(http): PrebuiltUi + resolveUiDelivery                    |
| 49883d9f | fix(kernel): override module absent WARNING/INFO selon source |
| 0c124aef | feat(studio): molette ui + build publish statique             |
| bdec43ea | docs(ui-static): MEMORY/CLAUDE/skills                         |
| cdab1d1f | fix(cli): create app build l'app                              |
| 87ea9a67 | feat(dev): bilan Process = tableau status                     |
| df10c593 | feat(dev): ligne Firewall au bilan                            |
| 38aed3d1 | fix(dev): colonne MODULE + dé-échappement + aération          |
| 624b7225 | feat(scaffold): zone ^/api + fix WS + favicon                 |
| db3a7c06 | feat(scaffold): démo login/logout home                        |
| b366a168 | feat(scaffold): /api/secure/hello + fix identifier            |
