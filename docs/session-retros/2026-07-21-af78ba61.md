---
date: 2026-07-21
session_id: af78ba61-d152-440c-a95e-9d4d4cfb9ad9
focus: F83 — authentifier le bus realtime, prouvé sur un banc multi-applications réel
---

# Session retro — 2026-07-21 — af78ba61

## Fait

- **F83 (`44becb5a`)** — le backplane n'authentifiait rien. Deux couches : admission par canal à
  l'ingress du hub (générique, tous drivers) + sceau HMAC des transports partagés
  (`backplane.secret`, fail-closed strict). 12 tests d'attaque écrits **avant** le correctif.
- **Déclaration broadcast statique (`2369deb0`)** — `@RealtimeBroadcast` : les préfixes étaient
  déclarés au handshake d'un client, donc un pod publiant sans abonné local ne propageait rien.
  Trouvé par le banc, pas par la suite.
- **Skill `nodefony-multipod-bench` (`48c88b8b`)** — puis rendu **rejouable** (`23263631`) :
  `setup.sh` + `run.sh`, validés en détruisant le décor et en le remontant.
- **Studio (`23263631`)** — le bus devient lisible : canal effectif, scellement, messages refusés,
  chaque libellé avec sa fiche d'aide interpolée sur les valeurs live et un lien vers la doc.
  `NF_REALTIME_BACKPLANE_NAMESPACE` aligné sur le driver et le secret.

## Frictions

- **Le kit d'écarts périme** (règle déjà connue, re-vérifiée) : F83 décrivait le symptôme, pas
  l'asymétrie entrée/sortie qui en était la cause.
- **Lancer N pods** : `nodefony development` est instance unique par racine → mode `production`.
  `pkill -f` sur une variable d'environnement ne matche rien (elle n'est pas dans la ligne de
  commande) → on croit avoir redémarré, et on diagnostique sur l'ancien code (404 trompeur).
- **Écran d'abord, jargon ensuite** : « Ingress refusés » n'a rien à faire sur un écran humain.
  Corrigé dans l'interface ET dans la prose de la doc.
- **Trop d'options proposées d'un coup** : le user a répondu « je comprends plus rien quoi faire ».
  Trancher et annoncer UNE action vaut mieux qu'un menu.

## Mesures du banc (2 pods + Redis conteneur)

- Latence bout-en-bout hors saturation : p50 **3 ms**, p95 4 ms.
- Charge : 500 connexions, **~180 k livraisons/s**, 0 perte réseau — le plafond est le fan-out WS.
- Sceau : **+3,9 µs/message**, dominé par `createHmac` ; la double sérialisation ne pesait que 8 %.
- Mémoire : 583 MB sous rafale → 152 MB au repos (pas de fuite, mais **file publisher non bornée**).

## Trous ouverts

1. File du publisher backplane sans borne (le pendant manquant de la back-pressure WS).
2. Plan S1 (fan-out mutualisé) — déclencheur désormais atteint.
3. F82 cas (2) et la suite du lot sécurité.

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `44becb5a` | authentifier l'ingress backplane (admission + sceau HMAC)   |
| `2369deb0` | déclaration statique des canaux broadcast + banc Redis réel |
| `48c88b8b` | skill `nodefony-multipod-bench`                             |
| `23263631` | namespace par env, bus visible dans Studio, banc rejouable  |
