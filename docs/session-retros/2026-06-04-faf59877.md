---
date: 2026-06-04
session_id: faf59877-b8d1-4477-a3d9-98b03f514e50
focus: Chantier config app COMPLET (5 lots) — .env conv B + deref + catalogue env + découpage domaine + Zod boot Kernel
---

# Session retro — 2026-06-04 — faf59877

## Focus

Chantier de refonte de la config de l'app (`./nodefony/config/`) — la « Priorité 1 » du
`_state` 2026-06-03 (« le foutoir complet »). Périmètre tranché avec le user : **config APP
seule** (dette Zod par module séquencée ailleurs : drizzle→ORM, frontend/studio→passes).
Mené en plan mode (5 lots cadrés), exécuté lot par lot, gate verte + commit à chaque.

## Fait — 5 lots, 5 commits poussés en local (working tree clean)

- `e575fdf` **LOT 1** : `.env` Convention B (Vite/Next) + 2 axes runtime/déploiement.
  `loadEnv({runtimeEnv,appEnv,cwd})`, cascade `*.local` > committés ; bin canonise NODE_ENV ;
  `.gitignore` → `.env` committé / `*.local` ignoré ; `.env.example`. Test CliIntegration
  verrouille le contrat dev-gating du help.
- `a58e6d5` **LOT 2** : `config.ts` sans deref kernel top-level (switch mort supprimé).
- `bb837c2` **LOT 3** : catalogue env typé `env.ts` (helpers envStr/Bool/Enum, 5 vars log).
- `e4f5edf` **LOT 4** : découpage par domaine (`app.ts`/`servers.ts`/`log.ts`) → config.ts
  index mince (262 → 95 lignes), forme de l'objet inchangée.
- `5df006c` **LOT 5** : validation Zod au boot Kernel. `schema.ts` (app) + `validateConfig`
  ré-exporté par `index.ts` ; `Kernel.loadApp` l'appelle avant `initializeLog`. Core zod-free.

## Frictions (→ RETEX.md mis à jour)

- **memory.test = serveur dev UP requis** (re-vécu, `[2×]`) : je l'ai STOPPÉ avant le test → 3
  fails `before all`. Les tests TAPENT le serveur, ne le spinnent pas. Proche graduation.
- **memory.test 1000-GET flake** (`[1×]`) : 36.4 vs 35 MB dans la suite, vert 2/2 en isolation.
  Diff boot-only → 0 impact per-requête. Isolation = vérité.
- **zod non bundlé malgré absent de `external`** (`[1×]`) : node-resolve l'a laissé externe
  (schema.js 3.5 KB), résolu au runtime via hoisting. Vérifier le dist, pas présumer.

## Décisions structurantes (→ \_state)

- **Défaut runtime = production** (révélé par le retrait de NODE_ENV de `.env`) : `nodefony --help`
  nu boote prod → commandes dev-only absentes. Aligné `resolveRuntimeEnv` (12-factor). Verrouillé
  par 2 tests (prod = mandatory only ; development = test:batch présent).
- **Coquille `_state` corrigée** : précédence `.env.local` vs `.env.<env>.local` → suit Vite/Next.
- **Validation config app au boot Kernel sans coupler le core à Zod** : convention « l'app exporte
  `validateConfig`, le Kernel l'exécute sur `app.options` ». Réutilisable par toute app tierce.

## Commits produits

| Commit  | Sujet                                             |
| ------- | ------------------------------------------------- |
| e575fdf | feat(core): convention .env B + axe APP_ENV       |
| a58e6d5 | refactor(config): supprime deref kernel top-level |
| bb837c2 | refactor(config): catalogue env typé (env.ts)     |
| e4f5edf | refactor(config): découpe config.ts par domaine   |
| 5df006c | feat(config): validation Zod au boot Kernel       |
