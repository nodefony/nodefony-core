---
date: 2026-07-18
session_id: f51e5c26-e158-4709-81dc-5d97b1c2a255
focus: Pagination = standard décliné store par store — Users + API Keys/PAT (6 backends) + fix bug SCAN Redis
---

# Session retro — 2026-07-18 — f51e5c26

## Focus

Décliner le standard de pagination (`IPage`/`IPageQuery` core) sur les stores, 2 briques :
**Users** puis **API Keys/PAT**. Un seul banc de contrat par store, cross-package, prouvé sur tous
les backends réels.

## Fait

- **Users** (`2763462c`) : `IUserRepository.listPage(IUserListQuery)` + `countActiveAdmins` natifs
  × InMemory / Drizzle (sqlite/pg/mariadb/mysql) / Mongoose. Filtres NON portables (role=containment
  JSON, q=substring insensible casse) → queryKit dialect-routé (SELECT ids paginé + COUNT) → recharge
  typée ré-ordonnée. `UserAdminApi GET users` → `listPage` (fin du `find()` O(N)) ; garde-fous lockout
  → `countActiveAdmins` natif. Banc UNIQUE `@nodefony/user/tests/support/userPaginationContract.ts`.
- **API Keys/PAT** (`21d6fef8`) : `ITokenStore.listPage(ITokenListQuery)` + `countTokens`. Filtres
  PORTABLES → helper `paginate()` orm-core (offset) côté SQL/Mongo, **curseur SCAN** côté Redis, slice
  mémoire. Façade `listPagePat`, `SecurityAdminApi GET apikeys` paginé serveur. Banc UNIQUE 2 modes
  (offset/curseur) `@nodefony/security/tests/support/tokenPaginationContract.ts`.
- **`fix(redis)`** : bug latent SCAN cursor `number`→`string` (node-redis v6 refuse un number à
  l'encodage) — présent aussi dans `listAll`, masqué par le FakeRedis typé number. Corrigé + **prouvé
  sur VRAI Redis** (docker, `REDIS_TEST_URL`). Banc redis en dual (fake par défaut + vrai gaté).

## Frictions (→ RETEX.md)

- **« un seul banc, pas un par dialecte »** (user, 2×) : banc chez le PROPRIÉTAIRE du contrat, importé
  cross-package, 1 caller/package (pas /dialecte). Pattern cross-package test-import validé.
- **« pas prouvable » Redis = sur-affirmation** : un test réel qui échoue révèle souvent un VRAI bug,
  pas une impossibilité. Ne jamais graver « dette/impossible » sans lire l'erreur exacte. Le fake trop
  permissif (typé number) masquait le bug.
- Infra docker était UP — vérifier `docker ps` + creds compose avant de conclure « pas d'infra ».

## Gates

Typecheck 5 packages verts · security 789 · drizzle 272 (+18 token pag 3 dialectes réels) · mongoose
110 · redis 46 (+ vrai Redis 4/4 curseur) · builds verts. 0 régression.

## Commits produits

| Commit   | Sujet                                                                             |
| -------- | --------------------------------------------------------------------------------- |
| 2763462c | feat(user,orm) pagination native Users — 6 backends, banc unique cross-package    |
| 21d6fef8 | feat(security,orm) pagination native API Keys/PAT + fix(redis) SCAN cursor string |
