---
date: 2026-05-30
session_id: 060c80da-bc7a-4242-8daa-b3089561fd8a
focus: Config Zod @nodefony/http (schema/builder/meta) + audit ÉTAPE 0 clés mortes
---

# Session retro — 2026-05-30 — 060c80da

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    57 |
| Edit            |    31 |
| Read            |    17 |
| Write           |     7 |
| Skill           |     2 |
| AskUserQuestion |     2 |

## Top fichiers Read/Edit

| Fichier                         |   × |
| ------------------------------- | --: |
| http/config/schema.ts           |  11 |
| http/config/defineHttpConfig.ts |   4 |
| http/index.ts                   |   4 |
| http/package.json               |   3 |
| http/config/configMeta.ts       |   3 |

## Coûts évidents

- **Build rollup module test → log dolibarr géant** : la ligne d'inputs (~400 entités) a craché ~2× dans le contexte (gotcha déjà connu `| tail -1`, oublié une fois). Pollue le cache.
- **Digressions design en cours de session** (proxy, HTTP/3, xml2js, meta Zod) : 4 questions hors-tâche → contexte rechargé à chaque tour. Riches en valeur mais coûteuses (cache read).
- **Turbo dist poisoning** (gotcha connu) : force-build module test → framework dist périmé (`Session` manquant) → 11 fails intég trompeurs. Fix = rebuild framework direct + leaf-last. ~3 cycles build/restart perdus.

## 💶 Coût (€)

- Total **~€49.9** (≈ $54.2) — **cache-dominé ~78 %** (cache read 23.8M tok = $35.8 / 66 % ; cache write 1h = 270k = $8.1). Output 131k = $9.9. 122 tours.
- Enseignement : session longue mono-module mais étirée par 4 digressions Q&A design → le cache read explose. Levier = `/clear` entre la tâche et les questions exploratoires, ou répondre puis reprendre dans un contexte plus mince.

## Recommandations

1. **Réflexe `| tail -1` sur tout build rollup** — la ligne d'inputs est énorme (entités dolibarr). Déjà en mémoire, à appliquer systématiquement.
2. **Digressions design = candidates `/clear`** : quand le user enchaîne des questions hors-scope (proxy/h3/xml2js), proposer de répondre puis `/clear` avant de reprendre le code (cache).
3. **Turbo poisoning** : ne PAS `--force-build` le module test si le souci n'est pas dans le module test → rebuild direct le module réellement modifié + ses consommateurs leaf-last. [[feedback_turbo_cache_stale_logs]]

## Patterns récurrents (déjà gérés)

- ✅ ÉTAPE 0 audit clés (vivante/morte/réservée) AVANT de schématiser — a évité d'enshriner 4 clés mortes + de retirer `watch`/`http3` réservés.
- ✅ Catch « allowedHosts redondant » : audit du code AVANT d'ajouter → mécanisme `domainAlias` existait déjà.
- ✅ memory.test flake en groupe vs vert isolé (native TypeError) — diagnostic isolation. [[feedback_load_tests_separation]]
- ✅ `.git/index.lock` orphelin → `scripts/safe-commit.sh`. [[feedback_git_index_lock]]

## Commits produits

| Commit  | Sujet                                                               |
| ------- | ------------------------------------------------------------------- |
| 2fef678 | feat(http): config validée par Zod (schema source de vérité) + meta |
