---
date: 2026-09-04
session_id: 46525d9b-ec62-46c3-becc-fb11ff500cae
focus: grappe #165 entière — cinq contrôles du banc qui affirmaient plus qu'ils ne mesuraient
---

# Session retro — 2026-09-04 — 46525d9b

## Fait

- **#165 fermé avec ses cinq enfants** (#166 #167 #168 #169 #170), 5 commits poussés sur `claude-ts`.
- Chaque enfant porte sa **preuve négative mesurée**, pas racontée : l'ancienne règle est rejouée
  dans le test à côté de la nouvelle, et la divergence est assertée.

## Coûts évidents

- **Le hook pre-commit dépasse 2 minutes** dès que le banc devkit est touché (il lance les 31
  auto-contrôles). Deux commits ont été tués par le délai d'attente de l'outil avant que je passe
  les suivants en arrière-plan. Le hook, lui, allait au bout : le commit existait.
- **Un commit rejeté par son propre gate** — prettier réécrit le `SKILL.md` pendant le pre-commit,
  ce qui périme les fiches régénérées juste avant. Formater d'abord, régénérer ensuite.
- Un serveur jouet **par cas de test** fait tomber la requête suivante en `ECONNRESET` (connexion
  persistante survivant à la fermeture) : un seul serveur, plusieurs états.

## Recommandations

1. **Recontrôler le motif d'une exclusion comme on recontrôle une ancre** — deux entrées `HORS_LOT`
   portaient un motif écrit, dont un faux ; personne ne les avait relues depuis leur écriture.
2. **Asserter les annotations d'un test, jamais les laisser décoratives** — le champ écrit d'après
   l'énoncé portait deux valeurs fausses que rien ne lisait.
3. **Rejouer l'ancienne règle DANS le test** quand on remplace une sonde : sans cette comparaison,
   on peut réécrire une collecte sans rien changer au verdict et croire avoir corrigé.

## Patterns récurrents (déjà gérés)

- ✅ La garde anti-`git checkout` du dépôt a mordu sur un arbre sale — retrait du témoin fait à la main.
- ✅ Le contrôle d'exhaustivité des causes (`imputation.selftest.mjs`) a exigé le classement de la
  cause neuve **le jour de sa naissance**.

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `9444fca8` | chore(skills): ne plus compter une mention comme un lancement  |
| `a035fba5` | test(devkit): juger l'envoi de fichier sur un fait, non un mot |
| `e790cf99` | test(devkit): trouver le témoin même hors de la première page  |
| `b3f0535e` | test(devkit): invalider la référence quand un juge change      |
| `74ec5845` | test(devkit): lancer les deux contrôles que rien n'exécute     |
