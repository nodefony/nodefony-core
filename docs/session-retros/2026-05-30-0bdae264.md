---
date: 2026-05-30
session_id: 0bdae264
focus: HTTP perf — fix bleed variablesMap, builder metaData monomorphe, bench dev/prod/cluster + cluster prod sans Redis
---

# Session retro — 2026-05-30 — 0bdae264

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |   101 |
| Read  |    33 |
| Edit  |    26 |
| Write |     5 |
| Skill |     3 |

## Top fichiers Read/Edit

| Fichier                               |   × |
| ------------------------------------- | --: |
| http/src/context/Context.ts           |   5 |
| memory/MEMORY.md                      |   4 |
| framework/src/Route.ts                |   3 |
| skills/nodefony-start-server/start.sh |   3 |
| http/src/context/metaData.ts          |   2 |

## Coûts évidents

- **3 features en 1 session** (fix variablesMap → builder monomorphe → bench prod/cluster) → contexte
  jamais purgé, 175 tours, cache relu en boucle. **Aurait dû être 3 sessions avec `/clear`.**
- **~10 cycles stop/start serveur** pour debug prod/cluster (Redis storm, dist stale) — 101 Bash dont
  une grosse part en boucle de diagnostic infra (Redis NOAUTH, backplane, trustedHosts, root dist).
- **Re-dérivation de contexte connu** : le user a dû dire « on a fait ça hier » — la procédure prod
  cluster (rebuild root, domain matching) existait déjà, j'ai re-fouillé au lieu de lire la mémoire.

## 💶 Coût (€)

- Total **~€101.5** (≈ $110) — cache write 1h $21.8 + cache read $65.8 = **79 % cache** ; output $22.2 ;
  input $0.6. Enseignement : **cache-dominé** sur 175 tours → le levier #1 = `/clear` entre features.
  Coût/livrable ~€34/commit (3 commits) — gonflé par le débogage infra non livrable.

## Recommandations

1. **Découper** : 1 feature = 1 session + `/clear` (vu 8×/8 en retex — ici flagrant, €101 d'un coup).
2. **Lire la mémoire AVANT de fouiller** : `project_prod_cluster_launch` aurait évité 30 min de
   re-dérivation Redis/dist/domain. RESUME au début même pour une « petite » exploration.
3. **`domainAlias` mort → supprimer** (dette §5 backlog) : a coûté du temps (j'ai cru que c'était le levier).

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin husky → `rm` + retry (vu, mémoire `feedback_git_index_lock`).
- ✅ Dist root périmé → rebuild root avant boot (`feedback_root_dist_stale_modules`) — mais PAS encore
  dans `start.sh` au début → corrigé cette session.
- ✅ commitlint header < 100 chars → raccourci (récurrent, à internaliser).

## Commits produits

| Commit  | Sujet                                                              |
| ------- | ------------------------------------------------------------------ |
| 0266cf6 | fix(routing): variablesMap partagé → bleed inter-requêtes + perf B |
| 02c32c2 | perf(http): setMetaData builder monomorphe + unit durci            |
| 9faa404 | chore(prod): cluster local sans Redis (IPC) + localhost trusté     |
