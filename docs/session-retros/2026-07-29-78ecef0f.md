---
date: 2026-07-29
session_id: 78ecef0f-327b-45c7-9a85-b37587a3d7e6
focus: deux tâches de banc pour deux angles morts du socle (consommer un service, servir un média), et trois gates remis d'aplomb
---

# Session retro — 2026-07-29 — 78ecef0f

## Focus

Donner leur tâche de banc à deux capacités livrées aveugles : `create service`
(la CONSOMMATION d'un service, jamais mesurée) et le streaming de fichiers (la
seule méthode de controller dont le contournement passe les tests). Plus trois
réparations d'outillage rencontrées en chemin.

## Fait

- **T13 « consommer un service depuis un autre composant »** — 4 sondes + 2 gates.
  Domaine TVA/facture choisi pour son vocabulaire contraint et sa distinction
  d'avec la remise de T10, qui partage le même décor. **Jouée pour de vrai :
  PASS 5/5**, `create service` appelé, deux services `@injectable`, dépendance
  obtenue par `container.get("taxRate")` — la bonne clé d'INSTANCE.
- **T14 « servir un gros média sans le charger en mémoire »** — le juge est une
  demande de morceau (`Range: bytes=0-99` → 206 + `Content-Range`), que le
  contournement `readFileSync` ne peut pas imiter.
- **Faux positif `skills:check`** (renvoi mort vers `lib/isolation.mjs`) : le
  motif n'acceptait pas les chemins à sous-dossier.
- **CI rouge sur `oxlint --deny-warnings`** : 3 alertes, toutes dans des scripts
  de skills — corrigées, pas muselées.
- `SKILL.md` du banc : il annonçait 9 tâches pour 13, puis 14.

## Coûts évidents

- Un inventaire délégué (formes de consommation d'un service) dont la moitié
  était un `rg` agrégé — refait à la main en parallèle, donc payé deux fois.
- Un banc de preuve monté faux (serveur témoin dans le process qui bloque sa
  propre boucle d'événements), puis un diagnostic erroné bâti dessus, puis le
  correctif de ce diagnostic à retirer. Trois allers-retours pour une preuve.

## Recommandations

1. **Toute preuve à N cas doit contenir au moins un cas attendu VERT.** Sans lui,
   « le juge mord » et « rien ne s'est passé » rendent le même verdict.
2. **Une sonde d'observation vaut un verdict.** T13 est PASS 5/5 et n'aurait rien
   appris sans le `👁` qui a montré que `@inject` n'est jamais trouvé.
3. **Le trou à combler maintenant** : aucun gabarit ne MONTRE un constructeur
   injecté (les 4 `@inject` des gabarits sont des commentaires). De la prose
   juste ne remplace pas un exemple de code.

## Patterns récurrents (déjà gérés)

- ✅ `timeout` n'existe pas sur macOS et sort en 0 — piège déjà consigné, revu ici.
- ✅ Décor isolé constaté avant l'agent (hors dépôt, tarballs, aucune source `.ts`
  atteignable) : les trois constats verts sur les deux runs.

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `08711e9b` | fix(skills) : chemin à sous-dossier déclaré mort à tort     |
| `c2810906` | feat(bench) : T13, la tâche qui manquait à `create service` |
| `ae49bce8` | fix(lint) : les 3 alertes qui faisaient échouer la CI       |
| `191516f4` | feat(bench) : T14, le streaming et sa demande de morceau    |
