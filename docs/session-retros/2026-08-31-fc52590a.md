---
date: 2026-08-31
session_id: fc52590a-270c-4342-b576-ca1ebde90e90
focus: fermer la grappe migration — #118 #121 #122 #123 ; #129 et #130 nés en route
---

# Session retro — 2026-08-31 — fc52590a

## Fait

Quatre tickets de la grappe migration fermés, deux ouverts.

| Ticket | Ce qui a changé                                                                                            |
| ------ | ---------------------------------------------------------------------------------------------------------- |
| #122   | Le refus « entité déclarée sans fichier » prouvé sur le MONTAGE — variante `+orphelin`, table NON exportée |
| #123   | Le refus ne propose plus un second geste qui ne produit rien — critère = la COUVERTURE, qui se constate    |
| #121   | La casse d'un nom rend le même verdict que le moteur ; banc neuf sur 3 moteurs                             |
| #118   | Fermé sur arbitrage : (a) le produit ne pousse plus à détruire ; (b) versé à #99                           |

Jalon 10.0.0 : **42 ouverts / 47 fermés**.

## Coûts évidents

- **Trois dérives de `cwd`** en une session : un hook a bloqué un `cd` relatif, un `-t` a rendu
  « no tests » depuis la racine, un `sed` a rendu « No such file ». Chaque fois, la commande
  suivante s'exécutait ailleurs sans le dire. Mémoire déjà graduée — le réflexe, lui, ne l'est pas.
- **Deux vérificateurs, deux périmètres** : `npx tsc --noEmit` dans le module passait, `tsgo` du
  gate de pré-envoi refusait (`TS2741`, champ `unique` manquant). Le push a échoué après ~4 min de
  build. Lancer `npm run typecheck` (celui du dépôt) plutôt que `tsc` du module aurait coupé court.
- **Le protocole de mesure coûte du temps mural, pas des tours** : 10 passes du banc = ~28 min, en
  arrière-plan. Bon usage — la session a continué pendant.

## Recommandations

1. **Avant de bâtir ce qu'un ticket prescrit, chercher qui le fournit déjà** — `rg` sur le CONCEPT,
   pas sur le nom de la chose à construire. #122 aurait coûté un espace de travail permanent.
2. **Un ticket sur un symptôme intermittent porte un TAUX, jamais une corrélation sur deux runs.**
   #130 a dû être recadré et renommé après mesure.
3. **Le gate qui fait foi est celui du dépôt** (`npm run typecheck`), pas celui du module.

## Patterns récurrents (déjà gérés)

- ✅ Gate vu rouge avant de conclure : **trois fois** cette session, chaque fois avec reconstruction
  du paquet (la commande s'exécute depuis `dist`).
- ✅ Preuve sur l'artefact reçu : `grep` dans `dist/` pour vérifier que le débranchement a EU LIEU.
- ✅ Suspecter son propre diff : le rouge MySQL a été qualifié (mysql seul → vert) avant d'être
  déclaré pré-existant.

## Commits produits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `fec9f06e` | test(orm): prouver le refus d'une entité qu'aucun fichier ne fournit          |
| `997e096b` | fix(orm): n'offrir le second geste que s'il produit quelque chose             |
| `4f6a252f` | fix(orm): rendre le même verdict sur la casse d'une table                     |
| `39da4955` | fix(test): compléter le type de colonne attendu par la comparaison de schéma  |
| `13b5f291` | chore(board): régénérer l'empreinte après la fermeture de la grappe migration |
| `6f556508` | docs(orm): graver la règle de casse, et recaler la carte des phases           |
