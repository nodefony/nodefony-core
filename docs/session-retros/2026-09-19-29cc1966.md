---
date: 2026-09-19
session_id: 29cc1966-7362-490c-9da6-4b0a2110d4d0
focus: le tableau de bord de la migration confronté au code, puis démonté — et un gate dont les deux tiers des rouges étaient impossibles à satisfaire
---

# Session retro — 2026-09-19 — 29cc1966

Deux tickets de pilotage enchaînés, #80 puis #384 qui en dépendait : rendre un verdict sur chaque
case de la feuille de route, puis l'archiver en promouvant ses décisions. Un audit extérieur
gouvernait les deux. Fini sur `doc:lint`, ramené de 53 rouges à 9 sans écrire une ligne de doc.

## Fait

- **#80 fermé** (`f3982195`) — les 52 cases ⬜/🔶 confrontées au code, chacune avec son verdict, son
  ancre dans `src/` et sa destination. **8 étaient fausses** : P5.12 (le store Redis s'enregistre
  seul depuis toujours), P6.10 (les journaux d'auth existent), P7.10 soldée (l'écran des migrations
  est livré), P7.7 et P13.2 (la **même tâche** écrite dans deux phases), 16.E.2 (la capacité existe
  hors skill), 16.E.1 caduque, plus P11.2/P11.3 partiellement. Cinq chiffres de P12 avaient dérivé.
  Trois cases décrivaient **un seul travail** (LB.3b, compté trois fois).
- **#384 fermé** (`5ad94b9d`) — `MIGRATION_STATUS.md` archivé sous `docs/archives/`, et ses trois
  décisions promues en ADR-0008 (ORM), 0009 (session hybride), 0010 (identité `IUser`), toutes
  autosuffisantes. 35 renvois recalés dans 24 fichiers ; 0 restant hors archives et retex.
- **Les 14 renvois privés des ADR antérieurs** remplacés par des sources du dépôt (`95a3de3d`), avec
  un gate neuf dans `corpusLinks.test.ts`, **vu rouge** par débranchement.
- **`doc:lint` 53 → 9** (`524c176d`) — un 5ᵉ régime pour les pages `generated:`, et `navTitle` émis
  par le générateur de fiches. Les 7 ADR et 4 index reçoivent le leur à la main.
- **#436 ouvert** — les 9 pages internes restantes, en backlog : le ticket demande de TRANCHER
  (standard / régime / sortie du corpus), pas de remplir.

## Coûts évidents

- **Trois sous-agents `haiku` butés sur `maxTurns: 40`** — aucun n'a rendu son rapport au premier
  tour, il a fallu les relancer un par un pour récupérer le partiel. `check-claims` est celui qui
  traite des LISTES et porte le plafond le plus bas des trois agents maison (40 contre 60).
- **Deux des trois verdicts `FAIT` rendus par les agents demandaient un recontrôle**, et l'un était
  un contresens franc : « 5 fichiers src manquants » sur des comptes purement DESCRIPTIFS.
- **Une ancre fausse dans mon propre ticket**, attrapée avant de poster (`docs/packages/…` pour
  `docs/architecture/…`).

## Recommandations

1. **Monter `maxTurns` de `nodefony-check-claims` à 80** — il est l'agent des listes et plafonne
   plus bas que les deux autres ; trois relances sur trois runs, c'est le plafond qui est faux.
2. **Graduer les deux thèmes mûrs** — « une méthode écrite et non imposée » (15) et « un contrôle
   qu'on ne peut pas satisfaire » (13), dus depuis sept sessions, et nourris de première main par
   celle-ci comme par la précédente.
3. **Avant de remplir ce qu'un gate réclame, regarder si le lot est HOMOGÈNE** — 34 fiches accusées
   du même manque n'étaient pas 34 fautes, mais un gabarit mal appliqué.

## Patterns récurrents (déjà gérés)

- ✅ La garde du dépôt a refusé mon `git checkout` sur arbre sale — à raison ; la sonde a été retirée
  par édition.
- ✅ Le gate `corpusLinks` a MORDU tout seul : l'archive gardait des liens écrits depuis la racine.

## Commits produits

| Commit     | Sujet                                                                       |
| ---------- | --------------------------------------------------------------------------- |
| `f3982195` | un verdict et une destination pour chaque case de la feuille de route (#80) |
| `f920b350` | empreinte après la fermeture de #80                                         |
| `5ad94b9d` | archiver le tableau de bord, promouvoir ses décisions en ADR (#384)         |
| `95a3de3d` | sortir des ADR les quatorze renvois vers une mémoire hors du dépôt          |
| `8c99cbda` | empreinte après la fermeture de #384                                        |
| `524c176d` | cesser d'exiger d'une page générée ce qu'aucun auteur ne peut y écrire      |
