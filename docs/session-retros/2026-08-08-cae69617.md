---
date: 2026-08-08
session_id: cae69617-11d9-45e3-8e05-943df13e16df
focus: Éprouver l'outillage devkit là où il n'avait jamais tourné — Windows, application générée, canal refusé — et le 405 de Studio
---

# Session retro — 2026-08-08 — cae69617

Cinquième session du 08-08, écrite APRÈS coup : la clôture a été oubliée, si bien que ces quatre
commits n'apparaissaient dans aucun `_state`. Elle solde la Priorité 1 des sessions 08-08 et 08-08d.

## Fait

- **`60ea39f8`** — `realtime:denied` EXERCÉ pour la première fois. En montant le décor (compte
  `user` contre `nodefony:syslog`), la mesure a rendu `admin` : **l'état d'authentification
  sauvegardé était repris quel que soit le compte DEMANDÉ**. Le fichier d'état porte désormais
  l'identifiant (`nomEtatAuth`, pure) — deux comptes gardent chacun leur session. Refus gravé en
  test à DEUX passes sur le MÊME canal. Windows : le gate laissait passer `&&`/`||` (refusés par
  PowerShell 5.1, le shell préinstallé) ; les quatre sondes publiées tournent maintenant dans le
  job `windows-latest` sur leurs chemins de refus (codes 64), sans serveur ni docker ni navigateur.
- **`64ea617b`** — budgets de temps explicites par cas. Un lancement coûte ~0,2 s ici et
  2,3 à 2,7 s sur l'exécuteur Windows ; le défaut vitest (5 s) laissait une marge de deux, et le
  seul cas enchaînant DEUX lancements a expiré à 5 041 ms.
- **`3b67b8a2`** — banc des sondes joué contre une application produite par `nodefony create app`.
  Le rouge n'accusait pas l'application : le scénario socket exigeait `api.request` (capacité du
  plan d'administration), hypothèse cachée dans une valeur par défaut. Pont désactivable +
  `NF_BROWSER_TEST_ACTION`. App générée 9 verts + 1 sauté (dit) ; ce dépôt 10 verts.
- **`98080f87`** — `httpMethodDecorator` ÉCRASAIT `requirements.methods` : `@Get(p, { requirements:
{ methods: ["WEBSOCKET"] } })` compilait, se lisait comme une déclaration, et disparaissait.
  Union désormais. `/nodefony/studio/api/stats` pontable ; `admin` sert (153 ms), `user` reçoit
  403 — le transport dit par où l'on entre, jamais qui a le droit d'entrer.

## Frictions

- **Un état sauvegardé sans IDENTITÉ répond pour quelqu'un d'autre** — on réclame une mesure sous
  un compte de moindre privilège, on obtient celle de l'administrateur, sans un mot.
- **Un budget de temps calibré sur mon poste tombe sur la plateforme la plus lente** — celle qu'on
  éprouve justement le moins souvent.
- **Une valeur par DÉFAUT cache une hypothèse** jusqu'au premier décor étranger : `api.request`
  supposé présent partout, alors qu'un contrôleur temps réel d'application ne l'expose pas.
- **Aucun script publié n'avait jamais été EXÉCUTÉ hors du conteneur** — donc toujours sous Linux,
  alors que la portabilité était « vérifiée » par lecture.
- **Un test « état forgé » était complaisant** : il passait sans que l'état soit jamais lu.
- **Une seule passe ne discrimine rien** sur un refus de canal — un canal fermé à tout le monde
  rendrait le même verdict ; deux passes gardent aussi le cloisonnement.

## Ce qui a coûté

- La clôture oubliée : quatre commits orphelins, `_state` 08-08d périmé, et une session suivante
  qui a dû reconstituer le contexte depuis `git log` et le transcript.

## Non couvert, et il faut le dire

- Les commandes `docker cp` / `docker exec` du skill restent non exécutées sous Windows réel.
- Deux applications GÉNÉRÉES lancées côte à côte (décor du repli de port Vite) : non rejoué.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `60ea39f8` | état d'authentification sans propriétaire + `&&` refusé sous Windows |
| `64ea617b` | budget de temps non portable                                         |
| `3b67b8a2` | banc dit paramétrable pour une app générée, à tort                   |
| `98080f87` | transport déclaré accepté puis jeté en silence                       |
