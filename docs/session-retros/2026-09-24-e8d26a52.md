---
date: 2026-09-24
session_id: e8d26a52-9321-431d-a145-2b0704134655
focus: #468 (Créer lit le registre ORM en mémoire) → trou #477 (base SQLite fantôme sur MongoDB) → matrice de décors en CI
---

# Retex 2026-09-24 — e8d26a52

- **Fait** : `f4f80d6b` #468 (Créer : connecteurs du registre en mémoire, présélection du porteur des stores, questions SQL par connecteur) · `05804b38` #477 (pas de `default` Drizzle sur MongoDB, pastille « défaut » = porteur des stores, entités orphelines NOMMÉES au boot + invariant d'intégration) · `d9e2232c` règle unique `devModulesLoaded` → `ctx.devModules` · `2a5f6e30` refus nommé d'un `await` de premier niveau dans une entité (drizzle-kit relit en CommonJS) · `3d515668` base `:memory:` → schéma dérivé en production · `2736b0dd` CI Windows (bases fermées avant `rmSync`) · `3439b35e` job `test-decors` (PostgreSQL · MariaDB · MongoDB, à chaque poussée).
- **Frictions** : deux rouges CI venaient de MES correctifs (`await` de premier niveau dans `User.ts`, puis `/readyz` 503 en production) — un correctif poussé sans rejouer le décor voisin rouvre la famille ; un débranchement jugé vert alors que `start.sh` ne rebâtit pas l'app racine (ancien `dist`), puis un vert faussé par le rechargement du DevSupervisor ; lecture statique de la config dans un data plane qui tourne DANS l'app (corrigé sur remarque du user) ; session très longue (relecture quadratique).
- **Décisions** : un data plane lit l'état EN MÉMOIRE, la relecture de config est réservée à ce qui ne démarre pas (le niveau de démarrage décide, pas le front) ; « défaut » est un RÔLE constaté, pas un nom ; pas de réécriture de `orm:generate` sur `drizzle-kit/api` (recopier un format tiers dériverait) — refus nommé à la place ; pas de gel de l'alpha : les défauts structurels restent en alpha, la réponse est de les trouver structurellement (matrice de décors) ; MariaDB suffit dans la matrice (MySQL Community : `test:all -- --dialects`).
