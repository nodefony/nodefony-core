---
date: 2026-09-25
session_id: 6736c5ad-2fa9-4f56-9e12-84dc23dfcf22
focus: publication 10.0.0-alpha.9, puis tâche 0 du banc devkit et ce qu'elle a révélé
---

# Retex 2026-09-25 — 6736c5ad

- **Fait** : alpha.9 publiée (`df909c019`, tag posé par le user, 15 paquets, `latest` recalé,
  vitrine réparée sur son `main`). Correctifs : `c533f77fd` identité e2e de la chaîne de
  production · `b1e5b5706` doctor (ports sous `portPolicy: auto`, config Vite générée) ·
  `30621cc61` indice après option refusée + `--wait` · `1cff11bb5` status/stop refusent l'inconnu ·
  `3c7796566` faux vert du juge client · `afafd1946` page HTML dans la vitrine + virgules
  d'entité. Ticket #480 ouvert (doc de la commande sous toute erreur d'usage, beta).
- **Frictions** : diagnostic doctor annoncé sur la mauvaise section (versé 🎯) ; faux vert du juge
  vu seulement en relisant le transcript (🎯) ; `amend ; push` a publié l'ancien message (🧵) ;
  `exitOverride` non hérité par `addCommand` (🧵) ; boucle zsh `$c` non découpée → faux verdict
  « unknown command » ([[feedback_shell_false_diagnostics]]).
- **Décisions** : `resolvePortPolicy` déplacée dans le cœur (une règle, une implémentation — doctor
  et serveur jugent un port tenu pareil) ; `-y` refusé avec indice, jamais absorbé (accepté puis
  jeté = pire qu'un refus) ; juge client borné aux lignes ajoutées, pas aux fichiers touchés.
