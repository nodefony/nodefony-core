---
date: 2026-08-03
session_id: ff542447-f048-469d-a5cf-2af477d98c71
focus: Les 3 reliquats du chantier pagination — et un banc générique qui trouve ce qu'on ne cherchait pas
---

# Session retro — 2026-08-03 — ff542447

## Fait

- **`13e5a18e`** — un store **CURSEUR** ne trie pas. `assertPageQuery(…, "cursor")` refuse
  désormais l'`order` (`CursorOrderError`, 400) au lieu de l'avaler : **six** stores curseur du
  dépôt fermés d'un geste. L'ordre appartient au jeton — le rendre paramétrable rendrait
  ininterprétable tout curseur déjà émis.
- **`4d35a75a`** — `routes/page` **publie** ses capacités (`sortable`, `search`) et `RoutesView`
  les DEMANDE. Six colonnes étaient `sortable: true` en dur et coïncidaient avec l'allowlist du
  serveur par hasard.
- **`d6697fac`** — le **journal** cesse de rendre TOUT sur un critère invalide. Nature
  multi-valeurs `{ each }` au contrat de filtre du cœur, `SEVERITY_NAMES` en source unique,
  `SyslogAdminApi` passe par les deux traducteurs. Plus la correction de quatre endpoints
  `*/stats` dénoncés par le banc générique.

## Coûts évidents

- **UN seul cycle build + restart** pour les trois gestes backend (règle d'hygiène §4 tenue).
  Le serveur tournait déjà au début de session — vérifié avant de le relancer.
- Une boucle de vérification a rendu un verdict **entièrement faux** (400 partout) à cause du
  modificateur zsh `:A` dans `$f:ASC`. ~3 tours perdus à soupçonner le code avant l'instrument.
- Un `tsc --noEmit -p tsconfig.json` **inerte** sur le front (le tsconfig exclut `frontend`) :
  vert sans rien vérifier. Le vrai gate est `npm run typecheck` (3 projets).
- Un hook pre-commit en échec sur un `ENOENT` de `dist` — race avec le watch du DevSupervisor.

## Recommandations

1. **Écrire les bancs contre la LISTE, pas contre l'exemplaire** — le banc qui parcourt le
   catalogue a trouvé deux trous hors périmètre, et couvrira les endpoints futurs.
2. **Garde anti-banc-vide** systématique dès qu'un test itère sur une collection découverte.
3. **Devant un résultat uniforme et inattendu, suspecter la commande** — c'est la troisième
   séance consécutive où le shell fabrique un faux diagnostic.

## Patterns récurrents (déjà gérés)

- ✅ Débrancher pour voir rouge : fait 3 fois (garde curseur, gate de vocabulaire, contrat audit).
- ✅ Prouver sur le serveur RÉEL, avec des témoins qui filtrent — pas sur le diff.
- ✅ Batcher les edits backend avant un seul rebuild + restart.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `13e5a18e` | un store CURSEUR ne trie pas — l'ordre appartient au jeton       |
| `4d35a75a` | `routes/page` PUBLIE ses capacités — la vue cesse de les deviner |
| `d6697fac` | le JOURNAL cesse de rendre TOUT quand on se trompe de critère    |
