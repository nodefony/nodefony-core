---
date: 2026-06-12
session_id: 560bdbc3-bb2f-4d7a-be96-594baa565090
focus: P6 J2 — Argon2idEncoder + MigratingEncoder + throttling NIST + colocalisation zone test-secure
---

# Session retro — 2026-06-12 — 560bdbc3 (session h, J2)

## Fait

- **Zone `test-secure` colocalisée au module test** (`c4d2c1e3`) : override `module-security`
  dans la config du module (mécanisme `module-<nom>` existant) ; policy dev → zone morte en prod,
  `ctx.isDev` racine supprimé. Prouvé 401/200 avant J2.
- **J2 complet** (`08ae659e` user · `a7e2520e` security · `bd9cd602` banc · `79f19891` dashboard 28 %) :
  Argon2idEncoder (lazy, m=19 MiB/t=3/p=1, bench ~56 ms) ; `supports()` au contrat + MigratingEncoder
  (migration bcrypt→argon2id au login, UserService inchangé) ; LoginThrottler NIST (backoff, 429 +
  Retry-After RFC 6585, Map lazy bornée) câblé AVANT le verifier (anti-DoS argon2) ; schéma Zod
  rateLimit révisé (lockoutThreshold supprimé — anti-NIST) ; IPasswordBlocklist hook + WeakPasswordError.
- Gates : 60 unit user + 53 security + 450 intég http + memory 9/9 + security-review ✅ + preuves curl.

## Frictions

- ENOSPC harness re-vécu (2×) — builds turbo répétés ; dépannage rm tasks/\*.output.
- `grep -cE "error"` matche `UserNotFoundError` → faux positif de gate (motif `TS[0-9]+:` à la place).
- Turbo replay des warns caché (connu) — TS2353 rejoués par http/redis après fix config.ts security.
- Décision design notable : t=3 vs minimum OWASP t=2, tranchée au BENCH (30 ms → 56 ms, cible 50-100) ;
  monter t plutôt que m = +coût attaquant sans RAM serveur (anti-DoS).
- Gap découvert (question user) : section Zod `encoders` validée mais consommée par PERSONNE —
  paramétrage réel = constructeur chez l'app. Repris à J3 (pont config→UserService/UserProvider).

## Commits

| Commit     | Sujet                                                                 |
| ---------- | --------------------------------------------------------------------- |
| `c4d2c1e3` | refactor(test,app): zone test-secure colocalisée via module-security  |
| `08ae659e` | feat(user): argon2id rfc 9106 + migration d'algorithme + blocklist    |
| `a7e2520e` | feat(security): throttling login nist 800-63b — backoff, 429 rfc 6585 |
| `bd9cd602` | feat(test,http): banc j2 — migration observable + throttling prouvé   |
| `79f19891` | docs(migration): p6 j2 coché — p6.5b, bandeau 28 %                    |
