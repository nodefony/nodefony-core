---
date: 2026-09-25
session_id: 613a3ea5-2611-402f-b551-68855b2c98de
focus: "#490 — gate mémoire en octets retenus par itération ; FileTransport borné et ordonné"
---

# Retex 2026-09-25 — 613a3ea5

- **Fait** : `37304d8bc` gate mémoire par pente (Theil–Sen, GC forcé, fenêtre de confirmation,
  ring syslog coupé, échauffement global) + compteur exact de contextes par époque ;
  `7d3cf1e86` seuils renvoyés à une table unique ; `52a2be42d` bancs `tests/load/*` convertis,
  helper `retention.ts` ; `b2a1cf387` `FileTransport` = un flux borné (8 Mio), ordonné, `close()`.
  #490 fermé. Ancre de #487 recalée.
- **Frictions** : bruit étalonné sur serveur chaud alors que la CI juge à froid ; décor consommé et
  pollué par des clients extérieurs ; un test vu rouge pour une autre raison que la sienne ; le
  user a dû me corriger sur « #481 n'est pas une tâche » — c'est le parent du chantier en cours,
  à présenter comme tel (versées au sas, thème 🎯).
- **Décisions** : seuil commun 1 Ko/itération (×2,6 au pire scénario mesuré, moitié de la fuite
  témoin de 2 Ko) ; rouge = deux fenêtres consécutives, pour ne pas faire flancher la CI ; coût
  d'une connexion WS tenue mesuré à part (32 Ko), ce n'est pas une rétention ; upload innocenté
  (plateau à ~1 Mo sur 30 000) ; erreurs de fermeture de transport sur un événement additif
  `onTransportCloseError` pour ne pas casser le contrat `onTransportError(err, pdu)`.
