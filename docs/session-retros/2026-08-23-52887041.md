---
date: 2026-08-23
session_id: 52887041-edc8-439a-85a4-34500c22b18f
focus: Tests de charge — et cinq instruments qui mentaient, dont un défaut produit qui bloquait la production
---

# Session retro — 2026-08-23 — 52887041

## Focus

Reprise de la priorité 1 du 08-23d (tests de charge en session dédiée). Point de départ
inattendu : un **crash Claude** dans un autre onglet, dont l'enquête a ouvert toute la soirée.

## Fait

- **Cause du crash trouvée** : la purge d'ouverture d'un banc (`lsof -ti tcp:… | xargs kill -9`,
  sans `-sTCP:LISTEN`) vise aussi les CLIENTS d'un port et n'épargne pas son lanceur. Garde
  partagée `kill-guard.sh` (filtre LISTEN + liste d'épargne : ce shell, ses ancêtres, tout
  `claude`), vue mordre dans les deux sens.
- 🔴 **Défaut PRODUIT** (`dfdada9e`) — trouvé par le user, en criant : un superviseur de
  développement orphelin, ne détenant **aucun port**, interdisait tout démarrage `production`.
  Le garde déduisait la collision d'une présence de process quand son propre TSDoc écrivait « le
  motif du garde est la collision de PORTS ». Le chemin dev, lui, la CONSTATAIT déjà (`probePorts`).
  Test vu ROUGE avant vert (`expected [ 69 ] to deeply equal []`).
- **Les trois mesures demandées** : capacité + percentiles, comparatif inter-frameworks, soak 20 min.
- **Rapport HTML** `prod-readiness-report.mjs` (calculateur de pods, section « ce que ça ne prouve
  pas »), vérifié dans un vrai navigateur : axe-core 50 règles / 0 manquement, 0 erreur console.

## Les chiffres

| mesure                                    | résultat                                                               |
| ----------------------------------------- | ---------------------------------------------------------------------- |
| Nodefony vs Express équipé (même travail) | **12 226 req/s (92 %)** · p99 9,57 ms (+2,40)                          |
| repères                                   | bare 33 821 · fastify 31 960 · express nu 17 418 · express-fair 13 333 |
| soak 20 min                               | tas **plat** (R² 0,00) · RSS plateau ~244 MB · débit **+5,2 %**        |
| capacité (dev, borne basse)               | p50 0,33 / p95 0,45 / p99 0,54 ms · 293 µs de boucle/req               |
| A/B battement de cœur ORM à 100 Hz        | −6,3 % de débit (3000× le défaut ⇒ inobservable au défaut)             |

## Coûts évidents

- **~30 séries wrk** enchaînées : la machine sature thermiquement et refuse la fenêtre à 3 %.
  Arrêter les conteneurs inutiles a paradoxalement **empiré** la dispersion (plus de CPU libre ⇒
  le banc pousse plus fort ⇒ chauffe plus).
- Une cible qui traverse PostgreSQL derrière Docker Desktop **ne descend jamais** sous 3 % de
  dispersion sur ce poste. Changer de cible (CPU pure, décor ORM monté) plutôt que de seuil.
- Un rejeu complet du comparatif payé deux fois : changer le protocole oblige à **rejouer tous les
  camps**, jamais celui qui arrange.

## Recommandations

1. **CONSOLIDATE en retard de 4 sessions** — `RETEX.md` dépasse 1 250 lignes.
2. Refaire le comparatif sur **Linux natif** : aucun absolu mesuré derrière Docker Desktop n'est
   transposable (facteur 3,7 documenté).
3. Un **soak long** (plusieurs heures) reste à faire : 20 min éliminent les fuites grossières, pas
   les lentes.

## Patterns récurrents (déjà gérés)

- ✅ « Suspecter son instrument » — mémoire enrichie plutôt que dupliquée en RETEX.
- ✅ Un test neuf doit être vu ROUGE : appliqué au garde de collision.
- ✅ Le gate du dépôt est l'autorité : `tsgo` a attrapé un `Severity` typé `string` que vitest
  laissait passer ; `oxlint` a refusé un spread dans un `map`.

## Commits produits

| Commit     | Sujet                                                                                   |
| ---------- | --------------------------------------------------------------------------------------- |
| `7ee95ac6` | un banc ne tue plus son lanceur — le kill par port visait aussi les clients             |
| `dfdada9e` | **la collision de ports se CONSTATE** — un runtime fantôme ne bloque plus la production |
| `427487a7` | un boot qui échoue dit POURQUOI — les journaux coupés pour mesurer le rendaient muet    |
| `212fd1b9` | le banc arrête proprement avant de purger — un superviseur ressuscitait le serveur      |
| `07b6fd05` | le banc rend enfin les LATENCES                                                         |
| `e51992f7` | latences dans le comparatif, et un soak qui refuse d'extrapoler du bruit                |
| `2e3c5fca` | fermer partout la purge de port sans filtre — et cataloguer le soak                     |
| `a3f6eba8` | le soak distingue un PLATEAU d'une rampe                                                |
| `636ff8f4` | rapport « peut-on partir en production ? », et trois défauts vus à l'écran              |
