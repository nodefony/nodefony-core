---
date: 2026-05-30
session_id: 2f03aa21-878d-4474-b967-eae528b7b85a
focus: Piste perf #1 — logging async/buffer : sink stdout bufférisable livré, mais A/B prouve que ce n'est PAS le levier RPS
---

# Session retro — 2026-05-30 — 2f03aa21

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |    64 |
| Read       |    28 |
| Edit       |    28 |
| TaskUpdate |     8 |
| TaskCreate |     5 |
| Skill      |     3 |
| Write      |     1 |
| ToolSearch |     1 |

## Top fichiers Read/Edit

| Fichier                     | Read | Edit |
| --------------------------- | ---: | ---: |
| `syslog/Syslog.ts`          |    6 |    7 |
| `nodefony/config/config.ts` |    0 |    7 |
| `kernel/Kernel.ts`          |    2 |    2 |
| `tests/Syslog.test.ts`      |    0 |    2 |
| `tests/CliKernel.test.ts`   |    0 |    1 |

## Coûts évidents

- **Bench A/B = le gros poste.** `config.log.buffered`/`active` togglé **7×** → 7 cycles
  `edit → npm run build → stop.sh → start.sh → 3 runs`. ~5 restarts serveur + 5 rebuilds root.
- **1er round A/B gâché** : Brave ~98 % CPU (2 procs) → variance absurde (1317↔3163), mesures
  jetées. Refait sur machine quiète (user a fermé Brave). Leçon : vérifier `ps` AVANT de bencher.
- **Faux départ wiring** : j'ai _annoncé_ avoir restauré `active:true` sans faire l'Edit → config B
  benchée en `active:false` (= A bis), invalide. Le user a flairé (« t'es sûr que c'est câblé ? »).
  Corrigé via preuve terrain (`grep` req logs = 0). Leçon : prouver l'état, ne pas l'affirmer.

## 💶 Coût (€)

- Total **~€65** (≈ $70.6) — cache-read 58 % + cache-write 1h 23 % = **81 % cache** ; output 18 %.
  137 tours. Classique cache-dominé : le contexte porté (CLAUDE.md + skills + tool results des
  benchs) coûte, pas l'output. Le gros des tool results = sorties de bench répétées.

## Recommandations

1. **Skill `nodefony-perf-ab`** (candidat) — automatiser un A/B config (toggle clé config → build →
   restart → N runs médiane) en 1 invocation. Vu 7× ce run, sera revu pour la piste 0x. Évite les
   7 edits + 5 restarts manuels.
2. **Bencher = vérifier `ps` d'abord** (à mettre dans `nodefony-load-test`) — un desktop chargé
   (navigateur) invalide tout. 1er round perdu cette session.
3. **Prouver l'état avant d'affirmer** (déjà dans l'esprit `feedback_session_hygiene` tests-first) —
   un « j'ai restauré X » non vérifié a coûté un round de bench.

## Patterns récurrents (déjà gérés)

- ✅ Isomorphisme Core (user l'a rappelé) — `setImmediate`/`process` via `globalThis`, build client vert.
- ✅ Backplane logs (user l'a rappelé) — write reste fire-and-forget, buffering = propriété du sink.
- ✅ `.git/index.lock` orphelin husky → `rm` + retry (`feedback_git_index_lock`).
- ✅ memory.test flake full-suite = contamination GC → isolation = vérité (`nodefony-debug`).
- ✅ Reprise/état mis à jour + mémoire poussée (`nodefony-session`).

## Commits produits

| Commit    | Sujet                                                                 |
| --------- | --------------------------------------------------------------------- |
| `0b61453` | feat(syslog): sink stdout bufférisable opt-in (log.buffered) + rawLog |
| `2bce055` | perf(backlog): #1 logging fait mais PAS le levier (mémoire IA)        |
| `229abaa` | session state + index reprise (mémoire IA)                            |

## Enseignement clé

Résultat **négatif mais précieux** : le logging par requête n'est PAS le goulet RPS (sink local
rapide). €65 ont surtout acheté un **diagnostic qui évite de micro-optimiser au mauvais endroit** —
la vraie piste = profiler le plafond ~3600 RPS avec `0x`. La prémisse du backlog était fausse.
