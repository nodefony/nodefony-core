---
date: 2026-07-12
session_id: 8e8a200f-04ee-4fbb-bda3-bb8fda1fa9b3
focus: bilan de fin de boot + onboarding app générée (zéro warning) + DX multi-projet — 14 commits
---

# Session retro — 2026-07-12 (2ᵉ du jour) — 8e8a200f

## Fait (14 commits `e8cca076`→`07e565ae` + MIGRATION_STATUS)

- **Bilan de fin de boot** (`e8cca076`) : bloc « Bilan » du verdict — modules
  ignorés (policy/when) motivés, échecs fail-soft, topologie process (pid+RSS,
  scopée projet), journal WARNING/ERROR (ring figé à postReady). Preuve
  immédiate : a exposé un boot fantôme (cwd) dès son premier run.
- **Onboarding app générée** (`4ac7245e`, plan validé user) : secrets à la
  CRÉATION (.env.local gitignoré, .env commité catalogue), provisionUsers +
  admin/admin dev (prod = NF_ADMIN_PASSWORD sinon refus), HTTPS par défaut
  (revirement WebRTC), `create app` = install + git init/first commit,
  `security:secrets` (3 itérations DX sur retours user à chaud) +
  `security:user:add` (prompt masqué). Banc réel : **0 WARNING** + logins 200.
- **Fixes de fond débusqués par le banc** : console one-shot à onPostReady ne
  terminait jamais (`finishOrPark`, exitCode préservé) · override config
  inter-modules WARNING→INFO · EPIPE WS client parti = DEBUG résolu (+ callback
  broadcast) · complétion arguments positionnels (`create <TAB>` → app) ·
  start.sh : cd racine + filet de ports SCOPÉ projet (il a SIGKILLé l'app du
  user) · messages multi-projet aérés + status hors projet explicite.

## Frictions (détail → RETEX.md, thèmes App générée + CLI)

- 2× boot fantôme par cwd (start.sh, puis --write dans le package security).
- Filet kill -9 non scopé = contradiction avec la règle multi-projet.
- eta autoTrim, $pipestatus zsh, manifest complétion = cache.
- LogSink flake 1× en full suite (vert isolé + re-run) — à surveiller.

## Méthode qui a payé

- 2 agents Explore en parallèle (scaffold + users) → implémentation sans
  re-lecture ; le banc réel apps `--link` a trouvé 5 bugs que les suites ne
  voyaient pas ; chaque retour user à chaud (zsh parse error, « il faut faire
  les 3 ? », SIGKILL) → fix immédiat + test.

## Commits

`e8cca076` bilan boot · `4118741d` multi-projet aéré · `55629781` start.sh cd ·
`ac095e38`+`f8d61ad8`+`faa743eb` security:secrets · `4ac7245e` onboarding ·
`3896313e` override INFO · `2c8d1ab5` docs · `c48ce8be` complétion ·
`50a195d8` EPIPE WS · `07e565ae` filet scopé
