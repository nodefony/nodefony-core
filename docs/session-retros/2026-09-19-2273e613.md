---
date: 2026-09-19
session_id: 2273e613-3f33-46f3-a54e-bc6a5c63fe4b
focus: "#138 — les contraintes d'intégrité des relations ; puis #433, la sonde qui ne vérifiait pas QUI répond"
---

# Session retro — 2026-09-19 — 2273e613

## Focus

**#138** : une colonne `ref:` naissait indexée et sans contrainte. Émission de `.references()`
côté scaffold, clés étrangères dans le DDL dérivé des trois dialectes, tri topologique, cycle
nommé, `PRAGMA foreign_keys` en SQLite. Puis **#433**, né de l'enquête sur les rouges de la
campagne.

## Fait

- `b7659c7d` — #138 : scaffold + `ddlPlan.ts` (module pur) + `DrizzleOrm` + migrateur documenté.
  Politique `ON DELETE` **dérivée de la nullabilité** (`restrict` / `set null`), jamais `cascade`,
  aucune grammaire neuve au lendemain de #431.
- `77c2581b` — #433 : la sonde du banc MCP demande son NOM à l'application au lieu de se contenter
  d'un code HTTP.
- Doc recalée : `drizzle/{CLAUDE,MEMORY}.md`, `cli/CLAUDE.md`, gabarit d'entité, `migrations.md`
  (GO user), `mongoose/MEMORY.md`, skill `framework-dev`.
- Tests : 11 (plan pur) + 7 (SQLite) + 6 (e2e PG/MariaDB) + 4 scaffold + 1 migration générée.

## Frictions

- 🔴 **J'ai édité les sources pendant ma propre campagne `test:all`** → turbo a rebâti les `dist`
  au milieu des tests → 5 rouges et un serveur qui ne démarre pas. Relancée sur arbre stable :
  12 → 7 échecs. La règle est écrite, lue le matin même. → versé au sas.
- 🔴 **7 rouges devkit imputables à l'application voisine du user** qui tenait 5151 : la garde
  `skipIf` constatait une disponibilité, jamais une identité. → #433, versé au sas.
- 🔴 **SQLite ne peut PAS juger l'ordre de création des tables** (résolution tardive des clés
  étrangères) : tri débranché, le banc SQLite restait VERT. Sans l'e2e sur serveurs réels, le
  point 3 du « Fini quand » aurait été prouvé par un test aveugle. Famille déjà graduée
  (`feedback_gate_must_bite`) — pas de doublon au sas.
- **`better-sqlite3` pose déjà `foreign_keys = 1`** : mon premier commentaire affirmait l'inverse.
  Corrigé sur place, et un test LIT le réglage pour que « posé par nous » reste distinguable de
  « posé par le pilote ».
- Le skill `framework-dev` enseignait encore `title:string!`, refusé par #431 **la veille** — dans
  la référence que charge tout agent codant du backend.

## Coûts évidents

- Deux campagnes `test:all` complètes (~6 min chacune) dont la première invalidée par ma faute.
- L'enquête sur les 12 rouges : ~15 appels d'outils pour aboutir à « aucun n'est une régression ».

## Recommandations

1. **Déclarer le banc MCP au `gateReporter`** — avec #433, il SAUTE désormais au lieu d'échouer,
   donc il peut ne jamais tourner en campagne sans que personne le voie. Dit en clair dans le
   compte rendu de fermeture de #433.
2. **Auditer les autres bancs qui visent `localhost:5151`** — le défaut d'identité peut s'y trouver.
3. **Une barrière contre l'édition pendant un run long** — la règle seule n'a pas suffi, deux fois.

## Commits produits

| Commit     | Sujet                                                                                   |
| ---------- | --------------------------------------------------------------------------------------- |
| `b7659c7d` | feat(orm): poser les contraintes d'intégrité des relations (#138)                       |
| `77c2581b` | test(devkit): sauter le banc MCP quand le port répond pour une autre application (#433) |
