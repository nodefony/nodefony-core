---
date: 2026-06-15
session_id: c648f429-3fd1-40e9-bc15-6851c233bc45
focus: J9 drivers store WebAuthn credentials — Redis/Drizzle/Mongoose (approche B)
---

# Session retro — 2026-06-15 — c648f429

## Focus

P6 J9 (suite) : adapters cluster du store de credentials WebAuthn/passkeys —
`RedisWebAuthnCredentialStore`, `DrizzleWebAuthnCredentialStore`,
`MongooseWebAuthnCredentialStore`. Convention-frère exacte du `tokenStore` (J4b).

## Fait

- **Redis** (`52c5c7e6`) : HASH `nf:wac:cred:<id>` + SET `nf:wac:user:<uid>`, pas de TTL
  (passkey permanente → pas de gc), dégradation gracieuse offline. + config security
  `passkeys.store` enum→`z.string()` (pluggable). 12 tests (FakeRedis déterministe).
- **Drizzle** (`634a1c8c`) : entité `webauthn_credential` (schema-as-code, index userId) +
  store via `IRepository` + mapping `Row↔contrat`. 11 tests `:memory:`.
- **Mongoose** (`0d9be502`) : schéma `_id`=credentialId (clé naturelle) + store + mapping. 11
  tests (mongodb-memory-server, a réellement tourné).
- **Docs** (`94d5f6e9`) : MEMORY security (snippet câblage app) + MIGRATION_STATUS ligne P6.
- Gates : build 19/19 modules touchés verts du 1er coup ; non-rég redis 37 · drizzle 71 ·
  mongoose 70 · security 231 ; security-review 0 blocker. **0 régression.** Non poussé jusqu'au END.

## Décisions / POURQUOI

- **Approche B** (tranchée par le frère VIVANT, pas le commentaire du registry) : adapter dans le
  module backend en `import type` SEUL (0 dép runtime, 0 modif package.json), l'app câble
  `registerWebAuthnStore(...)`. Le commentaire de `webAuthnCredentialStoreRegistry` (« adapters depuis
  LEUR module ») laissait croire à un couplage runtime → `RedisTokenStore` a montré la réalité.
- **Mapping Row↔contrat** (divergence assumée vs token) : `IWebAuthnCredential` a `nickname?` + readonly
  (≠ `IAccessTokenRecord` tout `| null`) → row `{nickname:null}` casse `deepEqual` → `WebAuthnCredentialRow`
  plate + `#toCredential`/`#toRow`.
- **`WebAuthnAuthenticator` step-up MFA différé** : login passkey passe par BFF/session
  (`AuthFlow.establishSessionFor`), pas par le firewall → pas de consommateur d'un authenticator.

## Frictions

- **Read-before-Edit oublié 2×** (redis/index.ts, MIGRATION_STATUS) → erreur harness « File has not
  been read ». Déjà gradué `feedback_edit_requires_read_tool` (≥6× désormais) — le feedback ne « prend »
  pas ; réflexe à ancrer : sur un fichier EXISTANT à éditer, Read l'outil AVANT, même après un grep.
- Le linter pre-commit reformate les fichiers fraîchement écrits (multi-ligne) — normal, husky.

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `52c5c7e6` | feat(redis): store credentials webauthn redis (pluggable J9) |
| `634a1c8c` | feat(drizzle): store credentials webauthn drizzle (J9)       |
| `0d9be502` | feat(mongoose): store credentials webauthn mongoose (J9)     |
| `94d5f6e9` | docs(security,migration): drivers webauthn store ORM/Redis   |

## ➡️ Prochaine session

OAuth2 `arctic` (social login Google/GitHub) — J9 reste. Boussole `project_p6_security_kit` + design
`project_decisions_p5_p6_orm` (Arctic+jose+Bcrypt) + `project_auth_future_vision`.
