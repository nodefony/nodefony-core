---
date: 2026-07-13
session_id: c6824490-81d5-47b4-976b-6db392d0c969
focus: Playground Studio lot 1 (data plane + page) + fix pont renderJson (bug user réel) + upload
---

# Session retro — 2026-07-13 (soir) — c6824490

## Fait

- `6c17ce59` feat(framework) : data plane playground `GET /nodefony/framework/api/playground/routes`
  (dev-only, form-ready : transports/params/gardes), 6 tests unit.
- `11ce18c3` feat(studio) : page Playground générique data-driven (`routes/playground/` éclaté),
  switch HTTP⇄Socket côte à côte + rejeu même clé + badges gardes ; fallback SPA littéral.
- `6f8a4177` fix(realtime) : action `renderJson` via pont `api.request` — capture de rendu ALS
  (`renderSink` core + garde `WebsocketContext.send`) + filet fail-safe stringify du peer.
  Bug trouvé PAR LE USER au Playground sur sa cci-app (< 1 h après livraison — la console a payé).
- `2c903c47` docs(retex) : leçon « chemin voisin » graduée 2×.
- `d2ca2dc7` feat(studio) : détection upload (`@UploadedFile`/`@UploadedFiles`) → FileInput +
  multipart réel ; socket désactivée honnêtement ; `build:ui` régénéré.

## Frictions / leçons (→ RETEX.md)

- Chemin voisin 2× : le banc duplex ne testait que le retour NU — renderJson via pont = trou.
  Règle : chaque forme de retour × chaque transport.
- Mantine v9 `Grid gutter`→`gap` ; UI statique `--link` = `build:ui` obligatoire.
- Ports partagés banc/app user : 3 stop/start croisés (filet strict OK, coordination à annoncer).
- Watch DevSupervisor muet sur edits backend → build manuel avant banc de fermeture.

## Gates

Framework 440+6 · realtime 314 · memory 9/9 ×2 · intégration http 570/570 (dont 2 tests de
fermeture rouge→vert) · tsgo core/http/realtime/studio 0 erreur · security review ✅.
