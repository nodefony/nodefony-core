---
date: 2026-07-04
session_id: cfeafab5-2b87-4527-9736-d1a0ffe256dc
focus: Phase 0.8 lot 4 — doctrine d'échec + prod-guard des stores (5e session du jour)
---

# Session retro — 2026-07-04 — cfeafab5 (lot 4)

## Fait

- **Lot 4 livré en 1 commit** (`ea2442e9`, 11 fichiers) :
  - Store EXPLICITE introuvable : prod = throw au boot (kernel avorte via `isBootErrorFatal`),
    dev = dégradation annoncée (session → repli `files` WARNING ; token/passkeys/audit/webhooks →
    brique désactivée CRITIC, audit uniformisé WARNING→CRITIC).
  - Prod-guard : brique durable résolue `memory` en PRODUCTION → WARNING nommant l'impact
    (tokenStore, passkeys, audit, webhooks, idempotence, NF_USER_STORE) — couvre auto ET explicite.
- Décision user : **lots 5/6 inversés** (doc+ménage AVANT Studio Stores — nettoyer les champs
  avant de les exposer). Kit mis à jour + items ménage enrichis (relicats formidable,
  rejectUnauthorized, balayage champs morts).

## Frictions

- « granite » (valeur bidon test terrain) posée dans nodefony.config.ts sans annonce → 2 questions
  user pendant le revert. → RETEX.md (annoncer la sentinelle + nom auto-descriptif).
- Relicats config repérés par le USER, pas par les lots 2-3 (uniformisation ≠ audit de vie des
  champs). → RETEX.md + kit lot 5.

## Ce qui a bien marché

- Terrain AVANT code (idempotence identifiée comme référence déjà conforme → pattern copié 6×).
- Batch complet 7 fichiers puis UNE passe de gates : tout vert du 1er coup, 2 restarts serveur
  seulement (nominal + preuve terrain granite).
- Harness de test existant (fake kernel) réutilisé : +6 tests doctrine à coût quasi nul.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `ea2442e9` | feat(config): doctrine d'échec + prod-guard des stores (0.8 lot 4) |
