---
date: 2026-08-27
session_id: add12811-c867-4499-881d-f358d481f572
focus: Donner un ticket à chaque dette (#56) — et découvrir que les kits annonçaient du travail déjà fait
---

# Session retro — 2026-08-27 — add12811

## Fait

- **#56 soldé** : les 24 dettes de `MIGRATION_STATUS.md` ont chacune leur verdict — 4 déjà
  couvertes, **24 tickets créés** (#57→#80), 2 sans ticket avec leur raison écrite. Le tableau des
  dettes est devenu un INDEX.
- **Contrôle des kits mémoire** (exigé par le user) : **9 items sur 11 étaient déjà livrés**,
  confrontés au code ET au `git log`. Zéro ticket créé pour eux.
- **Sous-tickets** : #63 réécrit en parent (#76/#77/#78), #79 créé comme frère de #60. Le skill
  `nodefony-ticket` passe en 1.3.0 avec le déclencheur qui manquait.
- **Backlog** introduit (5 tickets sans jalon) + `board-snapshot.mjs` le nomme correctement.
- **Ordre visuel** du tableau posé physiquement (63 items), par grappes de contexte.
- **CI réparée** : le seul rouge des 5 matrices était 4 liens de l'archive de la roadmap, restés
  relatifs à la racine après son déplacement de la veille.
- **Toutes les dépendances montées**, TypeScript 6 → 7 compris. `npm outdated` ne rend plus rien.
- **Bug WebSocket « ~30 s » tranché** : ne se reproduit pas (48,5 s d'observation, socket jamais
  fermé, 0 erreur hors les 401 du parcours de connexion).

## Coûts évidents

- Trois délégations `haiku` enchaînées (inventaire kits partiel → 18 kits ciblés → confrontation au
  code). La première a lu 4 fichiers sur 84 — mais **l'a dit**, ce qui a permis le pré-tri mécanique
  qui a suivi. Un `rg` de densité a coûté zéro et a réduit 84 kits à 18.
- Un `npm install` bloqué par un verrou qui retenait Angular en 22.1.3 alors que tous les
  `package.json` disaient 22.1.4 : trois tentatives avant la résolution forcée sur le workspace.

## Recommandations

1. **Confronter un kit au code avant de s'en servir** — la friction est au sas, à graduer si elle
   revient : un kit est un plan, pas un état.
2. **Ne jamais lire le résultat d'un script dans ses propres `echo`** — `declare -A` a échoué en
   silence sous bash 3.2 et six priorités sont parties par défaut.
3. **Le seuil de découpage d'un ticket ne se compte pas en tickets** : deux critères de fin
   atteignables séparément SONT un parent. Écrit dans le skill.

## Patterns récurrents (déjà gérés)

- ✅ Dérive de `cwd` entre deux appels — rattrapée par le garde-fou du dépôt.
- ✅ `rg -r` refusé par le garde-fou.
- ✅ Capturer une sortie entière puis déléguer sa lecture (5 521 lignes de journal CI).

## Commits produits

| Commit     | Sujet                                                                 |
| ---------- | --------------------------------------------------------------------- |
| `2fc1a9fa` | chaque dette a son ticket — et huit « restes » étaient déjà faits     |
| `274facd7` | l'archive de la roadmap pointait vers la racine, pas vers son dossier |
| `d2c95a78` | monter TypeScript 7 — le catalogue des apps générées suivait plus     |
