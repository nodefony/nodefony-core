---
date: 2026-05-20
session_id: a6ff1e1b-5b3d-4445-a291-f8eaede80013
focus: Data plane admin — IAdminApi/AdminBroker (P10.2) + 4 producteurs kernel/http/framework/syslog (P10.3) + frontend Studio (System explorer, Dashboard/Logs réels, page Modules + détail à onglets)
---

# Session retro — 2026-05-20 — a6ff1e1b

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Bash | 136 |
| Edit | 123 |
| Read | 39 |
| Write | 16 |
| TaskUpdate | 9 |
| TaskCreate | 5 |
| Skill | 2 |
| ToolSearch | 1 |

Transcript : 1428 lignes / 4,0 MB. Tool results : 333 events, ~273k chars (avg 819).

## Top fichiers Edit/Write
| Fichier | × |
| ------- | -: |
| framework/index.ts | 12 |
| framework/MEMORY.md | 9 |
| ModuleDetail.tsx | 8 |
| studio/MEMORY.md | 8 |
| AdminApiController.ts | 7 |
| Modules/Dashboard/AdminBroker/IAdminBroker/KernelAdminApi/App.tsx | 6 |
| mémoire project_admin_data_plane_iadminapi | 6 |

## Coûts évidents
- **`start.sh` chemin relatif a échoué ×3** (`No such file or directory`) : après `cd <subdir> && npm run build`, le cwd Bash persiste → `bash .claude/skills/.../start.sh` relatif casse. **Déjà en mémoire `feedback_cd_startsh_relative_path` mais re-rencontré 3×.** Chaque échec = 1 relance perdue.
- **Turbo cache → dist périmé ×2** : édité `studio/index.ts` (apiProxyPaths) et `http` (HttpAdminApi) puis `npm run build` (turbo) a CACHÉ le workspace → runtime servait l'ancien dist (regex `/nodefony/api` fantôme, `active:null`). Force rebuild par workspace nécessaire.
- **Cycle build→restart→curl ×~9** : coût dominant. Quelques-uns batchables (édits backend kernel+http+framework avant 1 seul restart) mais l'approche producteur-par-producteur était délibérée pour valider chaque étage.
- **Bug runtime React** : `e.currentTarget.value` lu DANS l'updater fonctionnel `setValues((v)=>…)` → crash (currentTarget nullifié). Trouvé seulement au test navigateur user.

## Recommandations
1. **Durcir le skill `start-nodefony-server`** : utiliser un chemin ABSOLU pour `start.sh`/`stop.sh` (ou `cd` racine en tête du script) → tue définitivement le piège cwd vu 3× malgré la mémoire.
2. **Mémoire/règle « turbo cache stale »** : après édition d'un `index.ts`/source d'un module consommé au boot (apiProxyPaths, config), forcer `cd <module> && npm run build` (pas le turbo racine). Candidat ajout `feedback_session_pitfalls`.
3. **Promouvoir le pattern `esbuild parse-check`** (frontend Studio = pas de gate tsc, Vite/esbuild strip les types) en mini-skill : `npx esbuild <file> --loader:.tsx=tsx --jsx=automatic --bundle=false` avant restart. Utilisé efficacement ici, attrape la syntaxe avant le navigateur — mais PAS les erreurs de type (le bug React event-null est passé).

## Patterns récurrents (déjà gérés)
- ✅ Skill `start-nodefony-server` (start/stop) + skill `retex`.
- ✅ Split contrat core / transport framework (inversion dépendance) figé en mémoire `project_admin_data_plane_iadminapi`.
- ✅ Gotchas data plane mémorisés : `Service.get` collision→`getApi`, RBAC différé, params `route.variables` string[], enveloppe double-wrap, per-instance + header `x-nodefony-instance`.
- ✅ Tests sérieux écrits dans la session (24 tests admin) — ont révélé 3 bugs (mountAll idempotent, params, enveloppe).
- ✅ Décision stateless auditée avant de coder (sessions `deprecated`).

## Commits produits (11)
| Commit | Sujet |
| ------ | ----- |
| 51ee9aa | contrat IAdminApi + AdminBroker + kernel migré |
| 496348f | producteur http + header instance |
| cad14c4 | producteur framework (dump routes) |
| b55fc7f | fix params + enveloppe (regexp testée) |
| a2f0f90 | producteur syslog — P10.3 complet |
| 2a563a6 | suite tests admin (24) + fix mountAll idempotent |
| 2dd1c35 | endpoint catalogue /framework/api/admin |
| c6e3f4d | frontend System câblé sur le catalogue |
| c12740e | Dashboard+Logs réels + explorer params {x} |
| 1393046 | fix React event-null updater |
| 71acba9 / bdade35 / e8172e8 | KPI Sessions + page Modules + détail à onglets (Config/Services réels) |
