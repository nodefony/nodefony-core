---
date: 2026-08-20
session_id: 60f8a79e-a1c3-4945-afd6-4b0f04cf30b5
focus: Reprise après 2 semaines — P6.9d tranché (après la 10), surface frontend assainie, `sub` externe borné, défi RFC 9728
---

# Session retro — 2026-08-20 — 60f8a79e

Première session après deux semaines d'interruption. Elle a commencé par une restitution
(fil directeur + 10 dernières sessions) et a enchaîné trois lots, dont deux non prévus au
départ : le user a relevé lui-même une incohérence du `MIGRATION_STATUS`, et le troisième
lot est né d'une vérification en `curl`.

## Coûts évidents

- **Relire un `MIGRATION_STATUS` dont une ligne fait ~10 Ko** : chaque `grep` sur la phase P6.9
  ramène un pavé. La ligne est un journal, pas une entrée de tableau.
- **Turbo masque les workspaces en aval** quand un typecheck échoue : 2 erreurs vues, 9 cachées
  derrière. Deux passes au lieu d'une — le réflexe « corriger puis RELANCER pour voir le reste »
  a servi.
- **Dérive de `cwd`** : un `grep` lancé depuis le module security au lieu de la racine a rendu un
  résultat faussement rassurant. Rattrapé au coup d'œil (chemins trop courts), pas par une garde.
- **Le débranchement en LIVE coûte deux redémarrages** (édit → attente restart → banc → rebranche
  → attente → banc). ~4 min, et ça a payé les deux fois.

## Recommandations

1. **La ligne P6.9 du `MIGRATION_STATUS` doit être scindée** — elle agrège cinq lots successifs et
   dépasse 10 Ko. Elle est devenue illisible pour un humain comme pour un agent.
2. **Déréférencer toute URL qu'on vient de publier** — c'est ce `curl` qui a trouvé le 404 et le
   mojibake, deux défauts qu'aucune suite ne voyait.
3. **Précéder toute assertion négative d'une assertion positive** (gradué au SAS ce jour).

## Patterns récurrents (déjà gérés)

- ✅ Compte de rouges annoncé AVANT de couper — a rattrapé le test creux (1 rouge au lieu de 2).
- ✅ Débranchement systématique de tout test neuf, y compris sur le décor réel.
- ✅ Capture des sorties entières en fichier avant filtrage.
- ✅ Gate mémoire lancé avant de conclure (il manquait au 1ᵉʳ commit du lot sécurité, rattrapé).

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `e628c7ad` | refactor(frontend): la surface ne promet plus que ce que le builder rend  |
| `b5c2e4b7` | fix(security): un sujet étranger n'entre plus dans l'espace de noms local |
| `08a3f3f8` | feat(security): un refus qui dit où aller chercher un jeton (RFC 9728)    |
