---
date: 2026-05-24
session_id: 20abfd55-b23d-4998-bd86-4732519bf2f7
focus: Mode cluster sans PM2 — Phase 1 (port IBackplane + LoopbackBackplane câblé au RealtimeHub)
---

# Session retro — 2026-05-24 — 20abfd55

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    21 |
| Edit  |    17 |
| Read  |    16 |
| Write |     3 |
| Skill |     3 |

## Top fichiers Read/Edit

| Fichier                                        |   × |
| ---------------------------------------------- | --: |
| `nodefony` (JS) `kernel/kernel.es6` (Read)     |   4 |
| skill `nodefony-framework-dev/SKILL.md` (Edit) |   7 |
| `framework/.../RealtimeHub.ts` (Edit)          |   4 |
| `framework/index.ts` (Edit)                    |   3 |
| `framework/.../RealtimeHub.test.ts` (Edit)     |   2 |

## Coûts évidents

- Session **propre** : 1 module (framework), exploration JS ciblée (cluster), 0 restart serveur
  (refacto pur testé en unit + mémoire, pas de boot runtime). Aucune re-lecture chère (kernel.es6 4×
  = lecture progressive des 4 zones cluster, normal pour de l'exploration de référence).
- 1 friction : `.git/index.lock` orphelin (vide, crash terminal antérieur) a bloqué le 1ᵉʳ commit →
  retiré après vérif `pgrep git`. ~2 tool-calls perdus. **Récurrent** (2ᵉ fois en 2 sessions).

## 💶 Coût (€)

- Total **~27,3 €** (≈ 29,7 $) sur 69 tours — décompo : cache_read 9,06M tok → 13,6 $ · cache_write 1h
  366k → 11,0 $ · output 66k → 4,9 $ · input négligeable. **Cache-dominé ~83 %** (contexte porté =
  CLAUDE.md + MEMORY.md long + 2 gros skills chargés : framework-dev ~1300 l. + session). 2 commits +
  1 mémoire vision → ~9 €/livrable, correct pour une fondation d'archi (port + tests + doc).
- Levier : le chargement du skill `nodefony-framework-dev` (gros) pèse sur le cache. OK ici (session cœur
  qui le justifie), mais ne pas le charger pour une micro-tâche.

## Recommandations

1. **Garde-fou `index.lock`** (hook husky / pré-check) — orphelin 2× en 2 sessions, déjà dans
   [[project_retex_improvements_kit]] action #I (« le plus rentable »). À exécuter en session process.
2. Rien à ajouter à l'allowlist (`echo/grep/git/npx/jq` standards ou `npx` générique volontairement en prompt).
3. Décision archi capturée en mémoire **avant** de coder ([[project_cluster_backplane_vision]]) — réflexe à garder.

## Patterns récurrents (déjà gérés)

- ✅ Mémoire de vision écrite AVANT le code (survie au `/clear`).
- ✅ 1 feature = 1 session (Phase 1 seule, Phases 2-4 différées en sessions dédiées).
- ✅ Skill cœur MAJ + bump SemVer + RETEX dans la même session (lockstep ré-aligné 1.10.0).
- ✅ memory.test après modif pipeline realtime (8/8 verte).
- ⚠️ `index.lock` orphelin post-crash : retrait après vérif `pgrep` (procédure connue, à automatiser).

## Commits produits

| Commit    | Sujet                                                                    |
| --------- | ------------------------------------------------------------------------ |
| `ac21bec` | feat(realtime): port IBackplane + LoopbackBackplane — seam cross-process |
| `49736b0` | chore(skills): nodefony-framework-dev 1.10.0 — §4 backplane              |
