---
date: 2026-09-11
session_id: f9bd0ca0-77d0-400f-a68d-498fa62b8a0c
focus: solder les dettes — et un audit Lighthouse du user ouvre une chaîne de secrets publiés
---

# Session retro — 2026-09-11 — f9bd0ca0

## Focus

Solder les quatre dettes nommées par la session précédente, avant de prendre #316. Aucune n'était
un simple rattrapage : deux d'entre elles ont trouvé des défauts, et un audit Lighthouse apporté
par le user en séance a ouvert la chaîne la plus longue de la journée.

## Fait

- **La frise entre dans l'empreinte** (`af6a2f78`) — `.ai/board.json` portait ordre et estimation
  mais pas les dates : hors ligne, un calendrier décalé de dix jours était invisible. Trois maillons
  pouvaient perdre la donnée sans erreur, tous trois débranchés et vus rouges. En prime, le contrôle
  `FRISE-TROP-COURTE` : **13 tickets sur 86** s'accordent une fenêtre plus courte que leur propre
  estimation.
- **`test:all` relancé** — 10 366 verts, 1 rouge, 176 sautés. Le rouge (`ViteBuilder` svelte5,
  délai dépassé) n'était pas une régression : le test payait le chargement réel d'un plugin Vite.
  Cause retirée plutôt que budget élargi (`7bcb1268`).
- **Le banc de publication rejoué sur `dev`** — sa dernière passe verte datait de `main`, quatre
  jours plus tôt. Il a REFUSÉ l'image du scénario à frontend : clé privée TLS. Chaîne établie de
  bout en bout, corrigée (`79a363f0`), et **prouvée sur l'image reconstruite**.
- **Audit Lighthouse du user** → contraste sombre corrigé sur les quatre vitrines, sondé à l'écran
  (4,84 AA contre 2,21) ; et le défaut trouvé EN VOULANT mesurer : `lib/wcag.mjs`, publiée dans le
  devkit, rendait du NOIR sur une couleur hexadécimale (`cab83127`).
- **Audit délégué en `fable`** sur la conteneurisation → le contrôle d'image était aveugle à
  `keyset.json` et aux trois `.env` d'agents ; le `.dockerignore` laissait entrer `.npmrc`, les
  jetons MCP et **tous** les journaux (`b28d7586`).
- **Les trois tags publics retirés** de Docker Hub, constaté sur le registre (0 tag, 404).
- **Six tickets ouverts** (#353 → #358), rattachés à #316, datés, tableau remis cohérent.

## Coûts évidents

- **Session très longue, très ramifiée** — quatre dettes + deux demandes du user en cours de route.
  Le coût de relecture croît en carré ; c'est le poste principal, et il était évitable en partie :
  la chaîne « audit Lighthouse → contraste → parseColor → sonde » aurait pu être une session à elle
  seule.
- **Deux commandes proposées au user ont bloqué son terminal** — `read -rsp` puis un `;` échappé.
  Deux allers-retours perdus pour une commande que je n'avais pas exécutée moi-même.
- Trois `npm install` + build d'une application témoin pour mesurer un contraste à l'écran. Justifié
  (la règle exige de regarder l'écran), mais c'est le geste le plus cher de la séance.

## Recommandations

1. **Graduer le thème 🤝 du sas** (5 frictions, mûr) — le terrain qu'on donne à un délégué. Travail
   de CONSOLIDATE.
2. **Trancher la frise** — 13 tickets promettent moins de temps qu'ils n'en ont estimé. Décision de
   planification, pas technique.
3. **Une commande destinée au terminal du user ne contient ni interactif ni boucle** — trois lignes
   indépendantes plutôt qu'un `for`. Versé au sas.

## Patterns récurrents (déjà gérés)

- ✅ **Une correction couvre le cas vu, pas la famille** — [[feedback_fix_the_family_not_the_instance]].
  **Trois fois dans la même journée** : contraste corrigé en clair seulement, budget de temps donné
  au seul test le plus lourd, nettoyage d'image posé pour `var/` seulement. Le remède était même
  exposé au défaut qu'il corrige : le gate vu mordre sur une clé TLS laissait passer une clé JWT.
- ✅ **Prouver sur l'artefact reçu** — [[feedback_prove_on_received_artifact]] : le correctif
  d'image n'a été déclaré qu'après reconstruction sur la forge, et le retrait des tags n'a été
  déclaré qu'après lecture du registre.
- ✅ **Un gate jamais vu échouer n'est pas un gate** — [[feedback_gate_must_bite]] : tous les cas
  neufs débranchés, y compris dans le sens inverse (ajouter `.env` au `.dockerignore` doit tomber).

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `af6a2f78` | porter la frise dans l'empreinte, et confronter fenêtre et estimation  |
| `7bcb1268` | éprouver la règle de déduplication sans charger les plugins Vite       |
| `79a363f0` | effacer dans l'étage de CONSTRUCTION ce que le build fabrique          |
| `cab83127` | lire l'hexadécimal, et corriger le contraste du thème SOMBRE           |
| `b28d7586` | refuser les deux secrets qu'une app Nodefony publie le plus facilement |
