---
date: 2026-09-25
session_id: 171fd74e-d566-4321-900e-bedeaa9ac3c0
focus: vitrine de release réparée, audit externe trié, chantier scope de requête ouvert (#481)
---

# Retex 2026-09-25 — 171fd74e

- **Fait** : `996bdca60` fix(release): juger la vitrine sur son dépôt, et ignorer un run annulé doublé ·
  `cd88885d5` chore(board) · #175 fermé (soldé par les alphas forge 2→9) · #259 requalifié (Docker Hub
  fait, reste GHCR sans secret) · #312 recalé · audit ChatGPT trié (versions « incohérentes » FAUX,
  tailles/cycle de types/`as any` VRAIS) · audit `fable` du scope DI (exécuté, 12 preuves) → parent
  #481 + sous-tickets #482-#486, P0 beta · audit externe → #487 (cycle de types, beta), #488
  (découper Kernel.ts, backlog) · `a67fd3614` retrait du champ `types` du package.json racine.
- **Frictions** : (1) job Vitrine rouge 3 releases pour une surcharge `GITHUB_*` ignorée par la forge
  — versée au sas 🎯 ; (2) j'ai proposé de retirer le scope en comptant ses écritures au lieu de lire
  sa construction — versée 🎯 ; (3) commit refusé par commitlint (titre > 100) : le commit suivant a
  embarqué les fichiers indexés — rattrapé par un amend local non poussé ; (4) un audit externe a
  présenté comme priorité n°1 un fait périmé (versions) : trié par commande avant toute réponse.
- **Décisions** : on GARDE le scope prototypal (lecture héritée = lecture racine, 46,5 vs 47 ns) ; ALS
  = OÙ est le scope, prototype = CE QU'IL EST ; portée `request` lazy, captive dependency refusée ;
  Nodefony 7 sert l'INTENTION, jamais le code ; livrer avant 10.0.0 (API publique) ; la doc en fait le
  premier concept (métaphore du calque).
