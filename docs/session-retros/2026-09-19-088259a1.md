---
date: 2026-09-19
session_id: 088259a1-2897-49ca-a2f6-fb05b9af209c
focus: "#432 — retirer les dix pages d'instructions copiées dans chaque app, verser le contenu dans les skills livrés par npm"
---

# Session retro — 2026-09-19 — 088259a1

## Fait

- **#432 ouvert, traité et fermé** dans la séance. Trois commits : `fcc4685b` (retrait des dix
  pages `agents/nodefony/*.md`, 662 lignes, + versement), `fd41941e` (renommage
  `templates/app/agent-instructions/`), `7ac81bf1` (empreinte du tableau). Poussés sur `dev`.
- **Versé** dans `nodefony-dev` (479 → 497 l.) : inspection (`card`, `symbols`, `inspect`),
  commandes, pilotage du serveur, environnement et précédence `NF__`, données et fichiers
  (409/422, façades de flux), poids du modèle, pièges serveur (deux paquets `nodefony` dans un
  process, stockage de session synchrone). Dans `nodefony-protect-route` : `@CsrfProtect`, les
  docs installées, et le bloc `csrf` **qui avait perdu sa clé `secret`**.
- **Porte `AGENTS.md`** : budget 20 → 26 Ko, raisonnement écrit dans le test. Contenu remonté =
  le conditionnel (commandes des modules installés, briques absentes, docs pointées) **et** ce
  dont l'ignorance coûte cher (geste destructeur, CSRF, 409, comment chercher la doc).
- **Sonde retirée** : `a ouvert une annexe agents/nodefony/`, devenue insatisfiable.

## Coûts évidents

- **~9 min d'attente pure** : trois passes complètes de `format:scaffold` (4 apps installées,
  ~3 min chacune) pour trois décalages d'une ligne vide, alors que `--raw` répond en 5 s et
  couvre exactement le cas qui mord. L'option est écrite ligne 26 du script.
- **Un premier commit refusé** par le hook : le gate générait depuis un `dist` périmé, qui
  cherchait encore les gabarits supprimés. Rebuild du core, puis vert.
- Itérations sur le budget de la porte : ~10 cycles de resserrement à 25-250 o près avant de
  reconnaître que le plafond lui-même était l'arbitrage à rendre, pas le contenu à raboter.

## Recommandations

1. **Lire l'en-tête d'un script du dépôt AVANT de le lancer une deuxième fois** — les modes
   rapides y sont documentés (`--raw`, `--dry-run`). Versé en friction au sas.
2. **Après tout retrait, relire le NOM de ce qui reste** — `templates/app/agents/` a fabriqué
   une méprise chez le user. Thème neuf au sas.
3. **Avant de déplacer ou supprimer, lire les tests qui couvrent la zone POUR CE QU'ILS
   AFFIRMENT** : ici ils portaient les seuls faits mesurés qui rendaient mon tri faux. Thème neuf.

## Patterns récurrents (déjà gérés)

- ✅ Preuve sur l'artefact REÇU : le `prettier --check` sur l'app installée disait vert quand le
  rendu brut était faux — `feedback_prove_on_received_artifact` couvre déjà la règle.
- ✅ Gate vu mordre : appliqué deux fois (budget de la porte à 29 012 o ; chemin des gabarits
  débranché ⇒ `ENOENT`).
- ✅ Délégation : aucune. Les lectures tenaient sous le plancher de ~15 (12 fichiers), et
  l'édition ne se délègue pas.

## Commits produits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `fcc4685b` | refactor(scaffold): cesser de copier dix pages d'instructions dans chaque app |
| `fd41941e` | refactor(scaffold): nommer le dossier de gabarits d'après ce qu'il produit    |
| `7ac81bf1` | chore(pilotage): rafraîchir l'empreinte du tableau                            |
