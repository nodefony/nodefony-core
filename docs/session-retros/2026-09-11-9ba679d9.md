---
date: 2026-09-11
session_id: 9ba679d9-534b-47d9-850c-064cb7916ba0
focus: consolidation du sas, frise au lint de pilotage, sauvegarde de ~/.claude, CI remise au vert
---

# Session retro — 2026-09-11 — 9ba679d9

Session d'entretien, pas de feature : consolidation, outillage de pilotage, durabilité.
Aucun ticket du jalon pris — #316 reste la prochaine étape.

## Fait

- **CONSOLIDATE** (rapport `CONSOLIDATION-2026-09-11.md`) : sas 695 → 458 lignes, 59 → 29
  frictions. Gradués : 🪟 « un contrôle VERT qui ne POUVAIT rien voir » (21) →
  `feedback_gate_must_bite` ; 🧾 « un rituel de pilotage qui coûte plus qu'il ne rend » (5) →
  mémoire neuve `feedback_ritual_must_earn_its_keep` ; 🕳️ « déclarer ABSENT ce qu'on n'a pas
  cherché » (5) → `feedback_capability_unreachable_is_absent`. Versés : 🚪 fast-path (2) →
  `feedback_single_source_rule`, le `✓` imprimé → `feedback_suspect_instrument_and_own_diff`.
  9 retex bruts archivés. `--recos` lancé : 30/36 mémoires, 17/28 skills — décision de n'en
  créer aucun.
- **`ticket:lint` voit la frise** (`0fba6019`) : les champs `Début`/`Cible` n'étaient pas lus.
  Contrôles FRISE-DECALEE (départ à plus de 5 jours d'aujourd'hui, dans les DEUX sens),
  CIBLE-AVANT-DEBUT, FRISE-A-TROUS (muet sur un jalon pas encore planifié). Quatre cas vus
  rouges par débranchement ; #349 et #352 datés dans la foulée.
- **Sauvegarde de `~/.claude`** : le `.git` du dépôt privé est remonté à la racine — le
  `CLAUDE.md` global, `settings.json` et la ligne d'état n'étaient sauvegardés nulle part.
  `.gitignore` en liste blanche (les ~1 Go de transcripts restent dehors), `README.md` qui
  remonte un POSTE en six étapes (outillage relevé sur la machine), et le banc Dolibarr —
  ignoré par le dépôt public, donc nulle part — mis à l'abri.
- **CI remise au vert** (`f678aa8d`) : les huit jobs échouaient sur un seul cas.

## Coûts évidents

- La CI était rouge depuis `03ae2150` (la veille au soir) sans que personne le voie : le test
  n'avait pas été relancé après le dernier correctif du profil `edge`.
- Deux allers-retours perdus à affirmer des trous de pilotage qui n'existaient pas (thème 🕳️,
  gradué le jour même — la leçon a été payée dans la séance où elle était écrite).
- Un hook a refusé une commande à cause du TEXTE d'un heredoc (mots `push`/`-f` dans un README) :
  fichier non écrit, geste à refaire par l'outil d'édition.

## Recommandations

1. **Relancer `create.test.ts` après toute modification d'un gabarit d'application** — c'est la
   suite que les correctifs de scaffold cassent, et elle ne tourne pas au pre-commit.
2. **L'empreinte `.ai/board.json` ne porte pas les dates** : une frise décalée reste invisible
   à qui lit le tableau hors ligne. Trou nommé, non comblé.
3. Le `.gitignore` de `~/.claude` est une liste BLANCHE : tout nouveau fichier de configuration
   à sauvegarder demande une ligne — sinon il ne partira jamais, silencieusement.

## Patterns récurrents (déjà gérés)

- ✅ Débrancher avant de croire un test neuf (fait 2× : contrôles de frise, découpage du compose).
- ✅ Suspecter son propre diff avant d'imputer un rouge — ici la CI était rouge AVANT mes commits,
  vérifié sur l'historique des runs plutôt que supposé.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `d836961f` | chore(retex): consolider le sas — graduer 2 thèmes, archiver 9 retex |
| `0fba6019` | feat(pilotage): faire voir au lint la frise du tableau de bord       |
| `241782cd` | docs(session): la sauvegarde porte tout ~/.claude                    |
| `f678aa8d` | fix(test): découper un bloc compose sur son INDENTATION              |
