---
date: 2026-06-06
session_id: a9231d78-a986-4ba7-8faf-0941cd41fd17
focus: POC Studio Workspace — bureau composable + solution cluster + grille 2D + 16 widgets + suppression Dashboard Dev
---

# Session retro — 2026-06-06 — a9231d78

## Focus

POC « Nodefony Workspace » : transformer les dashboards figés en **bureau composable de widgets**
(1 sonde = 1 widget) sur branche `refactor/studio-workspace`. Demandé en cours de route : appliquer le
pattern console Logs, soigner la vue cluster, widgets « beaucoup mieux », drag + resize, supprimer le
Dashboard Dev (migré).

## Fait — 5 commits (branche refactor/studio-workspace, NON pushée, ahead 5)

- **81b8fcf** L0/L1/L2 : `IWidgetDef` + registry + `WorkspaceStore` (presets+persistance) + `WidgetHost`
  (pattern sonde+hub encodé 1×) + **`ClusterView`** (solution cluster>1 : `realtime:health` master +
  `normalize` + résumé pod / grille worker dépliable) + 9 widgets + route `/nodefony/workspace` + doc
  figée `studio/docs/workspace.md` (+ charte mouvement « temps réel calme »).
- **3bd28fd** widgets RICHES : `useLiveSeries`/`useRate`, `BigMetric` (grande valeur flash + MiniChart),
  anneau santé (`RingProgress`), ORM req/s, hub fan-out/s, **logs refait** (`toRecord`/`ansiToReact`/
  `SeverityBadge`), drag&drop.
- **13e48f9** grille header-drag + resize + 7 widgets (cluster.workers, runtime.modes/vite, system.git/
  uptime, realtime.channels, supervision.alerts) → **16 widgets** ; presets dev/superviseur enrichis.
- **0ae39cc** **suppression Dashboard Dev** (tout migré) + redirection `/dev`→`/workspace` ; Supervision
  CONSERVÉE (demande user).
- **fc25d48** **grille 2D dense** (`auto-flow: dense`, span colonnes×rangées) + **resize coin**
  largeur+hauteur + fantôme de drag.
- Gates : `typecheck` 0 à chaque lot ; serveur dev UP + transform Vite des 18 fichiers = 200.

## Frictions (→ RETEX.md)

- react-grid-layout INCOMPATIBLE React 19 (`findDOMNode` supprimé) → grille maison dense 0-dep.
- `syslog:stream` pousse `{logs,dropped}` coalescé (pas un Pdu) → réutiliser les vraies briques logs.
- commitlint `subject-case` (sujet minuscule) — **6× vu, DÛ pour graduation**.
- valider esbuild (`curl @fs` Vite 5173) avant hard-reload user.

## Décision structurante (survit au /clear)

Le Workspace = couche **DONNÉES** (1 sonde = 1 widget). Le mode héros **« Jumeau Vivant » (L3)** = une
nouvelle **SURFACE** qui consomme les MÊMES sondes en **espace + mouvement**. Réutilise **`FlowGraph` +
`liveNodeData`** (DÉJÀ livrés) + `realtime:health` (servi) → **faisable, pas du rêve**. Le user veut LE VOIR.

## Commits

| Commit  | Sujet                                           |
| ------- | ----------------------------------------------- |
| 81b8fcf | poc workspace L0-L1-L2 (bureau + cluster)       |
| 3bd28fd | widgets riches (courbes/jauge/logs) + drag&drop |
| 13e48f9 | grille draggable/resizable + 7 widgets          |
| 0ae39cc | suppression Dashboard Dev + redirection /dev    |
| fc25d48 | grille 2D dense + resize coin (largeur+hauteur) |
