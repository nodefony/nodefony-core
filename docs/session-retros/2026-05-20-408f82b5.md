---
date: 2026-05-20
session_id: 408f82b5-85f4-4f04-96be-f312918d0f75
focus: Studio — routing /nodefony tranché + realtime WebSocket (JSON-RPC pub/sub) + dashboard live (graphes SVG) + instanceId ; stress tests + analyse multi-process
---

# Session retro — 2026-05-20 — 408f82b5

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Edit | 91 |
| Bash | 85 |
| Read | 28 |
| TaskUpdate | 20 |
| Write | 11 |
| TaskCreate | 10 |
| Skill | 3 |
| AskUserQuestion | 3 |
| ToolSearch / browser | 2 / 2 |

Transcript : 1185 lignes / 4,0 MB. Tool results : 256 events, ~211k chars (avg 823).

## Top fichiers Edit
| Fichier | × |
| ------- | -: |
| studio/CLAUDE.md | 14 |
| Dashboard.tsx | 13 |
| StudioController.ts | 11 |
| studio/MEMORY.md | 10 |
| realtime/providers.ts | 6 |
| StudioRealtimeController.ts | 5 |
| MIGRATION_STATUS.md | 5 |

## Coûts évidents
- **Cycle stop→build→start ×7-8** (start.sh ×10, stop.sh ×8, `npm run build` ×7) = le coût dominant. Chaque restart backend = boot + Vite + watch ~30-40s. Plusieurs auraient pu être **fusionnés** (ex : fix métrique CPU + champ debug + instanceId = 3 restarts, 1 aurait suffi).
- **Dashboard.tsx édité 13×** : détour recharts → découverte que **recharts 2.x est cassé sous React 19** (defaultProps supprimés) → réécriture en SVG-maison. ~3 edits + 1 restart perdus sur ce détour.
- **3 AskUserQuestion rejetées** d'affilée (user préfère que je décide + explique, pas du QCM sur décisions design).
- MCP browser (MCP_DOCKER) **indisponible** → diagnostic dashboard fait par logs/transpile au lieu du navigateur.

## Recommandations
1. **Batcher les edits backend avant rebuild+restart** — regrouper toutes les modifs d'un même fichier/feature serveur, puis 1 seul cycle. Gain : ~3-4 restarts/session.
2. **Mémoire `feedback_recharts_react19`** (ou note frontend module) : graphes Studio = **SVG-maison direct**, ne PAS retenter recharts/@mantine/charts sous React 19. Évite le détour la prochaine fois.
3. **Garder le réflexe `/@fs/<abs>` transpile-check** pour valider le front sans restart (utilisé efficacement ici, HMR) — à promouvoir comme pattern standard frontend.

## Patterns récurrents (déjà gérés)
- ✅ Skill `start-nodefony-server` (start/stop) utilisé partout.
- ✅ Décisions mémorisées : routing, realtime WS, multiprocess scaling, debug bar, lib cliente = Core isomorphe.
- ✅ Piège `cd` + start.sh relatif rencontré 1× → déjà en mémoire `feedback_cd_startsh_relative_path`.
- ✅ Gotcha WS push (`ctx.send` rejette après handshake → `ctx.connection` brute) mémorisé.

## Commits produits
| Commit | Sujet |
| ------ | ----- |
| ddee516 | feat(studio): realtime WS pub/sub + dashboard live + routing /nodefony |
| b5e104e | feat(studio): dashboard graphes SVG interactifs + aides utilisateur |
| 3de6dec | docs(roadmap): P10.5/P10.7/P10.8 🔶 |
