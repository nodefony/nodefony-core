---
date: 2026-05-21
session_id: 6a0c912d-c360-43f6-a955-95b87aedc3aa
focus: Realtime Studio (debug 0-msg → drawer + promotion Core) puis tests WS (stress 2 axes, traceparent, scaffold Studio, skill load-test)
---

# Session retro — 2026-05-21 — 6a0c912d

## Tool usage
| Outil | Calls |
| ----- | ----: |
| Bash | 124 |
| Edit | 83 |
| Read | 73 |
| Write | 25 |
| TaskUpdate/Create | 21 |
| AskUserQuestion | 4 |
| Skill / ToolSearch | 4 |

335 tool_result, ~327k chars (avg 974/event).

## Top fichiers Read/Edit
| Fichier | Read | Edit/Write |
| ------- | ---: | ---: |
| studio `ConnectionStore.ts` | 25 | 25 |
| studio `ConnectionDrawer.tsx` | 8 | 14 |
| `ws-connections-load.test.ts` | 2 | 7 |
| core `RealtimeClient.ts` | — | 5 |
| studio `Logs.tsx` | 3 | 3 |

## Coûts évidents
- **Boucle realtime « 0 msg » = le gros poste** : `ConnectionStore.ts` lu+édité 25× chacun. 3 causes empilées (MobX proxy des valeurs de Map / compute débit en interval React / **Vite prébundle périmé**) → ~10 itérations « curl verify served bundle » + reload + réouverture d'onglet avant de trouver. Toutes déjà mémorisées (`feedback_studio_realtime_stats_mobx`).
- **Commits FR à apostrophes** : `/tmp/commit-msg.txt` réécrit 4× + 1 commit rejeté (apostrophe dans string single-quote shell : « lévent », « à lécran »). Friction RÉCURRENTE (déjà vue session précédente). → nouvelle mémoire créée.
- **Sonde rupture sous-estimait (4741)** : 1 run mocha + 1 run script + 1 fix + 1 re-run pour trouver/corriger le vrai plafond 16372. Coût justifié (a révélé un vrai bug de mesure).

## Recommandations
1. **✅ Mémoire créée `feedback_commit_fr_apostrophes`** — toujours `git commit -F` (fichier/heredoc) pour les messages FR, jamais `-m "...'..."`. Évite 4 réécritures/session.
2. **Vite prébundle staleness** — déjà en mémoire mais coûte cher à chaque session frontend ; envisager un flag du skill `start-nodefony-server` qui purge `node_modules/.vite` des consumers au boot (`--fresh-front`). À valider en consolidation.
3. **Debug front sans navigateur** — la boucle « curl verify served » est lente ; le marqueur de version (badge `v4`) reste le bon réflexe (déjà mémorisé). RAS.

## Patterns récurrents (déjà gérés)
- ✅ MobX proxy / sampler store / Vite prébundle → `feedback_studio_realtime_stats_mobx`
- ✅ Stress WS limites + 2 axes → `project_ws_stress_studio_lag` + skill `load-test`
- ✅ Tests unit = vitest source pure / charge+intégration = mocha live-server (split documenté)
- ✅ Ouvrir WS par batches (sinon AggregateError) → SKILL load-test + test

## Commits produits
| Commit | Sujet |
| ------ | ----- |
| f6f10c5 | coverage vitest @nodefony/frontend |
| f82b3de | fix lag Studio (coalescing syslog + session WS) |
| 3003ec2 | realtime hub — compteurs/débit par abonnement + drawer |
| a250262 | stats realtime promues dans le Core RealtimeClient |
| 62dd415 | stress WS 2 axes + propagation traceparent WS |
| 21674c4 | scaffold tests vitest Studio (providers realtime) |
| 750c4f5 | skill load-test (mocha + scripts client) |
| 4184528 | fix sonde rupture → plafond réel 16372 |
