---
date: 2026-08-22
session_id: c33870da-6b8e-4eb2-a346-eab68f1355ed
focus: Lot 1 du câblage agents — la table remonte au cœur, la porte se déclare par la CLI de chaque agent
---

# Session retro — 2026-08-22 — c33870da

## Fait

- **`AGENT_TARGETS` remontée au CŒUR** (`src/nodefony/src/cli/agentTargets.ts`) : la table, les
  fonctions pures (`poseVariable`, `porteDejaLaCle`, `agentsDemandes`) et la résolution de racine
  (`racineAgent`) quittent `@nodefony/security` — `ai:mcp`/`create app` sont des commandes du cœur.
  `security-token.ts` les consomme, `#racineDe` n'est plus une copie.
- **`nodefony ai:mcp --agent <liste|all|none> [--remove]`** : déclare la porte chez chaque agent en
  appelant SA CLI (`planAgentDeclaration` pur + `declarerChezAgents`), `shell: false` pour que
  `${NF_MCP_TOKEN}` arrive littéral. Repli affiché quand la CLI manque (`ENOENT` constaté).
- **« Aucun agent » est un choix explicite** (demande du user en cours de session) : case à cocher
  **rien de coché**, message dédié, `none` accepté, rien hors TTY.
- **Quatre défauts sortis du terrain**, tous muets côté code de sortie : `gemini mcp remove`
  exit 0 sans effet · CLI lancée depuis le cwd de l'appelant · mon `--dry-run` annonçait une autre
  URL que l'exécution · le rendu disait encore « écrit dans `.env` ».
- **`.gemini/.env` gitignoré**, ici et dans le gabarit `create app`.
- Doc : section dédiée dans `src/nodefony/src/cli/CLAUDE.md`.

## Coûts évidents

- **Trois rebuilds du cœur** pour prouver au disque (inévitable : la preuve porte sur l'artefact
  reçu, pas sur la source).
- **Un aller-retour de doc à réécrire** : mon TSDoc sur la portée Codex/Vibe était faux, il a fallu
  le refaire après le recontrôle demandé par le user. Coût direct d'une conclusion tirée d'une
  absence.

## Recommandations

1. **La sonde d'un tiers se conçoit sur la variable qu'on croit mesurer** — deuxième fois en deux
   jours sur le MÊME agent (Codex). Candidat à graduation si ça revient.
2. **Lire ≠ écrire, pour tout outil tiers** : son `--help` décrit ce qu'il écrit ; seul son
   source/binaire dit ce qu'il lit. Renforce `feedback_source_over_memory`.
3. **`tmp/mcp-audit.md` du user** (7 défauts du pont MCP, D1→D7) reste entier — ordre d'attaque
   donné dans le document, à traiter en prochaine session.

## Patterns récurrents (déjà gérés)

- ✅ Test neuf vu ROUGE (6 débranchements joués, preuve du sabotage par `diff` — `git diff` est
  aveugle sur un fichier non suivi).
- ✅ Preuve sur l'artefact reçu (rebuild puis exécution réelle, lecture du fichier écrit).
- ✅ Gate du dépôt qui mord : `standaloneOptions.test.ts` a refusé `--agent`/`--remove` invisibles
  dans l'aide.

## Non lancé / hors preuve

- Suites `http`, `framework`, `devkit`, gate mémoire, `test:all` : non lancées (aucun code de
  pipeline touché).
- Vérifiés : cœur **2960 verts**, security **1019 verts**, typecheck des deux, prettier, oxlint.

## Commits produits

| Commit     | Sujet                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------- |
| `5c4771c1` | feat(cli): la porte se déclare CHEZ l'agent, par SA CLI — et « aucun agent » est un choix |
