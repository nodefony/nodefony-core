---
date: 2026-07-30
session_id: 62cccef5-ce61-4bf8-abcb-455e735e3989
focus: banc devkit — mesurer le paramètre de route (soupçon faux) puis l'état par visiteur ; `check` devient aussi `doctor`
---

# Session retro — 2026-07-30 — 62cccef5

## Ce qui a été fait

| Commit     | Objet                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------- |
| `e4477bbc` | `check` attrape `:id` (`route-colon-param`) · alias **`doctor`** · alias visibles dans `-h` |
| `79df93ef` | tâche 15 du banc — paramètre de route, juge à 6 causes + son selftest                       |
| `9b299570` | le gabarit `hello` MONTRE une route à segment variable                                      |
| `34cfc613` | tâche 16 — état par visiteur + preuve d'intention ; 4 selftests en CI                       |
| `25379452` | le juge de T16 DEMANDE ses routes ; retrait d'une règle `check` fausse                      |

## Les deux mesures

- **T15 (paramètre de route)** : 3 runs, **PASS 3/3**, médiane **33 tours** (25 · 33 · 37) — la
  plus basse du banc. Le soupçon d'un trou, hérité de deux observations faites dans d'AUTRES
  tâches, était faux.
- **T16 (session + CSRF)** : **2/2 PASS** après correction du juge (57 tours, 0,68 $ pour l'agent
  neuf).

## Ce que les transcripts ont montré, et que le verdict cachait

Les 12 tours d'écart entre les runs de T15 ne viennent pas du hasard du modèle mais du **gabarit
choisi** : `--kind rest` contient `@Get("/{id}")` + `@Param("id")`, recopié depuis le diff que
l'agent vient de générer, sans ouvrir la moindre doc. `--kind hello` ne montrait rien → `:handle`
d'instinct, 404, puis build + boot + `inspect routes` + deux lectures de `routing.md`. D'où l'ajout
au gabarit `hello`, et **rien** dans l'`AGENTS.md` : la mesure ne le justifiait pas.

## Mes erreurs — toutes dans mes instruments, toutes vues par l'exécution

- **Règle `check` « surface réservée de `Service` » : 37 signalements sur du code qui compile.**
  Redéfinir un nom hérité est légal tant que la signature reste assignable. Retirée.
- **Juge de T16 : recalait un agent ayant fait juste** (il présumait d'où venait le jeton CSRF).
  5ᵉ occurrence du mode de défaillance n° 1 du banc, la 1ʳᵉ portant sur un TRAJET.
- **Première version de `route-colon-param` : accusait les 5 routes react-router de Studio.**
  Attrapée en lançant la commande sur le dépôt, pas en relisant la regex.
- **Mon banc d'épreuve accusait le juge de tout** — `spawnSync` bloquait la boucle du parent.
- **Sonde T16 aveugle à `new Map<string, string[]>()`** — le générique entre `Map` et la
  parenthèse ; trouvée par le selftest, avant tout agent.

## Ce qui n'a PAS été fait

- Priorités 1 (gros modèle vs petit, ~20 $) et 3 (superviseur Windows) : **non entamées**.
- Priorité 5 (surface réservée de `Service`) : **partielle** — et on sait maintenant qu'une règle
  `check` est structurellement impossible ; reste au choix un enrichissement du message TS2416.
- **La CI câblée n'a pas tourné sur un vrai runner** : prouvée en local seulement.
- Aucun run COMPLET des 16 tâches n'a jamais eu lieu (dette antérieure).

## Gates passés

`npm run typecheck` 24/24 · core **2357 tests verts** · sondes du banc **55/55**, amputation
vérifiée sur 55 · juges : 8/8 et 11/11 causes distinguées · `nodefony doctor` propre sur le dépôt.
