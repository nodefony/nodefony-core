---
date: 2026-08-06
session_id: 0ea2d552-b79b-4f69-9144-60b7d855f23f
focus: Session BANCS HTTP — banc durci, +8,9 % re-audité, enquête « régressions » → 2 bugs cursor corrigés
---

# Session retro — 2026-08-06 — 0ea2d552

## Fait

- Banc A/B durci (`d779f294`) : warmup non compté, refus dispersion > 3 %, thermal, purge, LC_ALL=C.
- Re-mesure cumulée A→D : **+8,9 % [7,7–10,1]** — remplace les annonces par lot.
- Comparatif frameworks (`5ebb280d`) : Express ×2,23 → ×1,61 · capacity prod +41 % http vs 14/07.
- Re-profil : postes attaqués tombés ; **goulot n°1 = scope DI 22,4 %** → session D4/contexte.
- Enquête rouges test:all → **2 bugs runtime pré-existants** : `cursor` perdu par `sessions/list`
  (`00ad1a6c`) et `sessions/mine` (`7802b2bd`) — pagination bouclant sur la page 1 en SCAN Redis.
  Bancs de test paginés (`9890e560`). test:all clôture : **7 842 verts, 0 rouge**.
- Rapport HTML : `tmp/bancs-2026-08-05.html` (photo, données embarquées).

## Coûts évidents

- 3 séries A/B refusées avant de comprendre la rampe thermique PUIS l'App Nap (mon propre cooldown
  fabriquait le biais qu'il combattait) — ~30 min.
- Enquête « régressions » : 7 runs test:all au total ; 3 fausses pistes (pagination sqlite,
  identité session.user, accumulation redis) avant la sonde qui a montré le cursor immobile.
  Les artefacts d'environnement (serveur test:all resté up, Docker zombie) ont fabriqué 2 faux
  symptômes qui ont coûté ~40 min de qualification.
- Docker quitté brutalement pendant conteneurs actifs (mon geste) → forwarders zombies → tout
  diagnostic suivant pollué.

## Recommandations

1. Purge périodique des stores de test (sessions + audit_event) — hook ou script d'hygiène.
2. Session perf dédiée D4/scope DI (22,4 % du CPU utile, design d'abord).
3. Dette produit à trancher : un listing filtré par user sur SCAN Redis rend des pages quasi vides
   (filtre APRÈS pagination) — index par user ou tri, à peser avant la console Sessions à l'échelle.

## Patterns récurrents (déjà gérés)

- ✅ Vert de cache turbo sans `--force` (mémoire `feedback_turbo_cache_stale_logs`).
- ✅ Cwd qui dérive entre appels Bash (`feedback_bash_cwd_drift`) — encore mordu 2× (run 7 lancé
  depuis le workspace redis).
- ✅ `index.lock` résiduel (`feedback_git_index_lock`).

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `d779f294` | fix(bench): le banc A/B refuse une serie dispersee et survit aux pauses |
| `5ebb280d` | fix(bench): le comparatif frameworks porte les memes gardes             |
| `9890e560` | fix(http): le banc de revocation lit TOUTES les pages du listing        |
| `00ad1a6c` | fix(http): la pagination a curseur du listing de sessions avance enfin  |
| `7802b2bd` | fix(http): sessions/mine transmet aussi le cursor, les bancs paginent   |
