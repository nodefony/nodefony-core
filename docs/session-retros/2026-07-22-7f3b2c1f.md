---
date: 2026-07-22
session_id: 7f3b2c1f-cba9-4d5e-827b-8aa3347109d8
focus: Durcissement realtime — file d'envoi bornée, fan-out mutualisé, plancher des canaux de plateforme ; cadrage Kafka ; kit namespace `nodefony:`
---

# Session retro — 2026-07-22 — 7f3b2c1f

## Fait

- **File d'envoi du backplane bornée** (`0e288bf7`) — `backplane.maxQueueBytes` (8 MiB par défaut),
  compteurs dans la carte d'identité (sonde + Studio), alerte de transition, `unhandledRejection`
  fermé au passage. Prouvé au banc (`27f10e1d`) : pic **3 231 MB → 387 MB**, à perte de messages
  identique.
- **Fan-out mutualisé** (`6abd3538`) — une frame diffusée n'est plus sérialisée qu'une fois pour N
  abonnés. Étage 26× à 62× moins cher selon la taille de charge.
- **Plancher des canaux de plateforme** (`f78b68ac`, F82 cas 2) — sans `@nodefony/security`, les
  namespaces réservés sont fermés aux connexions clientes, le refus est dit au client et compté ;
  la liste des namespaces devient la propriété du hub, consommée par security.
- **Kafka cadré** (`d080b897`) — deux jalons (driver seul, puis module d'infra avec déclencheur).
- **Kit `nodefony:`** (mémoire IA) — inventaire chiffré du renommage des canaux de sonde.

## Coûts évidents

- **Le banc a coûté cher pour rien sur S1** : montage, 3 séries de runs, un stash/rebuild
  aller-retour… pour finir sur un instrument incapable de trancher (variance ×3). Le micro-banc
  d'étage, écrit en 10 minutes, a donné la réponse. Aurait dû être le premier réflexe.
- **Dérive de `cwd` 3×** (soak lancé depuis la racine, vitest lancé sur tout le monorepo) — déjà
  gradué en `feedback_bash_cwd_drift`, continue de mordre.

## Recommandations

1. **Pour un gain d'étage, banc d'étage.** Le banc système sert à prouver un comportement
   (fan-out cross-pod, injection, mémoire sous rafale), pas à mesurer quelques dizaines de %.
2. **Rejouer le banc trouve des bugs DANS le banc** — 2ᵉ session consécutive : cette fois la rafale
   ignorait `?n=` (`context.query` au lieu de `request.query`) et mesurait 10 000× moins que demandé.
3. **Les questions du user ont produit deux trouvailles** que je n'aurais pas cherchées : les clés
   de session Redis sans cloison par application, et le namespace `nodefony:` manquant. Les
   « pourquoi ? » valent des audits.

## Patterns récurrents (déjà gérés)

- ✅ Tests-first (rouge vu d'abord) sur les trois chantiers — a changé le design du plancher
  (deux portes `subscribeClient`/`subscribe` au lieu d'une garde dans `subscribe`).
- ✅ Gate cassé exprès pour vérifier qu'il mord (borne retirée → 3 des 8 tests tombent).

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `0e288bf7` | fix(realtime) — borner la file d'envoi du backplane              |
| `d080b897` | docs(migration) — cadrer Kafka en deux jalons                    |
| `27f10e1d` | test(realtime) — prouver la borne au banc (3 231 MB → 387 MB)    |
| `6abd3538` | perf(realtime) — fan-out mutualisé (plan S1)                     |
| `f78b68ac` | fix(realtime,security) — plancher des canaux de plateforme (F82) |
