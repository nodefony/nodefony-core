---
date: 2026-06-20
session_id: 3d8df3ee-327c-4cdb-ae42-4412649e7779
focus: P6.14 audit lots 2b + 4 — émission hot-path + stream WS live (security:audit)
---

# Session retro — 2026-06-20 — 3d8df3ee

## Focus

P6.14 journal d'audit : **lot 2b** (émission hot-path firewall/WS/token/clés) + **lot 4**
(stream WS live `security:audit`). P6.14 ✅ COMPLET (lots 1-4). 3 commits poussés.

## Fait

- **Lot 2b** (`0d47555b`) — émission audit hot-path, invariant « succès MUET » : `Firewall.handleSecurity`
  (auth.failure/throttled/denied, helper `#recordAuth`), verrou WS `frame.denied` (`buildFrameAuthorizer({onDeny})`),
  `TokenService` token.issued/reuse_detected + login.failure/throttled grant, `ApiKeyService` apikey.created/revoked.
  +17 tests (banc `auditEmissionHotPath`, dont 0-émission-succès + câblage WS bout-en-bout).
- **Lot 4 infra** (`932f73fe`) — `RealtimeHub.registerSystemChannel` + fallback dans `subscribe` (réutilisable,
  découple un canal système de tout controller). +4 tests realtime.
- **Lot 4 sécu** (`c09ea73e`) — canal WS `security:audit` : `createAuditBridge` (coalescé lazy, calque
  `createSyslogBridge`), plancher `ROLE_NODEFONY_ADMIN` (`SECURITY_CHANNEL_POLICY`), câblé `Firewall.#wireRealtime`.
  +8 tests (banc `auditBridge`, dont câblage firewall→hub→bridge live).

## Frictions / leçons (→ RETEX.md section sécu/firewall)

- Audit hot-path = instrumenter la branche RARE (échec), pas le passage commun (succès) → gate mémoire intact.
- Découpler une feature transverse sans toucher les consommateurs = registre + fallback au point de jointure
  déjà traversé (`subscribe`), pas un hook par controller.
- Lot transverse realtime↔security : cartographier QUI monte l'endpoint AVANT de coder (longue exploration
  hub vs Studio ; un seul endpoint WS monté = Studio).
- Question archi du user en cours de dev = input de design (multi-tenant → plancher `ROLE_NODEFONY_ADMIN` + seam tenantId).

## Gates

typecheck 0 (security + realtime) · security **499/499** · realtime **294/294** · mémoire **9/9** (serveur live).

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `0d47555b` | feat(security): émission audit hot-path P6.14 (lot 2b)              |
| `932f73fe` | feat(realtime): registre de canal système (registerSystemChannel)   |
| `c09ea73e` | feat(security): stream WS live audit P6.14 (lot 4 — security:audit) |
