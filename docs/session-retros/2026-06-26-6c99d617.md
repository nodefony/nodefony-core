---
date: 2026-06-26
session_id: 6c99d617-c94b-44a0-bb3b-3b4422bd7225
focus: RedisIdempotencyStore — store d'idempotence distribué cross-pod (axe 3, P6.8)
---

# Session retro — 2026-06-26 — 6c99d617

## Focus

Axe 3 (stores distribués) de l'idempotence des mutations : premier backing
distribué (Redis), prouvé cross-worker contre un VRAI Redis.

## Fait

- **`b90cccc2`** — contrat `IIdempotencyStore` rendu **async** (union `T|Promise<T>`,
  Memory reste sync) + `RedisIdempotencyStore` (DANS framework, façon `RedisBackplane` :
  résout `redis` par nom structurellement, `SET NX PX`, fingerprint préservé au complete,
  dégradation gracieuse) + registre `idempotencyStoreRegistry` + driver `redis` builtin +
  config `idempotency.store` (knob `NF_IDEMPOTENCY_STORE`) + politique prod=fatal /
  dev=WARNING+fallback. Gates : framework 396 unit + 96 intég · mémoire 9/9 · typecheck 0.
- **`7578978d`** — route `@Idempotent` de démo (module test) + chargement à la demande de
  `@nodefony/redis` (when redis sélectionné) + 2 bancs e2e reproductibles.
- Preuves LIVE contre `redis:7-alpine` : store **6/6** (SET NX concurrent + cross-pod),
  e2e userland single-pod **5/5**, **cross-worker cluster -w2 4/4**.

## Frictions / leçons (→ RETEX.md)

- **Mauvais convention-frère d'abord** : copié `RedisTokenStore` (store dans redis, app-wired) ;
  user a corrigé → modèle `RedisBackplane` realtime (store chez le consommateur, résolution
  structurelle, registration dans le module, app SÉLECTIONNE seulement). Split sélection/connexion.
- **FakeRedis prouve la logique, pas la valeur de l'infra** : user a poussé pour du VRAI Redis →
  banc cross-worker différentiel (memory per-pod montrerait 2 exécutions, redis 1).
- **`throw` dans onKernelBoot = fail-SOFT en dev** (guard résilience Ph.3 l'avale → framework
  dégradé/ignoré) → try/catch + prod=rethrow / dev=WARNING+fallback. Prouvé LIVE.
- **Async sans régression hot path** : union `T|Promise<T>`, défaut Memory sync, court-circuit cold path.
- Gotchas banc : `timeout` absent macOS ; resolution `redis`/`tsx` hors repo ; cluster=prod (test
  dev-only + users non seedés → gate temp + NF_ADMIN_PASSWORD + NF_USER_STORE=memory, reverté).

## Commits produits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `b90cccc2` | feat(security): store idempotence Redis distribué + contrat async |
| `7578978d` | feat(security): banc démo @Idempotent + e2e Redis cross-worker    |
