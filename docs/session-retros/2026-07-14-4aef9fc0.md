---
date: 2026-07-14
session_id: 4aef9fc0-07c1-4a14-adae-d6686f198526
focus: Radiographie de la porte SOCKET (profil par frame) → puis skill de rapports HTML + banc de capacité mesuré en production
---

# Session retro — 2026-07-14 — 4aef9fc0

## Fait

- **Radiographie de la porte socket** (`53475239`) : l'unité profilée devient l'**invocation**, pas la
  connexion. `FrameProfile` (http) voyage dans l'ALS, satisfait structurellement le contrat que
  `Profiler.collect()` lit d'un contexte → **zéro ligne changée dans le Profiler**. `WebsocketContext`
  surcharge `phaseStart`/`phaseEnd` → le chemin HTTP n'est pas touché. L'id revient au client dans un
  champ `meta` frère du `result` (`RpcEnvelope`), et dans `RpcError.data` pour les refus.
  Effet de bord trouvé : le Resolver poussait `initialize` dans `Context.phases` **à chaque frame** →
  timeline cumulative _et_ croissance sans borne. Supprimé.
- **Fix repo** (`780dce5b`) : `.gitignore` avait `logs/` **non ancré** → masquait
  `studio/frontend/src/routes/logs/`. Tout nouveau composant de la page Logs serait resté hors commit
  (`git add <dossier>` saute un chemin ignoré **sans rien dire**). Ancré en `/logs/`.
- **Skill `nodefony-html-report`** (`1d516b79`, `871822c9`, `1cd64e3a`) : bibliothèque de rendu de
  rapports HTML autonomes (graphes SVG, tableaux triables/exportables, calculateurs, glisser-déposer,
  onglets, mode présentation, impression PDF, marque). **0 erreur / 0 avertissement au validateur W3C.**
  Specs W3C bundlées offline. Règle de livrable gravée dans le `CLAUDE.md` racine.
- **Banc de capacité** (`capacity.mjs`) : constantes d'un process (RAM/socket par régression, µs de
  boucle par message/livraison/requête, débits WS + h1/h1-TLS/h2) → rapport HTML avec **calculateur de
  pods**. Mode `--target studio` pour mesurer la **production**.

## Frictions (→ SAS `RETEX.md`)

- **`ps -o time` ne mesure pas la boucle** (tous les threads, GC inclus) → chiffre incohérent avec le
  débit observé. La bonne métrique est l'**ELU** échantillonnée pendant la charge.
- **Une mesure unique ne vaut rien** : « ws 35 % plus lent que wss » — absurde, invalidé à la 2ᵉ série.
- **Delta mémoire = bruit** : RSS par socket **négatif**. Régression sur paliers + R² publié.
- **Backtick dans un commentaire à l'intérieur d'un template literal** → casse le fichier. **3× dans la
  même session.**
- **Le module `test` est `policy: "dev"`** → un banc qui suppose ses routes ne peut mesurer que le dev.
- **Fixture de test qui ne porte pas le contrat réel** → 5 tests rouges qu'on peut prendre pour une
  régression du code.

## Mesures marquantes

| Mesure             | dev        | production                   |
| ------------------ | ---------- | ---------------------------- |
| RSS au repos       | 1 233 Mo   | **217 Mo**                   |
| HTTP h1 TLS        | ~2 000 rps | **4 902 rps**                |
| HTTP h2            | ~2 000 rps | **6 476 rps**                |
| Pont `api.request` | —          | **13 278 msg/s** (70 µs/msg) |

Porte HTTP vs porte SOCKET (même route, dev) : **2,55 ms vs 1,04 ms** — la socket ne paie ni `parse`,
ni `firewall`, ni `send` (donc pas `saveSession` : 2 requêtes SQL de session en moins), mais paie une
phase **`identity`** (0,35 ms) = re-validation Zero Trust du token à chaque frame, jusque-là invisible.

## Gates

core 1948 · http 597 unit + 576 intégration + **mémoire 9/9** · framework 440 · realtime 318 ·
typecheck Studio 0 · W3C 0/0.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `53475239` | La porte SOCKET se radiographie — un profil par frame            |
| `780dce5b` | `.gitignore` masquait les composants de la page Logs             |
| `1d516b79` | Skill rapports HTML + banc de capacité (calculateur de pods)     |
| `871822c9` | Numéros de page : Chrome 131 les implémente (ma doc se trompait) |
| `1cd64e3a` | Les rapports portent la marque Nodefony (en-tête + pied)         |
