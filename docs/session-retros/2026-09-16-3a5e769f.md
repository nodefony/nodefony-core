---
date: 2026-09-16
session_id: 3a5e769f-85a5-4980-b9b6-0f01d21516d6
focus: "La « fuite RSS » de #218 était un artefact de comptage de `rss` sous macOS — il n'y a pas de fuite"
---

# Session retro — 2026-09-16 — 3a5e769f

## Fait

- **Audit délégué en `fable`** (lecture + écriture de sondes, aucune mesure) sur le corpus
  existant : 90 échantillons versionnés, ticket #218, banc, sonde, pipeline. Rendu : les
  hypothèses classées avec leur sort, l'expérience minimale qui discrimine, et trois sondes
  prêtes. Index git intact, aucun fichier de `src/**` touché.
- **E1 — la mesure qui DÉSIGNE** (29 fenêtres, décor propre) : `rss` +78,9 MB/h (R² 0,979)
  pendant que `phys_footprint` tient à 164 MB (R² 0,487) et que les pages réutilisables montent
  de 33 à 76 MB (+73,6 MB/h, R² 0,973) — **93 % de la hausse**. `leaks` : 37 blocs,
  3 216 octets sur 20,7 M de requêtes. Localisé par la sortie brute de `footprint` dans la
  colonne _Reclaimable_ de `MALLOC_MEDIUM`.
- **Banc corrigé** (`ab18d034`) — quatre défauts : relève `phys_footprint`/`VmRSS` par fenêtre
  avec les couches Dirty/Clean/Reclaimable et FONDE son verdict dessus ; nomme l'artefact quand
  les deux compteurs divergent ; enregistre le Node du SERVEUR ; calcule l'érosion du débit sur
  des médianes de tiers (le `-6,4 %` publié comparait le MAXIMUM du run à son dernier point —
  il vaut `-1,9 %`).
- **Vu mordre** : le run de validation de 14 min satisfaisait les trois conditions de l'ancien
  verdict (R² 0,94, amplitude 19 MB, aucun plateau) et aurait rendu `rss-growth` ; il rend
  `clean` + `ARTEFACT DE COMPTAGE`. Seul le compteur jugé a changé.
- **Pages publiques recalées** : `docs/performance/index.md`, README racine, les trois verdicts
  du site qualité, et le générateur `prod-readiness-report.mjs`. Le jeu versionné passe en
  `verdict: "non-conclusif"` avec son motif.
- **Trois figures ECharts** qui portent la démonstration : aires EMPILÉES (de quoi le résident
  est fait), cascade (le seul poste positif est celui qui ne coûte rien), boîtes à moustaches
  (trois tiers qui se recouvrent ⇒ pas d'érosion). `lines()` gagne l'empilement.
- **#218 fermé** avec compte rendu complet.
- **Le site publié repris à l'écran** (`fb3cc40e`, `5276a1ac`, `596ff061`) — quatre défauts
  CONSTATÉS au navigateur, aucun par un contrôle : page d'accueil en pleine largeur
  (`h1` 1372 px → 441, paragraphe → 746), **aucune coloration syntaxique** (Shiki, à la
  génération, zéro JS servi), le sommaire `/performance/` qui **ne liait pas `latest/`**, et la
  page de mesures **découpée en cinq** avec navigation persistante (tenue dans la durée :
  ~5 000 px → 2 400). Plus le doublon signalé par le user : la page injectait le README ENTIER
  **et** écrivait quinze sections calculées — sept sujets en double, un titre identique.

## Coûts évidents

- **Un soak Linux de 90 min lancé sur un job plafonné à 60** → annulé, 60 min de forge perdues.
- Deux affirmations à corriger devant le user dans la même session (les échantillons « dessinés
  nulle part », « pas d'érosion ») — les deux par un périmètre trop étroit, pas par une erreur
  de mesure.
- Trois soaks locaux enchaînés (30 + 14 min) pendant lesquels rien d'autre ne pouvait tourner
  sur la machine sans polluer la mesure.

## Recommandations

1. **Rejouer un soak long avec le banc corrigé** pour remplacer le jeu versionné
   `non-conclusif` — c'est le seul point du « Fini quand » de #218 qui reste ouvert.
2. **Relever `timeout-minutes` de `soak.yml`**, ou refuser une entrée `minutes` qui dépasse le
   plafond du job : aujourd'hui l'automate accepte une consigne qu'il ne peut pas tenir.
3. **Lire les artefacts que la forge produit déjà.** Quatre runs Linux disaient le contraire du
   ticket P0 en cours, et aucun n'avait été ouvert.
4. **Publier `docs/performance/methode.md` et `analyses.md` dans le site de doc** — le lien
   « protocole » sort du site vers GitHub faute d'une cible interne. Demandé par le user, non fait.
5. **Un gate anti-doublon de CONTENU** entre un document injecté et des sections écrites : celui
   posé ne couvre que la page d'accueil, et c'est un lecteur qui a trouvé le défaut.

## Patterns récurrents (déjà gérés)

- ✅ Décor contrôlé avant mesure (secteur, `lowpowermode`, Docker, purge par PORT) — a évité
  deux mesures fausses d'emblée.
- ✅ Délégation avec interdits écrits en toutes lettres (index git, code produit) — respectés.
- ✅ Instrument éprouvé sur la sortie réelle avant usage (parseur `footprint`).

## Commits produits

| Commit     | Sujet                                                                                 |
| ---------- | ------------------------------------------------------------------------------------- |
| `9b1a19c2` | chore(pilotage): rafraîchir l'empreinte du tableau à la reprise                       |
| `ab18d034` | fix(perf): mesurer l'empreinte du noyau, et cesser de publier une fuite inexistante   |
| `fb3cc40e` | feat(site): colorer le code, mesurer la colonne de lecture, conduire vers les mesures |
| `5276a1ac` | feat(perf): découper le dossier de mesures en cinq pages, et borner sa largeur        |
| `596ff061` | fix(site): cesser de dire deux fois la même chose sur la page d'accueil               |
