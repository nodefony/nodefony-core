---
date: 2026-07-10
session_id: ba06fafd-0ff4-4928-9840-020193b20869
focus: Chantier CLI (P8/P11) — audit points d'arrêt, filet P11.1, refacto parse-pur, détacheur natif
---

# Session retro — 2026-07-10 (5ᵉ) — ba06fafd

## Fait

1. **Audit CLI** : mécanisme « point d'arrêt par phase » (kernelEvent + bitmask) validé —
   plus fin que le legacy JS (comparatif code à l'appui : legacy = flag binaire
   CONSOLE/SERVER + un seul arrêt post-onReady). Verdict tranché : garder, corriger 3 défauts.
2. `0b883854` — filet P11.1 renforcé (typo=64, happy-path module, onReady, status 0-Kernel).
3. `4a7d46ae` — refacto parse-pur : `resolveCommand` point unique + `runProfile` déclaratif
   (commande de module SERVER possible) + `finishOrPark` uniforme.
4. `be9ca8b2` — lancement détaché natif `--detach/--wait/--health` (volet F kit DX) ;
   `start.sh` aminci en wrapper. Fin STRUCTURELLE des faux TIMEOUT.

## Frictions / leçons (→ RETEX.md thème « CLI / tests e2e process »)

- e2e binaire validé sur dist périmé (rebuild AVANT l'e2e).
- Sortie vitest finit par ~40 lignes blanches → `tail` aveugle, grep sur fichier.
- Mémoire de dette périmée côté « déjà réglé » (double-boot corrigé par `e51af263`).
- cwd bash dérive entre appels → script npm lancé du mauvais workspace (1×).

## Efficacité

- Faux TIMEOUT start.sh vécu EN DIRECT pendant la gate mémoire → argument massue du volet F,
  livré 2 h après. Boucle courte demande user → preuve → feature.
- 3 commits verts du premier coup sur leurs suites (gates systématiques avant commit).

## Lot 2 (après 1ʳᵉ clôture) — complétion shell

- Complétion bash/zsh/fish : `completion [install|uninstall]` + `__complete` (fast-path
  standalone 0-boot) + **manifest cache** écrit au boot dev (commandes de module incluses).
- 2 bugs zsh trouvés par le RETOUR USER (TAB listait les fichiers), invisibles à `zsh -n` :
  offset `:2` (0-based !) + compdef sans compinit → harnais zsh réels ajoutés (→ RETEX.md).
- `install` = bloc rc marqué idempotent/réversible (pattern conda init), prouvé sur HOME jetable.

## Follow-ups

- ⏱️ `#ensureBuilt` (vérif turbo) ~45 s à CHAQUE boot même cache chaud → investiguer.
- Commandes métier P11.2/11.3 (`http:*`, `security:*`, `user:*`) sur le socle propre.
- Complétion : descriptions riches zsh (`compadd -d`) = bonus différé.

## Commits produits

| Commit     | Sujet                                                |
| ---------- | ---------------------------------------------------- |
| `0b883854` | test(cli): filet P11.1 renforcé — points d'arrêt e2e |
| `4a7d46ae` | refactor(cli): parse pur + resolveCommand            |
| `be9ca8b2` | feat(cli): lancement détaché natif (volet F)         |
| `c1ee5b5e` | feat(cli): complétion shell — manifest cache         |
| `12c2a773` | feat(cli): complétion — résolution binaire au TAB    |
| `55ed499a` | fix(cli): zsh offset :1 + auto-compinit              |
| `87c9452d` | feat(cli): completion install/uninstall              |
