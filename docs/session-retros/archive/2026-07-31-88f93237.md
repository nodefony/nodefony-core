---
date: 2026-07-31
session_id: 88f93237-f3a9-4ed2-8146-0905d765044f
focus: classer les 68 causes des juges du banc (à qui le rouge est opposable), puis jouer T22 en 3 runs
---

# Session retro — 2026-07-31 — 88f93237

## Fait

- **68 causes des juges classées** (`scripts/lib/imputation.mjs`) — 48 agent · 2 décor ·
  18 indéterminées — et **câblées** : `runGates` fige cause + imputation, un rouge non opposable
  rend la tâche NON JUGEABLE au lieu de FAIL. Les runs d'avant se relisent par la ligne `CAUSE=`
  restée dans l'`evidence`. `imputation.selftest.mjs` referme les deux sens (cause émise jamais
  classée / entrée classée plus jamais émise) en scannant `lib/`.
- **T22 jouée 3 fois** (décor isolé, haiku) : 1 PASS / 3 → FAIL instable, enregistré en référence.
  Les 4 sondes « ne pas affaiblir » sont vertes **3/3** ; les deux chutes viennent d'ailleurs
  (page non montée, un `any`).
- Deux corrections d'instrument : `juge-en-erreur` (une exception du juge sortait sous
  `aucune-reponse`) et le message de `page-absente` (proposait « pas rebâtie » alors que la gate
  rebâtit).

## Coûts évidents

- Le relevé délégué a manqué 7 sites sur 68 — recontrôle `rg` obligatoire, donc travail fait deux
  fois pour la LISTE (le contexte, lui, a servi).
- Un `sleep` posé après la fin du banc : attente pure, et le user a cru à un blocage.

## Recommandations

1. **L'automate établit la liste, le modèle rend le contexte** — ne jamais demander à un modèle un
   inventaire censé être exhaustif.
2. **Un diagnostic qui DISCULPE doit être plus dur à obtenir que celui qui doute** — d'où la
   dissymétrie DECOR / INDETERMINE.
3. Vérifier `pgrep` avant tout `sleep` d'attente ; le harnais notifie la fin.

## Patterns récurrents (déjà gérés)

- ✅ La garde anti-geste-git a mordu (2×) et sa conduite prescrite — committer d'abord — était la
  bonne.
- ✅ Le contrôle neuf a été vu ROUGE avant d'être cru (cause bidon → sortie 2 ; câblage débranché
  → 3 rouges au lieu de 2 + 1 écartée).

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `922911fc` | feat(bench): classer les causes des juges pour ne plus accuser au hasard  |
| `34f6f877` | docs(bench): dire a qui le rouge d un juge est opposable                  |
| `e77cc1fe` | test(bench): T22 jouee 3 fois — la garde tient, ce qui tombe est ailleurs |
