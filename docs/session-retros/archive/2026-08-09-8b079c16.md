---
date: 2026-08-09
session_id: 8b079c16-260b-4fda-8ea7-1992cb8e76f9
focus: Le refus de canal mesuré sur une app générée, puis status/stop rendus multi-projets (nommer, cibler, refuser, sonder) — Windows compris
---

# Session retro — 2026-08-09 — 8b079c16

Partie d'un décalage : la session précédente n'avait pas été clôturée, ses quatre commits étaient
orphelins. Poursuivie sur une remarque du user (« il fallait vérifier en ajoutant un user à une app
générée »), qui a ouvert tout le chantier `status`/`stop` multi-projets.

## Fait

- **`352db1fe`** — clôture rétroactive de 08-08e (retex, état de reprise, sas), et le `MIGRATION_STATUS`
  enrichi : les sondes devkit ne sont plus seulement écrites portables, elles sont EXÉCUTÉES ailleurs.
- **`4ed4b7d9`** — le refus de canal `realtime:denied` MESURÉ sur une application générée : le cas
  était sauté « faute d'un second compte », il manquait deux gestes d'utilisateur. 10 verts, 0 sauté ;
  débranchement vu rouge (compte autorisé à la place du refusé).
- **`493de57a`** — `status` NOMME les projets vivants, `stop <nom|chemin>` les prend comme cible.
  L'argument était jusque-là accepté puis IGNORÉ : lancé depuis le dépôt, `stop monapp` arrêtait le
  dépôt. Refus (sortie 1, rien de tué) sur cible inconnue, homonyme, ou combinée à `--all`.
- **`f444be9a`** — l'`AGENTS.md` généré apprend le cloisonnement à l'agent d'une app : « aucune
  instance » signifie « aucune à MOI », un refus a un code de sortie, `--all` emporte les voisins.
- **`26c3c2b5`** — le rendu répétait trois fois la même information, et annonçait « non sondés » des
  ports qu'il venait de sonder. Un bloc par projet, ports des voisins réellement sondés, résumé.
- **`f6ff1233`** — « aucune instance de CE PROJET » s'affichait là où il n'y a pas de projet ; la
  supposition était à quatre endroits.
- **`53628bc7`** — `--all` prouvait l'arrêt sur 2 ports quand il en libérait 4 ; et sous Windows la
  cible par nom niait le projet au lieu d'énoncer sa cécité. Grammaire de chemins injectable.

## Frictions

- **Un rendu qui s'AJOUTE sans retirer ce qu'il double embrouille** — refusé par le user, à raison.
- **Le rapport doutait de ce qu'il pouvait vérifier** (« déclarés, non sondés ») : le lecteur en
  conclut que le service est mort.
- **J'ai affirmé sur un écran, pas sur un test** — « tu es sûr de tout ça ??? ». Trois tests étaient
  rouges à cet instant, cassés par mes propres changements.
- **Windows n'a été regardé que sur demande du user**, et le trou était réel.
- **J'ai tué le serveur de dev du user deux fois** : une fois pour le test `--all` qu'il avait
  demandé, une fois par accident (`npm run build` du cœur = `rimraf dist` sous un superviseur qui
  watch). Relancé les deux fois.
- **Dérive de `cwd`** : deux commits refusés par le gate `check-package-deps`, qui cherchait un
  `dist` momentanément absent — diagnostic perdu à chercher au mauvais endroit.

## Ce qui a coûté

- Les allers-retours de RENDU (4 itérations sur `status`) : structure figée trop tard. Le
  mini-cahier des charges était écrit, mais après le premier jet.
- Deux commits rejoués (oxlint `no-shadow`, commitlint en-tête > 100 caractères).

## Non couvert, et il faut le dire

- `stop <nom>` ne peut pas FONCTIONNER sous Windows (il refuse proprement) : aucune source de `cwd`
  par pid n'y est disponible.
- Le chemin de `stop` qui TUE n'est pas couvert en vitest (`terminateDevProcesses` non injectable) —
  prouvé en réel seulement.
- framework (517) et studio (79) ont été rejoués APRÈS la feature, mais AVANT la refonte du rendu.

## Commits produits

| Commit     | Sujet                                                   |
| ---------- | ------------------------------------------------------- |
| `352db1fe` | clôture rétroactive 08-08e                              |
| `4ed4b7d9` | refus de canal mesuré sur app générée                   |
| `493de57a` | status nomme, stop cible                                |
| `f444be9a` | AGENTS.md généré : le cloisonnement                     |
| `26c3c2b5` | un bloc par projet, ports voisins sondés                |
| `f6ff1233` | plus de « ce projet » là où il n'y en a pas             |
| `53628bc7` | `--all` sonde tous les ports ; Windows énonce sa cécité |
