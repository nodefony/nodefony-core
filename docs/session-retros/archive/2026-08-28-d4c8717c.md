---
date: 2026-08-28
session_id: d4c8717c-2c49-4943-a814-4ef2ecce85d5
focus: #57 (tri mal formé refusé) puis #96 (migrations du framework générées et prouvées sur 3 dialectes)
---

# Session retro — 2026-08-28 — d4c8717c

Deux tickets fermés, cinq commits. Le second a coûté trois fois plus que son estimation d'une
journée — non pas en écriture, mais en mesures : presque chaque supposition sur `drizzle-kit`
s'est révélée fausse au premier contact.

## Coûts évidents

- **`test:all` lancé deux fois** (~5 min chacun) parce que j'ai pollué le premier run en travaillant
  sur la même base MariaDB. Coût direct, et coût indirect : j'ai cherché la cause dans mon diff.
- **Quatre sondes successives** avant de trouver comment faire lire un schéma à `drizzle-kit`
  (table nichée, chaîne d'imports, dossier absolu, condition `exports`). Aucune n'était évitable —
  la doc publiée ne documente rien de tout ça — mais elles se sont enchaînées une par une.
- **Deux réécritures complètes des scripts** : d'abord pour factoriser un `resolveDrizzleKitBin`
  dupliqué, puis pour passer de `.mjs` à `.ts` (le test les importe, et un `.mjs` sans déclaration
  donne un `any` implicite). La seconde était prévisible dès l'écriture du premier test.

## Recommandations

1. **Interdire toute commande sur une base réelle pendant qu'une suite tourne dessus** — la règle
   `fileParallelism: false` du module protège une passe contre elle-même, pas contre moi.
2. **Un outil tiers se sonde AVANT d'être câblé** : les quatre sondes de cette session ont chacune
   remplacé une supposition du document de conception, dont une qui aurait produit une migration
   vide et verte.
3. **Écrire les scripts de dépôt en TypeScript d'emblée** (`tsx`, comme `scripts/test-all.ts`) dès
   qu'un test doit les importer.

## Patterns récurrents (déjà gérés)

- ✅ Voir chaque garde mordre avant de la croire — appliqué neuf fois ici, et ça a payé : mon
  premier contrôle de dérive était inopérant.
- ✅ Suspecter son propre diff — appliqué, mais j'ai oublié le cas voisin : suspecter sa propre
  INTERFÉRENCE avec une suite en cours.
- ✅ Prouver sur l'artefact reçu — `npm pack` pour le contenu publié, pas le `package.json`.

## Commits produits

| Commit     | Sujet                                                                       |
| ---------- | --------------------------------------------------------------------------- |
| `32f2073d` | fix(orm): refuser une option de tri mal formée au lieu de l'ignorer — #57   |
| `4d162a95` | chore(docs): recaler les ancres de la doc ORM sur le code courant           |
| `4d25742d` | feat(orm): générer les migrations de schéma du framework — refs #96         |
| `dfeb8a14` | feat(orm): prouver la parité migrations ↔ développement sur trois dialectes |
| `819395ee` | docs(orm): recaler le CLAUDE.md du module                                   |
