---
date: 2026-06-25
session_id: 94d6626d-c62d-45e3-80f5-c9081533db71
focus: P6.8 — décorateur @Idempotent userland + contrat idempotence au core (S5)
---

# Session retro — 2026-06-25 — 94d6626d (S5)

## Focus

Étendre l'anti double-effet (S4 = mutations socket admin) aux controllers userland via
`@Idempotent`, et préparer les stores distribués en déplaçant le contrat au core.

## Fait

- **Axe 1** — `IIdempotencyStore`/`IdempotencyOutcome`/`IdempotentResponse` déplacés
  `@nodefony/framework` → `nodefony` (`src/types/`) ; façade re-export côté framework
  (imports internes intacts) → redis/drizzle pourront brancher un store distribué sans cycle.
- **Axe 2** — helper PARTAGÉ `framework/src/idempotency.ts` (`evaluateIdempotency` → verdict
  neutre `execute|guarded|replay|reject`) ; décorateur `@Idempotent({required?})` classe+méthode
  (strict 400 défaut / souple / WS toujours strict) → `RouteActionMeta.idempotent` ; seam
  `Resolver.callController` → `_callWithIdempotency` ; `AdminApiController.idempotencyGate`
  refactoré pour CONSOMMER le helper (admin + userland = 1 logique, 1 store).
- **Perf** (sur alerte user) : `meta` résolu 1× dans `callController` puis passé à
  `executeAction(data,reload,meta)` = 0 double résolution sur le hot path ; 0 alloc/lookup
  si `idempotent===null`. Prouvé : gate mémoire HTTP 9/9.
- Gates : framework unit 378 (idempotency 13 + seam userland 8 + actionMeta 5) + intég 96 ·
  mémoire 9/9 · banc admin live ws-data-plane-auth+attack 31/31 · typecheck 0.
- Docs : MEMORY framework + MIGRATION_STATUS (P6.8) + skill framework-dev **1.30.0**.

## Frictions (→ RETEX.md)

- **turbo `cache hit` trompeur après un edit POST-build** : `npm run build` a re-cache-hit framework
  alors que `Resolver.ts` avait changé → dist périmé silencieux. Fix : build module direct + `grep`
  le symbole attendu dans le dist (`metaArg`). [RETEX ⚙️ Build, 2×]
- **push + hook pre-push lourd** : foreground timeout 2 min harness (SIGTERM 143) ; background OK →
  vérifier `git status -sb` SANS "ahead", pas l'exit code. [RETEX 🔧 Git, 3×]
- `grep`/ugrep ne matchait pas `^#` du SKILL.md → `command grep` explicite.

## Leçons techniques (→ skill framework-dev §11 / changelog 1.30.0)

- Helper de **verdict neutre** pour partager une logique entre 2 call-sites de FORMAT différent.
- Ne JAMAIS double-résoudre `meta` sur le hot path (`callController`/`executeAction`).
- Contrat partagé → le CORE quand des modules hors-framework doivent l'implémenter.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `e2e6c1dd` | feat(security): décorateur @Idempotent userland + idempotence au core (P6.8) |
| `bacdde86` | docs(skill): @Idempotent userland (P6.8) — framework-dev 1.30.0              |
