---
date: 2026-06-01
session_id: 8b47ba7d-cc0b-4059-a27a-7cba7e5c030c
focus: Chantier CLI — fin du double-boot (1 seul Kernel) + commander 15 + filet + audit boot + 3 modes + Docker + splash dev-only
---

# Session retro — 2026-06-01 — 8b47ba7d

## Fait (8 commits poussés `…b05e381`)

- **A `55132c0`** filet d'intégration CLI (spawn réel) + trace boot-count `NODEFONY_KERNEL_TRACE_FILE`.
- **B `fb410bb`** bump commander 14→15 (ESM-only, 0 `--no-*` impacté).
- **C `e51af26`** **1 seul Kernel/process** (fin double-boot prod/cluster) : prod/cluster alignés sur
  recette dev (`kernelEvent:onPostReady` + `setType` conditionnel) ; `launchTopology` ne crée plus de
  Kernel ; bin `cluster`→production ; suppression fallback serveur legacy ; **hooks lifecycle tous modes**
  (PreStart/PreRegister/PreBoot/ServersReady/PostReady/Terminate, câblage lazy).
- **`faef194`** audit boot + outils `boot-bench`/`boot-profile`.
- **`4dbeafe`** banc 3 modes (server/batch/daemon : `test:batch`/`test:daemon`) + guide Docker cloud-native.
- **`684742e`** fix doc endpoint santé. **`e27470e`** splash ASCII dev-only. **`b05e381`** durcissement filet (intégrité modules).
- Gates : suite core 1522 ✓ · memory 9/9 ✓ · filet boot réel 6/6 ✓.

## Décisions / découvertes (le POURQUOI)

- **Double-boot = coût MÉMOIRE, PAS vitesse** (mesuré : 2721 ms avant / 2776 ms après). kernel#1
  s'arrêtait à `onStart`. Le gain = 1 container/injector/syslog (cause du doublon JSONL) + clarté.
- **Aligner sur le pattern qui marche plutôt qu'inventer** : prod/cluster recopient la recette DevCommand
  (1 Kernel) au lieu d'un registry risqué. Le `type` SERVER/CONSOLE dépend du rôle runtime → `setType`
  conditionnel, pas un attribut statique.
- **Boot = 91 % imports de modules** (~1337 ms import statique + ~1203 ms instanciation app de démo).
  Framework core ~3 ms. Levier vitesse = lazy vendors CLI (~180 ms) — backlog.
- **Docker = foreground, jamais détaché** ; SIGTERM → graceful exit 0 en 364 ms (prouvé).
- **Daemon CONSOLE** : `await new Promise(()=>{})` ne garde pas Node vivant (besoin d'un handle).
- **Mon filet ne vérifiait que « Server Listen »** → a laissé passer `@nodefony/test` en fail-soft (404).
  Le user l'a vu. Filet durci (modules chargés + route 200). Race build one-shot 1er boot dev = backlog.

## Frictions (→ RETEX.md)

- Filet de boot doit prouver l'INTÉGRITÉ (modules), pas juste le listen.
- MESURER un gain perf avant de l'affirmer (double-boot = 0 gain temps).
- commitlint subject-case (sujet minuscule) ; pre-push typecheck nécessite dist/types des packages
  (`@nodefony/user`/`orm-core`) → rebuild ciblé `--force` avant push.

## Commits produits

| Commit  | Sujet                                                   |
| ------- | ------------------------------------------------------- |
| 55132c0 | test(cli): filet intégration + trace boot-count         |
| fb410bb | chore(core): bump commander 15                          |
| e51af26 | refactor(core): 1 seul Kernel (fin double-boot) + hooks |
| faef194 | perf(core): audit boot + outils                         |
| 4dbeafe | test(cli): 3 modes + guide Docker                       |
| 684742e | docs(guides): endpoint santé Docker                     |
| e27470e | fix(core): splash ASCII dev-only                        |
| b05e381 | test(cli): durcit filet (intégrité modules)             |

## Reste

1. **Prochaine session** : démarrage DEV spinners/loaders ([[project_dev_boot_spinner_ux]]).
2. Race build one-shot 1er boot dev (rebuild ⟷ import) ; lazy vendors CLI (vitesse boot, cf audit).
