---
date: 2026-09-14
session_id: 76e40aa8-02fa-453d-ab05-5cebb4b8bbc0
focus: Campagne de test complète, et la chasse à un écart de performance qui n'était pas une régression
---

# Session retro — 2026-09-14 — 76e40aa8

## Focus

Analyse du transcript vibe (second agent tiers), campagne de test A→C complète, puis
investigation d'un écart de débit qui a mobilisé la moitié de la séance.

## Fait

- **Transcript vibe dépouillé** : l'agent a lancé la BONNE commande (`inspect routes`) pour
  trouver les routes d'authentification, a reçu 8 routes sur 370 après un `head -100`, et a
  conclu qu'aucune n'existait. ~40 tours et 12 réécritures à réimplémenter l'authentification
  avant de trouver `authFlow` en grepant du JS compilé. → #394.
- **Campagne A→C** : étage A (12 259 passés), B (0 rouge, gate mémoire 9/9), C1/C2/C3.
  Une seule régression produit sur toute la campagne — la mienne de la veille.
- **Correctifs livrés** : `3213cf23` (le garde de container disait UNE cause sur deux),
  `09c22774` (donner le corps au `end()`), `f8d560f4` (paires alternées + garde de décor).
- **6 tickets ouverts** : #394, #395, #396, #397, #398, #399, #400.

## Frictions

- Le harness a annoncé « exit code 0 » sur deux runs ROUGES. Seule ma ligne `EXIT=$?` disait vrai.
- Les 6 rouges de `@nodefony/studio` venaient de MON commit de la veille : j'avais lancé les
  tests que je pensais concernés, pas ceux d'un autre paquet.
- 5 588 sessions Redis résiduelles ont fait tomber 4 tests innocents.
- Mon hypothèse sur la `new Promise` de `end()` était fondée sur une prémisse FAUSSE (je
  croyais qu'on passait le corps à `end()`) — l'audit délégué l'a réfutée par lecture.
- J'ai mesuré cinq camps en séries séquentielles alors que les paires alternées sont
  documentées dans le skill que je venais de charger.

## Coûts évidents

- Quatre campagnes `test:all` complètes (~5 min chacune) dont deux évitables : la première
  rouge sur ma régression, la seconde pour la confirmer.
- La machine, épuisée en fin de séance, a fait refuser 6 séries de mesure sur 12 pour
  dispersion — le banc a eu raison à chaque fois, mais le temps était perdu.

## Recommandations

1. **Un garde « décor déclaré » sur tout banc comparatif** — écrit dans `bench-pairs.sh`, à
   généraliser aux autres bancs qui dépendent d'un `node_modules`.
2. **Le champ `node` doit être obligatoire** dans `docs/performance/data/*.json` — son absence
   a rendu trois semaines de mesures incomparables (#398).
3. **Ne pas conclure sur un changelog** : `gh api .../pulls/<n>/files` coûte dix secondes et a
   renversé une conclusion ce soir.

## Patterns récurrents (déjà gérés)

- ✅ `ECONNREFUSED` sur une suite d'intégration = serveur absent, jamais le code (protocole).
- ✅ Le rejeu isolé tranche une saturation en une minute (arbre de décision de la campagne).
- ✅ Un banc qui REFUSE de mesurer est un bon banc — 6 refus ce soir, tous justifiés.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `3213cf23` | fix(core): dire LAQUELLE des deux causes fait refuser un container     |
| `09c22774` | perf(http): donner le corps au end() pour regagner un tick par réponse |
| `f8d560f4` | test(load-test): imposer les paires alternées et le décor déclaré      |
