---
date: 2026-05-27
session_id: 1381eacf-c2fa-46d9-9795-094f9f565b65
focus: Purge dettes en série (A retex + D types + E syslog requestId + J security headers + fix runners + G Service listeners)
---

# Session retro — 2026-05-27 — 1381eacf

Session marathon de purge des « trucs en suspend » + debug intensif (memory flake, régression `delete→undefined`, 59 fails framework sans serveur, ClusterProbe mocha/vitest, 5 fails statics 404).

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |   153 |
| Read            |    50 |
| Edit            |    38 |
| TaskUpdate      |    33 |
| TaskCreate      |    15 |
| Write           |     6 |
| Skill           |     3 |
| ToolSearch      |     2 |
| AskUserQuestion |     2 |

## Top fichiers Read/Edit

| Fichier                           |                           × |
| --------------------------------- | --------------------------: |
| `nodefony-framework-dev/SKILL.md` |                         6 R |
| `MEMORY.md` (auto-mem)            |                         7 W |
| `http-kernel.ts`                  |                   4 R + 4 E |
| `Service.test.ts`                 |                   3 R + 2 E |
| `Service.ts`                      |                   2 R + 3 E |
| `nodefony-studio-dev/SKILL.md`    |                         3 R |
| `server-static.ts`                | 2 R (diagnostic régression) |

## Coûts évidents

- **Cache read 62.7M tokens (98% du coût)** — session longue, MEMORY.md+CLAUDE.md+skills relus à chaque turn. Levier #1 = `/clear` plus tôt entre features.
- **N restarts serveur** : ~8 cycles `stop.sh→start.sh` (memory.test, baseline stash, diagnostic régression). Cumul = source du flake observé.
- **Régression `delete→undefined`** : 1 patch poussé, baseline manquée, re-stash, re-rebuild. Coût ~30 min + diagnostic complet.
- **Validation locale `@nodefony/http` au lieu de suite racine turbo** : 59 fails framework manqués au commit J. Erreur méthodo.

## 💶 Coût (€)

- **Total ~€140 (≈ $152)** — 296 turns. Décompo : cache read **98%** (62.7M tk × $1.50/M), cache write 1h 2% (1.4M), output 0.1% (206k). Enseignement : session cache-dominée à l'extrême → contexte gonflé par les MEMORY.md/CLAUDE.md/skills relus. À 8 commits, coût/commit = €17.5 — élevé pour des fix S à M (acceptable pour 1 régression diagnostiquée + 1 dette test runners cassante).

## Recommandations

1. **Skill `nodefony-debug` à extraire (action C)** — soirée prouve la valeur : 5 patterns de debug récurrents (memory flake isolation, baseline stash diagnostic régression, fail intégration→serveur down, mélange runners mocha/vitest, `for...in` pattern delete-dependent). À codifier.
2. **Validation cross-module avant commit** — quand un patch touche le pipeline HTTP, lancer `npm run test:integration` à la racine (turbo), pas seulement `@nodefony/http`. La suite framework requiert serveur up → procédure `start.sh` + suite racine + `stop.sh`.
3. **Mémoires de 6+ jours = audit terrain obligatoire** — `project_service_base_improvements.md` point 3 (« délete déoptimise V8, préférer undefined ») était un piège mort. Le code consommateur `server-static` dépendait de la sémantique `delete`. Règle : si mémoire ≥ 7 jours suggère une micro-optim, grepper les consommateurs avant.

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin → `bash scripts/safe-commit.sh` (action A #I commit `95588cc`, ce soir).
- ✅ Mémoires périmées → garde-fou RESUME `_state` vs commits (mémoire `feedback_doc_periodic_audit` à créer si récurrent).
- ✅ Memory test flake en suite full → isolation = 8/8 stable. Pattern documenté dans `feedback_session_pitfalls`.

## Commits produits

| Commit    | Sujet                                                             |
| --------- | ----------------------------------------------------------------- |
| `95588cc` | chore(tooling): safe-commit.sh — anti index.lock orphelin         |
| `294a6bc` | docs(claude): cahier des charges amont étendu aux gros artefacts  |
| `184e5e5` | chore(skills): nouveau skill nodefony-frontend-verify             |
| `8e6cf47` | chore(types): standard exports.types — 3 legacy ORM nettoyés      |
| `4ed673c` | feat(core): ajoute requestId au Pdu (corrélation log↔requête ALS) |
| `f3bafb4` | feat(http): security headers OWASP par défaut (purge Phase 3b)    |
| `8d388e4` | fix(framework): exclure tests/unit du spec mocha intégration      |
| `8cbf6bb` | fix(core/Service): tracker listeners settingsToListen (point G2)  |
