---
date: 2026-08-07
session_id: a9a0d7be-591d-4ca8-a8cf-bb12d8863e61
focus: Studio réparé (2 pannes), accessibilité et lisibilité par un agent mesurées sur audits réels, skill nodefony-browser
---

# Session retro — 2026-08-07 — a9a0d7be

## Fait

- **Deux pannes de Studio, causes distinctes** : `process.env` nu au top-level de `Service.ts`
  (fichier ISOMORPHE → `ReferenceError` avant le premier import, bundle entier mort), puis deux
  cycles d'imports front. Deux gates neufs, vus rouges — le second a trouvé un cycle que la lecture
  avait manqué.
- **a11y / agentic mesurés sur audits Lighthouse RÉELS en production** : supervision 87→97 (a11y),
  91→100 (SEO), 50→78 (agentic) ; documentation 88→93 et 33→84. `agent-accessibility-tree` vert
  des deux côtés.
- **Skill `nodefony-browser`** : voir ET mesurer un écran sans navigateur sur le poste. Deux
  scripts, doc Playwright hors ligne, référence heartbeat MCP. Gates 24/24, banc 65/65 + 7/7.
- Dette « noms de rôles dupliqués » gravée au dashboard, avec le canal qui existe déjà.
- Plan de reprise : lot B (origine Vite dérivée du `Host`) + demandes devkit non traitées.

## Coûts évidents

- **~2 h sur un faux problème** : le heartbeat MCP fermait les sessions, et les symptômes
  accusaient le `fetch` puis le formulaire React. La cause était temporelle. Le pilotage direct de
  Playwright (~8 s, une commande) rendait le MCP inutile depuis le début.
- **Une dizaine de tentatives de pilotage par `curl`** avant de déléguer le diagnostic — le
  sous-agent `fable` a tranché en un run, et sa conclusion a tenu à la vérification.
- **Studio cassé sur le poste du user** par une variable d'observation oubliée : coût direct pour
  lui, et c'est la friction la plus importante de la session.

## Recommandations

1. **Lot B en priorité** — supprimer `NF_FRONTEND_PUBLIC_ORIGIN` plutôt que rappeler de la retirer.
2. **Porter au scaffold ce qu'on a appris** : son `AGENTS.md.tpl` ne parle que de captures, alors
   que la mesure est le vrai apport ; et il recommande le MCP là où le direct suffit.
3. **Vérifier la fraîcheur des mémoires avant de s'y fier** : `project_devkit_ai_kit` affirme
   « lot 5 non entamé » alors que quatre skills sont distribués.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact REÇU : le bundle servi ≠ le bundle bâti a resurgi deux fois (le service
  d'assets lit son `index.html` au démarrage).
- ✅ Suspecter son propre diff : le second cycle d'imports n'a été vu que par un automate.

## Commits produits

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `56d69297` | core — `process.env` nu dans `Service` tuait le bundle navigateur        |
| `da045961` | studio — deux cycles d'imports faisaient tomber le front                 |
| `f072b2fc` | CLAUDE.md — il y a un navigateur, arrêter de faire jouer la sonde        |
| `2c04bd66` | dette — noms de rôles écrits deux fois, front et back                    |
| `ea904a1f` | dette rôles — le canal existe déjà, et ce n'est pas `RoleRegistry`       |
| `b96ec54f` | studio — accessibilité et lisibilité par un agent, mesuré à chaque étape |
| `8ae3f101` | skill `nodefony-browser` — voir ET MESURER un écran                      |
