---
date: 2026-09-24
session_id: 4e20b8c0-17b1-4626-8d96-2fb15e45c9c5
focus: sonde Windows (#479) requalifiée, verdict CI de la reprise, #476 migrations par connecteur
---

# Retex 2026-09-24 — 4e20b8c0

- **Fait** :
  - `06bfdb128` ci : la sonde nomme TOUT process détaché né pendant l'étape (#479 requalifié : c'est
    `turbo.exe` qui ne rend pas la main, sans enfant ; ni turbo 2.11 ni Node 26.9 — 15 jobs figés
    recensés depuis le 17/09).
  - `f1d0da650` fix(session) : une CI annulée sur le dernier commit n'est plus affichée verte.
  - `8965458fb` feat(orm) : migrations propres à un connecteur secondaire (#476 FERMÉ) ;
    ancres de #396 recalées.
- **Frictions** :
  - `session:resume` affichait `CI ✅` sur un job Windows figé : `cancelled` compté vert.
  - La sonde filtrait par nom (`node|turbo`) : premier gel capturé sans coupable nommé.
  - #476 : le partage des tables par connecteur n'était dans aucun document, seulement dans le code.
  - Test de démarrage d'abord rouge : `NODE_ENV=test` éteint la bascule `auto`→`migrate` (règle
    connue, oubliée en écrivant le test).
  - Fausse alerte « `User` dans mediasoup » : deux `grep` concaténés, mal lus.
- **Décisions** :
  - Ne PAS tuer turbo quand son résumé est vert : l'étape enchaîne `turbo && rolldown && tsgo`, le
    kill couperait le `&&` sous un code 0.
  - Dossier `migrations/<connecteur>/<dialecte>` ; `default` garde la racine ; `sqlite`, `postgres`,
    `mysql`, `meta` refusés comme noms de secondaire (collision avec un dossier de dialecte).
  - Table sans entité → `default` (comportement antérieur préservé) ; tables du framework comptées
    pour `default` seul.
