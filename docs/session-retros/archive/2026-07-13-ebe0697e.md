---
date: 2026-07-13
session_id: ebe0697e-9e32-49a7-9237-f5f0e0708e55
focus: Scaffold lot 3a complet — homes vue/angular, create controller (3 saveurs), vitrine rest, create front, catalogue versions + fix framework @Idempotent
---

# Session retro — 2026-07-13 — ebe0697e

## Fait (4 commits)

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `94d0a33f` | homes vue/angular à parité react — logos frameworks + studio static épinglé  |
| `e2f9ceb5` | create controller lot 3a — 3 saveurs hello/realtime/rest, in-project, wiring |
| `27cb0c18` | saveur rest = vitrine décorateurs + FIX framework @Idempotent/renderJson     |
| `6617896b` | create front (react/vue/angular) + briques partagées + catalogue de versions |

## Frictions / coûts

- **`ui:"auto"` Studio crashe la famille Vite d'une app vue/angular** (--link) →
  page blanche user ; fix scaffold = `ui:"static"` épinglé (gotcha gravé au kit).
- **Vieux dist du core ×2 REVÉCU** : `create controller` version dégradée
  (hasSecurity absent du dist), puis crash `it.pkg` attrapé par le USER en
  live (templates disque ≠ moteur dist) — leçon à 3× dans le SAS → à graduer.
- **Bug framework débusqué au banc réel** : @Idempotent + renderJson → circular
  JSON → abort silencieux post-201 → dédup morte. 5 sondes successives (store,
  ALS, verdict, complete/abort) pour l'isoler — fix + test rouge/vert.
- **$pipestatus zsh ×1 revécu** (typecheck=0 mensonge) ; **NODEFONY_DEV_PORTS**
  ne pilote que la sonde (2 EADDRINUSE) ; **port Vite 5173 fixe** = collision
  multi-projet (front de la 2ᵉ app mort).

## Recommandations

1. Dette framework : port Vite dérivé/configurable (multi-projet dev).
2. Dette framework : fail-soft `ui:"auto"` (repli static + NOTICE si plugin
   non résolvable) — déjà au kit studio static.
3. 3b `create entity` : réutiliser la brique rest branchée repository (validé).

## 💶 Coût

- Session très longue (lot 3a complet + 2 fix framework) — 4 commits, 3 apps
  de banc générées/validées runtime, ~35 tests ajoutés (banc scaffold 21→35).
