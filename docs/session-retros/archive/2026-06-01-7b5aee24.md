---
date: 2026-06-01
session_id: 7b5aee24-5a46-438a-8f1b-681bea763631
focus: LB.4 — drivers prod Loki + OpenSearch (registre de fabriques, sonde/ping, switch Studio, docker-compose)
---

# Session retro — 2026-06-01 — 7b5aee24

## Fait

- **LB.4 LIVRÉ + PUSHÉ** (`6d8e17f`, `9cf9faa..6d8e17f` sur `claude-ts`).
- Drivers prod **Loki** (query LogQL) + **OpenSearch** (`_search` DSL), adaptateurs (filterPdus = autorité).
- Write batché : `LokiTransport`/`OpenSearchTransport` sur `BatchingHttpTransport` (ring+drop+flush+beforeExit).
- `httpFetch` `FetchLike` injectable (tests sans réseau, 12 tests mockés).
- **Registre de FABRIQUES** (`registerLogDriverFactory` + `builtinLogDrivers`) → 0 `if(name)` dans le Kernel.
- Sonde `probe()` + endpoint `backplane/ping` + bouton « Tester la destination » + switch Studio loki↔opensearch.
- Config env-driven (`NF_LOG_QUERY_DRIVER`/`LOKI_URL`/`OPENSEARCH_URL`) + docker-compose (réseau `nodefony-net`,
  profils loki/opensearch, Grafana datasource auto + OpenSearch Dashboards).
- **Validé RUNTIME** contre vrais conteneurs : write↔read Loki+OpenSearch identiques, requestId 32/32, ping 2-6ms.

## Frictions (→ détail dans RETEX.md)

- **Hardcode `if(name)` dans le Kernel rejeté 2× par le user** → registre de fabriques (la bonne réponse).
- **Log Backplane = 2 axes** : le user n'a pas compris select=LECTURE vs écriture=fan-out → l'UI doit séparer.
- **Docker distroless** (`grafana/loki`) = pas de healthcheck interne → faux « unhealthy » bloque `depends_on` Grafana.
- **memory.test flake** sous charge (JVM OpenSearch + Vite) : test différent déborde à chaque run = bruit, pas leak ;
  « requires server » = se connecte au serveur dev (pas le spawn) → relancer avant.
- **orm-core dist/types** manque au pre-push typecheck (turbo cache) → `turbo --filter @nodefony/orm-core --force` (2× ce jour).

## Décision structurante (survit au /clear)

**Registre de fabriques** pour les drivers de logs : le Kernel ne connaît plus aucun nom de driver, il résout

- branche. Les drivers natifs s'auto-enregistrent (`builtinLogDrivers`), un userland ajoute le sien (drop-in).
  ET : **écriture (fan-out, plusieurs destinations) ≠ lecture (un seul fond de panier, select)** — à rendre
  explicite dans l'UI (prochaine session).

## Commits produits

| Commit    | Sujet                                                                                 |
| --------- | ------------------------------------------------------------------------------------- |
| `6d8e17f` | feat(syslog): drivers prod Loki + OpenSearch (LB.4) — fabriques, sonde, switch Studio |

## Prochaine action

Page Logs — **clarté lecture/écriture** (cases à cocher écriture + select lecture) PUIS page **Dashboards** (iframe).
