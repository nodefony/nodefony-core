---
date: 2026-05-29
session_id: d2e874f9-d8b5-49a9-91f8-5e6accd0ff7e
focus: C6 — retrait franc de PM2 du framework (service + commandes pm2/kill + dep npm)
---

# Session retro — 2026-05-29 — d2e874f9

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Edit            |    44 |
| Bash            |    36 |
| Read            |    33 |
| TaskUpdate      |    13 |
| TaskCreate      |     7 |
| ToolSearch      |     1 |
| Skill           |     1 |
| AskUserQuestion |     1 |

## Top fichiers Read / Edit

| Fichier                  | Read | Edit |
| ------------------------ | ---: | ---: |
| `Kernel.ts`              |    8 |    8 |
| `KernelCommands.test.ts` |    5 |    8 |
| `CliKernel.ts`           |    3 |    3 |
| `MIGRATION_STATUS.md`    |    2 |    4 |
| `CLAUDE.md` (racine)     |    — |    4 |
| `StartCommand.ts`        |    — |    3 |

## Coûts évidents

- **1 restart serveur** (justifié = non-régression boot sans PM2, le vrai test). 0 restart évitable.
- **1 fail test rattrapé** : `--no-daemon` retiré côté code mais un test assertait son existence → fix immédiat.
- **`.git/index.lock` orphelin** (réapparu, séquelle start serveur) → diagnostic + `rm` (déjà géré [[feedback_git_index_lock]]).
- **Commit partiel** : `git add <liste>` incluant un fichier déjà `git rm` → pathspec « did not match » fait échouer TOUT le `git add` → seules les suppressions déjà stagées ont été commitées → amend nécessaire. **Nouveau gotcha** (cf Recommandations).

## 💶 Coût (€)

- Total ~**€30** (≈ $32,6) sur 78 tours. Décompo : cache-read $17,1 (52 %) + cache-write 1h $9,9 (30 %) + output $5,1 (15 %) + input $0,6 (2 %).
- **Cache = 82 %** → dominé par le CONTEXTE porté (CLAUDE.md racine massif + MEMORY.md index + skill `nodefony-session`), pas par le travail produit (output 15 %). Levier = `/clear` entre features, pas « écrire moins ».
- Coût/livrable ≈ €15/commit (2 commits, C6 complet + gates vertes) — session rentable (feature entière livrée).

## Recommandations

1. **MÉMOIRE — `git add` + fichier déjà `git rm`** : ne pas remettre un fichier supprimé dans la même liste de pathspecs `git add` (le pathspec échoue → tout le `git add` avorte → commit partiel). Stager les modifs séparément des suppressions (déjà prises par `git rm`), ou `git add -A`. Vécu : commit ffc9869 partiel (4 suppr seules) → amend.
2. **Pattern « kit a tranché » vs réalité code** : le kit C6 disait « réécrire `kill` en kill-par-port ». Le terrain a montré que les ports vivent dans `@nodefony/http` (couplage core→http anti cloud-native) → j'ai re-soumis l'arbitrage au user (AskUserQuestion légitime : choix produit + tension avec [[feedback_legacy_no_backcompat]]) → décision = supprimer la commande. Bon réflexe : ne pas exécuter aveuglément un kit que le code contredit.
3. **Branche morte révélée par le retrait** : `Kernel.onReady` testait `MODE_START!=="PM2"` mais `production` complète à `onStart` (jamais atteinte) → retrait PM2 = occasion de purger du code mort. Vérifier le flux avant de supprimer (fait : production → launchTopology à onStart).

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin → check process + `rm` [[feedback_git_index_lock]]
- ✅ memory.test exige serveur UP → restart avec nouveau build d'abord
- ✅ clean build après suppression de fichiers / index public touché
- ✅ commit FR via `git commit -F` heredoc [[feedback_commit_fr_apostrophes]]
- ✅ décision design tranchée + expliquée (pas QCM) sauf vrai choix produit → AskUserQuestion

## Commits produits

| Commit  | Sujet                                                                   |
| ------- | ----------------------------------------------------------------------- |
| 5317ed1 | refactor(core): retrait PM2 — service, commandes pm2+kill, dep npm (C6) |
| 51c53ab | docs(core): retrait PM2 (C6) — CLAUDE/MEMORY/README/MIGRATION_STATUS    |
