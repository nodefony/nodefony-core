---
date: 2026-06-20
session_id: 491e9baa-cd31-4eec-997f-3c2cff8cd39c
focus: DevSupervisor DX volets C+D (single-instance robuste + binaire direct) + fix faux « dégradé » + skill aligné
---

# Session retro — 2026-06-20 — 491e9baa

## Focus

Boucler le chantier DX du DevSupervisor (volets C + D du kit), corriger un faux « boot dégradé »
révélé par le test runtime, et purger la dette doc du SKILL.md nodefony-start-server.

## Fait

- **C — single-instance robuste** (`f0dc0083`) : `#claimSingleInstance` balaie `ps`
  (`discoverDevProcesses`) et tue TOUT résiduel au démarrage — superviseur empilé ET orphelins
  `nodefony-dev-server`/`-vite` laissés par un `kill -9` (pidfile périmé). Fallback pidfile
  conservé. Kill SIGTERM→SIGKILL factorisé dans `terminateDevProcesses` (devProcess.ts, partagé
  avec `nodefony stop` → fin de la duplication dans devStop.ts).
- **D — topologie propre** (`f0dc0083`) : `start.sh`/`stop.sh` lancent le binaire EN DIRECT
  (`node $BIN`) au lieu de `npx` → plus de wrapper `npm exec nodefony` parasite ; superviseur en
  PPID 1, `srv.pid` pointe le vrai superviseur.
- **fix faux « DÉGRADÉ »** (`f0dc0083`, révélé par le test C) : `booted=true` posé à `onBoot`
  AVANT `captureBootServers` (onReady) → `getBootReport().healthy` confondait `bootServers===null`
  (pas mesuré) et `[]` (0 serveur). Fix cause racine (flag `measured`) + sonde `#probeDegraded` ne
  lit le verdict que sur `booted:true`.
- **skill aligné** (`d716857f`) : SKILL.md nodefony-start-server purgé de la dette (§ « boot direct
  - restart MANUEL » périmée réécrite en « DevSupervisor auto-restart » ; note config corrigée ;
    binaire direct ; parsing logs `[dev]` ; symptômes DÉGRADÉ/orphelins).

## Validation runtime

- C : 3 orphelins fabriqués (`kill -9` superviseur) → nettoyés au redémarrage direct.
- D : `nodefony status` → arbre propre (1 superviseur PPID 1 · 1 serveur · 2 Vite), 0 npm exec.
- stop : 4 process arrêtés proprement, ports libérés ; status idempotent après stop.
- fix : `livez degraded:false`, superviseur affiche « framework ready » (plus de faux DÉGRADÉ).
- Gate : 1654 tests core verts, build clean.

## Coûts évidents

- 3 boots dev complets avec rebuild turbo FULL (cache invalidé à chaque modif core → cascade
  mediasoup ~400 entités). Inévitable (modifs core répétées), mais explique les `start.sh` TIMEOUT.

## Méthode

- Volet C testé en lançant le binaire EN DIRECT (pas `start.sh`) pour que son `pkill` préalable ne
  masque pas le nettoyage testé. La preuve runtime a révélé le faux `degraded` (invisible aux unit).

## Commits produits

| Commit     | Sujet                                                           |
| ---------- | --------------------------------------------------------------- |
| `f0dc0083` | feat(dev): superviseur single-instance robuste + binaire direct |
| `d716857f` | docs(skill): nodefony-start-server aligné sur le DevSupervisor  |
