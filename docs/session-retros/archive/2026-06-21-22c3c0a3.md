---
date: 2026-06-21
session_id: 22c3c0a3-6254-4dea-8cf7-0fea9db35ce3
focus: Studio — page Roles P6.15 + FIX sticky structurel (minHeight:0) + layout commun PageLayout + migration 15 pages
---

# Session retro — 2026-06-21 — 22c3c0a3

## Focus

Frontend Studio. Page Roles (P6.15), puis un **fix structurel du sticky** qui s'est étendu à toute
l'homogénéisation des topbars (nouvelles briques `PageLayout`/`StickyTabsList`, migration 15 pages).

## Fait (NON commité — 29 fichiers, user gère ; tout vert : typecheck 0, transforms 200)

- Page `/nodefony/roles` (P6.15) : onglets Hiérarchie + Graphe `FlowGraph` ; `roleHierarchy` ajouté à `nodefony.config.ts`.
- **Fix racine sticky** : `minHeight: 0` sur `AppShell.Main` (`AdminLayout.tsx`) → Main scrolle enfin → sticky OK partout.
- Briques `PageLayout` + `StickyTabsList` (`components/ui/PageLayout.tsx`) ; `PageHeader` `mb` retiré.
- 15 pages migrées en `PageLayout` ; 4 gardées custom (Chat/OrmOverview/Workspace/DashboardSupervision).
- Database ERD + Modules liste + ModuleDetail : headers → PageHeader/PageLayout ; graphe module height paramétrable.

## Frictions (→ RETEX.md + skill nodefony-studio-dev 1.27.0)

- **Debug layout à l'aveugle ~10 itérations** avant de demander 1 snippet console → `scrolle:false` a tout résolu d'un coup.
  Leçon : exiger 1 fait observable (console/capture) au 2ᵉ aller-retour.
- **Sticky bug STRUCTUREL** : Mantine `min-height` écrase `height` sur Main → main ne scrolle pas → sticky piégé.
- **ReactFlow exige hauteur CONCRÈTE** (jamais `height:100%` → erreur #004).
- **Navigateur Docker MCP injoignable** pour le dev (Vite cross-origin `127.0.0.1:5173` + cert + trustedHosts 421).
- **Quiproquo liste/détail** « page module » → confirmer l'URL exacte tôt.

## Coûts évidents

- Très long aller-retour visuel à l'aveugle (la cause #1 du coût). 1 restart serveur (config roleHierarchy = backend).
- 1 agent délégué (migration 9 pages, OK) mais son rapport final n'a pas pu être parsé → j'ai vérifié l'état moi-même.

## Commits produits

| Commit  | Sujet                                                                               |
| ------- | ----------------------------------------------------------------------------------- |
| (aucun) | NON commité — 29 fichiers, le user commite (suggestion de message dans le `_state`) |
