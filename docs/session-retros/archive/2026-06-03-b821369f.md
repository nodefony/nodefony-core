---
date: 2026-06-03
session_id: b821369f-c429-4267-8e50-a7bf351b4811
focus: Chargement de modules config-driven + env 12-factor + lot CLI UX
---

# Session retro — 2026-06-03 — b821369f

> Session DENSE (8 commits poussés sur `origin/claude-ts`). Le user a piloté par
> questions d'archi successives (décorateur ? mandatory ? chunks ? .env ? RFC ?
> help ? sysexits ?) → tout tranché + livré inline (0 sous-agent).

## Fait (8 commits, poussés `…3359321..9ded1ef`)

- `18b1370` **chargement de modules piloté par config** : la liste sort d'index.ts
  → manifeste ordonné `config.modules` (`IModuleManifest`, policy mandatory/optional/dev
  - `when(config)`), orchestré par le Kernel (`resolveModules`/`loadModulesFromManifest`
    à onPreRegister). Boot validé : 15 modules dans l'ordre exact, 0 régression.
- `979ecd6` **suppression du décorateur `@modules`** (anti-pattern auto-exécutant ;
  seul consommateur = son test). `@services`/`@entities` conservés.
- `d6d02b6` **env 12-factor** : `resolveRuntimeEnv` (NODE_ENV > commande > courant),
  `appEnvironment` ravivé via `APP_ENV`/`NODEFONY_ENV` (axe déploiement, string libre),
  flags `isProd`/`isDev` rendus complémentaires, header `development · staging`.
- `6ef6c27` **secret Redis hors `.env` versionné** + uniformité `.env.<env>`.
- `a50cc56` purge docs `@modules` (5 fichiers + 2 TSDoc).
- `1010a44` **lot CLI UX A** : boot silencieux (`quietBoot` → plancher sévérité [0..3])
  → `frontend:status`/help propres, `terminate:0` parti, `-d` restaure.
- `9ded1ef` **lot CLI UX B** : help global GROUPÉ + brandé + « Modules chargés (14) »
  (Commander 15 `helpGroup`), **NO_COLOR**/FORCE_COLOR (par-dessus `Kernel.isTTY`),
  **sysexits.h** (`SysExit` : EX_USAGE 64, EX_SOFTWARE 70).

## Frictions / leçons (→ RETEX.md, thème Core/pipeline)

- Boot : `debug`/`environment` résolus à `preRegister`, APRÈS `initSyslog`/`loadApp`
  → scanner `process.argv` pour gater tôt (comme le bin pour l'env). `-d` cassé sinon.
- Sortie CLI propre = plancher de sévérité syslog (pas toucher chaque log) ; sortie
  `console.log` (stdout direct) survit. Réutiliser `Kernel.isTTY`, pas re-fetch (user catch).
- Memory test : flake 1ʳᵉ passe (8/9) → 9/9 en re-run isolé. Vu 2× ce jour (connu).

## Décisions d'archi capturées (mémoire — survivent au /clear)

- [[project_module_loading_architecture]] : manifeste ordonné, lazy=non-chargement (ESM),
  mono-distrib 10 = sous-arbres preserveModules par subpath.
- [[project_app_config_refonte_chantier]] : 2 axes env (runtime/déploiement) ✅ wirés ;
  convention `.env` **B (Vite/Next)** actée ; reste config.<appEnv>.ts + catalogue + Zod.

## Reste

- Chantier config EN ENTIER (session dédiée) : config.ts deref top-level, loadEnv conv. B,
  découpage par domaine, catalogue 29 env vars, Zod.
- Phase 11 CLI couches 2-3 : ORM lazy-connect + gating modules par commande.
- ⚠️ `.env.production` : modif user non commitée (retrait `MINIFY=true`) — lui laisser.

## Commits produits

| Commit  | Sujet                                               |
| ------- | --------------------------------------------------- |
| 18b1370 | feat(core): chargement de modules piloté par config |
| 979ecd6 | refactor(core): supprime le décorateur @modules     |
| d6d02b6 | feat(core): env 12-factor NODE_ENV + APP_ENV        |
| 6ef6c27 | chore(env): secret Redis hors .env versionné        |
| a50cc56 | docs(core): purge références @modules               |
| 1010a44 | feat(cli): boot silencieux commandes utilitaires    |
| 9ded1ef | feat(cli): help groupé + NO_COLOR + sysexits        |
