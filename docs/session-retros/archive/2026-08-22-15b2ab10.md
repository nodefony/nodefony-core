---
date: 2026-08-22
session_id: 15b2ab10-dae8-4db6-b5ce-acc2cae0e0c6
focus: le câblage d'un agent se choisit — et une app générée ne pouvait pas ouvrir sa propre porte
---

# Session retro — 2026-08-22 — 15b2ab10

## Fait

- `create app` : l'**agent** se choisit, le MCP vient avec — question portée par la SPEC, donc
  terminal + flags `--agents` + **Studio** (qui n'en montrait rien).
- Un **pointeur par dialecte** : Gemini CLI ne voyait RIEN d'une app Nodefony. Ce que chaque agent
  lit est une donnée de la table, ancrée dans son SOURCE.
- 🔴 **Une app générée ne pouvait pas obtenir le jeton de SA porte** — masqué par `src/modules/test`
  qui déclarait `jwt.audiences` pour toute l'application.
- Le **499** : le journal du serveur se découvre par marqueur (il mentait dans les deux sens — faux
  rouge ici, faux vert là).
- La sonde navigateur **agit** avant de mesurer (`NF_BROWSER_ACTIONS`) ; Docker relégué en dernier
  recours dans les DEUX skills.
- Le banc de découvrabilité sait monter un décor **MCP authentifié** (`NF_DEVKIT_BENCH_MCP`), et
  tourner avec **Vibe** (foyer jetable, sans rien écrire sur le poste).

## Coûts évidents

- Trois lancements du banc pour trois erreurs d'ORDRE dans le décor, chacune masquée par la
  précédente (~12 min de décor à chaque fois). Un décor ne se relit pas, il se joue.
- Deux diagnostics perdus sur un `npm notice` pris pour la cause d'un échec (je n'affichais que la
  première ligne de stderr).

## Recommandations

1. Gate « spec ⇄ flags » créé — le refaire pour les autres surfaces qui prétendent être une source
   unique (une affirmation d'automatisation se vérifie).
2. Ne jamais conclure d'un code de sortie ce qu'une requête peut constater (`69` ≠ porte morte).
3. Instruire : 8 appels MCP ont eu lieu application ÉTEINTE, ce que le banc affirme impossible.

## Patterns récurrents (déjà gérés)

- ✅ « prouver sur l'artefact reçu » : le banc de vérité installe depuis les tarballs — c'est lui
  qui a pu porter la preuve du jeton.
- ✅ « un gate qu'on n'a jamais vu échouer n'est pas un gate » : le mien cherchait la mauvaise
  chaîne et restait vert, débranché.

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `fbe3b92c` | l'AGENT se choisit, la porte MCP vient AVEC                 |
| `2ecd99cc` | un pointeur par DIALECTE — Gemini CLI ne voyait rien        |
| `7794215a` | le journal du serveur se DÉCOUVRE (499)                     |
| `da093073` | l'octet ANSI s'écrit, il ne se colle pas                    |
| `5c11a658` | le choix d'agent vit dans la SPEC — terminal ET Studio      |
| `abcbcd26` | la sonde AGIT avant de mesurer ; Docker en dernier recours  |
| `11eb022d` | une question sans drapeau est inatteignable pour un script  |
| `ce931e99` | une app générée ne pouvait pas obtenir le jeton de SA porte |
| `73815ed4` | l'audience suit les PORTS de l'app                          |
| `47a0ecea` | le banc monte un décor MCP AUTHENTIFIÉ                      |
| `6d506bf1` | le décor d'un run se documente (agents, régimes, silences)  |
