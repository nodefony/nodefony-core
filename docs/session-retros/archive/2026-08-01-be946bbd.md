---
date: 2026-08-01
session_id: be946bbd-df2f-4dbc-8a7f-f1b83c8d1795
focus: T22 fermée (3 PASS/3) — le rouge n'était pas la CSP mais l'absence de façade de réponse ; matrice des 28 T ; essai d'instruction déléguée
---

# Session retro — 2026-08-01 — be946bbd

## Fait

- **T22 instruite puis FERMÉE** — `e450848f`. Diagnostic : la CSP était **intacte 3/3**, le FAIL
  venait de ce que servir une PAGE n'existe nulle part dans une app générée (`AGENTS.md` : 0
  occurrence de `render`/`setContextHtml`/`renderJson` ; les 3 controllers du preset rendent du
  JSON). Deux gestes : bloc de façades en tête de l'`AGENTS.md` + règle `nodefony check`
  **`reponse-a-la-main`** (3 motifs, vue rouge débranchée, 3 cas d'innocence).
  Mesure : 13/13 sondes, **3 PASS/3**, façade employée **0/3 → 3/3**, effort **57 → 38** tours.
- **Matrice des 28 tâches** du banc — `project_devkit_bench_matrix.md`, avec le protocole
  d'instruction déléguée et les deux pièges déjà payés.
- **Essai de délégation validé** (T29, `haiku` lecture seule) : 4 ancrages sur 4 exacts, conclusion
  fausse. Partage retenu : automate EXTRAIT → modèle RELÈVE → principal CONCLUT.

## Coûts évidents

- Deux cycles complets `build + décor isolé + 3 runs` (~25 min cumulées, ~1,7 $) — le second
  entièrement causé par une erreur de syntaxe de MA ligne dans l'`AGENTS.md`.
- Le décor du banc se remonte intégralement à chaque invocation (install de 11 tarballs).

## Recommandations

1. **Gate « le code de l'`AGENTS.md` compile »** — pendant d'`anchor-check` côté docs. Le défaut a
   coûté un cycle complet et serait passé en production du gabarit.
2. **`--analyze-only` veut un CHEMIN, pas un nom de run** depuis que le décor est hors dépôt ;
   l'aide affiche `<run>` et le nom nu échoue en ENOENT.
3. Ne pas reprendre la section « proposition » d'un sous-agent sans la re-dériver.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff avant le décor — a tranché en une commande.
- ✅ Voir le gate rouge débranché avant de le croire.
- ✅ Médiane de 3 runs, jamais un verdict unique.

## Commits produits

| Commit     | Sujet                                                                          |
| ---------- | ------------------------------------------------------------------------------ |
| `e450848f` | fix(agents): la facade de reponse manquait au fichier que l agent lit d office |
