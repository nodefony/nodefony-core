---
date: 2026-09-14
session_id: 7a61c0d9-a2ac-431b-9ef0-5d99717d6534
focus: Dualité de paquet nodefony — détecter, refuser, documenter (#386 fermé)
---

# Session retro — 2026-09-14 — 7a61c0d9

## Fait

- **#386 fermé** avec compte rendu. Trois commits : `1d72231c` (registre d'instances +
  verdict), `9aa6a356` (second point de contrôle + `catch` de `loadApp` + `EX_CONFIG`),
  `6c0dbd4c` (`Service.ts` — la cause racine — + cosmétiques + documentation).
- **Audit `fable`** du premier correctif : il s'auto-désarmait (`Symbol.for` rend la marque
  lisible des deux côtés → la garde ne mord plus quand le parc est à jour). Ses trois
  trouvailles graves vérifiées à la main puis fermées.
- **Cause racine trouvée par la mesure** : `Service.ts:120`, `container instanceof Container`
  échoue d'une copie à l'autre → le service JETTE le container reçu. Ni le commit précédent ni
  l'audit ne l'avaient : tous deux accusaient le singleton `Nodefony`.
- **Documentation** : `docs/guides/deux-paquets-nodefony.md` (conforme `doc:lint` +
  `doc:anchors`), le piège dans le gabarit `AGENTS.md` des apps générées, deux renvois.
- **#387 ouvert** : le garde-fou du banc de démarrage ne sonde qu'un port sur deux.

## Coûts évidents

- **Un run d'intégration entier jeté** (53 rouges) parce que j'éditais des sources pendant
  qu'il tournait — le superviseur a redémarré 14 fois. Relancé propre : 679 verts.
- **Deux instruments faux** annoncés au user avant recontrôle : `git cat-file -e` (délégation
  déclarée absente de trois versions où elle est présente) et un extracteur JSON écrit à la
  volée (hub de documentation déclaré cassé — il est valide).
- **Une question posée au user sur un fait faux** : autorisation de tuer « son » serveur, qui
  appartenait à une autre application du poste. Le produit le savait et l'a nommé.
- Trois skills chargés en séance (`framework-dev`, `check-memory-health`, `start-server`,
  `ticket`, `documentation`) — coût réel, mais chacun a évité une faute de protocole.

## Recommandations

1. **Publier une alpha.7** — sans elle, ce travail ne protège que ce dépôt : les alphas 1 à 6
   portent toutes le défaut, et le code qui détecte vit dans le noyau qui s'exécute.
2. **Avant d'imputer un lot de rouges à son diff**, regarder si le décor a bougé pendant le run
   (`grep "↻ changement" /tmp/nodefony-server.log`) — c'est une commande, contre une heure.
3. **Un verdict NÉGATIF rendu par un outil écrit sur le moment** (« absent », « cassé ») se
   recontrôle avec un second outil avant d'être dit. Une présence, non.

## Patterns récurrents (déjà gérés)

- ✅ Le débranchement systématique des gardes neuves — il a payé trois fois, dont une où il a
  révélé un e2e complaisant que rien d'autre n'aurait attrapé.
- ✅ Le gate `check:lang` a mordu deux fois sur des identifiants français écrits en production.
- ✅ `gateReporter` a fait sortir drizzle en 1 sans aucun rouge : 733 tests non exercés.

## Commits produits

| Commit     | Sujet                                                                       |
| ---------- | --------------------------------------------------------------------------- |
| `1d72231c` | feat(kernel): détecter deux paquets nodefony, et refuser de servir avec     |
| `9aa6a356` | fix(kernel): refuser la dualité de paquet là où elle arrive vraiment        |
| `6c0dbd4c` | fix(core): refuser un container venu d'une autre copie, au lieu de le jeter |
