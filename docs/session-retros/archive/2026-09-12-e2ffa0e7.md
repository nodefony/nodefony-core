---
date: 2026-09-12
session_id: e2ffa0e7-2f62-46a0-a26c-2fbfc1a8ea88
focus: "#365 et #374 — le démarrage muet sur une migration en attente, et la migration générée qui échoue"
---

# Session retro — 2026-09-12 — e2ffa0e7

Cinquième session du jour (09-12e). Deux tickets pris dans l'ordre, deux fermés,
trois ouverts.

## Fait

- **#365 fermé** (`dd8f95e2`) — le bilan de démarrage nomme désormais ce qui n'est pas
  prêt, sa cause et le geste ; le mode `auto` publie son état au lieu de sortir avant
  toute publication. Champ `action` ajouté au contrat de disponibilité.
- **#374 fermé** (`a1067a1e`) — le générateur d'entité ne change plus le DDL de cinq
  colonnes que personne n'a demandées (`defaultJsOnly`), et la recopie d'une recréation
  de table ne lit plus une colonne absente de la table de départ (`repairRebuildCopy`).
- **#374, #375, #376 ouverts** ; #375 et #376 découverts en éprouvant #374.

## Coûts évidents

- **Quatre applications générées** montées pour prouver (temoin, temoin2, t374, plus
  celle du banc relue). C'est le prix de la preuve sur l'artefact reçu : aucune de ces
  vérités n'était lisible dans le dépôt.
- Une chaîne `&&` avec sortie masquée a envoyé le diagnostic dans la mauvaise direction
  (versé au sas).
- Le décor du témoin s'est abîmé sur le défaut de #374 lui-même — d'où le second témoin.

## Recommandations

1. **Rejouer les dialectes en CI** — PostgreSQL et MySQL n'ont pas été exercés (docker
   éteint), soit 733 tests skippés sur la suite drizzle. Le correctif du générateur
   d'entité est commun aux trois dialectes.
2. **CONSOLIDATE** — 6 thèmes mûrs, dont un à 11 frictions ; le sas passe 765 lignes.

## Patterns récurrents (déjà gérés)

- ✅ Preuve sur l'artefact REÇU (application générée, pas le dépôt).
- ✅ Test neuf vu ROUGE par mutation de la ligne active, prouvée par `git diff`.
- ✅ Nommer ce qui n'a PAS été lancé avant de dire « fait ».

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `dd8f95e2` | fix(orm): dire au démarrage qu'une migration attend, avec le geste        |
| `a1067a1e` | fix(orm): écrire une migration sqlite qui n'attend pas la colonne ajoutée |
