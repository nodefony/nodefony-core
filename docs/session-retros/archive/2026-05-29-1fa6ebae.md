---
date: 2026-05-29
session_id: 1fa6ebae-d084-428a-8831-e29910fb81b3
focus: Durcissement @nodefony/http — H5 (docs/tests réconciliés) + H6 (formidable→busboy)
---

# Session retro — 2026-05-29 — 1fa6ebae

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    81 |
| Edit  |    50 |
| Read  |    30 |
| Skill |     3 |

## Top fichiers Read/Edit

| Fichier                           |   × |
| --------------------------------- | --: |
| `http/Request.ts`                 |  17 |
| `http/parser.ts`                  |   5 |
| `upload/upload-service.ts`        |   5 |
| `tests/unit/UploadedFile.test.ts` |   3 |
| `http/CLAUDE.md`                  |   3 |
| kit `hardening_http`              |   3 |

## Coûts évidents

- **Cycle build→restart→test ×7** (8 builds, 7 restarts, 12 `test:integration`) = poste #1.
  Légitime en partie : 3 causes racines distinctes débuggées (JSON @Body, requestEnded session,
  timing drain). Mais aurait pu être groupé : j'ai itéré fix-par-fix au lieu d'analyser les 5 fails
  d'un coup.
- **Piège turbo stale-dist** : `start.sh --force-build` a restauré un dist http périmé via cache
  turbo → 5 tests rouges fantômes → 1 boucle de diagnostic confuse (mauvais chemin grep relatif en
  prime). Coût évitable.

## 💶 Coût (€)

- Total ~**€68** (≈ $74) — cache-read 33,8 M tok = **68 %** ($51), output 172 K = 17 % ($13),
  cache-write 1h 327 K = 13 % ($10). Cache-dominé classique : le contexte (gros `Request.ts` relu 17×
  - cycles test) coûte, pas l'output. ~€23/commit (3 commits) — refacto sécu-critique justifie.

## Recommandations

1. **MAJ gotcha turbo** : après édition source http → `npm run build -w @nodefony/http` PUIS
   `start.sh` **sans** `--force-build` (force-build rebuild le module test via turbo → peut restaurer
   un dist http périmé du cache). Ajouté au kit / à surveiller.
2. **Analyser TOUS les fails d'un bloc avant de fixer** : ici 5 fails = 3 causes ; j'ai fixé en 3
   passes (3 rebuilds). Lire les 5 stacks d'abord aurait pu fusionner en 1-2 passes.
3. **Mémoriser** : busboy = multipart SEUL → JSON/raw passent par Parser maison ; tout corps à
   décoder doit drainer (`await ended()`) avant parse, et `requestEnded` gate le start session.

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin (lint-staged) → `rm -f` après vérif aucun git actif.
- ✅ SIGPIPE build→grep → build en log fichier puis grep (respecté).
- ✅ macOS sans `timeout` → relancer sans (vu en cours de session).
- ✅ Suspecter son propre diff avant « pré-existant » (les 5 fails étaient bien les miens).

## Commits produits

| Commit    | Sujet                                                            |
| --------- | ---------------------------------------------------------------- |
| `5f9b75a` | docs(http) H5 — comptes tests réconciliés (254 unit / 621 intég) |
| `c6f232a` | feat(http) H6 — formidable → @fastify/busboy (upload streaming)  |
| `821d226` | docs(migration) — H1→H6 clos, P2.9 upload streaming              |
