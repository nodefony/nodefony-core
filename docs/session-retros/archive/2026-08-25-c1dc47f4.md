---
date: 2026-08-25
session_id: c1dc47f4-374e-4104-a130-5fdfe40cc47d
focus: Instruire les 4 tâches instables du banc devkit — 2 étaient des faux rouges ; puis le scaffold met en forme ce qu'il produit
---

# Session retro — 2026-08-25 — c1dc47f4

## Fait

Neuf commits, `a851c1a3` → `77c76f5e`. Dashboard : ligne `BANC devkit` 26/30 → **28/30**.

- **T5 et T17 n'étaient pas instables — le banc les recalait à tort.** T5 : l'agent
  arrête par le framework (« ✓ arrêté proprement »), puis pose une ceinture
  `if ps -p $PID; then kill -9 …; fi` que ce succès rend MORTE — la sonde comptait
  le `kill` jamais exécuté. T17 : zone de firewall IDENTIQUE aux deux passes, seul
  l'ENDROIT change ; le motif exigeait moins de 800 caractères depuis `areas: {`,
  et le commentaire du gabarit — qui dit « AJOUTER une route ici » — en occupe
  1 100. Distance 1 251 → rouge, 145 → vert.
- **T18 et T22 sont de vrais échecs**, instruits : le produit désamorçait un
  contournement sur deux (liste de rôles sur l'action jamais avertie), et la table
  des symptômes ne connaissait qu'une cause au 404 (le préfixe du controller
  manquait).
- **`create` met désormais en forme ce qu'il produit**, avec le prettier DU PROJET :
  16 fichiers non conformes → 0. La cause n'était pas corrigeable dans les
  gabarits — la forme canonique dépend d'un identifiant que l'utilisateur choisit.
- **Purge des décors du banc** : 13 Go → 514 Mo, sans perdre une mesure.

## Coûts évidents

- **Le cycle « corriger un gabarit → relancer `format:scaffold` » coûte ~90 s**, et
  prettier réagit en cascade (un éclatement en révèle d'autres). Trois tours perdus
  avant de prendre TOUS les écarts en une passe (formater le rendu, reporter le diff).
- Deux mesures fausses à refaire, toutes deux par cible mal visée : prettier sur une
  copie sous `tmp/` (ignorée), et `os.tmpdir()` cherché dans `/tmp`.
- Un `npm run build` complet (~90 s) obligatoire avant toute mesure du CLI — le
  binaire s'exécute depuis `dist`.

## Recommandations

1. **Reporter un diff de RENDU dans un gabarit se scripte** — chaque bloc contrôlé
   unique avant remplacement. Fait à la main trois fois avant de l'automatiser.
2. **Chercher la convention-frère avant d'écrire une règle** : la règle de T18
   existait déjà pour les routes, dans la puce voisine du même fichier.
3. **Un test neuf se voit rouge dans le sens qui le concerne** : pour une garde
   anti-débordement, débrancher ne prouve rien (une fonction inerte ne déborde pas)
   — il faut la faire DÉBORDER.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff : les deux défauts les plus graves du jour étaient
  dans du code écrit une heure plus tôt dans la même session.
- ✅ Prouver sur l'artefact reçu : app régénérée puis relue, jamais le gabarit.
- ✅ Un gate rejoué sur l'état HEAD pour distinguer un rouge pré-existant du sien.

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `a851c1a3` | deux « instables » étaient des faux rouges du banc            |
| `bff1d3b9` | dire ce que coûte le geste CONCURRENT                         |
| `7ff89b5c` | le gabarit React arrivait non formaté chez l'utilisateur      |
| `946940f0` | le formateur disait « 0 à reformater » sur un terrain non vu  |
| `3062091d` | `create` met en forme ce qu'il produit                        |
| `0aaa919b` | purger les décors des runs — 13 Go pour 200 Mo de mesure      |
| `dc4d32b8` | la purge dit OÙ elle travaille — `os.tmpdir()` n'est pas /tmp |
| `a6cbfa0d` | la passe de formatage ne déborde plus, et vit sous Windows    |
| `77c76f5e` | une jonction NTFS, pour que le cas tourne aussi sous Windows  |
