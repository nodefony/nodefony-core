---
date: 2026-09-25
session_id: b97f3790-b6e6-4544-a026-5611926e2655
focus: chantier scope de requête — #482 fuite et imbrication, #483 registre des scopes
---

# Retex 2026-09-25 — b97f3790

- **Fait** : `d256429a4` fix(core) fuite de paramètres, imbrication, orphelin HTTP (#482 fermé) ·
  `36f834631` perf(core)! registre `Set`, id paresseux, plus d'écriture morte de
  `notificationsCenter` (#483 fermé, −37 % sur le cycle `enterScope` + `leaveScope` sous charge) ·
  `b9dc1755d` sonde in-situ relevée par `bench-ab-mono.sh` · `a20dace4c` exemple du pont
  `api.request` du skill navigateur. Gate mémoire : drainage des scopes après chaque boucle.
  Corps de #484 et #485 recalés (ancres) ; #485 et #486 commentés (faits changés).
- **Frictions** : #483 sauté en recopiant l'ordre du `_state` au lieu du tableau — arrêté par le
  user (versé à `feedback_dependency_encoded_elsewhere`) ; premier gate mémoire joué contre l'ANCIEN
  dist (rattrapé avant de conclure) ; le heap aveugle à une fuite de registre (versé à
  `feedback_gate_must_bite`) ; critère −40 % fixé sur microbench, jugé in-situ (sas 🎯) ; débit
  refusé 4/4 sur machine chaude (sas 🎯).
- **Décisions** : scope par copie prototypale GARDÉ ; `Set` plutôt que liste intrusive (le retrait
  toucherait deux voisins froids sous charge) ; −37 % accepté par l'auteur ; rupture annoncée par
  `BREAKING CHANGE:` (le changelog naît à la publication) ; comparatif face à Express non rejoué
  (gain < 1 % par requête, sous le bruit).
