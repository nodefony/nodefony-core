---
date: 2026-05-29
session_id: b397da0f-32b0-4f15-9ad1-d468b6042317
focus: Clôture P4 (relabel P4.3/P4.5) + décorateurs param framework étendus (@Headers/@Cookie/@Session/@Req/@Res/@UploadedFile(s))
---

# Session retro — 2026-05-29 — b397da0f

## Tool usage

| Outil           | Calls |
| --------------- | ----: |
| Bash            |    84 |
| Edit            |    19 |
| Read            |    18 |
| Skill           |     4 |
| Write           |     2 |
| AskUserQuestion |     1 |

## Top fichiers Read/Edit

| Fichier                                 |   × |
| --------------------------------------- | --: |
| modules/test/.../DecoratorController.ts |   4 |
| MIGRATION_STATUS.md                     |   3 |
| framework/.../Resolver.ts               |   2 |
| framework/.../routerDecorators.ts       |   2 |
| framework/.../config/config.ts          |   2 |

## Coûts évidents

- **Turbo dist poisoning × 3 modules** (framework, http, module test) : `npm run build` (turbo)
  a restauré des dist périmés à **mtime frais** → 404 fantômes (Session import KO) PUIS 12 faux
  échecs intég (@Body vide, abort/timeout — dist http pré-fix de session). Diagnostic + 3 clean
  rebuilds + 3 restarts serveur. ~30 % du temps de session.
- **2 logs `rollup` géants** : `npm run build` dump la liste COMPLÈTE des inputs (entités dolibarr)
  sur UNE ligne énorme → 2× dans le contexte (cache). J'ai utilisé `tail -8`/`tail -6` au lieu de
  `tail -1` → la ligne géante est passée. Coût cache évitable.

## 💶 Coût (€)

- Total ~**€43.6** (≈ $47.4) — cache-read **66 %** ($31.5, 21M tok), cache-write 1h $7.7, output
  $7.6, input $0.48. **Cache-dominé** : 132 tours portant gros contexte (MEMORY index + CLAUDE.md
  - skills + 2 logs rollup géants). Levier #1 = `tail -1` sur les builds rollup + /clear entre features.

## Recommandations

1. **Build rollup → toujours `| tail -1`** (pas `tail -8`) : la ligne d'inputs est unique et énorme.
   Ou `grep -E "created|error"`. Évite de polluer le cache avec la liste d'entités.
2. **Durcir le réflexe anti-poisoning** ([[feedback_turbo_cache_stale_logs]]) : quand on touche un
   module consommé par d'autres via TS project-references, rebuild le module **leaf en DERNIER** +
   vérifier le dist par `grep` du contenu (jamais mtime). Vécu : rebuild du module test re-déclenche
   un build framework périmé.
3. **Pattern fonction-pure-testable** confirmé payant : extraire la résolution (`buildParamArgs`/
   `resolveParamArg`) hors pipeline → 16 unit sans serveur, l'intég ne valide que le câblage. À
   réappliquer systématiquement quand une logique est enfouie dans une méthode privée du pipeline.

## Patterns récurrents (déjà gérés)

- ✅ Turbo dist poisoning à mtime frais ([[feedback_turbo_cache_stale_logs]] — la cause #1 de cette session)
- ✅ memory.test "async crashes" flaky GC en groupe → vert isolé (non bloquant)
- ✅ `.git/index.lock` orphelin (husky/commitlint interrompu) → pgrep puis rm
- ✅ commit FR via heredoc (apostrophes) + commitlint refuse sujet sentence-case

## Commits produits

| Commit  | Sujet                                                                                   |
| ------- | --------------------------------------------------------------------------------------- |
| 2914b74 | test(http): clôt P4 — bump concurrence ALS 10→100 + relabel P4.3/P4.5 ✅                |
| 76629ef | feat(framework): décorateurs param @Headers/@Cookie/@Session/@Req/@Res/@UploadedFile(s) |
