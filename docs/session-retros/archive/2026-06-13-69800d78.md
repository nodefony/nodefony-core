---
date: 2026-06-13
session_id: 69800d78-57e8-447c-956b-8ac9d8d7ee34
focus: P6 co-évolution frontend — L2 (identité au welcome + close codes WS) + refonte LOGIN Studio (vitrine) + fix perf overlay
---

# Session retro — 2026-06-13 (soir) — 69800d78

## Focus

Reprise du « NEXT » P6 (co-évolution frontend ratée en J3b). Audit de la polymorphie front↔back de
la Socket Nodefony → découverte d'une dette d'isomorphisme → choix d'un chantier **L2** (identité au
welcome) avant le gros refacto **L0**. Puis grosse passe **UX sur le login Studio** (vitrine).

## Fait (NON commité — le user gère les commits)

- **Lib L2 — identité au `realtime:welcome`** : type partagé `RealtimeIdentity`/`IRealtimeWelcome`
  (core `RealtimeEventMap`), serveur `RealtimeController` enrichit le welcome depuis le **token neutre**
  (0 dép security), client `RealtimeClient` ingère → `identity`/`serverChannels`/`serverMethods`/
  `onIdentity()` + reset au logout, hook React `useNodefonyIdentity()`.
- **Lib L2 — respect des close codes WS** : `isReconnectableCloseCode` (notice.ts) → le client **ne
  reconnecte plus** sur un close DÉFINITIF (1008 policy/1002/1003/1007/1010/4004) ; reco réservée aux
  transitoires (1006/1011/1001). Ferme la boucle de reco anonyme (le « (c) » du gap J3b).
- **Tests** : realtime +2 (welcome identité anon/auth) → 30/30 ; core +RealtimeClientIdentity 7/7 +
  notice/transport (isReconnectable, close fatal→pas de reco) ; **intégration `ws-data-plane-auth`
  enrichie** (welcome.identity authentifié) → **6/6**, modèle A confirmé en LIVE (handshake anonyme
  zone admin → close 1008). Typecheck core+realtime+studio = 0.
- **Login Studio refondu** (P10, vitrine) : split hero (logo + « Nodefony Studio »), **identifier-first
  sûr** (anti-énumération) + « rebonjour » (mémorise le dernier identifiant), **gestion d'erreurs
  best-practice** (zone à hauteur RÉSERVÉE en haut → 0 layout shift, erreurs classées credentials/réseau/
  serveur/**throttle 429 non-réessayable**, `role="alert"`, s'efface à la frappe, note épurée ≠ Alert),
  seams **SSO Keycloak / Passkey WebAuthn** (« bientôt ») aux 2 étapes.
- **Fix perf `ConnectionOverlay`** (bug machine qui RAME sur Brave au switch de fenêtre) : retrait du
  `backdrop-filter: blur` plein écran + **pause Page Visibility** (tick + animations coupés onglet caché)
  - `prefers-reduced-motion`.
- **lexique** : entrée `seam` enrichie (point d'extension `null` posé à l'avance ; livre les couches dans l'ordre).

## Frictions / leçons (détail → RETEX.md + skill studio-dev)

- **Écran vitrine improvisé = ~12 itérations UI** sur le seul login → mini-cahier des charges VISUEL AVANT.
- **Layout shift d'erreur** senti 3× (« c'est mon œil qui bouge ») → toutes les erreurs dans UNE zone
  à hauteur réservée, jamais inline ni au-dessus ni en bas ; centrage conservé via hauteur constante.
- **`backdrop-filter` plein écran + tick non pausé = rame machine** → 0 blur + Page Visibility.
- **Doc-comment ≠ code réel** : `RealtimeClient` ne compose PAS `JsonRpcPeer` (dette L0, chronologie git).
- **Honnêteté** : j'avais survendu L2 « ferme le gap anonyme→login » ; le terrain (modèle A : handshake
  anonyme refusé, pas de welcome) → le déclencheur reste le 401 HTTP + close 1008. Corrigé devant le user.

## Commits produits

| Commit     | Sujet                                                                                                     |
| ---------- | --------------------------------------------------------------------------------------------------------- |
| `c7d86c2c` | feat(realtime) — L2 : identité au `realtime:welcome` + respect des close codes WS (lib + serveur + tests) |
| `98dd34e1` | feat(studio) — refonte login (identifier-first sûr, erreurs zéro-saut) + fix perf overlay reconnexion     |
| `21c1eec4` | docs(retro,lexique) — ce retex + RETEX.md + lexique `seam` + leçons login (skill studio-dev)              |
