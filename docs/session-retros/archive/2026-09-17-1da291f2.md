---
date: 2026-09-17
session_id: 1da291f2-25cc-466d-aa12-a6da9c668b36
focus: "#404 — livrer aux applications le skill de développement, et prouver qu'il est atteint"
---

# Session retro — 2026-09-17 — 1da291f2

## Focus

Livrer `nodefony-dev`, le skill généraliste publié aux applications par `@nodefony/devkit`, et
surtout **prouver** qu'un agent le charge — la demande du user était « très très très
sérieusement, il faut qu'il soit chargé presque tout le temps ».

## Fait

- **Skill `nodefony-dev` livré** (146 → 257 lignes) avec son script `docs.mjs`, conçu **sur la
  mesure** : un script cité dans la porte est atteint dans 80 % des runs, un `SKILL.md` dans 17 %,
  une page de `references/` dans **0 %** sur 1020 transcripts — d'où un script, aucune `references/`,
  et la porte qui NOMME le skill.
- **Deux tris éditoriaux délégués** (`fable`, périmètres disjoints) sur ~7 000 lignes de skills du
  dépôt : 18 règles versées (7 serveur, 11 front), le reste écarté avec motif — dont 32 déjà
  publiées dans la doc de module, donc déjà transmises.
- **Quatre règles de méthode** tirées des mémoires, après avoir mesuré que 121/123 sont du pilotage
  d'agent, inutile à une application.
- **Chaînage des skills dans les deux sens** : le généraliste oriente (mesuré : `nodefony-dev` puis
  `nodefony-add-crud`), et les six spécialistes renvoient désormais vers lui — idée du user, après
  qu'un run a pris `protect-route` directement sans jamais voir la conduite commune.
- **Le gate des skills élargi aux 7 skills publiés sur npm** — il n'en gardait aucun. Il a trouvé
  aussitôt deux descriptions hors standard déjà livrées (1559 et 1339 car., plafond 1024).
- **Un défaut de scaffold qui coûtait 2-3 tours à CHAQUE utilisateur** : `writeAgentPointers`
  recomposait ses options eta et perdait `autoTrim: false` → `CLAUDE.md` refusé par le prettier de
  l'app → boucle `verify` / `prettier --write` / `verify`.
- **Deux dettes de #389 soldées** : 6 identifiants français, un test de #280 laissé rouge.
- **Mesure multi-harness** : claude ✅ (3/3), copilot ✅ (chaîne complète), codex ❌ (plan ChatGPT
  `free`), vibe ❌ (aucune réponse).
- **#404 fermé** avec compte rendu complet.

## Coûts évidents

- **Cinq instruments faux**, tous du même genre : une sortie tronquée ou une erreur masquée lue
  comme une réponse (`2>/dev/null` sur `ARG_MAX`, `xargs -a` BSD, `\|` en `grep -E`, `timeout`
  absent sur macOS, `head -6`). Chacun a produit un chiffre faux que j'ai d'abord cru.
- **Deux runs `fable`** pour les tris (~450 k tokens cumulés) — justifiés : tri éditorial sur un
  corpus, aucun verdict mécanique.
- **Quatre runs de banc**, dont deux perdus sur des décors d'agent hors de notre contrôle.

## Recommandations

1. **Le dialecte `copilot` du banc ne lit pas la sortie `-p`** (texte formaté, pas JSONL) : le run le
   plus démonstratif a été classé « aucun tour d'assistant ». Ticket à ouvrir.
2. **Les 23 règles classées `DOC`** par les deux tris doivent devenir des pages de doc de module
   publiées — pas du contenu de skill. Chantier distinct.
3. **La médiane sur 3 runs** reste à payer pour conclure sur l'effort : chaque tâche n'a qu'un run.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son instrument — appliqué cinq fois, mais **après** avoir cru le chiffre faux.
- ✅ Un test neuf se voit ROUGE une fois — appliqué trois fois, et les trois ont mordu.
- ✅ Prouver sur l'artefact reçu — le tarball a été ouvert avant de mesurer.

## Commits produits

| Commit     | Sujet                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------- |
| `2b2ff738` | fix(scaffold): solder la dette de langue et le test que #389 avait laissé rouge           |
| `7b52a67a` | feat(devkit): livrer aux applications le skill de développement du framework              |
| `78d39ba9` | fix(devkit-bench): la sonde du script mesurait la PORTE, pas le geste                     |
| `579b839d` | fix(scaffold): rendre le pointeur d'agent conforme au formateur qu'il embarque            |
| `b077a695` | feat(devkit): verser le savoir de dev du dépôt dans le skill livré, et chaîner les skills |
| `807a5309` | feat(devkit): verser au skill les quatre règles de méthode que six mois ont payées        |
| `ebefd757` | docs(agents): codex LIT les skills depuis la 0.154 — la table du dépôt était périmée      |
