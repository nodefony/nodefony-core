---
date: 2026-08-01
session_id: df99d285-3ef0-49c7-869f-dbf73c1bb4a9
focus: fermer les deux gestes de l'analyse du banc — puis découvrir que la « vitrine de code » était déjà là aux trois quarts
---

# Session retro — 2026-08-01 — df99d285

## Fait

- **`6f084cf9`** — le banc de VÉRITÉ n'exerçait que 3 des 7 générateurs. Deux étapes neuves :
  `create service` + méthode d'exemple REMPLACÉE + `create command --service` (jouée AVANT les
  entités, seule position qui la rende déterministe) ; puis la commande générée **s'exécute** et
  sa SORTIE est lue. Sonde T9 élargie à `devkit:card` (+ 2 cas de selftest).
- **`1358ae8d`** — `create service --inject <AutreService>` : la dépendance déclarée au
  constructeur, avec son appel réel (méthode CHERCHÉE dans le service visé). Note de sortie qui
  apprend le geste quand la cible porte déjà un service. 3 gardes avant écriture.
- `MIGRATION_STATUS.md` : ligne devkit mise à jour (les deux gestes de l'analyse sont soldés).

## Coûts évidents

- **4 runs complets du banc de vérité** (~1 min en `--link`, ~2 min en isolé) — dont 2 uniquement
  pour prouver un débranchement. C'est le prix de la règle « voir le test rouge », et il est juste.
- Deux rebuilds du cœur pour un débranchement de 3 lignes (le BIN lit `dist/`). Les tests du
  dépôt, eux, lisent la SOURCE — le débranchement s'y prouve sans rebuild (fait pour les 3 tests
  de `--inject`, ~3 s).
- Un faux diagnostic de cwd : `vitest` lancé depuis la racine au lieu de `src/nodefony` →
  « describe is not defined ». Déjà gradué en `feedback_bash_cwd_drift`.

## Recommandations

1. **Compter les occurrences ACTIVES d'un geste qu'on croit enseigné** — `@inject(` : 0 en code,
   2 en commentaire. Une commande, et elle explique un rouge de banc entier.
2. **Confronter un plan de « code de référence » au terrain avant d'écrire** — 4 items sur 6
   étaient déjà couverts. Quatre `rg` contre une session.
3. **Mesurer ce qui serait copié avant de trancher copier/publier** — le décor d'un test realtime
   pèse ~100 lignes de plomberie framework.

## Patterns récurrents (déjà gérés)

- ✅ Le débranchement prouvé par `git diff --stat` avant de conclure (les 3 tests `--inject` vus
  rouges sur leurs 3 causes distinctes, en une passe).
- ✅ Sortie capturée ENTIÈRE dans un fichier, jamais `| tail` (les logs de banc font 300+ lignes).

## Commits produits

| Commit     | Sujet                                                |
| ---------- | ---------------------------------------------------- |
| `6f084cf9` | le banc de vérité n'exerçait que 3 des 7 générateurs |
| `1358ae8d` | `create service --inject` — la dépendance se DÉCLARE |
