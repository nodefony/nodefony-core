## Suite — CONSOLIDATE, hook de langue, CI prettier

- **CONSOLIDATE** (`d212db3a`) : 5 thèmes gradués (83 frictions, 2 mémoires neuves), spec MCP
  enfin surveillée (`AMONT.json`), `check-claims` maxTurns 80. Plan d'action tranché : clé `counts`,
  `--dependents`, verrou `test:all`, ticket flakes ; condition de levée avant la beta ; banc A/B non.
- **`check:lang` au pre-commit** (`7ac4b985`) : fichiers indexés sous `src/`, 0,2 s ; vu refuser.
- **CI rouge trois commits de suite** : prettier 3.9.9 (ma montée) reformatait un `SKILL.md`.
  Corrigé `d1930bea`. Friction : **une montée de FORMATEUR reformate le dépôt — lancer
  `format:check` après, pas seulement la suite de tests.**
- Le test Vite « port occupé » a rougi sur Windows puis macOS le même jour → ticket flakes.

| Commit                         | Sujet                                            |
| ------------------------------ | ------------------------------------------------ |
| 6a769d8f · 5a426b67 · 42f5d5e7 | décor Mongo vierge · fronts Vite · sonde de port |
| d212db3a                       | consolidation                                    |
| 7ac4b985                       | check:lang au pre-commit                         |
| d1930bea                       | ligne stable sous prettier 3.9.9                 |
