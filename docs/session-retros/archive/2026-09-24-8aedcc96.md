---
date: 2026-09-24
session_id: 8aedcc96-b2e8-40e8-95d6-8e0ee666536e
focus: CI rouge réparée, montée des dépendances (Angular borné), #456, trois défauts de décor et de sonde
---

# Session retro — 2026-09-24 — 8aedcc96

## Fait

- **CI rouge** (3 OS) : `doctor` refusait l'app générée — un controller créé dans un module
  importait `@nodefony/user` sans le déclarer (régression de `cd69c97e`). Le générateur déclare
  désormais ce qu'il écrit avec la règle même de `doctor` (`nodefonyImports` extrait de
  `packageDeps.ts`). + test Windows qui littéralisait un chemin.
- **Montée de 30 dépendances** (minor/patch) via `deps:check --json` + script de réécriture.
  npm a dé-hissé `jsdom` et la famille Angular → fichiers de test non démarrés, non comptés.
- **Angular borné à 22.1** : `@angular/build` 22.2 casse analogjs (`cache.has`) ; gabarits en
  tilde, sinon toute app Angular générée tirait la 22.2.
- **#456** fermé : plus de connecteur Drizzle `default` sous infra non SQL.
- **Trois points** : base `nodefony_boot` remise à zéro par `--mongo` ; test qui exige que chaque
  front Vite démarre ; `isPortListening` qui déclarait libre un port occupé sous charge.

## Frictions

- Test unitaire vert, boot réel rouge (#456) : `this.options` arrive fusionné avec les défauts.
- 117 rouges Mongo identiques sans le diff : décor sale (cf `feedback_stale_decor_poisons_verdicts`).
- Une CI entièrement verte sur un front Angular mort.
- Un timeout « de charge » cachait un défaut de sonde (minuteurs avant I/O).
- `_state` de reprise disait « 7 commits non poussés » : ils l'étaient. Le vérifier au `git`.

## Non fait / hors preuve

- `--load`, `--dialects` non lancés. Studio non regardé à l'écran après Mantine/icônes.
- Flakes de charge restants en suite unitaire (`RedisBackplane`, `migrate-adopt`) : verts isolés,
  cause probable = parallélisme de la suite — sujet à ouvrir en ticket.
- Panne GitHub en cours : #466–#471 hors tableau, ordre de #467 non posé.

## Commits produits

| Commit   | Sujet                                                                                   |
| -------- | --------------------------------------------------------------------------------------- |
| 49b5010a | fix(scaffold): déclarer dans un module chaque paquet que ses fichiers générés importent |
| d4959165 | chore(deps): monter les 30 dépendances en retard de ligne                               |
| e8664762 | fix(deps): borner Angular à 22.1                                                        |
| 7cc1b57b | fix(orm): pas de base SQLite sous infra non SQL (#456)                                  |
| 6a769d8f | test(test-all): base MongoDB vierge à chaque `--mongo`                                  |
| 5a426b67 | test(framework): chaque front Vite déclaré démarre                                      |
| 42f5d5e7 | fix(cli): port occupé jamais déclaré libre sous charge                                  |
