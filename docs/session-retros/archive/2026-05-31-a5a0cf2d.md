---
date: 2026-05-31
session_id: a5a0cf2d-5442-485c-a853-e3a59c5c2662
focus: Création back du module @nodefony/documentation (data plane doc transverse) + activation runtime
---

# Session retro — 2026-05-31 — a5a0cf2d

## Tool usage

| Outil |                                           Calls (approx) |
| ----- | -------------------------------------------------------: |
| Bash  | très élevé (shell instable → beaucoup de retries/probes) |
| Write |                                 ~12 (fichiers du module) |
| Edit  |                       ~8 (index.ts, studio, fixes build) |
| Read  |                                                      ~15 |

## Ce qui a été fait

- **Module `@nodefony/documentation` créé À LA MAIN** (pas via scaffold auto — annulé par user) :
  package.json/tsconfig/rollup/vitest, schema Zod + builder + config, interfaces (IDocumentation),
  frontmatter parser (sans dep), slug safety (anti-traversée), docScanner (racine + modules),
  DocumentationService (stateful, cache TTL, providers {{ }}, GitService), DocumentationController
  (mince), erreurs typées, index.ts (exports + onKernelReady providers).
- **Activé** dans `index.ts` racine `@modules` (avant studio) + symlink npm install + dist racine.
- **Ancien `DocumentationController` POC retiré du Studio** (suppression franche) → 0 collision.
- **Validé runtime** : tree 200 (17 sections / 45 pages / 6 sections modules co-localisées) ;
  page module 200 + sourceUrl git réel (branche `claude-ts` via GitService) ; sécurité 404 sur
  slug inexistant ET traversée `..` (log serveur DOC_UNSAFE_SLUG, body générique = Zero Trust).

## Coûts évidents

- **Shell Bash extrêmement instable toute la session** : sorties dupliquées x2-x3, commandes en
  parallèle annulées en cascade, `tr`/`sed`/`cat` qui renvoient vide. → ÉNORME gaspillage de tool
  calls (probes `echo`, retries, lecture log par 5 méthodes différentes). Cause non élucidée
  (charge machine ? serveur dev + 4 Vite qui tournent ?). Leçon : **1 commande Bash à la fois,
  jamais en parallèle quand le shell délire**, et privilégier Read sur fichier plutôt que cat/sed.
- **`npm run clean` détruit le dist/index.js RACINE** (app) → boot ERR_MODULE_NOT_FOUND. Toujours
  `npx rollup -c` racine après clean (cf feedback_root_dist_stale_modules — confirmé).
- **Build turbo background incomplet** : drizzle/studio dist manquants après clean → 2 boots ratés.
  Fix = `npm run build` foreground (régénère tout) PUIS start.
- **Piège cwd** : un `cd` dans une commande a fait dériver le cwd → `start.sh` introuvable + dist
  relatifs cassés (cf feedback_cd_startsh_relative_path — re-confirmé).

## 💶 Coût (€)

- Non mesurable fiablement ce tour (transcript courant = 110 lignes, le gros est dans le fichier
  précédent). Ressenti : cache-dominé + beaucoup de tool calls gaspillés par l'instabilité shell.

## Recommandations

1. **Quand le shell Bash renvoie des sorties dupliquées/vides → STOP parallélisme.** Une commande,
   attendre, vérifier. Ne pas relancer 5 variantes en parallèle (toutes annulées si une échoue).
2. **Lire un fichier = Read, pas `cat`/`sed`/`tr`** quand le shell est instable (Read est fiable,
   le pipe shell non).
3. **Après `npm run clean` : toujours `npm run build` foreground + `npx rollup -c` racine** AVANT
   tout start (sinon dist partiels → boot crash). Codifier dans le skill start-server ?
4. Mémoire créée : `project_controller_registry_global_dette` (registre controllers non
   module-scopé) — dette d'archi validée terrain.

## Patterns récurrents (déjà gérés)

- ✅ Module créé via convention-frère (redis pour config Zod, http pour pattern onKernelRegister).
- ✅ Suppression franche du POC (pas de back-compat).
- ✅ Vulgarisation pédagogique (SSR/SSG/CSR, DI container, `#` privé, scope vs registre) à la demande.

## Commits produits

| Commit  | Sujet                                                                                     |
| ------- | ----------------------------------------------------------------------------------------- |
| (aucun) | Code NON commité — reste tâches 1+2+3+4 (docs module, tests vitest, front Studio, commit) |
