---
date: 2026-06-14
session_id: c8408448-d0bd-4b93-b43a-3dffcaac10e6
focus: P6 J6 autorisation (AuthorizationService + voters) + J7 décorateurs (@IsGranted/@Anonymous/@CurrentUser) + preuve E2E
---

# Session retro — 2026-06-14 — c8408448

> Reprise après le `_state` J4 JWT. Cible : J6 (noyau autorisation) → J7 (décorateurs) → preuve E2E.

## Fait

- **J6a autorisation** (`4e336d50`) : `AuthorizationService.decide()` (affirmative + DENY veto, défaut DENY,
  **fail-closed si voter throw**), `voterRegistry` (fabriques pluggables), `RoleVoter` (niveau A, ABSTAIN si rôle absent),
  firewall pose le token dans l'ALS + `roleHierarchy` au container. Niveau B (RBAC ORM) **différé J6b** (pas de consommateur). 193 tests.
- **J7 décorateurs** (`df673ef8`) : `@IsGranted`(AND empilé / OR tableau / `{subject}`) · `@Anonymous` · `@CurrentUser`,
  **dans @nodefony/framework** (0 import security → 0 cycle ; moteur par nom). Garde `Resolver.executeAction` AVANT `newController`.
  Descripteur figé sur `route.actionMeta` (mémo) → 0 Reflect/await/alloc si non gardée.
- **Pont forward** (`4a11523b`) : test du chemin `route=null`/`computeActionMeta`.
- **Preuve E2E** (`5bf2087a`) : route réelle (zone test-secure) → curl + test auto **admin 200 / user 403 / anon 401**.

## Frictions (→ RETEX.md SAS, par thème)

- **memory.test ≠ gate de régression** pour un changement Resolver/pipeline : j'avais annoncé « fait » avant la suite
  intégration HTTP (463). Le user a dû insister 2× (« testé à fond ? », « ça touche le Resolver »). Lancer `test:integration` AVANT.
- **chokepoint → matrice de ponts** : `executeAction` traversé par HTTP/WS/forward ; le forward oublié jusqu'à la passe rigueur.
- **kit périmé (DEVISE)** : le kit disait « décorateurs au hook beforeResolve » — FAUX (fire avant résolution de route). Vrai seam = `executeAction`.
- **`#private` casse le proxy de test** `Object.create` (brand check) → convention Resolver = `private _xxx` (7 fails → fix).
- **deep.equal d'un meta partagé** re-cassé (2×) : `security:null` ajouté à `RouteActionMeta` → snapshot `bare` à MAJ.

## Décisions

- **Découpe J6a/J6b** : noyau autz (decide+voters) maintenant ; RBAC ORM persisté différé (multi-module, pas de consommateur ;
  se branche comme un voter sans toucher `decide()`).
- **Décorateurs dans framework, pas security** : ils écrivent leur metadata, le moteur est appelé par nom → 0 cycle (framework⊥security vérifié).
- **Garde au Resolver** (pas beforeResolve) : post-résolution + pré-instanciation, partagé HTTP+WS → « 1 garde = N transports ».

## Honnêteté (non prouvé)

- « 1 garde = N transports » **prouvé HTTP** (E2E live + auto). **WS GRANT = J8** (token pas encore dans l'ALS du message WS → fail-closed/refus, sûr).

## Commits

| Commit     | Sujet                                                                               |
| ---------- | ----------------------------------------------------------------------------------- |
| `4e336d50` | feat(security) J6a — AuthorizationService + voters + RoleVoter                      |
| `df673ef8` | feat(security) J7 — décorateurs @IsGranted/@Anonymous/@CurrentUser + garde Resolver |
| `4a11523b` | test(framework) — pont forward (route=null)                                         |
| `5bf2087a` | test(security) — preuve E2E @IsGranted (admin 200/user 403/anon 401)                |
