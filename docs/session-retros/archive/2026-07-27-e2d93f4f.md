---
date: 2026-07-27
session_id: e2d93f4f-367c-4fe8-afcb-53c5230f9043
focus: finir la CI (mode production sur toute branche), puis banc reverse-proxy réel et tri des alertes d'analyse
---

# Session retro — 2026-07-27 — e2d93f4f

## Fait

- **CI TERMINÉE** — `main` vert sur `f70d2894`, 16/16 jobs. Le mode du serveur devient une
  dimension de la MATRICE (24/dev · 26/dev · 26/prod) au lieu d'une propriété de branche : la
  casse du mode livré n'apparaît plus après la fusion.
- Les 19 rouges de `main` en trois familles : compte `user` absent en production (le banc se le
  crée par la commande publique), stack d'erreur et profil par frame éteints en production
  (retournés en cas propres au mode, pas skippés).
- `detachedStart` : flake diagnostiqué — `freePort()` tirait dans la plage ÉPHÉMÈRE, le système
  reprenait le port avant le child.
- Banc **reverse-proxy réel** (nginx + haproxy) : 4 raccords, vus rouges contre un haproxy portant
  le défaut d'origine.
- `proxy:generate` audité par les binaires (`nginx -t`, `haproxy -c`) : 3 défauts corrigés, dont
  `proto=https` annoncé à un client venu en clair.
- Analyse de code : 144 → 20 alertes. Tests hors périmètre, `Bearer` sans automate, tirage des
  codes de récupération sans biais, 5 faux positifs fermés avec motif.
- `dependabot.yml` créé ; actions CodeQL passées en v4.

## Frictions

- **Diagnostic annoncé trop vite** : j'ai décrit un déni de service par en-tête avant de mesurer.
  La mesure a montré 0,3 ms sur l'entrée que je croyais pathologique — le pire cas d'un ReDoS
  polynomial exige un ÉCHEC final. Corrigé publiquement et dans le test.
- **Deux échecs d'intégration accusés à tort** : ils venaient de mon propre décor (`NF_BIND_ALL=1`
  active `trustProxy`). Vérifié en restaurant un serveur standard avant de conclure.
- **Chemin relatif compté à la main** (`../../../../../..`) faux, et le repli était SILENCIEUX :
  la CA introuvable faisait valider par le magasin système. Remplacé par une recherche ascendante
  qui LÈVE.
- `docker compose` refusait de démarrer les proxies (réseau recréé, ID périmé dans l'état du
  projet) — contourné par `docker run` sur le réseau existant pour ne pas toucher à l'infra du
  user, montée depuis 4 jours.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `9b3e5b55` | le mode livré s'éprouve sur toute branche                        |
| `ea020bce` | le banc réservait ses ports dans le vivier du système            |
| `f70d2894` | la config générée annonçait « https » à un client venu en clair  |
| `32c1d29f` | quatre raccords qu'on ne peut pas se relire à soi-même           |
| `a722715c` | l'analyse tournait sur un Node qu'elle ne déclare pas            |
| `1d8ae116` | ce que le dépôt consomme, et que personne ne relit (dependabot)  |
| `9eb905fd` | l'extraction du porteur ne dépend plus d'une garde qu'on n'a pas |
| `7948d819` | les codes de récupération naissaient d'un tirage biaisé          |
| `0086a38f` | pourquoi ces deux sondes n'exigent aucune autorité               |
