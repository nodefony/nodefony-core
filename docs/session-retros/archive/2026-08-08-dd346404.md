---
date: 2026-08-08
session_id: dd346404-14f1-4399-9713-f369754b815f
focus: Deux bugs consignés corrigés (status/stop, port Vite), gates skills réparés, merge sur main
---

# Session retro — 2026-08-08 — dd346404

Quatrième session du 08-08. Partie des deux bugs laissés par la 08-08c, terminée par la
première remontée sur `main` depuis longtemps — avec un rouge de forge qui n'était pas le mien.

## Fait

- **`9748a330`** — `status` et `stop` délimitaient deux « mon projet » différents. Une seule
  partition, faite là où les syscalls ont lieu ; `buildDevStatus` redevient pure (elle payait un
  `lsof` par pid). Runtimes étrangers NOMMÉS, ports ATTRIBUÉS à leur projet.
- **`6ca77b2f`** — le repli de port Vite existait, l'échéance de démarrage le contournait.
  `startupTimeoutMessage` (pure) reporte le constat ; premier test du conflit de port en vrai spawn.
- **`4d996b5a`** — trois gates skills empilés derrière un seul (`&&`) : description à 1270 car,
  renvoi mort, recouvrement « WCAG ». Le recouvrement a été SUPPRIMÉ, pas arbitré.
- **`4b66c9cf`** — `@sveltejs/vite-plugin-svelte` 7.2.0 → 7.3.0 (les deux sites déclarants).
- **`635b9f12`** — merge sur `main` (336 fichiers), conflit unique résolu par suppression du
  workflow de publication mort. **6 workflows verts sur `main`**, `npm audit` à 0.
- PR #10 (dependabot mermaid) : rebase demandé → **fermée par dependabot lui-même**
  (« no longer updatable »). Plus aucune PR ouverte.

## Frictions

- **Un gate en chaîne `&&` ne dit pas combien de défauts restent derrière le premier** — et
  l'étape SUIVANTE du job n'avait jamais tourné, donc n'avait jamais rien prouvé.
- **Un verdict de CI se lit avec son horodatage et son SHA** : « 7 fail » datait de 08:20 UTC,
  avant toute la session. J'allais annoncer un échec qui n'existait plus.
- **`git diff A...B` compare la base commune à B**, pas deux branches — j'ai failli annoncer une
  régression d'actions au merge alors que les deux côtés étaient identiques.
- **`devDependencies` ne protège rien de ce qu'un bundler INLINE** — corrigé par le user :
  mermaid part bien en production dans le bundle pré-buildé de Studio.
- **Chercher la régression plutôt que l'attendre a payé** : sans `lsof`, le nouveau filtre aurait
  annoncé « aucune instance » pendant que le serveur répond. Trouvé avant de pousser, pas après.

## Ce qui a coûté

- Le user a exprimé une inquiétude forte sur la zone (« la ligne de vie du framework ») : la
  réponse utile n'a pas été de rassurer mais de PROUVER — diff montrant zéro ligne de kill
  touchée, puis cycle d'arrêt réel (4 process tués, 0 résiduel) et relance.

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `9748a330` | fix(core) — status/stop, un seul « mon projet »             |
| `6ca77b2f` | fix(frontend) — repli de port Vite contourné par l'échéance |
| `4d996b5a` | fix(skills) — trois gates empilés derrière un seul          |
| `4b66c9cf` | chore(deps) — vite-plugin-svelte 7.3.0                      |
| `635b9f12` | Merge branch 'claude-ts' sur `main`                         |
