---
date: 2026-09-01
session_id: b152b0c5-118b-4c1e-81b5-ff8032b2332d
focus: dépendances, réparation de trois flakes, merge sur main, recalage de la feuille de route, trois alertes d'analyse de code
---

# Session retro — 2026-09-01 — b152b0c5

## Fait

- **Dépendances** : 29 écarts montés (`tsx`, `@vitejs/plugin-react`, `markdown-it`, `react-router`,
  `@tanstack/react-table`, `@mantine/*`, `svelte`, `zod`, `qs`, `@node-rs/argon2`). Deux blocages
  d'amont énoncés au lieu d'être contournés : TypeScript 7 (Angular + typedoc exigent `<6.1`) et les
  4 alertes `moderate` de `npm audit` (chaîne `drizzle-kit` → `@esbuild-kit` déprécié, devDep, gate
  CI `--audit-level=high` vert).
- **Un défaut de portabilité RÉEL** : `.pathname` d'une URL `file:` rend `/D:/a/…`, relatif au
  lecteur courant sous Windows → `D:\D:\…`. Trois jobs CI rouges. Éprouvé sans machine Windows en
  injectant `path.win32`.
- **Trois flakes réparés** — tous du même défaut : juger sur une prémisse supposée. Ordre de
  `sessions/mine` (que le produit ne promet pas), port « libre » tiré au sort, budget de 60 s.
- **PR #16 mergée**, `main` fast-forward sur `claude-ts` (223 commits, 0 divergence).
- **Feuille de route** : 2 cases prouvées au code, un statut `⏭️` que le comptage ignorait, un total
  qui ne tombait pas juste.
- **3 alertes d'analyse de code fermées** : 1 ReDoS (high) + 2 pollutions de prototype (medium).

## Coûts évidents

- **Deux délégations perdues (~200 k tokens)** : les deux sous-agents ont confronté les libellés au
  FICHIER audité au lieu du code. Résultat inutilisable, travail refait à la main sur les cas
  décisifs. Le prompt exigeait un ancrage `fichier:ligne` — sans interdire le document audité.
- Une commande refusée par le harnais (`cd` relatif) a fait tourner un test sur un fichier NON patché.
- `test:all` joué deux fois (~6 min chacun), dont une inutile faute d'avoir vu que la CI portait déjà
  le verdict sur le même commit.

## Recommandations

1. **Prompt de délégation** : quand la tâche est « ce document dit-il vrai ? », interdire
   explicitement le document lui-même comme source d'ancrage. Sans quoi le verdict est circulaire.
2. **Aligner `scripts/test-all.ts` sur la CI** (`development --no-watch` pour la phase `--load`) :
   deux implémentations d'une même règle, dont la locale rend un faux rouge documenté depuis juin.
3. **Avant de croire un seuil qui saute en local**, regarder avec quel décor la forge le joue.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son instrument avant le fichier (motif `awk` cassé, puis refus de commande).
- ✅ Voir le test ROUGE une fois (mutation sur `admin-dataplane`, garde débranchée sur `envOverride`).
- ✅ Lire un journal via `nodefony-run-log-report` plutôt qu'un `tail`.

## Commits produits

| Commit     | Sujet                                                   |
| ---------- | ------------------------------------------------------- |
| `9f7cdbfb` | chemin des vitrines par `fileURLToPath` — CI Windows    |
| `e5f9fde4` | montée des dépendances, sauf ce qui est bloqué en amont |
| `c1b7ef6d` | cesser de juger sur des prémisses que rien ne garantit  |
| `7073a636` | deux cases prouvées, statut « caduc » rendu visible     |
| `0f90ee2a` | bandeau recalé, dont un total qui ne tombait pas juste  |
| `b24adaed` | fermer les trois alertes d'analyse de code              |
