---
date: 2026-08-22
session_id: b1e05186-c1af-4d94-92f9-cc1c50d7a170
focus: la doc et les types atteignables par MCP, puis le jeton qui va enfin là où l'agent le lit
---

# Session retro — 2026-08-22 — b1e05186

## Focus

Lot 0 du kit MCP (rendre atteignable ce que `rg` n'indexe pas), puis un blocage
utilisateur — MCP en 401 — qui a ouvert quatre défauts en cascade.

## Fait

- **Doc et types par MCP** : `searchModuleDocs` (recherche cross-module, termes
  cumulatifs, sans accents), `readSymbolDeclaration` (la SIGNATURE, lue dans les
  types livrés — le graphe n'en porte aucune), découpe par sections (une page du
  framework pèse 50–80 ko). Outil `nodefony_docs`, `nodefony_symbols` enrichi.
- **Le 401 du user, trois causes distinctes** : l'app lisait son propre JWKS par
  HTTPS chez elle-même (`SELF_SIGNED_CERT_IN_CHAIN`) → vérification locale ; une
  ressource n'avait qu'une adresse → `acceptedResources` (http ET https) ; une
  panne de vérification sortait en 500 avec trace → verdict `unverifiable` (503).
- **La vraie cause du 401 côté client** : `.mcp.json` fait `${NF_MCP_TOKEN}`,
  résolu dans l'environnement de l'AGENT — jamais dans `.env.local`.
- **Le jeton va où l'agent le lit** — 4 emplacements constatés outil par outil,
  `--ttl` (borné 30 j), `--agent <liste>`, rotation muette qui suit les porteurs.
- **`AGENTS.md` généré** : dit comment chercher une doc que `rg` n'indexe pas.

## Coûts évidents

- **Deux fois corrigé après insistance du user** : « pareil pour les types ? »
  (j'avais borné au lot 0) et « Codex, tu as loupé quelque chose » (j'avais pris
  une absence de chaîne dans un binaire pour une preuve). Les deux fois il avait
  raison — le coût est le run refait, pas la correction.
- **Nombreux `turbo build --force` + attente du DevSupervisor** entre chaque
  vérification runtime. Batchables pour partie.
- Un rouge d'intégration attribué à tort au diff avant d'être daté (07:55).

## Recommandations

1. **Décor du banc ≠ produit livré** — thème neuf au SAS, deux occurrences le
   même jour. Candidat à graduation si ça revient.
2. **Installer l'outil pour lire son source** plutôt que citer une doc : sur 4
   agents, 2 emplacements ne sont documentés nulle part ailleurs.
3. Un test qui ne peut pas être vu rouge doit le DIRE, pas prétendre garder.

## Patterns récurrents (déjà gérés)

- ✅ Débranchement systématique avec preuve d'empreinte (7 gates vus rouges).
- ✅ Sonde suspectée avant l'agent qu'elle juge (`feedback_bench_probe_false_verdicts`).
- ✅ Preuve sur l'artefact reçu (serveur réel, pas seulement les tests).

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `260a2128` | doc et types livrés sous node_modules — donc introuvables               |
| `f3b837a2` | toutes les briques ne sont pas des MODULES, et deux `file`              |
| `29d98632` | une application n'a pas à se joindre par le RÉSEAU pour se vérifier     |
| `79968ca5` | un jeton est opaque à l'œil — le commentaire dit ce qu'il PEUT          |
| `61c9366a` | l'AGENTS.md pointait huit docs qu'une recherche ordinaire ne trouve pas |
| `dc5f1bc0` | `.env.local` n'est pas l'environnement de l'agent — et 15 min           |
| `0378b8a0` | le jeton va où l'AGENT le lit — quatre emplacements CONSTATÉS           |
| `1d53d9b3` | la rotation SUIT les agents déjà câblés                                 |

## Non lancé / hors preuve

- `npm run test:all`, gate mémoire, suites ORM/realtime/studio.
- `client-abort-499` rouge — décor (journal figé), pas une régression du diff.
- Tests d'intégration `framework` et `devkit` exclus des passes.
