---
date: 2026-06-14
session_id: 88d1ad2f-18c4-497d-965a-51db8afcd300
focus: P6 J4 — authentification JWT EdDSA (authenticator + keystore + TokenService + endpoints)
---

# Session retro — 2026-06-14 — 88d1ad2f

## Fait

- **P6 J4 JWT livré + commité** `4e4e7b2c` (26 fichiers, +2383). Branche `refactor/p6-security`.
  - `JwtAuthenticator` (Bearer EdDSA, jose lazy, allowlist `["EdDSA"]`+aud+iss+typ RFC 8725, denylist `jti`+`invalidBefore`, sub revalidé).
  - `JwtKeystore` Ed25519 (source env→fichier(chmod 600)→mémoire+warn, kid thumbprint 7638, JWKS public sans `d`).
  - `TokenService` (password grant→access+refresh opaque haché, rotation+reuse detection RFC 9700, **orchestrateur du seam `gc()`** : `runGc()` public + timer unref/jitter).
  - Endpoints framework `TokenAuthController`/`mountTokenAuthRoutes` + zone test M2M `/nodefony/test/m2m`.
  - jose@6.2.3 (dep runtime security + external rollup) ; config `tokenStore` + `jwt.{issuer,keystore}`.
- Lexique security : ajout `cnf` (sender-constrained), Token Exchange (RFC 8693), `act`.

## Cadrage amont (avant code)

Session dirigée par le user via questions au fil de l'eau (keyset persisté ? jitter+setInterval ? cron ?
pourquoi pas le module secu porte l'API ? OAuth2 hors-plan ?). Chaque réponse = décision tranchée + POURQUOI.
RFC 8725 confirmées à la source + chaîne jose prouvée end-to-end AVANT de coder.

## Frictions / leçons (→ RETEX.md)

- jose v6 `generateKeyPair("Ed25519",{extractable:true})` (kit périmé disait `"EdDSA"`) — vérifier le code de la lib.
- jitter timer = phase (setTimeout initial) pas intervalle ; seam `runGc()` public → délègue au cron P5.0b.
- tester un Service hors serveur = vrai Container + faux kernel capturant `onBoot`.
- `git diff HEAD` ignore les fichiers neufs → grep explicite avant security-review.
- piège shell multiligne re-vécu (2×) + ugrep sans lookahead `(?!...)`.

## Gates (tous verts)

build monorepo 0 TS · security **181 tests** (+38 : matrice 8725, keystore, rotation/reuse, ponts firewall/api.request/subscribe) ·
mémoire HTTP+WS **9/9** · **banc serveur réel curl 7 ponts** (émission/Bearer 200/401+challenge/rotation/reuse→401/bad-cred) · security-review **0 blocker**.

## Commits produits

| Commit      | Sujet                                                                                  |
| ----------- | -------------------------------------------------------------------------------------- |
| `4e4e7b2c`  | feat(security): auth JWT EdDSA (J4) — authenticator, keystore, TokenService, endpoints |
| _(clôture)_ | docs(security,retro): lexique DPoP/Token Exchange + retex J4                           |
