---
date: 2026-09-05
session_id: 32e426da-0d24-48cf-a330-9567d671e17c
focus: #192/#193 soldés, puis quatre défauts de `doctor` trouvés en s'en servant — et la CI remise au vert
---

# Session retro — 2026-09-05 — 32e426da

## Fait

**La grappe `nodefony -h` est soldée** (#192, #193, page de manuel comprise), puis la session a
basculé sur ce que le user constatait EN UTILISANT l'outil.

- **#192** — 39 descriptions de commandes réécrites (français, minuscule initiale, ≤ 53 car., soit
  la place que la mise en page laisse RÉELLEMENT) + les 3 options globales restées en anglais.
  Gate neuf `commandDescriptions.test.ts`, adossé à `descriptionWidth` extraite de `renderHelp` —
  jamais à un nombre écrit dans le test.
- **#193** — le pied de l'aide promet `nodefony <commande> --help` ; **dix** commandes le
  démentaient, sept de plus que ce que le ticket avait relevé. Sept refusaient en code 64, trois
  s'EXÉCUTAIENT — `nodefony stop --help` arrêtait le serveur, `completion --help` crachait sept
  cents lignes de shell. Socle `cli/usageReport.ts` (rendu pur), 23 cas qui APPELLENT chaque
  commande au lieu de lire son analyseur.
- **Page de manuel** rangée par intention, via `grouperCommandes` — la même fonction que `--help`.
- **CI verte** : deux causes empilées sur « Code généré » (une date de fichier non restaurée par
  le banc ; deux entités d'un autre dialecte non déclarées), plus le rouge Windows (axiome 2).
- **Quatre défauts de `doctor`**, tous trouvés en s'en servant — cf frictions.

## Frictions (versées au SAS par thème)

- Un contrôle qui ne pouvait **jamais** être vert, et un décor qui parlait la même langue inventée.
- Un geste prescrit sans qu'on ait constaté qu'il s'applique — dont un geste nuisible.
- Un état NORMAL rangé en « anormal » : 22 des 23 états « non exécuté » condamnaient en forge.
- Restaurer un fichier sans restaurer sa DATE ; un générateur aveugle au `dist` périmé.
- `.not.toThrow()` qui jette le rapport de la commande — deux allers-retours de forge pour rien.
- Des états de mesure non indépendants : mon premier tableau de chiffres était faux.
- **L'usage a trouvé 6 défauts là où deux audits `fable` en avaient rendu 11.**

## Coûts évidents

- Trois débranchements sur cinq n'ont pas débranché du premier coup (motif `perl` muet, garde-fou
  vert à vide, monitor comparant 7 caractères de SHA à 8). Chaque fois, un vert qui ne prouvait rien.
- Un tableau de mesures entier refait, protocole cassé (décor partagé entre les essais).
- Un log de CI lu par un sous-agent qui a rendu une cause racine PLAUSIBLE et FAUSSE (« migrations
  manquantes ») : c'était du bruit de journal, le e2e passait 20/20. Le vrai rouge était ailleurs.

## Recommandations

1. **Ne pas relancer d'audit `fable` sur `doctor`** — budgéter de l'usage réel à la place.
2. **Un sous-agent qui rend une « cause racine » se recontrôle** : celle-ci citait une ligne vraie
   du log pour une conclusion fausse.
3. **Restaurer un décor, c'est restaurer les dates** — à porter partout où un banc écrit puis remet.

## Patterns récurrents (déjà gérés)

- ✅ Le `dist` périmé qui fait mesurer l'ancienne version (garde ajoutée au générateur ce jour).
- ✅ « Prouver que le débranchement a EU LIEU » — la règle a mordu trois fois aujourd'hui.

## Commits produits

| Commit     | Sujet                                                                               |
| ---------- | ----------------------------------------------------------------------------------- |
| `b086ecbe` | docs(cli): une description de commande est un titre d'index (#192)                  |
| `435bbf54` | fix(cli): tenir la promesse de --help sur les commandes standalone (#193)           |
| `078f36c6` | fix(cli): ranger la page de manuel par intention, comme l'aide (#192)               |
| `2a031e31` | fix(devkit-bench): restaurer un fichier, c'est restaurer sa DATE                    |
| `5c79f34a` | fix(cli): normaliser un chemin AVANT de le comparer, pas après                      |
| `9e405330` | fix(cli): comparer le verdict des migrations au mot que le producteur rend          |
| `1ebe95ad` | fix(cli): un module `policy: "dev"` qui disparaît en production n'est pas un défaut |
| `018939fa` | fix(devkit-bench): déclarer les entités d'un autre dialecte                         |
| `039db19e` | fix(cli): un contrôle SANS MATIÈRE n'est pas un contrôle bloqué                     |
