---
date: 2026-06-08
session_id: 8dab4b9b
focus: ORM Ph.2.5 (contrat CRUD) + Ph.3 (legacy kernel/orm OUT) + fix gate mémoire WS + audit couverture WebSocket
---

# Session retro — 2026-06-08 — 8dab4b9b (S5)

## Fait

- Audit SOLIDITÉ ORM (confronté au code) → 2 bugs contrat CRUD + sécu savepoint → insère Ph.2.5.
- **Ph.2.5** : `update`→`updateOne` atomique (RETURNING/findOneAndUpdate) + `updateMany` ; critère strict
  (`UnknownCriteriaField`) ; savepoint validé ; tx mongoose idempotente. Découplage ORM↔API préservé.
- **Ph.3** : legacy `kernel/orm` OUT (3 fichiers + chaîne `@entities`/addEntity/EntityConstructor + 3 méthodes Kernel).
- **Fix gate** : « heap WS 180 MB » = faux positif (sonde sans GC + serveur sans `--expose-gc`) → sonde force GC + start.sh `--expose-gc`.
- **Audit WS** : 3 trous DURS (backpressure / heartbeat non câblé / latence) → chantier prochaine session.

## Coûts évidents

- Beaucoup de cycles serveur (stop/start ×3) pour les gates mémoire/charge — inhérent (gate exige serveur live).
- 1er run mémoire = warmup flake (40 MB) → re-run pour la vérité (isolation).

## Frictions / leçons (→ RETEX.md)

- Gate mémoire sans GC forcé = mesure du garbage, pas une fuite (180 MB WS). Forcer `global.gc()` avant `heapUsed`.
- Config knob déclaré ≠ câblé : `keepaliveInterval`/`Grace` en Zod mais 0 consommateur (config qui ment). Vérifier les CONSOMMATEURS.
- Ph.2.5 committée sans `memory.test` alors que SessionStorage est dans le pipeline request → process miss (rattrapé).
- « Suspecter son diff puis PROUVER par isolation » : ws-load sans import ORM + repro serveur frais = pré-existant prouvé.

## Recommandations

1. Sonde mémoire/gate : toujours forcer GC + `--expose-gc` (gravé start.sh + CLAUDE.md http).
2. Audit config = vérifier câblage des knobs, pas juste leur déclaration Zod.

## Patterns récurrents (déjà gérés)

- ✅ memory test warmup flake (isolation = vérité) — déjà en skill nodefony-debug.
- ✅ commit FR via `git ci -F` (apostrophes) — déjà en feedback.

## Commits produits

| Commit  | Sujet                                                   |
| ------- | ------------------------------------------------------- |
| 0904d47 | docs(orm): audit solidité ORM + plan Ph.2.5             |
| 220c00a | refactor(orm): ph.2.5 contrat CRUD durci                |
| 5ba6bd1 | refactor(orm): ph.3 retrait legacy kernel/orm           |
| 31283d8 | fix(http): gate mémoire fiable (GC forcé + --expose-gc) |
