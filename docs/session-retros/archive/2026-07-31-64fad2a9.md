---
date: 2026-07-31
session_id: 64fad2a9-2f29-4428-b2f6-4a51325ecb7f
focus: Dépistage du banc, gabarit à exemple actif, et deux trous produit trouvés par un agent tiers
---

# Session retro — 2026-07-31 (4ᵉ) — 64fad2a9

## Focus

Livrer le mode `--depistage` (1 run partout, 3 sur ce qui a bougé), puis mesurer — ce qui a
exposé deux faux verdicts de l'instrument, un trou de gabarit, et deux défauts produit qu'un
agent tiers a trouvés en jouant.

## Fait

- **Mode dépistage + référence versionnée** (`ba9de547`) : `baseline.json` porte modèle, décor,
  agent, empreinte de tâche et verdicts ; sorties 0 / 3 / 78 ; unanimité, asymétrie, refus de
  décor, run non jugeable — **6 règles vues rouges** par `reference.selftest.mjs --prove`.
- **Deux faux verdicts d'instrument corrigés** : `endsWith("tâche N")` faisait juger les commits
  de DÉCOR (`0086b8c6`, extrait en `lib/passes.mjs` + selftest `--prove`), et T26 devenue triviale
  parce que la zone du gabarit couvrait sa route (`e17c407f`, énoncé refondu sur
  `/api/partenaire/depot` + empreinte de tâche).
- **Zone `machine` ACTIVE dans le gabarit** (`5c0e3580`) — l'exemple commenté ne se recopiait pas.
  T26 : **3 PASS / 3** ensuite (47/44/46 tours, 0,52–0,59 $).
- **Trou produit n° 1** (`1687c128`) : une app générée **mourait au boot en production** —
  `@node-rs/argon2` absent alors que l'encodeur par défaut l'exige. Étape « démarre en
  production » ajoutée au banc de vérité, qui **ne garde pas ce défaut-là** (décor sous le dépôt)
  et le dit en toutes lettres.
- **Trou produit n° 2** (`b0b889f5`) : `vibe` a **désarmé la zone d'exemple** du gabarit
  (`stateless: true` → `false`, `"session"` ajouté). Sonde de qualité « zone à porteur non
  désarmée », sur `deleted` — rouge sur son diff, verte sur les 3 passes Claude.

## Coûts évidents

- ~6 $ d'agents réels (3 runs T26 × 2 séries + dépistage + 3 runs vibe).
- Un run de 3 passes jeté : verdicts faux à cause du bug `endsWith` (re-jugé gratuitement ensuite
  via `--analyze-only`, qui a sauvé la mesure).
- Investigation `argon2` : ~10 commandes pour établir la cause, arrêtée avant d'avoir expliqué
  pourquoi le développement passe.

## Recommandations

1. **Classer les causes des juges** (agent vs décor) + marqueur machine — la sémantique n'est
   aujourd'hui que dans le texte, 5 juges sur 11 l'écrivent, et un correctif partiel serait
   silencieux. ~50 causes, verdict binaire chacune.
2. **Sortir le décor de `verify-generated.mjs` du dépôt** — fermerait la famille entière des
   dépendances manquantes, pas seulement `argon2`.
3. **Rejouer T22** — vibe y a desserré la CSP, la tâche mord pour la première fois, mais un run
   ne conclut pas.

## Patterns récurrents (déjà gérés)

- ✅ « suspecter son instrument » — appliqué deux fois, et c'est ce qui a évité deux conclusions
  inversées.
- ✅ « un gate jamais vu rouge n'est pas un gate » — `--prove` sur `reference`, `passes`, la sonde
  de désarmement, le test `create.test.ts`, l'étape production (qui a échoué à discriminer, et
  c'est ainsi qu'on l'a su).

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `ba9de547` | dépister plutôt que tout rejouer, sur une référence versionnée |
| `0086b8c6` | deux verdicts sur trois jugeaient le DÉCOR, pas l'agent        |
| `5c0e3580` | la zone machine devient du CODE ACTIF                          |
| `e17c407f` | la tâche 26 ne mesurait plus rien, et rien ne pouvait le voir  |
| `1687c128` | une app générée mourait au boot en production                  |
| `b0b889f5` | garder la zone du gabarit contre son propre désarmement        |
