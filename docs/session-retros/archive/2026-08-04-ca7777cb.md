---
date: 2026-08-04
session_id: ca7777cb-2990-4b62-8584-ed1a5a720c66
focus: le banc devkit — quatre juges réparés, un défaut produit fermé (T10), passe complète 30×3
---

# Session retro — 2026-08-04 — ca7777cb

## Focus

Lot A des audits `fable` (l'instrument du banc de découvrabilité), puis lot B, puis le
correctif produit que la mesure a fini par désigner. Passe complète 30 tâches × 3 runs.

## Fait

- **Lot A (4/4)** — 9 sondes ancrées sur `commandeQuiContient` (elles acceptaient une LECTURE
  de l'`AGENTS.md` pour un geste) · compteur d'éditions à la main élargi à l'arbre entier
  (`entit*` où qu'il soit + second chiffre `editsAutres`) · garde « run vide → ÉCARTÉ »
  (transcript muet OU zéro fichier) · juge de T10 déduit du décor au lieu du vocabulaire.
- **Lot B (clos)** — table « tâche → doc » remontée de la ligne 265 à la 86 dans l'app générée
  - 2 renvois doc (observabilité, webhooks) ; juge de T8 durci.
- **Lot C** — (8) supprimer T3 **REFUSÉ avec preuve** (elle porte la façade CLIENT, absente de
  T19) ; (10) « 25 tâches » **sans objet** (la référence en porte 30, T21 n'existe nulle part).
- **Défaut produit fermé** — l'app naissait avec 3 controllers à imiter et aucun service :
  `AppInfoService` rejoint le preset `complete`. **T10 : FAIL 0/3 → PASS 3/3.**
- **Effet de bord de mon propre diff, corrigé** — le câblage d'un décorateur réécrivait le
  COMMENTAIRE qui le cite (`String.replace` sans `g`, sans ancre de position).
- **Passe complète** — 30×3, 5 h 30, 12,58 $, **16/30 PASS**.
- **Un verdict FAUX identifié dans cette passe** — un agent avait committé « tâche 10 », le
  message par lequel le harnais repère ses passes.

## Coûts évidents

- 4 remesures de 3 runs (T10 ×2, T8, puis la passe complète) — inévitable : chaque correctif de
  juge invalide le verdict qu'il portait.
- Une preuve rendue FAUSSE par un `dist` périmé : le binaire lit `dist/`, vitest lit la source.
  La première vérification du correctif de décorateur montrait un échec qui n'existait plus.

## Recommandations

1. **Remesurer T10** avec l'instrument réparé — le `0/3` de la passe complète n'est pas opposable.
2. **Instruire T5** (`FAIL 0/3`, « a démarré par le framework » absent des 3 transcripts) — c'est
   la seule chute franche non expliquée.
3. **Remesurer les 4 instables à 2/3** avant de les instruire (T3, T6, T13, T14 — leçon T16).

## Patterns récurrents (déjà gérés)

- ✅ suspecter son propre diff : deux défauts de la journée venaient de mes propres modifications
  (le TSDoc réécrit, le message de garde qui annonçait la mauvaise famille).
- ✅ prouver sur l'artefact reçu : app générée, typecheck, `inspect services` — pas le gabarit.
- ✅ voir le gate ROUGE avant de le croire : chaque correctif a été débranché puis rebranché.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `ae2b06e1` | huit tâches jugeaient une lecture, pas un geste                  |
| `aec8ec81` | le juge de T10 exigeait le mot « remise »                        |
| `1a1c59b0` | l'index d'adressage arrivait après 265 lignes, pour ~100 lues    |
| `ea45d73a` | le juge de T8 se satisfaisait d'une paraphrase de l'énoncé       |
| `8e20a712` | un gate rouge rendait « exit 1 » et rien d'autre                 |
| `f9e916c4` | l'app naissait avec des controllers à imiter et aucun service    |
| `0aa7c4d9` | le câblage d'un décorateur réécrivait le commentaire qui le cite |
| `524e33f5` | la référence enregistre T10 en PASS et T8 sous son juge durci    |
| `95be53db` | un agent qui commite « tâche 10 » se comptait comme une passe    |
