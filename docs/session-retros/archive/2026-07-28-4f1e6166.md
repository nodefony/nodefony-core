---
date: 2026-07-28
session_id: 4f1e6166-bc43-4204-98ed-d0d0573103e3
focus: create service — combler les 2 trous de découvrabilité du run isolé, et prouver au banc
---

# Session retro — 2026-07-28 (nuit) — 4f1e6166

## Fait

- **`nodefony create service <Nom>`** (délégué à un sous-agent `sonnet`, plan fourni) :
  gabarit `templates/service/` autonome, câblage `@services([…])` — **seul décorateur que
  le moteur CRÉE quand il manque** (`app/base` ne le rend jamais).
- **Les 2 trous d'`AGENTS.md`** du run isolé de la veille : le générateur + la vérité
  « un service n'est pas une classe utilitaire » (3ᵉ savoir, à côté d'ISOMORPHE et
  PROTOTYPAL) ; `inspect` ENGLOBE les sources (ordre de grandeur NORMAL) + anti-repli
  quand l'outil résiste. **10 assertions** les gardent, vues rouges sur gabarits amputés.
- **Défaut trouvé par le smoke-test** : les gabarits rendaient `async initialize()` sous
  le commentaire « appelé au démarrage » — le kernel ne cherche que `init`. Méthode morte
  silencieuse, dans les DEUX gabarits (service autonome + celui de `create module`).
- **Défaut trouvé en relisant le diff délégué** : ancre de câblage en `^class` — refusait
  `export class X extends Module`, la forme que montre la doc du kernel.
- Docs : `cli/CLAUDE.md`, `cli/MEMORY.md`, skills `nodefony-create-module` et
  `nodefony-framework-dev` (+ `references/core.md`).

## Coûts évidents

- **Un rebuild du core manquant** a fait rendre un faux verdict à `--describe-json`
  (l'app charge `dist/`). Diagnostiqué en 1 tour, mais c'est le piège n°1 récurrent.
- **`timeout` inexistant sur macOS** — une commande vide interprétée comme un échec
  fonctionnel. Le piège que je venais d'écrire dans `AGENTS.md`.
- **Prettier a cassé un paragraphe au commit** → amend nécessaire.

## Recommandations

1. **Ajouter une TÂCHE de banc pour `create service`** — la règle du skill devkit-bench
   est « une capacité arrive AVEC sa tâche ». T10 la couvre par accident (il demande de
   factoriser un calcul), pas par conception. Sans tâche propre, angle mort au prochain
   générateur.
2. **CONSOLIDATE est très en retard** — `RETEX.md` fait 700+ lignes pour un objectif de
   « ~1 écran ». Plusieurs thèmes sont clos et archivables.
3. `MIGRATION_STATUS.md` non touché (comme les 2 commits de la veille sur le même
   chantier) — la ligne devkit décrit le chantier à gros grain, pas les générateurs.

## Patterns récurrents (déjà gérés)

- ✅ Preuve négative systématique (5 gardes vues rouges : câblage, gabarits, ancre, hooks).
- ✅ Sous-agent : plan précis + interdiction d'index git + interdiction des fichiers en
  cours d'édition par le principal → zéro collision.
- ✅ Sondes du banc éprouvées AVANT le verdict (39/39, zéro token).

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `04474a97` | feat(scaffold): ecrire un service ne s improvise plus, la porte visible   |
| `d1b38f4d` | fix(scaffold): le hook de demarrage d un service dormait sous un faux nom |
