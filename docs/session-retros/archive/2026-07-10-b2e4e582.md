---
date: 2026-07-10
session_id: b2e4e582-3ce2-418a-a81d-14c95bb6da3d
focus: Studio écran Stores — endpoint réseau, découvrabilité moteurs + couverture par nature, rôle connecteur
---

# Session retro — 2026-07-10 — b2e4e582

## Focus

Refonte de l'écran Stores (`/nodefony/stores`) + drill ORM pour lever 3 ambiguïtés
vécues par le user : « quelle est ma base ? » (multi-connecteurs), « quels backends
puis-je utiliser ? » (découvrabilité), « toutes les briques sont-elles portées ? »
(couverture). 100 % front + 1 enrichissement data-plane (`/kernel/api/stores`).

## Fait

- **1 commit** `d4138242` — `feat(studio): écran Stores — endpoint réseau, moteurs & couverture, rôle connecteur` (9 fichiers, +1027/−195).
- Back : `endpoint` réseau redacté par store ; `source` auto→infra honnête (gate sur infra déclarée) ; catalogue moteurs (installé×chargé) + `kind` (durable/cache) + `provides` (couverture curatée : drizzle 8/8, mongoose 5/8, redis vocation éphémère).
- Front : URL/endpoint visibles + infra en clair ; base locale sqlite dérivée ; section Moteurs (couverture par brique lue par NATURE) ; provenance (configPath + source) ; Redis multi-rôle en fiche ⓘ ; rôle connecteur (primaire/dédié) ORM + drill ; logos DB en data-URI (fix 404) ; aides ⓘ typées+dynamiques (norme).

## Frictions (→ RETEX.md)

- Vite : asset > 4 Ko servi en URL `/@fs/` → **404 via le proxy dev 5152** ; fix `?raw` + data-URI inline. (mongodb.svg / postgresql.svg)
- Couverture backend à modéliser par **NATURE** (durable vs cache), pas X/8 ; « implémenté » ≠ « vocation » (Redis token/passkey = durable-si-persistant).
- **« Cards pas aux normes » (2ᵉ fois)** : prose/Alert sur l'écran factuel → fiche ⓘ DocHint dynamique. → graduation `feedback_*` au prochain CONSOLIDATE.
- Discipline serveur : front-only = HMR (0 restart) ; restart = back seulement + attendre boot COMPLET (uptime<5s + connecteur loggé) ; 2 acteurs serveur = confusion config.
- Vérif : login BFF curl = rabbit-hole → prouver par typecheck + transform Vite + logs, confirmation visuelle au user.

## Décisions structurantes

- **Modèle de couverture par domaine** (`kind` durable/cache) : la vocation d'un backend prime sur l'existence d'une impl. Un cache ne « manque » pas les briques durables — elles sont hors vocation.
- **Rôle par connecteur** dérivé front (`default` + `:memory:`) : primaire (suit NF_DATABASE_URL) vs dédié·volatile (fixtures isolées). Lève « quelle est ma base ».
- **Fixtures mono-dialecte sur connecteur dédié** confirmé sain (dolibarr/mediasoup `:memory:`) — ne suivent pas l'infra par design.

## Commits produits

| Commit   | Sujet                                                                               |
| -------- | ----------------------------------------------------------------------------------- |
| d4138242 | feat(studio): écran Stores — endpoint réseau, moteurs & couverture, rôle connecteur |

## Prochaine étape

Porter `audit` / `totp` / `idempotence` sur **mongoose** (5/8 → 8/8) — session dédiée
(idempotence = la délicate, atomicité cross-pod à prouver sur mongo réel). Puis Palier 3 :
capabilities auto-déclarées par adapter (`package.json` `nodefony.stores`).
