---
date: 2026-08-01
session_id: 0b042e19-2079-43df-836f-22fcabacecfd
focus: créer @nodefony/devkit — et faire hériter le dépôt de son propre générateur
---

# Session retro — 2026-08-01 (d) — 0b042e19

## Fait

- **`create module` sert enfin le dépôt du framework** (`b42fb819`). Le layout se CONSTATE dans
  les `workspaces` de la racine (`scaffold/moduleLayout.ts`, pur) : `<prefixe>/@<scope>/*` ⇒ paquet
  PUBLIABLE. Rien en dur, pas même `@nodefony`. Un dépôt qui déclare déjà ses workspaces garde SES
  scripts ; son `AGENTS.md` racine n'est jamais réécrit.
- **`@nodefony/devkit` existe, GÉNÉRÉ par la commande** (`a7923c7f`) — module, controller et
  commande. Carte de visite dérivée du Kernel, composée par une fonction pure, trois portes
  (CLI · HTTP · export). `policy:"dev"`, câblé dans `create app`, adressé depuis l'`AGENTS.md`.
- **`GET /` d'une app générée ne divulgue plus son architecture** (`ffd1a8ed`).
- Deux défauts produit trouvés en dogfoodant : garde `--service` qui exigeait la méthode d'exemple
  du gabarit ; contrôle « TSDoc recollé » absent de 2 scaffolds sur 3.

## Analyse du banc devkit (demandée en fin de session)

Relevé des 28 tâches délégué (`haiku`, lecture seule), puis **recontrôlé** — un ancrage du rapport
était faux. Quatre conclusions :

- ✅ **appliqué** : le `SKILL.md` annonçait « 25 tâches » quand le code en porte **28**. Chiffre
  retiré des deux endroits plutôt que remis à jour — un nombre écrit à la main se périme.
- 🔴 **le trou est dans `verify-generated.mjs`**, pas dans le banc d'agent : sur les **7 types** de
  `create`, il n'en exerce que **3** (app, module, entity) — `controller`, `service`, `command`,
  `front` : zéro. C'est par là que le défaut `--service` du jour est passé. Geste : `create service`
  → remplacer la méthode d'exemple → `create command --service` → `typecheck`.
- 🚫 **pas de tâche d'agent pour `devkit:card`** : la doctrine du kit exige un contournement
  _crédible_, or ici il est **légitime** (`inspect` est le verbe qu'on veut lui apprendre). En
  revanche élargir la sonde de **T9** pour qu'elle l'accepte — sinon un agent qui fait le bon geste
  sort rouge (mode de défaillance n°1 du banc).
- 🚫 **pas de tâche « chaîne de générateurs »** : une chaîne mesure la robustesse du générateur, pas
  la découvrabilité — sa place est le script déterministe, pas 3 runs à 0,3 $.
- ℹ️ **soupçon FAUX** que j'avais : le banc vérifie bien que le code généré compile
  (`bench-discoverability.mjs:454-456`). C'est le dépôt qui lit des chaînes.

## Frictions

- **J'ai suivi un skill qui entérinait un écart** au lieu de lancer l'outil pour voir. Le
  `--dry-run` répondait en 3 secondes ; il a fallu que le user morde.
- **Trois `vitest run` lancés depuis la racine au lieu de `src/nodefony`** → « no tests » pris pour
  un verdict, deux fois. Le cwd d'un run de tests se vérifie AVANT de lire le résultat.
- **Le CLI tourne sur `dist/`** : un premier dry-run a « prouvé » que rien n'avait changé alors que
  seule la source était modifiée. Rebuild avant toute mesure sur une commande.
- La garde anti-effacement du dépôt a refusé mon `git checkout` sur arbre sale — elle a bien
  travaillé, mais elle m'a coûté un détour : committer avant d'annuler un fichier isolé.
- **Un test vert en `vitest` qui ne typecheck pas** : c'est le hook de pre-push qui l'a vu, après
  trois commits. `tsgo --noEmit -p tsconfig.tests.json` avant de dire « vert ».
- **Un ancrage de sous-agent faux, répercuté de justesse** : le rapport situait le compte périmé en
  `SKILL.md:553` ; il était en `:229` et `:310`. Le recontrôle a coûté une commande.

## Gates vus rouges (le débranchement a EU LIEU)

| Débranché                           | Rouges                                        |
| ----------------------------------- | --------------------------------------------- |
| détection du layout                 | 6 (dont l'écrasement de l'`AGENTS.md` racine) |
| saut de ligne du gabarit `index.ts` | 2 (TSDoc recollé)                             |
| devDependency devkit                | 2                                             |
| découverte de méthode `--service`   | 1                                             |
| lien dans la racine générée         | 1                                             |

## Preuves sur l'artefact REÇU

App générée `--link` : devDep (pas dep), manifeste `policy:"dev"`, racine sobre,
`devkit:card` sur 4 modules réels avec portes conditionnelles justes, `typecheck` + tests verts,
et en `NODE_ENV=production` **le verbe disparaît**. Route HTTP éprouvée sur serveur réel : 401
derrière la zone admin — d'où la CLI comme porte utile.

## Non fait, nommé

- Support `.raw` du moteur (prérequis d'une vue `.eta` livrée) — sans consommateur, ce serait du
  code mort : il vient avec la vitrine.
- La **vitrine de code** et sa tâche de banc.
- Les vues passeront par `Controller.renderView` (`framework/nodefony/src/Controller.ts:322`),
  jamais par un `new Eta` — rappel du user.

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `b42fb819` | le depot herite de son propre generateur de module          |
| `a7923c7f` | la carte de visite de l application, et la porte qui compte |
| `ffd1a8ed` | la racine d une app ne decrit plus son architecture         |
| `b10e4464` | cloture — un outil qui ne sert pas le depot qui le publie   |
| `50e26fbc` | un test qui PASSE en vitest peut ne pas typechecker         |
| `29cdec86` | un nombre de taches ecrit a la main se perime               |
