---
date: 2026-08-26
session_id: 902f4317-3e68-4b53-9daa-2a1ad30675d4
focus: un audit externe confronté au code — puis le dernier gap du dashboard fermé, et trois instruments qui accusaient le produit
---

# Session retro — 2026-08-26 — 902f4317

## Focus

Une IA tierce a audité le dépôt public. Trier ce qui est vrai, faux, assumé — et découvrir que
trois de ses conclusions venaient de NOS documents périmés. Puis fermer le dernier gap que le
tableau de migration déclarait ouvert (E2E système), rejouer les smokes `front`/`studio` et le
banc de durée.

## Fait

- **56 affirmations de l'audit confrontées au code** (4 lots `haiku`, lecture seule) : 46 exactes,
  3 fausses, 2 partielles, 2 non vérifiables, 3 points qu'elle n'a pas vus. Rapport HTML
  (`tmp/audit-tiers-confronte.html`, non commité).
- **Trois documents corrigés** : le README de `@nodefony/security` annonçait comme restant deux
  briques livrées ; le README racine promettait « zéro `any` » ; le `MEMORY.md` du Studio
  contredisait le dashboard sur le RBAC. Plus `CONTRIBUTING.md`, qui manquait.
- **R6 requalifié en dette de FIN de cycle** (décision user), gravé aux trois endroits qui la
  relisent : ligne P8, cellule P8.5, kit release.
- **Banc E2E système livré** — la chaîne qui manquait : écriture HTTP → transaction en échec →
  arrêt gracieux → **redémarrage** → relecture avec comparaison d'EMPREINTE. Vu rouge en
  débranchant (`sqlite::memory:`), et rouge sur les deux seules assertions de persistance.
  **Vert en forge sur un vrai Postgres.**
- **Smokes `front` + `studio` rejoués** : `studio` vert d'emblée, `front` a trouvé un défaut réel
  de l'instrument (cf frictions).
- **Soak 90 min / 61 M requêtes** : heap PLAT (+0,1 MB/h, R² 0,01), RSS +26,4 MB/h **sans
  plateau** (pente tardive +35,3), +0,56 MB par million de requêtes. ⚠️ décor non tenu — par moi.

## Frictions (versées dans RETEX.md, sous des thèmes EXISTANTS)

- Une doc périmée est lue comme la vérité par un tiers : l'audit a noté la sécurité 8/10 sur la
  foi de notre propre README.
- Le compteur de handles du soak comparait deux instantanés pris dans des régimes différents →
  « défaut PRODUIT » annoncé sur une fuite inexistante (plancher 5 → 5).
- `grep -q` sous `set -o pipefail` : SIGPIPE (141) transforme un succès en échec, de façon non
  déterministe. 6 sites fermés.
- J'ai corrigé sur une cause plausible non prouvée, et commité l'explication — le rejeu suivant
  l'a réfutée. Le commit d'après a dû corriger le précédent.
- Le pre-commit ne lançait qu'une des trois gardes des skills (coût de l'ensemble : 1 s).
- J'ai pollué ma propre mesure en travaillant pendant le soak que j'avais annoncé bloquant.
- `$?` après un pipe relu **deux fois** dans la soirée.

## Patterns récurrents (déjà gérés)

- ✅ Délégation des listes d'affirmations à `nodefony-check-claims` (4 lots `haiku`) — et
  vérification de leurs retours : l'un d'eux s'est trompé sur le décompte des paquets publiables.
- ✅ Capture de sortie entière dans un fichier avant analyse.
- ✅ Chaque gate neuf vu rouge une fois.

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `7ec3530a` | trois documents qui nous faisaient dire le FAUX + `CONTRIBUTING.md` |
| `fd251ba6` | R6 = dette de fin de cycle                                          |
| `1c4c1ef6` | banc E2E système — la donnée survit-elle au pod ?                   |
| `3a46bc06` | ⚠️ correctif smoke à cause FAUSSE (assumé, corrigé par le suivant)  |
| `cd4e8fed` | `grep -q` sous `pipefail` — 6 sites, classe entière fermée          |
| `999ca70c` | le hook ne lançait qu'une des trois gardes des skills               |
| `6f4112b7` | le compteur de handles accusait le produit d'une fuite inexistante  |
