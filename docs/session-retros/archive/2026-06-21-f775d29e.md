---
date: 2026-06-21
session_id: f775d29e-de78-4d1d-a94d-999496444cd6
focus: Page Studio API Keys (PAT) full-stack 3 modes + fix scripts kill-LISTEN + 2 kits post-Studio figés
---

# Session retro — 2026-06-21 — f775d29e

## Focus

Page Studio **API Keys** (PAT, P6.15) **full-stack**, 3 modes (Utilisateur / Administration / Tenant-P17).
Découle d'une correction de périmètre du user en cours de route (« il y a 2 modes, voir 3 avec tenant »).

## Fait

- **Backend (5 modules)** : `ITokenStore.listAll()` (contrat) + impl **4 stores** (Memory trivial / Drizzle
  `find({})` / Mongoose `find({})`+idOf / **Redis via SCAN** + `scan` ajouté au `RedisClientLike` + double
  de test) ; `ApiKeyService.{listAllPat,revokeAnyPat(actor+audit),describeCapabilities}` ; `IApiKeyCapabilities` ;
  `SecurityAdminApi` 2 endpoints (`GET apikeys` liste cross-porteur · `POST apikeys/{id}/revoke` RBAC ADMIN +
  audit acteur/cible) ; `ApiKeyController` `GET /keys/capabilities` (formulaire). Build turbo 19/19 vert.
- **Frontend (6 fichiers `routes/apikeys/*` + câblage)** : orchestrateur 3 modes (SegmentedControl gaté admin),
  KPIs, table DataGrid+Modal réutilisée user/admin (colonne Porteur conditionnelle), création secret-1× (catalogue
  → MultiSelect | libre → TagsInput), révocation confirmée, onglet aide. Câblage App/navConfig(wip retiré)/stubs.
  typecheck Studio 0.
- **Validé bout-en-bout** : clé créée par le user dans Studio → `curl /test/m2m/whoami -H "Authorization: Bearer nf_…"`
  → **200** `{identifier:"admin",roles:[…],m2m:true}` ; sans clé → 401.
- **Fix scripts** `start.sh`/`stop.sh` : `lsof -ti:PORT` → `-sTCP:LISTEN` (ne plus tuer le navigateur).
- **2 kits post-Studio figés** (ordre logique) : `project_p6_8_scopes_per_api_kit` (scopes par API : `@RequireScope`
  - découverte + `ScopeVoter`) **puis** `project_socket_mutations_idempotency_kit` (mutations socket = idempotency-key ;
    CSRF déjà résolu au handshake `checkWebsocketOrigin` B4). Autz AVANT surface de mutation.

## Frictions (coûts évitables)

- **Fausse piste « process fantôme » (la plus chère).** `lsof -ti:5152` listait un PID « en trop » que j'ai pris
  pour un serveur fantôme nodefony (+ annoncé un « trou DevSupervisor ») → en fait le **navigateur Brave** (client
  ESTABLISHED). Kills agressifs + relances inutiles avant d'identifier le PID (`ps -p`). → RETEX shell gravé.
- **Bug réel masqué = path frontend.** `/admin/apikeys` au lieu de `/apikeys` (le broker monte sous le namespace
  SANS sous-préfixe `admin`). Le **route-match hissé avant firewall** → route absente = **404** (pas 401) ; j'aurais
  dû lire 404≠401 comme « route absente » d'emblée au lieu de suspecter dist/fantôme. Discriminant : 401=montée+gardée, 404=absente.
- **Périmètre élargi en cours de route** (user) : self-service → +admin cross-user → +tenant. A imposé `listAll` sur
  les 4 stores (interface partagée = toucher tous les implémenteurs) + endpoints admin. Bien d'avoir clarifié en prose.

## Décisions structurantes (survivent au /clear)

- **Mutations = HTTP, Socket reste GET-only** (tranché avec le user) : une mutation doit traverser le pipeline
  HTTP (CSRF) ; `ApiClient` route les mutations en fetch auto. Les mutations-socket = chantier Ph.4 séparé (idempotency).
- **Admin ne crée jamais de clé pour autrui** (usurpation) : mode Admin = lecture + révocation + stats. Création = self-service.
- **Multi-API = trou de mécanique, pas de modèle** : `scopes/audience/resources` déjà dans `IAccessTokenRecord` →
  P6.8 (décorateur + découverte + enforcement) figé, 0 migration.

## Commits produits

| Commit     | Sujet                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------- |
| `c383171f` | feat(studio): page roles P6.15 + fix sticky structurel (session PRÉCÉDENTE, poussé en début de session) |
| —          | **Page API Keys + backend + fix scripts = NON COMMITÉ** (user gère)                                     |
