---
date: 2026-06-11
session_id: 40f2a99c-08b5-45cd-91e0-5d18224c345f
focus: T4 churn listeners — dernier levier coût/requête, chantier fast path étape 3 CLOSE
---

# Session retro — 2026-06-11 (4e) — 40f2a99c

## Fait

- **T4 livré** (`5a37a0b`, poussé) : ~4 onceWrappers + 5 closures + 2 removeListener supprimés PAR REQUÊTE.
  - T4a teardown http-kernel : pair `finish`/`close` + didFinish + 2 removeListener → 1 seul `once("close")`
    (node garantit close h1+compat h2 ; `writableEnded` rejoue ex-didFinish ; 499/abort intacts).
  - T4b onTimeout : closure `once` du ctor HttpContext → méthode `_onTimeout()` directe (event gardé,
    guard 0-listener).
  - T4c onRequestEnd : closure `once` du ctor Request → alias `request.body` dans `fireRequestEnd()`.
- A/B 2 paires stash/pop : old 6034/5957 vs new 6065/6145 RPS → +1,8 % méd., min(new)>max(old) mais < ±5 %
  → **structurel assumé** (3e issue du verdict, pattern T2/T3 confirmé).
- Gates : unit 456 ✓ · intégration 438 ✓ · mémoire 9 ✓ · banc routing 25 ✓. Policy `@nodefony/test` revertée.
- **Étape 3 (coût/req) du chantier fast path CLOSE** — T1 +10,8 % net, T2/T3/T4 structurels.

## Frictions

- `replace_all` a réécrit le corps de `fireRequestEnd()` fraîchement extraite → récursion infinie (Maximum
  call stack), attrapée par le HEALTH check 500 du start.sh. → RETEX.md (ordre : replace_all PUIS extraire).
- A/B structurel sans toggle env : recette `git stash push -- <fichiers>` + rebuild à CHAQUE flip (le dist
  ne suit pas le stash). → RETEX.md.

## Commits produits

| Commit    | Sujet                                                                               |
| --------- | ----------------------------------------------------------------------------------- |
| `5a37a0b` | perf(http): churn listeners par requête — levier T4, structurel (RPS dans le bruit) |
