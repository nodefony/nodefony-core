---
date: 2026-06-19
session_id: 4e44160f-386e-4d96-abad-f317f0631c85
focus: P6.12 API Keys (PAT) — authenticator + service + endpoints + tests d'attaque (e2e réel)
---

# Session retro — 2026-06-19 (soir) — 4e44160f

## Focus

P6.12 — clés API personnelles (PAT) façon GitHub, au-dessus du `ITokenStore` déjà livré (J4).
Mode pédago P6 + exigence sécu MAX (RFC + matrice d'attaques). Commit `2c3e252c`.

## Fait

- **Code** : `apiKeyFormat` (pur, `nf_<pubid><secret 256b><crc32>` base64url positionnel, CRC public =
  rejet O(1) sans store) + `ApiKeyAuthenticator` (`apikey`) + `ApiKeyService` (`apiKeys`, create/list/revoke)
  - `ApiKeyController` (framework) + `ApiKeyError` + `IApiKey`. Store **partagé avec JWT** (TokenService boot
    étendu `jwt‖apiKeys`). `JwtAuthenticator.supports()` resserré `a.b.c` → JWT+PAT coexistent. Config
    `apiKeys.{lastUsedThrottleS,maxPerSubject,allowedScopes}`. Zone `test-api` += apikey.
- **Décisions** : pas de pepper/argon2 (secret 256 bits aléatoires → sha256 suffit, justifié) ; discrimination
  JWT/PAT par FORME (découplé) ; markUsed throttlé 60 s (perf) ; anti-énum (401 uniforme + IDOR→404).
- **Tests** : 44 unit (format/authenticator/service + matrice d'attaques + compteur anti-DoS) + 8 e2e réels
  (`http/apikey-flow` : flux + 5 attaques + IDOR + coexistence JWT/PAT). 0 régression (376 secu / 522 intég /
  mémoire 9/9). Revue sécu ✅ (0 any, 0 secret loggé, RFC 6750/7009/6749).
- **Méta** : chantier **red-team P6** acté ([[project_p6_redteam_attack_tests_kit]], matrice 11 briques,
  gabarit = JWT + API Keys).

## Frictions (→ RETEX.md)

- zsh ne word-split pas `$VAR` espacée → grep multi-fichiers « No such file » + faux négatif de garde (**3×** → candidat graduation).
- `start.sh` « SKIP build » alors que la config du module test a changé → serveur avec zone périmée → `--force-build`.
- Revue sécu par grep : `git diff HEAD` IGNORE les untracked → nouveaux fichiers non scannés (croiser `git status '??'`).

## Commits produits

| Commit     | Sujet                                                                                  |
| ---------- | -------------------------------------------------------------------------------------- |
| `2c3e252c` | feat(security): clés API (PAT) — authenticator + service + endpoints + tests d'attaque |
