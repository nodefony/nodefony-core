---
date: 2026-08-27
session_id: 0ac3033f-a048-4c83-9c90-763a1cef08e6
focus: "#36 — extraire le socle client commun aux quatre fronts, puis le PROUVER (serveur réel, quatre vitrines)"
---

# Session retro — 2026-08-27 — 0ac3033f

## Fait

- **#36 livré** (`dbd3cc9a`) : `nodefony/client` porte les onze règles qui vivaient dans les hooks
  React — `connectShared` + dix `observe*` de forme unique `observeX(client, …, emit) → dispose`,
  plus la table des événements locaux. Six, pas cinq : `__reconnect__` n'était nommé nulle part et
  n'avait aucune porte. React réécrit dessus, surface publique inchangée.
- **Le socle éprouvé contre le POD RÉEL** (`a0b44695`) : `client-isomorphe-e2e.test.ts`, 10 cas —
  globale `WebSocket` → `BrowserWsTransport` → `connectShared`/`observe*`. Seize bancs WebSocket
  existaient, aucun n'employait le client livré.
- **Les quatre vitrines remises à leur rôle** : salon partagé (fan-out visible entre onglets) et
  action bi-transport (38 ms HTTP contre 2 ms socket, même process). Charte de la page d'accueil,
  feuille commune aux quatre, barre de navigation entre vitrines.
- **`socketSnapshot`/`observeSnapshot`** (`3774a7c9`) + retrait du battement périodique + sonde de
  socket au survol et bascule de la barre de debug dans la barre des quatre.

## Coûts évidents

- **Trois diagnostics perdus sur ma propre sonde** qui mesurait avant l'aller-retour serveur. Le
  piège est écrit dans le skill `nodefony-browser` ; l'attente discriminante l'a réglé en un run.
- **Deux refus du garde-fou `cd` relatif** ont annulé tout l'appel (patch python compris) — il faut
  constater l'état après chaque refus, pas enchaîner.
- Trois réécritures de la carte temps réel avant qu'elle démontre quelque chose. Un cahier des
  charges d'écran (« que doit comprendre un visiteur en trois secondes ? ») aurait économisé deux
  passes ; je l'ai improvisée, ce que l'hygiène §2 interdit précisément.

## Recommandations

1. **Le battement d'une seconde vit encore dans les gabarits d'application** (`templates/app/…`,
   gardé par `create.test.ts`) — le dépôt distribue ce qu'il vient de juger nuisible. À trancher.
2. **Trou serveur constaté et non corrigé** : un `subscribe` vers un canal refusé ou inexistant est
   ignoré SANS un mot — ni `realtime:denied`, ni erreur. Écrit dans le banc E2E ; mérite un ticket.
3. `ticket-verify --touched-by` corrigé au passage : il comparait l'arbre au commit, donc acquittait
   au seul moment où on l'emploie.

## Patterns récurrents (déjà gérés)

- ✅ Sabotage systématique des gardes neuves — a révélé deux gardes inopérantes sur douze.
- ✅ Mesure au navigateur avant de conclure — a chiffré un échec WCAG (2,5:1) que l'œil validait.

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `dbd3cc9a` | refactor(client): sortir des hooks React ce qui n'a jamais été de React |
| `a0b44695` | feat(vitrines): montrer ce que la socket change, dans les quatre fronts |
| `3774a7c9` | feat(client): un instantané de socket, et une socket qui se tait        |
