---
date: 2026-06-22
session_id: f7bc1612-0125-42aa-b5dd-f430750b529d
focus: Fail-open RBAC du data plane admin (fermé, test-driven) + front stale au changement d'identité + capture ip/ua au login
---

# Session retro — 2026-06-22 — f7bc1612

## Focus

Parti d'un bug visuel signalé par le user (« reloggé en user, je vois encore l'admin sans F5 »).
Forage → **deux trous distincts** + un test pourri ; les trois fermés, test-driven, 3 commits poussés.

## Fait

- **`62be7b0d` fix(framework)** — **fail-open RBAC** du data plane admin fermé. Le 403 était gardé
  par `request.roles.length > 0 &&` (vestige « mode mock » pré-P6) → un authentifié SANS rôle
  (`roles=[]`, créable via `POST /nodefony/user/api/users`) franchissait TOUS les endpoints admin.
  Fix = fonction pure `isAdminGranted` (fail-closed). **1ʳᵉ couverture RBAC du data plane** : unit 8/8
  - e2e `ROLE_USER`→403 et compte `roles=[]` créé via API→403. + aligne l'assertion `sessions`
    pourrie par 91403f01 (deprecated → driver/storage/revocationHardened).
- **`1560b61a` feat(security)** — capture **ip/ua au login** dans `authFlow.#openSession` (proxy-aware,
  duck-typé). La plomberie d'affichage existait déjà (DTO/`toSessionSummary`/table) mais rien ne la
  remplissait. Preuve wire : login avec UA connu → retrouvé dans `sessions/list`.
- **`4d5238b7` fix(studio)** — purge de l'état admin au changement d'identité : `AuthGuard` remonte le
  sous-arbre par clé `auth.user.id` + `AdminStore.reset()` via réaction `RootStore` (cache singleton).

## Décisions / POURQUOI

- Le symptôme du user (compte `user`=ROLE_USER) était du **stale front** (back déjà 403) ; le vrai
  trou de fond (back, `roles=[]`) était **distinct et non testé** — révélé par le forage, pas par le
  symptôme. Fixer LES DEUX.
- Décision RBAC **extraite en fonction pure** (testable isolément, comme `toUserSummary`) → le cas
  manquant `roles=[] → refusé` est verrouillé sans booter de serveur.
- Garant de sécurité = **RBAC serveur** ; le remontage front = défense en profondeur (poste partagé).

## Frictions (→ RETEX.md)

- Garde de sécurité testée seulement aux EXTRÊMES (anon→401 / admin→200) → le cas « milieu »
  (authentifié-mais-pas-le-rôle) pourrit → un fail-open survit à toute une phase.
- Rot d'assertion **cross-module** : test framework assertant un endpoint http changé sans relancer
  la suite framework → rouge dormant.
- turbo `cache hit, replaying logs` rejoue de VIEUX warnings TS → on croit son fix cassé ; vérité =
  build frais + dist sur disque.
- Agents Explore : ancrages utiles mais imprécis (`getRemoteAddress` « sur le context » = faux, sur
  sous-classes ; `UserAdminApi` « sans role » = faux, `descriptor.role`). Typecheck/terrain corrige.

## Gates

unit adminRbac 8/8 · e2e admin-dataplane 33/33 · e2e session-revocation 5/5 · gate mémoire 9/9 ·
framework unit 298 · security unit 512 · studio front typecheck OK.

## Commits produits

| Commit     | Sujet                                                       |
| ---------- | ----------------------------------------------------------- |
| `62be7b0d` | fix(framework): ferme le fail-open RBAC du data plane admin |
| `1560b61a` | feat(security): capture ip/ua à l'ouverture de session      |
| `4d5238b7` | fix(studio): purge l'état admin au changement d'identité    |
