---
date: 2026-06-12
session_id: a9a31185
focus: POC API souveraine Ph.3 BACKEND — pont api.request (data plane admin duplex REST≡WS)
---

# Session retro — 2026-06-12 — a9a31185 (4ᵉ du jour)

## Fait

- **Ph.3 backend livrée** (`a07fdf2`, poussé) : `RpcError` isomorphe (core, erreurs fetch-like
  `data.status`, Zero Trust préservé) · overload client `socket.request("/path")` → `api.request {path}` ·
  pont opt-in `RealtimeController.realtimeApiRequest()` (Studio activé) · `Resolver.queryOverride`
  (query per-invocation anti-bleed, consommée par `@Query` + shadows controller) · `AdminApiController.dispatch`
  duplex (`runAdmin` transport-agnostique, HTTP byte-identique, WS body nu) · `AdminBroker` GET→GET+WEBSOCKET.
- **Preuves** : 9 tests intég `api-souverain-bridge.test.ts` (snapshot deep-equal REST≡WS, param, query,
  no-bleed même socket, 404 producteur/router, 405 transport sans bypass, -32602) + 4 unit RpcError.
- **Gates** : memory 9/9 · http intég 438 · fw intég 76 · realtime 167 · core 1582 · studio 31 · typecheck 11/11.
- Docs : `docs/api/README.md` §11.2 tranché + §12 ; MEMORY framework/realtime ; dashboard (1 ligne jalon).

## Frictions (détail au SAS RETEX.md)

1. Race watch-rebuild ↔ import au boot → boot dégradé fail-soft avec health 200 → 9 timeouts (diag :
   grep « BOOT dégradé » du log, pas le health).
2. `grep -c` à 0 = exit 1 → start.sh silencieusement sauté dans une chaîne `&&`.
3. `git stash` partiel sous index.lock → stash dupliqué + arbre intact ; preuve `apply --check --reverse` → drop sûr.
4. TS4114⇄TS4113 inconciliables node/client (augmentation `Error.code` globale) → declaration merging.
5. (gradué, re-frappé 1×) Edit-exige-Read sur un fichier vu au `sed`.

## Décisions

- Enveloppe WS du souverain = **JSON-RPC `api.request {path}`** (plus le protocole maison POC) — doc §11.2.
- Pont **opt-in** par hub + n'atteint QUE les routes déclarant le transport WEBSOCKET (zéro bypass) ;
  mutations HTTP-only ; parse query plat (nested qs → Ph.6).
- Front useResource → **session dédiée** (directive user en cours de session).

## Commits

| Commit    | Sujet                                                                                      |
| --------- | ------------------------------------------------------------------------------------------ |
| `a07fdf2` | feat(realtime,framework,core): pont api.request — data plane admin duplex (souverain ph.3) |
