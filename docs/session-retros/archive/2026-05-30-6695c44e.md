---
date: 2026-05-30
session_id: 6695c44e-bb93-4bc7-86f9-7f3b078c864e
focus: L5a dédup pathname routing (+4,3 %) + clôture chantier perf + découverte goulet logging cluster (+28 %) + cadrage Log Backplane LB.W
---

# Session retro — 2026-05-30 — 6695c44e

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    53 |
| Edit  |    23 |
| Read  |    21 |
| Skill |     3 |
| Write |     1 |

## Top fichiers Read/Edit

| Fichier                                         |     × |
| ----------------------------------------------- | ----: |
| `project_request_cycle_perf_plan_kit.md` (Edit) |     8 |
| `MEMORY.md` (Edit)                              |     5 |
| `project_log_backplane_vision.md` (Edit)        |     4 |
| `router.ts` (Edit)                              |     2 |
| `tasks/*.output` (Read, benchs background)      |     5 |
| `Route/Resolver/IRoute/IResolver.ts` (Edit)     | 1 ch. |

## Coûts évidents

- **Investigation cluster = ~80 % des turns** (113 turns) : benchs en background + re-lectures d'interim
  (`b2uq6gbgx.output` lu 3×, `bfw619qw2` 2×) + cycles serveur (start `--cluster` ×2 + boot `/dev/null`
  manuel). Nécessaire (A/B + test logging), mais c'est le gros du coût.
- **Malentendu « clôturer »** : kit perf édité **8×** (marqué CLOS → annulé → « reste L5+S3 » → re-CLOS).
  ~4 edits évitables si j'avais clarifié l'intention AVANT d'agir sur la mémoire.
- **Parsing `top` raté 2×** (awk sur `-stats pid,cpu,command`) avant de trouver le bon (`top -l N -pid`).

## 💶 Coût (€)

- Total **~€55,6 (≈ $60,5)** sur 113 turns. Décompo : cache read 21,2 M tok = $31,8 (**53 %**) · cache
  write 1h $12,4 (20 %) · output 211 k tok $15,8 (26 %) · input $0,4. **Cache-dominé 73 %** — le CLAUDE.md
  massif + skills `nodefony-session` (×2 RESUME/END) + `nodefony-start-server` rechargés à chaque turn.
  Levier : session longue = cache porté 113×. Une investigation aussi longue gagnerait à un `/compact`.

## Recommandations

1. **Skill mesure CPU cluster par process** — le `top -l 2 -pid <master> -pid <workers>` (instantané
   fiable, 100 %=1 cœur) a tâtonné 2×. Candidat enrichissement `nodefony-load-test` ou `nodefony-debug` :
   helper « répartition CPU master/workers/load-gen sous charge ».
2. **Clarifier l'intention AVANT d'éditer la mémoire** — « clôturer » était ambigu (archiver vs finir) ;
   8 edits du kit. Demander 1 mot de clarification quand un verbe d'action mémoire est ambigu.
3. **Finding logging cluster capturé** — `project_log_backplane_vision` (axe LB.W). Ne pas le perdre.

## Patterns récurrents (déjà gérés)

- ✅ **Protocole A/B atomique** (kit §5) appliqué pour L5a (stash AVANT / build / bench / restore).
- ✅ **`.git/index.lock` orphelin** ([[feedback_git_index_lock]]) rencontré sur `git stash` → `rm` lock.
- ✅ **memory.test flake GC** innocenté en isolation (1000-GET 2/2).
- ✅ **Zombies process** (orphelins -2 j, 730 MB) tués au boot — leçon protocole atomique confirmée.

## Commits produits

| Commit    | Sujet                                                                    |
| --------- | ------------------------------------------------------------------------ |
| `36991a8` | perf(framework): dédup pathname routing — 1 calcul/req (L5a, +4,3 % RPS) |
