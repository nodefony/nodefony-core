---
date: 2026-09-26
session_id: 11b09ec8-c53f-4fb4-83e8-b30987b8280e
focus: #498 — prefer-optional-chain et prefer-nullish-coalescing allumées, quatre défauts réels trouvés
---

# Retex 2026-09-26 — 11b09ec8

- **Fait** : f8728d345 prefer-optional-chain (108 sites) · 6d725811b `??` sur 130 opérandes non
  primitifs · af8b6e0c3 prefer-nullish-coalescing allumée, primitifs exclus (324 sites relus) ·
  07b9b3f4a fix(http) q=0 d'Accept · ebf46c7d4 fix(syslog) sévérité 0 · 1bbf32f2e fix(cli) F_OK ·
  853fccf75 fix(studio) compte ORM 0. #498 commenté (avancé, non soldé).
- **Frictions** (versées au sas, thème 🎯) : le `--fix` « sûr » d'optional-chain a introduit trois
  défauts (garde `typeof window`, null fondu dans un `.mjs`, `hasRole` non booléen) ; gate mémoire
  rouge par le client MCP qui sonde chaque seconde, message à cause unique qui m'a fait suspecter
  mon diff puis accuser à tort Brave ; lint rouge fantôme pendant que le superviseur rebâtissait
  `dist` ; drizzle en délai dépassé sous turbo parallèle (1,2 s isolé).
- **Décisions** :
  - Une règle à la fois sur tout le dépôt, jamais fichier par fichier ; suggestions d'UNE règle via
    une config minimale temporaire, pour qu'aucune autre ne s'applique.
  - `ignorePrimitives` plutôt que 324 dérogations : `||` sur un primitif est l'idiome voulu ici
    (env vide = absente, id de session `""` à générer, chaîne de tri, OU booléen) ; `??` aurait
    INTRODUIT des défauts. Les vrais défauts se corrigent à la main, un `fix` par défaut pour le
    changelog, chaque test vu rouge — et un test anti-correctif-naïf (NaN de `q`) vu rouge sous `??`.
  - Test de `Cli.exists` par espion sur `fs.access`, pas par fichier en lecture seule (POSIX absent
    sous Windows).
