---
date: 2026-08-27
session_id: fefe552e-7bf7-43e2-bf02-4a65c2131c36
focus: retirer de la surface publiée ce que rien n'exerce — puis outiller ce que le pilotage ne voyait pas
---

# Session retro — 2026-08-27 — fefe552e

## Fait

- **#41 fermé** — `IClientKernel` sort du barrel client avant que la 10.0.0 ne le gèle : rien ne
  l'implémentait, donc le compilateur ne l'avait jamais vérifié, et il portait déjà deux défauts
  qu'un seul `implements` aurait fait tomber. Gate `clientSurfaceExercised.test.ts` — une interface
  publiée que rien n'exerce est refusée. Vu rouge, vert sans. 1 orphelin sur 44 types publiés.
- **#43 fermé** — `NodefonyProvider` accepte `url` : le cas simple passe de quatre concepts à deux.
  Volet de #42 au passage : plus d'adresse devinée, un échec franc qui nomme les routes réelles.
- **#91 et #92 ouverts** — porter Studio sur le noyau client ; le refus intermittent d'une session
  valide au handshake (flake CI diagnostiqué, pas corrigé à l'aveugle).
- **Outillage de pilotage** — `ticket-verify.mjs` (quels tickets un commit vient de rendre faux),
  `ticket-effort.mjs` (estimé vs constaté : **×8**). Skill `nodefony-ticket` 1.5.0.
- **Audit d'architecture** (`fable`) sur les liaisons front de la socket, conclusions inscrites dans
  #36, #37, #38, #39.

## Coûts évidents

- Deux commits refusés par le hook parce que `skills:doc` avait tourné AVANT prettier : la fiche
  générée devient périmée dès que le formateur retouche le `SKILL.md`. Ordre correct :
  prettier → `skills:doc` → commit.
- Trois faux départs sur le gate de surface avant de trouver le bon critère (les alias dérivés
  d'une valeur n'engagent personne ; seules les interfaces promettent une forme).
- Un `cd` relatif rattrapé par le hook, un `rg -r` refusé, un heredoc perdu parce que le hook avait
  bloqué la commande entière avant son exécution.

## Recommandations

1. **Le format du champ `Jours`** — l'unité « jour-homme » mesure la mauvaise chose. Le vrai coût
   est le contexte à charger et les décisions à rendre, pas la frappe. À rediscuter au prochain
   CONSOLIDATE, avec `ticket-effort.mjs` comme instrument.
2. **`npm run ticket:verify`** — alias non posé, `package.json` ne se touche pas sans accord.
3. **La règle « Studio est la référence »** est gravée dans le skill ticket ; elle a immédiatement
   mordu sur le travail de la session même.

## Patterns récurrents (déjà gérés)

- ✅ Un gate neuf se voit ROUGE en débranchant, avec le diff prouvé.
- ✅ Une preuve porte sur l'artefact relu APRÈS le formateur.
- ✅ Un sous-agent PROPOSE, l'agent principal APPLIQUE et recontrôle ce qu'il affirme.

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `9c3f3a26` | retirer de la surface publiée un contrat que rien n'exerce          |
| `5d288f7e` | trouver les tickets qu'un commit vient de rendre faux               |
| `7bb772b6` | écrire une preuve d'absence sans salir l'ancre voisine              |
| `331bd4ed` | dériver l'effort réel d'un ticket, au lieu de croire son estimation |
| `9f50c6f3` | donner l'adresse au fournisseur React, et refuser de la deviner     |
| `8adcce0d` | faire de la console d'administration la référence de non-régression |
