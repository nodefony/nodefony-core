---
date: 2026-09-19
session_id: 620142d9-c5a8-4cb9-9547-60c2ab7c94d0
focus: Le programme posé par le user (deps, CI rouge, CodeQL) — et le rouge trivial qui cachait une régression livrée
---

# Session retro — 2026-09-19 — 620142d9

## Focus

Programme imposé par le user : dépendances en retard, CI rouge « CodeQL », skills > 500 lignes.
Les deux premiers points ont dérivé sur une régression du produit, livrée la veille.

## Fait

- **CI « Code généré » réparée** — rouge depuis 3 jours, 7 jobs, 3 systèmes : le `!` retiré de la
  grammaire de champs par #431 survivait dans les scripts du banc qui VÉRIFIE le produit. Banc de
  vérité passé de **3 étapes à 21/21**.
- **Régression #138 corrigée** — démasquée par la réparation ci-dessus : `create entity X ref:Y`
  produisait un test rouge à la naissance. `seedEntityRow` (@nodefony/drizzle) + l'échantillon
  déplacé dans l'ENTITÉ, une seule fabrique au lieu de deux copies.
- **#434 et #435 faits dans la foulée** (règle du contexte chaud, rappelée par le user) et fermés
  avec compte rendu.
- **#435** : 63 scripts npm décrits, groupés par usage avec un classement **dérivé du nom**, rendus
  dans `nodefony --help`, `nodefony scripts` et `--json` ; gate `check:scripts` branché à la forge ;
  gabarits d'application couverts par un test.
- **CodeQL** : #205 corrigé par dé-duplication (`scripts/lib/html-text.mjs`), #206 rejetée motivée.
  **Alerte de scan de secrets** fermée : faux positif prouvé (vecteur officiel de Standard Webhooks,
  445 dépôts publics), note posée dans le test.
- **Dépendances** : 8 écarts → 2, tous deux `typescript` bloqué en amont par Angular.

## Coûts évidents

- **5 commits, chacun ~3 min de hook pre-commit** (selftests devkit + régénération des symboles sur
  846 fichiers). Le hook est utile mais il domine le temps de clôture ; regrouper en moins de
  commits aurait coûté moins, au prix de la lisibilité.
- **3 lancements du banc de vérité** (~2 min chacun) — justifiés : chacun a rendu un verdict
  différent, et c'est la seule preuve qui porte sur l'artefact REÇU.
- **49 faux rouges** instruits pour rien, faute d'avoir lancé la commande du dépôt.

## Recommandations

1. **CONSOLIDATE** — dû depuis 5 sessions, deux thèmes à **11** et **9**. Le seuil est 5.
2. **Les 3 skills > 500 lignes** restent à découper ; structure figée dans le `_state`.
3. **Un contrôle « le banc va-t-il jusqu'au bout ? »** mériterait d'exister : un rouge précoce
   masque le reste, et rien ne dit combien d'étapes n'ont pas été jouées.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff avant d'imputer au code — appliqué 3 fois, juste 2 fois sur 3.
- ✅ Prouver sur l'artefact REÇU (banc sur tarballs) plutôt que sur ce qu'on a écrit.
- ✅ Voir un gate MORDRE avant de le déclarer utile — fait 3 fois (secret, `check:scripts`, `seedEntityRow`).

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `b382b00f` | fix(devkit-bench): recaler le banc sur la grammaire de champs actuelle       |
| `2a921873` | fix(scaffold): semer la ligne parente des tests générés (#138, #434)         |
| `ca5bc22b` | refactor(scripts): une seule implémentation du retrait de balises            |
| `64d0e0a5` | chore(deps): monter les dépendances en retard, sauf celles bloquées en amont |
| `c0c994c4` | feat(cli): décrire les scripts npm du projet dans l'aide (#435)              |
