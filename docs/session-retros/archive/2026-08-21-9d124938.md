---
date: 2026-08-21
session_id: 9d124938-0ba4-4d17-9ef0-a35e0e27147e
focus: devkit v2 axe B (MCP atteint, skills miroir) + tri axe D exécuté + run large 25/30
---

# Session retro — 2026-08-21 — 9d124938

## Fait (10 commits)

| Commit                  | Sujet                                                                                                   |
| ----------------------- | ------------------------------------------------------------------------------------------------------- |
| `ef706523`              | fix(mcp) : resultType par ère + retrait mesuré de server/discover (conforme ≠ joignable, 2ᵉ occurrence) |
| `f42e6df4`              | feat(devkit-bench) : --mcp-config/--strict au banc + compteur mcpCalls                                  |
| `77ac85e7`              | fix(ai-sync) : miroir .claude/skills (claude-code ne lit pas .agents/)                                  |
| `8461b833`              | chore(git) : hooks natifs core.hooksPath, husky supprimé                                                |
| `2ea71333` + `70d2e76c` | feat(cli) : nodefony git:hooks (3 refus totaux, realpath/toplevel)                                      |
| `6c6382f8`              | fix(cli) : ai:sync/ai:mcp/git:hooks entrent dans nodefony -h                                            |
| `3d9a01f5`              | feat(cli) : create app --git-hooks (commit bootstrap --no-verify)                                       |
| `06dc8971`              | feat(scaffold) : .prettierrc + CI GitHub ET GitLab (images source unique)                               |
| `cb95332f` + `7826cb27` | feat(cli) : env --example + dogfood complet (script supprimé, source-fresh, .env.example.head)          |
| `31a8c0e8`              | feat : décor MCP tranché (prémisse T9, AGENTS.md dit l'ordre)                                           |

Run large : 25/30 PASS (10,87 $, médiane 31 t), FAILS {9,10,12,24,28} — leviers MCP/skills
INERTES sur ce run (failed à l'init / tarball antérieur au miroir).

## Coûts évidents

- Enquête MCP : ~6 runs headless de sonde (~0,1 $ pièce) + 2 proxys jetables — le prix de
  « la conformité se mesure sur un client », bien payé.
- 4× vitest depuis le mauvais cwd (« no tests » en jugeant un sabotage) — reruns gratuits mais
  contexte brûlé.
- 1 `npm run test` racine par dérive de cwd pendant le banc (turbo entier + contention).

## Recommandations

1. Compte de tests > 0 avant tout verdict de débranchement (bullet au sas).
2. Dater le tarball d'un run long avant d'imputer un levier (bullet au sas).
3. Relevé haiku d'inventaire : recompter chaque ABSENT (2 faux constatés sur l'axe D).

## Patterns récurrents (déjà gérés)

- ✅ exit code jamais lu derrière un pipe (reconnu 2× en direct, RC lu à part).
- ✅ garde anti-restore du dépôt a mordu (arbre sale) — contourné par réécriture, pas par force.
