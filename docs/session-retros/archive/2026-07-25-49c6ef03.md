---
date: 2026-07-25
session_id: 49c6ef03-128e-47fc-85d3-97ea6d2b3de6
focus: trous devkit — create command, catalogue publié, nodefony env, banc 3→9 tâches, audit des CLAUDE.md
---

# Session retro — 2026-07-25 (3ᵉ) — 49c6ef03

## Fait

- **`nodefony create command`** (`9690c1d2`) — trou devkit n°1. Nom dérivé du `super("blog")` que
  l'`index.ts` DÉCLARE ; `addCommand` inséré après le `super(…)` ; `create module --command`
  délègue (0 template dupliqué). Preuve terrain : app `--link`, typecheck vert, commandes
  exécutées et listées par `--help`.
- **Catalogue des modules publié** (`6c9a11bc`) — `docs/catalogue.md` dans `files` du cœur, gate
  à double sens (a mordu 2× en naissant).
- **`nodefony env`** (`b455dc72`) — cascade, valeur effective, PROVENANCE, masquées, `NF_`
  inconnues avec suggestion. Standalone par nécessité. `envFileOrder` extraite de `loadEnv`.
- **300 retex triés → pièges dans l'`AGENTS.md` généré** (`5b57763f`, sous-agent fable) — 12
  pièges manquants, 5 corrections, conditionnés par les briques présentes.
- **Audit des `CLAUDE.md` du cœur** (`d970e73c`, sous-agent fable) — 3 fichiers CONTREDISAIENT le
  code (constructor `Module`, plafond de listeners, phases DI), 4 règles ajoutées, 2 redites
  supprimées.
- **Règle sous-agents + choix du modèle** (`0bd65f97`) — demande user.
- **Banc 3 → 9 tâches** + correction de mes propres sondes (`f1056d50`).

## Coûts évidents

- **La tâche 7 a coûté 4 runs** (≈ 4 × décor `npm install` + agent). Aucun imputable au
  framework : question devinable, puis doc allusive, puis deux faux positifs de sondes.
- **Deux échecs de commande dus au `cwd`** (`--prefix` et chemin relatif depuis `src/nodefony`),
  dont un qui a fait passer un banc entier à la trappe en silence — le piège que je venais
  d'écrire dans l'`AGENTS.md`.
- Un `grep -o` sur un gros fichier de mémoire a dépassé 120 s (motif trop large).

- **Le run complet des 9 tâches a été tué par le quota** (`api_error 429`) après la tâche 1 : huit
  tâches jamais parties, rapport `1/9` indiscernable d'une app devenue indécouvrable. Garde posé
  depuis. Seule information réelle du run — et elle est bonne : la **tâche 1 PASS** après 71
  échanges, donc l'`AGENTS.md` enrichi ce jour ne l'a pas diluée.

## Décision de fin de session

**Une session SÉCURITÉ conditionne la publication npm de 10.0.0** (GO user). Raisonnement retenu :
le paquet publié contient `dist/`, `.d.ts` et souvent les sourcemaps — fermer le code ne
protégerait de rien ; ce qui décide est le **délai découverte→correctif**. Ordre inscrit au
dashboard : `SECURITY.md` du dépôt → 166 vulnérabilités Dependabot (1 critique) → provenance npm
et 2FA → red-team sur la surface PUBLIÉE → scaffold (`SECURITY.md` généré + § sécurité de
l'`AGENTS.md` en **interdits exécutables**, chacun avec sa sonde).

## Recommandations

1. **Rejouer les 9 tâches d'un bloc après TOUTE modification de l'`AGENTS.md`** — il a doublé de
   taille aujourd'hui ; les tâches 1-3 n'ont pas été rejouées, leur vert d'hier ne vaut plus.
2. **Concevoir une tâche de banc contre le modèle qui devine** — sinon on mesure sa culture
   générale, pas la découvrabilité de l'app.
3. **Une sonde de transcript vise le CONTENU, jamais un nom de fichier.**

## Patterns récurrents (déjà gérés)

- ✅ preuve négative systématique (gates cassés exprès : wiring, ordre `super`, forme TSDoc, ports)
- ✅ délégation aux sous-agents avec modèle choisi (fable pour les deux audits)

## Commits produits

| Commit     | Sujet                                            |
| ---------- | ------------------------------------------------ |
| `9690c1d2` | `create command`                                 |
| `6c9a11bc` | catalogue publié + tâches de banc 4 et 5         |
| `b455dc72` | `nodefony env`                                   |
| `5b57763f` | pièges hérités des 300 retex                     |
| `d970e73c` | audit des fichiers d'instructions du cœur        |
| `0bd65f97` | modèle d'un sous-agent = poste de dépense        |
| `f1056d50` | sondes du banc : mentions ≠ lectures             |
| `2b26601d` | retex de session                                 |
| `1a7f15c8` | garde quota : un agent qui n'a pas pu travailler |
| `511cbcb2` | session sécurité = prérequis publication npm     |
