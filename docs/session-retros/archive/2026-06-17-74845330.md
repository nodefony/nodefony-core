---
date: 2026-06-17
session_id: 74845330-2b04-43d9-9966-5ae50bfa83f8
focus: P6 étape B — CSP nonce par requête (J5 headers) + design @Csp figé
---

# Session retro — 2026-06-17 — 74845330

## Focus

Étape B du CSP : **nonce par requête** remplaçant le hack `setHeader('unsafe-inline')` traînant dans
5 controllers (dont Studio en prod). 1 commit `eb59daf6`.

## Fait

- `eb59daf6` — `Context.cspNonce` lazy · `SecurityHeaders` placeholder `{{nonce}}` + `cspFor`/`hasNonce` ·
  `Firewall.registerCspOrigins` + merge pur `csp.ts` (9 tests) · défaut **secure-but-usable 10 directives** ·
  `TemplateHelper` nonce sur TOUS les `<script>` · `FrontendService` fragment Vite sur trustedHosts × ports ·
  retrait `getCspDirectives` + 5 controllers nettoyés.
- Gates : security unit 317, http intég 502, memory 9/9, security-review 0 blocker.
- Banc live : dev 5 surfaces (0 script sans nonce, 3 trustedHosts) + **prod strict** (0 `unsafe-*` script,
  bundle 200).
- Design `@Csp` per-route **figé** → `project_p6_csp_decorator_kit` (prochaine étape).

## Coûts évidents

- **~12 restarts serveur** (va-et-vient dev↔prod pour tester le CSP strict prod vs permissif Vite).
- Banc live prod (build front + serveur production + curl) lourd, fait à la main — candidat automatisation.
- Plusieurs allers-retours de fix CSP révélés un par un par le user (navigateur) au lieu d'un défaut complet
  d'emblée → coût évitable (cf RETEX « défaut réaliste complet »).

## 💶 Coût (€)

- Session TRÈS longue (debug prod intensif). Décompte non lancé (END rapide). Cache-dominé probable (gros
  contexte porté : pipeline http, 5 surfaces, dev+prod). → justifie la reco `/clear` avant le décorateur @Csp.

## Recommandations

1. Décorateur `@Csp` en **session fraîche** (kit figé) — chantier 3 modules, cache léger.
2. Banc CSP prod **automatisé** (Vitest : build front + serveur prod + asserts) — éviter les 12 restarts manuels.
3. Mémoriser : le nonce ne couvre pas `style=""` → `style-src 'unsafe-inline'` obligatoire CSS-in-JS (in `_state`).

## Patterns récurrents (déjà gérés)

- ✅ piège « 2 sources de défaut » (config.ts + Zod) = variante [[feedback_convention_frere]].
- ✅ banc live > unit pour prouver (déjà dans RETEX « preuve runtime »).

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `eb59daf6` | feat(security): nonce CSP par requête (étape B) — headers durcis + pont Vite |
