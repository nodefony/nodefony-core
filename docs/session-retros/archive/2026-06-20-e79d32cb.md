---
date: 2026-06-20
session_id: e79d32cb-aa89-4aa4-af87-7b559190e551
focus: red-team Authorization + CSRF/CORS (2 passes) + skill sécu hub red/blue-team
---

# Session retro — 2026-06-20 — e79d32cb

## Focus

Chantier red-team P6, deux briques + outillage : Authorization, CSRF/CORS, et extension du skill
`nodefony-security-review` en hub red/blue-team (suite à une directive méthodo du user).

## Fait

- **Authorization** (`17a5b3dc`) — `authorization.attack.test.ts`, 17 tests sur VRAI walker+voter+service :
  escalade verticale refusée (hiérarchie unidirectionnelle), DoS cycle hiérarchie (fail-fast ctor),
  confusion d'attribut, non-véto RoleVoter. Brique SAINE.
- **CSRF/CORS** (`98dacb8e`) — `csrf.attack` + `cors.attack`, 36 tests : origin-spoofing
  (suffixe/userinfo/scheme/port), token HMAC malformé, allowlist bypass. csrf/cors/csrfToken **100 %**.
- **Skill** (`8dfe0a88`) — `nodefony-security-review` étendu : mode RED/BLUE-TEAM (2 passes), conception
  d'attaques propres au framework (§4.4), rapport précis (§4.7), référentiels (§5 : ANSSI/CWE/CAPEC/
  OSV/GHSA/CISA-KEV/0-day), check `npm audit` (§2.H).
- 2 docs migration (`a53bbc9b`, `d2a0c20c`). Gates : security 433/433, tsc 0, couverture 100 % csrf/cors.

## Frictions / leçons

- **Biais de confirmation red-team** (directive user, MAJEURE) : j'ai lu le code AVANT d'écrire les
  attaques → influencé. Correctif = **2 passes** (threat-first PUIS code-first). Gradué direct en
  `feedback_redteam_threat_first` + opérationnalisé dans le skill.
- **Faux-ami de nommage** : `scope.test.ts` (framework) = scope DI `@Scope`, pas un scope de
  permission ; `@RequireScope` n'existe pas (aspirationnel dans le kit). Vérifier l'artefact (ancrage).
- **Passe 2 (couverture white-box)** a trouvé `csrf.ts:125` (Referer illisible) que la menace générique
  ratait → asymétrie Origin/Referer documentée. Preuve que les 2 passes sont complémentaires.

## Coûts évidents

- Aucun restart serveur (unit pur). Itération du skill en 4 messages user (conception affinée à chaud)
  — acceptable (le user pilotait la portée), mais structure figée plus tôt aurait économisé des passes.

## Commits produits (5, locaux NON poussés)

| Commit   | Sujet                                     |
| -------- | ----------------------------------------- |
| 17a5b3dc | test(security) red-team Authorization     |
| a53bbc9b | docs(migration) authz                     |
| 98dacb8e | test(security) red-team CSRF/CORS         |
| d2a0c20c | docs(migration) CSRF/CORS                 |
| 8dfe0a88 | docs(skill) security-review red/blue-team |
