---
date: 2026-08-28
session_id: 38c066c1-9234-4cc8-9f31-425da63fdc3d
focus: "#98 — ouvrir les migrations par cinq commandes ; six manques que seule l'exécution a montrés"
---

# Session retro — 2026-08-28 — 38c066c1

## Focus

Ticket #98 (S5c) : les commandes `orm:migrate*`, leur configuration, le mode de schéma au
démarrage et la sonde de disponibilité. Non fini — le ticket reste ouvert avec quatre points
nommés.

## Fait

- **`5139f48e`** — cinq verbes (`orm:migrate`, `:status`, `:baseline`, `:repair`, `orm:reset`),
  configuration Zod (`ddl` par connecteur, `migrations.*`) aux défauts résolus par environnement,
  démarrage qui obéit au mode et l'énonce, sonde `/readyz` retenue avec re-vérification
  périodique (`/livez` jamais touché), grille de codes figée, `formatVersion: 1`, `--json` pur.
- **`3d2b6f48`** — garde destructif : refus d'appliquer une suppression de données hors
  développement, plus strict encore au démarrage (sans drapeau pour le lever).
- Tickets #98, #102 et #99 commentés ; `MIGRATION_STATUS.md` recalé.

## Coûts évidents

- **~1500 lignes écrites AVANT la première exécution de la commande.** Les six manques sont sortis
  dans les vingt minutes qui ont suivi le premier `npx nodefony orm:migrate:status`. C'est le poste
  de dépense n°1 de la session, et il était évitable.
- Session **très longue**, sur un ticket estimé 1,5 j : le périmètre réel (config + 5 verbes + boot
  3 modes + sonde + garde destructif + tests) valait deux sessions. Le découpage annoncé en 6 lots
  au départ était juste ; ne pas s'arrêter à la fin du lot 4 ne l'était pas.
- **Le user a repris la trajectoire neuf fois** — et sept de ces reprises ont produit du travail
  livré ou tracé (explications béton, garde destructif, priorité #102 avant #99, dette du script
  npm, gros schéma d'échelle, `status`/`livez`, résilience du démarrage). Ce n'est pas du bruit :
  c'est ce que la conception n'avait pas vu.

## Frictions (versées dans RETEX.md)

1. Deux messages EXACTS qui envoyaient chercher au mauvais endroit → thème « message d'erreur ».
2. Un défaut vivant dans la DIVERGENCE de deux lecteurs du même chemin (base fantôme `:memory:`,
   qui rendait le code du succès) → thème « une porte a plusieurs entrées ».
3. La question jamais posée : et si la migration DÉTRUIT ? → thème « commande de déclaration ».
4. Le dépôt qui ne consomme pas sa propre commande, ce qui a caché le trou → thème « exigence sans
   regarder qui produit ».

## Gradué en mémoire durable

- **`feedback_design_doc_is_not_a_verified_scope`** — une conception validée vaut pour le POURQUOI,
  jamais comme périmètre vérifié. **Troisième occurrence en trois sessions** (08-28c, 08-28f,
  celle-ci), et celle qui a coûté la confiance du user dans l'audit. Porte les cinq questions à
  poser avant la première ligne d'une commande d'exploitation.

## Ce qui n'a PAS été fait, et qui est écrit dans #98

- `schemaDiff.ts` **commité mais branché nulle part** — code mort ; il porte le rattrapage additif
  et le verdict `divergent`.
- La page `drizzle/docs/migrations.md`.
- Le `hint` dans le corps d'erreur.
- Les e2e PostgreSQL / MySQL **des commandes** (l'applicateur, lui, est prouvé sur les trois).
- `nodefony status` et `/livez` non observés en état de schéma retenu — demandé en séance.
- `CLAUDE.md` / `MEMORY.md` du module.

## Preuves

- 44 tests neufs, dont **8 vus ROUGES par débranchement ciblé**.
- 7 cas sur **BOOT RÉEL** (`NF_RUN_CLI_BOOT`), dont le flux `--json` d'une seule ligne, le code du
  processus égal au verdict annoncé, et le refus destructif laissant la base INTACTE.
- Suite du module : **1121 passés / 0 échec** sur sqlite + PostgreSQL + MariaDB.

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `5139f48e` | les cinq commandes, la configuration, le démarrage, la sonde |
| `3d2b6f48` | le garde destructif (commande et démarrage)                  |
