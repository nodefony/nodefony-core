---
date: 2026-09-11
session_id: ec49c776-812d-4f49-8807-9a0179497356
focus: quatre tickets alpha avant l'alpha.5 — ancres, spawn, licences, horodatage, garde d'image
---

# Session retro — 2026-09-11 — ec49c776

## Focus

Reprise après `/clear`, puis quatre tickets du jalon `10.0.0-alpha` pris à la demande du user, plus
les deux « non fait » que la session précédente avait laissés derrière elle.

## Fait

- **Ancres du corpus (#359)** — 433 → 84 suspectes, **0 FILE_NOT_FOUND / 0 LINE_OUT** (le critère
  qui ne se discute pas). 350 recalées par l'automate, diff contrôlé paire par paire (335 paires,
  0 changement hors numéro de ligne). Le gate lui-même était aveugle à `scripts/` : une ancre JUSTE
  vers `release-core.mjs:100` sortait FILE_NOT_FOUND.
- **#21, son « non fait »** — test d'intégration PAR SPAWN du cycle de vie d'un compte
  (add → list → password → delete, plus le compte inconnu). Vu rouge, puis 16 verts / 14 sautés.
  Le renvoi mort du corps (« arbitrage #7 » = une PR Dependabot) corrigé, l'arbitrage cité avec son
  ancre.
- **#360 ouvert** — aucune politique de mot de passe : `nodefony security:user:add x --password abc`
  réussit. `IPasswordBlocklist` n'a AUCUN implémenteur dans le dépôt. Enrichi de la source de liste
  (SecLists / NCSC), de la forme embarquée (SHA-1 tronqué en `Uint32Array`, ~40 Ko lazy) et des
  quatre voies pour la nourrir.
- **Gate des descriptions** étendu aux 21 commandes de modules — a trouvé `orm:generate` (53 > 52).
- **#355 FERMÉ** — `edge` entre dans les options, la matrice `all` et la passe hebdomadaire, plus un
  run local complet : 39 ✓, 0 échec.
- **#323 (partiel)** — la règle des licences passe dans le PRODUIT (`nodefony licenses`), le script
  du dépôt délègue, l'app reçoit son `LICENSE` et ses scripts. Bloqué sur `npm sbom`.
- **#328 FERMÉ** — `@timestamp` ISO + modèle d'index, prouvé sur OpenSearch réel, index à neuf.
- **#317 FERMÉ** — les deux images du scénario `edge` passent enfin sous la garde de matière
  sensible ; elle y était absente.

## Coûts évidents

- Le banc `edge` : ~25 min d'attente, dont une dizaine de sondes de suivi qui ont chacune produit une
  notification. Une seule attente `until` aurait suffi — les `sleep` de confort ont fait du bruit.
- Six sondes npm pour isoler la cause du relevé de licences vide. Nécessaires (la cause était
  contre-intuitive), mais c'est le poste le plus cher de la séance.

## Recommandations

1. **Reprendre #323** avec la question tranchée d'avance : `npm ls --omit=dev --all --json` remplace
   `npm sbom` partout, ou l'écart entre les deux est éprouvé par un test. La table de reproduction
   est dans le ticket — ne pas la refaire.
2. **Ne pas déléguer une liste longue sans exiger la preuve CITÉE.** Un numéro de ligne ne coûte rien
   à inventer ; le texte de la ligne, si.
3. **Thèmes du sas à graduer au prochain CONSOLIDATE** : 🤝 (6) et 📐 (6), tous deux au-dessus du
   seuil.

## Patterns récurrents (déjà gérés)

- ✅ `feedback_suspect_instrument_and_own_diff` — quatre instruments faux en une séance : `node --test`
  sur un test vitest, un `pgrep` que le quoting rendait muet, un `echo $?` après un pipe, et l'exit 0
  du détachement pris pour celui du banc.
- ✅ `feedback_gate_must_bite` — trois gardes vues mordre (spawn, licences, horodatage), plus celle du
  banc `edge` qui se débranche elle-même.
- ✅ `feedback_prove_the_target_not_the_verdict` — deux contrôles rendaient un verdict sur un périmètre
  amputé (ancres aveugles à `scripts/`, descriptions aveugles aux commandes ORM).

## Commits produits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `4fb8d611` | docs(corpus): recaler 350 ancres par l'automate — #359            |
| `6c86438a` | fix(doc): rendre le contrôle d'ancres capable de voir les scripts |
| `e5309d78` | test(cli): éprouver par SPAWN le cycle de vie d'un compte — #21   |
| `7be37e7e` | test(cli): étendre aux MODULES le contrôle des descriptions       |
| `582a7f46` | chore(pilotage): régénérer l'empreinte — #360 ouvert              |
| `b8c7c6ce` | ci(release): jouer la topologie avec frontal dans la forge — #355 |
| `f59df9bb` | feat(cli): livrer les licences des dépendances à l'app — #323     |
| `b71b5e52` | fix(log): écrire un horodatage que les outils savent lire — #328  |
| `2849818b` | chore(pilotage): rafraîchir l'empreinte — #328 et #355 fermés     |
| `c8a17bcc` | test(release): faire passer sous la garde les images — #317       |
