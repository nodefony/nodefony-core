---
date: 2026-06-06
session_id: 2026-06-06-session2
focus: Studio Jumeau forages (realtime/orm/http) + refonte Workspace en BUREAU LIBRE
---

# Session retro — 2026-06-06 (2e) — Jumeau forages + Workspace bureau libre

## Fait

- **Jumeau** : nettoyage code mort + 3 forages — **realtime** (`SocketExplorer`, 6 vues `realtime/socket/`
  en onglets), **orm** (`OrmOverview` réutilisé en mode `embedded`, map `specialViews`), **http** (sous-schéma
  `TwinSchema` du pipeline, d'abord FAUX puis corrigé sur le vrai `HttpKernel`). Règle skill « forage exact ».
- **Workspace = BUREAU LIBRE** : refonte complète `workspace/*` — fenêtres flottantes px/fraction, z-order,
  drag/resize `setPointerCapture` (compositor, commit au drop), gouttière, « Ranger »=autoTile, gestion des
  espaces (créer/renommer/dupliquer/supprimer, persistance v2 autoritaire), bandeau **Mission Control** docké
  (vignettes fantômes repliables), nom bureau actif réactif. `grid.ts` testé (7 tests).
- Commits : `57aa3ca`, `ceae738`, `bd9f76a`, `605c0bb`, `ba46be9`, `0e9443f`. Skill studio-dev **1.22.0**.

## Coûts évidents

- **Pivot de modèle tardif** (grille → tiling → bureau libre) : 2 réécritures du Workspace avant le bon
  paradigme. Leçon : demander le PARADIGME (stacking vs tiling) AVANT de coder un canvas (gravé RETEX + skill).
- « tout figé » diagnostiqué via raisonnement (HMR + singleton MobX) sans navigateur — hard-reload = le fix.

## Recommandations

1. Avant un « bureau/canvas » : trancher stacking-libre vs tiling AVANT (RETEX gravé).
2. Refonte de store MobX → toujours annoncer « hard-reload » (singleton survit au HMR).
3. Forage/contenu de schéma = lire la source (MEMORY.md/code), jamais improviser (skill).

## Patterns récurrents (déjà gérés)

- ✅ Mantine v9 `Collapse` = `expanded` (pas `in`) — déjà au skill.
- ✅ commitlint header ≤ 100 car (raccourci au 2e essai).
- ✅ prettier (lint-staged) reformate les nouveaux fichiers au commit.

## Reste — prochaine session

- **Migrer la Supervision en bureau** (preset depuis les widgets actuels). Cf `project_studio_workspace_kit`.

## Commits produits

| Commit            | Sujet                                                              |
| ----------------- | ------------------------------------------------------------------ |
| 57aa3ca           | Jumeau : nettoyage code mort + commit Twin/blocs                   |
| ceae738           | forage realtime (SocketExplorer)                                   |
| bd9f76a           | forage orm (OrmOverview embedded)                                  |
| 605c0bb / ba46be9 | forage http (provisoire → corrigé HttpKernel) + règle forage exact |
| 0e9443f           | Workspace = bureau libre + gestion espaces + Mission Control       |
