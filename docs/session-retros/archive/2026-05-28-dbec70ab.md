---
date: 2026-05-28
session_id: dbec70ab-bdc4-4a24-8154-bd5b0bc5c6ae
focus: P13.5 RedisBackplane + registre de drivers + wiring cluster live + describe() observabilité
---

# Session retro — 2026-05-28 — dbec70ab

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    86 |
| Edit  |    53 |
| Read  |    28 |
| Write |     7 |
| Skill |     1 |

## Coûts évidents

- **6 cycles clean+build+relance cluster** : dist realtime périmé 2× (turbo a réécrasé
  mon clean build lors du build root → ancien message loggé en live). Leçon réapprise :
  **rebuild du module ciblé EN DERNIER**, après le build root (sinon turbo restore stale).
- **3 rejets commitlint** (sujet PascalCase « RedisBackplane »/« FakeBackplane ») → sujets
  doivent être lowercase. Friction récurrente.
- **tmpfs harness plein (ENOSPC)** sur un run perf verbeux (workers `stdio:inherit` + reporter
  verbose) → corrigé en `stdio:ignore` dans le banc.

## 💶 Coût (€)

- Total ~**€72** (≈ $79) — 177 tours. **cache read 37,5M tokens = $56 (71%)**, output 162k = $12,
  cache write 1h 312k = $9. Cache-dominé classique : le CLAUDE.md racine (énorme) + les CLAUDE/MEMORY
  modules rechargés à chaque tour dominent. Session longue (1 feature étendue en plusieurs sous-tâches
  réactives au user) → aurait gagné à un `/clear` entre P13.5 livré et le refactor registre.

## Recommandations

1. **Rebuild module ciblé APRÈS build root** (turbo réécrase) — déjà en mémoire `feedback_turbo_cache_stale_logs`,
   re-vécu en live (logs cluster trompeurs). Réflexe à ancrer : si on lance un runtime, vérifier `grep` le dist.
2. **Sujets commit toujours lowercase** (commitlint subject-case) — 3 rejets cette session.
3. **`/clear` entre sous-features** : P13.5 driver / refactor registre / observabilité = 3 features ;
   tout en une session = cache porté cher.

## Patterns récurrents (déjà gérés)

- ✅ Seam injectable testable sans infra (transport fake) — réutilisé du ClusterBackplane.
- ✅ Tests intégration auto-skip si Redis down (`describe.skipIf`) — convention redis P13.2.
- ✅ Perf opt-in `RUN_PERF=1` — doctrine respectée.
- ✅ Registre vs if-en-dur, source unique du littéral (`X.driver` static) — réflexe anti-magic-string.

## Commits produits

| Commit  | Sujet                                                                     |
| ------- | ------------------------------------------------------------------------- |
| 7d6795e | feat(realtime): driver redis backplane — fan-out cross-pod pub/sub        |
| aef89bc | test(realtime): banc e2e cluster Redis multi-process + perf               |
| 61207c5 | feat(realtime): registre de drivers backplane + wiring runtime redis      |
| f7d6a5a | chore(app): charge @nodefony/redis + driver realtime redis (démo cluster) |
| adb2f75 | feat(realtime): backplane.describe() — carte d'identité stdio + health    |
| 578992a | test(realtime): backplane factice implémente describe()                   |
