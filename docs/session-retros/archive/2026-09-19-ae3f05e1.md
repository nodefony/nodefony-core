---
date: 2026-09-19
session_id: ae3f05e1-596e-40a3-961a-154494c84fe0
focus: Autopsie d'un run Copilot réel — le « ! » de la grammaire, le DDL de boot contre les migrations, #430 et #431 fermés
---

# Session retro — 2026-09-19 — ae3f05e1

## Fait

- **Transcript Copilot dépouillé en ENTIER** (`~/.copilot/session-state/4ca27d2d-…`, 08:57 → 13:20,
  13 demandes, 145 tours, 248 commandes, 251 approbations) — la session 09-19b n'en avait lu que
  les 37 premiers tours.
- **La cause racine trouvée, et elle n'est pas où le ticket la plaçait** : `create entity Message
--fields "content:text!"`. Dans notre grammaire `!` = UNIQUE, alors qu'il veut dire non-null
  partout ailleurs — et **notre propre aide disait « requis »**. Un salon de discussion où deux
  fois le même message est un 409.
- **`9f0a0e63`** — `!` refusé en nommant les deux intentions, unicité en `:unique`, 14 sites
  recalés ; les deux refus de #431 nomment la forme attendue ; `create front` régénère la porte ;
  la porte nomme `login`/`me`/`logout`.
- **`ef7c8953`** — bascule `auto` → `migrate` dès la première migration versionnée,
  `orm:generate --apply`, et la sortie de boucle de #430 (`alreadyInTargetState` → `baseline`).
- **`f4c3cf44`** — trois tests laissés rouges sur `dev` par le renommage de #432 (commit
  `fd41941e`), recalés.
- **#430 et #431 fermés** avec compte rendu ; ancre de #138 recalée (elle avait dérivé de 36 lignes).

## Coûts évidents

- **Le dépouillement du transcript a coûté cher et il le valait** : 6 Mo de JSONL, réduits par `jq`
  à une trame de 144 Ko lue en entier. Les trois défauts produit majeurs sont sortis de la lecture
  des COMMANDES et de leurs sorties, aucun d'un compte rendu.
- **Le test d'exécution réelle (~20 min) a trouvé ce que 5 007 tests verts ne voyaient pas.** Sans
  lui, `--apply` partait cassé.
- Trois allers-retours évitables : un `if` eta ouvert sans être fermé, un shadowing `version`
  attrapé par le lint, une ligne d'aide à 81 caractères pour un maximum de 80. Les trois rattrapés
  par des gates, aucun par ma relecture.

## Recommandations

1. **Lancer `npm run check:lang` avant de proposer un commit** — la règle « code en anglais » est
   la seule des cinq à n'être portée par aucun gate automatique du pre-commit, et c'est la seule
   sur laquelle j'ai fauté (le user l'a vue avant moi).
2. **CONSOLIDATE dû** : deux thèmes MÛRS depuis trois sessions — « une méthode écrite et non
   imposée ne s'applique pas » (**8**) et « un contrôle qu'on ne peut pas satisfaire finit
   désarmé » (**6**).
3. **Rejouer le banc de découvrabilité** quand une campagne sera payée : la porte enrichie et les
   trois refus n'ont été jugés que par leur auteur.

## Patterns récurrents (déjà gérés)

- ✅ « Prouver sur l'artefact REÇU » — le symlink et `create entity --help` ont été vérifiés avant
  de conclure quoi que ce soit de l'app jetable.
- ✅ « Suspecter son propre diff » — les 4 rouges du core ont été instruits avant d'être imputés ;
  3 appartenaient à la session précédente, 1 était bien de moi.
- ✅ « Un gate neuf se voit ROUGE une fois » — les 4 gardes débranchées puis rebranchées.
- ✅ Le mode `--raw` du contrôle de format, payé 3 passes en 09-19c, employé du premier coup ici.

## Commits produits

| Commit     | Sujet                                                               |
| ---------- | ------------------------------------------------------------------- |
| `f4c3cf44` | test(scaffold): recaler les trois tests laissés par le renommage    |
| `9f0a0e63` | fix(scaffold): cesser d'apprendre « ! » pour « obligatoire » (#431) |
| `ef7c8953` | feat(orm): écrire et appliquer une migration d'un seul geste (#430) |
| `0500a069` | chore(pilotage): rafraîchir l'empreinte du tableau de bord          |
