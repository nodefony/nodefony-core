---
date: 2026-06-25
session_id: b076cabc-39e4-4db5-836b-8e15abf76f30
focus: Red-team scope DI singleton (P6 12/12) + refonte page Runtime full-stack + grosse session bench perf (mono/cluster/mémoire/session)
---

# Session retro — 2026-06-25 — b076cabc

## Focus

Trois temps : (1) **red-team scope DI singleton** (dernière surface §4.4 sans matrice
d'attaque → P6 red-team 12/12) ; (2) **refonte full-stack de la page Studio `/nodefony/runtime`**
(« très mal organisée ») = endpoint `/kernel/api/processes` (topologie dev live, source de vérité
partagée CLI↔Studio) + 2 onglets ; (3) longue **exploration perf live** (débit mono/cluster 3/6/10
workers, mémoire pod prod, coût d'une route à session).

## Fait

- **Red-team scope DI singleton** (`f7108b8c`, poussé) : `scope.attack.test.ts` 7/7 (A1 anti-escalade
  « 1er arrivé fige l'identité », A2 concurrence entrelacée, A3 route, A4 session, A5 query, A6
  queryOverride, contrôle per-request). Brique SAINE, 0 finding. Boussole red-team → **12/12 surfaces**.
- **Page Runtime** (`675707d5`, poussé) : core `buildDevStatus`/`collectDevStatus` (rapport JSON PUR
  partagé `nodefony status` ↔ Studio) + `discoverDevProcesses({includeSelf})` + barrel ; framework
  endpoint `GET /kernel/api/processes` (RBAC ADMIN, dev-only, pidfile relatif) ; front 2 onglets
  (Utilisation : mode + process live + Vite ; Doc : 3 modes + « comment Nodefony fait tourner les
  fronts » + axes + workers). Prouvé live dev (4 process) ET prod/cluster (devMode:false).
- **Bench perf live** (wrk, machine 6 phys/12 log) : courbe mono **6683** → 3w **17114** → **6w 19076
  (pic = cœurs phys)** → 10w **15562** ; cluster = gain LATENCE p99 surtout (mono 285ms → 18-47ms) ;
  mémoire pod prod = pas de fuite (heapUsed reclaim, RSS collant normal) ; **route à session = ÷12
  du débit** (519 vs 6181 RPS), dominé par le **store SQLite synchrone**.

## Frictions / leçons (→ RETEX.md)

- **Coût session ÷12** re-confirmé (2× → candidat graduation) : store sync = goulot, pas firewall/TLS.
- **Optimum workers = cœurs physiques** ; sur-fork contre-productif ; client co-localisé bride le scaling.
- **Cookie `__Host-`/Secure rejeté en HTTP** + jar curl exclut HttpOnly → extraire via `Set-Cookie`.
- **Page Studio vide après build:front = bundle périmé navigateur** (re-vécu), pas un bug code → hard-reload.
- **Source de vérité partagée CLI↔Studio** (`buildDevStatus` pur) + `includeSelf` (data plane dans le process observé).

## 💶 Coût

- Session longue (perf exploratoire + 2 livrables full-stack). Cache-dominé (skills studio/load-test/security
  - gros contexte chargés). Levier : `/clear` aurait pu séparer red-team / Runtime / bench.

## Commits produits

| Commit     | Sujet                                                                             |
| ---------- | --------------------------------------------------------------------------------- |
| `f7108b8c` | test(security): red-team scope DI singleton — fuite inter-utilisateur (P6 12/12)  |
| `675707d5` | feat(studio): page Runtime — topologie process dev live + onglets utilisation/doc |

## Reste

- Valider VISUELLEMENT la page Runtime en dev (le serveur tourne en PROD — repasser `start.sh`).
- Test unit `buildDevStatus` (pur) — dette mineure notée dans le commit.
- 4 chantiers post-Studio toujours en attente + P6 Webhooks P6.13.
