---
date: 2026-06-25
session_id: 98483828-5032-498d-b257-d5f8e5adb9ad
focus: P6.8 autorisation par scope (@RequireScope + ScopeVoter + découverte + formulaire Studio)
---

# Session retro — 2026-06-25 — 98483828

## Focus

P6.8 — 1er des 4 chantiers post-Studio. Rendre les scopes API (`api:action`) RÉELS
(étaient décoratifs, aucune route ne les vérifiait) + découverte + formulaire Studio.

## Fait

- **Cœur enforcement** (`723da293`) : `@RequireScope` (framework, frère `@IsGranted`,
  metadata dédiée fusionnée au même `SecurityRequirement` → 0 touche Resolver) +
  `ScopeVoter` (built-in registre, dérive « scopable » du `token.type`, no-op humain /
  fail-closed machine). 13 + 6 tests, 0 régression, gate mémoire 9/9.
- **Découverte + Studio + banc** (`e7c04962`) : `extractActionScopes` /
  `collectDeclaredApiScopes` (scan `Router.routes`) → `ApiKeyController.capabilities`
  enrichi `declaredScopes` ; `CreateApiKeyModal` cases groupées par API ; banc
  `ApiM2mController` `/m2m/scoped/*`. Prouvé LIVE (clé `[m2m:read]` → read 200 /
  write+export 403 / whoami 200 ; catalogue `{m2m:[read,write],reports:[export]}`).
- **Skills** (`0130cdd0`) : framework-dev 1.28.0 + studio-dev 1.33.0 (co-évolution).
- Les 3 commits POUSSÉS sur `refactor/p6-security`.

## Frictions / leçons (→ RETEX.md)

- Placer un endpoint d'introspection LÀ où vit la donnée (catalogue = routes =
  framework), pas dans le module sémantiquement attendu (security ne voit pas Router).
- Tracer le TYPE concret au point d'appel avant de suivre un kit : le pont WS
  `api.request` passe un `IRealtimeToken` à `decide` → écart au kit (token.type vs
  isScopable()).
- Tester sans serveur = cobayes jetables (classe décorée + token à la main + Router
  mocké) ; e2e = curl authentifié. Distinction explicitée (le user a buté dessus).
- commitlint : sujet MINUSCULE (« P6.8 … » rejeté → « scopes par API (P6.8) … »).

## Commits produits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `723da293` | feat(security): scopes par API réels — @RequireScope + ScopeVoter |
| `e7c04962` | feat(security): découverte + formulaire Studio + banc live (b3+4) |
| `0130cdd0` | docs(skill): scopes par API (P6.8) — framework-dev + studio-dev   |
