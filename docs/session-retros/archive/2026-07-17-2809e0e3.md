---
date: 2026-07-17
session_id: 2809e0e3-3972-4127-99f6-2518b294f6b0
focus: La dette DI soldée de bout en bout — et le diagnostic qui se trompait de cause
---

# Session retro — 2026-07-17 — 2809e0e3

> Parti de « les dettes go » sur **une** dette 🔴 (« le scope singleton n'en est pas un »).
> Le diagnostic était faux ; la vraie racine en cachait **six**. Chaîne complète soldée :
> le code fait le bon DI, les skills l'enseignent juste, les scaffolds le génèrent et l'expliquent.

## Fait

- **7 commits** — 4 sur le core (DI), 3 sur la doc/skills/scaffolds.
- La classe entière du DI : pollution de prototype (6 vecteurs) · propagation d'args
  parent→dépendance · `Fetch` 1/requête → 1/kernel · `singleton` mémoïse · `@services`
  qui avalait tout (**la prod démarrait amputée en silence**) · ordre de `@services` ·
  **token = la CLASSE** (1/7 → **7/7** round-trip, vérifié en runtime).
- 3 fichiers d'attaque : `injector.attack.test.ts` (13 vecteurs), `services.attack.test.ts`
  (12), `serviceOrder.test.ts` (11) + intégration `di-singleton.test.ts` (4, serveur réel
  via sonde `/nodefony/test/di/{probe,tokens}`).

## Coûts évidents

- **~8 cycles build+restart serveur** — mais tous justifiés : chacun était un **point de
  mesure** (sonde runtime, reproduction de la bombe d'ordre ×2, round-trip des tokens).
  Le seul évitable : le 1ᵉʳ `--force-build` oublié.
- **3 runs turbo complets** pour trancher un flake (dont 1 baseline `git stash`). Cher, mais
  c'est ce qui a évité de classer un vrai échec en « contention » sans preuve.
- **2 subagents** (carto @injectable + carto ctors) → ont donné la mesure statique qui,
  croisée avec la sonde runtime, a réduit l'option 1 de « chantier » à « une ligne ».

## Recommandations

1. **`makeKernelStub()` partagé et typé** — 4 mocks de kernel incomplets en une journée
   (cf RETEX « fixture = contrat »). Le contrat kernel est implicite (`as any`) → aucun
   compilateur ne dit à un stub qu'il ment. C'est la dette d'outillage la plus rentable.
2. **`CliIntegration` : readiness dérivée d'un budget**, pas un `45_000` en dur — 3 faux
   rouges aujourd'hui, dont 2 investigations complètes. Piège gravé au dashboard : ces cas
   tournent **en production**, donc ils ressemblent à une régression dès qu'on touche au boot
   (`readiness timeout` = charge · `exited before ready` = vraie régression).
3. **`.claude/conventions.md` : vestige mort ET faux** (Rollup retiré, « Runtime dev : Bun »,
   3 décorateurs inexistants). Contredit le CLAUDE.md courant → suppression à trancher.

## Patterns récurrents (déjà gérés)

- ✅ **Preuve négative** systématique (débrancher le fix → le test DOIT rougir) : faite 3×
  aujourd'hui (Object.create(null), tri topologique, token-classe). C'est elle qui a
  démasqué 2 assertions complaisantes sur le scaffold.
- ✅ **Baseline `git stash`** avant de qualifier un échec de « pré-existant ».
- ✅ **Instrumenter le goulot** (sonde sur `_resolveWithStack`) plutôt que raisonner : a
  donné « 1 seul service concerné, 10 req = 10 instances, args=[HttpContext] » en 2 min.
- ✅ **Contrôle positif** dans chaque matrice d'attaque : a démasqué 2× **mon propre
  câblage** (modules sur `/tmp` → ENOENT ; C3 posant un service sans `addService`).

## Commits produits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `b4684a56` | le scope « singleton » n'en était pas un — le diagnostic se trompait de cause |
| `4e08619e` | `@services` avalait tout — la prod démarrait amputée en se déclarant saine    |
| `9e6ccc01` | l'ordre de `@services` ne décide plus du boot — il se calcule                 |
| `9b1ab9ef` | le token de résolution devient la CLASSE — 1/7 → 7/7 round-trip               |
| `42a5b847` | les skills de dev enseignaient un DI qui n'existe plus                        |
| `56ad1d2f` | les templates générés expliquent les deux noms d'un service                   |
| `63251303` | deux décorateurs fantômes que la doc faisait écrire                           |
