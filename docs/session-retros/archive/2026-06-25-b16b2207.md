---
date: 2026-06-25
session_id: b16b2207-b598-48b6-a4ff-edc744d20026
focus: Mutations socket idempotentes (P6.8) — Idempotency-Key conforme draft IETF (HTTP+WS)
---

# Session retro — 2026-06-25 — b16b2207 (S4 du jour)

## Focus

Chantier post-Studio 2/4 : ouvrir le pont WS-RPC `api.request` aux **mutations**
(POST/PUT/PATCH/DELETE), jusqu'ici GET-only. Idempotency-key (modèle draft IETF) =
le vrai verrou (la socket reconnecte et rejoue → anti double-effet).

## Fait (8 lots)

- **Contrats core** : `IAdminRequest.idempotencyKey?` + `RequestContextPayload.body?/idempotencyKey?`.
- **`IdempotencyStore`** (framework, service DI) : mémoire borné cap+TTL+bail in-flight, clé scopée
  IDENTITÉ (`JSON.stringify([identity, clientKey])`), **fingerprint SHA-256** du payload, clé bornée 255.
- **`methodOverride`** threadé `Router.resolve → Resolver.match → Route.matchRequirements` : en WS
  `context.method` vaut toujours "WEBSOCKET" → désambiguïse la méthode logique (zéro bypass = 405).
- **`AdminBroker.mountAll`** monte WEBSOCKET sur mutations ; **`AdminApiController`** porte la dédup
  APRÈS le RBAC (un 403 ne consomme pas le cache). Statuts conformes draft : 400/409/422 + replay.
- **Pont realtime `invokeApiRequest`** : method/body/clé via l'ALS per-invocation (0 bleed).
- **Clients** : `RealtimeClient.mutate` (core) + `ApiClient` Studio (clé rejouée sur fallback fetch =
  anti double-effet CROSS-transport ; `httpOnlyRoutes` re-scopé par méthode).
- **Bancs** : store unit 15 · routing NR invariant **K** (8) + Route methodOverride (4) ·
  apiClientSocketBridge 14 · banc LIVE `ws-data-plane-auth` **20/20** (dédup WS+HTTP, 409, 422, 400
  absente/abusive, scope identité admin≠user, méthode 405).
- **IAdminApi de test** `idem-probe` (compteur observable) pour prouver l'idempotence en live.

## Frictions / leçons

- **RFC d'abord** : j'avais figé 400/409 « façon Stripe » de mémoire → le user m'a repris (« t'as vu
  les RFC ? »). Le draft `idempotency-key-header-06` ajoute le **422** (payload différent, fingerprint)
  qui me manquait. Toujours `nodefony-rfc` AVANT de figer un code/header.
- **Scope = `request.user`** (uniforme ALS HTTP+WS), PAS `getUserId()` (le firewall HTTP ne le pose
  pas → rejeu HTTP ne dédoublonnait pas, compteur 10≠9). Bug trouvé par le banc live, corrigé.
- **Octet nul LITTÉRAL dans le source = fragile** (invisible, prettier, 2 Edits ratés) →
  `JSON.stringify([a,b])` ou script Node de remplacement.
- **`start.sh` TIMEOUT ≠ serveur mort** (Vite compile 5 frontends après le listen) + collision
  rebuild→restart rapproché tue le serveur → re-poll health, relance propre.

## Décision structurante

Idempotence = **1 store + 1 clé scopée identité pour HTTP ET WS** ; la seule ligne qui distingue les
transports = `if (isWs && !key) → 400`. Périmètre = **data plane admin** ; le décorateur `@Idempotent`
(controllers userland) = prochaine session, **strict par défaut** (400 si clé absente) + opt-out
`{ required: false }` (validé avec le user, aligné draft §2.7).

## Commits produits

| Commit     | Sujet                                                           |
| ---------- | --------------------------------------------------------------- |
| `febd06fa` | feat(security): mutations socket idempotentes — Idempotency-Key |
| `05d7a657` | docs(skill): framework-dev 1.29.0 + studio-dev 1.34.0           |
