---
date: 2026-08-22
session_id: f8d76f29-2718-4017-a8a7-06fad328b0dd
focus: "`scopesSupported` dérivé des outils MCP — et le défaut trouvé par le user en s'en servant : présenter mal valait moins que se taire"
---

# Session retro — 2026-08-22 — f8d76f29

## Focus

Priorité 1 du plan MCP (le point « non négociable ») : faire dériver les scopes publiés
de ce que la porte EXIGE. Puis, en cours de route, un défaut signalé par le user en
utilisant sa propre porte — qui a mangé la moitié de la session, à raison.

## Fait

- `d2bd59ca` — `scopesSupported` DÉRIVÉ. Clé de config SUPPRIMÉE ; au cœur, la traversée
  du catalogue extraite (`declareMcpTools` / `collectMcpTools` / `mcpDeclaredScopes`).
- `741dcb84` — présenter MAL ne vaut plus moins que ne rien présenter (`readBearerHeader`
  à trois verdicts ; jeton rejeté servi en anonyme sur une porte tolérante, avec journal).
- `e68b2815` — README/docs/MEMORY alignés ; un fait périmé corrigé (`RemoteJwtVerifier`
  EST livré, le MEMORY du devkit disait le contraire).

## Coûts évidents

- 3 `npm run build` complets (~30 s chacun) + 4 passes de suites. Le cycle
  build → sonde → suite est ce qui coûte ici, pas l'écriture.
- Un run `vitest` mal pointé (racine au lieu du paquet) : 562 fichiers, verdict à jeter.
- Deux réponses au user avant de chercher le vrai défaut — l'aller-retour a coûté plus
  que la correction elle-même.

## Recommandations

1. **Tableau des entrées d'une porte** avant de la déclarer finie : ce que rend chaque
   forme d'appelant, côte à côte. C'est la seule vue où l'inversion se voit.
2. **Sonde de décor VIVANTE** pour tout test qui lit un journal : mtime ou ligne témoin,
   sinon un fichier figé rend un faux rouge que rien ne signale.
3. **Assertions de SENS** sur tout résultat dérivé d'un décor (contient / ne contient pas).

## Patterns récurrents (déjà gérés)

- ✅ Débranchement joué avant de croire un test neuf (4 rouges sur 5 cas).
- ✅ Preuve sur l'artefact REÇU (document servi par la porte, pas le code qui le compose).
- ✅ Suspecter son instrument : le rouge `client-abort-499` était du décor, pas le diff.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `741dcb84` | fix(oauth) — présenter MAL ne peut pas valoir moins que se taire |
| `d2bd59ca` | feat(mcp) — la porte publie ce qu'elle EXIGE (`scopesSupported`) |
| `e68b2815` | docs(devkit) — doc et MEMORY alignés, un fait périmé corrigé     |
