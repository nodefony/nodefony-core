---
date: 2026-07-30
session_id: 1e76c2a2-54db-48ca-b06b-7cd123bd86c5
focus: Cahier des charges du banc de SÉCURITÉ devkit — conception seule, zéro code produit
---

# Session retro — 2026-07-30 — 1e76c2a2

## Focus

Priorité 1 du state 07-30e : le **banc de sécurité devkit**. Mini-cahier des charges AVANT
d'écrire (règle d'hygiène §2). Aucune ligne de code produite — c'est voulu.

## Fait

- **4 sous-agents `haiku` en lecture seule, en parallèle** : anatomie du banc · surface de
  protection du framework · ce qu'une app générée dit de la sécurité · vecteurs d'attaque déjà
  codifiés. ~275 k tokens lus, seuls 4 tableaux sont entrés dans le contexte principal.
- **Cahier des charges écrit** → mémoire IA `project_devkit_security_bench_kit.md` (indexée) :
  9 tâches en 2 familles, doctrine de sonde à double étage, décor d'authentification résolu,
  5 faux-FAIL bannis, 4 trous produit, protocole.
- **Inventaire de surface fermé par automate** sur les 3 schémas Zod (http, realtime, security).
- **Question CSWSH tranchée** — le soupçon de brèche était faux.

## Constats produit (non traités, écrits dans le kit)

1. La tâche 2 du banc « protège une route » n'exécute **aucune attaque** : son seul gate est
   `npm test`, écrit par l'agent jugé. Présence de chaîne ≠ effet.
2. `entity/controller/…Controller.ts.tpl` (265 l.) : **zéro** mention de garde, alors qu'il génère
   un DELETE. Le même DELETE est gardé dans `controller/rest/…:129`.
3. `--preset minimal` efface tout le bloc sécurité d'`AGENTS.md` (`engine.ts:811`) ; le banc
   tourne en `complete`, donc il mesure le cas favorable.
4. Deux implémentations du contrôle d'Origin à l'upgrade WS (`http-kernel.ts:542` toujours active,
   `RealtimeHub.checkOrigin():896` off par défaut). Série ou chemins disjoints : **non prouvé**.

## Coûts évidents

- 4 sous-agents en parallèle : le bon geste (le volume ne remonte pas), mais le tableau rendu par
  le plus gros d'entre eux a dû être **recontrôlé par `jq`** — 12 briques manquaient. La
  vérification aurait dû être planifiée, pas déclenchée par le doute du user.
- 3 `rg` successifs mal calibrés (motifs `-or` qui ne mordent pas) avant de basculer sur
  `.ai/symbols.json` puis sur la lecture directe des schémas Zod. L'automate prévu par le projet
  était le bon dès le premier coup.

## Recommandations

1. Écrire le gate `gate-secure-route.mjs` en **session dédiée** — c'est un instrument, il se voit
   rouge deux fois avant d'être déclaré bon.
2. Qualifier la double implémentation du check Origin WS avant d'y toucher (motif
   « 1 RÈGLE = 1 implémentation »).
3. Le trou du gabarit d'entité (constat 2) est corrigeable **indépendamment** du banc — mais le
   banc doit le prouver d'abord, sinon on corrige à l'aveugle.

## Patterns récurrents (déjà gérés)

- ✅ Modèle des sous-agents annoncé dans le champ `description` (`[haiku] …`) — 4/4.
- ✅ Interdiction de toucher à l'index git écrite dans les 4 prompts de délégation.
- ✅ Périmètre en chemins exacts donné à chaque sous-agent.

## Commits produits

| Commit | Sujet                                        |
| ------ | -------------------------------------------- |
| —      | aucun commit de code : session de conception |
