---
date: 2026-09-26
session_id: 7cbea8ae-95ce-4146-991d-8f5fcbe5aca0
focus: banc devkit de nuit + #486 (scope de requête) + audit de la doc publique
---

# Retex 2026-09-26 — 7cbea8ae

- **Fait** : banc devkit recalé, 27/32 à l'unanimité (`5e0c1d4ab`, transcripts archivés
  `tmp/devkit-bench-archive/`). #486 fermé, puis #481 (chantier du scope soldé) : page canonique,
  ADR-0011, tutoriel, README/AGENTS, gabarits (`3ac9b4239`), test `7c804d2ad`. Audit des 100 pages
  publiques (7 lots `sonnet`, chaque constat revérifié), 4 638 ancres contrôlées, lecteur de schémas
  réparé + plancher d'échelle à 90 % (`53d63eda6`), doc-lint 5 ter (résidus d'outil).
- **Frictions** : `anchor-fix` a cassé 12 ancres justes (relecture ligne à ligne) ; les audits
  d'agents ont manqué `vue-ensemble` (« session » sur le scope) et proposé un `_id` ObjectId faux
  pour l'audit Mongoose — un constat d'agent se revérifie au code ; mon propre schéma utilisait une
  syntaxe que le moteur jetait en silence ; des accents graves dans un commentaire CSS ont fermé un
  gabarit JS (build cassé, vu au rebuild) ; la garde git lit l'arbre principal depuis un worktree.
  Versé au sas (thème 🎯).
- **Décisions** : travailler la nuit dans un WORKTREE — le banc relisait gabarits et CLI de l'arbre
  principal ; docs publiques commitées sur une branche locale, fusionnées après le GO de l'auteur ;
  défauts de CODE trouvés signalés sans correction (hors périmètre doc, gate mémoire impossible
  pendant le banc) : `refererCheck` qui coupe les expirations de session, `DOCS_REPO_*` sans `NF_`,
  catégories d'audit hors contrat.
