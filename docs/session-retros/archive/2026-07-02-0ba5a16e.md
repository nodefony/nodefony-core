---
date: 2026-07-02
session_id: 0ba5a16e-3a03-4707-a183-df0efea7c8a7
focus: Rate-limit général par IP (@nodefony/http, Phase 0.3 volet 2) — clôt 0.3
---

# Session retro — 2026-07-02 — 0ba5a16e

## Focus

Phase 0.3 volet 2 du plan release Nodefony 10 : rate-limit général par IP dans
`@nodefony/http`. Clôt 0.3 (volet 1 audit persistant déjà livré `bc521223`).

## Fait

- `MemoryRateLimitStore` (fixed window, Map lazy null, éviction FIFO + `gc`, `now` injectable) + contrat `IRateLimitStore` SYNC.
- Config `http.rateLimit` (Zod strict, OFF par défaut) + augmentation `NodefonyModuleConfig` (`use()` typé).
- Check `HttpKernel.onHttpRequest` AVANT `handle()` (lazy null = 0 coût off ; rejet avant alloc context). IP via `resolveForwarded().clientIp`.
- Headers `X-RateLimit-*` + `Retry-After`/429 (RFC 6585). `GcScheduler` armé si enabled, reconfig live `onConfigChanged`.
- Banc e2e versionné `ratelimit-e2e.mjs` (+ alias `run.sh ratelimit`). Doc MEMORY + README.
- Commit `0ddf4c84` POUSSÉ `claude-ts`.

## Gates

build 0 warning · unit store 9/9 · unit http 530/530 · **mémoire 9/9** · intégration 560/560 · **e2e wire 429 live**.

## Frictions (→ RETEX.md)

- **Banc e2e non gardé** (curl jeté) → le user a demandé de le versionner → pattern `scripts/*-e2e.mjs` dès le départ.
- **Boot dev lent après rebuild module cœur** (turbo revérifie la chaîne) → `TIMEOUT` start.sh ≠ crash, attendre `curl --retry`.
- **Union h1|h2 → TS2349** sur `writeHead`/`end` (`response` raw) → cast `as http.ServerResponse` (le build reste « vert » = piège).
- (méta) charger `nodefony-security-review` + prompt « attack/red-team » a **routé Fable→Opus** (classifieur sécu plateforme, pas un refus ; Opus meilleur pour concevoir des attaques de toute façon).

## Commits

| Commit     | Sujet                                                                                   |
| ---------- | --------------------------------------------------------------------------------------- |
| `0ddf4c84` | feat(http): rate-limit général par IP — fixed window, opt-in, hot-path lazy (Phase 0.3) |
