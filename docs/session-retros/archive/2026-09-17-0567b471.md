---
date: 2026-09-17
session_id: 0567b471-49c1-4dd7-9391-46409a9ec13a
focus: "#392 et #393 — la commande de création dit enfin ce qu'elle attend, et ce qu'elle ne pose pas"
---

# Session retro — 2026-09-17 — 0567b471

## Fait

- **#392** (`9292055a`) — l'aide de `create app` porte les DEUX formes d'appel, dont
  `npm create nodefony@<version> -- <nom>`, et le motif du `--` (l'erreur exacte d'npm est nommée).
- **#393** (`ca2aca53`) — arbitrage rendu : le mode non interactif ne pose aucune interface web et le
  DIT. Deux canaux neufs (« Prochaines étapes », `AGENTS.md`), liste des moteurs dérivée de
  `FRONTEND_CHOICES`.
- Ancres de **#349** recalées (dérive de plus de 340 lignes, antérieure à ce diff), `Jours` du parent
  **#388** ramené à 6 après la fermeture de deux enfants.

## Coûts évidents

- **Deux rebuilds obligatoires** pour constater quoi que ce soit : `npx nodefony` sert le `dist`, et
  ma première mesure portait sur un binaire périmé qui ne montrait PAS la correction.
- **Trois allers-retours sur la forme d'une ligne de tableau markdown** (barres nues, puis padding
  refusé par `format:scaffold`, puis alignement pris sur le formateur) — le mini-cahier des charges
  aurait coûté une lecture des lignes voisines.
- Deux passes complètes de la suite core (2 min chacune) pour qualifier un flake — justifié : c'est
  ce qui a évité d'imputer à mon diff un rouge de décor.

## Recommandations

1. **Avant d'ajouter une ligne à une table rendue, lire ses voisines ET demander au gate** — versé au
   sas (thème `🪤`, désormais mûr à 5).
2. **`Closes #N` ne ferme rien depuis `dev`** — fait consigné dans `nodefony-ticket` §7, là où le
   geste de fermeture est décrit.
3. Ne pas ouvrir de ticket pour le flake `CliIntegration > invocation nue` (une occurrence, vert en
   isolation et à la reprise) — le noter suffit tant qu'il ne revient pas.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact REÇU (`feedback_prove_on_received_artifact`) — la règle a mordu, mais
  APRÈS que j'aie mesuré sur le dist périmé.
- ✅ Un test neuf se voit ROUGE (`feedback_gate_must_bite`) — 5 débranchements, chacun prouvé sur le
  fichier.
- ✅ La commande du dépôt est l'autorité (`feedback_repo_command_is_authority`) —
  `format:scaffold` a refusé mon alignement calculé à la main.

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `9292055a` | fix(cli): montrer la forme complète de la commande de création |
| `ca2aca53` | feat(scaffold): nommer le geste qui pose une interface web     |
