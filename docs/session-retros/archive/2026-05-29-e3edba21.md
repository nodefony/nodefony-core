---
date: 2026-05-29
session_id: e3edba21-929c-4737-91e8-ea13170a4079
focus: migration template Eta (retire Twig+EJS, API neutre) + page d'accueil app + clôture backlog majors npm
---

# Session retro — 2026-05-29 (2) — e3edba21

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |   120 |
| Edit       |    69 |
| Read       |    40 |
| Write      |     9 |
| WebFetch   |     5 |
| ToolSearch |     1 |
| Skill      |     1 |

## Livrables (4 commits poussés `claude-ts`)

| Commit    | Sujet                                                                       |
| --------- | --------------------------------------------------------------------------- |
| `feb8675` | majors UI Studio — mantine 8→9 (7 pkgs) + react-markdown 9→10               |
| `1fde039` | moteur template UNIQUE Eta (retire Twig+EJS) + page d'accueil app           |
| `c81370e` | deps tooling — purge morts core (glob/npm-run-all2/visualizer) + glob 11→13 |
| `3ffbc19` | e2e cluster realtime opt-in (RUN_CLUSTER_E2E) — gate fiable                 |

Hors commits : MEMORY.md dégraissé 34→23 KB (sous limite), backlog majors clos.

## Coûts évidents

- Session TRÈS longue (250 tours) — multi-feature dans une seule session (majors UI + migration
  moteur + landing itérée + deps + fix gate). Aurait dû être 2-3 sessions courtes (/clear entre).
- Piège turbo logs cachés : TS2358 "fantôme" relu du cache → 2 builds gaspillés avant `--force`.
- Page d'accueil itérée en ~5 passes (liens faux /react vs /react/app, Symfony à virer, piliers,
  debugbar) → le mini-cahier des charges en amont aurait économisé des allers-retours.

## 💶 Coût (€)

- **~€111.6 (≈ $121)** — 250 tours. **Cache-dominé ~86 %** : cache_read 60.2M tok ($90) +
  cache_write_1h 482k ($14.5) ≫ output 204k ($15.3) + input 70k ($1). Levier #1 = contexte plus
  mince / `/clear` entre features, PAS écrire moins. Session longue = cache porté cher.

## Recommandations

1. **Découper** : « majors UI » / « migration moteur template » / « landing » = 3 sessions distinctes.
   Une seule session a porté tout le contexte (CLAUDE.md + skills + gros fichiers) sur 250 tours = cache cher.
2. **Stopper le dev server avant `npm test`** — `npm test` (turbo) rebuild les dists → race avec un
   dev server watch → `ERR_MODULE_NOT_FOUND` transitoire (gotcha capté, dans le state).
3. **e2e fork = opt-in** confirmé doctrine (RUN_CLUSTER_E2E) — appliquer le même pattern aux futurs e2e lourds.

## Patterns récurrents (déjà gérés)

- ✅ Turbo rejoue logs cachés → `--force` ([[feedback_turbo_cache_stale_logs]]).
- ✅ Tests perf/e2e opt-in, pas dans le gate ([[feedback_perf_tests_optin]]).
- ✅ Décision design = trancher + justifier (API neutre `template`, Eta vs Twig) sans QCM.
- ✅ Vérif front = curl transform Vite + confirmation visuelle user (no headless).
