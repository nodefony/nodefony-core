---
date: 2026-09-10
session_id: 43e8a164-d029-42e4-9db7-bdb219bcf8cb
focus: trois tickets de la grappe pré-alpha.5 — la garde d'image, le décor lié du banc, le Dockerfile généré
---

# Session retro — 2026-09-10 — 43e8a164

## Focus

#317 (garde d'image), #329 (décor lié du banc devkit), #318 (droits, persistance et
reproductibilité de l'image générée). Trois tickets pris à la suite, tous prouvés sur
l'artefact reçu et non sur ce qui a été écrit.

## Fait

- **#317** — `895b5ef9`. La règle existait déjà (`detecterSuspects`, qui garde les tarballs npm) ;
  elle n'était appliquée à AUCUNE image. `detecterSuspectsImage` n'est qu'un filtre devant elle.
  Le contrôle lit `docker save` et jamais `docker export` : l'export rend l'arborescence aplatie,
  où un `COPY secret` suivi d'un `RUN rm` est invisible alors que la couche reste lisible par qui
  télécharge. Vu mordre sur l'image PUBLIÉE (`10.0.0-alpha.4`, 8 couches, 22 557 chemins, les 5
  fichiers nommés) et sur une image jetable à couche effacée. Ticket laissé OUVERT : son troisième
  critère (« constaté sur l'image tirée du registre ») ne peut l'être qu'à l'alpha.5.
- **#329** — `2fd4e248`, fermé. Épinglage DÉRIVÉ des pairs sur l'exemplaire du dépôt, plus une
  sonde qui CONSTATE avant de compiler et impute au DÉCOR. Banc lié : 21 étapes vertes, exit 0.
- **#318** — `33c1f5c5`, fermé. `mkdir` propriétaire de `tmp/`+`var/`, `USER 1000:1000` numérique,
  code laissé à `root`, `npm ci` si verrou, étiquettes OCI, volume nommé au README et au compose
  (service `app` sous profil). Les cinq critères prouvés sur l'image réellement construite.
- **`5766481e`** — le geste triple de fermeture : deux ancres voisines recalées (#226, #229), deux
  pages de doc, un commentaire sur #173, et le parent #316 ramené de 9,5 à 8,5 j.

## Coûts évidents

- **Quatre lancements du banc devkit en décor lié** (~2 à 10 min chacun) : un perdu sur un drapeau
  refusé, un sur `EOVERRIDE` (paquets du dépôt), un sur `EOVERRIDE` (dépendance directe), le
  dernier vert. Les deux `EOVERRIDE` n'étaient pas devinables — npm ne documente pas qu'un
  `overrides` puisse contredire une dépendance directe.
- **Trois faux verdicts d'instrument**, tous rattrapés avant de conclure publiquement (voir
  RETEX.md) : le code de sortie d'un `echo`, `${PIPESTATUS}` en zsh, une boucle de sonde sans
  `sleep`.
- Deux `docker build` complets (~2 min chacun) pour l'image témoin de #318, plus deux témoins
  fautifs — nécessaires : c'est ce qui a fait mordre les gardes.

## Recommandations

1. **Le thème « Un contrôle VERT qui ne POUVAIT rien voir » est à 14 frictions** (seuil 5), après
   trois de plus cette session. C'est de loin le plus mûr du sas, et il attend depuis des semaines.
   À graduer au prochain CONSOLIDATE, en priorité sur tout le reste.
2. **Écrire le code de sortie dans un FICHIER pour toute commande de fond** — trois faux verdicts
   en une séance sur ce seul geste. Ne jamais terminer une commande de fond par un `echo`.
3. **`#255` porte une ancre fausse (`create.test.ts:992`) qui l'était DÉJÀ avant ce lot.** Signalée
   dans le compte rendu de #318, pas corrigée — elle appartient à son ticket.

## Patterns récurrents (déjà gérés)

- ✅ « 1 RÈGLE = 1 implémentation » a mordu utilement dès la première heure : `detecterSuspects`
  existait, la réécrire aurait donné deux listes divergentes.
- ✅ « prouver sur l'artefact REÇU » : le gate #317 a été éprouvé sur l'image publiée, pas sur le
  gabarit ; #318 sur l'image construite, pas sur le `.tpl`.
- ✅ « un gate qu'on n'a jamais vu échouer n'est pas un gate » : trois débranchements avec `diff` à
  l'appui (parseur tar, assertions du Dockerfile, sonde de pairs).

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `895b5ef9` | test(release): refuser une image qui embarque une clé privée            |
| `2fd4e248` | fix(devkit-bench): rendre le décor lié incapable de dédoubler une pair  |
| `33c1f5c5` | fix(docker): réparer droits, persistance et reproductibilité de l'image |
| `5766481e` | chore(pilotage): recaler ce que la fermeture de #318 et #329 rend faux  |
