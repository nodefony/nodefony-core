---
date: 2026-06-12
session_id: be29638f-514e-4af4-9e87-0ec6f9ebb762
focus: P6 J1 — authenticators socle (Anonymous/UserPassword, mode first|all, registre, RFC 7235) prouvés bout en bout
---

# Session retro — 2026-06-12 (soir) — be29638f

## Fait

- **J1 complet en 1 session** (effort max, plan §6 de l'audit) — 4 commits poussés :
  - `ecd3dab7` user : contrat `IPasswordVerifier` + **bcrypt natif → import lazy** (fix d'un bug latent).
  - `c84c0ba3` security : Anonymous/UserPassword (Basic RFC 7617, anti-énumération), `UserToken`
    (credential effacé au succès), `mode first|all`, registre de fabriques, firewall **fail-closed**
    au boot (Zod + noms validés), challenge `WWW-Authenticate` RFC 7235. 25 tests unit.
  - `25e90b29` banc `module test nodefony/secure/` (directive user : dossier = préfixe = zone
    `test-secure`) + migration config legacy `firewalls` → `areas` + 9 tests intégration.
  - `9d7f7e99` dashboard : P6.3/6.4/6.5 ✅, bandeau 24 %.
- Gates : 42 unit security + 38 user + **447 intég http (0 régression)** + memory 9/9 + tsc 11/11
  - security-review ✅. Preuve curl AVANT suite (401+challenge / 200+identité ALS).

## Frictions / leçons (→ SAS RETEX.md)

- **Barrel ESM + peerDep optionnelle importée statiquement** = binaire natif chargé à chaque boot
  (bcrypt) — détecté par la directive lazy du user → `import()` dynamique dans l'instance (nouvelle).
- **Config fossile 3×** : bloc `firewalls` legacy jamais lu → app dev SANS zone (incrémentée, candidate
  graduation).
- **Constante présumée** : `anonymousUser.identifier` = `"anon."` pas `"anonymous"` → 3 fails évitables
  (incrémentée avec « vérifier le contrat »).
- **Tester un Service hors kernel via contrat public** (fake Module structurel + ALS run) (nouvelle).

## Directives user captées en cours de route

- Controllers de test sécurisés SÉPARÉS (répertoire `secure/`, politique de route explicite) — appliqué.
- **Lazy imports partout où possible** — appliqué + gravé au kit P6 (argon2/jose/simplewebauthn).

## Fichiers protégés touchés (signalés au récap)

- `src/modules/test/rollup.config.ts` : +1 entrée external `@nodefony/user` ; `package.json` : +1 peerDep.

## Commits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `ecd3dab7` | feat(user): contrat ipasswordverifier + chargement lazy bcrypt          |
| `c84c0ba3` | feat(security): p6 j1 authenticators socle — first\|all, registre, 7235 |
| `25e90b29` | feat(test,app): banc zone protégée test-secure — 401/200 bout en bout   |
| `9d7f7e99` | docs(migration): p6 j1 coché                                            |
