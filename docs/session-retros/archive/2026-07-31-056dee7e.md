---
date: 2026-07-31
session_id: 056dee7e-087a-4bb6-a375-66586afec186
focus: jouer la famille sécurité du banc devkit avec de vrais agents — et refermer ce que la mesure a ouvert
---

# Session retro — 2026-07-31 — 056dee7e

## Focus

Priorité 1 du `_state` : jouer T2/T20/T22/T23/T24 avec de vrais agents, médiane ≥ 3 runs.
La mesure a d'abord échoué sur son propre décor, puis a désigné trois trous produit.

## Fait

- **4 runs exploitables** (`haiku`, décor isolé) : T2/T20/T24 **4/4**, T22 3/4 (l'échec = décor),
  T23 **0/4**. ~195 tours médians par passe de 5 tâches, 7,75 $ au total, dispersion ±16 %.
- **Trou produit n°1** — la suite e2e générée démarrait en `production` avec
  `NF_ADMIN_PASSWORD=e2e-admin-jetable` **dans la base de développement** : le seed idempotent
  figeait `admin` sur un mot de passe publié dans le dépôt, et `admin`/`admin` mourait pour
  toujours. En sens inverse, un `admin` de dev faisait échouer les tests e2e — les agents
  contournaient à l'aveugle (`rm -f var/databases/*.db`, jusqu'à 6× dans une tâche). Base e2e
  dédiée et purgée.
- **Trou produit n°2** — le mot `stateless` n'existait dans **aucun gabarit** (compté). Un agent à
  qui on demande une API pour un programme n'avait aucun chemin vers la troisième nature de zone.
  Écrit dans la config générée ET dans `AGENTS.md`.
- **Trou produit n°3** — le gabarit annonçait `npx nodefony certificates` ; la commande est
  `http:certificates`.
- **T23 requalifiée** : sa prémisse était fausse (route non montée). Crochet `prepare` + **T25**.
- **Banc élargi** : T26 (API pour un programme — témoin « aucun cookie semé »), T27 (bourrage de
  login — 429 + `Retry-After`), avec leurs deux juges et leurs 20 causes vues rouges.
- **Sonde partagée réparée** : 5 briques gardées sur 13 ; la liste se déduit du schéma Zod.
- **CI réparée** : deux gates rouges depuis la veille sur les 6 jobs (concaténation inutile ;
  8 juges déclarés morts par une règle qui n'existait que pour les scripts de la racine).

## Coûts évidents

- **Travailler sur la machine qui mesure** a pollué le run C (décor non bouté en 15 s).
- Le premier `git push` a échoué : hook `pre-push` en concurrence avec le build du banc.
- Deux monitors morts en cours de route (exit 144) → surveillance perdue, réarmée à la main.

## Recommandations

1. Ne plus lancer de build/test lourd pendant un run de banc — la mesure est le travail.
2. Jouer T25/T26/T27 avec de vrais agents : les instruments sont prouvés, pas ce qu'un agent en fait.
3. T21 (scope) devient une extension de T26 plutôt qu'une tâche à part — même terrain.

## Patterns récurrents (déjà gérés)

- ✅ La sonde/le décor est le premier suspect : quatre causes de décor ont correctement dit
  « ce n'est pas l'agent », dont celle qui a ouvert toute la session.
- ✅ Preuve négative systématique : chaque test et chaque sonde neufs vus rouges avant d'être crus.

## Commits produits

| Commit     | Sujet                                                   |
| ---------- | ------------------------------------------------------- |
| `47a7934a` | la suite e2e n'écrit plus dans la base de développement |
| `c0be8490` | config générée en TSDoc + commande fausse corrigée      |
| `7a35ff01` | deux gates CI rouges sur six jobs                       |
| `6fa8ca06` | la garde `when` s'explique là où elle est utilisée      |
| `d0de4a25` | la sonde gardait 5 briques sur 13                       |
| `34a02074` | prémisse fausse → crochet `prepare` + T25               |
| `9cf9b41f` | le mot `stateless` n'existait nulle part                |
| `fc34d591` | T26 (M2M) + T27 (throttle)                              |
| `88d546b4` | les 20 causes des deux juges, chacune vue rouge         |
| `dbe9838e` | les deux auto-contrôles entrent dans le SKILL           |
