---
date: 2026-08-06
session_id: cb9ab582-4e4d-4aaf-a66b-9636f6f87057
focus: sonde perf in-situ NF_PERF_PROBE — verdict scope DI + design fabrique allégée
---

# Session retro — 2026-08-06 — cb9ab582

## Fait

- Sonde in-situ `NF_PERF_PROBE=1` posée à demeure (2 vagues : postes globaux puis tranches
  internes des ctors Service/Request), dump `GET /nodefony/kernel/bench/probe`.
- Verdict : poste total ~18,7 µs/req (profil confirmé en TOTAL, faux en ATTRIBUTION) ;
  `Request.url` ~5,5 µs (URL double-parsée), ctor Service ~36 % (delete + Event/req + Map),
  enterScope réel ~2 µs → S2/S3 enterrés ; dictionary mode innocenté (`%HasFastProperties`).
- Design « fabrique allégée » figé au kit : lots F-A → F-F → F-C → F-B → F-D (~7-9 µs visés).
- F-A entamé sur demande puis ANNULÉ proprement (fin de session demandée) — contrats
  pré-vérifiés consignés au kit (Event ne lit pas options.events ; Kernel passe nbListeners 60).

## Frictions / coûts

- Machine sur BATTERIE découverte en cours de banc : −25 % RPS, fenêtre stable mais fausse en
  absolu — `pmset -g batt` doit précéder toute série (bullet RETEX).
- DevSupervisor résiduel (7 h) au titre nommé, invisible du `pkill bin/nodefony` → boot
  production refusé, log muet (`NF_LOG_DRIVER=null` coupe aussi l'erreur de refus).
- Field initializers d'une classe dérivée exécutés APRÈS super() : première attribution de la
  tranche « delete » fausse (elle contenait randomUUID + get(router)) — corrigée en cartographiant
  les inits avant d'attribuer.
- Hook pre-commit à garde chemin-relatif : commit depuis un sous-dossier → faux « cœur pas
  construit ».
- Feedback user en cours de session : être PLUS BAVARD pendant les devs (2ᵉ signalement,
  gravé dans `feedback_user_visibility`).

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `f20454df` | feat(perf): sonde in-situ NF_PERF_PROBE — le poste scope DI dit où il vit |
| `565afcd0` | feat(perf): la sonde décompose les ctors Service et Request — l'URL avoue |

## Recommandations

1. Lots F-A/F-F/F-C en session fraîche, A/B sur SECTEUR uniquement.
2. F-B (URL fast-path) = session dédiée avec red-team (normalisation WHATWG = protection routing).
3. CONSOLIDATE toujours dû (10ᵉ signalement — RETEX.md ~1000 lignes).
