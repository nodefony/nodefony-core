---
date: 2026-05-25
session_id: 6b08c302-d8c0-488b-aa7f-eb59f7e73ce6
focus: Clarification modes de lancement (HMR = dev only) + retrait commande staging/preprod
---

# Session retro — 2026-05-25 (soir) — 6b08c302

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Edit  |    23 |
| Bash  |    20 |
| Read  |    12 |
| Skill |     1 |

## Top fichiers Read/Edit

| Fichier                                                                     |        × |
| --------------------------------------------------------------------------- | -------: |
| `tests/KernelCommands.test.ts`                                              |        6 |
| `clusterMaster.ts`                                                          |        2 |
| `StartCommand.ts` / `CliKernel.ts` / `bin/nodefony.ts` / kernel `CLAUDE.md` | 2 chacun |

> Aucun fichier relu 5×+ : exploration ciblée (grep d'abord, lecture chirurgicale ensuite).

## Coûts évidents

- **Phase clarification longue** (HMR/cluster/staging) avant le code : ~la moitié des tours
  en explication, 0 livrable — mais a évité de coder le mauvais chantier (« dev-cluster HMR »).
- Pas de restart serveur, pas de boucle build (refacto CLI hors hot path → 0 memory.test).
- 1 commit rejeté par commitlint (sujet `CLAUDE.md` majuscule) → recommit minuscule.

## 💶 Coût (€)

- Total **~€16.94** (≈ $18.41) — cache-read 6.94M tok ($10.4) + cache-write 1h 139k ($4.2) =
  **~79 % cache** (contexte porté : CLAUDE.md géant + MEMORY.md + SKILL session entier). Output
  46k ($3.5) faible. Enseignement classique : le coût = le CONTEXTE, pas ce que je produis.
- ~€8.5 / commit (2 commits). Acceptable vu qu'une moitié = décision archi durable (HMR=dev only).

## Recommandations

1. **Pas de nouveau skill** — pattern non répété 3×. Refacto one-shot.
2. **Mémoire** — décision « HMR = development only, dev-cluster HMR REJETÉ » actée dans 3 fichiers
   (launch_modes_decision, live_cluster_debug_workflow, \_state). Évite de re-proposer le chantier.
3. **Pré-vérifier la résolution d'env avant de supprimer du câblage** (j'ai lu Cli.ts/Kernel.setEnv
   AVANT de retirer la détection argv staging) → 0 régression, env `staging` préservé. Bon réflexe.

## Patterns récurrents (déjà gérés)

- ✅ Commits FR via `-F` heredoc ([[feedback_commit_fr_apostrophes]]).
- ✅ Décision archi = trancher + POURQUOI, pas d'AskUserQuestion ([[feedback_session_hygiene]]).
- ✅ Séparer « commande » (mode) de « valeur d'env » avant de supprimer ([[feedback_refactor_grep_consumers]]).

## Commits produits

| Commit    | Sujet                                                    |
| --------- | -------------------------------------------------------- |
| `5b69ae0` | refactor(cli): retire la commande staging/preprod        |
| `bc22a2c` | docs: liste des commandes CLI à jour (cluster ↔ staging) |
