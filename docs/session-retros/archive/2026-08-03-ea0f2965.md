---
date: 2026-08-03
session_id: ea0f2965-8e37-41f0-a35b-eb37893fea42
focus: Les skills rattrapent 6 sessions de contrat de page ; le devkit et paginate() cessent de prouver le refus à la place de la capacité
---

# Session retro — 2026-08-03 — ea0f2965

## Focus

Rendre VISIBLE et ÉPROUVÉ ce que six sessions de chantier pagination avaient livré :
la doc d'agents (skills), le banc du générateur, et le dernier maillon du socle ORM.

## Fait

- **Skills** — `nodefony-framework-dev/references/pagination.md` NEUF (le contrat back en
  10 sections) + section 8 de `nodefony-studio-dev/references/ui-kit.md` (le front qui DEMANDE
  la capacité). Neuf symboles du contrat avaient **zéro** occurrence dans les 16 fichiers de skill,
  alors que la description de `framework-dev` les annonçait dans ses déclencheurs.
- **Gate** — `skills-doc.mjs` contrôle enfin les renvois `references/*.md` (il ne voyait que les
  scripts). Résolution du cas **croisé** incluse.
- **devkit** — le gabarit e2e éprouve désormais que le tri ORDONNE et que le filtre FILTRE, pas
  seulement qu'ils refusent. Trois sondes au moteur décident de l'émission. Deux défauts
  préexistants corrigés au passage (test de rejet sur un filtre `"string"`, échantillon en double).
- **orm-core** — `paginate()` honore `q` sur déclaration (`searchable`) et le REFUSE sinon ; la
  règle de recherche, écrite quatre fois, vit au socle.

## Frictions du jour (détail → RETEX.md)

- **Trois faux diagnostics dus à l'outil** : `\{` casse une alternance BRE ; `rg -oh` = `--help` ;
  un rejeu e2e sans `NF_PORT` tombe sur le serveur de dev du dépôt (12 tests en 404).
- **Le gate ne mordait pas** — cible supprimée, passe verte. Et son premier jet criait sur un
  renvoi croisé légitime.
- **Prouver le refus ne prouve pas la capacité** — le tri débranché laissait le test de refus vert.
- **Un échappement non émis est pire que pas d'échappement** — divergence SQLite ↔ PG/MySQL,
  mesurée sur les trois moteurs.
- **Une régression causée par un commentaire** — `notInclude(e2e, "409")` vise la chaîne brute.

## Ce qui n'a PAS été lancé

- Banc devkit en décor **ISOLÉ** (sans `--link`) — le seul qui voie une dépendance manquante.
- Suites `security`, `http`, `framework`, `core` complètes (hors `create.test.ts`) ; gate mémoire.
- Conteneurs `nodefony-postgres` et `nodefony-mariadb` **laissés démarrés** (ils étaient éteints).

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `08f6cabe` | docs(skills) — le contrat de page cesse d'être promis sans être écrit        |
| `1ecc11b6` | fix(skills) — un renvoi vers une ressource absente cesse de passer pour vert |
| `34e1240c` | docs(skills) — la console cesse d'ignorer ce que le serveur publie           |
| `baefb92e` | test(devkit) — prouver le REFUS ne prouve pas la CAPACITÉ                    |
| `a358af85` | feat(orm) — `paginate()` cesse d'avaler `q`, l'échappement cassé retiré      |
