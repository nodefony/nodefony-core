---
date: 2026-09-25
session_id: 40119e26-7a5c-491c-bc5b-886fa52109d7
focus: trois décisions tranchées (dashboard, savepoints Mongo, anchor-check) + #479 reporté en beta
---

# Retex 2026-09-25 — 40119e26

- **Fait** :
  - `d33eeb462` chore(board) — #479 reporté en fin de `10.0.0-beta` (ordre 6.07, 25/09 → 01/12) : figement intermittent, sonde `run-watched.mjs` déjà posée, surveillance pendant la beta.
  - `2e47c1dfe` refactor(realtime) — `PLATFORM_CHANNELS.dashboard` `@deprecated` → `supervision`, retrait en 11.0.0.
  - `f394e8c2e` fix(docs) — `anchor-check` signale une ancre dérivée loin de sa déclaration (exceptions : en-tête TSDoc, site d'usage) ; autotest 4 cas en CI ; `anchor-fix` lit le nouveau suffixe.
  - `8d11de01f` docs — 856 ancres recalées (1 035 → 182 SUSPECT), diff prouvé « numéros seulement ».
  - `6b1875b52` fix(mongoose)! — `savepoint()`/`rollbackTo()` lèvent `SavepointNotSupportedError` ; le contrat éprouve le refus au lieu de sauter le cas.
- **Frictions** (toutes sous des thèmes déjà gradués — pointées, pas recopiées) :
  - L'autotest de `anchor-check` passait À VIDE : l'outil plantait dans le dépôt jetable (`find` sur des racines absentes), stdout vide = « aucun suspect ». Remède : exiger le bilan de l'outil avant de juger. → [[feedback_gate_must_bite]]
  - Débrancher l'exception « en-tête TSDoc » ne faisait rien tomber : le cas tenait dans la tolérance fixe, puis l'exception « usage » l'absorbait (le mot figurait dans le TSDoc). Deux réécritures du cas avant qu'il discrimine. → [[feedback_test_discriminant_or_dead]]
  - Ma vérification « le diff ne touche que des numéros » a crié faux : `uniq` comparait aussi le `-`/`+` de tête. → [[feedback_suspect_instrument_and_own_diff]]
  - Découpe d'un test par sous-chaîne `"    });\n"` : a coupé au premier `});` imbriqué, restes à nettoyer.
- **Décisions** :
  - `dashboard` déprécié et non retiré : publié depuis alpha.1, la politique interdit le retrait direct ; le réaliaser vers `supervision` changerait son comportement en silence.
  - Savepoints Mongo REFUSÉS dès `savepoint()` : un no-op mentait sur l'annulation ; lever au `rollbackTo()` seul cacherait la faute dans le chemin d'erreur, le moins testé. Rupture admise en alpha.
  - Jalon `10.0.0-alpha` gardé ouvert jusqu'à la publication d'alpha.9 (décision user).
