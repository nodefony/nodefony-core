---
date: 2026-06-27
session_id: 892bebaf-d957-43eb-a636-269e2ecfa3e2
focus: Webhooks P6.13 backend (SSRF + signature Standard Webhooks + livraison bornée anti-DoS) + fix déconnexion session
---

# Session retro — 2026-06-27 — 892bebaf

## Fait

- **Webhooks P6.13 BACKEND complet** (3 commits, `refactor/p6-security`) :
  - `4d9006c5` slice A — `WebhookService` (CRUD + SSRF enregistrement + secret `whsec_` chiffré at-rest)
    - briques génériques `secretCipher` (AES-256-GCM/HKDF, totp délègue), `ssrfGuard` (`node:net.BlockList`,
      RFC 6890 + OWASP), `SsrfError(422)`, contrats + registry + MemoryWebhookStore.
  - `02726434` slice B — `WebhookDispatcher` (abonné audit → filtre → signe **Standard Webhooks v1** →
    livre → retry/auto-disable) + `webhookSignature` (vecteur officiel) + `webhookDelivery` (`node:http(s)`
    pin IP anti-rebinding + 3xx non suivi + timeout). **PERF** : hot-path 0-alloc, concurrence + file bornées.
  - `32ef5069` — auto-désactivation → audit `webhook.disabled` (borné) + garde anti-boucle.
- **fix session** `35d38d6e` — `maxLifetimeS: 8h` en dev (pansement déconnexion Studio).
- Gates : security **744/744** · tsc 0 · **memory.test 9/9** · red-team 0 faille.

## Frictions / leçons (→ RETEX.md)

1. **Le red-team révèle la VRAIE protection** : mon helper regex IPv4-mapped était redondant ET incomplet
   (ratait la forme longue) ; `node:net.BlockList` le fait mieux nativement → ne pas réinventer une lib auditée.
2. **Perf webhook = anti-DoS du framework** : auto-disable+retry insuffisants → BORNER concurrence + file
   (le user a dû me le rappeler). Réflexe « burst d'envois ratés = DoS ».
3. **`maxLifetimeS` = reliquat PHP** ; norme = idle + absolute (NIST 800-63B-4/OWASP), pas un seul timeout.
4. **`.git/index.lock` orphelin** (lint-staged/husky) → `rm -f` avant commit (3× cette session).
5. **digest sous-agent ≠ terrain** : re-lu le vrai code avant de cloner.

## Décisions structurantes

- **Signature = Standard Webhooks v1** (HMAC-SHA256) — PAS RFC 9421 (trop lourd consommateur) ni HMAC maison.
  Slot Ed25519 (`v1a`) réservé. Conforme au vecteur officiel de la spec.
- **RBAC webhooks = data plane** (Slice C), service = CRUD pur sans garde. Modèle admin PLATEFORME
  (`ROLE_NODEFONY_ADMIN`), pas par-user. Slots tenantId/createdBy réservés P17.
- **Audit lean** : seul l'auto-disable est tracé (borné), pas chaque échec/drop → 0 charge audit sous burst.
- **Bug session = reliquat PHP** : le vrai fix = modèle NIST idle/absolute + touch (chantier).

## Commits produits

| Commit     | Sujet                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| `4d9006c5` | feat(security): webhooks slice A — registre + SSRF + secret chiffré             |
| `02726434` | feat(security): webhooks slice B — dispatcher signé + livraison bornée anti-DoS |
| `32ef5069` | feat(security): webhooks — auto-désactivation tracée + anti-boucle              |
| `35d38d6e` | fix(session): maxLifetimeS 8h dev — pansement déconnexion Studio                |
