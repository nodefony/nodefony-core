# Session retro — 2026-09-24 — e929127f

## Focus

#465 : `create entity` sur une application MongoDB — devenue, à la demande du user, une remise à
niveau du générateur entier (terminal + Studio) et de ses copies (doc, skills, banc).

## Fait

- `02c6fb3c` #465 — entité DOCUMENT (schéma Mongoose, Zod, service, controller, tests) ; options SQL
  refusées en le disant ; connecteur `nodefony` ; capacité `hasSqlOrm` qui tait la clé primaire.
- `2c66f3e8` — tri Mongoose sur `id` traduit en `_id` (départage du tri par défaut inerte).
- `9af1e189` — Studio tait les mêmes questions que le terminal (`capAllows`, test de parité).
- `cd69c97e` — audit `fable` du générateur : 10 défauts corrigés, dont 🔴 DELETE de module né SANS
  `@IsGranted` (entity ET controller), import multi-ligne corrompu, tests générés rouges sans champ
  obligatoire, auto-référence refusée ; index unique PARTIEL pour `?`+`:unique` (Mongo réel) ;
  connecteur = 1ʳᵉ question ; `adminLogin` dans `nodefony/testing` ; doc recalée (7 pages, 7 ancres).
- `a36f5cf3` #468 — Studio : éditeur de champs ligne par ligne, index par sélection, panneau des
  types par moteur ; `create/spec` = même composition que le terminal (test d'identité).
- `a915bbc3` — `ticket:lint` nomme la panne GitHub en cours (page de statut).
- `70e1ac12` — banc de vérité : entités Mongo éprouvées pour de vrai ; bilan de chaque commande au journal.
- Tickets : #465 fermé (compte rendu) ; ouverts #466 → #471, tous `alpha`.

## Preuves

Suite complète du cœur 4336 verts · Studio 106 · Mongoose 355 · banc de vérité VERT SQLite et
MongoDB (61 e2e d'entités sur Mongo réel) · Studio vu à l'écran (SQLite), axe : 1 violation
préexistante (bouton « Générer » 3,35:1) · chaque garde neuve vue rouge.

## Frictions

- J'ai créé une divergence terminal/Studio (source « serveur démarré ») que la règle interdit —
  vue par le user, retirée, test d'identité posé.
- Une note « connecteur ouvert en code n'y figure pas » posée à la place d'une correction — refusée
  par le user à raison ; la correction est #469.
- Cinq de mes propres tests étaient faux avant le code (compte de caractères, décor de module,
  `version` non exportée…) : écrire le test, puis le lire comme un suspect.
- `git commit -- <chemins>` échoue sur un index temporaire (objet `package-lock.json` absent).
- Panne GitHub Projects toute la séance : #465 → #471 invisibles au tableau, inscrits.

## Non fait / prochaine étape

#469 (mediasoup DÉCLARE son connecteur — plan dans le ticket) → #470 (doctor signale un connecteur
non déclaré) → #471 (champs un par un dans le terminal) ; #466, #467 ; CONSOLIDATE dû (5 thèmes mûrs).
