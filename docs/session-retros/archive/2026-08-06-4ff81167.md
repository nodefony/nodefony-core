---
date: 2026-08-06
session_id: 4ff81167-f30c-49e3-b80d-2f6c4529b714
focus: lot prepared-statement mémoïsé (session 11 kit perf) — ~+86 % read, ~+96 % session+find
---

# Session retro — 2026-08-06 (soir, 3ᵉ) — 4ff81167

## Fait

- **Lot prepared mémoïsé LIVRÉ** (`1f1926a7`) : SELECT préparés par FORME dans
  `DrizzleRepository` (placeholders re-bindés, repli `$or`/riches/tx/cap 128,
  sondes conservées). A/B c25 2 paires propres × 2 routes : read-lean
  1 077-1 090 → 2 003-2 035 RPS (**~+86 %**) · session+find 769-778 →
  1 515-1 518 (**~+96 %**). Gates : drizzle 425 + intégration http 619/0 +
  memory 9/9 ; 4 tests vus ROUGES au débranchement + anti-staleness rouge.
- **E2e PG/MariaDB/MySQL Community rejoués sur docker réel** (`923c18d5`) :
  1 001 verts PG+MariaDB ; MySQL 8.4 a révélé une table `session` STALE
  (user TEXT pré-colKit) que MariaDB masquait par auto-préfixe — drop → renaît
  varchar(512), 53/53 des deux côtés. Gotcha gravé au MEMORY drizzle.

## Frictions / leçons (versées au SAS)

- Lire le SOURCE de la lib pour chaque méthode appelée (exigence user) a rendu
  2 contrats invisibles de la doc : `bindIfParam` exclut les Placeholder ;
  drizzle+mysql2 = `client.query()` sans prepare protocole.
- « Cache » annoncé sans son périmètre (forme ≠ données) → inquiétude user
  légitime ; le test anti-staleness doit partir AVEC le lot.
- Garde de banc : 5 refus old2 (médianes pourtant ±1 %) — attendre la fenêtre,
  jamais négocier la garde.
- Récidive `cd` relatif (hook a mordu 2×) + un `vitest run` lancé de la racine
  = 453 fichiers rouges en faux décor.

## Commits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `1f1926a7` | perf(drizzle): selects préparés mémoïsés par forme               |
| `923c18d5` | docs(drizzle): gotcha table stale — MariaDB masque, MySQL révèle |
