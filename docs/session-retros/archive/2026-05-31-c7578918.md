---
date: 2026-05-31
session_id: c7578918-34ba-4208-8906-bcbd38a6af8c
focus: LB.5 (cluster-file) + console Logs cluster-honnête + chip runtime/backplanes + logDir configurable + fix test upload
---

# Session retro — 2026-05-31 — c7578918

## Focus

Finir les logs (Log Backplane) côté cluster + visibilité runtime Studio. Session
très longue, multi-requêtes entrelacées du user (chaque réponse = un nouveau raffinement).

## Fait (5 commits, pushés `ea834db..a7f8d80`)

- **LB.5** driver `cluster-file` (`ea834db`) : globbe `nodefony-*.jsonl`, merge trié `timeStamp`
  (uid non comparable cross-process), `scanJsonlTail` partagé, anti-OOM double. 7 tests.
- **back logs** (`bd7270b`) : dir de logs configurable (`config.log.dir`, source unique write↔viewer) ;
  `file`+`cluster-file` montés en dev (switchables) ; cluster + backplanes au data plane (kernel/info, syslog/backplane).
- **console Logs front** (`6ea92e5`) : `ClusterScopeNotice` (honnêteté vue partielle), select tous les modes,
  OpenSearch (pas Elastic), « Non persistant » (label négatif).
- **chip runtime topbar** (`bf8efed`) : `RuntimeModeChip` (env+mono/cluster, popover infos + fonds de panier
  Logs/Realtime, re-sync au survol).
- **fix test upload** (`a7f8d80`) : nettoie ses fichiers temporaires (snapshot diff before/after).

## Frictions (→ RETEX.md)

- Annoncé « reste LB.3c » sans vérifier → c'était déjà commité (3d6158e/c48858b). Vérifier le code avant.
- Couleurs ANSI bakées au payload (clc, multi-modules) ; strip per-log = perf NON → gate boot-time (TODO).
- Test upload laissait des résidus → cleanup `after` obligatoire.
- logDir hardcodé ≠ viewer (tmp) → config source-unique.
- pre-push typecheck casse sur dist/types croisé périmé → `npm run build` avant push.

## Reste / prochaine étape

1. **Gate couleur ANSI boot-time** (TODO ciblé, refactor helper de tag d'event gaté par flag isTTY).
2. Validation runtime **cluster réel** (`nodefony cluster -w N`) de la console cluster-honnête.
3. Puis LB.3b (CLI `syslog:filter`) / LB.4 (loki/opensearch) ; sinon **durcissement ORM** → API souveraine → P6.

## Commits

| Commit  | Sujet                                                                          |
| ------- | ------------------------------------------------------------------------------ |
| ea834db | feat(syslog): driver cluster-file (LB.5)                                       |
| bd7270b | feat(syslog): dir configurable + file/cluster-file en dev + cluster data plane |
| 6ea92e5 | feat(studio): console Logs cluster-honnête + tous drivers select               |
| bf8efed | feat(studio): chip mode runtime + fonds de panier topbar                       |
| a7f8d80 | test(http): upload nettoie ses fichiers temporaires                            |
