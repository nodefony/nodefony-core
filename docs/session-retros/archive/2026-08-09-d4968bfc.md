---
date: 2026-08-09
session_id: d4968bfc-6527-40fa-8e09-9b3ef0d82714
focus: devkit lot 7 — le serveur MCP est une ROUTE, pas un process ; et une clé de chiffrement qui sortait en clair
---

# Session retro — 2026-08-09 — d4968bfc

## Focus

Reprise sur l'état du kit devkit, puis chantier complet du **lot 7 (MCP)** : de la
question « qu'est-ce que WebMCP déjà ? » jusqu'à un serveur éprouvé sur une
application générée.

## Fait

- **Kit devkit remis au vrai** (3 passes) : lot 5 annoncé « non entamé » alors qu'il
  est livré, dette des 16 décorateurs soldée, banc à 30 verdicts et non 14, section
  **Les YEUX** (le paquet ship 6 scripts exécutables, Lighthouse authentifié).
- **`llms.txt` et WebMCP tranchés** — et rangés hors du devkit : ils s'adressent à
  l'agent qui VISITE l'app, or le devkit est `policy:"dev"`. `llms.txt` : pas
  maintenant, la doc est derrière RBAC, l'index pointerait des 403.
- **Serveur MCP livré** : `POST /nodefony/mcp`, 4 outils qui appellent les briques
  extraites au cœur, `ai:mcp` pour le câblage, conformité `2026-07-28`.
- **Fuite de secret trouvée et corrigée** dans le producteur admin.
- **Doc** : gabarit `AGENTS.md` d'app générée + MEMORY/CLAUDE/README/docs du module.

## Coûts évidents

- **La spec lue en trois fois.** J'ai lu `transports`, conclu, implémenté — puis le
  user a demandé « à la lettre ? » et il a fallu lire `versioning` + `discover`, qui
  portaient trois MUST et la notion d'ÈRE. Lire la page qui parle de son sujet ne
  suffit pas pour une norme.
- **Un commit fourre-tout à refaire** : `git add -A` a mis quatre sujets dans un
  message qui n'en décrivait qu'un. Redécoupé en 4 commits (`reset --soft`).
- **Un flake non identifié** : un test devkit rouge au premier run, vert aux 4
  suivants, sortie non capturée. Suspect : `check`, qui sonde des ports réels.

## Recommandations

1. **Remonter le protocole MCP au cœur** — il vit dans un module `policy:dev`, donc
   le futur `@nodefony/mcp` de P12 ne pourra pas le réutiliser sans le dupliquer.
2. **Registre d'outils MCP** — une app ne peut rien ajouter aujourd'hui ; le patron
   `IAdminApi` existe déjà et répond exactement à ce besoin.
3. **Capturer la sortie AVANT de relancer** un test rouge : la relance efface la
   preuve.

## Patterns récurrents (déjà gérés)

- ✅ Gardes vues ROUGES avant d'être rebranchées (Origin, localité, préservation
  du `.mcp.json`, redaction).
- ✅ Prouvé sur l'artefact REÇU : app générée par `create app`, pas le dépôt seul.
- ✅ Une source, plusieurs portes : rien n'est recalculé par les outils MCP.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `dbf6c6cc` | feat(devkit) — le serveur MCP, une route et non un process         |
| `2834ee17` | feat(core) — `nodefony ai:mcp` câble l'agent sans rien lancer      |
| `f2da16d7` | fix(security) — une clé de chiffrement sortait en clair            |
| `9f8335da` | docs(devkit) — l'agent d'une app générée apprend que le MCP existe |
