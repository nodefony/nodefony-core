---
date: 2026-08-06
session_id: 31f69209-6069-4fe7-b00c-9a268debd1b2
focus: Profil du cycle ORM (session 10b) — escalier session/entité + 2 cpuprofiles fenêtrés, décor NF_BENCH_ORM versionné
---

# Session retro — 2026-08-06 — 31f69209

## Fait

- Banc du cycle ORM conçu en dialogue (escalier de routes plutôt que route unique — chaque
  marche isole UN composant par soustraction), corpus dolibarr réutilisé (fk_user réaliste).
- Décor versionné `NF_BENCH_ORM=1` : BenchOrmController (read/read-lean/write/reset/status +
  variante zone secure), entités sur connector `default`, seed idempotent 50/200/10 k.
- `bench-ab-mono.sh` : `BENCH_HEADER` (routes authentifiées au cookie, bash 3.2-safe).
- Escalier mesuré (c25, disp ≤ 3 %) : find() +850 µs/req · session sqlite +322 µs (memory +17) ·
  INSERT +666 µs · JSON 20 rows +43 µs · additivité vérifiée (~1 %).
- 2 cpuprofiles fenêtrés (`--cpu-prof` + analyseur maison) : couche Nodefony < 2,5 % du CPU ;
  goulot = build+prepare drizzle refaits à CHAQUE requête (~48 % du chemin read) ; Route.match
  7,4 % et Tools.extend 4 % vus en passant.
- Rapport photo `tmp/perf-orm-cycle-2026-08-06.html` (skill html-report).

## Frictions / leçons (détail au SAS RETEX.md)

- Import statique vers le corpus GITIGNORÉ committé de justesse — vert local, cassait tout
  clone ; rattrapé au staging, corrigé en import dynamique + fail-loud prouvé rouge.
- `timeout` absent sur macOS → 2 faux verdicts (rc 127) en une commande.
- r0b sandwich refusé 3× : le pollueur était le process de l'agent lui-même.
- c128 sur route sqlite sync = queue > 2 s (event loop sérialisé) → c25.

## Commits produits

| Commit     | Sujet                                                                     |
| ---------- | ------------------------------------------------------------------------- |
| `967f571b` | feat(test): banc du cycle ORM opt-in NF_BENCH_ORM — escalier sur dolibarr |
