---
date: 2026-07-25
session_id: 1ca77d41-b5b3-4146-b9d4-246eb70cf5af
focus: devkit S3 (exemples exemplaires) + solution pérenne front-prod (directive user)
---

# Session retro — 2026-07-25 — 1ca77d41

## Fait

- **devkit S3 livrée** (`779ead02` scaffold, `435d175e` frontend, `a8bc2376` banc) :
  realtime en `@RealtimeAction` policy inline, redirections WS, duplex `@IsGranted`,
  vitrines 3 fronts par la façade (`nodefony/client`) sur `LiveController` délégué,
  e2e realtime généré, AGENTS.md app+module (isomorphisme + container prototypal —
  demande user), gates +40 assertions (cassées exprès 1×), banc T3 haiku réel.
- **Front-prod jamais muet** (directive user en cours de session) : build chaîné au
  `npm run build` généré, auto-build one-shot annoncé au boot prod, absence de
  manifest jamais cachée. Doc publique + IA + kit release nourris.
- 404 Studio static/mandatory : NON reproduit (200 + assets sur chaîne à jour) —
  diagnostic livré (config dist périmée ou `build:ui` manquant en --link).

## Frictions / leçons (versées au sas RETEX.md)

- TS2305 chez le consommateur : export racine conditionnel (`browser`) invisible de
  `tsgo` — 2ᵉ occurrence « le typecheck de l'app générée voit ce que le repo ne peut
  pas voir ».
- Cacher l'ABSENCE d'une ressource fige la panne (manifest Vite → page blanche
  post-build jusqu'au restart).
- Sonde négative sur fichiers entiers → faux positif sur un agent irréprochable ;
  juger les lignes AJOUTÉES.
- Premier run banc = 429 quota : un FAIL d'agent qui n'a pas tourné n'est pas un
  verdict — lire `terminal_reason` avant de conclure.

## Commits produits

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `779ead02` | feat(scaffold): exemples exemplaires — façade client partout (devkit S3) |
| `435d175e` | fix(frontend): jamais de page blanche muette en production (devkit S3)   |
| `a8bc2376` | fix(bench): sonde T3 client + interdits sur lignes ajoutées (devkit S3)  |
