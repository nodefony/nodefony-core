---
date: 2026-08-28
session_id: 52b97e15-ca25-4a29-97f0-47331d0f1c2f
focus: "#38 Angular puis #39 Svelte — la grappe #54 close ; trois défauts trouvés en chemin, aucun dans le travail demandé"
---

# Session retro — 2026-08-28 — 52b97e15

## Focus

Livrer les deux dernières liaisons front (#38 Angular, #39 Svelte), qui ferment la grappe #54 :
les quatre fronts ont désormais la MÊME surface de temps réel. Puis corriger `format:scaffold`,
puis l'intégration continue.

## Fait

- **#38** — `nodefony/angular` : `provideNodefony()` + 12 fonctions `inject*` rendant des `Signal`.
  Zéro décorateur Angular publié. Banc sur une vraie `ApplicationRef`.
- **#39** — `nodefony/svelte` : `configureNodefony()` + 12 liaisons `.current` via
  `createSubscriber`. Zéro rune publiée. Banc sur de vraies fixtures `.svelte` compilées.
- **#54** fermé (parapluie, sept sous-tickets soldés).
- `format:scaffold` : deux gabarits rendus conformes à leur propre formateur.
- **Intégration continue réparée** — elle était rouge depuis la veille, sur six plateformes.

## Coûts évidents

- **Trois passes complètes de `test:all`** (~4 min chacune), dont une pour rien : la première a
  rendu 643 échecs parce que le serveur de dev n'écoutait pas. Le décor se vérifie AVANT la passe,
  pas après (`lsof` sur 5151/5152 coûte une seconde).
- **Un `npm run build` lancé pendant une suite en arrière-plan** → deux rouges qui accusaient mon
  diff, et une enquête pour rien. Le script commence par `rimraf dist`.
- **Une reproduction d'échec CI qui a été verte** à cause d'un binaire global — j'ai failli
  conclure que la forge se trompait.
- Deux bancs devkit rejoués en variante (Angular, Svelte) via une copie temporaire du script :
  le banc n'accepte pas de paramètre de front, et son témoin est en dur (`vue`).

## Frictions du jour (versées dans RETEX.md)

- L'environnement du développeur masque le défaut — ici un binaire **global** invisible.
- Un gate neuf doit être vu **vert** une fois, pas seulement rouge : sinon on ne distingue pas
  « il mord » de « il ne peut pas s'exécuter ».
- J'ai écrit un test **complaisant** (il levait lui-même l'erreur attendue).
- Un build destructif concurrent d'une suite fait accuser le code.
- J'ai annoncé au user un défaut **déduit** et non mesuré — il a agi dessus.

## Recommandations

1. **Vérifier le décor avant `test:all`** — un `lsof` sur 5151/5152. Les bancs `(requires server)`
   représentent 643 cas : leur échec en masse est un signe de décor, jamais de régression.
2. **Ne jamais lancer un build pendant une tâche de fond** qui exécute du code du dépôt.
3. **Le banc devkit gagnerait un paramètre de front** (`--frontend <fw>`) : son témoin est en dur,
   et les quatre fronts ont maintenant chacun une liaison à éprouver.

## Patterns récurrents (déjà gérés)

- ✅ Débrancher chaque garde neuve pour la voir rouge — fait 8 fois cette session.
- ✅ La commande du dépôt est l'autorité — mon `oxlint` manuel accusait un lint que le banc
  déclarait vert ; c'était mon instrument qui était mal calibré.
- ✅ Le cwd dérive — deux fois rattrapé par le garde-fou du harnais.

## Commits produits

| Commit     | Sujet                                                                            |
| ---------- | -------------------------------------------------------------------------------- |
| `98275a58` | feat(client): ajouter les services Angular du temps réel                         |
| `f220f948` | fix(scaffold): rendre conforme au formateur le code que deux gabarits produisent |
| `e34aa265` | feat(client): ajouter les liaisons Svelte 5 du temps réel                        |
| `9c5c6b70` | fix(ci): appeler le binaire du dépôt par son chemin, jamais par son nom          |
