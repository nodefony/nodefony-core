---
date: 2026-06-11
session_id: a7ebf767-e71d-4d26-a2e6-d6ff01194de8
focus: VAGUE 4 cycle requête (souverain stateless + @Scope singleton + ResourceController) + revue faisabilité/complexité API souveraine
---

# Session retro — 2026-06-11 — a7ebf767

## Fait

- **V4 ENTIÈRE** en 1 session, ordre voulu 4.1 → 4.3 → 4.2 (le ResourceController naît
  singleton → 1 seule passe E2E pour les 2 lots).
- E2E anti-data-race : 8 req concurrentes sur l'instance partagée → body.requestId ≡
  header X-Request-Id, 8 ids uniques, 1 instance. Même action en REST + WS invoke.
- A/B 3 paires : singleton ≈ per-request (bruit) — verdict honnête publié au plan
  (vague archi/sécu, le ROI RPS = fast path P2/P3b backlog).
- Dette Ph.1 corrigée au passage : garde-fou `instanceof` pointeur container (2ᵉ invoke
  WS même socket cassait) + pont invoke déballe les thenables.
- Clôture : revue état POC souverain + **garde-fou DX gravé** (directive user : « un
  framework trop compliqué = une galère ») → mémoire `project_api_souveraine_poc`.

## Frictions (→ RETEX.md, incrémentées)

- `cd X && …` cwd piégé [5×] (git add depuis framework → pathspec) ; Edit-exige-Read [2×] ;
  commitlint subject-case rejette un nom de classe en tête [1×] ; pre-push typecheck
  attrape TS4114 invisible au build rollup [1×] ; A/B paire aberrante à écarter+refaire [2×].

## Recommandations

1. Avant push de toute redéclaration de membre : `npm run typecheck` (build vert ≠ typecheck vert).
2. Banc A/B : toujours 3 paires, alterner l'ordre, écarter+refaire une paire incohérente.
3. POC souverain : la phase 3 (data plane Studio = vrai consommateur) tranche la DX —
   prioriser la lib client `socket.request()` avant d'exposer le pont aux users.

## Commits produits

| Commit    | Sujet                                                                                      |
| --------- | ------------------------------------------------------------------------------------------ |
| `6905ec3` | feat(framework,http,core): controller stateless-ready — context via ALS + accessors (V4.1) |
| `18b6e72` | feat(framework): @Scope("singleton") opt-in — controllers stateless partagés (V4.3)        |
| `b5cbfa6` | feat(framework,test): controller de ressource souverain stateless — POC Phase 2 (V4.2)     |
| `1d8a0cc` | docs(migration,framework): vague 4 souverain stateless livrée                              |
| `3766182` | fix(framework): modificateur override sur le statique scope de ResourceController          |
