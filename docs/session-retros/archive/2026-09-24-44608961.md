---
date: 2026-09-24
session_id: 44608961-c20e-46c9-9c38-595318201ccb
focus: #467 — MongoDB refuse la suppression d'un parent encore référencé ; sonde Windows #479
---

# Retex 2026-09-24 — 44608961

- **Fait** : `f99f267b0` feat(mongoose) — garde d'intégrité à la suppression dans
  `MongooseRepository` (index inverse au `connect()`, refus 409 / remise à `null`, auto-référence,
  alerte d'index), `ReferencedEntityError` (orm-core) projetée en 409 par le rendu HTTP, test e2e
  généré, défaut `refs` mort du générateur corrigé. #467 FERMÉ. #479 commenté : 3 passes CI sans
  gel, sonde muette sur 39 étapes.
- **Frictions** : comparaison à Prisma d'abord faite de mémoire (le user a relancé) ; deux `cd`
  relatifs bloqués par le crochet ; deux tickets de suite proposés puis refusés par le user.
  (versées au sas sous 🎯 et 🧵)
- **Décisions** : garde dans le repository et non dans le code généré (une règle, une
  implémentation, couvre le code écrit à la main) ; politique déduite de `required` = défauts de
  Prisma ; 409 reconnu par la FORME de l'erreur (http n'importe pas orm-core) ; pas d'option
  `onDelete` (aucune demande) ; le 500 SQL laissé non tracé, à la demande.
