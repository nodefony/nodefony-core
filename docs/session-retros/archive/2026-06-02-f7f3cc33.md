---
date: 2026-06-02
session_id: f7f3cc33
focus: Idée (a) « boot de rêve » — Vite dans la checklist de boot dev (pont events Kernel)
---

# Session retro — 2026-06-02 — f7f3cc33

## Focus

Idée (a) du backlog P11.6 : afficher la compilation Vite dans la checklist de boot dev,
alors qu'elle vit HORS du cycle Kernel (spawn async qui finit après `onPostReady`).

## Fait

- **Pont events Kernel** (`FrontendService`, dev-only) : `kernel.fire("onFrontendStart")`
  SYNCHRONE dans le handler `onServersReady` (avant `await startDev()` → garanti avant
  `onPostReady`) + `kernel.fire("onFrontendReady", {bundles,names,ready})` en `finally`
  (débloque toujours, succès/échec).
- **`BootReporter`** : phase dynamique « Frontend (Vite) » ; à `onPostReady` si Vite compile
  encore → DIFFÈRE le `✓ Prêt` (fige le spinner en statique, referme la ligne, rend le sink
  pour laisser défiler les bannières serveurs) ; à `onFrontendReady` → fige
  `✓ Frontend (Vite) — N bundles servis (…)` puis imprime `✓ Prêt`. Sans frontend = inchangé.
- Doc : `MEMORY.md` @nodefony/frontend (contrat payload pont) + MIGRATION_STATUS P11.6
  (idée (a) → LIVRÉ ; reste backlog (b) help modules, (c) REPL).
- **Validé runtime non-TTY** : 5 bundles servis (react/vue/angular/mediasoup/studio) 5.07s,
  Prêt 7.58s, ordre correct, serveurs UP avant Vite.

## Frictions (→ RETEX.md)

- commitlint `subject-case` : `feat(dev): Vite …` rejeté (majuscule initiale) → `affiche Vite …`.
  **2×** maintenant (déjà vu 2026-06-01 avec `ADR`). Attention noms propres en tête de sujet.
- Test `BootReporter` sans TTY = lancer le DevSupervisor (`npx nodefony development`), pas
  `start.sh` (qui ne l'instancie pas). Mode statique prouve l'ORDRE ; rendu animé = au user.
- `npm exec nodefony development` avale le SIGINT → orphelins Vite. Arrêt fiable d'un lancement
  background = `pkill -INT -f NODEFONY_DEV_CHILD` + `pkill -f nodefony-vite` + kill ports.

## Coûts évidents

- 1 boot dev complet lancé pour preuve runtime (build one-shot + 5 Vite ~7.5s). Justifié :
  seul moyen de valider l'ordre sans TTY. Arrêt manuel laborieux (SIGINT avalé par npm exec).
- Aucun restart serveur (1 seul boot). Build core+frontend turbo unique. Gate TS verte 1er coup.

## Commits produits

| Commit    | Sujet                                                               |
| --------- | ------------------------------------------------------------------- |
| `78ad4b0` | feat(dev): affiche Vite dans la checklist de boot (idée a) — poussé |
