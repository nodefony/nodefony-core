---
date: 2026-09-24
session_id: bed0b3ed-7567-43bc-8505-692e8ec2935a
focus: "#466 fermé, plan du CONSOLIDATE soldé, reprise et clôture de session en deux scripts"
---

# Retex 2026-09-24 — bed0b3ed

- **Fait** : `96f0488d` filtres `uuid`/`objectId` (400 à l'entrée) · `79bf5e1c` 404 d'un identifiant
  mal formé dans le chemin · #466 FERMÉ (banc de vérité vert PostgreSQL 80 et MongoDB 69 tests HTTP)
  · `63ae146a` compte qualifié sur la carte de connecteur (helper `entityCount` + garde statique)
  · `ea0a3db9` verrou « une passe `test:all` occupe l'arbre » (pre-commit + hook d'édition, trois
  filets anti-blocage) · `13f40299` `session:resume` / `session:end` (+ `--verify`),
  `ticket-verify --dependents` · #472 ouvert (trois suites instables), #427 élargi à Windows,
  #467 ordonné (4.6).
- **Frictions** :
  - Premier banc PostgreSQL rouge sur un historique de migrations laissé par un run précédent —
    décor sale ([[feedback_stale_decor_poisons_verdicts]]) ; vidé, vert.
  - En durcissant le verrou (date invalide = ignoré), deux de MES tests passaient pour une mauvaise
    raison (`since: "y"`) — vu en débranchant ([[feedback_test_discriminant_or_dead]]).
  - `_2026-09-24b` contient `_2026-09-24` : mon script prenait la session d'avant pour la nôtre
    ([[feedback_anchor_expires_silently]]) ; la règle est devenue positionnelle.
  - Deux refus du classifieur sur les barrières de l'agent → versé au sas (thème 🛡️).
- **Décisions** : un identifiant mal formé = 404 s'il vient du CHEMIN, 400 sinon (le chemin désigne
  une ressource) ; le hook d'édition est gardé (il ferme le geste qui a coûté deux campagnes, coût
  hors passe = un test d'existence) ; un contrôle de session neuf s'ajoute au SCRIPT, pas à la prose.
