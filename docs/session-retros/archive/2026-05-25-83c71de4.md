---
date: 2026-05-25
session_id: 83c71de4-5cf8-40c0-97a2-010fdbe689e1
focus: fix workflow reprise (garde-fou git↔_state) + drill-down worker cluster backend (étapes 1-3)
---

# Session retro — 2026-05-25 — 83c71de4

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    55 |
| Edit  |    41 |
| Read  |    28 |
| Write |     7 |
| Skill |     2 |

## Top fichiers Read/Edit

| Fichier                                                    |   × |
| ---------------------------------------------------------- | --: |
| framework/ClusterProbeClient.ts                            |   5 |
| core/ClusterProbeAggregator.ts                             |   5 |
| memory/MEMORY.md                                           |   4 |
| core/ClusterProbeAggregator.test.ts                        |   3 |
| memory/\_state + kit cluster                               | 3+3 |
| studio/clusterSupervision.ts + StudioRealtimeController.ts | 2+2 |

## Coûts évidents

- **Session très longue : 144 tours, 1 transcript de 2,1 MB.** 5 blocs enchaînés SANS `/clear`
  (fix workflow → étape 1 core → étape 2 framework → étape 3 studio → revue archi). Le contexte
  accumulé est porté tour après tour → cache-read 33,3 M tokens.
- **Le skill `nodefony-framework-dev` (très volumineux) chargé tôt reste dans le cache tout du long.**
- 1 serveur dev lancé pour le memory.test (nécessaire — la suite « requires server » tape un serveur externe).

## 💶 Coût (€)

- Total **~€74,9 (≈ $81,4)** sur 144 tours. Décompo : cache-read 33,3 M (~~€46) + cache-write 1h 570 k
  (~~€16) = **~82 % cache** ; output 182 k (~€13) = 17 % ; input négligeable. **Cache-dominé** = c'est le
  CONTEXTE porté (CLAUDE.md + gros skill + MEMORY.md × 144 tours), pas ce que j'ai produit.
- Coût/livrable : 4 commits backend + 1 fix workflow → ~€15/livrable. Acceptable mais le levier #1 = découper.

## Recommandations

1. **Découper en sessions courtes + `/clear`** — cette session = 5 blocs (workflow + 3 étapes + revue).
   Chaque étape committée était un point de `/clear` naturel ; ne pas les avoir enchaînés a triplé le cache.
2. **Skill `nodefony-framework-dev` = très gros** : une fois les recettes intégrées, il reste en cache.
   Sur une session longue mono-sujet, son poids domine → envisager de le charger juste pour la phase qui en a besoin.
3. Rien à ajouter à l'allowlist (git/npx/npm/jq/grep n'ont pas prompté de façon récurrente cette session).

## Patterns récurrents (déjà gérés cette session)

- ✅ **Garde-fou git↔`_state`** créé ICI (cause racine : END au milieu d'une session → `_state` périmé).
- ✅ memory.test flake « 1000 GET » rejoué isolé → confirmé flaky, pas une fuite (règle appliquée).
- ✅ Erreurs typecheck pré-existantes prouvées via `git stash` + tsc (#log core, `detail` non typé) → notées/fixées.
- ✅ Commit par étape verte = mémoire de reprise fiable (la règle « commit obligatoire, close optionnel »).

## Commits produits

| Commit  | Sujet                                                                      |
| ------- | -------------------------------------------------------------------------- |
| 0ef34b1 | fix(skill) garde-fou git↔_state dans nodefony-session (RESUME)             |
| 1caa45b | feat(cluster) fondations IPC drill-down (étape 1 core)                     |
| 4f70186 | feat(cluster) worker toggle sonde riche + ordre enrich (étape 2 framework) |
| a070b43 | feat(studio) canal dashboard:supervision@<pid> (étape 3 back)              |

## Revue archi (bonus) — 2 caveats capturés en mémoire

- Asymétrie : backplane interchangeable = realtime ; sondes + drill = master-gateway IPC **intra-pod**
  (Redis multi-pod → drill cross-pod à porter). `[[project_cluster_drilldown_kit]]`.
- `requestId` absent du syslog (Pdu.pid OK) → feature core séparée `[[project_syslog_requestid_correlation]]`.
