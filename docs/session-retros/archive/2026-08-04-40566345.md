---
date: 2026-08-04
session_id: 40566345-6148-4f48-a0a6-5973899c703a
focus: le corpus doc recalé sur le code, les dépendances remises à niveau, puis deux audits du devkit
---

# Session retro — 2026-08-04 — 40566345

## Focus

Solder le reliquat doc traîné depuis 08-03b, puis les dépendances, puis auditer le devkit et son
banc. Rien des audits n'a été appliqué — le user a demandé de les restituer d'abord.

## Fait

- **Corpus doc** : 531 ancres `fichier:ligne` décrochées sur ~50 pages → 158 (le reste = littéraux
  entre backticks, non résolubles par construction). 3 symboles fantômes fermés, dont un renommage
  de juillet jamais répercuté. `MEMORY.md` de drizzle/mongoose recalés (comptes de tests faux,
  `.mocharc` disparu, 4 stores mongoose non décrits). Page `webhooks.md` alignée sur le tri, le
  filtre `failing` et les compteurs qui suivent la recherche.
- **Dépendances** : jose 6.2.8 (deux durcissements crypto), ws 8.21.2, rolldown 1.2.2, tsx 4.23.5,
  oxlint 1.77.0 · `npm audit fix` → **0 vulnérabilité** (brace-expansion, fast-uri, toutes deux
  dans la chaîne de build).
- **Dependabot recadré** : il ciblait `main`, à 238 commits de la branche de travail →
  `target-branch: claude-ts` sur les deux écosystèmes. Les 4 PR mortes fermées avec leur motif.
- **`main` mis à jour** par merge de `claude-ts` (les actions `@v7` de main préservées, vérifié).
- **Deux audits `fable`** (couverture du devkit · efficacité du banc) et **un run du banc de schéma
  avec `vibe`** sur umami.

## Frictions (le détail est dans RETEX.md, par thème)

- Transposer les drapeaux d'un CLI agentique sur un autre : faux verdict complet, transcript vide.
- Une garde écrite pour UN format d'agent ne protège que celui-là.
- Un compteur ne voit que là où il regarde (`entity/` vs `entities/`).
- Un correctif automatique de doc se trompe une fois sur deux sur les gros écarts.
- Un renommage laisse dans la doc le cadrage faux qui avait causé le bug.

## Ce que les audits ont établi (non appliqué)

- Le banc **reproduit son propre mode de défaillance nº 1** : 8 tâches jugent sur une mention dans
  le transcript, alors que le transcript porte le contenu des fichiers LUS. T8, « le pari central
  du devkit », est falsifiable par simple lecture.
- **T10 (0/3) n'est pas instruit** : son gate exige `/remise|discount/` dans le nom du service.
  Je l'avais présenté comme le seul défaut dur du produit — c'est peut-être l'instrument.
- `AGENTS.md.tpl` fait **570 lignes** ; l'agent en lit ~100. La table « tâche → doc » est à la
  ligne 269 : toute la couche d'adressage est hors de portée. `vibe` l'a démontré en inventant
  `nodefony/entities/` au pluriel sans jamais rencontrer un générateur.

## Non lancé / à savoir

- **Rien des audits n'est appliqué** (demande explicite).
- Banc de schéma : **umami seul**, 1 run. calcom et ghost arrêtés en cours — inutile de payer deux
  runs de plus sur un compteur dont on venait de voir qu'il ne compte pas ce qu'il croit.
- `--load` et `--dialects` non joués. Les 3 rouges de `test:all` (completion, devProcess ×2) sont
  des timeouts de charge : 83/83 verts en isolation.
- Conteneurs PostgreSQL/MariaDB/Mongo/Redis laissés démarrés.

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `0cf50a1e` | 373 ancres recalées + le renommage jamais répercuté           |
| `cba788e8` | MEMORY drizzle/mongoose cessent d'annoncer des comptes faux   |
| `c7af0b59` | la page webhooks ignorait trois capacités déjà livrées        |
| `d65384ba` | 5 correctifs de dépendances, deux vulnérabilités HIGH fermées |
| `19c7cd24` | Dependabot surveillait une branche que personne ne développe  |
| `0eafafe4` | merge dans `main`                                             |
