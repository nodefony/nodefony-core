---
date: 2026-06-13
session_id: a5d6404b-9bed-40d5-b9bd-ff0e02c945d1
focus: P6 realtime — série « socket isomorphe » L0→L4 + banc d'intégration béton
---

# Session retro — 2026-06-13 — a5d6404b

## Focus

Finir la série « socket Nodefony » : L0 (client compose `JsonRpcPeer`) puis L1/L3/L4,
avec une exigence forte du user — un **banc d'intégration béton** (les unit à stubs ne
prouvent pas la refacto interne).

## Fait (commité + poussé)

- **L0 `d0934fa0`** : `RealtimeClient` compose `JsonRpcPeer` (délégation totale du plan de
  contrôle), `implements IRealtimePeer`, `register()` client ⇒ duplex S→C. −181/+163.
- **L1 `9a51438f`** : `RealtimeController.requestClient`/`notifyClient` (duplex S→C par connexion).
- **L3 `40684721`** : `RealtimeController<Emit, Actions>` générique + `requestClient`/`notifyClient`
  typés → contrat de canaux typé partagé client↔serveur. Test de typage compile-only.
- **L4 `f428d2cd`** : `ServerRealtimeSocket implements IRealtimeSocket` au-dessus du hub
  (un service back tient UN handle). `request` non supporté (pas de pair unique → L1).
- **docs `7db0b5f0`** : MIGRATION_STATUS (L0-L4 ✅, NEXT J4) + MEMORY realtime.
- **Banc loopback** `realtimeLoopback.e2e.test.ts` : 7 → **26 scénarios** VRAI client ↔ VRAI serveur.
- L2 déjà livré la session précédente (`c7d86c2c`). L5 (mutations) = post-P6.

## Frictions / leçons (→ RETEX.md)

- **Refacto interne = les unit ne prouvent rien** : contrat de surface inchangé → unit verts pour
  rien. Banc d'intégration de la JONCTION obligatoire ; TDD = le test duplex échoue avant L0, passe après.
- **Double identité source/dist** d'une classe (`RpcError`) dans un test isomorphe : handler serveur
  throw le `RpcError` du dist, assertions client gardent celui du source.
- **Génériques à défauts permissifs** pour étendre une classe héritée sans casse + TS6133 sur type
  param inutilisé (`Listen` retiré).
- **Baseline stash** a prouvé que l'unique « error » du core (`Cli.test.ts` signaux) est pré-existante.

## Coûts évidents

- 0 restart serveur (tout testé en source/loopback, pas de runtime). 1 rebuild core (dist client après L0).
- Plusieurs allers-retours typecheck sur les génériques (TS6133, signatures conditionnelles) — attendu
  pour un refactor de typage.

## Gates

memory WS 9/9 · typecheck core+client+realtime · realtime 210 (+9 skip docker) · core 1598 · loopback 26.

## Commits produits

| Commit     | Sujet                                                      |
| ---------- | ---------------------------------------------------------- |
| `d0934fa0` | feat(realtime): l0 — client compose jsonrpcpeer + loopback |
| `9a51438f` | feat(realtime): l1 — duplex serveur→client                 |
| `40684721` | feat(realtime): l3 — contrat de canaux typé partagé        |
| `f428d2cd` | feat(realtime): l4 — façade serveur socket                 |
| `7db0b5f0` | docs(migration,realtime): série L0→L4                      |

## ➡️ Prochaine étape

J4 JWT (jose, révocation forte WS) + RBAC P6.8 (granularité rôle/canal — seam `setFrameAuthorizer` prêt).
