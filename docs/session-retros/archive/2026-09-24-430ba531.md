---
date: 2026-09-24
session_id: 430ba531-df18-4275-9ee9-43161c28462d
focus: #238 parité de contrat Mongoose ↔ Drizzle — contrats extraits, trous fermés, audit de doc
---

# Retex 2026-09-24 — 430ba531

- **Fait** : #238 FERMÉ. Contrats webhook/webauthn/token/repository/totp extraits chez leur
  propriétaire et rejoués sur mémoire, Drizzle ×3, MongoDB, Redis (4507756de bc1734c6f 6018a0b72
  a72cf852f 6f73168fd 88ece7f87 fb9e9f2b6). Cérémonie WebAuthn e2e (09e35937c), index composite
  d'audit Mongo (19ba16a0f), page de parité (8e4d8fcb2), audit de fond du corpus doc en deux lots
  (1ad724ba1 1a6f69314 2c390deb2). `--mongo`, `--dialects`, `outage-real` : verts.
- **Frictions** : (1) `cat >` a écrasé un test existant non lu (feedback_edit_requires_read_tool,
  gradué — le geste n'a pas été fait) ; (2) le hook de build auto efface le dist du cœur pendant un
  run vitest → « no tests » deux fois ; (3) un `grep` sur la sortie de commit a masqué un refus
  commitlint (en-tête trop long) ; (4) paquets d'audit `haiku` minces ; (5) `anchor-check` trop
  tolérant. (3)-(5) versés au sas sous 🎯.
- **Décisions** : Mongo aligné sur SQL pour `$ne`/`$nin` (le contrat dit « identique ») ;
  `describeEntity` en vocabulaire du contrat ; `IEntity.indexes` optionnel (modèle `timestamps`) ;
  seuil Redis par EVAL (clé inchangée, pas de migration) ; capacités de banc déclarées
  (`savepoints`, `nativeTtl`, `clockDrivenExpiry`, `newStore`) plutôt que des sauts muets.
