---
date: 2026-07-05
session_id: 3b112380-8be4-42ec-af37-b3d236831bc0
focus: Lot 5 (config) — getter Module.config unifié + fixes config app + câblage realtime (scope étendu par le user)
---

# Session retro — 2026-07-05 — 3b112380

## Fait

- **Commit `233d33e2`** `feat(config): getter Module.config uniforme + fixes config app + câblage realtime enabled` (23 fichiers).
- **Getter `Module.config`** : core `Module<TConfig>` générique + getter `config` (= `this.options`, vue typée 0-alloc). Adopté par 8 modules (`extends Module<IXConfig>`).
- **Container keys SUPPRIMÉS** (drizzle/mongoose/redis/realtime) : services lisent `this.module.config`. Zéro doublon (code + tests).
- **Bugs config app** : `rejectUnauthorized`→`https` (TLS jamais forcé en prod + TS2353), `formidable`→`upload` (clé morte busboy), HttpAdminApi `handler`→`store`.
- **Câblage realtime** : `enabled:false` inerte + `cluster.probe.enabled` effectif (sonde différée à onKernelBoot).
- **Tests** : getter Module.config (core) + realtime enabled:false ; mocks migrés `config` direct.
- **Audit config exhaustif** (6 sous-agents, 9 modules) : peu de champs morts, MAIS vraie dette = reliques doc `schema.ts`/`defineXConfig.ts` (~8 modules) + exemples security périmés.

## Frictions (→ RETEX.md détaillé)

- **Doublon d'accès config** : getter ajouté SANS retirer les container keys → 3 façons ; user a poussé 3×. Le raccourci « mock délègue au container » a ré-introduit le doublon dans les tests. Règle : unifier = migrer TOUT.
- **Grep tous les mocks avant de retirer un mécanisme** : le mock e2e n'a cassé qu'au run (7 fails). « Fais tous les tests » (user) = justifié.
- **Boot TIMEOUT ≠ crash** après rebuild core (turbo) — `framework ready` ~4s après le TIMEOUT `start.sh`.
- **Fan-out sous-agents** avec gate par unité (audit + édition + refacto test) = efficace, contexte mince ; le sous-agent security a refusé d'appliquer l'exception archi (bien).

## Décisions

- **1 seule source de config** : `this.options` (stockage) exposé via `this.config` (vue typée). Plus de container key. security laissé NON typé (validation décentralisée 8× → follow-up centralisation).
- **Scope étendu par le user** en cours de lot 5 : l'unification config-getter n'était PAS dans le plan lot 5 initial → a consommé toute la session. Le reste du lot 5 (Batch 2 champs morts, Batch 3 doc/persistence/skeleton) NON commencé.

## Gates

~5000 tests verts (core 1796 · http 536+565 int+9 mem+27 load · framework 429 · security 787 · realtime 314 · drizzle 117 · mongoose 84 · user 145 · orm-core 75 · doc 32 · frontend 35) · boot réel `framework ready` · gate mémoire 9/9 · banc charge 27/29.

## Commits produits

| Commit     | Sujet                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------- |
| `233d33e2` | feat(config): getter Module.config uniforme + fixes config app + câblage realtime enabled |
