---
date: 2026-08-27
session_id: 2c7e9ddb-ac1c-4ddf-a9f6-f668df598ce4
focus: le pilotage quitte le fichier écrit à la main pour les tickets — et l'outillage suit
---

# Session retro — 2026-08-27 — 2c7e9ddb

## Focus

Un ticket d'arbitrage resté ouvert alors que ses décisions étaient rendues a ouvert la vraie
question : **deux artefacts tenaient le même rôle** — `MIGRATION_STATUS.md` écrit à la main et les
tickets GitHub. La session a tranché le partage, construit le filet hors ligne, dégraissé le
fichier, puis réparé ce que le dégraissage cassait en silence.

## Fait

- **#55 créé et fermé dans la foulée** — le plan de publication se corrigeait par un bandeau posé
  au-dessus du tableau au lieu de corriger ses cellules ; trois lignes du §8 étaient fausses.
- **#32 fermé** : les cinq arbitrages étaient rendus depuis le 27 au matin, dans ses commentaires.
- **Empreinte du tableau de bord** (`.ai/BOARD.md` + `.ai/board.json`), générée, jamais éditée,
  commitée — la réponse au « et si GitHub ne répond pas ». Trois conduites vues à l'œuvre.
- **`MIGRATION_STATUS.md` : 876 → 755 lignes**, 113 tâches faites parties en archive, 0 ligne de
  tableau introuvable (contrôle mécanique).
- **Trois scripts rendus à leur skill** ; `npm run board:snapshot` câblé.
- **Deux règles gravées** dans `nodefony-ticket` v1.2.0 : la devise appliquée aux tickets, et le
  contexte chaud comme critère de priorité.
- **`nodefony-migration-audit` réparé et symbionisé** : il n'est plus le pilotage, il est
  l'instrument de vérité — et son comptage rendait `✅=0` depuis le dégraissage.

## Coûts évidents

- **Trois délégations** (1 `fable` d'inventaire, 3 `haiku` de confrontation) — le bon geste : elles
  ont lu 907 lignes + le code sans que rien n'entre dans le contexte principal. Mais **4 verdicts
  « corrigée » recontrôlés ont livré 1 faux** : le recontrôle n'est pas optionnel.
- **Deux allers-retours évitables sur mon propre code** : `--check` qui comparait deux objets
  différents, puis un comptage `awk` qui doublait les lignes. Écrire l'instrument et le croire
  dans le même geste coûte deux fois.

## Recommandations

1. `nodefony-ticket` porte désormais « quand prendre un ticket » — vérifier à la prochaine session
   qu'il se déclenche sur « quel ticket prendre maintenant ? » (2 cas au banc, 72/72).
2. Le relevé des 48 cases de feuille de route est versé dans #56 **avec son défaut annoncé** : ne
   pas le réutiliser sans reposer la question « TOUT le travail est-il là ? ».
3. Ne plus jamais retirer d'une source sans passer `rg --hidden` sur son nom d'abord.

## Patterns récurrents (déjà gérés)

- ✅ Redemander la même donnée par une AUTRE voie devant une valeur surprenante (déjà appris sur le
  champ `title` figé la veille — a resservi le jour même sur `item-list`).
- ✅ Voir le gate mordre avant de le croire (barrière d'ancres étendue à `docs/release`, banc de
  déclenchement vu à 71/72).

## Commits produits

| Commit     | Sujet                                                           |
| ---------- | --------------------------------------------------------------- |
| `7bddeea5` | recaler le plan de publication sur ce qui est livré (#55)       |
| `3c7dc453` | la devise vaut pour les tickets, et le contexte chaud dit QUAND |
| `881f5ab9` | projeter les tickets dans le dépôt, pour reprendre hors ligne   |
| `d8311321` | rendre à leur skill les scripts qui exigent un protocole        |
| `61bc679a` | lire le tableau de bord par l'API, le client en ligne OMET      |
| `608203e5` | câbler l'empreinte du tableau de bord à npm                     |
| `2f0e2c2b` | sortir l'histoire du tableau de bord, garder ce qui reste dû    |
| `fc7cd53d` | rafraîchir l'empreinte du tableau de bord                       |
| `2ddf09f1` | symbioniser l'outillage avec la nouvelle méthode                |
