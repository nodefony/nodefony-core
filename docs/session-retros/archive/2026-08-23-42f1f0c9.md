---
date: 2026-08-23
session_id: 42f1f0c9-3ac8-4b2e-a9ca-40a988995a5d
focus: la CI rougissait pour une bonne raison, et le banc mesurait la mauvaise application
---

# Session retro — 2026-08-23 — 42f1f0c9

## Focus

Solder les points restés hors preuve (CI, `test:all`, gate mémoire), trancher la contradiction MCP
du 08-22h, réparer la sonde de la tâche 9 — puis mettre Codex et Gemini au banc.

## Fait

- **CI réparée** (`999aa9a9`). Le gate de couverture mordait sur « internal 499 » : le journal de la
  forge (`$GITHUB_WORKSPACE/nodefony-server.log`) n'était dans aucun candidat. Preuve prise sur
  l'artefact TÉLÉVERSÉ par la forge : marqueur présent, 43 lignes `GET 499 …/abort/wait`. Le kernel
  posait bien son 499 ; seule la découverte manquait. **CI verte**, le cas s'exécute pour la
  première fois (658 passés / 9 sautés, contre 656 / 11).
- **Contradiction MCP tranchée** (`535af898`). Les 8 appels du run « eteint » sont RÉELS et RÉUSSIS :
  la tâche 9 démarre l'application par sa prémisse. Le régime ne sépare pas l'allumage mais
  l'IDENTITÉ (anonyme vs jeton). Le banc CONSTATE désormais l'état de la porte pour tout régime,
  après la prémisse et avant l'agent.
- **Gate des routes réécrit** (`7671378d`, `lib/gate-routes-count.mjs`). Il opposait deux
  applications ; il interroge maintenant celle que l'agent a interrogée, et NOMME sa source.
- **Trois grammaires d'agent** (`3e267fee`). Compteur MCP, garde « a parlé » et compteurs d'effort
  lisent Claude, Codex et Gemini. Formes prises au SOURCE, puis vérifiées en lançant les agents.
- **Cible prouvée + arrêt garanti** (`120ba739`). `portDeLAppSousTest` dans le socle partagé ;
  arrêt armé sur `process.on("exit")` et les signaux.
- **Soldé** : `test:all` (8674 passés, 0 échec, 90 sautés), gate mémoire (9/9), CI observée.
- **Codex au banc** : 10 sondes vertes sur 11, 27 commandes, **0 appel MCP** alors qu'il VOYAIT la
  porte (`codex mcp list` → `enabled`, `Bearer token`).

- **Trois agents au banc, et un quatrième dialecte** (`a4b581a3`, `d63c6c4d`). Codex et Gemini ont
  tourné pour la première fois. Gemini par CLÉ D'API (l'OAuth reste refusé) : FAIL puis, la porte
  enfin déclarée, **PASS 11/11**. Antigravity (`agy`) : grammaire câblée, authentification bloquée
  par un choix de produit à prendre à froid.
- 🔴 **Un agent à portée PROJET n'était JAMAIS déclaré** : le banc décidait sur `foyer ? AGENT :
"none"`, condition qui confondait « foyer déportable » et « faut-il appeler sa CLI ». Gemini a
  joué une tâche entière sans outils, et son « 0 appel MCP » se lisait comme un choix — le piège
  que ce banc DOCUMENTE. Racine : une règle en deux exemplaires (le cœur l'avait, testée).

## Frictions du jour

- **Deux fois j'ai cru un instrument avant de le suspecter** : « jobs Windows pendus depuis 10 h »
  (il était 00h02, le run venait de démarrer — mon erreur d'heure) et « `logs.preuve-bak` a disparu »
  (dérive de `cwd` après un `cd` de tour précédent).
- **Une enquête sans fin sur une cause non reproductible** (l'écart de 2 routes) alors que le
  correctif n'en dépendait pas. Savoir arrêter de chercher la cause quand le geste juste est déjà
  établi.
- **Mon propre selftest a été faux avant d'être juste** : `spawnSync` étranglait la boucle
  d'événements de sa porte factice ; une assertion cherchait `FROID` contre un message qui écrit
  « kernel froid ».

## Non lancé / hors preuve

- **`agy` (Antigravity) n'est PAS câblé** : tout est établi (binaire, drapeaux, grammaire, `agy mcp
add`, portée globale respectant `HOME`), rien n'est écrit dans la table du cœur ni dans le banc.
- **Gemini CLI inéligible** (free-tier individuel, refus serveur) — non mesurable ici sans
  `GEMINI_API_KEY` ou Vertex AI. La clé annoncée par le user n'est visible ni dans l'environnement,
  ni dans les profils shell, ni dans `~/.gemini/.env`.
- **CI rouge non traitée** : `@nodefony/security` → `bearer.test.ts` « reste LINÉAIRE » (×3.32 > 3),
  macOS Node 26 seulement. Test à seuil de ratio sur runner partagé, étranger à ce travail.
- Le run Codex mesuré l'a été **avant** les correctifs de cible et d'arrêt : son verdict porte sur
  une application qui n'était pas la sienne. À rejouer.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `999aa9a9` | le journal d'un LANCEUR s'écrit là où il est appelé                    |
| `535af898` | un régime nommé « eteint » annonçait une porte morte                   |
| `7671378d` | le gate des routes opposait DEUX applications                          |
| `3e267fee` | le banc lit les grammaires de CHAQUE agent                             |
| `120ba739` | un port qui répond ne dit pas À QUI — arrêt couvrant les urgences      |
| `a4b581a3` | la grammaire d'Antigravity (`agy`) — et le mur de son authentification |
| `d63c6c4d` | un agent à portée PROJET n'était jamais déclaré                        |
