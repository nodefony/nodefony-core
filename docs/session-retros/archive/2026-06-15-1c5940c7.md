---
date: 2026-06-15
session_id: 1c5940c7-ef11-4b5f-990a-a1146b0e06d4
focus: P6 J9 — OAuth2 social login (arctic) bout-en-bout + refactor controllers
---

# Session retro — 2026-06-15 (soir 2) — 1c5940c7

## Focus

P6 J9 — **OAuth2 social login** (arctic v3.7.0) de bout en bout : provisioning Shadow User,
service + providers, controller BFF, banc E2E réel. Puis rangement des controllers framework.

## Fait (5 + 1 commits, `refactor/p6-security`)

- `41c702f2` feat(user) — `IOAuthUserProvisioner`/`IOAuthProfile` + `UserService.provisionOAuthUser` (JIT, 73 user)
- `6d7f4ff9` feat(security) — `OAuth2Service` arctic lazy + `IOAuthProvider` + registry + helper OIDC + google/keycloak/github (251 security)
- `1e31b646` feat(framework) — `OAuth2Controller` authorize/callback BFF + montage onKernelReady
- `d27bd414` test(security) — banc E2E réel `oauth2-flow` 6/6 + fixture (provider test, fix repo socialProviders)
- `1df19b93` refactor(framework) — 5 controllers concrets `src/` → `nodefony/controller/`
- docs : MIGRATION_STATUS (P6 68 %, J9 OAuth ✅) + MEMORY security + RETEX + mémoire `project_oauth2_social_identity`

## Décisions structurantes

- **Identité OAuth tranchée** : Shadow User PERSISTÉ + JIT (user virtuel écarté — FORCÉ par le re-fetch BFF J3,
  pas un goût) ; `password:null` ; `ROLE_USER` à la création, base=vérité ensuite (OAuth=authn pas authz) ;
  provisioning délégué app (capability, fail-closed) ; zéro liaison-email auto. → `project_oauth2_social_identity`.
- **3 builtins mandatory** google/keycloak/github (user a ajouté keycloak) ; helper OIDC générique → +OIDC = 1 entrée.
- **LDAP** : auto-corrigé — pas planifié ; via Keycloak/OIDC (déjà là). Extensibilité ≠ tâche.

## Frictions / leçons (→ RETEX.md §Conception)

- Lib tierce (arctic) = cerner le périmètre : protocole oui, profil non → helper + registry, pas 50 fichiers.
- Banc E2E réel a révélé 2 bugs cachés par les unit (repo `create` sans socialProviders ; assertion `/me`).
- Test rouge → curl l'état réel AVANT d'accuser le code (c'était l'assertion, pas le code).
- Avant d'arbitrer un design : chercher la contrainte déjà gravée (re-fetch BFF → persisté obligatoire).

## 💶 Coût

- (stats détaillées déléguées à CONSOLIDATE) — session longue, très interactive (user a steeré ~10 questions :
  identité user, drivers, anonymous, rôles, Studio config, LDAP, périmètre arctic, rangement controllers).

## Gates

build 19/19 · typecheck 0 (user/security/framework/test) · user 73 · security 251 · framework 284 ·
http intég 478 · banc oauth2-flow 6/6 · security-review 0 blocker · 0 régression.

## Reste P6

J5 (CSRF/CORS/headers) = DERNIER jalon du cœur ; puis scope élargi Studio sécu (API Keys/Webhooks/Audit).

## Commits produits

| Commit   | Sujet                                                           |
| -------- | --------------------------------------------------------------- |
| 41c702f2 | feat(user) provisioning Shadow User OAuth                       |
| 6d7f4ff9 | feat(security) OAuth2Service + providers google/keycloak/github |
| 1e31b646 | feat(framework) OAuth2Controller BFF                            |
| d27bd414 | test(security) banc E2E oauth2-flow + fixture                   |
| 1df19b93 | refactor(framework) controllers → controller/                   |
