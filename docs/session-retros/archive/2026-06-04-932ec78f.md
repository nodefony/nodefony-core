---
date: 2026-06-04
session_id: 932ec78f-6ea3-4199-acb1-96aee4dcab2f
focus: Chantier config defineConfig — Lots 3+4 + résilience config boot + hygiène tmp
---

# Session retro — 2026-06-04 — 932ec78f

## Focus

Suite du chantier configuration `defineConfig`. Lot 3 (`use()` + registre typé) puis Lot 4
(câblage Kernel boot), enrichi en cours de route par 2 exigences user : **résilience
exceptionnelle au boot** sur la config + **message explicite sur les valeurs par défaut**.
Puis correction d'une pollution `./tmp` signalée par le user (tests d'upload sans cleanup).

## Fait (4 commits, non poussés)

- `6dc306b` **Lot 3** — `use(name, config?, opts?)` + registre augmentable `NodefonyModuleConfig`
  - `ConfigOf<N>` (declaration merging, niveau ③ → typage pilier #1 = 4/4). Câblage Kernel :
    `resolveModuleEntries` porte la config, `loadModulesFromManifest` deep-merge avant validation. 12 tests.
- `60a7929` **Lot 4** — `loadApp` détecte/résout le descripteur (`buildConfigContext` + `resolveAppOptions`,
  symbole survit au spread Service), merge `defaultAppConfig` tous chemins, fallback legacy `validateConfig`. 14 tests.
- `08ad3e5` **résilience config** — `bootConfigError` (diagnostic clair + champ Zod + **défauts explicites**
  - EX_CONFIG=78 + `presented`) ; fix double-log : les 2 catch de `CliKernel.start()` respectent presented/exitCode
    (le chemin server ≠ dispatchModuleCommand). Prouvé en réel (`port="abc"` → diagnostic propre, `terminate:78`).
- `0915764` **hygiène** — `memory.test` nettoie ses uploads (snapshot-diff comme upload.test) ; **1403 `<uuid>.txt`
  purgés** de `./tmp` (légitimes préservés).

## Frictions (→ détaillées dans RETEX.md)

- Résilience boot : blinder la phase config À PART du lifecycle (déjà gardé) ; `presented` respecté par TOUS les catch ;
  serveur ≠ dispatchModuleCommand (2 catch).
- Descripteur brandé par symbole survit au spread `{...options}` (à tester explicitement).
- Pollution `./tmp` : un test d'upload DOIT nettoyer (le serveur ne nettoie qu'en abort).
- `new Kernel` test au tri précoce pollue le singleton (before/after restore) — vu 2×, prouvé par baseline stash.
- `process.env.X = undefined` → string "undefined" (delete si undefined).

## Coûts évidents

- Plusieurs cycles `casser config → rollup root → restart` pour PROUVER le diagnostic en réel (justifié : exigence user
  « montre le diagnostic »), dont 1 re-cycle pour confirmer le fix double-log. Builds core turbo en background (bons).

## Gate

- typecheck source vert · core 1550 · memory 9/9 (seuils tenus) · intégration HTTP 737 · boot dev 4 serveurs HEALTH 200.

## Commits produits

| Commit    | Sujet                                                       |
| --------- | ----------------------------------------------------------- |
| `6dc306b` | feat(config): use() + registre de types par module (Lot 3)  |
| `60a7929` | feat(config): câblage Kernel boot defineConfig (Lot 4)      |
| `08ad3e5` | fix(config): diagnostic boot sans double-log + EX_CONFIG    |
| `0915764` | test(http): memory.test nettoie ses uploads (0 résidu tmp/) |
