---
date: 2026-06-11
session_id: (3e session du jour — fast path)
focus: chantier fast path étapes 1-3 — banc comparatif + banc routing + profil delta + leviers T1/T2/T3
---

# Session retro — 2026-06-11 (3e) — chantier fast path

## Fait

- **Banc comparatif frameworks** (`2432c6f`) : Nodefony 5 264 vs Express 11 740 vs
  Fastify 20 782 vs nu 23 985 RPS — conditions égales (186 routes, route #31, même
  payload). Décomposition : ×2,23 vs Express = **coût/req, PAS le routing**.
- **Banc non-régression routing** (`8b91479`) : 25 invariants `Router.resolve`
  (prérequis index radix). Écart RFC découvert : Allow 405 non agrégé.
- **Profil delta vs Express** (`585cd27`) : CPU/req ×1,57, GC/req ×4,4, leviers
  T1 audit 5,9 % / T3 setTimeout 2,6 % / T4 listeners 1,9 % / T2 syslog 1,7 % ;
  routing scan 0,9 % (confirmé hors de cause).
- **T1** (`fd7107e`) : audit nominal gaté si driver null — **+10,8 % RPS** (seul gain net).
- **T2** (`6ef01ae`) : gate sévérité Syslog à l'entrée — structurel (RPS bruit).
  Seuil posé au Kernel (init() pollué par défaut "production").
- **T3** (`e180faf`) : 1 closure timeout/socket + re-arm conditionnel — structurel.
  Bug attrapé : arm 1×/socket écrasé par node dès la req 2 (120 s vs 30 s).
  Découverte : poste résiduel = node-interne (prix de la feature 408).

## Frictions

- A/B : 2 leviers sur 3 dans le bruit malgré profil positif → prévoir l'argument
  structurel d'emblée (cf RETEX.md « Perf / bancs A/B »).
- Stash/pop + rebuild root ×6 pour les paires old/new T2-T3 (~35 s chacun) —
  le toggle env (T1, NF_BENCH_AUDIT_NOMINAL) est bien plus économe quand possible.
- Edit sans Read préalable re-frappé 2× (audit-logger, AuditLogger.test).

## Commits

| Commit  | Sujet                       |
| ------- | --------------------------- |
| 2432c6f | banc comparatif frameworks  |
| 8b91479 | banc non-régression routing |
| 585cd27 | profil delta vs Express     |
| fd7107e | T1 audit nominal +10,8 %    |
| 6ef01ae | T2 gate sévérité Syslog     |
| e180faf | T3 timeout socket           |
