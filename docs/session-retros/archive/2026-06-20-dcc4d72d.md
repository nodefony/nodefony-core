---
date: 2026-06-20
session_id: dcc4d72d-e2d1-44d9-97a3-8684ff57ad63
focus: red-team P6 (Password + Session BFF) + suppression legacy contextSession
---

# Session retro — 2026-06-20 — dcc4d72d

## Fait

- **Red-team Password** (`5318f53c`) — **vraie vuln trouvée + fermée** : oracle de timing au login
  (`locked`/`disabled` répondaient sans payer de hash → existence du compte révélée par le temps malgré
  le 401 uniforme). Fix = `consumeDummy` sur ces 2 chemins. Matrice timing par COMPTAGE (encodeur
  instrumenté : chaque échec = exactement 1 verify) + credential incomplet (0 hash, 0 throttle) + e2e.
- **Suppression `contextSession`** (`522bd469`, −184 l, 23 fichiers) — legacy mort (0 conso applicatif,
  décision 06-13). Storage par `id` seul, anti-fixation triviale (`destroy(oldId)` sans aire). Touche
  http (contrat ISession + session/storage/kernel), 3 adapters (colonne `context` drizzle/mongoose +
  clé redis), framework (`@UseSession` perd `context`), security, app+test.
- **Red-team Session BFF** (`b01b6fbb`) — brique SAINE : 2 attaques prouvent les défenses (fixation
  closure = ancien cookie rejoué après login → 401 ; id forgé → 401). Pas de code touché.

## Décisions / POURQUOI

- Red-team = peut trouver une vraie faille (Password) OU confirmer une brique saine par des tests
  d'attaque (Session BFF). Les deux sont des issues légitimes — ne pas inventer un fix pour « justifier ».
- `contextSession` supprimé plutôt que conservé : isolation = RBAC (P6.8), jamais la ségrégation de
  session bucket (OWASP/RFC 6265). 0 back-compat. Mémoire `project_session_context_strategy_gap` MAJ.

## Frictions (→ RETEX.md)

- Schéma SQLite dev périmé gradué `[2×]` : retrait de la colonne `session.context` → `NOT NULL
constraint failed` → serveur HUNG (499/timeout) → `DROP TABLE session`. (à graduer `feedback_*`).
- Banc intég muet/hung ≠ lent : diagnostiquer par log+curl (le user l'a flairé avant moi).
- Ne pas conclure « terrain sain » sans preuve quand une mémoire de décision existe (contextSession).

## Coûts évidents

- 1 cycle serveur « perdu » sur le hang SqliteError (diagnostic) ; sinon HMR/0 restart inutile.

## Commits produits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `5318f53c` | fix(security) oracle timing login + matrice red-team Password     |
| `522bd469` | refactor(session) suppression contextSession (legacy)             |
| `b01b6fbb` | test(security) red-team Session BFF (fixation closure + id forgé) |
