---
date: 2026-09-25
session_id: 4816c22a-c5a8-4159-9f68-058fc2cb0b5b
focus: "#484 — le scope de la requête atteignable par RequestContext.getScope()"
---

# Retex 2026-09-25 — 4816c22a

- **Fait** : `790e478b7` feat(core) — `getScope()` / `requireScope()` (trois causes nommées), `IScope.closed` ; `ea00abbbd` feat(http) — le scope posé aux trois sites (HTTP, WebSocket, pont `api.request`), cause 2 dédoublée, tests sur le serveur réel. **#484 fermé** avec compte rendu et rapport `fable` en commentaire. Ouverts à la demande de l'auteur : #489 (banc de débit face à NestJS), #490 (seuils mémoire aveugles), #491 (`getParameters` rend la configuration partagée par référence). #486 enrichi (paragraphe d'ouverture du README sans comparaison + analyse de positionnement), #485 (contrat de #484 + audit `fable` de fin de chantier en critère 6), #481 recalé.
- **Frictions** (versées au sas) :
  - un camp A/B entièrement refusé lu comme un perdant (« −8 % ») sans mécanisme — le test nul aurait tranché ;
  - la route de test `/als-test/state` était la cible du banc comparatif : mon champ faussait l'égalité des camps ;
  - le rapport `fable` qui a tranché la conception n'a été posté sur le ticket que sur demande du user ;
  - le seuil mémoire publiait sa marge (×14 à ×678) sans que personne ne le resserre ;
  - hors sas : `anchor-fix` a de nouveau ramené `Scope.set` sur `Container.set` (piège documenté) ; `npm run typecheck` ne couvre pas `src/modules/test` (TS2367 vu seulement au build du module) ; `==` dans `[ ]` sous zsh a cassé un contrôle de restauration.
- **Décisions** :
  - scope posé dans la charge utile (conception A), arbitrée par `fable` contre une lecture dérivée de `context.container` : moins chère dès #485, bruyante sur un oubli, testable site par site ;
  - un scope refermé n'est jamais rendu (`closed`) ; la micro-bulle de `Context.log()` reste sans scope ;
  - perf : micro-banc plat (−0,6 %), A/B de débit non concluant — énoncé, pas contourné ;
  - l'audit `fable` de fin de chantier #481 est le dernier geste avant de fermer #485.
