---
date: 2026-05-28
session_id: 4c6711f8-6d4a-459a-9e17-3dd9fd0805af
focus: Major #2 mongoose 8→9 + audit/migration bancs ORM Mocha→Vitest + dette engines Node 22
---

# Session retro — 2026-05-28 — 4c6711f8

## Tool usage

| Outil                          | Calls |
| ------------------------------ | ----: |
| Bash                           |  ~130 |
| Read                           |   ~31 |
| Write                          |    14 |
| Edit                           |   ~14 |
| ScheduleWakeup                 |     5 |
| WebFetch / ToolSearch / Cron\* |     5 |

> Volume Bash anormalement haut : ~40 % = sondes `echo ping`/recover dues aux hoquets du harness (canal tool muet par intermittence toute la session).

## Top fichiers Read/Edit

| Fichier                                |   × |
| -------------------------------------- | --: |
| mongoose/SessionStorage.ts             |  5+ |
| drizzle load test                      |   4 |
| package.json (mongoose/seq/drz/racine) |  ~8 |
| memory backlog/feedback                |   5 |

## Coûts évidents

- **Harness instable** : ~10-15 min cumulées de canal muet → sondes répétées + relectures à froid. A causé UNE erreur réelle : commit mongoose sur lecture corrompue (« 8/8 + build 0 err » faux) → re-validé + amendé. Leçon réappliquée : ne jamais conclure sur lecture douteuse.
- **5 ScheduleWakeup** posés (puis superflus car canal revenu avant échéance) — pollution cron one-shot (1 nettoyé via CronDelete).

## 💶 Coût (€)

- Total ~**€50** (≈ $54) — cache read 19.2M tok ($28.7) + cache write 1.04M ($19.6) + output 79k ($5.9). **Cache-dominé ~89 %** : contexte lourd (CLAUDE.md racine massif + MEMORY.md + skills) porté sur une session longue (3 features). Gonflé par les hoquets harness (relectures). Levier : `/clear` entre features aurait coupé le cache porté.

## Recommandations

1. **Sur canal tool corrompu → STOP, ne pas commiter** sur une lecture. Re-lancer la commande à froid d'abord. (Vécu : faux 8/8 mongoose.)
2. **Auditer un banc de test = l'exécuter**, pas `ls`. 2 ORM sur 3 étaient orphelins malgré ✅ en doc. → mémoire `feedback_orm_bancs_vitest_migration` créée.
3. **`/clear` entre features non liées** (mongoose / migration vitest / engines = 3 sujets) aurait réduit le cache porté (~€50 → estimé ~€30).

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin (retiré ~4× cette session) — [[feedback_git_index_lock]] / safe-commit.sh.
- ✅ Vitest = convention (Mocha legacy) — [[feedback_test_framework_vitest]].
- ✅ Lire guide officiel vN-to-vM AVANT bump major — appliqué (migrating_to_9).

## Commits produits

| Commit  | Sujet                                                                        |
| ------- | ---------------------------------------------------------------------------- |
| b81ae56 | mongoose 8.23.1→9.6.3 + mongodb 6.17→7.2 (QueryFilter, test orphelin réparé) |
| 1b46723 | bancs ORM sequelize+drizzle Mocha→Vitest (27+14+8 verts)                     |
| d81319a | engines Node >=22.11.0 (LTS paire) + .nvmrc + CI publish 16→22               |
