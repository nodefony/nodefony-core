---
date: 2026-08-09
session_id: 684b2101-1c9d-4286-945b-2df3fa390328
focus: MCP — les 2 dettes d'archi soldées, puis la porte rendue JOIGNABLE et les outils réservables
---

# Session retro — 2026-08-09 — 684b2101 (08-09c)

## Fait

- **Dette (a)** — le protocole MCP remonte au CŒUR (`nodefony/src/mcp/`) ; le devkit ne garde que
  la porte et n'en ré-exporte rien.
- **Dette (b)** — registre d'outils par COLLECTE (`IModule.getMcpTools?()`), lu à chaque requête :
  pas de structure au boot, pas d'ordre de `register()`, fraîcheur gratuite.
- **Négociation de version** — `initialize` échote la révision du client ; 5 révisions servies ;
  repli `2025-03-26` pour un client muet.
- **Outils RÉSERVÉS** — `scopes` (tous exigés) + `requiresAuth`, filtrés à la collecte donc
  `tools/list` ET `tools/call` d'un seul point ; handler `(args, caller)`.
- **Annonce des réservés** dans `server/discover` — un nombre, jamais un nom.
- **Découvrabilité** — le gabarit `AGENTS.md` d'une app apprend qu'on peut AJOUTER un outil, avec
  un exemple de CODE, et un gate le verrouille.
- **Rouge CI réparé** (6/6 jobs) : `check` sous le timeout implicite de 5 s.

## Coûts évidents

- **La CI rouge n'avait pas été regardée** au démarrage de la session — signalée par le user.
  Un `gh run list` en RESUME l'aurait montrée (le garde-fou du skill regarde les commits, pas la CI).
- **Deux allers-retours de doc** sur la même justification (l'écart OAuth) : écrite, puis corrigée
  une heure plus tard après lecture de la spec. La vérifier d'abord coûtait moins.
- Découpage de commits contraint : négociation et refactor vivaient dans les mêmes fichiers,
  impossible à séparer sans `add -p` (interdit ici). Assumé dans un message long.

## Recommandations

1. **Ajouter `gh run list --limit 3` au mode RESUME** du skill `nodefony-session` — la vérité
   terrain inclut la forge, pas seulement `git log`.
2. **RFC 9728** (`/.well-known/oauth-protected-resource`) : supprime le bruit OAuth des clients ET
   donne le chemin normalisé vers l'autorisation. Prérequis des scopes effectifs.
3. Vérifier une justification d'écart comme on vérifie une affirmation — elle se recopie autant.

## Les TROIS bancs devkit, joués après la clôture (2 h)

| Banc           | Verdict   | Ce qu'il apprend                                          |
| -------------- | --------- | --------------------------------------------------------- |
| vérité         | ✅ exit 0 | 15/15 étapes ; l'exemple `getMcpTools` du gabarit COMPILE |
| découvrabilité | ⚠️ exit 3 | 26/30 ; chutes T16/T17/T25, remontée T28                  |
| schéma (umami) | ❌ exit 1 | l'app n'a jamais démarré — trou `--dialect`               |

**Rejeu 3 runs sur les 4 tâches signalées — une seule était un vrai signal :**

- **T16 : 0/3** → régression RÉELLE, elle échoue systématiquement.
- **T17 : 2/3 · T25 : 1/3 · T28 : 1/3** → **instables**. Leur apparition au dépistage
  était du bruit d'échantillonnage, pas une dérive.

🔴 **Trois tâches sur quatre sont instables** — même gabarit, même décor, même modèle. Deux runs
de la MÊME tâche ont coûté 87 tours / 1,14 $ et 32 tours / 0,45 $ : un facteur 2,7. La référence
antérieure ayant été écrite sur des runs UNIQUES, une part de ses futurs « chutes / remontées »
sera du bruit. La référence porte désormais `passes/runs` pour ces quatre, donc l'instabilité est
inscrite au lieu d'être perdue (`232ee5fd`).

🔴 **Trou produit trouvé par le banc de schéma, et il se referme en trois faits** : `create entity`
génère une table **SQLite** alors que le connecteur configuré est **PostgreSQL** → l'application ne
démarre pas (`entity "Account" is not ported to this dialect`) → **0 colonne sur 6 tables**. Le
verdict « 0/9 » ne dit donc rien de la grammaire : rien n'a atteint la base. L'option `--dialect`
EXISTE (`CreateCommand.ts:59`), l'`AGENTS.md` généré n'en parle **jamais** (0 mention), et l'agent
a tenté `create entity` trois fois en trois syntaxes avant d'abandonner le générateur pour
6 fichiers écrits à la main — ce que ce banc mesure précisément comme l'échec du scaffold.

## Patterns récurrents (déjà gérés)

- ✅ Débranchement systématique avant de déclarer un gate vert (3 fois cette session : 9, 6 et
  1 rouge). `feedback_gate_must_bite` tient.
- ✅ Preuve sur l'artefact reçu : route réelle + deux clients tiers, pas seulement des fonctions
  pures. `feedback_prove_on_received_artifact`.

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `7eb7efe7` | protocole au cœur + registre d'outils (+ rouge CI réparé)    |
| `ca24c019` | MIGRATION_STATUS — les 2 dettes soldées                      |
| `ae0778a0` | AGENTS.md d'une app : on peut AJOUTER un outil               |
| `a24672aa` | outils réservés — `scopes` / `requiresAuth`, fail-closed     |
| `a2e816d3` | MIGRATION_STATUS — réservés + ce qui manque pour les activer |
| `52695e3a` | annonce des réservés dans `server/discover`                  |
