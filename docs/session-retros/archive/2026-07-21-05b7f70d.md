---
date: 2026-07-21
session_id: 05b7f70d-b3df-4901-8681-8b187356220e
focus: Vague doc 7(c) — glossaire, CLI, tutoriel + régime de gate « lexique » + 7 corrections doc↔code CLI
---

# Session retro — 2026-07-21 — 05b7f70d

## Focus

Clôture de la vague transverse (7c) du chantier doc. + moment de rassurance : le user, inquiet
d'avoir « vu trop gros », a demandé si le travail doc sert vraiment au devkit/LLM.

## Fait

- **Régime de gate « glossaire »** : 3ᵉ régime `lexique` dans `doc-lint` (détecté sur
  `lexique.md`/`glossaire.md`) + documenté standard §8bis-lexique. 2 lexiques (global + security)
  conformes, **2 niveaux GARDÉS** (ADR-0001, PAS de fusion — écarté le libellé « fusionner » du kit).
- **Page `cli.md`** (cœur) : moteur, 11 commandes intégrées + commandes de module, scaffold, extension,
  complétion. 3 gates verts (doc-lint, anchor 35/35, code-check). Compteur via `test-map` topic `cli`.
- **Tutoriel** `docs/tutoriels/premiere-application.md` (Diátaxis) câblé (demarrer + hub racine).
  `reference/` **absorbé** (API auto = symbols.json + Studio, ADR-0001 interdit une référence manuelle).
- **Vague transverse (7) CLOSE** dans MIGRATION_STATUS.
- **7 contradictions doc↔code CLI corrigées** (TSDoc + CLAUDE.md + menu `start` mort) ; 213 tests CLI verts.
- **Note backlog microservice** (3 niveaux) en mémoire IA.

## Décisions (POURQUOI)

- ⭐ **Régime de gate par NATURE de page** (idée du user) : un gate a du sens pour ce qu'il vérifie ;
  tordre un glossaire dans le moule « page-concept » = remplissage. Sobriété : tutoriel gardé en
  régime brique + `tests: none` plutôt qu'un 4ᵉ régime pour UNE page.
- **2 niveaux de lexique gardés** : ADR-0001 (colocalisation = surfaçage Studio) prime sur le libellé
  du kit. Ambiguïté specs code↔kit → AskUserQuestion (usage légitime).
- **`reference/` absorbé** : l'API auto EST la référence ; une manuelle = 2ᵉ vérité qui dérive.
- **Microservice pas maintenant** : 3 niveaux (preset trivial / inter-service post-release / infra
  déléguée). Règle : pas de feature fantôme dans le code (leçon du menu `start`).

## Frictions (→ RETEX.md)

- Grep de PREUVE cassé 2× le même jour (emoji `^`, zsh ne word-splitte pas `$var`) → faux « 0 régression ».
- anchor-check SUSPECT : citer le BON symbole près de l'ancre (`EX_USAGE` à `:484`, pas `:467`).

## Commits produits (branche `doc`)

| Commit     | Sujet                                                   |
| ---------- | ------------------------------------------------------- |
| `952add18` | docs(lexique) — régime « glossaire » + 2 lexiques       |
| `5d94def0` | docs(cli) — page de référence du CLI                    |
| `51e827b8` | docs(tutoriel) — première application + clôture vague 7 |
| `d5b6f2e0` | fix(cli) — 7 contradictions doc↔code                    |
