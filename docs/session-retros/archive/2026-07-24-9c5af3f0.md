---
date: 2026-07-24
session_id: 9c5af3f0-384b-44a3-bae3-3ecb5337b80f
focus: Registre des écarts doc↔code mené de 80 à 113 soldés — motifs D et E, puis les défauts un à un
---

# Session retro — 2026-07-24 — 9c5af3f0

## Focus

Reprise du registre doc↔code au **motif D** (docs IA périmées), puis motif E (trous de
gates), puis les défauts restants un à un jusqu'à ne laisser que des décisions d'archi.

## Fait

- **Motif D soldé** (`960729e7`) — 11 `CLAUDE.md`/`MEMORY.md` recalés sur le code. Plusieurs
  affirmations étaient l'inverse du vrai (sessions WS, phase `onReady`, `IRepository.update()`
  qui n'existe pas, stores drizzle « sqlite-only », CSP nonce « à faire » alors que livré).
- **Gate anti-journal à zéro** (`228f50fe`) — 64 dates et 8 sections d'avancement retirées des
  docs de module ; **F48** au passage (`types` pointait un fichier absent).
- **F189** (`a4999c60`) — une panne serveur ne rend plus son message d'exception ni sa stack en
  production. En WS la fuite partait vers un client **anonyme** (instanciation au handshake,
  avant le firewall). Trouvaille : cinq gardes `environment === "prod"` jamais vraies.
- **F96** (`96a0e37d`) — `gateReporter` câblé sur realtime + trois défauts qui rendaient le gate
  menteur (nom d'interrupteur faux, variable Redis divergente, catalogue jamais consommé).
- **Motif E** (`78bf74b2`) — branche infra mongoose enfin testée ; deux topics de couverture
  recadrés (`pipeline-requete` s'attribuait la sécurité, `scopeCatalog` était rangé sous le DI).
- **F12 + F10** (`145ff093`) — `Vary` fusionne au lieu de s'écraser (empoisonnement de cache
  fermé par construction) ; l'écran Firewall lit l'état RÉELLEMENT émis via un nouveau contrat
  `HttpKernel.describeTransportSecurityHeaders()`.
- **F188 + F60 + F61** (`14936ae4`) — path traversal par le nom de fichier client fermé ;
  `nbListeners: 20` appliqué pour de vrai ; argument fantôme retiré de `Event.listen()`.
- **`create.test.ts`** (`bbc6a955`) — rouge permanent réglé : l'assertion visait le fichier, pas
  le code. **Core vert : 2128/2167.**
- **F86 + F174** (`91d1d7a6`) — le verrou de frame WS garde aussi les connexions déjà ouvertes.
- **Dernier lot** (`30dd89d3`) — gardes morts des templates (dont un fail-soft inatteignable),
  surface d'export orm-core, frontmatter du corpus aligné sur ce que le runtime lit.

## Frictions

- Le compteur du registre **mentait** (68 ouverts pour 33) : il ne lit qu'une colonne, les lots
  étaient soldés en prose. Recalé, et le recalage a lui-même abîmé un tableau de synthèse.
- Trois faux diagnostics d'outillage : `head -40` tronqué pris pour une absence, `$FILES` non
  découpé par zsh, substitution `s/ ()//g` qui a mangé une arrow function.
- Un test asynchrone jugé cassé alors que c'était mon assertion qui était synchrone.
- commitlint refuse un sujet de commit commençant par une majuscule (deux rejets).

## Enseignement principal

**Presque tous les items de ce registre étaient des promesses non tenues, pas des bugs.** Un
`describe()` qui annonce un backend inexistant, un défaut qui n'est jamais lu, un gate qui ne
regarde qu'une moitié du silence, un message d'aide qu'aucun chemin n'atteint. Retirer une date
d'un fichier a suffi à faire tomber trois mensonges : relire une phrase force à la vérifier.

## Commits produits

| Commit     | Sujet                                              |
| ---------- | -------------------------------------------------- |
| `960729e7` | docs IA recalées sur le code (motif D)             |
| `228f50fe` | dé-journalisation + F48                            |
| `a699e2df` | preset Svelte inscrit dans la DoD de release       |
| `a4999c60` | F189 — masquage des erreurs en production          |
| `96a0e37d` | F96 — un vert ne tait plus ce qu'il n'a pas exercé |
| `78bf74b2` | motif E — F79/F43/F39                              |
| `145ff093` | F12 + F10 — `Vary` et écran Firewall               |
| `14936ae4` | F188 + F60 + F61                                   |
| `bbc6a955` | `create.test.ts` — visée resserrée                 |
| `91d1d7a6` | F86 + F174 — verrou de frame WS                    |
| `30dd89d3` | dernier lot (templates, exports, frontmatter)      |
