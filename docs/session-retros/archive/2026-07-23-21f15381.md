---
date: 2026-07-23
session_id: 21f15381-62f4-4111-9c7f-e2ac5a3815f9
focus: Lot C du registre — deux gestes (redis, hookKernel) + une régression de mon fait, trouvée par le user
---

# Session retro — 2026-07-23 (4ᵉ) — 21f15381

## Fait

- **`3bf01a40` — @nodefony/redis, 6 items d'un coup** (F67, F68, F74, F75, F76, F81). Racine commune :
  le service rendait un client **fermé** comme s'il était ouvert, et la règle de curseur `SCAN` était
  recopiée trois fois pour n'être durcie qu'une.
- **`13b734e5` — le banc de doc ne punit plus les exemples** qu'il cite (pré-existant `b8f2a28a`).
- **`5282137c` — `Module.hookKernel`** (F66) : `static critical = false` ne couvrait que les hooks de
  classe ; la connexion de mongoose interrompait le boot en production.
- **`b208ada4` — les doubles de test déclarent le hook** que le contrat exige (ma régression).
- Batterie finale **7783 passés, 0 échoué, TOUTES cibles d'infra exercées** + charge/mémoire 27 passés.

## Coûts évidents

- **4 lancements de `test:all`** là où 2 auraient suffi : le premier avec `| tail -60` (diagnostic
  détruit), le deuxième sans serveur ni dist security (614 faux échecs), le troisième bon, le
  quatrième pour les dialectes. Le décor aurait dû être vérifié AVANT le premier.
- Une régression commitée puis corrigée — 3 allers-retours qu'une relance ciblée aurait évités.

## Recommandations

1. **Relancer les suites des modules touchés après CHAQUE geste** — gradué en
   `feedback_green_covers_only_its_diff` (vu 2× le même jour, dont la baseline sans serveur).
2. **Vérifier le décor avant la batterie** : `docker ps` + profils compose + `ls <paquet>/dist/index.js`.
   Trois commandes contre quinze minutes de run.
3. **Jamais `| tail -N` sur une sortie longue capturée** — rediriger le fichier complet, filtrer après.

## Ce que le user a rattrapé

1. « refais les tests pour couvrir ce que tu corriges » — j'allais livrer le geste redis sans banc dédié.
2. « c'est quoi RedisTokenStore ? » puis « c'est pour la session ? » — a forcé la distinction des trois
   stores, qui a clarifié le périmètre du lot.
3. « nous avions pas décidé de ne pas fournir des stores redis qui ne sont pas légitimes » — vérification
   faite : les trois touchés sont exactement le périmètre acté (étage durable opt-in).
4. **« les 10 unitaires viennent d'où ? »** — la question de la session. Elle a débusqué MA régression,
   que j'avais déjà commitée et déclarée verte.
5. « les containers ne tournent pas » — PostgreSQL et MySQL n'avaient jamais été exercés.

## Patterns récurrents (déjà gérés)

- ✅ Corriger par MOTIF, pas par item (méthode du registre, confirmée : 6 items en un geste).
- ✅ Gate vu rouge avant d'être déclaré vert (fait sur chaque correctif).

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `3bf01a40` | redis — un client fermé n'est plus servi comme s'il l'était    |
| `13b734e5` | documentation — le banc ne punit plus les exemples             |
| `5282137c` | kernel — « ce module est optionnel » couvre enfin ses services |
| `b208ada4` | tests — les doubles de Module déclarent le hook du contrat     |
