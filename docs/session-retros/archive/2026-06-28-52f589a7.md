---
date: 2026-06-28
session_id: 52f589a7-ba0b-4724-9528-da37238cf48f
focus: config ADR-0006 « impeccable » bouclée + page Studio /nodefony/config en dashboard
---

# Session retro — 2026-06-28 — 52f589a7

## Focus

Deux blocs : (A) clôturer la config « impeccable » (ADR-0006) ; (B) construire la page
Studio `/nodefony/config` demandée par le user, jusqu'à un dashboard de cartes.

## Fait

- **A. Config impeccable (6 commits `f669d759`→`63f5c780`)** : fail-closed prouvé + « did you
  mean » sur typo `NF__*` · **`NF__APP__*`** (dernier trou, config app surchargeable par env) ·
  banc e2e terrain (live 2/2) · fix provenance app Studio · **auto-gen `.env.example`** (`defineEnv`
  introspectable) + **garde pre-commit** anti-dérive.
- **B. Page config Studio (4 commits `76cfbd28`→`5e13eba6`)** : endpoint agrégé `GET /nodefony/
kernel/api/config` (helper DRY `computeConfigEntry`) · **attribution cross-module** « qui surcharge
  vraiment » (`overriddenBy`, index inversé `module-<X>`) · carte synthèse intelligente (surcharges
  seules) · **DASHBOARD** de cartes `ConfigModuleCard` (remplace l'accordion) → clic = onglet Config
  module (`?tab=config`).

## Frictions / leçons (→ RETEX.md)

- **« Préparé » ≠ « live »** : annoncé la page « prête » sans rebuild framework + restart → endpoint
  absent du dist → 404 → « ça marche pas » (vécu). Vérifier live (curl admin) avant de dire fait.
- **Provenance re-dérivée ne connaît pas l'AUTEUR** : il a fallu un index inversé des `module-<X>`
  pour dire « `@nodefony/test` surcharge `http.upload.maxFileSize` » (≠ « nodefony.config.ts »).
- **Écran d'ensemble = mener par les écarts**, pas dumper 100+ réglages (dashboard de cartes >
  accordion ; carte synthèse = surcharges seules). Gotcha : DataGrid colonne sans `render` = vide.
- **Rafale de retours UI** du user pendant un gros chantier → j'ai commité par petits lots cohérents
  (attribution, UX, smart card, dashboard) en vérifiant typecheck à chaque fois.

## 🔴 Bug ouvert (next session)

Déconnexion Studio **~30s** : heartbeat SERVEUR (`@nodefony/http` `wsHeartbeat.ts`) `terminate()` la
socket `wss:5152` — le `pong` ne round-trip pas sur le port HTTP/2. Heartbeat client = no-op (pas lui).

## Commits produits (12, poussés `claude-ts`)

`f669d759` `15a3436a` `1f8af933` `a351e858` `d5b092f5` `63f5c780` `76cfbd28` `95e83217` `b25605e6`
`5e13eba6` (+ le commit `main`/deps précédent hors session).
