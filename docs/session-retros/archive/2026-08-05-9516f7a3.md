---
date: 2026-08-05
session_id: 9516f7a3-0a0a-4967-a0a9-0f68cc3c1781
focus: P14.17 dev déporté (origine publique Vite) + preset svelte5 bout-en-bout
---

# Session retro — 2026-08-05 — 9516f7a3 (4e du jour)

## Fait

- P14.17 livrée : origine publique Vite dissociée de l'écoute (template `{port}`, détection
  Codespaces/Gitpod, `allowedHosts` dérivé de `trustedHosts`, `hmr` client 443/wss) + 6 fixes de
  résilience du superviseur (orphelin du timeout tué, budget restarts réarmé, verdict npx win32…).
- Preset `svelte5` partout : builder, générateur, scaffold (app/front/module), module canonique
  `test-frontend-svelte`, card home. Skill `create-frontend-module` remis au vrai.

## Coûts évidents

- 4 tours navigateur/serveur sur la page blanche svelte (fichier créé après le boot Vite).
- 9 rouges fantômes de `test:all` : install+build lancés pendant le run (auto-infligé).
- Page du POSTE cassées en fin de session : variable d'observation conteneur restée posée.

## Recommandations

1. Dette DevSupervisor : le watch backend redémarre TOUT sur édition d'un fichier `frontend/`
   (le hot-update Vite est court-circuité dans le repo self-hosted) — exclure `frontend/**` du
   watch backend.
2. Décor mixte poste+conteneur : documenter l'option `/etc/hosts` (`127.0.0.1 host.docker.internal`).

## Patterns récurrents (déjà gérés)

- ✅ dist TROMPEUR servi ≠ bâti (gotcha gradué la session précédente — n'a pas mordu cette fois).
- ✅ sortie entière capturée + rapport par `nodefony-run-log-report` (6 450 lignes).

## Commits produits

| Commit   | Sujet                                                                         |
| -------- | ----------------------------------------------------------------------------- |
| 398bfad8 | feat(frontend): l origine publique de Vite se dissocie de l adresse d ecoute  |
| 63c9b05f | feat(frontend): le preset svelte5 arrive partout ou les trois autres vivaient |
