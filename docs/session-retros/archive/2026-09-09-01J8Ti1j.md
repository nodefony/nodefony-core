---
date: 2026-09-09
session_id: session_01J8Ti1jA29rqs17o7i9u4tY
focus: Six faux verdicts d'une génération alpha.3 relevés, ticketés (#301→#306) et soldés dans la séance, avec un e2e réel qui garde la porte d'entrée
---

# Session retro — 2026-09-09 — 01J8Ti1j

## Fait

- **Reprise** puis **audit d'une génération réelle** `npm create nodefony@alpha` (alpha.3, vitrine
  complète, PostgreSQL, Svelte, Claude Code, un PostgreSQL étranger sur 5432) : huit points relevés,
  chacun confronté au code (`fichier:ligne`), deux hors périmètre (#214, amont drizzle-kit).
- **Grappe #301 ouverte (parent + 5 enfants) puis SOLDÉE 6/6** : `ccc8d9a7` (cli — #302 cause de
  la migration, jeton débranché sur base morte, `--no-token`, verdict de `security:token` ; #303
  agent servi par fichier nommé, jetons lus au bon endroit, audience confrontée ; #305
  `svelte.config.js`), `0e1476a1` (kernel — #304 `Symbol.for` du catalogue d'env), `4424040f`
  (kernel — #306 `presented` posé par le cycle de vie, relu par `start()`, drizzle/mongoose sans
  `log + throw`).
- **E2E réel** sous `NF_RUN_CLI_BOOT` : app installée + construite (`--link`), base sur le port 1,
  transcript capturé entier et relu ; puis `orm:generate` rejoué et compté (3 → 1).
- **#289** remonté (16 → 7), **#301** recalé (jours = enfant ouvert), 8 « In Progress » fantômes
  redescendus, empreintes régénérées.
- Poussé `dev` (`56ea9aff`) ; CI 8/9 vertes sur `54c94d9c`, **Filet CLI Windows rouge** sur
  l'assertion « aucune stack » — assertion rendue parlante, CI relancée.

## Coûts évidents

- Un `cd` relatif bloqué par le hook, puis `set -- $pair` qui ne découpe pas en zsh : deux appels
  perdus sur du shell, piège déjà écrit dans le skill ticket.
- Le transcript e2e détruit par le `finally` du test : une génération rejouée pour rien.
- Le gate `standaloneOptions` : une option ajoutée au parseur doit se publier en quatre endroits —
  découvert par la suite complète, pas su d'avance.
- `--link` = une seule instance de `nodefony` : l'e2e ne peut pas reproduire #304 ; seule la preuve
  unitaire à deux instances tient.

## Recommandations

1. `post-commit` (ticket-progress) — exclure les commits de pilotage comme le fait déjà le lint :
   une règle, une implémentation.
2. Un banc PTY pour le flux jeton (`nodefony ai:mcp` → `security:token`), ou la génération alpha.4
   par un humain : c'est le seul chemin où le défaut vécu existe.
3. Consolider : thème 📐 (verdict binaire) atteint 5 frictions → mûr ; 👯 (jumeau) déjà mûr.

## Patterns récurrents (déjà gérés)

- ✅ suspecter son propre diff (rouge vu par débranchement, 9/9)
- ✅ preuve sur l'artefact reçu (app Svelte réelle construite ; doctor 6 → 5 dans l'app)
- ✅ `check-suites` pour la CI, jamais `run list`

## Commits produits

| Commit     | Sujet                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| `eb8727c8` | chore(board): grappe #301 + remontée de #289                                    |
| `ccc8d9a7` | fix(cli): nommer ce que npm create nodefony taisait quand la base ne répond pas |
| `0e1476a1` | fix(kernel): reconnaître le catalogue des variables entre deux instances        |
| `1c4e82bc` | chore(board): empreinte après #302 #303 #304 #305                               |
| `827586a4` | chore(board): #301 — jours recalés                                              |
| `4424040f` | fix(kernel): ne journaliser qu'une fois une erreur de configuration au boot     |
| `54c94d9c` | chore(board): empreinte après la grappe #301 soldée                             |
| `56ea9aff` | test(cli): faire nommer par l'e2e la stack qu'il trouve dans le transcript      |
