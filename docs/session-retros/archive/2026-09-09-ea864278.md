---
date: 2026-09-09
session_id: ea864278-1946-4e63-a6f5-f8d40c19461c
focus: grappe pré-alpha.4 soldée (#307→#310), puis montée des dépendances et découverte de la mise en attente npm
---

# Session retro — 2026-09-09 — ea864278

## Fait

- **Grappe pré-alpha.4 SOLDÉE 4/4**, fermée avec compte rendu après CI verte :
  #307 (`32a2092b`), #308 (`b3b17443`), #309 (`fb48431f`), #310 (`636449c6`).
- **Un gate débranché depuis des semaines est branché** : `format:scaffold` sortait en 1 sans que
  personne ne le lance. Pre-commit sur les gabarits (~5 s) + forge ubuntu — étape vue TOURNER, pas
  seulement job vert. Régime `--raw` neuf : le seul qui juge le rendu des gabarits à balises.
- **Montée des dépendances** : 42 → 16 écarts. Lot de correctifs (`f3639661`), puis quatre cas à
  preuve propre — `oxlint` (`68d88f4a`), `React` (`feb749a1`), `zod` (`0f062fff`),
  `rolldown` (`433d2e5c`).
- **L'inventaire voit enfin l'application générée** (`9e3b7849`) : `SCAFFOLD_VERSIONS` est entré
  dans son périmètre, et a rendu `mysql2` divergent dans la minute.
- **#312 ouvert** — la mise en attente npm (`npm stage publish`), à trancher avant #175.

## Coûts évidents

- Quatre rebuilds complets (`clean && build`) imposés par la nature des montées — le bundler
  exige un build entier, pas un `--filter`, sinon le cache rend l'ancien artefact.
- Un aller-retour de CI pour trois identifiants français : le gate de langue n'est pas en
  pre-commit, et rien ne me l'a rappelé au moment du geste.
- Une release préparée puis mise en pause (estampillage annulé) : le user a demandé les
  dépendances d'abord. L'annulation a coûté une réécriture, le garde-fou refusant `git checkout`
  sur un arbre sale — ce qui est son travail.

## Recommandations

1. **CONSOLIDATE** — trois thèmes au seuil depuis deux séances (voie fiable 9, verdict binaire 5,
   jumeau 6), sas à 790 lignes. Il ne se programmera que s'il se voit.
2. **Le gate de langue mériterait le pre-commit** sur les `.ts` staged — il coûte quelques
   secondes et m'a coûté un aller-retour de forge. À peser contre « la CI est gratuite ».
3. **Ne pas créer de skill pour ce qu'un automate fait** — proposé en séance, écarté : le dépôt
   porte 27 skills dont 11 jamais invoqués, et ce qui manquait était du périmètre, pas un rappel.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff — la CI rouge était la mienne, deux fois (langue, banc devkit).
- ✅ Prouver sur l'artefact reçu — app générée, app liée, rendu réel de `AGENTS.md`.
- ✅ Voir le gate MORDRE avant de le croire — débranché sur le vrai fichier, pas sur un témoin.

## Commits produits

| Commit     | Sujet                                                                               |
| ---------- | ----------------------------------------------------------------------------------- |
| `32a2092b` | fix(scaffold): rendre vert le premier verify d'une application générée              |
| `b3b17443` | fix(devkit): cesser d'envoyer l'agent configurer la sécurité au mauvais endroit     |
| `fb48431f` | fix(cli): ne plus contredire au diagnostic ce que la migration vient de faire       |
| `636449c6` | fix(cli): annoncer tous les frontends servis, et livrer un test de config qui teste |
| `0a0bddcc` | refactor(cli): écrire en anglais les identifiants introduits par la grappe          |
| `f3639661` | build(deps): monter les correctifs amont avant la préversion alpha.4                |
| `9e3b7849` | build(deps): faire voir à l'inventaire ce que reçoit une application générée        |
| `68d88f4a` | build(deps): monter oxlint en 1.82.0                                                |
| `feb749a1` | build(deps): monter React en 19.3.0                                                 |
| `0f062fff` | build(deps): monter zod en 4.6.0                                                    |
| `433d2e5c` | build(deps): monter rolldown en 1.2.8                                               |
