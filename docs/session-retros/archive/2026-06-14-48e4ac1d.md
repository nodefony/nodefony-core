---
date: 2026-06-14
session_id: 48e4ac1d
focus: P6 J8 — garde @IsGranted côté WebSocket (token dans l'ALS) ; (a) cookie prouvé + 3 fixes, (b) JWT cause racine isolée
---

# Session retro — 2026-06-14 (nuit) — 48e4ac1d

## Focus

J8 : poser le token du peer dans l'ALS pour que la garde `@IsGranted` (J7) fonctionne côté WS `api.request`.
Prouver « 1 garde = N transports ET N modes d'auth ». Bancs E2E réels (serveur live, vraie identité).

## Fait (code en working tree, NON commité — gates non passés)

- **Cœur J8** : `RealtimeController.invokeApiRequest` enveloppe `executeAction` dans `RequestContext.run`
  avec `hub.getTokenForPeer(peer)` ; `UserRealtimeToken.getAttribute("user")` pour `@CurrentUser`.
- **(a) banc cookie/BFF VERT 8/8** : garde @IsGranted prouvée côté WS — admin GRANT / user 403 / anon refus.
- **3 bugs trouvés + fixés PAR les bancs E2E** (unit verts) : `#auditDeny token.getUser()` → `IToken` aligné
  `getUserIdentifier()` ; **`realtime` opt-in fail-open → défaut sûr `default(true)`** (catch du user) ;
  403 garde `-32603` opaque → `RpcError(status)`.
- **(b) banc JWT : cause racine ISOLÉE** (logs temp DIAG1/2/3, retirés) : `handleSecurity` authentifie bien
  l'admin JWT, mais `set("user", token.getUser())` et en **JWT stateless `getUser()`=anonyme** (l'identité
  est dans le token, pas l'IUser) → `SessionRealtimeAuthenticator` (basé sur `getUser()`) lit anonyme. Fix =
  promouvoir le token realtime depuis le **IToken** de l'ALS, pas `getUser()`. + découverte : le JWT ne porte
  PAS les rôles (`tokenService.ts:323` scope only) → trancher RBAC JWT (claims vs lookup).

## Frictions / leçons

- **Bancs E2E réels = seul moyen de voir les trous d'assemblage** (4 trous sécu, unit verts). Réaction user :
  « il manque plein de tests E2E !! ». → matrice E2E `[transport × auth × décision]` comme gate AU FIL DE L'EAU.
- **`tail -N` sur logs = piège** en diagnostic comparatif (banc B noyé sous banc A) → m'a fait conclure 2× à
  tort. Grep ciblé (`-A1`, filtre path).
- **Diagnostic firewall WS** = logs temp ciblés en cascade (isSecure → gate → handleSecurity) ; a renversé mes
  hypothèses 2×. Vérifier le terrain à chaque cran, ne pas extrapoler.

## Commits produits

| Commit       | Sujet                                                                                               |
| ------------ | --------------------------------------------------------------------------------------------------- |
| `958fb0dc`   | docs(migration,security) : resync table P6.x + lexique autorisation J6/J7 (repo projet, NON poussé) |
| (mémoire IA) | `899252f`/`e8c34a6`/`68690c1` — plan J8, (a) prouvé+3 fixes, (b) cause racine + leçon E2E           |

> Code J8 (12+ fichiers) en working tree, NON commité (reste : fermer (b) + memory.test + matrice E2E + review).
