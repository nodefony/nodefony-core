---
date: 2026-08-01
session_id: ab38467d-e0a1-4819-95e3-76bd7f346007
focus: sortir le décor du banc de vérité du dépôt — et les deux défauts produit qu'il a révélés ; T17/T18/T19 écrites
---

# Session retro — 2026-08-01 — ab38467d

## Fait

- **Décor du banc de vérité SORTI du dépôt** (`b9a558a2`) : hors `tmpdir`, paquets installés depuis
  les tarballs, isolation CONSTATÉE (`lib/isolation.mjs`, appelé et non recopié). `--link` conservé
  pour la boucle courte, avec le verdict amputé annoncé dans le rapport.
- **Deux défauts PRODUIT trouvés par ce seul changement**, corrigés et prouvés :
  - `ff816841` — `nodefony <mode> --detach` bootait le framework du **CLI global** contre
    l'application locale : la garde anti-boucle `NODEFONY_CLI_DELEGATED` était héritée par l'enfant,
    relancé sur `argv[1]`. Un module chargé sur huit, sans un mot sur la délégation.
  - `8f08cae0` — le bilan de boot ne disait pas qu'**aucun module n'avait été tenté**. Ajout de
    `manifestEntries` + décompte dans le verdict + remédiation, posée seulement sous profil serveur.
- **Trois tâches du banc de sécurité écrites** : `16b764b5` T17 (protéger un préfixe),
  `c3fcb6f2` T18 (hiérarchie de rôles), `e205d71a` T19 (canal realtime privé, déléguée puis relue).
- **T18 jouée** : PASS, 15/15 sondes, 36 tours, 232 s, 0,34 $.

## Coûts évidents

- Le décor isolé coûte ~70 s par montage (pack + install tarballs) — largement rentable, il a
  trouvé deux défauts en deux passes.
- Une délégation sur le chemin critique a mis la session en attente : les six fichiers du banc
  étaient pris, rien d'autre n'était possible pendant ce temps.
- Quatre `TaskStop` inutiles sur un run mal diagnostiqué (le shell était déjà dans le bon dossier).

## Recommandations

1. Jouer **T17 et T19 en réel** — écrites, selftests verts, jamais confrontées à un agent.
2. **T21** (jeton machine + scope) reste à concevoir : dernière du cahier.
3. Rejouer T18 deux fois : un PASS unique ne dit pas si la tâche DISCRIMINE.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact REÇU (l'app installée, pas le dépôt) — a payé deux fois aujourd'hui.
- ✅ Voir le test ROUGE avant de le croire — fait sur les trois correctifs.
- ✅ Committer avant de déléguer (arbre propre) — respecté avant le lancement sur T19.

## Commits produits

| Commit     | Sujet                                                                                |
| ---------- | ------------------------------------------------------------------------------------ |
| `ff816841` | fix(cli) — un lancement détaché bootait le framework du CLI global                   |
| `b9a558a2` | feat(bench) — sortir le décor du banc de vérité du dépôt                             |
| `8f08cae0` | feat(kernel) — un boot qui n'a rien tenté doit le DIRE                               |
| `c3fcb6f2` | feat(bench) — une hiérarchie de rôles se déclare, elle ne se duplique pas            |
| `16b764b5` | feat(bench) — une zone protège un espace, des décorateurs protègent ce qu'on a écrit |
| `e205d71a` | feat(bench) — un canal realtime sans politique est public, et personne ne le voit    |
