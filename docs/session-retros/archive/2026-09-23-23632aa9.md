---
date: 2026-09-23
session_id: 23632aa9-6375-4403-950d-dee34c93cbc2
focus: MongoDB au choix de `create app` (#454), liste unique des bases (#463), banc du générateur sur MongoDB (#464) ; pilotage #460/#462 et type d'issue POC
---

# Session retro — 2026-09-23 — 23632aa9

## Fait

- Pilotage : #460 et #462 inscrits au tableau avec leurs champs ; #460 requalifié preuve de
  concept (sans jalon, label `backlog`, type d'issue **POC** créé au niveau de l'organisation
  après `gh auth refresh -s admin:org`) ; renvois #459 ↔ #460 par numéro.
- #454 : `create app --database mongodb` — Mongoose en tête du manifeste, sans Drizzle ;
  `User` et `provisionUsers` sur Mongoose ; clé `hasMigrations` distincte de `hasOrm` ; jeu de
  réplicas prêt au primaire élu (compose, CI GitHub en étape, GitLab initié par le pilote) ;
  `directConnection=true` ; `create entity` refuse en nommant MongoDB (`assertSqlOrmTarget`) ;
  `doctor` ne propose plus `create entity` sur Mongo. Preuve : app générée, e2e en production
  sur MongoDB réel, 7 briques + annuaire sur `mongoose`.
- #463 : les choix des questions `preset`/`database`/`frontend` se dérivent de leur constante
  (`choicesOf`, `Record` exhaustif).
- #464 : `verify-generated.mjs --database mongodb` (étapes SQL annoncées sautées, sonde des
  briques Mongoose, e2e Mongo) + job `dialectes` de la forge.
- #465 ouvert avec un kit de reprise en commentaire.

## Frictions (versées au sas RETEX.md)

- Deux affirmations non vérifiées reprises par le user (pagination Mongoose, jalon backlog).
- Filtre `grep` sur un `git commit` : refus du hook masqué.
- `--repack` a empaqueté un gabarit débranché ; un débranchement qui ne débranchait rien.
- `projectV2.items` figé : incident GitHub déclaré (Projects), pas nos scripts.

## Coûts évidents

- Session très longue (pilotage + trois tickets) : relecture de contexte dominante.
- Trois passes complètes du banc (~4 min chacune) dont une pour un débranchement inopérant.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `fcd03589` | feat(scaffold): proposer mongodb au choix de base de données                 |
| `8962fa6c` | refactor(scaffold): écrire une seule fois la liste des bases proposées       |
| `5c73a897` | test(devkit-bench): générer et démarrer une application MongoDB dans le banc |
