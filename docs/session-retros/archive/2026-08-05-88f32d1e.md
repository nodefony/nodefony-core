---
date: 2026-08-05
session_id: 88f32d1e-a783-484b-a741-13586a7ffcf5
focus: dette DevSupervisor = faux diagnostic (reproduit, refermé) + HMR conteneur prouvé + doc compose alignée
---

# Session retro — 2026-08-05 — 88f32d1e (5e du jour)

## Fait

- Dette DevSupervisor REFERMÉE sans toucher au superviseur : le filtre `frontend/**` existait
  depuis `de3dd07d` (13-07). Reproduction serveur réel : App.svelte édité → 0 restart ;
  contre-épreuve index.ts même module → restart. Le restart vu en 08-05d venait des `.ts` du
  scaffold, pas du front.
- Trou réel trouvé en voulant voir le banc rouge : filtre débranché, 13/13 verts (assertions
  satisfaites par la règle voisine non-`.ts`). Cas discriminant `.ts` client ajouté, vu ROUGE.
- HMR depuis le navigateur conteneur PROUVÉ (`hot updated` en wss, DOM sans reload, 0 restart
  backend, même PID) — molette `NF_FRONTEND_PUBLIC_ORIGIN` posée PUIS retirée (décor poste
  vérifié au curl après).
- Écart attrapé à l'écran : la démo svelte promettait « état conservé », le compteur repart à 0
  (comportement svelte-hmr par défaut) → texte corrigé (module + gabarit).
- Doc alignée : 2 composes (gabarit app + repo) sortis de la doctrine « pré-bâti obligatoire » ;
  piège « molette à retirer » écrit aux 4 endroits (skill build-hmr, AGENTS.md.tpl, 2 composes).
- Kit perf du cycle requête retrouvé (`project_request_cycle_perf_plan_kit`, clos 05-30, commande
  mocha périmée corrigée) + LE plan visé identifié : **5 goulots pipeline HTTP**
  (`docs/performance/2026-07-23-pipeline-http-vs-express-fastify.md` + `MIGRATION_STATUS.md:151`,
  « après devkit » → condition levée).

## Coûts évidents

- 2 relances serveur pour poser/retirer la molette (structurel : l'origine est cuite par process).
- Lignes géantes de MIGRATION_STATUS : 2 sorties shell persistées (145 Ko, 1 Mo) pour un grep —
  extraire par `awk NR==` + `fold`, jamais `sed -n a,bp` sur ce fichier.

## Recommandations

1. Prochaine session = chantier 5 goulots : PROFILAGE runtime d'abord (le rapport l'impose), pas
   les correctifs.
2. CONSOLIDATE toujours dû (4e signalement) — RETEX ~890 lignes.

## Patterns récurrents (déjà gérés)

- ✅ contre-épreuve avant de coder (un watch mort ne réagit à rien) — a évité le faux chantier.
- ✅ geste destructeur refusé sur arbre sale (garde git) → rebranchement par Edit, rien perdu.

## Commits produits

| Commit   | Sujet                                                                                      |
| -------- | ------------------------------------------------------------------------------------------ |
| 890827aa | test(core): le filtre frontend du watch se prouve sur un .ts client, seul cas discriminant |
| a182e618 | fix(frontend): la demo HMR svelte ne promet plus un etat que svelte-hmr ne conserve pas    |
| ed00a6ff | docs(front): l observation en conteneur n exige plus le pre-bati, et la molette se retire  |
