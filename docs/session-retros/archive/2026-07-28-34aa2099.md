---
date: 2026-07-28
session_id: 34aa2099-6e27-4965-ae2a-6c1408d5b383
focus: les 2 trous du générateur fermés et MESURÉS, puis le gate qui rattrape ce que la copie casse
---

# Session retro — 2026-07-28 — 34aa2099

## Fait

- **Les 2 trous du générateur** (`5c7d7351`) : `--table`, `--column-case snake`, `--id-name`.
  La colonne SQL change, la propriété TypeScript jamais.
- **Mesuré sur umami** : éditions à la main **29 → 2**, 6/6 tables, 82/83 colonnes, 41/41 index.
  Le diagnostic de la session précédente était juste.
- **Gate `nodefony check` étendu au câblage** (`04103776`, `bd99e5e6`) : entité/controller
  orphelins, nom réservé, brique exigée non déclarée. 32 classes du dépôt, 0 faux positif.
- **Banc** (`fe2c8d4f`) : étape MODULE (le générateur le plus structurant n'avait aucun banc) et
  3 tâches de SOCLE (service au conteneur, trace Syslog, hook de cycle de vie).
- L'étape module a sorti un défaut à son premier run (`6f191cb5`).
- Kit ORM du skill corrigé (`1d180c4a`) : il interdisait **quatre** choses que l'outil sait faire.
- Alertes d'analyse : ReDoS corrigé, faux positif fermé avec justification (`09aef7cc`).
- Montées mineures + alignement `@types/node` sur 25 workspaces (`0bcea1b4`).

## Coûts évidents

- Trois relances du banc de vérité, dont **deux perdues par ma faute** : `MODULE` non défini,
  puis un `cd` relatif qui a cassé le chemin du script lancé en arrière-plan.
- Le `dist/` périmé a masqué une vérification (`nodefony check` via le bin) — repoussée, puis
  soldée après build.

## Recommandations

1. **Lancer les 3 tâches de socle** — écrites, jamais exécutées. Un banc jamais vu échouer ne
   prouve rien, et c'est la règle du dépôt.
2. **Réparer le décor du juge HTTP** (session admin → 401) : troisième run où les sondes 4 à 9
   ne s'exécutent pas.
3. Trancher `website_event.li_fat_id` (30/31) : oubli de l'agent ou trou de grammaire ?

## Patterns récurrents (déjà gérés)

- ✅ `cd` relatif qui dérive — la garde a mordu deux fois, et j'ai quand même récidivé en
  arrière-plan, là où elle ne voit rien.
- ✅ Preuve négative systématique : 6 + 3 + 1 tests vus rouges avant d'être crus.

## Commits produits

| Commit     | Sujet                                                   |
| ---------- | ------------------------------------------------------- |
| `5c7d7351` | une table qui existe déjà impose ses noms               |
| `1d180c4a` | le kit ORM interdisait ce que le générateur sait faire  |
| `04103776` | on fait échouer ce qui n'a pas appelé le générateur     |
| `bd99e5e6` | une classe qui exige une brique que personne ne déclare |
| `6f191cb5` | deux gabarits justes, une combinaison qui échoue        |
| `fe2c8d4f` | le module, le socle, et un compteur qui accusait à tort |
| `09aef7cc` | montées mineures, et une regex bornée sous une alerte   |
| `0bcea1b4` | `@types/node` aligné sur TOUS les workspaces            |
