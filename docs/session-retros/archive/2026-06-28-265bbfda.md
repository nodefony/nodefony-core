---
date: 2026-06-28
session_id: 265bbfda-3b70-4953-b196-562a64e0182c
focus: Tirer parti de Node — 6 deps→natif, plancher Node 24 LTS, using/RegExp.escape, dette tslib
---

# Session retro — 2026-06-28 — 265bbfda

3ᵉ session du jour (après NIST timeout + nettoyage deps Dependabot). User a mis à jour Node (26.4) → « on peut tirer parti des MAJ ? ».

## Fait

- **6 deps runtime → natif** (1 commit chacune) : node-fetch→`fetch` (`b5b0270b`), bluebird→Promises (`4db4d6db`), moment→`Intl`+uuid mort http (`ee2d579d`), cli-color→façade `util.styleText` `colors.ts` (`69c6acd1`, +fix types `487ea53c`), uuid→`crypto.randomUUID`+`generateV5Id` supprimé (`96a283e8`).
- **Plancher Node 24 LTS** (était 22.13) : engines+`.nvmrc`+CI matrice `["24.x","26.x"]`+publish (`1f22c5a1`).
- **Patterns Node 24** : `await using` FileHandle (`FileLogDriver` core `588b32e5`, `SyslogAdminApi` framework), `RegExp.escape` (RealtimeHub), `lib: ESNext` aligné tous modules backend (`8f0f20cd`).
- **Dette tslib corrigée** : `tslib` en `dependencies` sur 15 packages (`065a163a`).
- **10 commits poussés** `claude-ts` (`b5b0270b`→`065a163a`). Gates : core 116 · framework 400 · http 516+559/560 · realtime 296 · build 19/19 · 0 warning TS · serveur UP.

## Frictions (→ RETEX.md)

- Mon diff cli-color avait 3 erreurs TS ratées (warnings rollup `(!)` non bloquants + vitest ne type-check pas). Devise : suspecter son propre diff même « vert ».
- zsh ne word-split pas `$files` non quoté (≠ bash) → perl reçut toute la chaîne, 0 fichier modifié. Array zsh.
- RPS abandonné : méthodo non objective (changements hors hot-path ; gain ALS = version Node, déjà sur 26). User validé « pas objectif ».
- tslib sous-déclaré repo-wide, masqué par hoist monorepo (casse en publish) → `npm pkg set`.

## Décisions structurantes

- **Viser 24 LTS** (décision user) : débloque `using`/Symbol.dispose (cleanup déterministe, aligné règle perf/mémoire), `RegExp.escape`, AsyncContextFrame (ALS rapide, gratuit runtime), `node:sqlite` (→ chantier ORM).
- **`using` ne convient QUE pour ressources scope-bound** (file handles, transactions), PAS les listeners event-lifetime.
- **node:sqlite → différé au chantier ORM** (évaluer driver `drizzle-orm/node-sqlite`).

## Commits produits

| Commit   | Sujet                                                |
| -------- | ---------------------------------------------------- |
| b5b0270b | node-fetch → fetch                                   |
| 4db4d6db | bluebird → Promises                                  |
| ee2d579d | moment → Intl + uuid http mort                       |
| 69c6acd1 | cli-color → util.styleText (façade)                  |
| 487ea53c | fix types Cli.ts                                     |
| 96a283e8 | retrait generateV5Id + dep uuid                      |
| 1f22c5a1 | plancher Node 24 LTS                                 |
| 588b32e5 | await using FileHandle (core)                        |
| 8f0f20cd | lib ESNext + RegExp.escape + await using (framework) |
| 065a163a | tslib en dependency (15 packages)                    |
