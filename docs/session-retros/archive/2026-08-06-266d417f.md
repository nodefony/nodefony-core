---
date: 2026-08-06
session_id: 266d417f-e50f-4401-9423-8561dc8c1aec
focus: lots F-A/F-F/F-C fabrique allégée — code + A/B secteur (+7 %) + sonde
---

# Session retro — 2026-08-06 — 266d417f

## Fait

- F-A (Service rest-destructuring, delete/double spread supprimés), F-F
  (`#trackedListeners` lazy), F-C (accept/acceptHtml/isHtml lazy) — en une
  passe chacun, contrats vérifiés au source AVANT édition (kit fiable).
- Gates : core 2592 · http 752 · framework 502 · intégration 605 · memory 9/9 ·
  load 27/27. A/B mono prod : +4,0 à +10,5 % (~+7 %), 0 chevauchement.
- Sonde : ctor Service 6,3→3,1 µs, accept 1,0→0,12, total 12,4 µs/req.
- Commit `ba1f0d17`.

## Coûts évidents

- **7 runs de banc pour 4 valides** : le cooldown interne du script (180 s)
  ne ramène jamais la machine sous 45 après un rebuild → 3 refus/invalides.
  Fix appliqué : boucle d'attente thermal ≤ 43 HORS script avant chaque run
  (3-10 min chacune) — à GRAVER dans bench-ab-mono.sh (candidat).
- new1 mesuré à th. 72 rendait un verdict INVERSÉ (−2 % au lieu de +10 %) —
  jeté sur le critère thermal, pas sur son RPS.

## Recommandations

1. `bench-ab-mono.sh` : remplacer le plafond 180 s par une attente
   paramétrable `BENCH_THERM_MAX_WAIT` (défaut ~600 s) — la boucle externe a
   fait le travail 4×.
2. F-B (URL fast-path) = prochaine session perf, red-team obligatoire.

## Patterns récurrents (déjà gérés)

- ✅ flip par `git show HEAD:` + rebuild `--force` + preuve marqueur au dist —
  le protocole du kit a tenu tel quel.
- ✅ vérifier `pmset` AVANT de coder le banc (secteur exigé — refusé 1× au
  mauvais moment, redemandé au bon).

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `ba1f0d17` | feat(perf): fabrique allégée F-A/F-F/F-C — +7 % RPS mono prod |
