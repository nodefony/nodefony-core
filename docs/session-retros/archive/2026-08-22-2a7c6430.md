---
date: 2026-08-22
session_id: 2a7c6430-a67f-4523-8285-73d8d9209b3e
focus: MCP data plane — étapes 1 et 2 du plan tranché (porte unique d'exécution, puis l'appelant porte son identité)
---

# Session retro — 2026-08-22 — 2a7c6430

## Focus

Étapes 1 et 2 du plan `project_mcp_dataplane_catalog_kit` : supprimer les DEUX chemins
d'exécution du plan d'administration, puis cesser de fabriquer le rôle d'administrateur.

## Fait

- **Étape 1 (`41c340ab`)** — `AdminApiController.runAdmin` était `private` : l'exécution (RBAC,
  porte d'idempotence, handler, normalisation, traduction des erreurs) était prisonnière d'un
  adaptateur de transport, pendant qu'un second chemin (`callAdminEndpoint`, cœur) la refaisait en
  dégradé pour `inspect` et le MCP. Extraite en `kernel/adminPlane/executeAdmin.ts`. D1/D2/D6 de
  `tmp/mcp-audit.md` tombent par construction : le corps du refus voyage entier.
- **Étape 2 (`df7a965a`)** — l'identité se PRÉSENTE (`IAdminCaller`, 3ᵉ paramètre obligatoire) au
  lieu d'être fabriquée. Deux bornes : `rolesFromScopes` (le rôle vient du scope) et
  `refusedAdminScopes` (un non-administrateur ne peut pas OBTENIR le scope). La règle de posture
  d'une porte MCP vit au cœur (`mcpCallerRoles`) pour les portes futures.

## Coûts évidents

- **Deux faux verdicts de mes propres instruments** : `vitest --root` depuis la racine (48 rouges
  fantômes) et `tsgo -p <tsconfig>` qui n'inclut pas les tests (3 erreurs invisibles). Le second
  a été rattrapé par le user, pas par moi.
- **Rebuilds en cascade** : le devkit typecheck contre `dist/types` du cœur, et le banc sur serveur
  réel lit le `dist` du devkit → un `npm run build` racine était nécessaire à chaque tour de
  vérification. Quatre builds complets sur la session.

## Recommandations

1. **Lancer le gate du dépôt (`npm run typecheck`) avant d'annoncer**, jamais l'invocation ciblée —
   celle-ci ne sert qu'à la boucle courte.
2. **Quand un fichier de test passe de rouge à absent**, exiger la liste des fichiers joués
   (`--reporter=verbose`) : un skip compte comme vert.
3. **Une exigence neuve se remonte jusqu'à ce qui produit l'artefact exigé** — et se publie là où
   le refus renvoie.

## Patterns récurrents (déjà gérés)

- ✅ La garde git du dépôt a refusé un `git stash` sur arbre sale — comportement voulu, j'ai
  qualifié l'échec par lecture au lieu de stasher.
- ✅ Débranchement systématique avant de croire un vert : 3 rouges unitaires (étape 1), 3 unitaires
  - 1 sur serveur réel (étape 2).
- ✅ Dist périmé suspecté en premier devant un banc rouge — puis écarté par un build complet.

## Commits produits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `41c340ab` | refactor(admin) — une PORTE, plus deux chemins : l'exécution descend au cœur  |
| `df7a965a` | feat(security) — l'appelant porte son identité, le rôle cesse d'être fabriqué |
