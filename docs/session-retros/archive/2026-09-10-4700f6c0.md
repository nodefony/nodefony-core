---
date: 2026-09-10
session_id: 4700f6c0-5901-4e0a-8eaf-031af70a9007
focus: Un audit externe des skills confronté au terrain, puis la garde des dépendances rendue capable de voir ce qu'elle jugeait
---

# Session retro — 2026-09-10 — 4700f6c0 (09-10e)

## Fait

- **#333** (`fad5e55f`) — trois `SKILL.md` sur 27 portaient un en-tête YAML invalide (`:` dans un
  scalaire plain). Le gate `skills:check` tournait en intégration continue et ne pouvait pas le
  voir : il découpe le frontmatter à la main. Verdict confié à un vrai parseur (`yaml` déclaré en
  devDependency, il n'était que transitive) ; descriptions portées en bloc plié, texte identique au
  caractère près.
- **#332** (`5025765b`) — `@types/node` 26.4.1 → 26.5.1 (**22 sites**, pas 2 comme l'annonçait le
  ticket) et `@angular/build` → 22.1.8. La mineure de types a RETIRÉ `IntervalHistogram` ;
  `ELDHistogram` le remplace, un seul consommateur.
- **#335 #336 #337** (`94686c98`) — la garde des dépendances, sur les trois familles de défauts que
  l'audit `fable` avait trouvées : quatre zones muettes (dont `--json` qui désarmait `--gate`), la
  règle qui posait la mauvaise question (nouvelle sévérité fatale `DÉDOUBLÉ`, décidée sur le
  verrou), et un rapport qui accusait à tort. `scripts/deps-gate.test.mjs` : 9 dépôts jetables,
  registre injoignable à dessein, chacun vu rouge avant sa correction.
- **`40960ad0`** — 15 sites `zod` alignés sur `^4.6.1`, peers compris ; épinglages exacts Angular
  desserrés ; `mongoose` 9.10.0 et `fastify` ^5.12.3. **5 paquets divergents → 0**, 13 écarts → 2.
- **Trois tickets ouverts puis faits dans la foulée**, sur demande du user. Jalon `outillage-agents`
  créé pour ce qui n'atteint aucun installeur et ne conditionne aucune publication.
- Compteur du sas réparé : il ne voyait pas les puces `- **[`, soit **9 frictions sur 46**.

## Coûts évidents

- Quatre instruments faux en une séance, chacun a coûté un aller-retour : `node --test` sur un
  fichier vitest · un `sed` coupant à la première ligne vide (« le prochain ticket est vide ! ») ·
  `$?` lu après un pipe (code de `tail`) · vitest lancé depuis la racine sur des chemins de modules
  → **161 faux rouges**, quand security passe 1098 depuis son propre dossier.
- Un `grep` trop étroit sur les flux d'intégration m'a fait conclure « personne ne lance ce gate »
  alors qu'il est lancé à `node.js.yml:335`. J'ai relayé la conclusion avant de la corriger.
- Un hook a refusé un `git push` en croyant à un `--force` absent : le commit de la commande
  composée a été perdu et refait.

## Recommandations

1. **Graduer le thème « Un contrôle VERT qui ne POUVAIT rien voir »** — 11 frictions, mûr depuis
   des semaines, invisible parce que le compteur ratait une puce sur cinq. C'est le travail d'un
   CONSOLIDATE.
2. **Ne pas relayer un audit externe sans contrôler ses ancrages** — 9/9 justes ici, mais 2
   affirmations d'inventaire fausses (un skill inexistant, un comptage à 22 pour 27).
3. **Un banc « avec et sans skills »** (#334) reste la seule mesure qui dirait si l'outillage
   agentic produit du meilleur code. Aucun banc ne prive aujourd'hui l'agent de ses skills.

## Patterns récurrents (déjà gérés)

- ✅ « suspecter son instrument » — quatre instances, toutes rattrapées avant conclusion publiée
  sauf une (le `grep` des flux).
- ✅ « un skip compte comme vert » — le `gateReporter` de mongoose a dit `NF_MONGO_TEST_URI`
  absente ; le banc de panne exigeait en fait **trois** interrupteurs, rejoué 8/8 en réel.
- ✅ « prouver sur l'artefact reçu » — descriptions comparées après reparse, pas à l'œil.

## Commits produits

| Commit     | Sujet                                                                         |
| ---------- | ----------------------------------------------------------------------------- |
| `fad5e55f` | fix(skills): réparer l'en-tête YAML que le contrôle ne voit pas               |
| `5025765b` | chore(deps): monter @types/node et @angular/build sur leur ligne              |
| `94686c98` | fix(deps): rendre la garde des dépendances capable de voir ce qu'elle jugeait |
| `40960ad0` | chore(deps): aligner zod sur la version exercée, monter mongoose et fastify   |
