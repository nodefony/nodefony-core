---
date: 2026-08-01
session_id: b9323069-0ec1-4492-b907-ad89a9918879
focus: harnais de test realtime publié (@nodefony/realtime/testing) + gabarit de test par-dessus
---

# Session retro — 2026-08-01 — b9323069

## Fait

- **`@nodefony/realtime/testing`** — `createRealtimeHarness()` : le décor d'un test de socket
  (faux `ContextType`, pont vers `handleRealtime` `protected`, reset du hub singleton, pose
  d'identité, verrou de frame en option) devient une brique du paquet.
- **`create controller --kind realtime`** pose `tests/<kebab>-realtime.test.ts` ; `create app
--preset complete` en hérite (`live-realtime.test.ts`).
- **P13.14** ajoutée au `MIGRATION_STATUS` (81 % → 82 %, 9✅ → 10✅).
- **Diagnostic délégué** (`Explore` en `sonnet`) : les trois agents maison SONT invoqués après leur
  dernier changement — la prémisse était fausse.

## Coûts évidents

- Un `create.test.ts` et un banc de vérité **verts sur un `dist` périmé** : le moteur de scaffold
  s'exécute depuis `dist/`, les gabarits `.tpl` se lisent à chaud → une modif d'`engine.ts` non
  buildée donne un rendu à moitié appliqué, sans un mot. Deux passes du banc perdues.
- Trois débranchements successifs pour voir les gardes rouges (verrou, notice, token) — coût
  assumé, c'est la règle du dépôt, et le troisième a révélé un vrai défaut.

## Recommandations

1. **Gate sur le CHEMIN quand un gabarit compose un nom de fichier** — appliqué dans
   `create.test.ts` ; le motif vaut pour tout scaffold à venir.
2. **Vérifier la prémisse avant la cause** — un `jq` de comptage avant/après aurait suffi ; passé
   en règle dans `RETEX.md`.
3. **`wc -l` sur ce qui serait copié** avant de trancher gabarit vs paquet publié.

## Patterns récurrents (déjà gérés)

- ✅ Débranchement pour prouver un gate (`feedback_gate_must_bite`) — a mordu 3/3.
- ✅ Preuve sur l'artefact reçu — subpath résolu depuis `node_modules`, suite rejouée après prettier.
- ✅ `gateReporter` nomme les 20 skips (Redis, cluster e2e) au lieu de les taire.

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `21f74bf0` | feat(realtime): le decor d un test de socket devient une brique publiee |
