---
date: 2026-07-04
session_id: 25382aca-a18c-47b9-97d3-861000eb52bb
focus: Phase 0.8 lancée — audit stores (matrice) + modèle 3 rôles + lot 1 auto-register + conventions config gravées
---

# Session retro — 2026-07-04 — 25382aca

## Fait

- Audit terrain COMPLET des stores/backends (4 agents Explore parallèles, format imposé) →
  matrice brique×backend×dialecte, 5 plaies structurelles, 3 faits périmés du kit débusqués.
- Design : modèle « 3 rôles » (database/cache/logs + dérivation, 0 cascade implicite, profils
  solo/serveur/cluster) — répond à « comment tout configurer en pg/mongo, qui peut utiliser redis,
  cette souplesse fait peur ».
- **Lot 1 livré** (`b25e2c3f`) : auto-register des stores framework par les adapters
  (drizzle 5, mongoose 3, redis 2), guards app-first, registres qui reflètent le réel,
  2 câblages app supprimés. Gates toutes vertes + preuve boot (8 tables).
- Conventions gravées (CLAUDE.md racine + kit) : `config.ts` + `defineModuleConfig.ts` +
  `configMeta.ts` core ; `store` vs `driver` ; NF_ partout.

## Frictions / leçons (versées dans RETEX.md par thème)

- Audit transverse = 4 agents parallèles à format imposé (délégation IA).
- 3 invariants d'ordre de boot pour l'auto-register (EntityRegistry throw, ordre manifeste,
  idempotence lazy) (build/boot).
- Registres = le réel ; nommer le rôle jamais l'outil (conception).
- « Sonder le code avant le kit » re-confirmé (2×) — 3 faux chantiers évités.

## Coûts évidents

- 1 seul cycle serveur (start→preuves→stop) ; builds modulaires avant build racine = erreurs TS
  isolées vite ; 1 fail de test (ordre registres process-wide) + 1 fail d'assertion API (ITokenStore
  sans `get`) corrigés en 2 itérations.

## Commits produits

| Commit     | Sujet                                                                                   |
| ---------- | --------------------------------------------------------------------------------------- |
| `b25e2c3f` | feat(stores): auto-register des backends framework par les modules adapters (0.8 lot 1) |
| (suivant)  | docs(session): convention config gravée + retex + RETEX.md                              |
