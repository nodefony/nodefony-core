---
date: 2026-07-20
session_id: 73ddf421-a7c5-4237-9b05-ab3376450561
focus: Vague 7 (a+b) — assainissement de `docs/` et canonisation de la socket Nodefony
---

# Session retro — 2026-07-20 — 73ddf421

## Fait

- **`docs/` ne porte plus que la doc du lecteur** : 29 artefacts internes (19 audits datés, 4 specs
  de migration, 1 brouillon, 4 versions de travail du livre blanc IA, 1 journal archivé) déplacés en
  mémoire IA `core-dev/`. Le portail scanne `docs/` intégralement — il les servait au même rang que
  les guides, et `docs/migration/MIGRATION_STATUS.md` était un **lien symbolique** vers le tableau de
  bord (385 Ko de pilotage interne servis comme page de documentation). 40 références réparées.
- **Socket canonisée** (F91) : `protocole.md`, `actions.md`, `observabilite.md` créées et revalidées
  contre le code ; les 8 pages `docs/realtime/socket/` supprimées ; la page de vision transverse
  amputée de ce que le module dit mieux et recentrée sur la trajectoire (SIP, ponts, preuve télécom).
- **Les 6 graphes temps réel sauvés** : ils étaient attachés au _chemin_ des pages supprimées par un
  `import.meta.glob`. Devenus une fence typée `nodefony-livegraph` + registre autonome — invocables
  par nom depuis n'importe quelle page, et placés en tête de page (demande utilisateur). Corrigés au
  passage : ils mentaient tous les six.
- **13 hubs convertis en grilles de cards** + le geste de navigation (le corps ouvre le hub, le
  chevron plie).
- Corpus **54/55** (seule `security/lexique.md` reste hors standard), 13/13 hubs en cards.

## Coûts évidents

- **4 lancements d'un gate qui ne vérifiait rien** (`npx tsc --noEmit` depuis `frontend/`). Coût réel
  faible ici (le code passait), coût potentiel maximal.
- Un `git add` multi-chemins échoué en bloc (pathspec déjà supprimé) → commit amputé, rattrapé au
  commit suivant. Piège déjà connu du RETEX, pas encore réflexe.
- Le chantier a débordé de la doc vers le code Studio (fence, registre, nav, CSS) — nécessaire pour
  ne rien perdre, mais c'est un élargissement de périmètre non prévu au plan des vagues.

## Recommandations

1. **`feedback_gate_must_bite` créée** — casser un gate exprès avant de lui faire confiance. C'est la
   leçon la plus transférable de la session.
2. **Étendre les gates quand on change un format** — fait pour `doc-lint` (cards), à garder en tête
   pour toute future fence déclarative.
3. **Session de correction code↔doc** — le kit porte maintenant ~180 écarts (F1→F183), dont 6
   systémiques. Elle devient plus urgente que les vagues restantes.

## Patterns récurrents (déjà gérés)

- ✅ Réparer les liens entrants AVANT de supprimer une page (leçon vague 4bis appliquée d'emblée).
- ✅ Décompter par le gate, jamais par la liste des agents lancés.
- ✅ Paralléliser des agents sur des périmètres disjoints (jusqu'à 4 simultanés, sans collision).

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `9ac3ed39` | le dépôt ne porte plus que la doc du lecteur (−8 050 lignes)     |
| `8d188f94` | trois pages neuves, revalidées contre le code                    |
| `afb80fab` | un graphe live se pose dans une page, au fil du propos           |
| `33067a4c` | la socket n'a plus qu'une seule vérité                           |
| `d1253064` | réparer les 22 liens vers la doc socket supprimée                |
| `f9c07ded` | les graphes reviennent, dans le propos cette fois                |
| `c2afe56a` | les graphes live disent enfin l'état réel                        |
| `2e38f39c` | les cards d'un hub sont des liens, elles se vérifient comme tels |
| `b300d74a` | le chevron plie, le corps ouvre le hub                           |
| `6c75a251` | les treize hubs deviennent de vraies grilles de cards            |
| `dd46e768` | une card de doc est une surface, pas un lien dans une phrase     |
