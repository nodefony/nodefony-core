---
date: 2026-08-06
session_id: fdac82de-09f9-4620-99ca-f94b36d499af
focus: Bancs WS axes 1+2 en A/B avant/après lots perf — non-régression confirmée
---

# Session retro — 2026-08-06 — ws-bench

## Fait

- **Bancs WS axes 1+2 A/B** : old = worktree `8d2942f3` (avant tout le chantier perf, le flip
  7 fichiers n'était plus viable après F-B), new = HEAD `f6a4e302`. Harnais `tmp/ws-ab.mjs`
  (churn clair / echo+bcast TLS, comptes exacts exigés) + série `tmp/ws-ab-serie.sh` (gardes
  secteur/lowpower/docker/thermal/mds, paires alternées, zéro rebuild inter-run).
- **2 séries** (la 2ᵉ durcie : warmup churn 600, bcast ×3, thermal ≤ 15).
- **Verdict (critère pré-engagé)** : echo BRUIT (+1,0 % paire propre) · broadcast BRUIT (le
  +5,9/+10,7 % de la série 1 s'évapore en série 2, old2 propre à 2 % ≈ les deux new) · churn
  NON CONCLUANT (rampe intra-série, 5/6 paires ≥ 0) → **lots HTTP neutres côté WS**.
- Rapport photo `tmp/ws-ab-report.html` (lib report.mjs, données embarquées).
- Kit perf + MIGRATION_STATUS à jour. CI Windows confirmée verte (item soldé).

## Frictions / coûts

- **Récidive locale fr** dans une garde neuve (`awk printf` → `0,0`) : 11 min de série perdues
  en boucle d'attente sur décor parfait. La leçon était gravée à DEUX endroits — aucun ne
  protège un script écrit from scratch. → bullet RETEX.
- Harnais au scratchpad : ESM ne résout pas `ws` → déplacé dans `tmp/` du repo.
- Churn : métrique à rampe (3 reps ne convergent pas, warmup 600 insuffisant) — gravé au kit.
- Décor initial batterie + lowpowermode auto : la garde de régime a mordu à raison (chiffres
  ×2 à ×10 entre batterie et secteur sur la même machine).

## Recommandations

1. `export LC_ALL=C` en ouverture de tout script de banc — réflexe, pas correctif.
2. Verdict de GAIN churn (si un jour cherché) : reps longues + fenêtre glissante, pas 3×.

## Patterns récurrents (déjà gérés)

- ✅ Critère pré-engagé avant mesure (leçon F-D) : a évité de publier le faux gain bcast série 1.
- ✅ Régime machine constaté jamais déduit (`feedback_bench_machine_regime`).

## Commits produits

| Commit          | Sujet                                    |
| --------------- | ---------------------------------------- |
| (cette clôture) | docs(retex+migration) : verdict bancs WS |
