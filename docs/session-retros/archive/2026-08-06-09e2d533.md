---
date: 2026-08-06
session_id: 09e2d533-2a45-49d0-af62-49636faf58b3
focus: lot perf F-B — URL paresseuse + découpe gardée du request-target, red-team de zone, garde de régime CPU au banc
---

# Session retro — 2026-08-06 (soir) — 09e2d533

## Fait

- **F-B** (`5fa6ee7a`) : `request.url` devient un getter paresseux ; le ctor pose
  `pathname`/`search`/`scheme`/`sUrl` par découpe (`urlFastPath.splitTarget`) quand elle est
  prouvablement l'identité du parse WHATWG, sinon bail-out `new URL` immédiat. Routing et
  firewall lisent `request.pathname`.
- Red-team dédié (14 cas) + équivalence exhaustive par caractère (10 cas), **les deux vus rouges**
  au débranchement, dist vérifié à chaque bascule.
- **Verdict A/B** : +10,5 % sur la paire pleinement valide (0 chevauchement), +6,6 % en moyenne
  des 4 médianes de la fenêtre, +3,8 % sur la fenêtre précédente → retenu **+4 à +10 % (~+7 %)**.
- `bench-ab-mono.sh` (`33172f6a`) : garde de **régime CPU** (`cpu_regime`, `cpuRegime` au JSON),
  éprouvée dans 4 états ; réglage cooldown/durée documenté pour les fenêtres secteur.

## Coûts évidents

- **4 séries A/B pour 1 verdict** : la première invalidée par une bascule batterie→secteur en
  cours de route, la deuxième par le throttling (2 refus sur 4), la troisième exploitable.
  Le poste dominant de la session est l'attente thermale, pas la production de code.
- Le banc ne disait rien du régime CPU : ~40 min perdues à mesurer une machine bridée, puis à
  comprendre l'écart. La garde ajoutée coûte deux appels `pmset` par run.

## Recommandations

1. **Toute grandeur qui change le régime de la machine se fige au 1ᵉʳ run et se re-vérifie** —
   pas seulement la chaleur. Ce qui n'est pas mesuré n'est pas su, et une série peut rester
   parfaitement cohérente en interne tout en étant incomparable à la suivante.
2. **Ne jamais desserrer le seuil de dispersion pour faire passer un verdict** : ajuster la
   cause (cooldown, durée des runs), pas la garde qui protège la décision.
3. **Suspecter son harnais avant son sujet** — l'extraction `sed` qui a « cassé » `cpu_regime`
   était fautive, pas la fonction.

## Patterns récurrents (déjà gérés)

- ✅ Voir rouge tout test neuf, en prouvant le débranchement dans l'artefact reçu (ici le `dist`).
- ✅ Flip A/B sans geste git destructeur (`git show HEAD~1:` + backup scratchpad).
- ✅ Commit du lot AVANT le banc long — le travail ne dépend pas de la survie d'une mesure.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `5fa6ee7a` | feat(perf) — URL paresseuse + découpe gardée (lot F-B) + 24 tests      |
| `33172f6a` | docs(perf) — verdict A/B au migration/kit, garde de régime CPU au banc |
