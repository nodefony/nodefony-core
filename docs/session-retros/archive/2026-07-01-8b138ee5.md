---
date: 2026-07-01
session_id: 8b138ee5-b160-4330-9b0e-06332fbe9dbd
focus: fix bloquant auth prod/cluster (encoders) + observabilité logs cluster/k8s + réparation banc CLI
---

# Session retro — 2026-07-01 — 8b138ee5

## Focus

Chasse d'un bug BLOQUANT (« framework HS en prod et cluster ») → cause = encoders dans un
module dev-only → puis levier observabilité (queryDriver adapté au mode de lancement + garde-fou
k8s multi-pod) → puis réparation du banc de tests CLI cassé par le même pattern dev-only.

## Fait

- **Bug bloquant prod/cluster CORRIGÉ** (`8651f5e7`) : `provisionUsers: passwordEncoder absent`
  → `terminate`. Les `encoders` (dont dérive `passwordEncoder`) vivaient dans `@nodefony/test`
  (`policy:dev`) → absents en prod. Fix framework = défaut Zod `encoders` = argon2id (OWASP).
  Audit exhaustif (sous-agent) : 0 autre brique de cette classe atteignant prod.
- **`queryDriver: auto`** (`83f8b9ff`) : la vue logs s'adapte au mode (mono→memory / cluster-worker
  →cluster-file), sentinelle `auto` aux 3 niveaux (defaults/env/Zod), résolue par `cluster.isWorker`.
  N'agit QUE sur le défaut ; surcharge explicite respectée. Prouvé end-to-end (mono 0 JSONL / cluster 2).
- **Garde-fou k8s multi-pod** (`a0408f10`) : `isInKubernetes()` + NOTICE « vue par pod, configure
  loki/opensearch » (avertir, pas deviner — le cross-pod n'est pas devinable du process).
- **Banc CLI réparé** (`70f74dce`) : `production -w1` gardé (via modules mandatory), `test:batch`/
  `daemon` skip documenté (mécanisme couvert unitairement par KernelLifecycle).

## Frictions / leçons

- Un fix qui compile + tests unit verts ≠ prouvé : 2 bugs cachés (getActiveLogDriver OBJET vs string ;
  warning dans initializeLog trop tôt) révélés SEULEMENT par le boot k8s simulé. Fonction pure = ancre le type.
- « Regarde le flush d'abord » (user) → confirmé dans le code que le flush est INNOCENT (filet anti-perte
  existant + prouvé) avant de coder quoi que ce soit.
- Baseline `git stash -u` + rebuild pour trancher flaky/pré-existant vs son diff (3 tests CLI innocents).
- Même classe de bug 2× (users 06-19, encoders 07-01) → graduée en `feedback_prod_brick_not_in_dev_module`.

## Commits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `8651f5e7` | fix(security) défaut argon2id encoders — auth morte prod/cluster  |
| `83f8b9ff` | feat(log) queryDriver `auto` adapté au mode de lancement          |
| `a0408f10` | feat(log) garde-fou observabilité multi-pod (k8s)                 |
| `70f74dce` | test(cli) répare le banc d'intégration CLI (module test dev-only) |

Tous poussés sur `claude-ts`. Gates : security 766 · core 1754+ · CliIntegration 9 passed/2 skipped.
