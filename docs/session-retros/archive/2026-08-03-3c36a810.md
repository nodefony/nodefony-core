---
date: 2026-08-03
session_id: 3c36a810-4009-4e96-a7c8-3d96ad657261
focus: Pagination — le tri traverse pour les clés d'API et les webhooks, puis la généricité du patron ; scaffold et devkit alignés ; CI réparée
---

# Session retro — 2026-08-03 — 3c36a810

## Fait

- **Geste 2, deux ressources de plus** : clés d'API (mémoire, drizzle ×3 dialectes, mongoose,
  redis) et webhooks (mémoire, drizzle ×3, mongoose). Le banc de contrat partagé gagne son volet
  tri, rejoué tel quel sur chaque backend.
- **Généricité du patron**, sur demande du user : `renameOrderFields` + `pickOrder` +
  `ISortableSource` au cœur, un seul nom `sortableFields()` sur les quatre services, la convention
  Mongo (`id`→`_id`) déplacée chez l'adapter. 3 traductions → 1, 4 redéclarations → 1 interface,
  2 fonctions `ORDER BY` du queryKit → 1.
- **Scaffold + devkit** : le gabarit `create entity` parlait `?sort=-champ` et ignorait un champ
  non triable ; il parle le contrat et refuse en 400. Le skill `add-crud` porte la règle.
- **CI réparée** : 2 violations oxlint, issues de mes commits de la veille.

## Frictions

- **Le patron répliqué 4× au lieu d'être factorisé** — vu par le user, pas par moi. Le fichier de
  vocabulaire qui contient une FONCTION est le signe distinctif.
- **`types` → source, `import` → `dist`** : une constante neuve compile chez le consommateur et
  vaut `undefined` à l'exécution tant que le paquet n'est pas rebâti.
- **`cmd | tail` rend le code de sortie de `tail`** — trois « typecheck exit=0 » annoncés à tort.
- **Un test qui boucle sur une liste vide passe au vert** — trouvé en débranchant, pas en relisant.
- **Le gabarit distribuait l'ancien dialecte** alors que le dépôt en était nettoyé.

## Preuves

Build 20/20 · core 2523 ✓ · http 717 unit + 597 intégration (serveur réel) · security 912 ✓ ·
user 181 ✓ · framework 495 ✓ · drizzle 957 ✓ (PostgreSQL + MariaDB réels, contrat de tri rejoué
sur les 3 dialectes) · mongoose 187 ✓ · oxlint 0.
**Sur l'artefact reçu** : app générée + entité, construite, démarrée — `?order=title:ASC` → 200,
`?order=secret:ASC` → 400 nommant les champs valides.

Débranchements vus rouges : mémoire jetons (5), Mongo `id`→`_id` (3), SQL webhooks (6).

## Non fait / trous ouverts

- Docs non relues : `webhooks.md`, `sessions.md`, MEMORY de http/user/drizzle/mongoose, `docs/guides/`.
- **Audit** = dernière ressource du geste 2 (son store mémoire trie encore en dur).
- `paginate()` d'orm-core ignore toujours `q`.
- **Filtres** : aucun des trois maillons (traducteur, déclaration, refus) — chantier suivant.
- Trouvé, non corrigé : le test généré par `create controller --kind realtime` importe
  `reflect-metadata` sans que l'app le déclare.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `68204d7b` | tri jusqu'au store — clés d'API, 5 backends                        |
| `22326d12` | tri jusqu'au store — webhooks, 4 backends                          |
| `e7722bf6` | réparation du lint de la forge                                     |
| `e3df182b` | une règle se factorise, une donnée se déclare — 4 concepts pour 15 |
| `77ba50c4` | le code généré parle le dialecte du framework                      |
