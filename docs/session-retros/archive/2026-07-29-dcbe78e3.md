---
date: 2026-07-29
session_id: dcbe78e3-f794-4db5-99f8-ded632f89303
focus: le banc DÉCOUVRE enfin des trous — un agent tiers (vibe) lâché dans une app fraîche en trouve trois en un run
---

# Session retro — 2026-07-29 — dcbe78e3

## Fait

- **Kits remis au terrain** (`project_devkit_ai_kit`, `project_scaffold_v2_design_kit`,
  index `MEMORY.md`) : leur description annonçait « PROCHAINE = devkit S (design) », six
  vagues en retard. État confronté au CODE, lot par lot, avec le fait structurant écrit
  en clair : **le module `@nodefony/devkit` n'existe pas** — tout est dans le core et les
  skills du repo, et les 4 lots restants sont précisément ceux qui exigeraient un paquet.
- **Lot 8 (juge extérieur) FIGÉ sur `vibe`** (Mistral CLI, déjà installé) après test de
  faisabilité : modèle `devstral-small`, invocation, axiome `< /dev/null`, format du
  transcript, piège de sonde, pré-vol `probe-agent.mjs` à écrire. GitHub Models **écarté**
  pour ce rôle (aucun outil, aucune boucle) et **retenu** comme endpoint de test gratuit
  pour P12, une fois écrit l'`OpenAICompatibleProvider` qui manque à `@nodefony/llm`.
- **Trois trous de découvrabilité fermés**, tous trouvés par le même run d'agent tiers,
  chacun avec son gate vu ROUGE avant d'être vert.

## Coûts évidents

- Deux conclusions tirées sur un test qui n'avait **jamais tourné** : `timeout` n'existe pas
  sur macOS, et le pipeline rendait exit 0. La seconde fois, c'est l'absence de session dans
  `~/.vibe/logs/` qui a trahi le blocage.
- Un run de 9 min 33 pour 1,07 s de CPU (stdin non fermé) — diagnostiqué comme « le modèle
  réfléchit » pendant ce temps.
- Un diagnostic FAUX annoncé au user avant vérification (« réflexe de modèle ») là où un
  simple comptage tranchait (39 formes nues contre 2).

## Recommandations

1. **Compter avant de conclure** sur un défaut de découvrabilité : la présence d'une phrase
   juste ne dit rien tant qu'on n'a pas compté ce que le fichier montre MAJORITAIREMENT.
2. **Baseline d'abord** : le gain 30 → 6 tours mêle l'effet des corrections et celui du
   décor isolé, faute d'avoir joué la baseline sur le décor final avant de corriger.
3. **Généraliser l'analyseur du banc** avant le prochain run vibe — les sondes ET le lecteur
   de transcript sont liés au harnais Claude Code.

## Patterns récurrents (déjà gérés)

- ✅ Gate vu rouge avant d'être vert, débranchement prouvé par `git diff --stat` (3 fois).
- ✅ Ne pas croire un agent sur parole : son « tests tous verts » a été relancé à la main.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `36ba23e7` | grammaire (défaut + enum) et 409 annoncés dans le gabarit AGENTS.md    |
| `ba95a315` | 57 commandes préfixées `npx` + gate qui compte les formes nues         |
| `26d54935` | le POURQUOI de `npx` : binaire local, global qui délègue mais vieillit |
