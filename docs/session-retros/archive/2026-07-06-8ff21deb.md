---
date: 2026-07-06
session_id: 8ff21deb-cfb4-4662-943c-6d174650adab
focus: Finition stores 0.8 (emplacement .db + brique user) → matrice test memory/sqlite/prod → fixs sécu → observabilité prod Studio
---

# Session retro — 2026-07-06 (3ᵉ) — 8ff21deb

## Focus

Boucler les 2 items « avant Phase 1 » du chantier stores 0.8, puis campagne de test
memory⇄sqlite⇄prod, puis une série de bugs/gaps trouvés en trafic live.

## Fait (9 commits)

- `cbde165f` emplacement `.db` Studio (getter `DrizzleOrm.location`, 7 stores) + **bases SQLite par défaut sous `var/`** (kernel.varDir) ; session+idempotency location résolue lazy au onReady.
- `06e9674d`+`d5f559d7`+`8d56677e` **brique user unifiée** : registre `userStoreRegistry` (memory builtin + drizzle/mongoose au register), puis user = **résolution de store de 1ʳᵉ classe** enregistrée par `provisionUsers` (configured=auto + emplacement + provenance) → suppression de la fabrique front synthétique.
- `c258013e` matrice test memory/sqlite : `stores-location` backend-agnostique + specs skills (load-test « matrice store », framework-dev « gate 5bis »).
- `b855431d` **fixtures in-memory Argon2id** (bcrypt → login prod+memory KO car encodeur legacy dev-only).
- `708f4d66` **wire `NF_TOTP_KEY`** (trou de config : totp.encryptionKey jamais fourni → 2FA off en prod).
- `04acf3ce` **fix DoS `acceptParser`** (en-tête `Accept` malicieux → `new RegExp` throw non catché → crash requête) : échappement+ancrage, ne throw plus. +tests unit & intégration.
- `2b751efb` **ProfilingTab prod** : encart « désactivé en prod → onglet Debug » au lieu du 404 brut.

## Campagne test (tous verts)

Unit 13 packages (core 1806, security 776, framework 429, user 145, drizzle 133, http 543…). Intégration http **567 en sqlite ET memory**. RPS (wrk, baseline `als-test/state`, intra-run) : auth/me **memory 1340 vs sqlite 689** (~1.9×), prod **sqlite 1384 / memory ≈ baseline** (memory ≈ gratuit, sqlite = SELECT sync). Chaos flood 10 min : 1,08 M req, kill/restart encaissés, **0 crash / 0 orphelin / SIGTERM propre**.

## Frictions (→ RETEX.md)

- **Bundle Studio PROD séparé** : `npm run build` ne rebuild PAS le front → prod figé ; il faut `npm run build:front` **+ restart** (manifest lu au boot, 404 sinon). `frontend:build` **silencieux quand rien à rebuild** = « marche pas » trompeur.
- **Dev résiduel bloque le boot prod** : DevSupervisor respawn l'enfant → « collision de ports refusé » ; tuer le parent + par port. `start.sh` kill 5151/5152 → a clobbé le prod du user.
- **RPS honnête** : baseline `als-test/state` (pas `/test/index` HTML), **intra-run** (dérive machine cross-boot).
- **Gotcha** : `*/*` dans un `/** */` TSDoc ferme le commentaire → parse error.

## Commits produits

| Commit                     | Sujet                               |
| -------------------------- | ----------------------------------- |
| cbde165f                   | emplacement .db + bases sous var/   |
| 06e9674d/d5f559d7/8d56677e | brique user 1ʳᵉ classe              |
| c258013e                   | matrice test memory/sqlite + skills |
| b855431d                   | fixtures argon2id                   |
| 708f4d66                   | wire NF_TOTP_KEY                    |
| 04acf3ce                   | fix DoS acceptParser                |
| 2b751efb                   | ProfilingTab prod encart            |
