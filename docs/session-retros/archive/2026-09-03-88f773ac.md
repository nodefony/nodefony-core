---
date: 2026-09-03
session_id: 88f773ac-3d4a-42ad-b88e-e3759f0f556e
focus: vitest 5 + deux alertes high — et trois défauts trouvés en éprouvant la montée
---

# Session retro — 2026-09-03 — 88f773ac

## Focus

Montée de dépendances demandée (2 alertes Dependabot high, vitest 5 sorti le jour
même), éprouvée jusqu'au merge sur `main`. Trois défauts pré-existants tombés en
chemin, un ticket ouvert pour le quatrième.

## Fait

- `fast-uri` 3.1.7 (alertes #705 #706), vitest 4.1.11 → 5.0.0 sur 15 workspaces +
  catalogue scaffold, simplewebauthn 14 (`AuthenticatorTransportFuture` disparu,
  `transports` en `string[]` mutable), Angular 22.1.5, rolldown 1.2.7, mineurs.
- `@angular/build` bloquait l'install (peer OPTIONNEL `vitest ^4`) → override
  scoped `"@angular/build": {vitest: "$vitest"}` + `vitest` déclaré à la racine.
- Réinstall → npm cesse de hisser `@angular/platform-browser` → test du cœur qui
  l'importait sans le déclarer : dépendance implicite déclarée.
- `vitest doctor` : `isolate: false` posé sur http (−51 %) et framework (−39 %),
  validé 2× en ordre mélangé ; cœur REFUSÉ avec causes (`process.chdir` dans un
  thread, `Nodefony.getKernel() === null` exigé par les tests d'attaque).
- Studio « lancer ce fichier » jouait toute la suite (`--` avalé, v4 comme v5) :
  `testRunCommand` pure + test vu rouge ; preuve 1 fichier / 3 tests vs 18 / 204.
- Garde anti-ReDoS `bearer.test.ts` : 4ᵉ flake (ratio entre tailles = hiérarchie
  mémoire) → témoin quadratique sur la même entrée, vu rouge sur témoin, 3/3 vert,
  2/2 sous couverture.
- #164 ouvert (étape externals : PowerShell avale le rouge, test littéralise `/`).
- Preuves : test:all 9 627 verts / 174 sautés (4 infra), gate mémoire 9/9,
  coverage 16/16, forge verte 3 systèmes. Merge `614e617d` sur `main`, PR #162 fermée.

## Frictions

- `sed` sur `$FILES` multi-lignes en zsh : 0 fichier réécrit, vu au `git diff --stat`.
- `gh` a disparu du PATH en pleine session (brew upgrade 2.96 → 2.99) : le monitor
  de forge tournait en silence sur `|| continue` — « toujours en cours » et « outil
  absent » se ressemblent. Reprise par le chemin Cellar, puis retour du lien.
- Mon monitor attendait 6 workflows quand 3 seulement se déclenchent sur un commit
  de test (filtres par chemin) : lu par le user comme « CI HS ».
- `git merge -F -` ne lit pas stdin ; commitlint refuse le type `merge` et un
  en-tête > 100 : remontée de `main` en `chore(main)`, sans `--no-verify`.
- 3 rouges de DÉCOR au coverage-all (timeout sqlite, ×3,98 sous charge + brew,
  dist en cours d'écriture) — tous verts rejoués ; le 2ᵉ run complet 16/16.
- TS 7 : toujours bloqué par `@angular/*` (`<6.1`, même en 22.2.0-next.6) et `typedoc`.

## Commits produits

| Commit     | Sujet                                                                                           |
| ---------- | ----------------------------------------------------------------------------------------------- |
| `e7631464` | chore(deps): vitest 5, simplewebauthn 14, et la dépendance qu'un test n'avouait pas             |
| `d84595bf` | perf(test): http et framework partagent leurs workers — mesuré, pas supposé                     |
| `0d45b86b` | fix(studio): « lancer ce fichier » jouait toute la suite, et répondait vert                     |
| `7a9867eb` | test(security): la garde anti-ReDoS juge un ÉCART, plus un ratio entre deux tailles             |
| `614e617d` | chore(main): remonter claude-ts — vitest 5, deux alertes high fermées, Studio filtre un fichier |
