---
date: 2026-07-27
session_id: f260b8e0-c14c-4160-8bd5-3f2b508e9211
focus: Chantier Windows — 27 tests rouges attaqués par familles, et la cause racine qui empêchait Nodefony de démarrer
---

# Session retro — 2026-07-27 — f260b8e0

## Focus

Chantier Windows (impératif produit). Les tests rouges de `windows-latest` ont été
attaqués famille par famille, jusqu'à la cause racine : le Kernel ne chargeait AUCUN
module sous Windows.

## Fait — 9 commits

| Commit     | Sujet                                                                            |
| ---------- | -------------------------------------------------------------------------------- |
| `853d4c26` | `FileClass` canonicalisait par DEUX voies (`realpathSync` vs `fsp.realpath`)     |
| `628c98b4` | `URL.pathname` pris pour un chemin (décor) + `resolve()` qui ancre sur le cwd    |
| `e283e9a9` | décor de test qui ne s'égalait plus à lui-même (`splitByProject`)                |
| `ee692afd` | `cross-env` (3 scripts) + 4 dépendances fantômes retirées de drizzle             |
| `91ad4667` | **cause racine** — `import()` prend une URL, pas un chemin (`toImportSpecifier`) |
| `65424ecc` | l'arrêt disait « déjà arrêté » pendant qu'un serveur écoutait                    |
| `eacc3d92` | le cycle dev complet (start → recharge → stop) enfin testé hors ubuntu           |
| `69dc5aef` | `status` rendait un rapport vide au moment où l'on a besoin de lui               |
| `fefda81a` | la disponibilité de `ps` se CONSTATE, elle ne se déduit pas                      |

## Ce qui a coûté

- **Les logs CI ne montraient pas les erreurs du core** : turbo streame le paquet et coupe
  son bloc « Failed Tests ». Le paquet le plus rouge était celui qu'on ne pouvait pas
  lire. Débloqué par accident, en réparant `cross-env` : le job `test:boot` streame, lui.
- **Deux runs de suite core relancés sans capture** → un échec flake perdu, jamais
  identifié. Le fichier de log aurait coûté trois caractères.
- Une dérive de `cwd` (shell resté dans `src/nodefony`) a fait échouer un `npm run lint`
  et cru à une régression pendant un tour.

## Recommandations

1. **Ne jamais déduire une capacité de la plateforme** — la constater. Vu 3× dans la même
   session, et le pari était faux jusque hors Windows (`procps` absent des images minces).
2. **Un test qui échoue peut en masquer plusieurs** : vérifier les `skipped` autant que les
   `failed` quand un compteur s'améliore d'un coup.
3. **`utimesSync` pour réveiller un watcher** — aucun diff laissé derrière.

## Patterns récurrents (déjà gérés)

- ✅ Voir chaque filet ROUGE avant de le poser (appliqué 6 fois cette session).
- ✅ Annoncer ce qui n'a PAS été lancé dans chaque message de commit.

## Commits produits

Voir la table ci-dessus — branche `claude-ts`, poussés au fil de l'eau.
