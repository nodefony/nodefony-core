---
date: 2026-06-05
session_id: 4f43b1a5-e8cf-4871-b774-ad14bf3cf4b6
focus: arrêt gracieux (signaux/WS 1001) + chantier perf pipeline HTTP (router-first +28%)
---

# Session retro — 2026-06-05 — 4f43b1a5 (6e, PERF)

## Fait

- **`2fa2123`** arrêt gracieux : `Cli.signalHandler` idempotent (2e signal = exit forcé) + WS close **1001** (au lieu de 1006). Test unit Cli 93/93. Symlink `docs/migration/MIGRATION_STATUS.md`.
- **`7ffd5cf`** perf(http) : **router-first** (static en fallback façon Express) = **+28% RPS** ; fix bug prod (gardes dev-only `"prod"`≠`"production"` → Profiler/timing exposés en prod, sécu+perf) ; fireAsync 0-listener (neutre). Gates : intégration 405 · memory 9/9 · unit 346.
- Mémoires : `reference_perf_profiling_method` (méthode), `project_perf_candidates_remaining` (kit #2/#4/#5).

## Diagnostic structurant (survit au /clear)

- **Nodefony = 11× un `node:http` nu** (10k vs 111k RPS, même machine). Le goulet = **pipeline par requête**, prouvé en éliminant machine/loopback/TLS(≈0% avec keep-alive)/client/Loki.
- **Static-first était le « loupé »** (chaque requête API payait serve-static). Router-first = le fix.
- **Loki bride −42%** (le push « async » n'est pas gratuit sous charge).

## Frictions (coûts)

- **Banc d'essai parasité ×3** : (a) `NODE_ENV=development` hérité → `production` bootait en dev+Vite+throttle (~2000 RPS au lieu de 6000) ; (b) process **Vite orphelins** (title `nodefony-core`) échappent au `pkill -f bin/nodefony` et survivent ; (c) mono fantôme. → mesures fausses jusqu'à NODE_ENV=production forcé + kill par PORT + vérif PID.
- **node --prof macOS** : C++ à 59% = faux symbole (`BlobSerializer`), pas un hot spot → lire `[JavaScript]`/bottom-up.
- **#3 (fireAsync) mesuré inutile** : gain noyé dans le bruit mono ±5% → 1 candidat abandonné après mesure (bonne discipline, mais coût de mesure).

## Commits produits

| Commit  | Sujet                                                   |
| ------- | ------------------------------------------------------- |
| 2fa2123 | fix(core,http) arrêt gracieux signaux + WS 1001         |
| 7ffd5cf | perf(http) router-first +28% + fix gardes dev-only prod |
