---
date: 2026-06-11
session_id: df3aa140-4d98-4687-afbc-d212b1446981
focus: Vague 5 robustesse RFC + contrat retours controller + durcissement Container (A/B)
---

# Session retro — 2026-06-11 — df3aa140

## Focus

Suite directe de la clôture V4 (même journée, après /clear « reprends »). Trois chantiers
enchaînés sur `poc/api-souveraine` : (1) Vague 5 robustesse RFC du cycle requête, (2) audit
complet des cas de retour d'action controller, (3) lot perf de durcissement du DI Container.

## Fait

- **V5 robustesse RFC** — R1 Range 416/ignore/clamp (RFC 9110 §14, `parseByteRange` pure exportée),
  R2 teardown blindé (anti-unhandledRejection + anti-fuite scope DI), R3 Host 421 Misdirected,
  R4 broadcast WS binaire préservé, R5 ReadStream destroy client parti, P7 aplatir new Promise(async).
- **Contrat retours controller** (demande user) : scalaires number/boolean → auto-JSON (RFC 8259 §2),
  Buffer → envoi direct (typeOf="buffer"), corps vide "" → 200 (anti ERR_STREAM_NULL_VALUES).
- **Durcissement Container** (demande user, A/B + gate mémoire) : protos parents adoptés, uuid→seq
  base36, Map scopes lazy, API `scopeCount()`. +6 % RPS A/B (paires alternées), 4 tests anti-data-race.
- **Bonus audit** (pendant P7) : hang `super.send` http2 jamais résolu, throw post-reject avalé.

## Frictions marquantes (détail → RETEX.md)

- 🚨 Fausse régression perf : « 7000 → 3674 » = charge machine (Brave 45%+36%), PAS le code.
  Réfutée par checkout pré-V5 + rebench MÊME fenêtre = 0,3 % d'écart. Un RPS absolu ne se compare
  jamais inter-fenêtre.
- typeOf(Buffer)="buffer" pas "object" → case dédié obligatoire dans returnController.
- Adoption proto parent dans Scope → set/remove own-only OBLIGATOIRE sinon data race cross-requêtes.

## Patterns récurrents (déjà gérés)

- ✅ A/B paires alternées intra-fenêtre (`bench-ab-mono.sh`) — méthode appliquée pour PROUVER ET RÉFUTER.
- ✅ Edit exige Read préalable (jamais sed/grep pour un fichier qu'on va modifier).
- ✅ Gate mémoire `test:memory` + `test:load` avant commit pipeline/core.
- ✅ Commits FR par lot (`git ci -F`), header ≤100 car (1 rejet commitlint sur c4, corrigé).

## Commits produits

| Commit  | Sujet                                                              |
| ------- | ------------------------------------------------------------------ |
| 023fd5e | fix(framework,test) V5 R1+R5 — Range RFC 9110 + destroy ReadStream |
| fd28a82 | fix(http,test) V5 R2+R3+R4 — teardown blindé, Host 421, WS binaire |
| 1aaa6f2 | refactor(http) V5 P7 — aplatir new Promise(async) restants         |
| 044df1d | feat(framework,http,test) contrat retours controller               |
| 60f9188 | docs(migration) V5 livrée                                          |
| 1c9ebe1 | perf(core) durcissement Container hot path (+6 % RPS A/B)          |
| 6caeb91 | docs(migration) Container livré                                    |

## Gates

framework 226 ✓ · http unit 445 ✓ · http intégration 438 ✓ · core 1570 ✓ (75 Container) ·
load+memory 27 ✓ · A/B Container +6 % (paires alternées).
