---
date: 2026-05-29
session_id: 211b3627-2642-4621-a14d-3ae289ba0ad1
focus: C2 — purge des `any` du cœur DI/events (Event + injector), durcissement core
---

# Session retro — 2026-05-29 — 211b3627

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Read       |    47 |
| Bash       |    32 |
| Edit       |    20 |
| Skill      |     2 |
| Write      |     1 |
| ToolSearch |     1 |

## Top fichiers Read/Edit

| Fichier                          |   × |
| -------------------------------- | --: |
| /tmp/full_build.log (Read)       |  11 |
| task outputs (build/server/test) | ~18 |
| Event.ts (Edit)                  |  10 |
| injector.ts (Edit)               |   7 |

## Coûts évidents

- **Polling de build/serveur en arrière-plan** = le gros poste. 11 `Read` de
  `/tmp/full_build.log` (qui contient le dump 1-ligne géant des ~400 entités
  dolibarr du module test → fichier énorme relu entier) + ~18 lectures de fichiers
  de tâches, dont plusieurs « Wasted call » (fichier inchangé).
- 109 tours : beaucoup étaient des attentes de build portant tout le contexte
  (CLAUDE.md + MEMORY.md + skills) à chaque tour → cache chaud mais répété.

## 💶 Coût (€)

- Total **~€37,6** (≈ $40,8) sur 109 tours. Décompo : cache-read $25,6 (62 %),
  cache-write 1h $10,2 (25 %), output $4,6 (11 %), input $0,45 (1 %).
  **Cache-dominé à ~87 %** — c'est le contexte porté pendant les attentes de build,
  pas l'output, qui coûte. Coût/livrable élevé pour 1 commit car beaucoup de tours
  d'attente.

## Recommandations

1. **Ne jamais `Read` `full_build.log` en entier** — le module test y dump une
   ligne géante (~400 entités). Toujours `grep -E "TS[0-9]{4}|error|created|exit="`.
   Gain : la lecture #1 coûteuse de la session.
2. **1 seul waiter `until grep "exit="` par build/serveur**, puis attendre la
   notification SANS re-lire les fichiers de tâche entre-temps (chaque re-Read
   inchangé = « Wasted call » pur). Pattern adopté en fin de session, à appliquer dès le départ.
3. **C2 = motif réutilisable** : purger `any` cœur = (a) générique `<T extends Base = Base>`
   pour les retours de fabrique DI (pas `unknown` → casse les `const x: Base =`),
   (b) `unknown` + cast aux frontières internes, (c) garder + DOCUMENTER les `any`
   imposés (mixin TS2545, augmentation globale TS2717). À capitaliser pour les
   prochains domaines (http/framework/orm).

## Patterns récurrents (déjà gérés)

- ✅ Fail intégration « requires server » = serveur down (ECONNREFUSED), pas une
  régression heap → start.sh puis re-run (skill nodefony-debug pattern #3).
- ✅ Turbo rejoue les logs cachés (warning core périmé d'avant le fix Resolver).
- ✅ `.git/index.lock` orphelin → `scripts/safe-commit.sh`.
- ✅ Refactor d'un type de retour → grep les consommateurs prod AVANT (Kernel/Module/Resolver).

## Commits produits

| Commit  | Sujet                                                               |
| ------- | ------------------------------------------------------------------- |
| 6245c2c | refactor(core): purge any du cœur DI/events — Event + injector (C2) |
