---
date: 2026-08-31
session_id: feb2a0db-2099-4157-afed-791f13e143c6
focus: grammaire de chaîne par moteur (#119), sonde du banc corrigée, puis quatre causes de CI rouge dont une perte silencieuse de contrainte
---

# Session retro — 2026-08-31 — feb2a0db

## Fait

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `98cf2a13` | #119 — grammaire de chaîne par MOTEUR, deux scanners jumeaux fusionnés |
| `64b28488` | banc — la sonde ne punit plus l'agent qui range sa copie d'essai       |
| `093fec1c` | MEMORY du module recalé (`indexHorsChaine` n'existait plus)            |
| `e083255e` | référence de la tâche 33 recalée (FAIL 2/3, médiane 40 tours)          |
| `59bdda35` | CI — cycle d'imports, `ReferenceError` sur `MIGRATE_URL_ENV`           |
| `1d3794f3` | CI — passe filtrée qui n'énonçait pas ce qu'elle n'exerce pas          |
| `a0bf7a08` | CI — condition du refus MariaDB POSÉE au lieu d'héritée                |
| `9866fa98` | CI — le banc constate le refus d'adoption et fait le geste prescrit    |
| `5a16b877` | CI — la commande atteinte par son CHEMIN, jamais par `npx`             |
| `1d93fe80` | #124 — les contraintes d'unicité survivent à l'adoption d'une base     |

Tickets : **#119 fermé**, **#124 ouvert PUIS fermé dans la foulée**, **#123 ouvert**,
#118 et #122 commentés.

## Coûts évidents

- **Quatre causes distinctes derrière « la CI est rouge »**, dont trois invisibles l'une
  de l'autre : un rouge en masque un autre, et le compte de jobs ne dit rien du nombre de
  causes. Trois lectures de journal (46 911 · 7 112 · 447 lignes), les deux grosses
  déléguées en `haiku`.
- **Un banc validé en `--no-e2e` puis poussé** : la forge est tombée sur l'étape que ce
  drapeau saute. Un aller-retour complet perdu.
- **Deux erreurs dans mon propre diff**, rattrapées avant mesure : un déplacement d'étape
  qui aurait fait entrer des entités PostgreSQL dans la migration, et une option
  `toleratedExit` que la fonction appelée n'a jamais eue.
- Un ré-export sans import local : **140 tests unit verts**, seul le typecheck l'a vu.

## Recommandations

1. **Ne jamais pousser sur une validation dont on vient d'écrire « non lancé : X ».** La
   phrase protège le lecteur, pas le dépôt.
2. **Instruire un run rouge avant de le rejouer** — deux faux verdicts de sonde trouvés
   ainsi, dont un qui punissait le geste prescrit par le produit.
3. **`gh project item-list` ne sert qu'au coup d'œil** : 1 item rendu sur 5 demandés ici.
   GraphQL avec pagination pour décider — le tableau compte 106 items.

## Patterns récurrents (déjà gérés)

- ✅ suspecter son propre diff avant de qualifier un échec de pré-existant
- ✅ capturer la sortie ENTIÈRE puis filtrer (jamais `tail`)
- ✅ un gate neuf se voit ROUGE une fois — ici dans les DEUX sens pour un routage

## Commits produits

10 commits, poussés en trois fois (`8dcbcf60..9866fa98`, puis `..1d93fe80`).
