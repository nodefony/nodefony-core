---
date: 2026-06-07
session_id: 13949b6c-b970-4158-b4a5-86b6ba5008ed
focus: Forwarded RFC 7239 (#2/#5) + banc reverse-proxy Docker nodefony.com (E2E)
---

# Session retro — 2026-06-07 — 13949b6c

## Fait

- **#2 + #5 Forwarded RFC 7239** (`1a2764e`) : module `context/forwarded.ts` (parser §4 quote-aware,
  `forwardedNodeIp` §6, `resolveForwarded` = résolution canonique Forwarded>X-Forwarded-\*, priorité
  proto>scheme = #5). Câblé HTTP/HTTP2/WS. Perf hot path prod (lazy 0-alloc, fast-path mono-proxy 0 array).
  Gates : unit 410, mémoire 9/9, intég 417/418. **Les 5 trous forwarded bouchés.**
- **Tests méthodes** (demande user) : `forwardedWiring.test.ts` (getFullUrl/getRemoteAddress HTTP/HTTP2/WS) +
  `resolveFromRight` direct, en plus des helpers purs.
- **Host nodefony.com** (`b2dfce1`) : `http.trustedHosts` (config app, pas le core).
- **Banc Docker** : nodefony.com de bout en bout (`715b45a`), haproxy re-encrypt HTTPS + note chaîne de
  certif (`8f6b171`), nginx edge écrase XFF (`df33111`), opt-in `NF_BIND_ALL` (`2613961`).
- **E2E validé** (avant arrêt du démon Docker) : #2 parse (https + IP from-right), #5 priorité, re-encrypt
  haproxy 200, anti-spoof edge (6.6.6.6 jeté → IP réelle gateway).

## Frictions (→ détail dans RETEX.md)

- Tester les MÉTHODES finales d'extraction, pas que les fonctions pures (recette Object.create proto).
- « hot path prod » = chemin DERRIÈRE proxy (cas réel), pas le fast-exit sans proxy.
- Banc anti-spoof : append + client trusted = faux spoof → edge ÉCRASE le XFF (RFC 7239 §8.1), trustProxy étroit.
- bind-mount macOS : `docker exec -t` lit le fichier en cours d'écriture → revalider sur disque + recréer conteneur.
- `000` partout = démon Docker tombé, pas un bug du code (`docker ps`).
- /etc/hosts (côté user) : IP en 1ʳᵉ colonne (`127.0.0.1 nodefony.com`), inversé 2×.

## Reste — prochaine session

1. **Banc TLS complet** : cert SAN=nodefony.com + `ca-file`/`verify required` + `sni` (au lieu de verify none) ;
   `proto=https` end-to-end (client TLS vers le proxy).
2. **Backlog forwarded** : `nodefony proxy:generate nginx|haproxy` (P11.7, ajouté au dashboard) + `static.enabled`.

## Commits

| Commit  | Sujet                                         |
| ------- | --------------------------------------------- |
| 1a2764e | feat(http) Forwarded RFC 7239 (#2/#5)         |
| b2dfce1 | chore(config) host nodefony.com               |
| 715b45a | test(docker) banc par nom de domaine          |
| 8f6b171 | test(docker) re-encrypt haproxy + note certif |
| df33111 | fix(docker) nginx edge écrase XFF             |
| 2613961 | feat(config) NF_BIND_ALL opt-in               |
