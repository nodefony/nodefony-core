---
date: 2026-09-08
session_id: c15f9d7e-7062-4ae0-a26b-f29c8a1fd92b
focus: Le pilotage remis d'aplomb, #274 et #271 fermés, et un audit de la config qui a ouvert 11 tickets
---

# Session retro — 2026-09-08 — c15f9d7e

## Focus

Reprise qui commence par un faux verdict — un ticket `beta` annoncé alors que neuf `alpha`
restaient ouverts — puis remonte la chaîne : l'instrument, la règle du choix, deux tickets fermés,
et un audit de la configuration d'application dont rien n'existait par écrit.

## Fait

- **Le pilotage réparé en trois étages** (`aee1293b`) : le gate anti-`item-list` étendu à toute la
  prose des skills (vu rouge, un seul site nommé) ; l'empreinte `.ai/BOARD.md` promue voie de
  lecture au lieu de repli hors ligne ; et surtout `chooseNextTicket` — le prochain ticket vient du
  **jalon courant**, plus du rang global. #274 ne sortait que par chance d'ordre.
- **#274 fermé** (`89f78497`) : le ticket se trompait de cause. Ce n'était pas les gabarits — le
  `.tpl` était lu et testé — mais un **trou de vocabulaire**. Sondé sur 48 mots français du dépôt,
  le dictionnaire en ignorait 26 ; 21 ajoutés, qui ont immédiatement trouvé 4 identifiants français
  en production, dont un type EXPORTÉ par le cœur.
- **#271 fermé** (`6dda2c28`) : l'écran de connexion affiche tout fournisseur configuré. Le piège,
  vu avant d'écrire : `listProviders()` sert AUSSI de garde d'autorisation — masquer en le retirant
  de cette liste aurait désactivé le flux.
- **`config/` racine supprimé** (`62cfcc12`), config OAuth ramenée dans le manifeste.
- **11 tickets ouverts**, dont 9 sous deux parents (#288, #289) qui portent le raisonnement des
  deux audits — il ne vivait nulle part ailleurs.

## Frictions (versées dans RETEX.md)

- La voie FIABLE reléguée en repli : l'empreinte nommait le bon ticket pendant que la commande
  cassée nommait l'autre.
- Un gate ne protège que le périmètre qu'il balaye — celui d'`item-list` couvrait les scripts, la
  commande interdite vivait dans un bloc de skill.
- Un décor de démonstration posé dans un fichier qu'un sous-agent analysait.
- La question du user reformulée de travers → un audit argumente contre une option que personne ne
  proposait.
- Mon commit a périmé quatre ancres de doc que je n'ai pas recalées en fermant → #290.

## Coûts évidents

- Deux audits `fable` (184 k et 286 k tokens de sortie) — justifiés : arbitrage de conception, pas
  du comptage. Le second a rendu D1, un défaut de typage ACTIF sur 4 modules.
- Trois faux diagnostics de shell rattrapés en les rejouant : `grep --include` empilés rendant vide,
  `ls -d a b` dont le code d'erreur déclenche le `||`, et un `tsc -p` ciblé sur un tsconfig qui
  n'incluait pas le fichier mesuré.
- Session très longue (3 chantiers) — le `/clear` a été proposé deux fois.

## Recommandations

1. **#281 en premier** (P0) — le registre typé est inactif pour 4 modules : une clé mal
   orthographiée y compile, puis Zod la retire en silence.
2. **Ne pas rouvrir la question du découpage `nodefony/config/`** avant #284 : quatre instruments
   lisent le TEXTE du manifeste, `doctor` deviendrait aveugle sur son rapport de sécurité.
3. Contrôler un audit délégué AVANT de le rapporter — sur le premier, une affirmation exacte
   (« ni le build ni le typecheck ne globbent ») m'a fait écrire un tableau faux.

## Commits produits

| Commit     | Sujet                                                                  |
| ---------- | ---------------------------------------------------------------------- |
| `aee1293b` | le prochain ticket vient du jalon COURANT, empreinte = voie de lecture |
| `89f78497` | le contrôle de langue ignorait `dialecte`, pas les gabarits (#274)     |
| `6dda2c28` | afficher tout fournisseur de connexion configuré (#271)                |
| `16630904` | empreinte après fermeture de #274 et #271                              |
| `62cfcc12` | ramener la config OAuth dans le manifeste et retirer `config/`         |
| `7864fa2d` | empreinte après les 7 tickets issus de l'audit                         |
| `ab5bd24d` | regrouper les tickets sous deux parents                                |
| `68afbe4c` | trois tickets manquants du premier audit                               |
