---
date: 2026-07-30
session_id: 02bfbd3d-41bd-4d10-bb5d-9fe79580adf4
focus: comparaison de poids de modèle sur le banc devkit, puis mise à jour des dépendances
---

# Session retro — 2026-07-30 — 02bfbd3d

## Focus

Répondre à « un gros modèle tourne-t-il moins en rond ? » (priorité 1 du kit devkit), puis
appliquer la mise à jour des dépendances en retard.

## Fait

- **12 runs du banc de découvrabilité**, décor isolé, empreinte d'arbre constante :
  - T14 (socle) × 3 en modèle fort, sur gabarit AFTER **et** BEFORE — la variable isolée par
    `git apply -R` du seul hunk, pas par retour au fichier entier ;
  - T1 (générateur) × 3 en léger et × 3 en fort.
- **Résultat central** : un générateur abaisse le poids de modèle nécessaire (T1 : écart nul,
  ×3 de coût ; T14 : le léger échoue 1 fois sur 2). Écrit dans le gabarit `AGENTS.md` (point 2 de
  la méthode de travail), le skill du banc, le kit et `MIGRATION_STATUS`.
- **Corollaire** : le modèle fort franchit indifféremment les deux gabarits (il ouvre la doc du
  controller 6/6, le léger 0/4) → un banc en modèle fort aurait déclaré l'app saine.
- **Surface réservée de `Service`** : tranchée (ne rien faire) après avoir LU le message réel du
  compilateur — `TS2416` et le `TS2322` de `@services([X])` nomment déjà la méthode fautive.
- **Doc CLI** : `check|doctor`, `inspect`, `env` absentes de toute page ; `create` annoncé à 5 types
  pour 7 ; 5 ancres périmées (dont 2 trouvées par `anchor-check`, pas par moi). 38/38 ancres OK.
- **Deps** : 4 lots sur 5 (rolldown, patchs runtime, vite+plugin-react, tabler). Angular refusé.

## Coûts évidents

- ~17 $ pour les 6 runs T14 + ~3,5 $ pour les 6 runs T1. Conforme à l'estimation (« ordre de 20 $ »).
- Un run T14 tué et un décor monté pour rien, à cause du `trap` sans `exit` (~4 min, 0 token d'agent).
- Une commande expirée à 2 min (regex sur une ligne de 34 KB).

## Recommandations

1. **Mesurer T1 dans les deux poids était l'expérience à faire en premier** — elle donne l'argument
   produit, là où T14 seule ne donnait qu'un constat de coût.
2. **Toute série de runs comparatifs commence par relever l'empreinte de l'arbre** et la revérifie
   à la fin ; sans ça la comparaison n'est pas défendable.
3. **Un gate du dépôt trouve ce qu'une relecture rate** : `anchor-check` a nommé 2 ancres périmées
   que je n'avais pas vues en éditant la même page.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter l'instrument avant le sujet (3 occurrences ce jour, toutes dans mes propres sondes).
- ✅ Médiane ≥ 3 runs — appliquée sans discussion cette fois.
- ✅ Arrêt après 3 échecs identiques (ERESOLVE Angular) → revert + `BUG_REPORT`.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `2b70fc4b` | `chore(deps)` rolldown 1.2.1 (build complet + gate mémoire 9/9)    |
| `6ddc2c83` | `chore(deps)` figlet, jose (21/21 attaques JWT), better-sqlite3    |
| `5e2fc8dd` | `docs(cli)` trois commandes livrées absentes de la doc, 5 ancres   |
| `03110761` | `feat(agents)` un générateur abaisse le poids de modèle — mesuré   |
| `068b88e6` | `chore(deps)` vite 8.2.0 + plugin-react, 12 déclarations d'un bloc |
| `6b7f7d26` | `chore(deps)` tabler 3.46.0 ; Angular refusé → BUG-2               |
