---
date: 2026-06-29
session_id: 6890472a-5c14-4f94-a11b-3daddfd4937f
focus: Debug runtime allumable à chaud — back (gate par-module + TTL + wildcard *) + front Studio (chip + panneau Logs▸Debug "intelligent")
---

# Session retro — 2026-06-29 (2ᵉ session) — 6890472a

## Focus

Chantier **debug-runtime** ([[project_runtime_debug_toggle_kit]]) : un user du framework
doit debugger un incident **sans redéploiement**. Réalisé back + front, prouvé e2e.

## Fait (6 commits poussés `claude-ts` `631f278a`→`32bca74c`)

- **CORE `Syslog`** : gate par-module au point d'entrée (clé = `msgid`), lazy null = 0 coût
  hot path (preuve blanc-box) ; `setDebugOverride/clear/getDebugOverrides` ; **auto-extinction
  TTL** (timer unref, expiry exposée) ; **wildcard `*`** (debug tout, réutilise le TTL) ;
  `severityFromInput` (validation stricte) ; `parseDebugSpec` (env `NF__DEBUG` au boot).
- **FRAMEWORK** : `GET/PATCH /nodefony/kernel/api/log/level` prod-safe (RBAC, par module ou `*`,
  niveau validé 422, TTL imposé 15min/plafond 60min, audité). Banc e2e `debug-runtime-e2e.mjs` 10/10.
- **STUDIO** : chip top-bar `DebugRuntimeChip` (rouge, breathe opacité reduced-motion, HoverCard,
  clic → deep-link `?tab=debug`) ; onglet **Logs▸Debug** (zone danger) avec **carte par debug actif
  = contrôle + son flux de logs filtré (`msgid`) qui défile dedans** (couplage action↔observation),
  plein écran, recherche, chips sévérité, clear par carte ; Autocomplete (msgid suggérés des logs).
  Règle « champ live = contrôle présent » → `queryDriver` éditable inline dans le panneau Config.
- **Finding profil (user)** : **2 SELECT session identiques / requête** (+ 1 SELECT User) →
  levier ORM (lecture session non mémoïsée per-req). Prochaine cible.

## Frictions (→ RETEX.md, par thème)

- **Build/boot** : éditer le BACKEND (core) en session de test live = rebuild turbo 38-66 s →
  serveur DOWN → **déconnexion + perte des overrides in-memory + login échoué** (vu 3×). Batcher
  les edits back / prévenir. Ironie : le debug-runtime existe pour éviter ça.
- **Front/HMR** : HMR ne hot-swap pas l'ajout de hooks → composant stale → hard-reload.
- **Front/UX** : contrôle sans observation = « il se passe rien » ; champ « live » = contrôle présent.
- **Git** : `.gitignore logs/` capture `routes/logs/` → `git add -f` les nouveaux .tsx.

## Commits produits (poussés)

| Commit   | Sujet                                                           |
| -------- | --------------------------------------------------------------- |
| 631f278a | feat(core): debug runtime par-module à chaud                    |
| fc25d268 | feat(studio): onglet Logs▸Debug + chip + wildcard * + countdown |
| 3af660b2 | feat(studio): chip HoverCard + deep-link ?tab=debug             |
| 1cf514ad | fix(studio): deep-link nettoie ?tab                             |
| 2ed17953 | feat(studio): carte par cible avec flux filtré                  |
| 32bca74c | feat(studio): carte — plein écran + recherche + chips + clear   |
