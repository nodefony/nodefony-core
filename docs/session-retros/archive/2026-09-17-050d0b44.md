---
date: 2026-09-17
session_id: 050d0b44-093d-4e98-aad1-79a7a26d7258
focus: "#394 — l'argument annoncé par inspect, puis la CI : 6 jobs unix, 3 Windows et le banc de tenue"
---

# Session retro — 2026-09-17 — 050d0b44

## Focus

Reprendre après `/clear`, trancher deux restes du 09-17c, faire #394 — puis, sur la question
« la CI est-elle verte ? », instruire les rouges au lieu de les constater.

## Fait

- **#394 fermé** (`df73becf`) : `inspect routes auth` filtre enfin (370 → 22 routes, dont
  `POST /nodefony/security/api/auth/login`) ; une cible donnée à un sujet qui n'en consomme
  aucune est REFUSÉE en la nommant (exit 64) ; l'aide de `[cible]` est dérivée du registre. Le
  prédicat de recherche n'a pas été réécrit — celui de `routes/page` est extrait et partagé.
- **Trois tickets ouverts** : #405 (le banc ne lit pas le journal d'un agent qui ne l'écrit pas
  sur sa sortie), #406 / #407 (les 23 règles de dev triées, qu'aucune page ne porte).
- **Les 23 règles RÉCUPÉRÉES sans rejouer les tris `fable`** — extraites du journal des
  sous-agents de la session précédente, archivées dans
  `core-dev/audits/tri-savoir-transferable-apps-2026-09.md`.
- **La CI de `dev` passée de 9 jobs rouges à 3, puis instruite jusqu'au bout** :
  - `a397ba78` — le seuil de taille du `CLAUDE.md` généré : cause commune des 6 rouges unix.
  - `b1f53da8` — les 3 rouges Windows : le plan du `dry-run` mélangeait deux grammaires de
    chemin. Règle éprouvée sur `path.win32` ET `path.posix` depuis un poste macOS.
  - `55cd6051` — le banc de tenue : plafond dynamique qui empêchait le workflow de compiler
    (9 runs, 0 job), et `paths` qui ne bornait rien. `push` réduit à `main`.
- **`3c6066c9`** — le skill livré aux applications dit désormais que l'inspection accepte un
  motif, et qu'on ne tronque jamais sa sortie.
- **#405 fermé** (`d941d128`) : le banc lit le journal LÀ OÙ l'agent l'écrit. Mesuré sur un run
  réel de `copilot` — sortie standard **4 octets** (la garde arrêtait la passe), journal réel
  **31 646 octets / 1 tour**, et **6 sessions antérieures** correctement écartées.
- **`2490a8ae`** — ma propre régression : la correction des chemins avait fait tomber une
  assertion qui LITTÉRALISAIT son attendu. Le verdict de la forge l'a montré, et il a montré
  aussi que `create.test.ts` était passé au vert sous Windows.

## Coûts évidents

- **Un sous-agent `haiku` sur un log de 7 900 lignes** — le bon geste (85 k tokens chez lui, pas
  chez moi), mais son résumé a mal lu le diff (il a rangé des écarts de POSITION parmi les
  écarts de contenu) : la cause exacte s'est vue en ouvrant le bloc moi-même.
- **Deux investigations CI à l'aveugle** : ni l'API ni `gh` ne rendent le message d'erreur d'un
  workflow qui ne compile pas. Le diagnostic est venu d'une bissection sur l'historique des runs
  (`jobs=1/success` jusqu'à telle heure, `jobs=0/failure` après), pas d'un journal.
- **Un `git stash list` refusé par la garde du dépôt** (arbre sale) — le motif attrape le mot,
  pas l'intention. Coût : une commande, contournée autrement.

## Recommandations

1. **Le rouge Windows a vécu deux jours parce qu'aucun test ne mord sous Linux.** Le levier
   « injecter la grammaire » est écrit dans le `CLAUDE.md` ; il n'a servi qu'après coup.
2. **#405 reste ouvert et le mérite** : le banc devkit ne lit toujours pas le journal de
   `copilot`, donc le run le plus démonstratif reste classé « aucun tour d'assistant ».
3. **Les 23 règles `DOC` sont maintenant dans le dépôt de mémoire**, plus dans un transcript.
4. **Une correction peut DÉPLACER son défaut d'un cran** : rendre une liste native a cassé le
   banc qui l'assertait en dur. Le verdict de la forge l'a dit en douze minutes — c'est le seul
   endroit où la règle Windows se constate, et il ne faut pas attendre pour l'y lire.
5. **Un `--prove` ne mute que ce qu'il ATTEINT** : les cas passant par une fonction qui appelle
   la règle en interne restaient verts débranchés. La règle a dû devenir injectable pour que
   leur vert veuille dire quelque chose.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff avant de dire « pré-existant » — fait sur les 3 rouges Windows,
  puis prouvé par l'historique des runs.
- ✅ Un gate neuf se voit ROUGE une fois — trois fois, et les trois ont mordu.
- ✅ Prouver sur l'artefact reçu — `npx nodefony inspect` relancé sur le `dist` rebâti.

## Commits produits

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `a397ba78` | fix(scaffold): recaler le seuil du pointeur d'agent sur sa taille réelle |
| `df73becf` | fix(cli): faire filtrer inspect routes par l'argument annoncé            |
| `bc7a3f65` | chore(pilotage): rafraîchir l'empreinte du tableau de bord               |
| `3c6066c9` | docs(devkit): dire au skill livré que l'inspection accepte un motif      |
| `55cd6051` | fix(ci): rendre au banc de tenue son déclenchement, et le sortir de dev  |
| `b1f53da8` | fix(scaffold): composer les chemins du plan dans UNE seule grammaire     |
