---
date: 2026-06-20
session_id: 3fd6621d-2b8c-4aa2-8bb5-b405d62d552e
focus: Red-team Firewall/WS data plane — DERNIÈRE brique du cœur P6 (CSWSH + identité ALS + confinement) + 1 finding fermé
---

# Session retro — 2026-06-20 (7) — 3fd6621d

## Focus

Red-team de la dernière brique du cœur P6 (Firewall/WS data plane), méthode 2 passes
(threat-first puis code-first), via `nodefony-security-review` mode RED/BLUE-TEAM.

## Fait

- Push initial des 25 commits accumulés (DevSupervisor DX) → origin à jour.
- **Passe 1 e2e** (`ws-data-plane-attack.test.ts`, 11/11) : anti-CSWSH (Origin spoofing
  cross-site/suffixe/userinfo/null + cookie → 1008), identité figée ALS (roles/token forgés
  dans api.request → 403), confinement api.request (route hors plan/traversée/path non-string).
- **Passe 2 unit** (`frameAuthorizer.attack.test.ts`, 6/6) : **FINDING** = plancher canal
  système contournable par la CASSE (`SYSLOG:stream` → canal libre → abonnement anonyme).
- **BLUE** : `startsWithCI`/`containsCI` 0-alloc dans `matchSystemPolicy`. RED→vert prouvé.
- Commits : `0a18fdc2` (test e2e), `cfcb24b0` (fix), `f61b0de9` (docs migration P6.11 ✅, 66 %).
- 🏁 Red-team COMPLET : 11/11 briques du cœur P6 attaquées + prouvées.

## Frictions / leçons

- **Sous-agent se trompe sur l'absence d'une garde** : l'agent Explore a rapporté « Origin check
  ABSENT » ; or `checkWebsocketOrigin` existait. Grep de contrôle avant de bâtir l'attaque → a
  redressé l'angle (attaquer la défense par spoofing, pas tester un trou imaginaire). → RETEX.md.
- **Plancher `startsWith` case-sensitive contournable par la casse** → tout match de namespace
  réservé doit être CI (fix 0-alloc). → RETEX.md.
- **Écart doc↔code** : le commentaire `SecureWsController` disait « verrou confine à /api/ » ; le
  confinement réel = restriction route WEBSOCKET-tagged dans `invokeApiRequest`. Noté.
- **Test red-team vert au 1er coup = suspect** → contrôles positifs (O5 same-origin, B0 admin)
  DANS le banc pour prouver qu'il PEUT virer rouge (déjà skill, re-confirmé).

## Efficacité

- 0 restart manuel : le DevSupervisor a rebuild+restart security tout seul sur l'édit de
  frameAuthorizer.ts (`↻ changement → ✓ serveur prêt`) → e2e relancés sur le bon code.
- 1 friction : commit docs rejeté par commitlint (sujet commençait par « P6.11 » majuscule) →
  reformulé sujet minuscule. + push background avait déjà poussé entre-temps (fetch + re-push).

## Gates

attack 6/6 · realtimeFrameLock 50/50 · security 443/443 · build 0 err TS · mémoire 9/9 · e2e WS 21/21.

## Commits produits

| Commit   | Sujet                                                                       |
| -------- | --------------------------------------------------------------------------- |
| 0a18fdc2 | test(security): red-team e2e firewall/WS data plane (CSWSH/ALS/confinement) |
| cfcb24b0 | fix(security): plancher de canal système WS insensible à la casse           |
| f61b0de9 | docs(migration): red-team firewall/WS data plane bouclé (P6.11 ✅, 11/11)   |

## ➡️ Prochaine étape

Cœur sécurité P6 défendable MVP. Suite = data plane Audit (P6.14) → débloque Studio sécu (P6.15),
ou autre phase. Red-team cœur P6 terminé.
