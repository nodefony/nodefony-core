---
date: 2026-09-03
session_id: 4f3ae9b4-e8ca-48d2-83c8-9c77cb34d089
focus: Audit fable du banc devkit, puis réparation de l'instrument — 7 commits
---

# Session retro — 2026-09-03 — 4f3ae9b4

## Focus

Un audit `fable` du banc devkit (skill + 60 scripts + 25 retex + 5 mémoires), puis
la réparation de ce qu'il a trouvé. Sept commits, tous poussés.

## Fait

- Audit `fable` en délégation (562 k tokens, 89 outils, 20 min) — ses affirmations
  lourdes recontrôlées au terrain avant toute action, six vérifiées, toutes vraies.
- `74fa157a` juge mort ressuscité + son selftest · `a1adfb9e` flake tué (ports
  éphémères, table SQL au PID) · `ffaf63be` garde de drapeaux unifiée + son contrôle ·
  `5f49a236` `--prove` rendu honnête, `--sans` · `02f1dfe0` branchement (npm +
  pre-commit ciblé + CI) · `3a396814` filet `juge-en-erreur` · `0a519024` trois juges
  qui punissaient `@CsrfProtect`.
- Lot d'auto-contrôles : 26 → 29, tous verts.
- Bancs lancés en arrière-plan pour la nuit (`tmp/bancs-2026-09-03/`).

## Frictions et coûts

- **Trois instruments menteurs dans la même session.** Un regex qui cherchait la FORME
  `const PORT` et ratait `PORT_JOUET` ; un faux positif sur `gate-media-range` (qui
  imprime sa cause par `console.error`, pas par `exit()`) ; un `node -e` dont le double
  échappement shell faisait croire à un cas Windows cassé. Aucun n'a produit de fausse
  conclusion — parce que chacun a été recontrôlé à la main avant d'agir.
- **Un `git reset` refusé par la garde du dépôt** pendant un test de gate — à raison :
  l'arbre était sale. Restauré par `cp` + `git add`, sans geste destructif.
- **Deux allers-retours sur un script d'édition** (regex qui remontait au TSDoc de tête,
  puis `matchAll` sur des imports multi-lignes). Le diff a été inspecté avant de
  conclure : zéro dégât, mais deux passes perdues. Écrire l'automate d'édition en
  ciblant la ligne plutôt qu'un bloc aurait évité les deux.
- **Le gate `skills-doc` a refusé deux commits** (fiches périmées, puis renvois écrits
  `lib/…` au lieu de `scripts/lib/…`). Il a mordu correctement les deux fois.

## Recommandations

1. **Un contrôle d'exhaustivité se cherche par le CONCEPT, jamais par la forme.** Deux
   sessions de suite, une recherche par motif littéral a laissé passer les cas écrits
   autrement. La parade qui a marché : lancer la chose et regarder ce qui sort
   (`juges-collecte` a trouvé les deux juges muets en une exécution, là où l'analyse
   statique rendait 16 candidats dont 9 faux).
2. **Un selftest dont le décor est plus pauvre que le réel valide ce qu'il devrait
   condamner** — l'app jouet de `gate-zone-firewall` ne semait aucun cookie CSRF, ses
   deux cas passaient au vert pour la mauvaise raison.
3. **Corriger une accusation à tort par une innocence à tort n'est pas une
   correction** : `jeton-csrf-absent` classé DECOR éteignait l'instruction d'une vraie
   faille. La règle du dépôt (« au moindre chemin par lequel l'agent la produirait,
   c'est INDÉTERMINÉ ») tranchait seule.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff — a évité trois fausses conclusions ici.
- ✅ Voir un test ROUGE avant de le croire — appliqué sur chaque contrôle neuf, et
  deux fois il a révélé plus que prévu.
- ✅ Prouver sur l'artefact reçu — le hook pre-commit a été éprouvé en conditions
  réelles (juge cassé volontairement, commit refusé, arbre restauré).

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `74fa157a` | le seul juge sans auto-contrôle était mort depuis cinq jours     |
| `a1adfb9e` | le lot d'auto-contrôles rendait deux verdicts pour un seul état  |
| `ffaf63be` | la garde qui protégeait le banc en refusait un drapeau documenté |
| `5f49a236` | le lot s'attribuait une garantie qu'il n'avait pas               |
| `02f1dfe0` | l'auto-contrôle des juges est enfin LANCÉ par quelque chose      |
| `3a396814` | un juge qui se casse n'accuse plus l'agent                       |
| `0a519024` | trois juges punissaient l'agent qui protège ses écritures        |
