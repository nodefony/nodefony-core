---
date: 2026-09-25
session_id: 857973a5-9fda-4c48-9db5-b996f353824c
focus: montée des dépendances, templates Angular réellement vérifiés, dist vidé à l'émission
---

# Retex 2026-09-25 — 857973a5

- **Fait** : `da8c84911` fix(test) deux rouges de la veille (lien mongoose, `get` du double
  Redis) · `089d4809f` vite 8.3.1 / rolldown 1.2.11 / analogjs 2.7.4 / xyflow / mobx-react-lite ·
  `4e7bedff2` Angular 22.2 (borne ~22.1 levée, copie unique hissée) · `fb29db43d` fix(frontend)
  typecheck des templates Angular activé + 3 défauts qu'il cachait · `adabf8a04` turbo 2.11.4 ·
  `b70e56e3a` fix(build) `cleanDir` à l'émission, `rimraf` retiré des 20 paquets · `03da2b7f6`
  graphe détaillé en `.ai/symbols.verbose.json`, racine en `cleanDir`. Poussé.
- **Frictions** (versées au sas sous 🎯) : option de vérification déclarée jamais exécutée
  (analogjs) ; faute injectée hors du périmètre de la règle ; trois instruments shell/sonde faux.
  Un échec inexpliqué : 1ʳᵉ build d'app générée qui a gardé le témoin, non reproduit (3 rejeux).
- **Décisions** : Angular reste en TILDE (analogjs vit d'API privées) ; `cleanDir` opt-in au socle
  (défaut false), activé partout où `dist` appartient au seul build — app générée comprise, avec
  la règle écrite dans le gabarit (un front ne sort pas dans `dist/`) ; `symbols.json` détaillé
  déplacé plutôt que supprimé (lu au `jq` par `nodefony-inspect`) ; TypeScript 7 attend l'amont.
