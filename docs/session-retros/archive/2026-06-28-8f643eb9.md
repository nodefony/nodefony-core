---
date: 2026-06-28
session_id: 8f643eb9-a2ea-4597-8b7b-a97bd8d99562
focus: Modèle de session NIST/OWASP (idle + absolute + touch throttlé) — P6
---

# Session retro — 2026-06-28 — 8f643eb9

## Focus

Remplacer le `maxLifetimeS` unique (reliquat PHP) par un vrai modèle NIST 800-63B / OWASP :
**idle + absolute + touch d'activité throttlé**. Vrai fix de la déconnexion des sessions
ACTIVES (Studio en WS read-only).

## Fait

- **Diagnostic terrain** (devise) : l'idle à la lecture était **dead-code** (`this.lifetime`
  jamais posé en prod), seul le GC purgeait ; l'activité WS/readOnly ne rafraîchissait jamais
  `updatedAt`. Le kit (06-27) était périmé : `absoluteTimeoutS` existait DÉJÀ. Tests verts par
  **omission** (unit posait `s.lifetime` à la main → logique testée, câblage jamais).
- **Livré** (`e27de035`, poussé) : `idleTimeoutS` (1800) + `absoluteTimeoutS` (43200, actif) +
  `ISessionStorage.touch?` (files `utimes` / drizzle+mongo `updateOne` / redis `EXPIRE`) câblé
  via `saveSession` (touch si la session n'écrit pas son blob, **y compris readOnly** = le fix) ;
  GC double-borne sur les 4 stores ; `RevocationGuardStorage.touch` respecte la pierre tombale ;
  FileStorage `read` pose created/updated depuis birthtime/mtime + `gc` rendu déterministe. Retrait
  FRANC de `maxLifetimeS`.
- **Red/blue team** : `session-timeout.attack.test.ts` 11/11 (absolute non contournable par touch,
  session révoquée non prolongée, Redis absolute à la lecture, file birthtime, concurrent idempotent,
  config négative rejetée). 1 fix blue (gc déterministe).
- **Perf** mesurée (prod mono logs off, wrk) : pipeline nu ~8900 · cluster -w4 ~22000 · touch ~2500
  vs write ~570 (×4,4, évite l'écriture SQLite synchrone) · authentifié ~480. Dev ~1850 = artefact
  (logs+watch). **Profiling** (`node --prof`) : brique session ET firewall P6 **tous 0,0 %** ; hot =
  drizzle query-building. → P6 valide, 0 hot method ; levier = ORM (2 lookups/req).
- Gates : http unit 516 · intég 559 · mémoire 9/9 · drizzle 95 · build 0 err. MIGRATION_STATUS P6 80 %.

## Frictions (→ RETEX.md)

- **Offline-first** : parti curl les RFC sur le réseau au lieu du bundle `nodefony-framework-dev/reference/rfc/`
  (38 RFC + OWASP offline) — user m'a corrigé. Réflexe à ancrer.
- Bench-isolate « coût = store, pas firewall » re-confirmé 3× (profiling) → graduation due.
- Ne pas grouper build+spawn+poll dans une commande Bash (timeout).

## Commits produits

| Commit     | Sujet                                                          |
| ---------- | -------------------------------------------------------------- |
| `e27de035` | feat(http): modèle de session NIST/OWASP (idle+absolute+touch) |
