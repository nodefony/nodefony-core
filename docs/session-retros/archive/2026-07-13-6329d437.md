---
date: 2026-07-13
session_id: 6329d437-00bc-45c8-892e-e099efc6150d
focus: Playground — radiographie de la requête (whoua 3) + ports auto multi-app
---

# Session retro — 2026-07-13 (nuit) — 6329d437

## Focus

Deux chantiers : (1) la **radiographie** du Playground Studio — le dernier des 3
moments « whoua » du kit ; (2) sur demande du user en cours de session, les
**ports auto** (deux apps Nodefony côte à côte, ce qui était impossible).

## Fait

- `ad421908` — **Radiographie du Playground**. Le Profiler dev-only existait déjà
  (phases + requêtes ORM indexées par `requestId`) : la page en était simplement
  débranchée. Trois trous fermés : la trace de décision du firewall
  (`ISecurityTrace`, dev-only, 0 alloc en prod), `ProfileEntry.user` qui mentait
  en zone (le token vit dans l'ALS, illisible au teardown), et `startMs` sur les
  requêtes ORM (le SQL se place DANS le waterfall).
- `d54188a3` — **Ports auto**. Molette `servers.portPolicy` (auto dev / strict
  prod+test), retry atomique au `listen()`, state file runtime pour la
  propagation, 3 gardes devenus faux positifs corrigés, 2 bugs Vite.
- `76379f0a` — MEMORY http + core/kernel.

## Coûts évidents

- **Les ports du poste** : le banc du repo et la cci-app du user se disputent
  5151/5152 → j'ai dû arrêter proprement sa cci-app pour lancer le banc. C'est
  précisément la friction qui a déclenché le 2ᵉ chantier — elle ne reviendra plus.
- **Mémoire IA fausse** : `admin/admin` alors que le compte dev est `admin/secret`
  (`provisionUsers`). Un aller-retour perdu sur la preuve runtime.
- **Un test rouge diagnostiqué au lieu d'être supposé** : bon réflexe (repro Node
  de 5 lignes → `http.Server` a 1 écouteur natif), mais l'assertion initiale était
  naïve.

## Recommandations

1. **Graduer « deux implémentations d'une même règle = dérive »** (3ᵉ occurrence)
   en `feedback_*` au prochain CONSOLIDATE.
2. **Annoncer les tests dans le plan**, pas seulement les écrire : le user a dû
   demander 2× (« il faut des ts », « il faut des tests béton »).
3. **Réflexe « lever une contrainte → auditer les gardes »** (cf RETEX.md).

## Patterns récurrents (déjà gérés)

- ✅ Vérifier le terrain avant d'éditer (devise) : le kit annonçait « le firewall
  n'a pas de trace » — confirmé au code, pas cru sur parole.
- ✅ Fail-loud systématique (décalage de port ANNONCÉ, radiographie socket
  indisponible qui DIT pourquoi au lieu d'un cadre vide).
- ✅ Gate mémoire lancée avant commit (pipeline touché).

## Commits produits

| Commit     | Sujet                                                    |
| ---------- | -------------------------------------------------------- |
| `ad421908` | feat(studio): playground — radiographie de la requête    |
| `d54188a3` | feat(http,core): ports auto — deux apps côte à côte      |
| `76379f0a` | docs(memory): portPolicy, portBinder, state file runtime |
