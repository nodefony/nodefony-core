---
date: 2026-08-02
session_id: cc1948fc-06b1-4654-b3ec-024d9f4892f9
focus: Passe 2 du banc devkit (21 tâches × 3 runs) — la référence passe à 28/28, T23 instruite et corrigée, 3 tâches neuves
---

# Session retro — 2026-08-02 — cc1948fc

## Fait

- **Passe 2** : 21 tâches × 3 runs, décor **repacké** (celui de la passe 1 était
  antérieur aux commits du jour). **13/21 PASS** là où un run unique avait dit
  21/21. Référence **7 → 28 tâches**.
- **T23 instruite, corrigée, remesurée** : `@CsrfExempt` 2/3 → **0/3**,
  `trustedOrigins` 0/3 → **2/3**, cause `defense-csrf-demontee` éteinte.
- **3 tâches neuves** (30 `symbols`, 31 `card`, 32 `check`) — les capacités des
  lots 3-4 avaient été livrées sans aucune mesure. Rodage **3/3**.
- **Défaut produit corrigé** : le diagnostic `module-not-installed` annonçait
  « le démarrage échouera à l'import » ; le Kernel écarte le module en fail-soft
  et l'app démarre AMPUTÉE. Assertion posée, vue rouge.
- **Garde de pack `.ai` vue mordre** (`PACK_EXIT=1` → `0`), point resté ouvert
  depuis la session précédente.
- **Runs sauvegardés** dans `tmp/devkit-bench-runs-2026-08-02/` (51 Mo utiles
  sur 21 Go), avec le mode d'emploi pour les relire sans les rejouer.

## Coûts évidents

- La passe 2 elle-même : ~3 h de fond, mais **hors chemin critique** — le travail
  a continué pendant (garde `.ai`, analyse T10, conception des tâches).
- **Trois faux diagnostics de lecture**, tous silencieux et tous crédibles :
  `git show` au lieu d'une plage, `grep "nodefony check"` qui rate
  `npm run check`, `awk` dont la borne de fin arrive trop tard. Chacun a produit
  un chiffre faux qu'il a fallu reprendre.
- Un comptage d'usage sur le transcript entier a rendu `card` 60/63 au lieu de
  **2/63** — il mesurait la lecture de l'`AGENTS.md`, pas l'invocation.

## Recommandations

1. **Suspecter la commande de mesure avant le sujet mesuré** — trois fois en une
   séance, sur des chiffres qui n'avaient l'air de rien.
2. **Un verbe maison ne s'impose pas par la position** : `check` est ligne
   adjacente à trois gates employés 8× plus. Automate ou adossement, pas prose.
3. **Écrire la tâche d'une capacité est un audit de cette capacité** — T32 a
   révélé un message de diagnostic faux que personne n'avait vu.

## Patterns récurrents (déjà gérés)

- ✅ Débranchement vu rouge avant de conclure (4 fois : 2 gates AGENTS.md,
  1 assertion readiness, 1 garde de pack) — restaurations prouvées par md5.
- ✅ Preuve sur l'artefact reçu : le correctif vérifié PRÉSENT dans l'app témoin
  avant de lancer la remesure de T23.
- ✅ Instrument éprouvé avant la mesure (127 sondes, 14 juges, 3 mécanismes).

## Commits produits

| Commit     | Sujet                                                                               |
| ---------- | ----------------------------------------------------------------------------------- |
| `213354ff` | passe 2 : 13/21, référence 7 → 28 tâches                                            |
| `3fb1b4c5` | l'origine tierce se DÉCLARE — correctif T23 + gate vu rouge 2×                      |
| `3ec1bbac` | remesure T23 : 0/3 → 2/3, `@CsrfExempt` disparu                                     |
| `298aa242` | 3 tâches pour les capacités livrées sans mesure + `check` cesse d'annoncer un crash |
