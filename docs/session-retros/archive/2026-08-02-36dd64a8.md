---
date: 2026-08-02
session_id: 36dd64a8-2edb-4b4b-b603-8b540d466882
focus: la carte de visite répond à froid · lots devkit 3+4 fermés · passe 1 du banc 21/21
---

# Session retro — 2026-08-02 — 36dd64a8

## Focus

Passe 1 du banc lancée en fond (21 tâches aveugles), et pendant qu'elle tournait :
les deux portes fermées de `devkit:card` ouvertes, puis les lots devkit 3 et 4 fermés.

## Fait

- **`nodefony card`** — la carte de visite quitte le module et devient un fast-path
  standalone du cœur. Deux portes étaient fermées au moment exact où elle sert :
  application NON CONSTRUITE (le Kernel refuse avant elle) et terminal sans
  `NODE_ENV` (module `policy:dev` non chargé ⇒ `unknown command`).
- **Lot devkit 3** — `check` gagne une troisième famille (`checks/readiness.ts`) :
  variable requise absente, module du manifeste non installé, dépendance absente
  de `node_modules`, port tenu par un tiers.
- **Lot devkit 4** — `.ai/symbols.json` publié par le paquet `nodefony` + commande
  `nodefony symbols` + UNE résolution partagée avec `docsReader`.
- **`AGENTS.md` généré** — les deux verbes neufs y sont, tenus par un gate.
- **Kit devkit corrigé** — il affirmait « le module n'existe pas » et deux
  résolutions de chemin cassées ; les trois étaient faux (12 affirmations
  confrontées au code en `haiku`).
- **Passe 1 du banc** : 21/21 PASS, 0 sonde rouge, 727 tours · 7,11 $ · 64 min.

## Coûts évidents

- Session TRÈS longue (une feature aurait suffi) — le banc tournait en fond, ce
  qui a invité à enchaîner carte → kit → lot 3 → lot 4 → AGENTS.md.
- Deux débranchements ratés : un partiel (resté vert, ne prouvait rien), un qui a
  mutilé le gabarit. Coût : deux allers-retours + une réparation à la main.
- Deux dérives de `cwd` rattrapées par la garde du dépôt.

## Recommandations

1. **Compter les occurrences avant de débrancher** (`rg -c`) — un débranchement
   partiel rend vert et fait conclure à tort.
2. **Une capacité neuve se pose dans l'`AGENTS.md`** au même rang que son test —
   c'est le user qui a dû le demander.
3. **Lire les décisions figées du `CLAUDE.md` de la cible AVANT de concevoir** :
   celle du module devkit tranchait seule l'arbitrage core ⇄ module.

## Patterns récurrents (déjà gérés)

- ✅ garde anti-`git checkout` sur arbre sale — a mordu, à raison (2ᵉ fois).
- ✅ garde anti-`cd` relatif — a mordu 2×.
- ✅ preuve de restauration par empreinte md5 après débranchement.

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `fe548cd9` | la carte de visite répond AUSSI à froid — et sans NODE_ENV              |
| `6ffeaf49` | la structure et le TSDoc disaient encore la porte CLI d'avant           |
| `68650959` | retrait de la commande orpheline — un seul rendu de carte               |
| `e0fea5bf` | les 4 diagnostics du démarrage (devkit 3)                               |
| `23d781ba` | le graphe est PUBLIÉ, et une commande l'interroge (devkit 4)            |
| `a726b848` | les lots 3 et 4 du devkit sont fermés                                   |
| `d6f4fc71` | l'AGENTS.md généré CONNAÎT les deux verbes neufs — et un gate l'y tient |
