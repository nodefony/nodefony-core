---
date: 2026-09-19
session_id: 8bc7d560-5787-4996-96df-10b11dfe357f
focus: Dépouiller un run Copilot réel, puis rendre à `create front` la page de `create app` — socket compris
---

# Session retro — 2026-09-19 — 8bc7d560

## Focus

Analyse du transcript d'un agent tiers (Copilot, dossier vide → app de chat, 37 tours,
56,9 min, 84 gestes, 0 MCP), puis correction de ce qu'il a révélé côté front.

## Fait

- **Transcript dépouillé** par l'outil du dépôt (`analyse-transcript.mjs`), pas à la main.
  Première partie (création d'app, 7 min) : le dispositif de découvrabilité MORD — 2ᵉ geste =
  `AGENTS.md`, puis les trois skills livrés ; générateurs employés ; **un seul `apply_patch` de
  tout le run**. Verts du premier coup : `test`, `typecheck`, `lint`, `format:check`, `build`.
- **Quatre défauts nommés côté création** : `create entity` ne nomme jamais `orm:generate` dans
  le régime « sans mode écrit » alors que le e2e livré tourne en production ; la sortie de
  `npm create` (9 163 o) dépasse le plafond de lecture de Copilot, qui n'a relu **aucun** de ses
  5 débordements ; le boot salit l'arbre git (certificats non ignorés, `rm -rf` par l'agent) ;
  0 appel MCP. Non ticketés — le user a orienté sur le front.
- **`create front` rend désormais la page de `create app --frontend`** (`d94d2e8a`) : couches
  partagées, capacité CONSTATÉE dans les manifestes, chemins annoncés recalés. Les 4 gabarits
  orphelins supprimés après contrôle du contenu perdu.
- **Second défaut, trouvé par la preuve** : `create front` ne câblait pas `&& nodefony
frontend:build` — une app enrichie après coup ne bâtissait jamais ses assets.
- **Tâche 0 du banc** : l'énoncé demande « une page de démonstration » et rien de plus ; une
  sonde d'observation (`create front`) + son échantillon. Recalage de la référence **reporté à
  l'alpha.8** (décision user).

## Coûts évidents

- Le décor de preuve (app témoin + `npm install` + build) : ~4 min, et c'est lui qui a trouvé
  le défaut du script `build` que 301 tests ne voyaient pas. Rentable.
- Une assertion écrite trop large, rouge sur le gabarit conforme : un aller-retour.

## Recommandations

1. **Prouver les 3 autres moteurs** (vue, svelte, angular) par un typecheck réel — seul react
   l'a eu ; les autres ne tiennent que par les chaînes du test.
2. **Voir la page à l'écran** (`nodefony-browser`) : rien n'a été constaté visuellement.
3. **Les 4 défauts de la partie « création »** attendent un arbitrage : D1 (`orm:generate` non
   nommé) et D2 (sortie illisible pour un agent) coûtent un cycle à chaque utilisateur.

## Patterns récurrents (déjà gérés)

- ✅ La preuve sur l'artefact REÇU trouve ce que la suite verte ne voit pas.
- ✅ Un gate neuf se voit ROUGE en débranchant — les trois l'ont été.
- ✅ L'outil du dépôt avant le `jq` à la main (`analyse-transcript.mjs`).

## Commits produits

| Commit     | Sujet                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| `d94d2e8a` | feat(scaffold): rendre à `create front` la page de `create app`, socket compris |
