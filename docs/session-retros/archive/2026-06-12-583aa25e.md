---
date: 2026-06-12
session_id: 583aa25e-7430-42a1-a047-fbd45bf4f74a
focus: POC souverain Ph.3 FRONT — ApiClient Studio route les GET via la socket (pont api.request) + fix régression 405 live
---

# Session retro — 2026-06-12 — 583aa25e (Ph.3 front)

## Fait

- **Ph.3 front livrée** : pont consommé dans l'`ApiClient` (PAS par page — 0 call-site touché) ;
  GET → `socket.request(url)` quand socket connectée ; kill switch `UiStore.apiViaSocket` + switch
  & DocHint dans `RealtimeHubContent` ; 10 tests unit (`apiClientSocketBridge.test.ts`).
- **Régression LIVE signalée par le user** (Studio cassé à la connexion) **fixée dans l'heure** :
  la v1 propageait `RpcError.data.status` → 405 des routes GET-only (`/stats`, `/health`,
  `/auth/me`) ≠ réponse REST. Politique finale : **la socket ne sert que les SUCCÈS**, tout échec
  → fallback fetch = réponse de référence ; mémorisations -32601 (session) + 405 → `httpOnlyRoutes`.
- Skills co-évolués : `nodefony-studio-dev` **1.24.0** ⇄ `nodefony-framework-dev` **1.22.0**
  (section POC recalée Ph.3 full-stack, réf croisée de contrat).
- POC API souveraine : ne restent que Ph.4 (mediasoup) et Ph.5 (GraphQL) avant P6.

## Frictions (détail au SAS `RETEX.md`)

- **Gates vertes ≠ pas de régression sur un diff qui intercepte un chemin global** — les tests unit
  mockaient les mêmes hypothèses fausses que le code ; le user a servi de test E2E. → dérouler les
  call-sites réels (boot/login) contre le serveur réel avant « fait ».
- Multi-bundle Vite : curl `@fs` sur l'instance angular = faux 500 (le code était sain).
- Boot dégradé hérité de la session précédente (race watch-rebuild, 2ᵉ occurrence) — détecté au
  RESUME par le grep filet, restart propre.
- `fetch` mocké : `Response` à usage unique → `mockImplementation`, pas `mockResolvedValue`.

## Commits produits

| Commit    | Sujet                                                                             |
| --------- | --------------------------------------------------------------------------------- |
| `6b82d0b` | feat(studio): api souveraine ph.3 front — GET data plane via la socket            |
| `de39aca` | fix(studio): pont api.request — la socket ne sert que les succès (régression 405) |
