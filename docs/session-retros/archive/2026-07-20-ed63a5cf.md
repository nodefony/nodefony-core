---
date: 2026-07-20
session_id: ed63a5cf-ef25-4d2a-b022-8a55b51190ee
focus: correction code↔doc — les 6 écarts SYSTÉMIQUES du chantier doc, fermés par tests d'abord
---

# Session retro — 2026-07-20 — ed63a5cf

Suite directe de la session « vague 7 ». Le kit portait 6 écarts marqués
systémiques (ils dépassent leur page). Tous fermés, 6 commits, arbre propre.

## Fait

| Écart    | Correction                                                                                                   |
| -------- | ------------------------------------------------------------------------------------------------------------ |
| **F131** | `RealtimeIdentity`/`IRealtimeWelcome`/`IRealtimeDenied` réexportés par `nodefony/client` ET `nodefony/react` |
| **F140** | `@services()` contraint à `Module<unknown>` — la convention `I` du CLAUDE.md ne casse plus la compilation    |
| **F142** | Le dev ANNONCE la sanction de prod ; le journal nomme le hook au lieu de « (anonyme) »                       |
| **F173** | Action RPC **fermée par défaut**, décorateur ET override `realtimeActions()`                                 |
| **F172** | `RealtimeConsole` cesse de porter sa copie du contrat de sonde                                               |
| **F115** | Config typée dans `use()` sur les 4 modules restants + **les 3 descriptions fausses de la règle**            |

Commits : `024f32a7` · `1e99a6c6` · `fec115ea` · `489fa541` · `221648a7` · `12cb02e4`.

## Ce qui a marché

- **Tests d'abord (demandé par le user en cours de route).** Chaque correctif vu
  ROUGE avant vert, avec les codes annoncés par le kit (TS2724/2459/1238/1270).
  Bénéfice inattendu : la rédaction du test a changé le DESIGN deux fois —
  `critical: false` doit rester silencieux (F142), et fermer le seul décorateur
  laissait l'override grand ouvert, la voie par laquelle Studio expose
  `scaffold:run` (F173).
- **Vérifier le terrain avant d'éditer.** 3 des 6 écarts étaient périmés ou
  partiels dans le code (F115 : 5 modules déjà conformes ; F172 : bug déjà
  corrigé ; templates : justes). Sans contrôle → 3 corrections inutiles.
- **Prouver que la garde mord.** `hasPolicy` (F173) et le typecheck Studio (F172)
  neutralisés une fois chacun pour vérifier qu'ils rougissent.

## Coûts évidents

- **Dérive de `cwd` × 5** — coût #1. Déguisée en `SyntaxError` de décorateurs
  (vitest sans la config oxc depuis la racine), en « 0 test », en TS5058.
  Déjà graduée en [[feedback_bash_cwd_drift]] : c'est l'APPLICATION qui manque,
  pas la règle. `cd` absolu dans CHAQUE appel, sans exception.
- **`perl -pi` sur du code** (2ᵉ occurrence, cf RETEX) — a mangé le commentaire
  qu'il ne fallait pas.
- **Diagnostic d'un test qui échouait pour la mauvaise raison** (~15 min) : nom
  `public:health` capturé par le plancher système. A produit une vraie trouvaille.

## Trouvailles

- **`frameAuthorizer.ts:202`** réserve TOUT nom contenant `:health` ou `:stats`,
  pas seulement les préfixes — invisible depuis le site de déclaration. Mon
  propre TSDoc en donnait un exemple qui n'aurait jamais marché (`fec115ea`).
- **8 ancres `Kernel.ts:*` périmées** par 20 lignes ajoutées, dans une page jamais
  ouverte. Le gate `anchor-check` les a attrapées → il fait partie du correctif.
- **Le scaffold était juste, ses 3 descriptions mentaient** (F115). Quand une
  convention n'est appliquée nulle part, suspecter sa description.

## Recommandations

1. **`anchor-check` après toute correction de code** touchant un module documenté
   — pas seulement après une édition de doc.
2. **`guides/configuration.md` est hors standard** (préexistant, vérifié sur HEAD)
   → à prendre dans une vague.
3. **Chantier `any`** chiffré et non lancé : 178 `any` de type, dont 59 mixins
   irréductibles (TS2545 prouvé) ; ~119 réductibles concentrés sur 4 fichiers
   (`Command.ts` 26, `routerDecorators.ts` 21, `Error.ts` 17, `Kernel.ts` 16).
   Session dédiée, foyer par foyer, avec la règle eslint activée à la fin.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son propre diff (`lifecycleTags.test.ts` cassé par mon champ ajouté
  → c'est le contrat qui change, pas une régression).
- ✅ « Test Files failed + 0 Tests failed » = échec de transformation, pas de logique.
