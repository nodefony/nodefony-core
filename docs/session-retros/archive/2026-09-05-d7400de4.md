---
date: 2026-09-05
session_id: d7400de4-005f-40b3-b6db-6ee4107f395d
focus: régression `doctor --deep`, grappe temps réel #197, et quatre causes de forge rouge
---

# Session retro — 2026-09-05 — d7400de4

## Fait — 6 commits, tous poussés

- **`fd1f51a9`** — la régression livrée la veille : une commande n'est pas un hook
  de boot. Correctif dans le cœur (borne de temps décidée PAR écouteur).
- **`64c15ead` `099be19b`** — #199 + #200 : le `realtime:welcome` n'annonce plus
  que les canaux obtenables, matrice e2e identité par identité.
- **`c2281c50`** — #198 : `realtime-coverage-map.mjs` + `docs/couverture-bancs.md`.
- **`832a53a3`** — les sept commandes autonomes acceptent les options que l'aide
  globale promet (`-i`, `--interactive`, `-d`, `--debug`).
- **`ed12a591`** — quatre causes de forge rouge, dont trois héritées.

Tickets : **#198 #199 #200 fermés** · **#201 #202 #203 ouverts**.

## Coûts évidents

- **Deux runs `test:all` complets** (~11 min chacun) pour prouver que 25 rouges
  locaux étaient des flakes de saturation. C'était le prix de la preuve, et il a
  été payé une fois pour toutes : le second run rend des rouges ENTIÈREMENT
  différents (2 seuils de perf), ce qu'aucun raisonnement n'aurait établi.
- **Une insertion d'import automatisée a cassé trois fichiers** (import inséré au
  milieu d'un import multi-lignes) — le typecheck l'a dit tout de suite, mais
  c'est exactement le piège « éditer par correspondance de texte » que le
  `CLAUDE.md` décrit. Un `python3` qui insère une ligne ne parse pas la syntaxe.
- **Le gate poussé sans lire son code de sortie** : j'ai lu le compte
  (« orphelins : 0 ») et conclu. Un aller-retour de plus.

## Recommandations

1. **`--trace-exit` en premier réflexe** devant une sortie muette : il nomme
   l'appelant de `process.exit`, ce qu'aucune lecture de code ne donne aussi vite.
2. **Faire varier une borne pour la confirmer** (`NF_BOOT_TIMEOUT_MS=2000`) —
   deux mesures valent mieux qu'une inspection.
3. **Chercher qui porte déjà l'information** avant d'écrire une condition
   d'environnement dans un test : `NF_TEST_ENV` existait, un seul cas l'ignorait.

## Patterns récurrents (déjà gérés)

- ✅ « la brique éprouvée, la chaîne jamais » — anticipé cette fois : chaque
  correctif porte un cas de CHAÎNE (l'action câblée porte le marquage ; les sept
  parseurs acceptent les drapeaux), pas seulement un cas de brique.
- ✅ Débranchement vu ROUGE sur les trois lots (2, 6 et 28 cas tombés).
- ✅ Vert en isolation / rouge en suite → ressource partagée, pas régression.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `fd1f51a9` | une commande n'est pas un hook de boot — elle sortait en 0 au milieu |
| `64c15ead` | ne plus annoncer les canaux qu'un visiteur ne peut pas obtenir       |
| `c2281c50` | mesurer ce que les bancs du temps réel n'éprouvent pas               |
| `099be19b` | le banc du welcome étalait un `serverChannels` qui peut être nul     |
| `832a53a3` | les commandes autonomes refusaient les options que l'aide promet     |
| `ed12a591` | acquitter le relevé dans la liste des scripts                        |
