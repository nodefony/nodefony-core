---
date: 2026-08-30
session_id: 15fc87c5-213d-4010-83a9-79633b4506c4
focus: éprouver les migrations générées sur les trois moteurs — et ce que la mesure a révélé du produit
---

# Session retro — 2026-08-30 — 15fc87c5

## Focus

Fermer #112 (les migrations générées éprouvées sur PostgreSQL et MySQL, pas seulement SQLite),
puis rejouer la tâche 33 pour fermer #99. Le premier est fait ; le second a rendu un verdict qui
a ouvert trois tickets sur le produit.

## Fait

- **#112 fermé** — le banc du code généré accepte `--database <moteur>` et recrée l'application POUR
  ce moteur (pas une variable d'environnement : le dialecte est une décision de création). Job
  `dialectes` à la forge + job `verdict` qui échoue si un moteur n'a pas été exercé. **Vert du
  premier coup sur GitHub**, les deux moteurs et les trois systèmes.
- **Trois défauts produit** que seule une application créée pour un moteur serveur pouvait montrer :
  échantillon de clé étrangère en TEXTE face à une colonne `uuid` (500 sur chaque POST en
  PostgreSQL), test d'entité montant une base SQLite en mémoire quel que soit le dialecte, bases
  jetables forgées en `sqlite:` codé en dur.
- **Tâche 33 rejouée** : 1 PASS / 3 (71 · 69 · 72 tours). #99 ne se ferme pas — mais l'instruction
  des trois transcripts a nommé la chaîne exacte qui enferme l'agent.
- **#116 et #117 ouverts, faits et refermés** dans la foulée ; **#118 ouvert** (le structurant).
- **CI réparée** : le gate de portabilité des skills publiés était rouge sur six variantes depuis la
  livraison du skill de migration.
- **CI réparée, deux fois** : après le gate des skills, les deux jobs **Windows** restaient rouges —
  la désignation de la base publiée sortait en `var\db.sqlite`. La grammaire de chemins est
  désormais injectée, et le cas Windows se joue sur macOS.

## Frictions du jour

- Le produit ne laissait **aucun geste** : 2 agents sur 3 ont édité l'historique à la main, 1 sur 3 a
  supprimé la base. L'interdit ne tient que si un chemin reste ouvert.
- Un **rouge pour la mauvaise raison** (mauvais répertoire de lancement) a failli compter comme
  preuve de vu-mordre.
- Deux défauts n'existaient que dans l'**artefact rendu** : YAML de CI cassé par un heredoc, script
  d'initialisation qui tue le conteneur.
- Un **skill enseignait le contournement** d'un manque comblé la veille.
- **Déclarer sans installer est un demi-geste** — vu par le user, pas par moi.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `4a8327fd` | fix(cli): rendre le code généré exécutable sur PostgreSQL et MySQL   |
| `3284a50f` | ci(scaffold): éprouver les migrations générées sur les trois moteurs |
| `974d8ee9` | fix(cli): installer l'outil de migration qu'une entité ajoute        |
| `46f26afe` | fix(orm): dire quel outil manque au lieu d'accuser la base           |
| `839a104c` | fix(devkit): rendre le skill des migrations tapable sous Windows     |
| `d1eda2da` | fix(orm): écrire en `/` la base visée, sur les trois systèmes        |
