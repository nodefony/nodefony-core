---
date: 2026-09-04
session_id: 113e06e8-af56-4489-9fcf-d14b2b116493
focus: grappe doctor soldée (#178) — l'instrument s'accusait lui-même, et n'auditait pas sa propre entrée
---

# Session retro — 2026-09-04 — 113e06e8

## Focus

Finir la grappe `doctor` : **#184** (ce qui disparaît dans l'environnement visé), **#186** (surface
ouverte + entités hors dialecte), **#185** (gardes du projet armées), puis fermer le parent **#178**
avec son compte rendu. Deux signalements du user en séance ont ouvert un troisième front.

## Fait

- **#184 · #185 · #186 fermés**, parent **#178** fermé — les 8 sous-tickets de la grappe le sont.
- Jalon 10.0.0 : **39 ouverts / 99 fermés**.
- La règle de gating a QUITTÉ le Kernel pour `kernel/moduleGating.ts` (fonction pure) : le boot
  l'applique, le diagnostic la rejoue à froid pour un autre environnement.
- Trois familles neuves (`gating`, `surface`+`dialect`, `guards`), 56 cas de test neufs, tous vus
  ROUGE par débranchement (12 points débranchés, 19 cas tombés).
- **Grappe `nodefony -h` instruite et ouverte** : #189 parent + #190 #191 #192 #193, sur la base d'un
  audit `fable` dont les affirmations ont été vérifiées au terrain avant d'être écrites en ticket.

## Frictions du jour (versées dans RETEX.md, sous thèmes existants)

- 🔴 **Mon contrôle de sécurité s'est accusé LUI-MÊME** : le module qui cherche `@BypassFirewall`
  contient la chaîne. Remède : composer le motif, pour qu'il n'y ait rien à excepter.
- 🔴 **`doctor --env produntion` rendait un rapport complet et plausible** sur un environnement
  inexistant. Vu par le user, pas par moi.
- 🔴 **`tsgo -p tsconfig.json` du cœur exclut `src/tests`** — six contrôles verts sans avoir lu un
  décor. Le piège était écrit dans la mémoire de la veille, et refait le lendemain.
- 🔴 **Quatre décors comptaient « 2 familles » en dur** ; une troisième les a tous rendus faux.
- 🔴 **Une commande shell refusée par le harnais n'exécute RIEN**, pas même ce qui la précède — j'ai
  cherché un tour entier pourquoi mes patchs étaient absents.

## Coûts évidents

- Six cycles `build + doctor` pour observer le rendu réel : c'est le prix d'un travail dont la sortie
  EST le produit. Non compressible, mais chaque cycle a trouvé un défaut (doublon de fichier,
  auto-accusation, périmètre, libellé) — aucun n'était visible dans les tests.
- Un tour perdu sur la commande shell rejetée (`cd` relatif), et un demi-tour sur le mauvais gate de
  typecheck.

## Recommandations

1. **Un contrôle qui cherche un littéral doit composer son motif** — sinon il se trouve lui-même. À
   graduer si le cas se represente (scanner de secrets, gate de convention).
2. **Le gate de typecheck du cœur est `npm run typecheck`, jamais `tsgo -p tsconfig.json`** : le
   second exclut `src/tests`. Déjà noté hier ; si ça revient, ça mérite une règle dans le CLAUDE.md
   du workspace.
3. **Faire vérifier au terrain les affirmations d'un audit délégué AVANT d'en faire des tickets** —
   fait cette fois (4 sur 4 confirmées), et c'est ce qui rend la grappe #189 prenable telle quelle.

## Patterns récurrents (déjà gérés)

- ✅ Débranchement systématique avant de déclarer un test probant — 12 points, 19 cas tombés.
- ✅ Compte rendu de fermeture avec ses blocs « Au-delà du ticket » et « Non fait ».
- ✅ Vérifier les ancres d'un ticket avant de coder dessus (celles de #186 étaient décalées).

## Commits produits

| Commit     | Sujet                                                                           |
| ---------- | ------------------------------------------------------------------------------- |
| `6f3c6cda` | feat(cli): dire ce qui disparaîtra en production avant d'y aller (#184)         |
| `eb42164b` | feat(cli): montrer la surface ouverte et les entités hors dialecte (#186)       |
| `e5f8975b` | feat(cli): vérifier que les gardes du projet sont armées (#185)                 |
| `5b264a32` | fix(cli): nommer le programme nodefony, et dire ce que http:network fait (#189) |
