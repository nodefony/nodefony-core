---
date: 2026-06-20
session_id: 511e7cc2
focus: red-team WebAuthn+OAuth2 (2 passes) + skill crypto-déléguée + durcissement DevSupervisor (DX/résilience)
---

# Session retro — 2026-06-20 — 511e7cc2

Session à deux fronts : red-team P6 (suite du chantier) PUIS pivot vers la DX du DevSupervisor
(déclenché par le 404 fantôme OAuth + le principe résilience exprimé par le user).

## Fait

- **Red-team WebAuthn** (`705b3111`) — 2 passes : store (anti-takeover/IDOR/anti-clone-doc/index, vrai
  `MemoryWebAuthnCredentialStore`) + e2e replay challenge usage-unique/cross-cérémonie/anti-énum. Passe 2 :
  origin allowlist + counter délégué `@simplewebauthn` + rpID figé. SAINE.
- **Skill `nodefony-security-review`** (`1c7b2c60`) — §4.1 principe « briques à crypto déléguée à une lib
  auditée » (prouver câblage sans crypto valide + lire la délégation, ne pas reconstruire l'authenticator).
- **Red-team OAuth2** (`9dd5486f`) — provisioning sur VRAI `InMemoryUserRepository` : account-takeover+
  escalade (email-collision admin → Shadow séparé), anti-élévation re-login, collision cross-provider + e2e
  replay state/mix-up provider. Passe 2 : iss RFC 9207 + PKCE + defaultRoles création. SAINE.
- **DevSupervisor durci** (`aefb8281`) — boot anti dist-périmé (`#ensureBuilt` turbo), framework-ready
  (sonde ports, 0 IPC, détecte boot pendu), noms process normalisés, logs HMR-backend. core 1641/0, e2e 13/13.

## Décisions structurantes

- **PRINCIPE « résilience SANS dégradation silencieuse »** (directive user, mémoire dédiée) : fail-soft sur
  la dispo / fail-loud sur la dégradation ; tout rebuild/restart/fallback ANNONCÉ ; anti-pattern « vert mais
  cassé ». A guidé tout le durcissement DevSupervisor. C'est la décision qui survit au /clear.
- **Pas d'IPC superviseur↔enfant** (rappelé par user) : observation externe (code sortie + sonde ports).

## Frictions (→ RETEX.md)

- **404 e2e = dist module périmé au boot, PAS le code** (cause réelle du « bug » OAuth) → `--force-build`.
- **Modifier le core en boucle = turbo cache froid → boot 140s** (vs ~80ms à chaud) → grouper modifs core.
- **`process.title` enfant écrasé par `Kernel.preRegister`** (Kernel.ts:608) → poser à `onReady`.
- **Empilement d'instances dev** (stop/start + kill -9 + npm exec parasite) → chantier `status`/`stop`.
- **Auto-RETEX corrigé** : j'avais d'abord blâmé un restart DevSupervisor pour le 404 — FAUX (le watcher
  exclut `tests/`). Vérifier la cause avant de l'écrire (la confiance n'exclut pas le contrôle).

## Coûts évidents

- Nombreux `rebuild core → boot turbo froid (140s)` en testant le DevSupervisor (je modifie le core que je
  teste) → friction inhérente, atténuée en groupant les modifs + pré-chauffe.
- Multiples stop/start/kill manuels → désordre process (d'où le chantier status/stop).

## Reste — prochaine session (ordre user)

1. **Suite chantier DevSupervisor** : `nodefony status` + `stop` + single-instance robuste + topologie propre.
   Kit `project_dev_supervisor_dx_kit` (LIRE EN PREMIER).
2. **Puis** dernière brique red-team P6 : **Firewall/WS** (`realtimeFrameLock`).

## Commits produits (4 — locaux, non poussés ; 10 au total depuis 98ebf52c)

| Commit     | Sujet                                                                             |
| ---------- | --------------------------------------------------------------------------------- |
| `705b3111` | test(security): red-team WebAuthn                                                 |
| `1c7b2c60` | docs(skill): briques à crypto déléguée                                            |
| `9dd5486f` | test(security): red-team OAuth2                                                   |
| `aefb8281` | feat(dev): DevSupervisor — boot anti dist-périmé + framework-ready + noms process |
