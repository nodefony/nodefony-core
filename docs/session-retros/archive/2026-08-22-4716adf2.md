---
date: 2026-08-22
session_id: 4716adf2-eee8-492b-87ff-6073b8bc0977
focus: Instruire les échecs du banc devkit — deux bugs du framework, cinq sondes fausses, et le mode par défaut tranché
---

# Session retro — 2026-08-22 — 4716adf2

## Focus

Reprise sur les 3 runs T13 du banc devkit. L'instruction des transcripts a sorti
deux bugs réels du framework et cinq sondes qui rendaient de faux verdicts —
puis la décision de fond sur le mode par défaut du CLI, laissée en attente depuis
trois sessions.

## Fait

- **`process.exit()` tronquait la sortie au-delà de 64 Ko** — 97 825 octets vers
  un fichier, exactement 65 536 vers un `| jq`. C'était l'usage que le TSDoc de
  `inspect` DOCUMENTE. Corrigé au point de sortie commun.
- **`inspect` muet sur son mode** — 136 routes en production, 354 en
  développement, jamais annoncé. L'agent du banc et le gate mesuraient deux
  applications différentes ; l'agent avait raison.
- **Mode par défaut → `development`**, source UNIQUE (7 copies), avec la
  distinction « absent » vs « posé mais non-moteur » (staging/canary → production)
  que la suite de tests a imposée.
- **Cinq sondes du banc corrigées** (T4, T7, T9, T26, + le parse non gardé) :
  toutes punissaient un travail juste.
- **La référence du banc garde la MÉDIANE DE TOURS** + classement
  ALLÉGÉES/ALOURDIES, 5 règles vues rouges.
- **CI Windows réparée** : CRLF, pas page périmée. `.gitattributes` posé.
- **Menu** : 8 entrées de qualité ; `verify`/`format` ajoutés au dépôt, qui ne
  les avait pas alors que le gabarit les génère.
- **Gabarit d'app** : `.prettierignore` + `.gitattributes` manquants, ajoutés.
- **87 fichiers formatés** (commit isolé), 20 écartés à raison (copies amont).

## Coûts évidents

- **Trois `test:all` perdus** : édition concurrente pendant un run (13 faux
  échecs), puis 69 cache hits turbo qui auraient fait conclure « vert » sur des
  logs rejoués. Reprouvé sans cache.
- **Sleeps longs en foreground** qui bloquaient la fenêtre du user — signalé par
  lui, corrigé en cours de session.

## Recommandations

1. **Grader le thème « corriger le MOTIF, pas l'occurrence »** — deux
   manifestations le même jour, dont une auto-contamination.
2. **`prettier --check` en gate CI** — désormais possible, décision ouverte.
3. **Un gate « une app générée passe prettier --check »** — 7 fichiers du
   gabarit sont non conformes à leur propre config.

## Commits produits

14 commits, poussés sur `claude-ts` (`ffc451d0..ddb1c964`).
