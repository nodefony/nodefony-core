---
date: 2026-09-26
session_id: 28f10e46-cda3-43fe-ba3e-38c016bc774a
focus: anchor-check sans faux positifs, doc recalée, CI « Code généré » réparée (MongoDB), passage sur Studio
---

# Retex 2026-09-26 — 28f10e46

- **Fait** :
  - 02baf81db fix(docs) : anchor-check ne juge plus une ancre sur le symbole de sa voisine (segment par ancre, nom qualifié court, signature d'options, `abstract`/`override`, en-tête TSDoc) — selftest 4 → 10 cas, les 6 neufs rouges sur l'ancien outil
  - 5e220161b fix(scaffold) : `unknown | null` → `unknown` pour un json facultatif, les deux ORM (`rowTypeOf`) ; banc mongodb vert de bout en bout
  - 9fa8c8ca7 docs : 32 ancres dérivées recalées (24 anchor-fix relues, 1 corrigée, 8 à la main) — corpus publié 0 SUSPECT
  - 4dccbf1e5 fix(studio) : ERD en grille pour les tables sans relation ; `/orm-entity` et `/orm-connector` sans paramètre sans 404
  - 15cf1cc52 fix(studio) : filtre « Connexion » des sessions traduit
  - #499 ouvert (backlog) : liens logiques des tables du framework vers `User`
- **Frictions** (versées au sas, thème 🎯) : banc « vert » sur un seul moteur ; règle recopiée entre anchor-check et anchor-fix ; fait du `_state` (CI rouge) jamais vérifié.
- **Décisions** :
  - Le gate d'ancres reste NON bloquant sur SUSPECT (la CI le disait déjà) ; resserrer l'outil a révélé 32 vraies dérives, surtout des lignes décalées par les `override`.
  - Pas de clé étrangère des tables de sécurité vers `User` (table de l'app, autre connecteur possible, sujet ≠ utilisateur) : un lien LOGIQUE, dessiné, sans contrainte — #499.
  - Balayage Studio par `watch.mjs` (5 s/écran, console + HTTP ≥ 400) : 43/46 propres, les 3 autres traités.
