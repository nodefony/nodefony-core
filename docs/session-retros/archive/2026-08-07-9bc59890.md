---
date: 2026-08-07
session_id: 9bc59890-3903-42d7-a99d-34d79d53b2c8
focus: Publier docs/performance (9 pages + rapport HTML) — rendu refusé faute de rattachement des mesures aux commits
---

# Session retro — 2026-08-07 — 9bc59890

## Fait

- **`docs/performance/` publié** (`cd972c07`) : 9 pages neuves (~1 970 l.) — hub, méthode,
  pipeline HTTP, boucle d'événements, ORM, comparaisons, instruments, dimensionnement, ouvertures.
  README refondu, rapport du 23-07 repassé en `superseded` avec sa section « ce qu'il a eu faux ».
- **Rapport HTML autonome** (144 Ko, 16 SVG dont 2 schémas générés, calculateur de pods), générateur
  versionné dans le skill `nodefony-load-test`.
- **4 micro-bancs rapatriés** de `tmp/` vers `scripts/micro/` + adaptés (chemin dist, table de
  routes paramétrable `NF_ROUTES_JSON` avec message de régénération, exit 1 vérifié sans pipe).
- **`doc-lint` corrigé** : une page datée (`2026-07-23-…`) n'est plus prise pour un ADR. Prouvé —
  7 ADR verts, un ADR amputé tombe toujours.
- **`tmp/` nettoyé** : 309 Mo → 3,5 Mo (périmètre choisi par le user), compteurs `doc-lint`
  régénérés derrière.
- **Rendu contesté puis corrigé** (`3ac40c54`) : table de chronologie mesure → état du code, dossier
  repassé en `draft`.

## Coûts évidents

- **Le vrai coût de la session n'est pas la production, c'est le rendu refusé.** ~2 000 lignes de
  doc et un rapport HTML écrits avant de s'apercevoir qu'il manquait la seule chose qui les rend
  vérifiables : le commit de chaque mesure.
- Deux délégations `haiku` recontrôlées, deux erreurs trouvées → le run n'a pas fait gagner de
  temps sur ce lot (le `rg` équivalent tenait en deux commandes).
- Extraction des données des rapports HTML : faite par automate (bon réflexe), mais 3 rapports sur
  9 n'embarquaient pas de bloc `report-data` → parsing de tableaux HTML en secours.

## Recommandations

1. **Graver « machine + protocole + COMMIT »** dans le standard de rapport (`docs/performance/README.md`
   liste déjà 5 exigences — le commit doit devenir la 6ᵉ, et le kit perf doit l'exiger à l'écriture).
2. **Comparer mécaniquement un livrable dérivé à sa source** avant de le rendre (fait ici, a trouvé
   l'écart 0,93/0,98) — candidat à un petit script réutilisable.
3. **Ne pas déléguer le verdict qui part en publication** : déléguer la lecture large, trancher soi-même.

## Patterns récurrents (déjà gérés)

- ✅ `timeout` absent sur macOS — reconnu immédiatement (déjà gravé).
- ✅ `$?` après un pipe en zsh — piège attendu, exit réel vérifié sans pipe.
- ✅ `rg -r` bloqué par le hook, `cd` relatif bloqué par le hook — les deux gardes ont mordu.
- ✅ Vérification **après** Prettier, pas avant (gates rejoués post-commit).

## Commits produits

| Commit     | Sujet                                                                   |
| ---------- | ----------------------------------------------------------------------- |
| `cd972c07` | docs(performance) : le dossier complet — méthode, mesures, et les trous |
| `3ac40c54` | docs(performance) : rattacher chaque mesure à son état de code — draft  |
