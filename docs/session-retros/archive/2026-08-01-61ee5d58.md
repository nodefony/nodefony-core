---
date: 2026-08-01
session_id: 61ee5d58-f7d0-4b59-a297-5b60fef05572
focus: T17 jouée puis fermée — le trou n'était pas dans la doc, il était dans le test généré
---

# Session retro — 2026-08-01 — 61ee5d58

## Focus

Jouer T17 et T19 en réel (suite de la session du matin), puis fermer ce que T17 a prouvé.
En chemin : audit des trois sous-agents, plancher de délégation mesuré, CI réparée.

## Fait

- **T19 → PASS** (1 run, 13 tours, 0,12 $) — l'agent recopie le patron `@RealtimeChannel` présent
  dans le `LiveController` généré. 13 tours contre 39 pour T17 : un patron sous les yeux vaut
  toute la doc.
- **T17 → FAIL 4/4**, cause unique et reproductible : `pattern: "^/api/account/(profile|invoices)"`
  — la zone énumère les routes du jour au lieu de couvrir le préfixe.
- **Fermeture en trois gestes**, dont un seul a compté :
  | geste                                  | effet          |
  | -------------------------------------- | -------------- |
  | `AGENTS.md` généré + gabarit de config | 0/4 → 1/3      |
  | règle `check` `firewall-area-enumere`  | 1/3 (inchangé) |
  | **test e2e généré authentifié**        | **2/2 — PASS** |
- **Cause réelle** : le test e2e de `create entity` n'authentifiait que le `DELETE` et présumait le
  reste du CRUD ouvert. Protéger l'espace cassait 3 cas sur 4 ; rouvrir une brèche était le moyen
  le moins coûteux de les reverdir. L'agent ne l'ignorait pas — l'outil le punissait.
- **`hook-lifecycle-inconnu`** — 2ᵉ règle de `check` : `onKernelBooted()` compile et n'est jamais
  appelé (`setEvents` n'attache que trois noms). Issue d'un audit dont la conclusion était fausse.
- **Trois sous-agents recadrés** + `CLAUDE.md` §5 : plancher mesuré à ~33 k tokens/délégation,
  `effort: low` + `maxTurns` posés (jamais vus agir — gain supposé).
- **CI réparée** : 3 selftests écrits la veille que personne n'appelait, 1 renvoi mort.
- **T17 figée** dans `baseline.json` (PASS 2/2).

## Coûts évidents

- 4 séries de T17 (×4, ×3, ×3, ×3) + T19 : ~13 runs d'agent, ~5 $ — le poste principal, et
  justifié : c'est la seule chose qui a départagé les trois correctifs.
- Un sous-agent `fable` sur le corpus retex+mémoires (124 k tokens) — rentable, rapport exploité.
- Un sous-agent `haiku` d'audit doc (91 k) — rendu faux, mais son candidat rejeté était le bon.
- Deux délégations `run-log-report` sur des journaux de 37 lignes : **perte sèche**, à l'origine
  du recadrage des seuils.

## Recommandations

1. **Instruire T22 (CSP, FAIL 1/3)** — seul autre FAIL ferme du banc sécurité, jamais creusé.
2. **Enregistrer les 6 tâches sécurité hors référence** — sans elles `--depistage` est aveugle.
3. **Vérifier le risque de throttle** introduit par `connexionAdmin()` par fichier e2e.

## Patterns récurrents (déjà gérés)

- ✅ Suspecter son instrument avant le sujet mesuré — a servi trois fois aujourd'hui.
- ✅ Prouver sur l'artefact reçu — le banc de vérité a validé le gabarit rendu, pas le `.tpl`.
- ✅ Un gate neuf doit être vu rouge — les deux règles de `check` débranchées puis rebranchées.

## Commits produits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `db2899c9` | un sous-agent coûte 33 k tokens avant d'avoir rien lu             |
| `0b9b7d4d` | une zone qui énumère des routes laisse naître la suivante ouverte |
| `cdede441` | trois auto-contrôles écrits hier, que personne n'appelait         |
| `acdbda1e` | un test généré qui casse quand on protège pousse à ouvrir         |
