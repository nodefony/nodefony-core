---
date: 2026-07-19
session_id: 8eb85651-2323-4c6c-95a3-2c2ac82fb8c5
focus: Vagues 2 et 3 de la doc (15 pages) + réparation du portail Studio (hubs, navigation, cards)
---

# Session retro — 2026-07-19 — 8eb85651 (session c)

## Fait

- **Vague 2 sécurité** (`b7063371`) : 9 pages au gabarit (cors, headers, oauth2, webauthn, totp
  reprises ; api-keys, webhooks, audit créées) + les 2 hubs (`docs/index.md`, `security/index.md`).
  1196 ancres résolues, 25 blocs « Démarrage rapide » compilent.
- **Vague 3 architecture** (`bcfe0054`) : les 6 pages transverses, 5-20 Ko → 36-44 Ko.
- **Portail réparé** (`86965f92`) : résolution serveur des liens en slugs, `isHub`, scan des modules
  **installés** (8 → 13 indexés), cards rendues côté React.
- **Accueil + menu de la doc** (`1c2d7ad4`, `7c5b7cf4`) : bloc déclaratif `nodefony-cards`, accueil
  = catalogue rangé par type (27 cards), parcours guidés déplacés vers `docs/demarrer.md`.
- **Outillage** : `code-check.mjs` créé (5ᵉ gate, que le standard nommait sans qu'il existe) ;
  `doc-lint` gagne navigation, liens morts, régime hub ; skill doc bumpé 2.1.0.
- **Rangement** : `tmp/doc-corpus/` supprimé, ledger porté en mémoire IA (kit de chantier).

## Coûts évidents

- **1 agent lancé pour rien** (accueil _Studio_ au lieu de l'accueil _doc_) — arrêté avant écriture,
  mais ~1 min d'agent et un aller-retour perdus sur une ambiguïté que j'aurais dû lever.
- **2 dérives de `cwd`** (typecheck puis prettier lancés depuis un sous-dossier) → un `grep` a menti,
  et j'ai cru avoir édité le mauvais fichier. Piège déjà connu, re-tombé dedans deux fois.
- 12 agents de rédaction, 160-280 k tokens chacun. C'est le poste dominant de la session.

## Recommandations

1. **Vérifier dans le rendu que l'utilisateur regarde**, jamais dans son propre outil — le défaut
   central de la session (cf RETEX.md, thème Doc).
2. **Un test qui scanne plus que le code testé ment par optimisme** — aligner le banc sur la source
   réelle (ici : `corpusLinks` scannait tout le dépôt, le service ne voyait que les modules chargés).
3. **`cd` absolu dans le MÊME appel Bash** — la règle existe, l'appliquer vraiment.

## Patterns récurrents (déjà gérés)

- ✅ Agents parallèles plafonnés à 4, `test-map.json` alimenté par l'orchestrateur seul (0 conflit).
- ✅ Gates ×5 avant « fait » : chacun a attrapé un défaut réel cette session encore.

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `b7063371` | vague 2 sécurité — 9 pages + hubs de navigation              |
| `0d86aac6` | skill doc 2.1.0 — navigation par hubs, gate de compilabilité |
| `86965f92` | portail : hubs visibles et navigation réparée                |
| `bcfe0054` | vague 3 architecture — 6 pages transverses                   |
| `1c2d7ad4` | accueil en cards + menu par familles                         |
| `7c5b7cf4` | accueil = catalogue de cards classé par type                 |
