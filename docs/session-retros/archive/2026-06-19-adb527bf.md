---
date: 2026-06-19
session_id: adb527bf-a502-4c92-951f-80338db4a5b4
focus: fix bug PROD auth — provisioning du service "users" par l'app (+ NF_USER_STORE drizzle/memory)
---

# Session retro — 2026-06-19 — adb527bf

## Focus

Fix du bug prod découvert le matin : auth 100 % cassée hors dev (HTTP 500 « aucun service
users ») car seul le module test (dev-only) posait `"users"`. L'app le provisionne désormais.

## Fait

- **App racine pose `"users"` au boot** (`App.onKernelReady → provisionUsers`, `nodefony/security/`),
  dev ET prod. Dépôt choisi par `NF_USER_STORE` : `drizzle` (défaut) | `memory` (volatil, 0 I/O — pour
  les tests de charge / scripts). Seed idempotent : dev = fixtures `admin/user:secret` ; prod = admin
  via `NF_ADMIN_PASSWORD` seulement (jamais de mdp par défaut — hash de `secret` public).
- **`InMemoryUserRepository` promu** module test → `@nodefony/user` (dépôt de référence réutilisable).
  Fixtures → `nodefony/security/devUsers.ts`. Module test ne pose plus `"users"`.
- Vars + docs : `env.ts`, `.env.example`, CLAUDE/MEMORY de `@nodefony/user` et du module test.
- Commits poussés : **`3d140de1`** (fix) + **`fd67db6d`** (docs).

## Frictions / leçons

- **Analyse d'impact biaisée par les tests UNIT** : j'ai d'abord conclu « aucun test ne dépend du
  `users` de test » en grepant les imports `*.test.ts`. FAUX : ~10 bancs d'INTÉGRATION bootent le
  serveur dev et tapent `/nodefony/test/secure/*` → en dépendent sans l'importer. (→ RETEX Vérification.)
- **DB dev au schéma périmé** : `CREATE TABLE IF NOT EXISTS` ne migre pas → `User` sans `createdAt` →
  `SqliteError` au login (pas au boot). Drop + reboot. (→ RETEX Build/dist/boot.)
- **Payload BFF login = `{username, password}`** (pas `identifier`) : vérifier le contrat du controller
  (`SessionAuthController`), pas supposer — un mauvais champ → 401 trompeur (≠ le 500 du bug).

## Décision structurante

Le bug n'était PAS la divergence dev/prod mais l'ABSENCE de `users` en prod. La divergence
fixture-in-memory(dev) / Drizzle(prod) est SAINE. Garder le banc déterministe (in-memory) + poser
Drizzle en prod, plutôt que tout unifier (casserait 10 bancs + non-déterminisme DB persistante).

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `3d140de1` | fix(security): provisionne le service users dans l'app        |
| `fd67db6d` | docs(user,test): users provisionné par l'app + InMemory promu |
