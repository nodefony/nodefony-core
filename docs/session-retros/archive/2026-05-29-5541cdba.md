---
date: 2026-05-29
session_id: 5541cdba-d66c-4aab-8781-55bd35076ca1
focus: Résilience de boot Phase 3 (garde lifecycle + gain perf emitAsync) + dette config ordering RÉSOLUE + diagnostic dette CLI
---

# Session retro — 2026-05-29 — 5541cdba

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    63 |
| Edit  |    51 |
| Read  |    42 |
| Write |     9 |
| Skill |     2 |

## Top fichiers Read/Edit

| Fichier                         | Read | Edit |
| ------------------------------- | ---: | ---: |
| `kernel/Kernel.ts`              |    3 |   13 |
| `kernel/Module.ts`              |    4 |    5 |
| `tests/KernelLifecycle.test.ts` |    2 |    5 |
| `tests/Module.test.ts`          |    3 |    3 |
| `Event.ts`                      |    1 |    3 |
| `realtime/index.ts`             |    2 |    3 |

## Coûts évidents

- **Cache-dominé 83 %** : SKILL.md `nodefony-framework-dev` (très long) chargé au début + reporté à chaque
  turn (cw1h 826k tok = 24,8 $) + Kernel.ts relu/réédité (13 edits). Prix assumé d'une session cœur riche.
- **2 restarts serveur** (Phase 3 + fix config) — justifiés (vérif runtime à chaque livrable), pas fusionnables.
- **2 builds turbo** (~2m37 le 2e) — nécessaires (core + app racine modifiés).
- 159 turns / transcript 3,1 MB → la longueur de la session (3 sujets enchaînés) gonfle le cache read.

## 💶 Coût (€)

- Total **~107 € (≈ 116 $)** — cache read 72,3 $ (62 %) + cache write 1h 24,8 $ (21 %) + output 18,8 $ (16 %)
  - input 0,6 $. **Cache = 83 %** → c'est le CONTEXTE porté (kit framework-dev + CLAUDE.md + gros fichiers
    cœur) qui coûte, pas l'output. Coût/livrable ≈ 35 € (2 commits + diagnostic + 3 mémoires).
- Enseignement : sur une session cœur multi-sujet, le levier #1 reste **/clear entre sujets** — ici les 3
  sujets (Phase 3 / config / diagnostic CLI) étaient liés (même chaîne de boot) donc enchaînés à raison,
  mais le cache cumulé en paie le prix.

## Recommandations

1. **MAJ kit `nodefony-framework-dev`** (RETEX résilience boot + emitAsyncGuarded/fireLifecycle) — fait ce
   END : capitalise → moins de relecture Kernel.ts/Event.ts au prochain passage.
2. **Mémoire `project_cli_module_command_dispatch`** enrichie (diagnostic 2 effets de bord vendor↔kernel) —
   évite de re-investiguer la chaîne cli↔kernel à la prochaine session Phase 11.
3. Pattern « microbench A/B inline » (ancien vs nouveau côte à côte, hors repo) = bon outil pour prouver un
   gain hot path sans git stash. À réutiliser quand on touche `emitAsync`/le pipeline.

## Patterns récurrents (déjà gérés — appliqués sans friction)

- ✅ memory.test flake en pleine suite → **rejoué isolé = vérité** (async crashes 7/8→vert isolé). RETEX kit.
- ✅ `.git/index.lock` orphelin → `rm -f` après vérif `pgrep -x git` (lock 12:29 du build). [[feedback_git_index_lock]]
- ✅ commit FR via `-F` (pas `-m`) + header commitlint ≤100 (1ʳᵉ tentative 114 → raccourci).
- ✅ tests qui encodent l'ancien contrat → réécrits sur le nouveau (4 KernelLifecycle « throw en dev »).

## Commits produits

| Commit    | Sujet                                                               |
| --------- | ------------------------------------------------------------------- |
| `f3b749d` | feat(core): résilience de boot Ph.3 — garde du cycle de boot        |
| `55b7202` | fix(core): dette config ordering — override module-<name> avant Zod |
