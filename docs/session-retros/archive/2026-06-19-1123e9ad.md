---
date: 2026-06-19
session_id: 1123e9ad-d18f-4c97-a5ac-106f914a74a7
focus: Audit multi-tenant / SaaS-readiness + acté chantier P17 (post-cloud-native, post-release 10.0.0)
---

# Session retro — 2026-06-19 — 1123e9ad

## Focus

Session de **réflexion/architecture** (0 code livré). Pédagogie P6 (API Keys/Audit/Webhooks) +
panorama push (webhooks/WS/SSE/Web Push/MQTT/AMQP) + positionnement workflow + **correction
TCP/UDP realtime** (legacy) + **audit multi-tenant complet** → acté chantier P17.

## Fait

- **Pédago P6.12 API Keys** : entité + stores (Drizzle/Mongoose/Redis) DÉJÀ persistés (`IAccessTokenRecord`
  kind:`pat`), reste `ApiKeyAuthenticator` + manager + endpoints. Store conçu pour JWT (refresh+denylist),
  dimensionné PAT par anticipation.
- **Pédago** P6.14 (audit events+stream WS), P6.13 (webhooks HMAC SSRF/replay/secret chiffré), Web Push
  (3 RFC, angle mort = dépendance FCM/APNs), MQTT/AMQP (drivers `IRealtimeHub` futurs, niche intégration).
- **Correction TCP/UDP realtime** : le legacy `realtime-bundle` (tcp/udp/unix/spawnSocket) = capacité
  socket polymorphe du framework, PAS le transport navigateur. « Oublier TCP/UDP » n'était PAS acté.
- **Audit multi-tenant** (effort max, matrice actif×isolation + ancrages) : 🔴 pas câblé MAIS archi saine.
  Modèle acté = pool + scope auto fail-closed + tenant ALS (pas IUser) + membership user×tenant×rôles.
  Livraison = module `@nodefony/tenant` + 1 seam scope orm-core + hook resolver firewall.
- **Garantie security vérifiée sur code** : contrats stables, modifs additives ; seul point profond =
  `UserToken.getRoles()` figé sur rôles globaux → P6.8 à concevoir tenant-aware.
- Écrits : mémoire `project_multitenant_chantier_kit` + section P17 MIGRATION_STATUS (commit `d0c3f95a`).

## Frictions (→ RETEX.md SAS)

- Présenté un brainstorming (« TCP/UDP oubliés ») comme acté → corrigé par user + legacy.
- Sur-promis « se dev par-dessus security sans modif » → nuancé après lecture code (additif sous condition P6.8).
- User a paniqué « erreur d'archi ? » → recadré par faits comparatifs (aucun framework multi-tenant par défaut).

## Commits produits

| Commit   | Sujet                                                                                |
| -------- | ------------------------------------------------------------------------------------ |
| d0c3f95a | docs(migration): chantier P17 multi-tenant — post-cloud-native + post-release 10.0.0 |
