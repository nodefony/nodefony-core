---
date: 2026-07-26
session_id: 52f920be-e9f9-49b0-bdcf-25fd0119b7e8
focus: README public + fiches des paquets, puis les 3 échecs du banc de découvrabilité
---

# Session retro — 2026-07-26 — 52f920be

## Fait

- **README public réécrit** (from scratch, cible « cinq minutes ») : cheminement 2017 → réécriture
  TypeScript, isomorphisme avec le refus assumé d'un portage, section **agent-ready**, équipe
  rouge / équipe bleue, logo et deux captures. Aucun framework tiers nommé.
- **Support de présentation** `scripts/readme-html.mjs` → 13 slides, matrice **calculée** depuis le
  dépôt, captures embarquées, zéro ressource externe. Sortie non versionnée.
- **14 fiches de paquets revues** : 3 affirmaient des faits faux (préréglage Svelte inexistant,
  console déclarée privée sans l'être, mécanisme livré annoncé comme restant) ; `user` et
  `orm-core` réécrites (tableau de phases faux, 55 lignes dupliquées).
- **Les 3 tâches en échec du banc PASSENT.** Cause : le `CLAUDE.md` de l'app recopiait une
  sous-liste de générateurs, périmée depuis l'arrivée de `create command`.
- **3 défauts de harnais corrigés** : gates jouées à la fin de leur tâche (une tâche en recalait une
  autre), sondes d'observation, motifs qui mordaient sur une mention plutôt qu'un geste.
- **TSDoc de 6 décorateurs publics** (`injectable`, `services`, `controllers`, `HttpCode`,
  `Header`, `Redirect`) — le survol ne montrait que la signature.
- **BUG-1 ouvert** : `inspect --json` muet quand le boot échoue. Canal `--json` rétabli ; cause
  racine du silence non trouvée.

## Coûts évidents

- Deux investigations à rallonge sur le silence de `inspect` sans trancher la cause — arrêtées et
  consignées, conformément à la règle des trois tentatives.
- Un rejeu du banc perdu sur une dérive du répertoire courant (chemin relatif depuis l'app témoin).

## Recommandations

1. **Trancher BUG-1 en priorité** — du fail-silencieux dans le cœur contredit un principe du projet.
2. **Ne jamais écrire de règle dans un fichier qui n'est pas lu à chaque tour** — mesuré trois fois
   dans la même session.
3. **Toute sonde textuelle s'ancre sur le geste**, jamais sur un mot qui peut apparaître ailleurs.

## Patterns récurrents (déjà gérés)

- ✅ Le sous-agent propose, l'agent principal vérifie — deux affirmations plausibles et fausses
  écartées avant d'entrer dans le code (`@Redirect` n'empêche pas l'action, `@HttpCode` ne prime pas).
- ✅ Un test vert peut verrouiller un défaut : celui de `initSyslog` affirmait « aucun listener ».

## Commits produits

| Commit     | Sujet                                                         |
| ---------- | ------------------------------------------------------------- |
| `3b660295` | README public réécrit pour cinq minutes d'attention           |
| `3739794e` | les fiches des paquets disaient des choses fausses            |
| `2a34d8b7` | l'app générée cesse de recopier ce qu'elle doit pointer       |
| `af949f74` | une tâche ne peut plus être recalée par une autre             |
| `f557ac53` | en mode machine, une commande qui échoue cesse d'être muette  |
| `aec772a6` | le fichier de racine dit aussi comment piloter le serveur     |
| `6b34906d` | six décorateurs n'avaient rien à dire au survol               |
| `a623c96a` | la sonde d'arrêt bricolé comptait la règle comme sa violation |
