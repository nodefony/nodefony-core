---
date: 2026-07-23
session_id: b8b5861e-f4af-458c-a804-22d0a061b6e0
focus: geste pagination des stores (F27/F51) + durcissement skills framework-dev/load-test
---

# Session retro — 2026-07-23 — b8b5861e (7ᵉ du jour)

## Focus

Registre doc↔code, motif « mode adverse avalé » : faire respecter le contrat `IPage`
(offset/cursor mutuellement exclusifs) sur les 26 stores. + outillage (skills).

## Fait

- `de6165cb` geste pagination : garde `assertPageQuery` en tête de `listPage` sur **26 stores**,
  `PaginationModeError` mappée 4xx→**400** au data plane, curseur mort retiré de l'API webhooks (F27),
  8 contrats de test partagés durcis. Fan-out 4 subagents par module.
- `a78ae058` mapping 400 prouvé WIRE (`admin-dataplane` 47/47) + `corpusLinks` aligné sur le portail.
- `1012d552` `framework-dev` se déclenche « dès toute édition de code back » (+ fix parseur `trigger-bench`).
- `4d43650d` `load-test` RÈGLE N°2 : décor requis par banc e2e.
- F27/F51 soldés au registre (**76 ✅ · 6 🔶 · 37 restants**).
- Validation : `test:all --dialects` **7852 verts** toutes cibles infra ; gate mémoire 9/9.

## Frictions (→ RETEX.md)

- Le skill de dev ne se déclenche que si sa **description porte les MOTS du besoin** ; « pagination /
  store / listPage » absents → chargé tard, sur rappel user.
- **Bug parseur `trigger-bench`** : `Déclencheurs (…) :` (parenthèse avant le `:`) = **0 déclencheur
  extrait** → scoring « aberrant » chassé 30 min côté formule avant de voir que le parseur ne lisait rien.
- **`assert.rejects` ne capte pas un throw SYNCHRONE** d'un thunk (garde qui lève avant `Promise.resolve`
  d'un store non-async) → `try/catch`.
- **Un banc e2e a un DÉCOR** : 13/17 « KO » = décor opt-in / destructeurs (`graceful`/`cluster` tuent le
  serveur) / store persistant → **0 vrai bug**.
- **Un test qui scanne plus large que la prod = faux positifs** (`corpusLinks` scannait `session-retros`
  - suivait un symlink racine).

## Commits

| Commit     | Sujet                                                 |
| ---------- | ----------------------------------------------------- |
| `1012d552` | skills: framework-dev déclenchement code back         |
| `de6165cb` | feat(pagination): garde IPage sur 26 stores (F27/F51) |
| `a78ae058` | test: mapping 400 wire + corpusLinks aligné portail   |
| `4d43650d` | skills: load-test RÈGLE N°2 décor e2e                 |
