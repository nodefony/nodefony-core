---
date: 2026-05-30
session_id: 4ce1b677-fcd2-4824-b388-47bb9eb8e528
focus: Durcissement framework F5 (gotchas décorateurs → bug redirect RFC) + F7 (config Zod)
---

# Session retro — 2026-05-30 — 4ce1b677

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    61 |
| Edit  |    26 |
| Read  |    17 |
| Skill |     6 |
| Write |     3 |

## Top fichiers Read/Edit

| Fichier                                       |   × |
| --------------------------------------------- | --: |
| skills/nodefony-framework-dev/SKILL.md (Edit) |   8 |
| skills/nodefony-studio-dev/SKILL.md (Edit)    |   4 |
| framework/index.ts (Edit)                     |   3 |
| http/.../Response.ts (Edit)                   |   2 |

## Coûts évidents

- **Push bloqué 2× par pre-push tsc** (cascade dist périmés orm-core→user→security, PRÉ-EXISTANT,
  hors diff) → `npm run build` complet (~1 min) puis push OK. Le pre-push n'avait pas tourné depuis
  longtemps (F1–F4 jamais poussés). 3 itérations push pour un problème non lié au diff.
- **Pre-commit husky** a planté 1× (`git apply --index null` lint-staged + `.git/index.lock` orphelin)
  → `rm -f .git/index.lock` + retry `git ci` OK. Garde-fou connu ([[feedback_git_index_lock]]).
- Co-évolution skills lockstep = 12 Edits (framework-dev 8 + studio-dev 4) sur 2 bumps (F5+F7). Coût
  normal du contrat full-stack, mais 1 Edit skill rejeté par le user (gros bloc recette config).

## 💶 Coût (€)

- Total ~€47 (≈ $51.6) — **cache-dominé 87 %** (cache read $32 + cache write 1h $13 ; output $6 ; input $0.3).
  112 tours, 1 session sans `/clear` couvrant 2 features (F5+F7) + push + END. ~€23/feature = rentable.

## Recommandations

1. **Garde-fou pre-push « build complet d'abord »** — quand le pre-push n'a pas tourné depuis plusieurs
   commits, les dist périmés cascadent (orm-core→user→security). Réflexe : `npm run build` AVANT le
   1ᵉʳ push d'une série, pas après l'échec. (Pattern dist périmé déjà connu, à appliquer au push.)
2. **Pré-push intermédiaire** — pousser à la fin de CHAQUE session (pas accumuler F1–F4 non poussés)
   évite la cascade de dist périmés au push groupé.
3. ✅ Confirmer un « gotcha » daté contre le SOURCE avant de coder (la moitié des 5 étaient résolus/voulus).

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin → `rm` + `git ci` ([[feedback_git_index_lock]]).
- ✅ Dist périmé → `npm run build` ([[feedback_session_pitfalls]] / [[feedback_root_dist_stale_modules]]).
- ✅ memory.test flake (1000 GET / native-crash / abort burst) → vert isolé, hors diff.
- ✅ Lockstep skills back/front à chaque feature cœur.
- ✅ Commit FR à apostrophes via `git ci -F` (fichier).

## Commits produits

| Commit    | Sujet                                                         |
| --------- | ------------------------------------------------------------- |
| `f5fd8a2` | fix(http): redirect — whitelist codes 3xx RFC 9110 §15.4 (F5) |
| `03f081f` | feat(framework): config Zod validée au boot (F7)              |

(Poussés sur `claude-ts` : `fa04403..03f081f`, avec F1–F4 antérieurs.)
