---
date: 2026-06-14
session_id: 552bee9d-4d77-40a0-8ee8-acfc20195589
focus: P6 J4a — store de jetons, les 3 derniers drivers (Drizzle / Mongoose / Redis)
---

# Session retro — 2026-06-14 — 552bee9d

## Focus

Finir le store de jetons pluggable `ITokenStore` : coder les 3 drivers restants
(Drizzle SQL, Mongoose NoSQL, Redis infra) après Memory + File (matin).

## Fait

- `d25575e6` **feat(drizzle)** `DrizzleTokenStore` + 3 entités (`access_token`/`denied_jti`/
  `subject_revocation`, `createTokenEntities(orm)` dynamique). 13 tests intég, suite 60/60.
- `785233e7` **feat(mongoose)** `MongooseTokenStore` + 3 schémas (`_id` = jti/subjectId). 13 tests
  intég (MongoMemoryServer), suite 59/59.
- `3a631ae6` **feat(redis)** `RedisTokenStore` (HASH + TTL natif, `gc()` no-op). 13 tests intég
  (FakeRedis horloge + smoke réel env-gardé), suite 25/25.
- Build monorepo 19/19 vert, 0 TS strict. MIGRATION_STATUS P6 34→36 %, J4a ✅.

## Décisions

- **Approche B partout** : `@nodefony/security` en `import type` (0 dép runtime) + devDep + external
  rollup ; aucun auto-register, l'app câble `registerTokenStore` + `registerTokenEntities(orm)`.
- **Portable sans natif** : `IS NULL` inexprimable (`eq(col,null)` faux SQL ; Mongo type-bracketing
  exclut null du `$lte`) → idempotence `revoke` par read-then-write, `gc` PAT-sans-exp par
  find+filtre JS+`delete {$in}`. Drizzle/Mongoose 100 % `IRepository`.
- **Mongoose** : `_id` porte le jti (contrat hijacke `id`→`_id`), reads normalisés.
- **Redis HASH** (≠ blob JSON) : `markUsed` = `HSET` partiel préserve le TTL ; TTL natif → `gc` no-op.
- Entités **dynamiques** (`createTokenEntities(orm)`, convention `userTable`) ≠ `@entity` figé du kit
  → approche B = l'app choisit le connecteur.

## Frictions (→ RETEX.md)

- Contrat portable cache des pièges moteur (IS NULL / type-bracketing / id→_id) → lire le code repo
  AVANT de transposer (0 itération de débogage grâce à ça).
- Redis HASH vs JSON (user a challengé) ; tester le TTL sans serveur = double à horloge injectée +
  smoke réel pour prouver les noms de commandes ; pré-purger un store partagé avant un delta gc.

## Commits produits

| Commit   | Sujet                                                           |
| -------- | --------------------------------------------------------------- |
| d25575e6 | feat(drizzle) store de jetons (DrizzleTokenStore + 3 entités)   |
| 785233e7 | feat(mongoose) store de jetons (MongooseTokenStore + 3 schémas) |
| 3a631ae6 | feat(redis) store de jetons (RedisTokenStore HASH + TTL natif)  |
