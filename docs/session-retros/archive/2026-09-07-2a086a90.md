---
date: 2026-09-07
session_id: 2a086a90-d405-44c9-96dc-842c1203b490
focus: CI rouge (une seule cause) puis #263 et #266 — et la régression que 6000 tests verts n'ont pas vue
---

# Session retro — 2026-09-07 — 2a086a90

## Fait

- **CI rouge remise au vert (hors ticket)** — 5 workflows tombés, **une seule cause** :
  `TS4114` sur `OAuth2RequestError.code`, qui redéclare le `code?: any` augmenté globalement
  par `Error.ts:26`. Le cœur compile les sources de `@nodefony/security`, donc `nodefony#build`
  tombait et emportait tout. Puis le TROU qui l'a laissée passer : le `pre-push` existait et
  faisait le bon geste, mais rendait « 21/21 cached ».
- **#263** — quatrième axe `externalServices` sur `IRunProfile` (opt-in) ; un run qui ne déclare
  rien n'ouvre aucune connexion. Recensement des déclarants par `rg`, pas au jugé.
- **#266** — `diagnoseConnectionFailure` dans `orm-core`, appelé par les DEUX adapters ;
  distingue « personne n'écoute » de « un serveur a répondu et refuse », plateforme injectée.
- **Régression de #263 trouvée par le user**, corrigée dans la foulée (voir Frictions).

## Frictions

- **Le cache turbo rendait un vert sur du code jamais recompilé.** Le cœur lit les sources de cinq
  modules qu'il ne peut pas déclarer en dépendance (cycle inversé) : elles étaient hors de ses
  `inputs`. Corrigé par `$TURBO_ROOT$` dans `turbo.json`, éprouvé avant/après (EXIT 0 / 21 cached
  → EXIT 2 / `Failed: nodefony#build`).
- **Mon test vérifiait mon intention, pas sa conséquence.** La garde sautait le hook `onBoot`
  ENTIER — donc l'ORM n'était plus créé, donc son plan d'administration disparaissait :
  `doctor --live` répondait « état des migrations introuvable (404) ». Aucune suite ne l'exerce.
  Le user a posé la question qui l'a fait apparaître.
- **Une garde posée trop haut retire plus que ce qu'elle vise** — descendue au seul point qui
  ouvre un socket, et `connectAll()` appelée explicitement reste un ORDRE (`connect = true`).
  Quatre tests d'intégration préexistants le disaient : ils étaient le signal, pas du bruit.
- **`test:all` : les rouges changent d'une passe à l'autre.** 6 puis 4, jamais les mêmes hors mes
  4 reproductibles — tous verts en isolé (realtime 494, migrate-adopt 12, doctorEnvTarget 15).
- **Le gate de langue a mordu sur mon propre code** (`geste` → `portHint`).

## Commits produits

| Commit     | Sujet                                                                      |
| ---------- | -------------------------------------------------------------------------- |
| `4793a057` | `override` sur le code d'erreur OAuth2 — la CI ne bâtissait plus           |
| `afbb4220` | le cache turbo du cœur ignorait les sources qu'il compile — faux vert      |
| `cf9b5ad4` | un run DÉCLARE s'il a besoin de l'infrastructure — #263                    |
| `0d9ff149` | l'ORM existe même sans connexion, et l'erreur dit qui tient le port — #266 |

## Non exercé

- Critère 1 de #263 (`npm create nodefony@alpha` de bout en bout) : le décor consomme les paquets
  npm **alpha.2**, pas ce code. Vérifiable seulement à l'alpha.3.
- Phases `test:all` non lancées : suite de charge/mémoire, MySQL Community.
