---
date: 2026-09-25
session_id: ee01fa8d-5b1c-471f-8ffb-74c49a10748c
focus: CI rouge (licence), #293 types sans réécriture, 193 ancres, audit des paquets minces
---

# Retex 2026-09-25 — ee01fa8d

- **Fait** : `b4ed7bb4a` fix(licenses) — conjonction examinée `Apache-2.0 AND BSD-3-Clause`
  (`@bufbuild/protobuf` via Angular 22.2, CI rouge) · `34c09388b` build(release) #293 FERMÉ —
  condition `nodefony-source` + `customConditions`, plus aucune réécriture au pack, audit qui
  refuse un `types` hors `files`, test sur tarball réel vu rouge en TS7016 · `0f638d980`
  `26b20f6ab` `edd286e62` docs — ancres 193 → 6 SUSPECT (6 = faux positifs vérifiés) ·
  `e4d8603d4` `.claude/worktrees/` ignoré · `59d5248ab` docs — 17 affirmations fausses
  (rôle système `ROLE_NODEFONY_ADMIN`, 7 outils MCP, ancres sur le mauvais bloc).
- **Frictions** :
  - haiku rate le « situer par le sens » (~25 % de citations fausses) → sas, thème 🎯.
  - audits G15/G16 encore minces malgré la consigne → sas, thème 🎯.
  - `git add -A` a embarqué le worktree d'un sous-agent comme dépôt imbriqué (commit corrigé
    avant push, `.gitignore` posé) → [[feedback_scope_wider_than_intended]].
  - deux garde-fous ont crié faux sur du TEXTE : le hook anti-force-push sur un message de commit
    contenant `--force`, `skills-doc` sur `` `nodefony-source` `` lu comme un nom de skill →
    [[feedback_gate_must_run]].
- **Décisions** : `customConditions` plutôt que `publishConfig` (npm n'applique pas
  `publishConfig.exports`) ; le gabarit d'app ne déclare PAS la condition, même en `--link` —
  une app liée doit voir ce que voit l'installeur ; point 4 de #293 tranché par argument (le user),
  la preuve par installation va à #294 ; une conjonction de licences s'accepte DÉCLARÉE, jamais
  déduite d'un `AND`.
