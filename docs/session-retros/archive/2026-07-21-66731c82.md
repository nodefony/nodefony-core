---
date: 2026-07-21
session_id: 66731c82-5244-4d3d-9c76-f82ab96d7d35
focus: vague 4ter doc (6 pages) + Lot 3 sécurité (F82 plancher WS, F84 révocation realtime)
---

# Session retro — 2026-07-21b — 66731c82

## Focus

Suite du chantier doc + attaque des écarts code↔doc. Deux blocs : **vague 4ter** (6 pages de
référence manquantes des hubs cœur) puis **Lot 3 sécurité** (2 des ~12 écarts 🔴 traités).

## Fait

- **Vague 4ter doc** (`325d8e47`) : 6 pages créées, 4 gates verts chacune —
  http : `cookies` · `upload` · `rate-limit` · `observabilite` ; framework : `admin` (AdminBroker) ·
  `templates` (Eta). Les 2 hubs cœur (http/framework) liés (12 cards) ; F-tpl-1 corrigé (le hub
  framework prétendait à tort une config Zod pour Eta). Corpus 54→60 conformes ; corpusLinks 5/5.
  Méthode : 6 rédacteurs (2 salves de 4+2), test-map pré-rempli, contradictions rapportées.
- **F82** (`e1698aa8`) : plancher système WS armé **inconditionnellement** dans `Firewall.#wireRealtime`
  (avant : seulement si zone `security && realtime` → `syslog:`/`security:audit` servis à l'anonyme
  sans zone). Fail-closed. Tests-first (rouge→vert). security 842/842, realtime 327/327.
- **F84** (`e618c234`) : révocation realtime fail-closed — `buildSessionRevalidator` ne renvoie plus
  `null` (session illisible → `isValid()→false` → socket révoquée 4001 au tick) + fail-loud (WARNING).
  Aligné sur l'intention déjà écrite du hub. security 843/843, realtime 327/327.

## Frictions / leçons (→ RETEX.md)

- Le KIT D'ÉCARTS périme : F82 à moitié corrigé (revue 0.6), F84 à moitié intentionné (commentaire
  hub) → relire le code cible AVANT d'appliquer une « suite à donner ».
- Un TEST EXISTANT fige l'ancien fail-open qu'un fix sécu corrige exprès (2× ce jour) → le réécrire
  pour asserter le fail-closed ET le prouver, pas juste le faire passer.
- Orchestration : test-map pré-rempli avant la salve ; agents rapportent les tests manquants.
- Outillage : `code-check` faux rouge sur `npm notice` (→ `NO_UPDATE_NOTIFIER=1`) ; commitlint refuse
  un header commençant par un token majuscule (→ sujet en minuscule).

## Reste — prochaine étape

Lot 3 : **F83** (backplane non signé) · F82 cas (2) (plancher dans le hub sans security) · F67 (redis
dégradation silencieuse) · F27/F28 (webhooks) · F51 (pagination stores) · F40 (`initialize()` avant
firewall) · F18 · F44. Écarts vague 4ter (F184-F188) = correctifs Lot 1 (doc/TSDoc, non urgents).

## Commits

| Commit     | Sujet                                               |
| ---------- | --------------------------------------------------- |
| `325d8e47` | docs(http,framework): vague 4ter (6 pages + 2 hubs) |
| `e1698aa8` | fix(security): F82 plancher système WS fail-closed  |
| `e618c234` | fix(security): F84 révocation realtime fail-closed  |
