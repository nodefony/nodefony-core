---
date: 2026-06-03
session_id: 695bc070-ceb9-4c18-82d1-946256684159
focus: Phase B (park centralisé via lifetime) + isTTY, puis ménage boot piloté par audit (rollup service, build --force, @inquirer lazy)
---

# Session retro — 2026-06-03 — 695bc070

## Focus

Suite directe du refacto `KernelType → IRunProfile` (2026-06-02) : rendre `lifetime`
EFFECTIF (Phase B) + champ `isTTY`. Puis l'échange a dérivé (questions user HMR/reload)
vers un **ménage du poids de boot** piloté par un audit d'import.

## Fait — 4 commits (poussés en fin de session)

- `b55c753` **feat(core)** — Phase B : `Kernel.park({keepAlive})` = source UNIQUE du park
  (remplace 3 `new Promise(()=>{})` inline) ; `finishOrPark()` au gate `onReady` → park si
  `lifetime:"longrunning" && !servers` (daemon auto) ; `lifetime` déclaratif (option commande) ;
  champ `isTTY` (résolu 1× boot, `NO_TTY` override) + ligne `tty` au banner dev. Validé cluster RÉEL.
- `6a1dcd4` **refactor(core)** — retrait du **service rollup runtime** (doublon de config + toolchain
  rollup importée à chaque boot serveur). `nodefony build` recâblée sur `npx turbo run build`.
  **A/B boot mesuré : 2642 → 2264 ms (−378 ms, ~14 %) + ~23 MB heap.**
- `a71d004` **feat(cli)** — `nodefony build --force` (passe `--force` à turbo).
- `68dd86e` **perf(core)** — `@inquirer/prompts` lazy (`loadPrompts()` dynamique, interactif only) →
  hors du graphe de boot des commandes non-interactives (−~39 ms/−7 MB).

## Audit poids d'import boot (livrable analyse)

Coût marginal (production -w1) : imports ~1130 ms / 94 MB heap. **Drizzle domine : 423 ms / 43 MB**
(cœur `drizzle-orm`, pas les dialectes — l'adapter ne charge que sqlite aujourd'hui). sequelize
127 ms/6 MB (condamné, virage ORM). core+http = baseline. **frontend/vite déjà lazy** (7 ms). La
phase « Serveurs » (~1 s, cert TLS + bind) = autre moitié du boot, axe séparé.

## Décisions / POURQUOI

- **Drizzle : on ne retire AUCUN driver.** Le futur multi-dialecte = **lazy par dialecte gaté par
  config** (pattern `@inquirer`), pas un hardcode. Tous les drivers restent supportés, chaque pod paie
  le sien. À faire dans le virage ORM. (Réaction user juste.)
- Park master/supervisor = `keepAlive:false` (ils ont leurs handles → 0 timer parasite bloquant la
  sortie) ; daemon = `keepAlive:true`. Validé : SIGTERM master → sortie autonome ~1 s, 0 zombie.

## Frictions (→ RETEX.md)

- Orphelins serveur au titre renommé → `pkill -f bin/nodefony` les rate ; tuer par PORT (`lsof -ti`).
- A/B de boot via mode serveur = flaky (socket lingering) → mesure d'import isolée / mode sans port.

## Commits produits

| Commit  | Sujet                                                          |
| ------- | -------------------------------------------------------------- |
| b55c753 | feat(core): park centralisé `lifetime` + isTTY (Phase B)       |
| 6a1dcd4 | refactor(core): retrait service rollup runtime (build = turbo) |
| a71d004 | feat(cli): `nodefony build --force`                            |
| 68dd86e | perf(core): `@inquirer/prompts` lazy                           |
