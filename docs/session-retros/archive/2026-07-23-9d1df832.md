---
date: 2026-07-23
session_id: 9d1df832-a1b5-4238-8ac4-680972bfedd9
focus: Resserrement du parc de skills (28→22) — fusions, splits, gates durcis, fiche = certificat
---

# Session retro — 2026-07-23 — 9d1df832

## Focus

Exécuter le resserrement de l'outillage d'agents décidé la session précédente (étude
`docs/outillage-agents.md`) : fusions, découpes, retraits, et rendre la conformité **visible et
vérifiable**. Parc **28 → 22 skills, 0 capacité perdue**.

## Fait

- **`517eaf4e`** — Fusion des 4 skills d'inspection en `nodefony-inspect` ; `frontend-verify` absorbé
  par `frontend-dev §8` ; `nestjs` retiré. Nouveau contrôle de gate : **aucun renvoi vers un skill
  inexistant** (prouvé par contrôle négatif). §6 « fusionner/absorber/retirer » ajoutée à `nodefony-skill`.
- **`25aa267c`** — Corps > 500 déportés (`session` 563→442, `documentation` 658→456) ; changelogs
  retirés (de-journal) ; standard AAIF rafraîchi → révèle 2 dérives du gate (`compatibility`, regex
  `name`) corrigées ; **fiche = certificat de conformité** (badge normatif/recommandé/projet +
  citation de la règle) ; **banc à 4 mesures** (phrases, cas négatifs, couverture, recouvrements
  arbitrés vs ouverts).
- **`ba1c6e7e`** — Fiches générées exclues de prettier (fin du diff perpétuel, idempotence prouvée).
- **`53b65805`** — `roadmap` recentré sur P12 (couche IA, seule phase future) ; phases livrées 10/13/14
  → pointeurs. 171→122 l.
- **`58d46bc1`** — `ts-docs` retiré (0 usage + 2/4 URLs pourries 2 mois), sources repliées dans
  `framework-dev §1`. `rfc` gardé (4 invocations, URLs revérifiées valides).
- `MIGRATION_STATUS.md` : ligne devkit LOT 0 mise à jour (28→22, gates durcis).

## Frictions / leçons (→ RETEX.md)

- Un lien pourri non remarqué en 2 mois = preuve de non-usage (plus fiable qu'un compteur).
- Rafraîchir une norme externe révèle la dérive silencieuse du gate qui la contrôle.
- Générateur + formateur sur les mêmes fichiers = diff perpétuel → un seul formateur (le générateur).
- `EXIT=$?` après `| grep` lit grep, pas node (cf `feedback_shell_false_diagnostics`) ; un `perl`
  qui remplace une ligne non unique frappe la mauvaise occurrence (cf `feedback_code_rewrite_mechanical_traps`).

## Commits produits

| Commit     | Sujet                                             |
| ---------- | ------------------------------------------------- |
| `517eaf4e` | fusion 4 skills → inspect + gate anti-renvoi-mort |
| `25aa267c` | corps déportés + fiche certif + banc enrichi      |
| `ba1c6e7e` | fiches hors prettier                              |
| `53b65805` | roadmap recentré IA                               |
| `58d46bc1` | ts-docs retiré                                    |

Gates verts en clôture : skills-doc 22/22 · trigger-bench 47/47 + 5/5 négatifs + 22/22 couverture

- 0 recouvrement ouvert · scripts-audit 76/76. Tout poussé sur `claude-ts`.
