---
date: 2026-09-25
session_id: 6b6f6892-889e-4e9b-964b-cb06243cb559
focus: #485 portée « request » de l'injecteur, audit fable de fin de chantier #481, ticket multi-tenant #492
---

# Retex 2026-09-25 — 6b6f6892

- **Fait** : `ca37ca339` feat(core)! portée request · `62eac0463` test(http) bancs serveur + gate · `274df7d0d` chore(bench) `micro-request-scope.mjs` · `9af206c2a` fix(core) durcissement après audit · `aaa2ddf40` feat(framework) refus au démarrage des contrôleurs captifs · `4cef065dc` test(http) sonde bornée, seuils re-mesurés. Audit `fable` : 11 constats, 8 traités. #485 reste ouvert (constats 8 et 10). #492 ouvert (multi-tenant, 12.0.0). Exercice multi-tenant versé en commentaire de #486.
- **Frictions** :
  - un banc MAISON écrit avant de charger `nodefony-load-test`, qui avait l'instrument (`micro-enterscope`, `bench-ab-mono.sh` + sonde) — rappel du user ; la méthode était écrite → `feedback_written_rule_needs_reread` ;
  - deux marqueurs de `dist` faux (un motif présent aussi à la DÉFINITION de la méthode ; un `&& false` replié par le bundler), une boucle zsh non découpée, un `${PIPESTATUS}` vide, un `str.replace` Python sans `assert` qui n'a rien remplacé (import mort vu par oxlint au commit) → `feedback_suspect_instrument_and_own_diff`, `feedback_shell_false_diagnostics` ;
  - la sonde du module test grossissait à chaque requête : la pente publiée (0,29 Ko) était surtout la sienne (0,04 une fois bornée) — vu par l'audit → `feedback_bench_probe_false_verdicts` ;
  - l'audit a trouvé deux défauts majeurs sous des tests verts (masquage d'un service du kernel ; singletons paresseux non refusés au boot) → `feedback_outside_look_finds_what_green_hides` ;
  - A/B à thermal inégal, RPS absolu sans régime, stash refusé par le hook → versés au sas (thème 🎯).
- **Décisions** :
  - la portée d'un DÉTENTEUR se lit sans coût sur le chemin chaud : pile de CONSTRUCTEURS, statique `scope` lu avant `Reflect.getMetadata`, analyse seulement quand un service request est résolu — l'A/B montre l'instanciation d'un contrôleur inchangée ;
  - pas de garde dans `Injector.instantiate()` (un `getMetadata` par requête) : limite documentée ;
  - dépendance captive = `BootConfigurationError` (fatale partout) ; barrière au DÉMARRAGE par analyse des déclarations, même règle et même message que la résolution ;
  - un service request listé dans `@services` est DÉCLARÉ, pas instancié ; `addService` le refuse ;
  - `clean()` reste synchrone ; un `async clean()` qui rejette est journalisé ;
  - #485 non fermé : son critère 6 exige « corrigé ou tracé » et le user a gardé 8 et 10 pour la prochaine session plutôt qu'en tickets.
