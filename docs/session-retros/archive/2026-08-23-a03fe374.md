---
date: 2026-08-23
session_id: a03fe374-c708-4bad-86f8-bf1e3b3897e3
focus: Dette env NODEFONY_* → NF_* soldée, puis la porte MCP passe en portée PROJET chez Vibe et Codex
---

# Session retro — 2026-08-23 — a03fe374

## Fait

- **Dette ⑤ soldée** : 18 variables `NODEFONY_*` → `NF_*`, rupture nette sans alias. 85 fichiers
  du dépôt + 21 de la mémoire IA. `MIGRATION_STATUS` passe de « APRÈS RELEASE » à « AVANT RELEASE »,
  chiffres recalés (il annonçait 47 lues / 12 `NF_` ; terrain : 122 / 90).
- **CI réparée** : `npm run skills:check` était rouge depuis 4 commits (renvoi mort
  `lib/http-probe.mjs` sans `scripts/`, `env-decor.selftest.mjs` orphelin). Les 5 workflows verts.
- **Dette ③ soldée** : `~/.vibe/config.toml` corrigé (entrée `models` partielle refusée par
  `mcp add`, acceptée au démarrage — bug de vibe localisé : `load_persistence_layer()` valide UNE
  couche contre le schéma complet, sans la fusion qui la complète).
- **Chantier de design** : la porte MCP se déclare désormais par PROJET chez Vibe et Codex, via
  redirection de `VIBE_HOME`/`CODEX_HOME` — leur CLI, leur format, dans le projet.
- **Preuves ancrées** : qui lit `.agents/skills` (vibe oui, gemini oui et en priorité, claude non,
  codex pas de skills) — le TSDoc affirmait « la convention qui a émergé » sans nommer un client.

## Frictions du jour

- **Mon inventaire de départ était faux de moitié** — le motif comptait `ROLE_NODEFONY_ADMIN`
  (263×) et le connecteur nommé `nodefony`. La sonde juste est l'USAGE (`process.env.X`), pas le nom.
- **`rg` ignore les dossiers cachés en silence** : `.claude/` et `.github/` hors de l'inventaire.
- **L'exit code lu était celui du `echo` final**, pas de `npm` : « exit 0 » annoncé sur une passe
  qui sortait en 1.
- **Un renommage mécanique a détruit un témoin de test** : le selftest gravait « l'ancienne forme
  échappe au filtre `NF_` » — corriger le défaut a fait tomber le test qui le décrivait.
- **Le gate du dépôt m'a pris en défaut** (`--global` absent de la complétion) et mon `mkdirSync`
  rompait la promesse « un verdict par agent, jamais une exception ».
- **3 commandes laissées en arrière-plan** (comptages `rg` contournés) restaient marquées actives
  chez le user — à arrêter dès qu'on contourne.

## Ce qui n'a PAS été fait

- Suites `--load` et `--dialects` non lancées ; `RUN_PERF` et `RUN_WS_RUPTURE` restés fermés.
- Gemini non instruit côté **déclaration** de porte (seulement côté skills).
- Banc devkit sur `claude` seul : toujours pas rejoué (priorité 1 de la session précédente).

## Commits produits

| Commit     | Sujet                                                                                |
| ---------- | ------------------------------------------------------------------------------------ |
| `d517db28` | refactor(env)! : `NODEFONY_*` → `NF_*`, rupture nette avant la release               |
| `e72d1b14` | fix(bench) : un témoin renommé n'éprouve plus rien, un compteur en dur ne compte pas |
| `2ed31a65` | feat(agents)! : la porte MCP se déclare par PROJET chez Vibe et Codex                |
| `98359b14` | docs(agents) : nommer QUI lit `.agents/skills`                                       |
