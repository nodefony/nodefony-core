---
date: 2026-05-25
session_id: 21a5b69a-81d9-4158-8d08-ece3d7eb0156
focus: fix(cli) dispatch des commandes de module (frontend:build, network) — bootait un serveur
---

# Session retro — 2026-05-25 — 21a5b69a

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    56 |
| Read  |    17 |
| Edit  |     5 |
| Write |     3 |
| Skill |     2 |

## Top fichiers Read/Edit

| Fichier                                                         |   × |
| --------------------------------------------------------------- | --: |
| src/nodefony/src/kernel/CliKernel.ts                            | 2 E |
| src/nodefony/src/kernel/Kernel.ts                               | 4 R |
| (autres : Command.ts, Module.ts, decorators, frontend/index.ts) | 1 R |

## Coûts évidents

- Session **archi/diagnostic** : ~17 Read + 27 grep pour remonter la chaîne de boot
  (CliKernel→Kernel→Command→@modules decorator→Event.emitAsync). Coût justifié — il fallait
  prouver le mécanisme avant de toucher au cœur. Pas de re-lecture inutile (chaque fichier lu 1×,
  sauf Kernel.ts 4× sur sections différentes).
- 2 skills nodefony-session chargés (RESUME + END) = gros CLAUDE.md projet re-cache 2×.
- 0 restart serveur gaspillé : 4 smoke tests CLI ciblés (frontend:build, network, typo, development).

## 💶 Coût (€)

- Total ~**€25.8** (≈ $28) sur 89 tours — **cache-dominé ~80 %** : cache read $16.8 (60 %) +
  cache write 1h $5.6 (20 %) ; output seulement $5.1. Le poids = le CONTEXTE porté (CLAUDE.md
  projet volumineux + MEMORY.md au-dessus de la limite + 2× skill session), pas la production.
- Coût / livrable : 1 commit (fix core + 8 tests + 3 docs + 2 mémoires) → session rentable
  (bug pré-existant bloquant résolu proprement, branche mergée).

## Recommandations

1. **MEMORY.md au-dessus de la limite** (25.2 KB > 24.4) — déjà signalé par le harness. Prochaine
   session « process » : raccourcir les lignes d'index (déplacer le détail dans les fichiers topic).
2. **Pas de nouveau skill** — pas de pattern ≥3× nouveau. lsof/pkill/npx déjà en allowlist.
3. ✅ Décision archi capturée en mémoire ([[project-cli-module-command-dispatch]]) avant tout `/clear`.

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` stale (kit #I) — retiré après vérif « aucun git actif » → commit OK.
- ✅ Cluster/serveur résiduel d'une session précédente (EADDRINUSE 5151) — `pkill`+`lsof` au début.
- ✅ Commit FR via `-F` heredoc (apostrophes).
- ✅ Branche dédiée pour le fix puis FF merge dans claude-ts.

## Commits produits

| Commit  | Sujet                                                        |
| ------- | ------------------------------------------------------------ |
| 87cb4f9 | fix(cli): dispatch des commandes de module (frontend:build…) |
