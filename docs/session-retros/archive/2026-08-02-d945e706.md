---
date: 2026-08-02
session_id: d945e706-f747-4cf5-adb8-c1b164b5668f
focus: CI réparée, trois trous d'instrument fermés au banc, et le lot 5 du devkit livré
---

# Session retro — 2026-08-02 — d945e706

## Focus

Reprise sur T18 (faux-FAIL annoncé). La séance a bifurqué trois fois, chaque fois
sur demande : CI rouge, puis doute de fond sur le rendement du banc, puis lot 5.

## Fait

- **CI réparée** — 6 jobs rouges sur `oxlint` (`no-useless-fallback-in-spread`),
  régression de `fe548cd9` et `e0fea5bf`. Verte de bout en bout ensuite.
- **T18 fermée et PROUVÉE** — faux-FAIL confirmé par la mesure : 3/3 PASS.
- **T10 fermée SANS écrire une ligne de produit** — le diagnostic de la matrice
  était faux (voir Frictions).
- **Trois trous d'instrument** fermés, chacun vu ROUGE en le débranchant.
- **Lot 5 devkit livré** — 4 skills partent par npm, `nodefony ai:sync` les pose.
- **Deux skills découpés** sous la limite de 500 lignes, 0 mot perdu (vérifié).
- **T30 reformulée** sur une relation inverse, **T31/T32 durcies**.

## Frictions du jour (déjà versées dans RETEX.md)

- La **matière** d'une sonde est trop large par défaut — trois occurrences dans
  la même séance, chacune recalant un agent qui avait fait juste.
- Un **diagnostic écrit dans une mémoire vieillit comme un ancrage** : T10 était
  décrite par un profil qu'aucun run ne portait.
- Un banc qui fait concourir des **corpus qui ne cohabitent jamais** fabrique de
  faux avertissements.
- Comparer les **scripts npm** dépôt ↔ gabarit trouve les verbes livrés sans nom.

## Coûts évidents

- **Quatre erreurs de LECTURE de ma part**, toutes silencieuses : trois
  tentatives de parsing YAML à la main (`^` sans le flag `m`, `$` en mode
  multiligne) avant de prendre le parseur ; un contrôle de non-perte qui
  recollait des lignes par-dessus des blocs exclus et fabriquait des phrases
  inexistantes. À chaque fois, **la commande était fausse, pas le sujet mesuré**.
- Une dérive de `cwd` (un `cd` relatif dans `tmp/specs-agents`) a fait échouer
  trois commandes d'affilée sur « fichier introuvable ».

## Recommandations

1. **Ne pas écrire de parseur de format à la main** quand un automate existe —
   règle déjà écrite (QUESTION ZÉRO), pas appliquée ici.
2. **T31 non remesurée au moment du retex** — lancée en fond, à conclure.
3. Le doute du user sur le rendement du banc est **fondé et acté** : plus aucun
   défaut produit ouvert qu'il ait établi.

## Patterns récurrents (déjà gérés)

- ✅ Débrancher pour voir rouge — appliqué 5 fois, prouvé par `diff` à chaque fois.
- ✅ Copie de sauvegarde avant de laisser un sous-agent éditer un corpus payé cher.
- ✅ Prouver sur l'artefact reçu — `ai:sync` joué sur une app réellement générée.

## Commits produits

| Commit     | Sujet                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| `8b2bb3cc` | une sonde de LECTURE n'a jamais à juger — T18 était un faux FAIL         |
| `7e28d03e` | la CI était rouge sur 6 jobs — un repli vide dans un spread              |
| `3bb3beec` | une sonde qui cherche un NOM doit dire dans QUEL fichier                 |
| `a81e9168` | lot 5 — 4 skills livrés par npm, et `nodefony ai:sync` pour les poser    |
| `40541894` | devkit-bench et load-test sous la limite, sans perdre un mot             |
| `e7e8ac79` | un marqueur de code CITÉ dans un document n'est pas une mise en sourdine |
