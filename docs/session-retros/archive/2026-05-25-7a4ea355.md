---
date: 2026-05-25
session_id: 7a4ea355-b691-420f-8491-390c3ed55408
focus: Portail documentation Studio (démo) — faisabilité + DocLayout unifié (portail + module)
---

# Session retro — 2026-05-25 — 7a4ea355

## Tool usage

| Outil   | Calls |
| ------- | ----: |
| Edit    |   115 |
| Bash    |    67 |
| Read    |    36 |
| Write   |    12 |
| Skill   |     4 |
| AskUser |     1 |

## Top fichiers Read/Edit

| Fichier                               |   × |
| ------------------------------------- | --: |
| routes/ModuleDetail.tsx               |  43 |
| routes/Documentation.tsx              |  39 |
| controller/DocumentationController.ts |  10 |
| components/ui/DocToc.tsx              |   9 |
| components/ui/MarkdownDoc.tsx         |   6 |

## Coûts évidents

- **Itération UX au coup par coup** sur le layout doc : ~10 allers-retours (scrolls, sticky,
  hauteurs, repliage…) → ModuleDetail édité 43×, Documentation 39×. Le user a explicitement
  reproché le **manque de rigueur de mise en page** → modèle posé (layout.ts + DocLayout) APRÈS coup.
- Bug `readdir` non importé (typecheck FRONT ne couvre pas le controller BACK) → sauté au runtime,
  trouvé via debug ciblé (1 build+restart de plus).
- Plusieurs cycles build+restart studio télescopés avec le DevSupervisor (auto-restart sur edit .ts).

## 💶 Coût (€)

- Total ~**€161** (≈ $175) — **cache-dominé ~84 %** (cache_read 84.6M tok = $127 ; output 353k = $26 ;
  cache_write 1h 712k = $21). 253 tours. = session LONGUE mono-feature (1 grosse feature UI, pas de
  `/clear`) → le contexte porté sur 253 tours coûte tout. Levier : découper, ou figer le modèle AVANT
  d'itérer (aurait évité ~moitié des éditions UX).

## Recommandations

1. **Figer le modèle de layout AVANT de coder** (mini-cahier des charges) — la rigueur demandée par
   le user aurait économisé ~30-40 éditions. → mémoire [[feedback_studio_layout_rigor]] créée.
2. **Reporter `DocLayout` + règles layout dans le skill `nodefony-studio-dev`** (section mise en page :
   0 magic number, sidebars sticky + 1 scroll, jamais sticky dans ScrollArea Mantine, Card overflow:hidden).
3. **Gate typecheck BACK manquant** pour les controllers studio (le front tsconfig ne les couvre pas) —
   le build rollup n'a pas attrapé `readdir` non importé. À prévoir pour le vrai module doc.

## Patterns récurrents (déjà gérés)

- ✅ Briques partagées réutilisables (FlowGraph/MarkdownDoc/DocToc/DocLayout) plutôt que copie.
- ✅ Frontend = HMR, 0 restart (sauf controller back = 1 build+restart).
- ✅ Décision design tranchée + justifiée (faisabilité portail, choix lib graphe).

## Commits produits

| Commit  | Sujet                                                                                |
| ------- | ------------------------------------------------------------------------------------ |
| (aucun) | Démo JETABLE non commitée sur claude-ts — POC pour décider de l'archi du portail doc |
