---
date: 2026-07-30
session_id: 8b8303c0-32f2-4fe0-a061-6c87d25d1301
focus: banc de sécurité devkit — la sécurité se mesure par une attaque, et le trou produit qu'elle a révélé est fermé
---

# Session retro — 2026-07-30 — 8b8303c0

## Fait

- **Tâche 2 refondue** (`4bbe0e2e`) — `gate-secure-route.mjs` : trois identités sur `/api/reports`.
  Socle `http-probe.mjs` (requête, bocal à cookies, garde de port en UNE implémentation), les 4
  juges branchés dessus.
- **Tâche 20 neuve** (`9ab83f71`) — `gate-entity-delete.mjs` sur le DELETE d'une entité générée.
  `identites.mjs` extrait (3 identités + vocabulaire du refus + causes de décor).
- **Trou produit fermé** (`69ed0aa0`) — le gabarit d'entité pose `@IsGranted("ROLE_ADMIN")` sur
  `destroy` quand `@nodefony/security` est présent ; décor e2e généré fournit l'identité.

## Ce que la mesure a retourné

- L'anonyme reçoit **403, pas 401** sur `/api/reports` (zone `main` qui liste `anonymous`). Un juge
  exigeant 401 aurait recalé un agent irréprochable à chaque run.
- Une route gardée **hors de toute zone** répond 403 à tout le monde, admin compris — cause nommée.
- `create entity` livrait un DELETE répondant **204 à un anonyme**. Prouvé sur application réelle,
  puis fermé, puis re-prouvé sur une entité 100 % générée.

## Coûts évidents

- 3 sous-agents `haiku` lecture seule (contrat des juges · recette d'auth · zones firewall) puis un
  4ᵉ (recette du CRUD généré) : ~478 k tokens lus, 4 fiches rendues. Le contexte principal est
  resté sur la décision.
- Un `nodefony-dev-supervisor` vieux de 3 h tournait au démarrage — tué avant toute mesure (piège
  documenté en 07-30e : il rebâtit le core à chaque Edit).
- 6 boots d'application témoin pour les preuves terrain (2 pour T2, 4 pour T20 + correctif).

## Recommandations

1. Famille « ne pas affaiblir » (T22-T24) — le mode d'échec le plus grave, toujours sans juge.
2. Jouer T2 et T20 avec de vrais agents (~3 runs chacune pour une médiane) : ce qui est prouvé,
   c'est que les instruments mordent, pas encore ce qu'un agent en fait.
3. Deux trous du même inventaire restent ouverts : `create module` sans doc sécurité, et rien ne
   dit qu'un canal temps réel sans policy est public.

## Patterns récurrents (déjà gérés)

- ✅ Le décor est le premier suspect (supervisor tué avant mesure).
- ✅ Un gate neuf vu ROUGE avant d'être cru — deux débranchements par juge, sur application réelle.
- ✅ Preuve sur l'artefact reçu : selftests re-joués APRÈS prettier, entité 100 % générée.

## Commits produits

| Commit     | Sujet                                                            |
| ---------- | ---------------------------------------------------------------- |
| `4bbe0e2e` | la sécurité se mesure par une attaque, plus par une chaîne       |
| `9ab83f71` | le CRUD généré livre une suppression OUVERTE — le banc le prouve |
| `69ed0aa0` | la suppression générée naît GARDÉE — le trou est fermé           |
