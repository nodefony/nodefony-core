---
date: 2026-09-11
session_id: a2d9b3ed-01d9-4add-8dc1-73d3155ff513
focus: sept tickets fermés avant l'alpha.5 — guide d'agent par moteur front, geste d'identité au refus, security:user:password, ancres de doc
---

# Session retro — 2026-09-11 — a2d9b3ed

## Focus

Le user a choisi de **fermer des tickets avant de publier l'alpha.5**, en prenant ceux dont le
contexte était déjà chargé (règle du contexte chaud). Sept fermés, tous avec compte rendu.

## Fait

| Commit     | Sujet                                                                       |
| ---------- | --------------------------------------------------------------------------- |
| `f8424136` | #347 — la liaison cliente du moteur front nommée dans le guide généré       |
| `d9e96c2a` | #344 — le 403 et `realtime:denied` disent comment obtenir une identité      |
| `f5a5d59b` | #346 + #343 — causes d'un échec hors app, cas worktree, contenu « Minimal » |
| `04b99066` | #21 + #290 + #23 — `security:user:password`, 54 ancres, `SECURITY.md`       |
| `182ca635` | #290 — les 23 dernières ancres recalées une par une                         |
| `a268ff18` | contrôles de doc atteignables (`doc:anchors`), menu sectionné               |

Ticket ouvert : **#359** (433 ancres du corpus entier, jalon beta).

## Coûts évidents

- 🔴 **`test:all` payé DEUX fois.** La première passe a rendu 41 rouges — 38 en intégration —
  parce que j'éditais le dépôt pendant qu'elle tournait : le superviseur a rebuild et redémarré le
  serveur en plein run. Rejouée sans rien toucher : 825 tests d'intégration verts, 0 échec, 1 seul
  rouge unitaire (un timeout, vert au rejeu isolé). ~6 min de machine + le doute.
- Trois débranchements de gate ratés au premier essai (mauvais motif `perl`, `assert.notMatch` qui
  n'existe pas en `node:assert`, `assert.include` non plus) — la preuve négative coûte, mais c'est
  elle qui a montré qu'un gate passait pour la mauvaise raison.

## Recommandations

1. **Mémoire enrichie** (faite) : `feedback_stale_decor_poisons_verdicts` — « pendant une passe, on
   n'écrit pas dans `src/` » ; les `.md` de `docs/` sont sûrs.
2. **Sas** (fait) : thème « un contrôle qui ratisse trop large crie faux » — 3 frictions.
3. **CONSOLIDATE** : deux thèmes du sas sont MÛRS (5 frictions chacun) — « le terrain qu'on donne à
   un délégué » et « composer une assertion de chemin ».

## Patterns récurrents (déjà gérés)

- ✅ Gate neuf vu ROUGE avant d'être cru — appliqué 7 fois, et il a attrapé un gate qui passait en
  satisfaisant la lettre.
- ✅ Prouver sur l'artefact REÇU : le `package.json` rendu, pas le gabarit.
- ✅ Constater plutôt que déduire : le menu d'une app générée a été RENDU, pas supposé.

## Commits produits

6 commits, poussés sur `dev`. CI verte sur `a5808f7c..a268ff18` au moment de la clôture.
