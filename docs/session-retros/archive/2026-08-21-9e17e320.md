---
date: 2026-08-21
session_id: 9e17e320-9e56-408f-8667-d27d91a939f0
focus: Le banc devkit instruit au transcript, puis la CLI reprise commande par commande sous le regard du user
---

# Session retro — 2026-08-21 — 9e17e320

## Focus

Deux moitiés. **(1)** Instruire au transcript les sept échecs du banc devkit, corriger leurs
causes, rejouer le run large. **(2)** Une boucle serrée avec le user, qui a essayé les commandes
une par une et signalé chaque défaut — douze commits en sont sortis.

## Fait

- **Diagnostic du banc** : trois des sept FAIL ne disaient RIEN de l'agent. `nodefony check`
  accusait les ports 5151/5152 — ceux du DÉPÔT — parce que `defaultDevPorts()` ne lisait pas le
  `NF_PORT` que le décor pose. Deux sources de vérité pour « quels ports cette app utilise ».
- **Quatre correctifs de guidage** (T9/T10/T16/T26), dont trois portaient sur nos propres textes :
  `AGENTS.md` disait « npm test puis typecheck » en tête et en pied alors que `npm run verify`
  existe ; rien ne contredisait « Fetch Metadata protège déjà » ; et il CONSEILLAIT de déplacer une
  route sous `/api/machine` — l'agent a obéi, le partenaire a reçu 404.
- **Run large rejoué** : **25/29 PASS** contre 21/29. Cinq remontées, une chute (T13).
- **CLI reprise sous le regard du user** : jeton MCP par commande (`security:token`), listage et
  suppression de comptes, choix des rôles, boot silencieux, sortie hors journal, enchaînement de
  commandes, options invisibles, code de sortie d'un échec.

## Frictions

- **Une sonde trop large a menti trois fois** (options `git`, commentaire pris pour du code, deux
  occurrences dont une seule comptait). Une quatrième fois en négatif : un `grep -c` à `0` que j'ai
  lu comme un succès alors que la commande n'avait pas tourné.
- **Cinq tentatives de pilotage du menu par `expect`**, aucune concluante — dont deux qui ont lancé
  un serveur de dev par erreur. Rendre le câblage testable a coûté dix minutes et prouvé davantage.
- **Un test gravait un bug** (« pas de deduplication ») sans le justifier.
- **Mon premier design a été rejeté à raison** : je faisais poser les questions au MENU ; le user a
  corrigé — c'est la COMMANDE qui connaît ses questions. J'ai défait ce travail.

## Commits produits

| Commit     | Sujet                                                                        |
| ---------- | ---------------------------------------------------------------------------- |
| `2daf4586` | le test de la porte MCP gardait une méthode retirée la veille                |
| `0d470366` | une application est jugée sur SES ports, plus sur ceux du voisin             |
| `8cbbaad5` | le MCP authentifié devient atteignable — et `stop` cesse de proposer `--all` |
| `bae39481` | les quatre échecs restants venaient de ce qu'on DIT à l'agent                |
| `3c84492e` | une option qui marche mais que personne ne voit n'existe pas                 |
| `b7151e5d` | une commande choisie au menu DEMANDE ce qu'il lui faut                       |
| `ea8c6543` | le journal RACONTE l'exécution, la sortie EST le résultat                    |
| `6b4aee7c` | un jeton s'obtient par une COMMANDE, et un échec sort en 1                   |
| `480dcbe3` | `security:secrets` dit enfin QUELLES clés et POURQUOI                        |
| `8394fb58` | choisir un rôle, supprimer un compte, et un message qu'on comprend           |
| `d119ae64` | `ai:mcp` ne désarme plus la porte, et enchaîne vers le jeton                 |
| `0caebbed` | le boot se tait vraiment depuis le menu — le syslog était EMPILÉ             |

## Mesures

- Banc : 84 min pour 18 tâches, dont **78 min d'agent** — le harnais ne coûte que 6 min. La durée
  n'est pas un défaut du banc, c'est sa mesure (T16 : 116 tours contre 40 pour T15).
- Core 2854 verts, security 1012 verts, suite `load` de `@nodefony/http` 27/2 skips.
