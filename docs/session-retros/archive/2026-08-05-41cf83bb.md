---
date: 2026-08-05
session_id: 41cf83bb-46b7-4323-b35d-e5fab1d56fb0
focus: chantier perf pipeline — session MODIFS 1, lots A (en-têtes) + B (nonce/timer) livrés
---

# Session retro — 2026-08-05 — 41cf83bb (session g)

## Fait

- **Lot A** `8d222d84` (+5,6 % : 9 632 → 10 168 RPS médian) — Content-Type posé 1× (filet
  `ensureContentTypeHeader` http1+http2), setLength 0 alloc/req, statusMessage standard en
  fast path node, hasHeader natif. **A3 rejeté AVANT codage** par micro-bench contre-épreuvé.
- **Lot B** `c4c373f2` (+11,1 % : 9 882 → 10 976 RPS) — nonce CSP par pool CSPRNG amorti
  (`randomFillSync` 4 Ko), timeout socket aligné 120 s → 30 s (3→2 `setTimeout`/req).
- **Cumul 9 813 → ~10 976 RPS (+12 %)** — chaque lot : 2 paires A/B alternées, séparation
  nette (lot B : 0 chevauchement même entre runs individuels).
- 3 tests vus ROUGES au débranchement (filet CT, refill pool, nonce figé) ; gates verts à
  chaque lot : unit 749, intégration 597, mémoire 9/9, tsgo 0.
- MIGRATION_STATUS ligne perf + kit `project_perf_pipeline_p2_kit` soldés en place.

## Frictions / leçons (→ RETEX.md)

- Micro-bench sans décor réel = verdict inversé (fast path writeHead conditionnel kOutHeaders).
- Débranchement AVANT test : 70 verts filet coupé = comportement couvert par rien.
- Garde anti-stash → protocole flip par `git show` + cp + marqueur dist prouvé.

## Commits produits

| Commit     | Sujet                                                             |
| ---------- | ----------------------------------------------------------------- |
| `8d222d84` | perf(http): lot A — la pose des en-tetes cesse le ping-pong       |
| `c4c373f2` | perf(http): lot B — nonce CSP en pool amorti, timer socket aligne |
