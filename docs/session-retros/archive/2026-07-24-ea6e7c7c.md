---
date: 2026-07-24
session_id: ea6e7c7c-6408-47fe-8624-84177a024511
focus: F59 (dernier item du registre) puis les restes — test:all, contre-pression client, mémoires orphelines
---

# Session retro — 2026-07-24 — ea6e7c7c (4ᵉ du jour)

## Fait

- **F59** (`16db7f53`) — le registre doc↔code passe à **120/120**. `route()` refuse à la déclaration
  tout nom déjà porté par `Controller` ; liste **dérivée du prototype**, donc couvre aussi les
  accesseurs (`session`, `request`…) qui masquaient l'action en silence.
- **`test:all`** (`7ee7335f`) — annonce les **41 bancs de mesure** qu'aucun flag ne joue.
- **Drop silencieux du client realtime** (`4dfdbc49`) — une frame émise socket fermée disparaissait
  sans un mot ; un `request()` attendait 30 s. Rejet immédiat + notice bornée + `framesUnsent`.
- **Mémoire IA** (`55cb08a`) — 13 orphelines rendues atteignables, 1 doublon d'observabilité fusionné.

## Coûts évidents

- Un `cd` relatif refusé a emporté le `cat >>` de la même ligne : tests jamais écrits, run suivant
  lu comme vert. ~3 outils perdus, et une conclusion fausse évitée de justesse.
- Élargir `send` en `boolean | void` a cassé 6 stubs concis — 3 allers-retours de typecheck racine.

## Recommandations

1. Après un refus d'outil sur une commande composée : **repartir du principe que rien n'a tourné**.
2. Élargir un type de retour de callback → **grepper les stubs concis** (`=> x.push(…)`) avant le push.
3. Un signal visible ajouté (notice, snackbar) → **lister ses appelants réels** avant de le livrer.

## Patterns récurrents (déjà gérés)

- ✅ Preuve négative systématique : chaque mécanisme a été débranché et vu tomber (3 tests F59,
  5005 ms vs 139 ms sur le rejet RPC, notice de plomberie réapparue).
- ✅ Un test qui s'appuyait sur le bug est tombé au fix — traité comme signal, décor corrigé.

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `16db7f53` | F59 — un nom d'action déjà porté par `Controller` est refusé |
| `7ee7335f` | `test:all` annonce les 41 bancs qu'il ne joue jamais         |
| `4dfdbc49` | le client realtime ne jette plus une frame en silence        |
