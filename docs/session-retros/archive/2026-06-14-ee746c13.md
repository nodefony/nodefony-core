---
date: 2026-06-14
session_id: ee746c13-6795-4ea7-8256-9130300b801f
focus: Passe RPS « avec la sécu » — coût firewall vs reprise de session (SQLite sync vs redis async)
---

# Session retro — 2026-06-14 — ee746c13

## Focus

Bench RPS d'une requête authentifiée (zone firewall P6) pour chiffrer le coût de la sécu.
Aucun code modifié (mesure pure) ; config app éditée temporairement pour le bench redis puis revertée.

## Fait

- **Firewall P6 = ~gratuit** : rejet anonyme (401) = −6 % RPS ; sur trafic authentifié ~0 % au-dessus
  de la session. Le coût d'une requête authentifiée = la **REPRISE de session**, pas le firewall.
- **3 hypothèses tuées PAR LA PREUVE** (pas l'intuition) : re-save par requête (non — dirty-tracking
  `session.ts:275`), `readOnly` = le levier (non — write déjà évité), table énorme/scan non-indexé
  (non — PK `session_id`, `SEARCH USING INDEX`).
- **Vraie cause** : store session dev = `drizzle` driver **better-sqlite3 SYNCHRONE** → SELECT qui
  bloque l'event-loop. Test discriminant : débit de `/auth/me` **PLAT ~400/s** de c=5 à c=50.
- **Preuve redis** (docker jetable) : async → débit qui MONTE avec la concurrence, ~1900/s (**×4,7**).
  Chemin session+sécu : **−83 % (SQLite) → −25 % (redis)** vs baseline. Le ×6 = artefact du store DEV.
- **Validation fin de session** : `build:force` 19/19 ✅ ; unit ~3030 ✅ (1 rouge pré-existant
  orm-core) ; intégration 22/22 ✅ ; gate mémoire 9/9 ✅ ; charge 27/29 ✅ (2 rupture gated).
- Mémoire `project_session_store_perf_finding` créée + TODO « cache session RAM pour tests de charge ».

## Frictions / coûts

- **2 hypothèses fausses AVANT de lire le handler réel** : supposé FileSessionStorage (défaut doc),
  c'était `drizzle` (config app). `grep "SESSION STORAGE active"` AURAIT dû être le 1er réflexe.
- **Rouge pré-existant masqué** : orm-core `ormWiring (C5)` caché par le cache turbo + cascade `^test`
  (les dépendants skippés). Révélé par `turbo run test --continue`.
- Restarts serveur : 2 (revert config). Docker redis lancé/supprimé proprement.

## Commits produits

| Commit            | Sujet                                          |
| ----------------- | ---------------------------------------------- |
| (aucun côté code) | mesure pure, config revertée — `git diff` vide |
