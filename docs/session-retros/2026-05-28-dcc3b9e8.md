---
date: 2026-05-28
session_id: dcc3b9e8-97b0-485f-8281-8e77cbfc666b
focus: Template doc impeccable + vitrine Socket Nodefony + cascade fixes sticky/scroll Studio
---

# Session retro — 2026-05-28 — dcc3b9e8

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |   101 |
| Edit       |    74 |
| Read       |    44 |
| TaskUpdate |    28 |
| Write      |    20 |
| TaskCreate |    12 |
| Skill      |     4 |
| ToolSearch |     1 |

## Top fichiers Read/Edit

| Fichier                                          |   × |
| ------------------------------------------------ | --: |
| `routes/Documentation.tsx`                       |  20 |
| `components/ui/DocLayout.tsx`                    |  16 |
| `.claude/skills/nodefony-documentation/SKILL.md` |  15 |
| `components/ui/DocToc.tsx`                       |   7 |
| `components/ui/layout.ts`                        |   5 |
| `nodefony/controller/DocumentationController.ts` |   6 |
| `routes/SocketPocPage.tsx`                       |   4 |
| `components/ui/PageHeader.tsx`                   |   3 |

## Coûts évidents

- **DocLayout.tsx édité 12×, PageHeader 3×, layout.ts 5×** — cascade de 6 commits successifs
  sur le modèle sticky/scroll (`cbbed90` → `7411172` → `b12553f` → `3ea7f05` → `756b1b6` → `ff85d25`).
  Cause racine : pas de diagnostic clair du scroll-ancestor (body vs AppShell.Main) en amont
  → série de patchs partiels avant d'identifier que le scroll devait passer SUR Main.
- **Sur-correction du toggle TOC (`756b1b6` → `ff85d25`)** — j'ai supprimé un bouton que
  l'utilisateur voulait juste déplacer. Coût : 1 commit annulé + 1 commit de restauration
  - frustration user. Leçon : « mal placé » ≠ « inutile ».
- **Documentation.tsx 13 Edit** — incluant 2 refactors (NavLink hiérarchique puis re-injection
  toggle TOC) qui auraient pu être faits en 1 passe avec un cahier des charges UX initial.
- **`npm run build` Studio caché par turbo** — 2× détecté qu'un rebuild n'avait pas pris une
  modif (dist obsolète malgré `rm -rf dist`). Fix : `npx rollup --config ... --configPlugin
typescript` direct contourne le wrapper turbo.

## 💶 Coût (€)

- **Total ~€165** (≈ $179) — 208 turns, 1313 lignes de transcript (4 MB).
- Décompo : **input 334 tokens · output 327 437 · cache write 1h 1 283 852 · cache read 77 342 320**.
- **Cache-dominé à ~99 %** : 77M tokens en `cache_read` (×1.5/M) = ~$116 + cache write 1h 1.3M
  (×30/M) = ~$39, soit ~$155 de cache pur. L'output (327k × 75/M = ~$25) est marginal.
- Enseignement : **le contexte porté est le poste de coût ÉCRASANT**. Le travail produit
  (9 commits, ~3000 lignes de code/MD écrites) coûte ~$25 ; le contexte rechargé à chaque
  turn coûte 6× plus. Levier #1 d'économie : `/clear` entre features + skills load-on-demand
  - MEMORY.md court. Confirmé une N-ième fois.
- Coût/commit : ~€18 — élevé pour des fix CSS, expliqué par la cascade sticky.

## Recommandations

1. **Skill `nodefony-studio-dev` — section « Sticky / scroll-ancestor »** : ajouter un paragraphe
   sur l'identification du scroll-ancestor AVANT de toucher un `position: sticky` (body vs
   AppShell.Main vs container interne). Le `top:` doit être relatif au bon ancêtre. Évite la
   cascade de 6 commits vécue ici.
2. **Mémoire `feedback_ui_toggle_displace_not_remove`** : quand un utilisateur dit « mal placé »
   à propos d'un toggle UI, c'est **un déplacement, pas une suppression**. Ne JAMAIS retirer
   un contrôle sans confirmation explicite. Lecture par défaut : « déplace-le », pas « vire-le ».
3. **Mémoire `feedback_turbo_silent_stale_dist`** (peut-être déjà existant — vérifier) :
   si `npm run build` Studio rend un dist sans nos modifs, `npx rollup --config ... --configPlugin
typescript` direct contourne le cache turbo. Diag : `grep <chaîne> dist/*.js` → vide alors que
   source contient = signal turbo silent stale. Vu 2 fois cette session.

## Patterns récurrents (déjà gérés)

- ✅ Vite glob `import.meta.glob` pattern littéral statique — pièges (mauvais `..`) documentés en 1.2.0.
- ✅ Règle layout sticky 6 — « 3 sticky parallèles à `CONTENT_STICKY_TOP` » figée dans skill.
- ✅ `safe-commit.sh` gère le `.git/index.lock` orphelin (déjà documenté, vu 2× ici).
- ✅ DevSupervisor watch les sources TS → `touch <source>.ts` déclenche restart auto.
- ✅ Lint-staged + prettier reformate au commit (cosmétique, ne pas revert).

## Commits produits

| Commit    | Sujet                                                              |
| --------- | ------------------------------------------------------------------ |
| `5cfcd63` | feat(studio): template doc impeccable + 0 magic number (session 1) |
| `d3c1696` | feat(doc): vitrine Socket Nodefony + registry Vite glob            |
| `cbbed90` | fix(studio/doc): titre sous-page DocLayout sticky                  |
| `7411172` | fix(studio/ui): PageHeader hauteur réelle var CSS (pixel-perfect)  |
| `b12553f` | fix(studio/ui): box-shadow opaque PageHeader (partiel, dépassé)    |
| `3ea7f05` | fix(studio): scroll interne AppShell.Main                          |
| `e0cec79` | fix(studio/doc): déplie auto section page active                   |
| `756b1b6` | fix(studio/doc): nav NavLink + retrait toggle TOC (sur-correction) |
| `ff85d25` | fix(studio/doc): restaure toggle TOC à un emplacement légitime     |
