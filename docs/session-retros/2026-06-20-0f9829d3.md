---
date: 2026-06-20
session_id: 0f9829d3-33cf-4252-a996-aefe0572e897
focus: P6.14 Audit de sécurité — socle + émission + data plane (lots 1-3/4)
---

# Session retro — 2026-06-20 — 0f9829d3

## Focus

P6.14 (journal d'audit de sécurité) : conception en 4 lots puis livraison des lots 1-3.

## Fait

- **Lot 1** `6f028fbe` — socle : `IAuditEvent` (métier, secrets presence-only) + `IAuditStore`
  (append-only) + `IAuditSink` + `MemoryAuditStore` (FIFO borné/query curseur/gc/snapshot) +
  `AuditService` (`@services`, no-op coût nul si off, fan-out live lazy, pose `auditStore`). 15 tests.
- **Lot 2** `9706b474` — émission cold-path : `AuthFlow` (login.success/failure/throttled/logout +
  fédéré) ; `Authorization` access.denied via `#auditDeny`. Helpers `recordAudit`/`readAuditContext`. 7 tests.
- **Lot 3** `a5999c09` — data plane : `SecurityAdminApi` (`IAdminApi` ns "security",
  `GET /nodefony/security/api/audit/events`, RBAC ROLE_NODEFONY_ADMIN, filtré/paginé curseur, 503 si off). 9 tests.
- `MIGRATION_STATUS` P6.14 ⬜→🔶, P6 67→68 %.

## Décisions structurantes (survivent au /clear)

- **Émission EXPLICITE** par point sensible, PAS un EventEmitter magique sur le firewall (raterait
  Token/OAuth/WebAuthn qui émettent hors chaîne).
- **Scope = transitions d'état** (login/refus/jeton), PAS le trafic par requête (déjà `JsonAuditLogger`
  P3.1). Conséquence perf : **0 event sur le hot-path succès**, émission cold-path only.
- **Audit ON par défaut** (secure-by-default, OWASP A09) ; coût nul si désactivé (prouvé).
- **Data plane = `IAdminApi`** (RBAC role par endpoint, broker applique 403), PAS routes classiques
  façon `ApiKeyController` (user-scopé).

## Frictions (→ RETEX.md)

- Preuve e2e ambiguë : 401 sous zone data plane ≠ « route montée » → preuve déplacée en unit (register idempotent).
- Le bon calque n'est pas le 1er candidat : `SyslogAdminApi` (IAdminApi) et pas `ApiKeyController` (routes classiques).
- Rapport sous-agent = piste (2×) : un agent a classé `ApiKeyController` comme `IAdminApi` (faux).

## Gates

typecheck strict 0 err · security **474/474** (+31) · mémoire 9/9 · e2e smoke 401 (route gardée).

## Reste

**Lot 2b** (émission firewall/token/WS, hot-path → memory.test dédié) + **Lot 4** (stream WS live
`security:audit`). Puis **Studio P6.15** consomme le data plane.

## Commits

| Commit     | Sujet                              |
| ---------- | ---------------------------------- |
| `6f028fbe` | socle audit (sink + store mémoire) |
| `9706b474` | émission login/logout/refus        |
| `a5999c09` | data plane HTTP (IAdminApi)        |
