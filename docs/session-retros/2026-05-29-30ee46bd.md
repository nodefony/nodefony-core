---
date: 2026-05-29
session_id: 30ee46bd-27e2-4318-9df1-55d1a8c902c0
focus: Refresh deps frontend (vite 8 unique + plugin-react 6) + investigation dette de boot → plan approuvé
---

# Session retro — 2026-05-29 — 30ee46bd

## Tool usage

| Outil         | Calls |
| ------------- | ----: |
| Bash          |    60 |
| Read          |    23 |
| Edit          |    21 |
| TaskCreate    |     3 |
| Skill         |     3 |
| Write         |     2 |
| ToolSearch    |     2 |
| ExitPlanMode  |     2 |
| EnterPlanMode |     1 |

## Top fichiers Read/Edit

| Fichier                                      |   × |
| -------------------------------------------- | --: |
| studio/package.json (Edit)                   |   5 |
| test-frontend-react/package.json (Edit)      |   3 |
| package.json racine (Edit)                   |   2 |
| Kernel.ts (Read)                             |   2 |
| kit boot-resilience + MEMORY.md (Write/Edit) |  2× |

## Coûts évidents

- **Cache-dominé (82 %)** : grosse phase d'investigation (lecture chaîne de boot Kernel.ts,
  Event.emitAsync, redis service/Connection/buildClientOptions, realtime index/RedisBackplane) →
  beaucoup de contexte porté tour après tour. Output seulement 16 % du coût.
- studio/package.json édité **5×** : bumps caret ajoutés au fil des demandes successives du user
  (vite/plugins, puis dayjs/mobx/router, puis @types). Non batchable (demandes incrémentales).
- **`.git/index.lock` orphelin rencontré 2×** (husky/lint-staged le laisse traîner) → friction connue.

## 💶 Coût (€)

- Total **~55,4 €** (≈ 60,2 $) — cache read 18,75 M tok (~28 $) + cache write 1h 719 k (~22 $) +
  output 131 k (~10 $) + input 45 k (~0,7 $). **Cache = 82 %.** Enseignement : sur une session
  d'investigation/archi, c'est le CONTEXTE relu qui coûte → si on implémente le plan, **`/clear`
  d'abord** (le kit recharge le contexte ciblé, pas toute cette session d'exploration).

## Recommandations

1. **Implémenter le plan en session fraîche après `/clear`** — éviter de porter les ~660 lignes
   d'investigation ; le kit [[project_boot_resilience_plan_kit]] suffit à reprendre.
2. **`safe-commit.sh` systématique** pour les commits — husky a laissé `.git/index.lock` 2× (déjà
   capté par [[feedback_git_index_lock]], mais le wrapper l'aurait évité d'emblée).
3. Rien de neuf à mémoriser côté deps : tout est dans le commit + l'état de session.

## Patterns récurrents (déjà gérés)

- ✅ `.git/index.lock` orphelin → `feedback_git_index_lock` + `safe-commit.sh`.
- ✅ Décision archi (résilience boot) capturée en mémoire kit avant `/clear` → `feedback_session_resume_workflow`.
- ✅ Lock orphelin (scheduled_tasks.lock / index.lock) = même schéma que tout lock : vérifier PID mort puis retirer.

## Commits produits

| Commit  | Sujet                                                                 |
| ------- | --------------------------------------------------------------------- |
| e29b3de | chore(deps): refresh frontend — vite 8 unique + plugin-react 6        |
| f953afb | chore(git): ignore les locks runtime de Claude Code (.claude/\*.lock) |

## Livrable de réflexion (pas un commit)

- **Plan APPROUVÉ « résilience de boot dev+prod + .env »** → kit [[project_boot_resilience_plan_kit]].
  Découverte clé : `emitAsync` sans timeout/isolation par listener + backplane redis `await subscribe()`
  sur offline queue → **fait pendre `onKernelBoot` → boot mort**. Fix : garde lifecycle-only
  (`Event.emitAsyncGuarded` générique + `Kernel.fireLifecycle` politique), `Event.ts`/hot path intacts.
