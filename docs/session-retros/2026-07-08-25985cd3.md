---
date: 2026-07-08
session_id: 25985cd3-84be-4635-8833-b2fb1ff5c9ba
focus: S4 multi-dialecte mysql/mariadb + banc de parité de contrat + boot fatal sur config
---

# Session retro — 2026-07-08 — 25985cd3

## Focus

Chantier ORM multi-dialecte S4 (mysql/mariadb), déclenché après « reprends ».
Puis diagnostic terrain d'un login cassé en PG → fix Dolibarr + durcissement
boot (BootConfigurationError). Session pilotée par les questions live de l'user.

## Fait — 3 commits (poussés sur claude-ts)

1. **`0ac019ef`** S4 : dialecte `mysql` = MySQL **ET MariaDB** (fork libre =
   quotidien projet). `#connectMysql` (mysql2 lazy, pool `timezone:"Z"`) ·
   colKit mysql (varchar(512) indexable, datetime(3), bigint, **mysqlJsonCompat**
   customType parse-si-string — MariaDB rend le JSON en LONGTEXT string) ·
   queryKit JSON_CONTAINS · **chemins repo sans RETURNING** (verbes *One =
   SELECT→mutation bornée PK + critère re-vérifié→relecture ; create=$returningId ;
   upsert=onDuplicateKeyUpdate) · OFFSET=MAX_SAFE_INTEGER · **idempotence mysql =
   INSERT IGNORE + vol UPDATE…WHERE expiré** (affectedRows d'un ODKU AMBIGU sous
   mysql2 CLIENT_FOUND_ROWS, prouvé serveur) · **banc de PARITÉ DE CONTRAT
   IRepository 14 tests × 3 dialectes** (a attrapé 2 bugs préexistants : drizzle
   0.45 IGNORE `limit(-1)` numérique → fragment `sql`-1`` ; gc audit non normalisé
   mysql). Preuves : **drizzle 274/274 avec PG + MariaDB réels**, e2e passés sur
   MySQL 8.4 ET MariaDB 11.4 (docker mariadb 3306 / mysql 3307).
2. **`af35e846`** fix : fixture Dolibarr (410 tables sqliteTable figées) déplacée
   du connecteur `default` vers un connecteur sqlite dédié `:memory:` — elle
   bloquait tout le boot dès que `NF_DATABASE_URL`=postgres (garde multi-dialecte
   refuse → ORM default down → login mort). peerDep/external `@nodefony/drizzle`.
3. **`ebf940a1`** feat(kernel) : `BootConfigurationError` = erreur de config
   FATALE au boot (dev ET prod si module critique) — un serveur « vivant » aux
   briques durables mortes est pire qu'un crash. DrizzleService wrappe + ping réel
   SELECT 1 (pool pg/mysql LAZY). http stores-location accepte le profil réseau
   (location undefined par design).

## Frictions du jour (→ RETEX.md SAS)

- **Fail-soft de boot = piège sur une config explicite** (infra déclarée
  injoignable / entité mono-dialecte) → invisible, login mort. → BootConfigurationError.
- **Entité mono-dialecte figée sur `default` = bombe au switch d'infra** → connecteur dédié.
- **`.env.local` doublon → parseEnv garde la DERNIÈRE ligne** (silencieux) → « marche pas » de l'user.
- **ORM réseau ne bat pas sqlite en local si pool < concurrence** (PG c=50=294 vs c=25=696 ; pool pg défaut=10).

## Preuves / gates

- drizzle **274/274** (PG + MariaDB réels) → 275 après le test injoignable
- core **1809** · http **568** · memory **9/9** · load **27/29** (2 opt-in) · typecheck 12/12 --force
- Boot réel : `NF_DATABASE_URL` port mort → **FATAL net** (BootConfigurationError + ECONNREFUSED + terminate 1, URL rédigée)
- Login navigateur 200 en PG (session persistée), stores auto → drizzle (postgres)

## Coûts évidents

- 2 restarts serveur (switch env PG + preuve boot fatal) — justifiés (points de mesure).
- Rebuild module test long (410 tables Dolibarr) × 2 (edit index + rollup.config) avant vert.

## Commits produits

| Commit     | Sujet                                                 |
| ---------- | ----------------------------------------------------- |
| `0ac019ef` | S4 multi-dialecte mysql/mariadb + banc parité contrat |
| `af35e846` | fix Dolibarr connecteur dédié (bloquait boot pg)      |
| `ebf940a1` | BootConfigurationError (config fatale au boot)        |
