---
date: 2026-08-08
session_id: 6b438007-f29f-4d76-8d03-3fc233e1a8b9
focus: Montée des dépendances, puis remise en état de la CI — trois rouges, trois sondes fausses
---

# Session retro — 2026-08-08 — 6b438007

Session menée **en parallèle d'une autre**, sur le même dépôt. Contrainte tenue de bout en bout :
aucun `git add -A`, aucun push tant que l'autre session avait du travail non poussé, et à la
clôture, ni son fichier d'état ni sa ligne de tête dans `MEMORY.md` ne sont touchés.

## Fait

- **34 pins montés** sur 21 espaces de travail, `npm audit` ramené à 0 (`dompurify` 3.4.12 → 3.4.13,
  transitif sous `mermaid`, fermé par le verrou et non par un pin).
- **PR #9** (actions GitHub v5→v7, `upload-artifact` v4→v7) rebasée puis fusionnée ; **PR #8**
  (TypeScript 7) fermée, les deux bloqueurs re-vérifiés au dépôt avant de l'écrire.
- **Deux sondes de test réparées** : `bearer.test.ts` (anti-ReDoS) et `LogSink.test.ts` (sink fichier).

## Coûts évidents

- **Le diagnostic CI a coûté plus que la montée elle-même.** Trois captures de journaux d'exécution
  complètes, dont une inutile (`--log-failed` sur un run dont les jobs n'étaient pas terminés).
- **Un premier guet mal conçu** : il a rendu les checks EXISTANTS dès sa première passe au lieu
  d'attendre le rebase, et j'ai conclu « toujours rouge » sur un état vieux de trois jours. Un guet
  doit surveiller le SIGNAL (ici : le changement de commit), pas l'état courant.
- **Un sous-agent lancé trop large** — campagne de mesure en cinq points là où le user voulait le
  test réglé. Resserré en vol, mais le prompt initial était déjà payé.

## Recommandations

1. **Verser `audit-pins.mjs` dans `scripts/`** — l'inventaire exhaustif des pins contre le registre
   est la seule réponse fiable à la cécité de `npm outdated`, deux fois constatée.
2. **Lire `RETEX.md` au démarrage, pas seulement au retour d'un `/clear`** — la leçon sur
   `npm outdated` y était écrite, `turbo` nommément, et elle s'est reproduite à l'identique.
3. **Relire les sondes VOISINES d'une sonde fautive** — le second cas de `LogSink` portait la même
   hypothèse temporelle en pire, sans avoir jamais rougi.

## Patterns récurrents (déjà gérés)

- ✅ Prouver sur l'artefact REÇU : versions relues dans `node_modules`, build complet refait après
  `turbo` (le moteur de build), front Angular rebâti car `compiler-cli` n'est exercé par aucune
  tâche turbo.
- ✅ Voir le gate ROUGE avant de le déclarer réparé : le doublon `LogSink` a été reproduit en
  retardant le rappel d'écriture à 100 ms.
- ✅ Suspecter son instrument : `oxlint` rendait une sortie VIDE en local (muet hors terminal) —
  vérifié par `--format=json` qu'il analysait bien 1698 fichiers avant d'appeler ça un vert.

## Commits produits

| Commit     | Sujet                                                                |
| ---------- | -------------------------------------------------------------------- |
| `2c302ca3` | 34 pins montés sur 21 espaces de travail — plus aucune vulnérabilité |
| `6022f472` | le gate anti-ReDoS mesurait le ramasse-miettes autant que le motif   |
| `65ea5b8f` | fusion PR #9 — bump du groupe d'actions GitHub                       |
| `404c81ce` | le banc du sink fichier pariait sur la vitesse du pool de threads    |
