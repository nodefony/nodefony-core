---
date: 2026-08-27
session_id: 01b65113-9674-475e-beab-400087b9972b
focus: Fermer les deux trous laissés par la session 08-27i — le subscribe muet, le battement des gabarits — puis éprouver les quatre fronts en production
---

# Session retro — 2026-08-27 — 01b65113

## Fait

- **Le `subscribe` muet est fermé** (`00eec46a`). Un canal que personne ne produit rendait
  `realtime:denied { reason: "unknown" }` au lieu du silence. `IRealtimeDenied.reason` resserré en
  `RealtimeDeniedReason` avant le gel SemVer, message client par motif, harnais de test qui LIT le
  contrat. Test unité vu ROUGE, cas e2e contre le pod par le client livré.
- **Le battement périodique quitte les gabarits** (`ced9b55c`). `live:ticker` (1 trame/s/client
  pour ne rien dire) → `live:events` alimenté par un canal entrant `live:dire`, sur le patron déjà
  en place dans les vitrines. Quatre fronts, e2e généré, README. Garde `notInclude("setInterval")`
  vue rouge par sabotage.
- **Le build de production réparé** (`606add6c`) : la vitrine Vue ne se bâtissait pas depuis sa
  création — trouvé parce que le user a demandé de lancer le build prod.
- **Les quatre fronts éprouvés en production** : 200 sur assets bâtis, 0 console, 0 CSP ; socket
  ouverte en 77 ms, pont `api.request` 1 ms de médiane, salon qui pousse d'une connexion à l'autre.

## Coûts évidents

- **Deux sabotages relancés pour rien** : cwd resté à la racine après un `python3` du même appel →
  `Tests no tests` lu comme l'échec attendu. Trois appels perdus.
- **Un appel Bash entièrement annulé** par le garde-fou `cd` relatif (piège déjà connu, repayé).
- **Une substitution qui coupe une chaîne** dans `create.test.ts` (quote fermante emportée) → deux
  tests rouges, un aller-retour de réparation.
- Un seul cycle build+restart pour tout le backend : la règle de batch a tenu.

## Recommandations

1. **Gate manquant — le front généré doit être BÂTI, pas seulement rendu.** `create.test.ts` prouve
   des chaînes dans des fichiers ; il ne voit pas qu'un SFC ne compile pas. Le contrôle jetable
   écrit ici (esbuild + `vue/compiler-sfc` + compilateur Svelte, ~3 s) mériterait d'être permanent.
2. **`LiveTickerController.ts` ment** depuis le retrait du ticker : renommer touche 4 vitrines et
   leurs tests — à faire dans une session qui les ouvre déjà.
3. **Un constat qui énumère se vérifie item par item** : ma note de banc affirmait deux défauts,
   un seul existait.

## Patterns récurrents (déjà gérés)

- ✅ Reproduire contre le pod AVANT de corriger (la sonde a démenti une des deux hypothèses).
- ✅ Test neuf vu rouge, avec lecture du MOTIF de l'échec.
- ✅ Batch des edits backend avant un seul rebuild + restart.

## Commits produits

| Commit     | Sujet                                                           |
| ---------- | --------------------------------------------------------------- |
| `00eec46a` | fix(realtime): un abonnement qui n'aboutit pas ne se tait plus  |
| `ced9b55c` | fix(templates): retirer le battement périodique des gabarits    |
| `606add6c` | fix(vitrines): réparer le build de production de la vitrine Vue |
