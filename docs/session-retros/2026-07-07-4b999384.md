---
date: 2026-07-07
session_id: 4b999384-9c6c-466f-b9eb-e4f21f38847a
focus: S2 multi-dialecte — user/token×3/webauthn/totp sqlite+pg (colKit + queryKit + wire)
---

# Session retro — 2026-07-07 (soir) — 4b999384

## Fait

- **S2 multi-dialecte livré en 1 commit** (`4b63eb51`, poussé) : 5 tables → specs colKit
  (+ kind `dateMs` timestamptz↔Date pour userTable), **queryKit** (`src/queryKit.ts`,
  miroir G1 : `findBySocialProvider` json_each sqlite / `@>` jsonb pg bindé, exécution
  routée `db.all`/`db.execute`), wire `X_PORTED` sqlite+postgres, entité **User
  auto-register** (fichier app `nodefony/entity/user.ts` supprimé — il figeait la
  variante sqlite = bug latent en pg).
- **Preuves demandées « très sérieuses »** : drizzle **198/198 avec PG réel** (32 e2e
  gatés NF_PG_URL, 4 nouveaux fichiers e2e par brique + parité S2 7 + auto-register
  scindé sqlite/postgres) · typecheck 12/12 --force · http 568 · memory 9/9 · load 8/8 ·
  boot réel 0 erreur (users/session drizzle via wire).

## Frictions / leçons (versées au SAS RETEX.md)

1. Erreur PG wrappée par drizzle (`err.cause`) → assert.rejects sur le message rate.
2. 1 dialecte de wire par process (fabrique = closure sur le dialecte) → tests scindés
   par fichier ; le refus cross-dialecte testé pour lui-même.
3. gc à comptage exact → purge explicite d'entrée (état résiduel du fichier).

## Coûts évidents

- 2 allers-retours e2e PG (2 fails de TEST, 0 fail de code) — corrigés en 1 passe.
- 1 seul cycle serveur (restart unique pour http 568 + memory) — batching respecté.

## Commits produits

| Commit     | Sujet                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------- |
| `4b63eb51` | feat(drizzle): multi-dialecte S2 — user/token/webauthn/totp sqlite+pg via colKit + queryKit |
