---
date: 2026-05-30
session_id: 198bc917-9867-47d3-9ee6-fe80c62d7c2b
focus: Chantier domain matching (trustedHosts + @Domain regexp/403 + vhosting e2e) + fix racine turbo poisoning + stress rupture
---

# Session retro — 2026-05-30 — 198bc917

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |    99 |
| Read       |    35 |
| Edit       |    32 |
| TaskUpdate |    11 |
| Write      |     8 |
| TaskCreate |     8 |
| Skill      |     3 |

## Top fichiers Read

| Fichier                       |   × |
| ----------------------------- | --: |
| `http-kernel.ts`              |   7 |
| `Route.ts`                    |   6 |
| `routerDecorators.ts`         |   5 |
| `Route.test.ts` / `router.ts` |   2 |

## Coûts évidents

- **Détour turbo poisoning** (~15-20 min) : dists périmés (framework sans `Domain`/`Session`, http
  sans `compileDomainPatterns`) → module test/studio non chargés → 404. N cycles rebuild/restart
  avant de trouver la racine (`turbo.json inputs = src/**/*.ts` au lieu de `nodefony/**`). MAIS a
  produit un fix de fond réutilisable (ed62d35).
- **Longue session interactive** : ~8 questions design du user (vhosts, concordance, comment font
  les autres, IPv6, regexp routing) → cache read dominant (69 %).

## 💶 Coût (€)

- Total **~€91.6** (≈ $99.6) — cache read **$68.6 (69 %)** + cache write 1h $13.7 + output $16.6 +
  input $0.67. 194 tours. Cache-dominé = le contexte porté (longue session, 3 modules). ~€23/commit
  (4 commits). Levier : `/clear` plus tôt entre la conception et l'exécution.

## Recommandations

1. **Skill check-dist-fresh / build direct par défaut** — le poisoning turbo a coûté cher 2× ce mois.
   Le fix `turbo.json` (ed62d35) devrait le supprimer ; à re-vérifier à la prochaine session multi-module.
2. **Charger ws au root** (ou run.sh des load-tests résout via NODE_PATH/symlink) — `ws` non hoisté
   casse les scripts de charge (symlink ad-hoc créé cette session). Vrai fix = skill load-test.
3. **AIMD / ORM async** — le stress prouve que l'event-loop bloqué par DB synchrone = LE point de
   rupture (ORM 1.8s→18s, 0 crash, récup 8ms). Argument concret pour la cadence adaptative.

## Patterns récurrents (déjà gérés)

- ✅ memory test flake GC en-suite → vert isolé / serveur frais (revécu, géré).
- ✅ cd + start.sh chemin relatif → absolu (revécu, mémoire `feedback_cd_startsh_relative_path`).
- ✅ `.git/index.lock` orphelin → rm + recommit (revécu 2×).
- ✅ turbo poisoning → cette fois RACINE corrigée (pas juste contourné).

## Commits produits

| Commit    | Sujet                                                         |
| --------- | ------------------------------------------------------------- |
| `93dd7d6` | domainMatcher pur + fix bug `instanceof String`               |
| `b987983` | domainAlias→trustedHosts + @Domain routing (regexp/403)       |
| `5a8eefc` | preuve e2e vhosting + fix Router 405-masque-403 + docs MEMORY |
| `ed62d35` | fix racine turbo poisoning (inputs nodefony/\*\* + index.ts)  |
