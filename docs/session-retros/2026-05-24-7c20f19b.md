---
date: 2026-05-24
session_id: 7c20f19b
focus: Lancements multi-process « 2 molettes » — implémentation + validation runtime (cluster, pid, sonde process)
---

# Session retro — 2026-05-24 — 7c20f19b

Session #5 (CODE, suite de #4 décisions). Passage au code du modèle « 2 molettes ».
6 commits, validé en runtime réel.

## Tool usage

| Outil      | Calls |
| ---------- | ----: |
| Bash       |    91 |
| Edit       |    39 |
| Read       |    32 |
| TaskUpdate |    15 |
| Write      |    11 |
| TaskCreate |     9 |
| ToolSearch |     2 |
| Skill      |     1 |

## Coûts évidents

- **Smoke-test cluster = boucle de debug runtime** (le plus gros poste après le code) : 4-5
  cycles start.sh/stop.sh/curl + nuke de masters immortels. Mais c'est CE qui a trouvé les
  2 bugs critiques (park) — débuggage rentable, pas du gaspillage.
- **index.lock husky** rencontré 1× (piège récurrent #I du kit retex) — retrait manuel + retry.
- Beaucoup de Read ciblés (Pdu, aggregator, providers) — nécessaires (code inconnu de la session).

## 💶 Coût (€)

- Total ~**€95** (≈ $104) — cache-dominé ~84 % (cache read 48M tok ≈ €66 + cache write 1h ≈ €14),
  output €14, input <€1. 206 turns. ~€16/commit (6 commits, bug critique + validation runtime).
- Enseignement : session longue mono-thématique → le CONTEXTE porté domine. Rentable ici
  (livrables + bug bloquant trouvé), mais un `/clear` entre « code » et « front » s'imposera.

## Recommandations

1. **Garde-fou « master immortel »** — le park rend le master cluster éternel (respawn) ; un
   start.sh échoué laisse des port-holders → EADDRINUSE au run suivant. stop.sh propre AVANT
   chaque smoke-test. (Déjà mitigé : patterns kill élargis aux `process.title`.)
2. **process.title ↔ patterns kill = couplés** — toute commande qui renomme un process doit
   MAJ les `pkill` de start/stop.sh. Leçon transverse (à garder en tête pour le front/daemons).
3. **Park obligatoire pour tout runtime serveur à `onStart`** (master/worker/mono) — sinon le
   CLI terminate après generate. Capturé dans [[project_launch_modes_decision]].

## Patterns récurrents (déjà gérés)

- ✅ memory.test après modif core/framework hot-path (Pdu, ProcessProbe) — 8/8 sous seuils.
- ✅ Smoke-test runtime AVANT de qualifier « fait » (hygiène #3) — a trouvé un bug que les
  tests unitaires + le script fork e2e masquaient (la commande n'était jamais lancée).
- ✅ Décider+justifier l'archi sans QCM (hygiène #5) ; AskUserQuestion évité.
- ✅ Commits FR via heredoc (-F), pas -m.

## Commits produits

| Commit    | Sujet                                                              |
| --------- | ------------------------------------------------------------------ |
| `262fa27` | modèle « 2 molettes » — `workers` source unique de topologie       |
| `f0695ab` | skills start/stop/load alignés                                     |
| `fd56cc8` | runtime prod single-command unifié (`launchTopology`)              |
| `6901ee2` | cluster boote vraiment (park master+worker) + `process.title`      |
| `135f009` | `pid` (procid RFC 5424) au Pdu — log groupable par worker          |
| `0e1ba7e` | sonde process par worker — CPU/mém/event-loop dans le snapshot pod |
