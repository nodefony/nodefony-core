---
date: 2026-06-15
session_id: 5748d6f8-b9b6-4aaf-84ec-892fa4a61f6c
focus: Social login OAuth (Google/GitHub) dans Studio — boutons + per-provider + rebonjour cohérent
---

# Session retro — 2026-06-15 — 5748d6f8

## Focus

Brancher le **social login OAuth (Google/GitHub) dans Studio** bout-en-bout (après le backend J9 de la
session précédente) + rendre le login « rebonjour » cohérent multi-méthodes. Test RÉEL par le user (vrais
credentials GitHub).

## Fait (4 commits `refactor/p6-security`)

- `7f692c5a` feat(security) : surcharges OAuth **par fournisseur** (successRedirect/failureRedirect/defaultRoles)
  — sinon global. Permet provider réel + fixture E2E `test-oidc` sans conflit (banc 6/6 intact).
- `1fb0096a` feat(framework) : **endpoint public** `GET …/oauth2/providers` (UI honnête) + `getRedirects(provider)`.
- `811c8994` feat(app) : `config/oauth.ts` (extrait, niveau app) + `nodefony.config.ts` `use(security,{oauth2})` +
  `env.ts`/`.env.example` (GOOGLE*\*/GITHUB*\*/OAUTH_REDIRECT_BASE, défaut **localhost**).
- `8d54bf4a` feat(studio) : boutons sociaux data-driven + **rebonjour method-aware** (social/passkey/mdp) +
  AuthStore mémorise user+mode + WebAuthn `SecurityError` mieux classé.

Gates : security 251/251 · framework 284/284 · **oauth2-flow E2E 6/6** · studio typecheck 0 · builds OK.

## Frictions / leçons (→ RETEX.md)

- **localhost partout, jamais 127.0.0.1** : WebAuthn refuse une IP comme rpId → callback OAuth + rpId + session
  = même host. Mon défaut 127.0.0.1 a cassé le passkey (SecurityError mal classé « réseau »).
- **OAuth App (`Ov…`) ≠ GitHub App (`Iv…`)** : le user avait créé une GitHub App → « redirect_uri not associated ».
- **Config provider social = niveau APP, pas le package Studio** (générique) : extrait dans `config/oauth.ts`
  (recette #6) ; per-provider pour coexister avec la fixture E2E sans toucher le banc.
- **UX login : modéliser « le compte a-t-il un mdp ? »** (social=non, passkey/mdp=oui), pas « dernier mode » —
  masquer le mdp pour un passkey = blocage. 3 allers-retours user.
- **Diagnostiquer avant de présumer « bloqué »** : logs serveur = 200 partout → le blocage était la logique UX
  du rebonjour, pas un crash. Lire les logs > deviner.

## Commits produits

| Commit   | Sujet                                                         |
| -------- | ------------------------------------------------------------- |
| 7f692c5a | feat(security) per-provider OAuth                             |
| 1fb0096a | feat(framework) découverte providers + getRedirects(provider) |
| 811c8994 | feat(app) config OAuth Google/GitHub                          |
| 8d54bf4a | feat(studio) login social + rebonjour cohérent                |
