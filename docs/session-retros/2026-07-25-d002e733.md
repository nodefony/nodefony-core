---
date: 2026-07-25
session_id: d002e733-3492-4490-9da5-c3d072d828b3
focus: devkit S4 (entity bout-en-bout) + nodefony inspect + skill des bancs devkit
---

# Session retro — 2026-07-25 — d002e733

## Fait

- **devkit S4 complète** (`61df17e8`→`2c6b1c1e`) — les 7 chantiers du kit :
  pagination de bout en bout avec tri déterministe et allowlist, PUT ≠ PATCH
  (la différence vit dans le service), 409 sur doublon (duck-typing du code
  pilote AVEC descente dans `cause`), grammaire `enum`/défauts/`:index` réellement
  émis, relations qui renseignent `defineEntity({relations})`, test HTTP généré
  par ressource, contexte projet (`getScaffoldContext` + `optionsFrom`), index
  émis en base — FK volontairement PAS émises (justifié).
- **`nodefony inspect <sujet> [--json]`** (`e11952bc`) — au CORE (écart au kit,
  assumé : sa jumelle `check` y est déjà). N'implémente rien, appelle les
  handlers du plan d'administration. Endpoint neuf `kernel/services`.
- **Skill `nodefony-devkit-bench`** — les deux bancs regroupés (découvrabilité
  déplacé depuis `scripts/`, vérité du code généré NEUF).
- **Gate AGENTS.md** (`bea10e97`) — réclamé par le user, preuve négative faite.

## Bugs trouvés (et ce qui les a trouvés)

| Bug                                                           | Trouvé par                 |
| ------------------------------------------------------------- | -------------------------- |
| échantillon généré violant son propre schéma Zod (enum)       | typecheck de l'app témoin  |
| relation déclarée → ORM lève au démarrage du test généré      | `npm test` de l'app témoin |
| `Kernel.initServers` ignorait `runProfile.servers`            | câblage d'`inspect`        |
| `dist/` périmé rend une entité neuve invisible                | banc de vérité, 1ᵉʳ run    |
| `--detach --wait` : faux positif de readiness (autre serveur) | banc de vérité, 2ᵉ run     |
| `create entity User` casse le boot (collision sécurité)       | app témoin                 |

Aucun n'était visible des assertions de chaînes du dépôt.

## Coûts évidents

- Rebuilds du core répétés (le bin CLI lit `dist/`) — inévitable pour éprouver
  le scaffold, mais à batcher davantage.
- Deux preuves négatives ratées avant d'être justes (stash sans effet, `cd`
  relatif après réinitialisation du shell).

## Recommandations

1. **`create command`** — le seul générateur manquant ; l'agent invente sinon.
2. **Publier le catalogue des briques** (`src/nodefony/docs/`, dossier déjà
   publié) — le pointage des clés est fermé, le CHOIX ne l'est pas.
3. **Corriger le faux positif de `--wait`** (vérifier que c'est SON process).

## Patterns récurrents (déjà gérés)

- ✅ dist périmé → désormais une étape explicite du banc.
- ✅ `cd` relatif qui dérive → garde-fou du harnais, rencontré 2×.
- ✅ un vert qui skippe ne prouve rien → drizzle rejoué sur les 3 dialectes.

## Commits produits

| Commit     | Sujet                                                 |
| ---------- | ----------------------------------------------------- |
| `61df17e8` | 409 sur contrainte unique + code pilote ≠ statut      |
| `8884bcfb` | la liste rend une PAGE, PUT cesse de mentir           |
| `0b43e895` | enum, défauts, index, relations, test HTTP            |
| `2d455593` | contexte projet + bug du dialecte multi-connecteur    |
| `2c6b1c1e` | index émis en base (FK délibérément non)              |
| `85238531` | MIGRATION_STATUS — S4                                 |
| `e11952bc` | `nodefony inspect` + `initServers` respecte le profil |
| `3db9cfcf` | MIGRATION_STATUS — inspect + bancs                    |
| `bea10e97` | gate sur les sections neuves de l'AGENTS.md           |
