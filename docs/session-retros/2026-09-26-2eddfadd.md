---
date: 2026-09-26
session_id: 2eddfadd-2e49-418d-a479-d1d5f6a9c66d
focus: trois défauts de code, cycle de types #487, lint typé strict-type-checked avec cliquet
---

# Retex 2026-09-26 — 2eddfadd

- **Fait** : c2d4c7887 fix(http) expirations de session avec `refererCheck` · e6f57baa3
  fix(documentation) `NF_DOCS_REPO_*` · 4abd84a69 + 61ffc3493 fix(security) catégories d'audit
  `authn`/`log` + gate des émetteurs · 53b6dc11e refactor(core) cycle de types cassé, gate
  `as any` (#487 fermé) · 5eef207b8 refactor ~600 assertions inutiles et 105 directives mortes
  retirées par automates · a2c3828d0 build(lint) oxlint aligné sur `strict-type-checked` +
  `oxlint-tsgolint`, cliquet par paquet.
- **Frictions** (versées au sas, thème « 🎯 … sur UN décor ») : correcteur qui juge chaque site
  isolément (chaînes d'assertions cassées) ; `oxlint --fix` global hors sujet, jusqu'à une spec
  figée ; mon `tsc -p` sans les tests ≠ le `typecheck` du dépôt (push refusé) ; un cycle de types
  masquait l'absence des types Node dans `orm-core`/`user`. Aussi : une regex perl au délimiteur
  `|` a vidé un fichier (restauré depuis HEAD) ; `typeAware` allume des règles par CATÉGORIE, pas
  seulement celles qu'on nomme.
- **Décisions** : c'est le LECTEUR qui définit son contrat (cœur : `IServerKernel` ; http :
  `IRouteResolver`/`IRequestRouter`/`IResolvedRoute`) — pas tout dans le cœur, qui aurait dû
  porter des notions HTTP. `log` → `config` et `authn` → `auth` plutôt que deux catégories neuves
  (Studio, filtres, webhooks, entité). Norme = préréglage `strict-type-checked` téléchargé ;
  `no-unsafe-type-assertion`, `consistent-return`, `require-array-sort-compare` coupées car hors
  norme. Cliquet unique par paquet : une session sort un paquet de TOUTES les règles.
