---
date: 2026-07-19
session_id: 9faf1357-d478-4744-84e9-aaebe5879dbb
focus: Convergence audit (9/9 du standard pagination), puis fiabilisation des PREUVES — skip bruyant, bancs partagés, couverture des stores
---

# Session retro — 2026-07-19 — 9faf1357

## Focus

Clôture du chantier pagination (audit = 9ᵉ et dernier store), puis un virage non prévu :
tout le reste de la séance a porté sur **la fiabilité de nos preuves** plutôt que sur des
features. Déclencheur : une question du user (« tout est fait sur tous les dialectes ? »)
à laquelle j'ai d'abord répondu FAUX.

## Fait

- **Convergence audit** (`d583b8f6`) — `IAuditStore.listPage` → `IPage<IAuditEvent>`, propagé
  jusqu'au front Studio (5 consommateurs). Curseur **composite auto-portant `<ts>:<id>`** :
  ferme un trou réel (curseur-id purgé par `gc` → rembobinage silencieux page 1) et supprime
  un SELECT par page. `total` refusable.
- **Idempotence paginée × 3 dialectes** (`b6dd0147`) + capacité `expireSeeded` au harness CORE :
  le filtre d'échéance était retirable sans une seule rougeur, sur TOUS les backends.
- **Skip bruyant** (`a509cf5b`, `4375f324`) — `vitest.gates.ts` racine : catalogue des variables
  d'infra + `gateReporter`. Les identifiants sont LUS dans le compose (`${VAR:-défaut}` + `docker/.env`),
  jamais recopiés.
- **Contrat session partagé** (`913a8723`) — drizzle et mongoose déroulent enfin le banc
  comportemental de `http` au lieu d'en avoir une rédaction maison. Passé sans modification.
- **Couverture des stores** (`3ccb0ced`, `d21fd68e`, `8a0ff749`) — mémoire 50→95 %, redis 64→96 %,
  drizzle-user 71→96 %. **Deux défauts réels** : `updateOne` mémoire ignorait `enabled`/`locked`
  (désactiver un compte = no-op silencieux) ; `decodeCursor` Redis ne validait pas le curseur SCAN
  (un `?cursor=` arbitraire faisait lever la lecture).

## Coûts évidents

- **Un diagnostic faux annoncé au user comme un fait** (3 stores déclarés non couverts, ils
  l'étaient tous). Cause : grep du MOT `listPage` au lieu de l'APPEL `run*PaginationContract(`.
  Coût = un aller-retour complet + la confiance entamée sur le reste de l'audit.
- **Deux relevés de couverture faux** faute des variables d'infra (le rapporteur que je venais
  d'écrire le disait ; je ne l'ai pas lu).
- **Deux fichiers cassés par script** (perl qui mange `${i}`, python qui découpe par index) —
  dont un test devenu faux MAIS VERT.
- Working tree **partagé avec une autre session Claude** : checkout de branche sous les pieds
  en pleine édition (signalé par le user, aucune perte — bases identiques).

## Recommandations

1. **Prouver par l'exécution, pas par le grep** — `--reporter=verbose` puis lire les `describe`
   réellement déroulés. Nouveau thème dans `RETEX.md`.
2. **Neutraliser la garde avant de croire le vert** — appliqué 5× cette session, a validé
   chaque correctif (et invalidé 3 de mes hypothèses, alignées sur le code plutôt que l'inverse).
3. **Lire son propre outillage** — le `gateReporter` existait depuis le matin quand j'ai
   produit un relevé faux l'après-midi.

## Patterns récurrents (déjà gérés)

- ✅ Banc de contrat UNIQUE chez le propriétaire — appliqué 3× (audit, idempotence, session).
- ✅ Capacités déclarées plutôt que parité forcée (`mode: offset|cursor`, `expireSeeded`).
- ✅ Le typecheck des tests attrape ce que les tests ne voient pas — 3 fois cette session.
- ⚠️ `rg -r` = replace : **5ᵉ récidive** (déjà graduée en `feedback_rg_no_replace_flag`).

## Commits produits

| Commit     | Sujet                                                        |
| ---------- | ------------------------------------------------------------ |
| `d583b8f6` | convergence du journal d'audit — 9ᵉ store du standard        |
| `b6dd0147` | idempotence paginée 3 dialectes + filtre d'échéance prouvé   |
| `a509cf5b` | un skip d'infra ne peut plus passer pour un succès           |
| `913a8723` | contrat comportemental des sessions cesse d'être réécrit     |
| `4375f324` | les gates lisent les identifiants du compose                 |
| `3ccb0ced` | fix : `updateOne` mémoire n'appliquait que password et roles |
| `d21fd68e` | fix : un curseur arbitraire faisait lever la lecture (redis) |
| `8a0ff749` | les verbes du repository user rendent un utilisateur         |
