---
date: 2026-06-15
session_id: 76379eca-ac55-4552-abd7-cc7a5b15052b
focus: P6 — autorisation RBAC par canal WS (subscribe/inbound) + refus observable + couverture massive
---

# Session retro — 2026-06-15 — 76379eca

## Focus

Combler la dette « matrice E2E subscribe × auth » (question user : la socket Nodefony
protège-t-elle les canaux selon le rôle à la souscription ?). Réponse terrain = NON
(gate binaire authentifié/anonyme sur namespaces système). → construit le **RBAC par
canal** complet + prouvé E2E + couverture poussée au maximum (demande user répétée).

## Fait (2 commits, poussés `0ba539fd`)

- **`1d4e9a47` feat(security)** — RBAC par canal WS : `IChannelPolicy` (miroir security↔realtime),
  `@RealtimeChannel/@RealtimeInbound` + policy, registre hub, `frameAuthorizer` RBAC (plancher
  système durci ROLE_ADMIN, surcharge `defineSecurityConfig().realtimeChannels`, check rôles
  hiérarchie/scopes/authenticated), **refus observable** (`realtime:denied` + seam client `onDenied`,
  type `IRealtimeDenied` isomorphe core). + 3 bancs E2E vrai client↔serveur.
- **`0ba539fd` test(core)** — banc couverture `RealtimeClient` (transport mock + fake timers).

## Couverture obtenue

frameAuthorizer/decorators/notice/config **100 %** · hub/service **100 % l/f** · firewall **100 %
stmt/func** · RealtimeController **72→99.5 % stmt / 100 % func** · RealtimeClient **67→97 % lignes**.
Reste = branches `window` browser-only (non exécutables sous Node) + gardes Zod mortes.

## Frictions (→ RETEX.md thème Tests)

- **Method-name shadowing** : méthode `syslog` shadowée par `Service.syslog` (logger) → canal absent
  du registre, silencieusement. 2 h de debug. Ne jamais nommer une méthode comme une prop de base.
- **Limite légitime du 100 %** : branches mortes (Zod défaut) + browser-only (`window`) → viser
  100 % lignes/fonctions, accepter ~95 % branches ; ne pas tester l'impossible.
- **Provider canal partagé (hub singleton)** : 2ᵉ abonné = pas de tick immédiat → prouver l'autz via
  `denied` vide, pas le tick.
- **E2E câblage réel** : VRAI Firewall + container partagé + handshake dans `RequestContext.run({user})`.
- **Coverage ciblé** : `coverage-final.json` + jq/node (le reporter text tronque).

## Gates

memory WS 9/9 · realtime 290 · security 221 · core 114 · typechecks core/realtime/security 0 · 0 régression.

## Commits produits

| Commit     | Sujet                                                |
| ---------- | ---------------------------------------------------- |
| `1d4e9a47` | feat(security): RBAC par canal WS + refus observable |
| `0ba539fd` | test(core): couverture RealtimeClient                |
