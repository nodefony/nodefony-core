---
date: 2026-09-26
session_id: 86e32a52-3b99-4961-b270-a2fa67935839
focus: "#498 — no-unnecessary-condition allumée par cliquet sur le cœur (hors kernel/cli/client)"
---

# Retex 2026-09-26 — 86e32a52

- **Fait** : cbe85c16b fix(syslog) — `Pdu.severityToString("WARN")` récursait jusqu'au
  `RangeError` (test vu rouge) ; f72b95bea build(lint) — cliquet `no-unnecessary-condition`
  (surcharge `.oxlintrc.json` par dossier relu), ~100 sites du cœur, helpers `unrefTimer` et
  `constructorName`. #498 commenté (doctrine + reste chiffré).
- **Frictions** :
  - Accesseur `Command.prompts` qui levait : rupture d'un `undefined` observable, vue par un seul
    test existant (versée au sas, thème ⏳).
  - Lint fichier par fichier vert, build ROUGE : élargir un type (`Partial<Record>`) se propage aux
    signatures voisines (`devStop`, `devStatusReport`) que le lint ciblé ne compile pas. Le
    typecheck du paquet doit suivre CHAQUE lot, pas le précéder d'un build.
  - Mon helper `constructorName` changeait `""` en `undefined` (→ `getType` rendait `"Error"`) :
    rattrapé à la relecture, pas par un test. Un helper qui factorise trois lectures doit
    reproduire la sémantique de la PLUS permissive.
- **Décisions** :
  - Cliquet par surcharge de dossiers (liste qui grandit) plutôt que règle globale + exclusions :
    le gabarit ne bascule qu'une fois, avec le banc `verify-generated`.
  - Quatre traitements par site (garde morte / type menteur / faux positif du rétrécissement /
    entrée publique JS), jamais de `--fix`.
  - `noUncheckedIndexedAccess` après la règle : 871 erreurs sur le seul cœur ; les gardes sur accès
    indexé sont rendues honnêtes dès maintenant (`Partial`, `.at()`, `Object.hasOwn`) pour ne pas
    être retirées puis remises.
