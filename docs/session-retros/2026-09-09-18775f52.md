---
date: 2026-09-09
session_id: 18775f52-89c4-402b-9500-08f9a1864af7
focus: La config d'un module a son fichier, une garde qui refuse le silence — et trois lecteurs devenus aveugles au déménagement
---

# Session retro — 2026-09-09 — 18775f52

## Fait

- **#285 · #296 · #289 fermés** (`43049bb4`) — grappe « où vit la config d'une application »
  soldée 4/4 enfants. La configuration de chaque module part dans
  `nodefony/config/<module>.ts` ; le manifeste ne garde que l'index ordonné.
  Dépôt : 570 → 215 lignes. Application générée : 366 → ~200.
- **Constat `fragment-without-satisfies`** ajouté à `nodefony doctor`, vu ROUGE avant d'être
  écrit (2 échecs exigés sur 5 contrôles neufs).
- **Le parcours du dossier des fragments** allait exister en trois copies → factorisé
  (`fragmentDirFiles`), et `fragmentsWithoutSatisfies` exposé au barrel.
- **CI réparée** (`c480eaf2`) — sept jobs rouges, une cause : le banc lisait le manifeste racine
  en dur. Il appelle désormais le lecteur du produit, résolu depuis le paquet REÇU.
- **Deux défauts du banc de conformité** qui se cachaient l'un l'autre : capture au premier
  crochet au lieu du crochet apparié, et filtre des modules conditionnels annoncé mais absent.

## Coûts évidents

- ~20 minutes de diagnostic sur trois faux verdicts, tous dus au lanceur `bin/nodefony` qui
  délègue au CLI installé dans l'application (donc à `alpha.3`). Une commande
  (`import.meta.resolve`) tranchait ; elle a été lancée en dernier.
- Un garde-fou de `cd` relatif a bloqué une commande composée SANS que le fichier soit écrit :
  17 tests verts pris pour une preuve alors que mes tests n'existaient pas encore. Rattrapé par
  un `grep -c` de contrôle.
- Trois allers-retours sur le banc de conformité (filtre parti du mauvais point d'ancrage, puis
  bloc `charges` emporté par mon propre découpage).

## Recommandations

1. **Un gate qui interdit de viser `nodefony.config.ts` en dur** dans les lecteurs du dépôt et
   des skills — trois aveugles trouvés le même jour, tous du même motif. Le gate existe déjà
   pour `gh project item-list` (`board-source.test.mjs`) : même patron, autre cible.
2. **Auditer les autres harnais de banc qui appellent leur corps sans l'attendre** — `step()` de
   `verify-generated.mjs` avale l'erreur d'une étape asynchrone, qui passe alors pour verte.
3. Publier l'alpha.4 lève trois symptômes constatés sur application générée (types d'entrée
   renommés, types de Studio, lecteur de fragments) — les trois sont des décalages de version,
   pas des défauts.

## Patterns récurrents (déjà gérés)

- ✅ Prouver la cible avant de croire le verdict — appliqué (sonde de config, sonde de zone
  ouverte, sonde de module fantôme), mais APRÈS avoir conclu trois fois trop vite.
- ✅ Suspecter son propre diff — le typecheck a rattrapé deux corps de config vidés par mon
  script de découpage.
- ✅ Un test neuf se voit rouge une fois — fait pour les cinq contrôles du gate.

## Commits produits

| Commit     | Sujet                                                                      |
| ---------- | -------------------------------------------------------------------------- |
| `43049bb4` | refactor(config): donner à la config d'un module son fichier, et une garde |
| `c480eaf2` | fix(devkit-bench): rendre au banc la vue qu'il croyait avoir               |
