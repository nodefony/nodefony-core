---
date: 2026-05-24
session_id: ac1da225-2c43-41bb-b7dd-cb26b015328a
focus: Mode cluster sans PM2 — Phase 4 COMPLÈTE (4a forward opt-in · 4b e2e cross-process · 4c sonde agrégée pod)
---

# Session retro — 2026-05-24 — ac1da225

> Session longue (135 turns) : reprise Phase 4 → 4a + 4b livrées → « fin de session » →
> l'utilisateur a relancé sur 4c (sonde agrégée) → Phase 4 complète. 4 commits.

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    54 |
| Edit  |    43 |
| Read  |    30 |
| Write |     9 |
| Skill |     1 |

> Session propre : aucun fichier relu 5×, aucune commande Bash répétée ≥3 (hors git/grep/echo).

## Coûts évidents

- **Dist framework périmé 2× au lancement d'un harnais fork** (`markBroadcastChannel` puis
  `processProbeTransport` introuvables) : (1) replay turbo pendant le commit 4a a écrasé le dist ;
  (2) export oublié dans `index.ts` (ajouté la const mais pas au barrel). → rebuild à chaque fois.
- 3 retries commitlint : sujet `Phase…` (sentence-case), header > 100 car. → sujet minuscule + court.
- Double-export `IRealtimeProbe` types dans `index.ts` (bloc existant non vu d'abord) → corrigé.

## 💶 Coût (€)

- Total ~**€59** (≈ $64) — **cache-dominé ~82 %** (cache-read 29.3M tok ≈ $44 + cache-write 1h 305k ≈ $9),
  output 139k ≈ $10.5. 135 turns, 4 commits → ~€15/livrable.
- Enseignement : session **longue** = cache-read qui gonfle à chaque tour (le contexte porté est relu).
  4a/4b et 4c étaient couplés (même feature), mais un `/clear` après 4b aurait coupé la moitié du cache-read.
  → pour une feature multi-phases, clore + reprendre entre sous-phases dès que le contexte n'est plus utile.

## Recommandations

1. **Rebuild AVANT tout harnais fork** qui importe les dist (vu 2×). Réflexe : `npm run build` du module
   ciblé juste avant `node *.mjs` / `run.sh cluster-*`. Cf [[feedback_turbo_cache_stale_logs]].
2. **Ajouter un export = 2 endroits** dans un barrel `index.ts` (import + bloc `export {}`) ; vérifier
   le bloc existant pour éviter le double-export (TS l'accepte au build Rollup mais c'est sale).
3. **Découper une feature multi-phases** par `/clear` entre sous-phases indépendantes (coût cache).

## Patterns récurrents (déjà gérés)

- ✅ Décisions design tranchées + POURQUOI (forward opt-in = Zero-Trust ; sonde push + désactivable).
- ✅ Seam testable systématique (IRelayWorker / IProbeWorker / IClusterProbeTransport) → tests SANS fork.
- ✅ Preuve e2e par harnais fork réel (cluster-realtime-e2e + cluster-probe-e2e) en plus des unit tests.
- ✅ Memory gate avant commit ; faux rouge GC combiné identifié (varie run-to-run, vert en isolé).
- ✅ Perf : tout le cluster est gaté (mono-process = 0 overhead) ; bypass total quand sonde coupée.
- ✅ Allowlist déjà couvrante (`node *`, `bash .claude/skills/*`) → 0 prompt, 0 ajout.

## Commits produits

| Commit    | Sujet                                                                   |
| --------- | ----------------------------------------------------------------------- |
| `9b4810f` | feat(realtime): forward backplane opt-in par canal (phase 4a)           |
| `c37f1cd` | test(cluster): phase 4b — preuve e2e realtime cross-process (fork réel) |
| `9dec309` | chore(retro): session 2026-05-24 (ac1da225) — cluster Phase 4a + 4b     |
| `2435eff` | feat(cluster): phase 4c — sonde agrégée pod (push, désactivable)        |
