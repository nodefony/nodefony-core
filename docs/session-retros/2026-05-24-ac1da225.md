---
date: 2026-05-24
session_id: ac1da225-2c43-41bb-b7dd-cb26b015328a
focus: Mode cluster sans PM2 — Phase 4a (forward opt-in par canal) + 4b (preuve e2e cross-process)
---

# Session retro — 2026-05-24 — ac1da225

## Tool usage

| Outil | Calls |
| ----- | ----: |
| Bash  |    32 |
| Read  |    20 |
| Edit  |    15 |
| Write |     1 |
| Skill |     1 |

## Top fichiers Read/Edit

| Fichier                         |   × |
| ------------------------------- | --: |
| RealtimeHub.ts (Edit)           |   5 |
| run.sh (Edit)                   |   3 |
| RealtimeHub.test.ts (Edit/Read) | 2+2 |
| RealtimeController.ts (Edit)    |   2 |

> Session propre : aucun fichier relu 5×, aucune commande Bash répétée ≥3 (hors git/echo).

## Coûts évidents

- **Dist framework périmé au 1er run e2e** (`markBroadcastChannel is not a function`) : le commit 4a
  a déclenché un **replay turbo** qui a écrasé le dist (méthode absente) → rebuild manuel. 1 cycle perdu.
  C'est le piège connu « turbo rejoue le cache » (mémoire `feedback_turbo_cache_stale_logs`).
- 2 tentatives de commit 4a (commitlint : sujet `Phase…` = sentence-case rejeté → minuscule).

## 💶 Coût (€)

- Total ~**€24.9** (≈ $27) — **cache-dominé ~80 %** (cache-read 10.07M tok ≈ $15 + cache-write 1h 213k ≈ $6.4),
  output 66k ≈ $5. 65 turns, 2 commits livrés → ~€12.5/commit (décision archi + preuve e2e = rentable).
- Levier : le contexte porté (CLAUDE.md racine + 3 CLAUDE.md modules auto-injectés + MEMORY index + SKILL)
  pèse le cache. Session déjà courte/ciblée → peu de marge sans `/clear` plus agressif.

## Recommandations

1. **Réflexe rebuild AVANT un harnais fork** qui importe les dist : après une modif source committée,
   le dist local peut être stale (replay turbo) → `npm run build` du module ciblé juste avant le `node *.mjs`.
2. **Commit subject toujours minuscule** après `type(scope):` — commitlint `subject-case`. (Déjà connu, re-vu.)
3. RAS skill/mémoire neuve : pattern bien couvert par les skills existants.

## Patterns récurrents (déjà gérés)

- ✅ Décision design tranchée + POURQUOI (pas de QCM) — forward opt-in justifié (Zero-Trust).
- ✅ Memory gate lancé avant commit framework (faux rouge GC combiné identifié, vert en isolé).
- ✅ Harnais fork réutilisé de Phase 3 (cluster-ipc.mjs) comme patron pour cluster-realtime-e2e.mjs.
- ✅ Allowlist déjà couvrante (`node *`, `bash .claude/skills/*`) → 0 prompt, 0 ajout.

## Commits produits

| Commit    | Sujet                                                                   |
| --------- | ----------------------------------------------------------------------- |
| `9b4810f` | feat(realtime): forward backplane opt-in par canal (phase 4a)           |
| `c37f1cd` | test(cluster): phase 4b — preuve e2e realtime cross-process (fork réel) |
