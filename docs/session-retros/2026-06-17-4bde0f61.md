---
date: 2026-06-17
session_id: 4bde0f61
focus: assainissement MIGRATION_STATUS (audit vérité passe 3) + perf GPU Studio (Supervision)
---

# Session retro — 2026-06-17 — 4bde0f61

## Focus

Reprise → jauge de ce qui reste dans P6 → l'utilisateur redirige : **assainir
`MIGRATION_STATUS` AVANT `@Csp`**. Audit vérité complet (confronté au code, 111 commits
depuis le 12/06) + assainissement de forme. Puis **incident perf** : GPU Brave à fond
quand l'onglet Studio (page Supervision) est ouvert → diagnostic + fixes.

## Fait

- **Audit vérité passe 3** (append `docs/migration/AUDIT-verite-2026-06.md`) +
  **assainissement `MIGRATION_STATUS`** (`495fcb40`) : pavé P6 **13 KB → 0,9 KB** ;
  P6 recalé **64 %** (10✅ 7🔶 4⬜ ; P6.9/P6.11/P6.12 périmés ⬜→🔶 ; +P6.9c passkeys) ;
  **GLOBAL recompté 62 %** (la ligne ne sommait pas ses propres lignes, 57 % faux) ;
  +P10.14 `@nodefony/documentation` (non tracké) ; encadré « Prochaine étape » réécrit
  (il décrivait P6 comme non commencé). Refs ⏭️ (mikroorm/pm2/sequelize) laissées (déjà
  correctes). Découverte : data plane sécu **PAT déjà persisté** (`ITokenStore` kind:pat +
  stores Drizzle/Mongoose/Redis) → P6.12 = quick win.
- **Perf GPU Studio** (`0a0476fd` + `42a9f0b5`) : glow `box-shadow` infini → anneau
  statique + respiration opacity + `prefers-reduced-motion` ; `LoadingOverlay` sans
  `blur` ; `contain:content` sur `KpiCard` ; valeur `KpiCard` en `<div>` (fin erreur
  d'hydratation `<p><div>`). **GPU 90 % → 63 %** (mesuré user).

## Frictions / leçons (→ RETEX.md)

- HMR Vite ne purge pas un `<style>` injecté-une-fois → hard-reload obligatoire pour voir un fix CSS.
- 2 sources divergentes de styles live (`ormFormat` vs `FlashValue`) = la dette qui a causé le glow.
- Bandeau GLOBAL ne sommait pas ses lignes ; refs ⏭️ déjà légitimes (ne pas purger — contrôle).
- `perl -CSD` : pattern emoji ne matche pas (script en octets) → sans `-CSD` ; awk macOS compte ⏭️=⬜.
- Diag perf navigateur = user-driven (pas de headless) ; couper « Temps réel » = test discriminant.

## Commits

| Commit   | Sujet                                                             |
| -------- | ----------------------------------------------------------------- |
| 495fcb40 | docs(migration): assainit P6 + resync vérité 2026-06-17 (passe 3) |
| 0a0476fd | fix(studio): widgets live en compositor + KpiCard rendu en div    |
| 42a9f0b5 | fix(studio): perf GPU Supervision (suite) — blur + contain        |

## Reste

- **GPU Supervision encore ~63 %** : page de **3315 lignes** trop lourde (LCP render
  delay ~4 s) → l'alléger (éclatement + `contain` ciblé + isoler les composants live).
- **`@Csp` per-route** : la prochaine étape P6 d'origine (avant la redirection migration).
