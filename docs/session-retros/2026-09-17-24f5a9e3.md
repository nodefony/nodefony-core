---
date: 2026-09-17
session_id: 24f5a9e3-ae50-4b21-ab83-a4eaaf714758
focus: "#327 — exiger les deux serveurs de logs au lieu de les espérer"
---

# Session retro — 2026-09-17 — 24f5a9e3

## Focus

Reprise après `/clear`, puis **#327** (enfant de #316) : `LogBackplaneE2E.test.ts` se sautait
faute d'URL — deux transports et deux pilotes de relecture jamais joints à un vrai serveur.

## Fait

- Le cœur **DÉCLARE** ses deux cibles d'infra (`gateReporter` + `proof` sur le titre de suite).
- `orm.yml` gagne la tâche « Backplane de logs » — décor levé par le **compose du dépôt**.
- Absence **énoncée** dans `test-cli`, qui serait devenu rouge sans un mot.
- `ciGateVars.test.ts` étendu au cœur (son motif ne voyait que `src/packages/@nodefony/…`).
- `test:all` lève et **attend** les deux serveurs (`EnvGate.readyUrl`).
- #327 fermé avec compte rendu ; sommes parents recalées (#316 → 3, #388 → 5).

## Coûts évidents

- Deux fois le **code de sortie de `tail`** lu à la place de celui de `vitest` / `curl` —
  la première fois j'ai failli conclure « le gate ne mord pas » sur un `EXIT=0` faux.
  Gradué : [[feedback_shell_false_diagnostics]]. Récidive malgré la mémoire.
- Un `cd` relatif refusé par le harnais (chemin absolu exigé) — un aller-retour perdu.
- Lecture du terrain avant d'éditer : ~12 fichiers. C'est ce qui a évité d'appliquer
  l'énoncé du ticket à la lettre et de rendre 6 variantes de forge rouges.

## Recommandations

1. `ticket-close.mjs` — son `--grep '#N\b'` ramène les commits qui citent le ticket en
   CONTEXTE (ici `d2c4aa42`, qui énumérait les enfants de #316). Même cause pour le
   `post-commit` qui a passé **#175 « In Progress »** sans qu'aucun travail ne l'avance.
   Un filtre « le numéro apparaît hors d'une énumération d'enfants » est difficile ; la
   parade réaliste est que l'auteur RELISE la liste, ce que le skill impose déjà.
2. Pas de skill neuf — le travail est couvert par `nodefony-session` et `nodefony-ticket`.

## Patterns récurrents (déjà gérés)

- ✅ Débrancher pour voir un gate rouge, avec preuve du débranchement.
- ✅ Nommer ce qui n'a PAS été lancé avant de dire « fait ».
- ✅ Lever le décor par la source unique (compose) plutôt qu'en le redécrivant.

## Commits produits

| Commit     | Sujet                                                              |
| ---------- | ------------------------------------------------------------------ |
| `af0d8972` | test(log): exiger les deux serveurs de logs au lieu de les espérer |
