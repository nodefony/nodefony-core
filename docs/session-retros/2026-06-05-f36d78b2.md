---
date: 2026-06-05
session_id: f36d78b2-6028-4135-9b79-92dee52f2ce6
focus: http→vitest (dernier mocha) + maj deps + 0 warning build + suppression totale mocha + READMEs
---

# Session retro — 2026-06-05 — f36d78b2 (4e du jour)

## Fait

- **`@nodefony/http` mocha→vitest** (`fa9120d`) : 3 configs (unit/integration/load séquentielles), gate
  `test:memory` 9/9, codemod 58 done→Promise + this.timeout→config. http = dernier détenteur mocha.
- **maj deps outdated** (`4a7d883` + angular) : tout l'in-range (vitest/rollup/mantine/react/vite/vue…) +
  `@angular 21→22` (POC). Reste `tedious` (major, sequelize en suppression). vuln mocha partie.
- **0 warning TypeScript** (`45ce47d`) : exclure les tests du build tsconfig (alignement convention) +
  chaîne anti-race `exports.types→source` complétée (user+orm-core) + 5 bugs source. 2024→0.
- **suppression totale mocha** (`f9c4c50`) : 55 imports strippés + 4 shims + 3 .mocharc + 6 configs nettoyés.
- **READMEs** framework/http/studio + docs/ security (`4578749`) — llm exclu (couche IA, trop tôt).
- **0 warning total** (`c1ba86f`) : filtre onwarn `EMPTY_BUNDLE` (9 configs + template). Build --force 20/20.
- Template `create-module` aligné (tsconfig exclut tests, EMPTY_BUNDLE, pins 4.1.8).

## Frictions clés (détail → RETEX.md)

- Retirer `@types/mocha` expose 2024 warnings build (rollup type-check les tests) → exclure tests du build tsconfig.
- `tail -15` masque les warnings → compter `grep -c`.
- `exports.types→source` = chaîne transitive (casser un maillon = TS2307, cascade).
- Turbo rejoue logs cachés → `--force` pour la vérité (18 « restants » → 0).
- commitlint subject-case : sujet minuscule après le type.
- Gate mémoire = serveur frais (heap GC-noisy sinon).

## Commits poussés (origin/claude-ts)

fa9120d · 4a7d883 · 45ce47d · f9c4c50 · 4578749 · c1ba86f (+ 2cef2d4/af4b47c = realtime, user).

## Reste

ws-messages-load `sustained < 30 MB` rouge pré-existant (~120-160 MB, identique mocha, GC sans --expose-gc).
CliIntegration --help/--version = filet CLI env-dépendant (redis/ports). 290 vulns dependabot sur `main`.
Prochaine = boussole durcissement ORM (sequelize suppression + mongoose refait).
