# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
name: "CodeQL"

# ⚠️ Deux pièges corrigés ici, qui rendaient l'analyse INEXISTANTE :
#
# 1. Seule `main` était écoutée, alors que tout le développement vit sur
#    `claude-ts`. Le code réellement écrit n'a donc jamais été analysé, et les
#    alertes affichées sont des fossiles de `main` datés de 2024 — jamais
#    rejoués, pointant des lignes qui sont aujourd'hui des commentaires.
# 2. Le filtre valait `- "src"`, qui ne correspond à AUCUN fichier : GitHub
#    attend un motif de chemins (`src/**`). Même un envoi sur `main` ne
#    déclenchait rien. Un filtre qui ne filtre pas ressemble à un filtre, et
#    c'est ce qui le rend coûteux.
on:
  push:
    branches: ["main", "claude-ts"]
    paths:
      - "src/**"
      - ".github/workflows/codeql.yml"
      - ".github/codeql-config.yml"
  pull_request:
    branches: ["main", "claude-ts"]
  schedule:
    - cron: "00 00 * * *"

jobs:
  analyze:
    name: Analyze
    # Runner size impacts CodeQL analysis time. To learn more, please see:
    #   - https://gh.io/recommended-hardware-resources-for-running-codeql
    #   - https://gh.io/supported-runners-and-hardware-resources
    #   - https://gh.io/using-larger-runners
    # Consider using larger runners for possible analysis time improvements.
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    timeout-minutes: ${{ (matrix.language == 'swift' && 120) || 360 }}
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript"]
        # CodeQL supports [ 'c-cpp', 'csharp', 'go', 'java-kotlin', 'javascript-typescript', 'python', 'ruby', 'swift' ]
        # Use only 'java-kotlin' to analyze code written in Java, Kotlin or both
        # Use only 'javascript-typescript' to analyze code written in JavaScript, TypeScript or both
        # Learn more about CodeQL language support at https://aka.ms/codeql-docs/language-support

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # Initializes the CodeQL tools for scanning.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          config-file: .github/codeql-config.yml
          # ⚠️ NE PAS déclarer `queries:` ici. Le champ ÉCRASE celui du fichier
          # de config (sauf préfixe `+`) : le renseigner des deux côtés ferait
          # disparaître en silence la suite déclarée là-bas. La suite de
          # requêtes ET les exclusions de chemins vivent dans
          # `.github/codeql-config.yml`, source unique — c'est aussi là qu'est
          # écrit le POURQUOI de `security-extended`.

      # Autobuild attempts to build any compiled languages (C/C++, C#, Go, Java, or Swift).
      # If this step fails, then you should remove it and run the build manually (see below)
      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      # ℹ️ Command-line programs to run using the OS shell.
      # 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun

      #   If the Autobuild fails above, remove it and uncomment the following three lines.
      #   modify them (or add more) to build your code if your project, please refer to the EXAMPLE below for guidance.

      # - run: |
      #     echo "Run, Build Application using script"
      #     ./location_of_script_within_repo/buildscript.sh

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{matrix.language}}"
