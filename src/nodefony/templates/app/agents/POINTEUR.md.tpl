# <%= it.pointeur %>

> **Lis `AGENTS.md`** — les instructions agent de cette application vivent
> là-bas (standard **Agentic AI Foundation** / Linux Foundation, précédence
> « le plus proche gagne »). <%= it.agents %> ne le lit pas d'office : ce
> fichier n'est qu'un pointeur, et ce qu'on y recopierait divergerait en
> silence.
>
> Deux réflexes gouvernent les autres, et `AGENTS.md` les détaille : **avant
> d'écrire un fichier, vérifie qu'un générateur ne le produit pas**
> (`npx nodefony create --help`) — et **avant de dire « fait »,
> `npm run verify`** : un `npm test` vert ne typecheck RIEN.
