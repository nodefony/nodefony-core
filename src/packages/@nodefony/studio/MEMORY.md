# MEMORY.md — @nodefony/studio

> IA, ultra-concis. Détails session : [`CLAUDE.md`](./CLAUDE.md). Kit reprise : mémoire `project_studio_prep_kit`.

## Purpose

Admin web Nodefony (successeur `monitoring-bundle`). Backend controller + SPA React 19 via `@nodefony/frontend`. État : **P6 BRANCHÉ** — auth réelle (firewall `@nodefony/security`, session BFF cookie, RBAC `ROLE_NODEFONY_ADMIN`), mocks `/auth/*` **SUPPRIMÉS**. `IAdminApi`/`AdminBroker` (P10.2) **livré** (data plane par module via broker). Pages Sécu livrées : Sessions/Users/API Keys/Firewall/Audit/Profil self.

## Core Components

- `index.ts` : `class Studio extends Module`. `onKernelBoot` → `frontendService.registerEntry({type:"react19", entry:"./frontend/src/main.tsx", root:"./frontend", name:"studio", apiProxyPaths:["/nodefony/studio/api"]})`.
- `StudioController` `@controller("/nodefony")` : UI = `/nodefony` (HTML+renderTags) + `/nodefony/{page}` (SPA fallback 1 seg) + **`/nodefony/modules/{name}` (SPA fallback 2 seg LITTÉRAL — deep-link/F5 sur `modules/:name`, fix 404 2026-05-20)** ; routes en `@Get`/`@Post` (migré depuis `@route`) ; API = `/nodefony/studio/api/{health,info,stats}` (`info` bypass **réduit** name/version/env · `stats` gardé **`ROLE_SUPERVISOR`** · `/auth/*` MOCK **SUPPRIMÉ** → auth réelle `/nodefony/security/api/auth/*`), `/nodefony/studio/api/realtime/info` (available:true). SSE `/logs/stream` SUPPRIMÉ 2026-05-23 (mort + cassé HTTP/2) → logs via canal WS `syslog:stream`.
- `StudioRealtimeController` `@controller("/nodefony/studio/api")` : **WS `/realtime`** (JSON-RPC 2.0). **Pub/sub par canal on-demand** : handshake = `realtime:welcome` seul ; client envoie `subscribe`/`unsubscribe {channel}` → serveur démarre/arrête le provider (`nodefony/realtime/providers.ts` : `createSyslogBridge`/`createStatsTicker`, transport-agnostiques). Canaux `syslog:stream`, `dashboard:stats` (1/s : cpu%/eventLoopMs/loadavg/memory). État pub/sub sur le ctx. Cleanup par canal + global `ctx.once("onFinish")`.
- Frontend : Mantine v8 + MobX 6 + Router 7 + TanStack Table 8 + Mantine charts/spotlight + Tabler icons + **`@xyflow/react` 12.10.2 (React Flow, ERD) + dagre 0.8.5 (auto-layout)**. 6 stores MobX (Auth/Connection/Ui/Chat/**Admin**/Root). Services : ApiClient(JWT, + `getAbsolute()` pour chemins data plane hors baseUrl)/AuthService/RealtimeClient. Pages réelles : Login(stepper 4), Dashboard, Logs(Live+Fichiers), Profiler, Chat, **System (Admin API explorer)**, **Routes (RoutesView)**, **Database (ERD React Flow)** ; reste = stubs. **Pages lazy-load** (`React.lazy` dans `App.tsx` + `<Suspense>` dans AdminLayout).
- **Database ERD** (`routes/Database.tsx`, 2026-05-22) : consomme le data plane ORM (`/nodefony/orm/api/{orms,graph,export/dbml}`). React Flow + nœud `TableNode` custom (colonnes PK clé/FK/unique/type, handles), arêtes = relations (`smoothstep`, label cardinalité, animées si many), **auto-layout dagre LR**, sélecteur connecteur, **bouton Copier DBML** (clipboard, pivot IA). Route `/nodefony/databases`. **Thème relié à Mantine** : `colorMode={useComputedColorScheme()}` + vars `--xy-*` mappées sur `--mantine-*` (`RF_THEME`) → suit light/dark, pas de canvas noir. ⚠️ React Flow exige une **hauteur DÉFINIE** sur le conteneur (minHeight seul → height:100% = 0 = canvas vide). `fitViewOptions={{maxZoom:1,padding:0.3}}` (sinon 1 nœud = zoom à fond).
- **Résilience UX (2026-05-22)** : `components/ConnectionOverlay.tsx` (overlay déconnexion temps réel — radar pulsé + compte à rebours **live** sur le backoff réel du client + bouton `retryNow` + flash vert à la reprise + logo/STUDIO ; visible que si `everConnected`) monté dans `AdminLayout`. `components/ErrorBoundary.tsx` (classe, `getDerivedStateFromError`) câblé autour de l'`<Outlet>` (`variant="page"`, clé=pathname) + racine App (`variant="full"`) → page propre au lieu de l'écran blanc (attrape les erreurs de **rendu**, pas d'import-module). ⚠️ **`keyframes` n'est PAS exporté par `@mantine/core` v8** → @keyframes CSS injectés via `<style>`. Backoff client court en dev (`reconnectDelay:800/Max:4000` dans `RootStore`) → l'overlay se retire seul en ≤4s au retour serveur.
- **Sidebar restructurée (2026-05-22)** (`layouts/navConfig.ts`) : groupes Overview / **Security** (Firewall/Users/Sessions — dédié, destiné à grossir) / Data (Database/Migrations) / Observability (Logs/Profiling) / System (Admin API/Modules/Services/Routes/NPM) / Account. **PM2 retiré** (déprécié, cf [[project_pm2_deprecation]]) — route + stub `Pm2` + stub `Databases` supprimés.
- **Sidebar v2** (`AdminLayout` + `layouts/navConfig.ts`) : nav **data-driven** (`NAV_GROUPS`). Capacités : **mode rail** (icônes-seules, largeur 68, tooltips ; toggle header `IconLayoutSidebar*`, persisté `ui.rail`), **groupes repliables** (chevron, persisté `ui.collapsedGroups`), **filtre live** (`ui.navQuery`, transient), **groupe « Data plane » auto-généré** depuis le catalogue `/framework/api/admin` (1 entrée/producteur + badge nb endpoints, lien `/nodefony/system?p=<ns>`). `System.tsx` lit `?p=` → ouvre l'accordéon du producteur (controlled).
- **RoutesView** (`routes/RoutesView.tsx`, route `/nodefony/routes`) : table **TanStack 8** réelle via `GET /nodefony/framework/api/routes` (méthodes badges, path, name, controller.action, module, firewall). Filtre global + tri. Remplace le stub `Routes`.
- **NodefonyLogo** (`components/NodefonyLogo.tsx`) : logo officiel (arcs bleu/vert/cyan) embarqué **data-URI PNG** (41×64, ~6 Ko, pas d'asset/dossier). Top bar = ce logo ; carte **Heap V8** = `IconBrandNodejs` (Tabler, V8=runtime Node).
- **AdminStore** (`stores/AdminStore.ts`, hook `useAdmin`) : consomme le **catalogue** `/nodefony/framework/api/admin` (discovery P10.2) → `producers[]` (namespace/label/icon/order/endpoints) + `invoke(path)` générique (GET). Page **System** (`routes/System.tsx`, route `/nodefony/system`) génère la nav admin depuis le catalogue + invoque chaque endpoint GET et affiche le JSON. Zéro URL data plane en dur. Endpoints paramétrés `{x}` (ex `module/{name}`) : champ de saisie par variable → chemin résolu+encodé avant l'appel (sinon on invoque le littéral `{name}` → 404).
- **Dashboard** : `/info` réel = `/nodefony/kernel/api/info` (expose `debug`, `version`, `node`...) + KPI **Routes** = `/nodefony/framework/api/info` `routesTotal` + KPI **Sessions** = `/nodefony/http/api/sessions` (`active` + storage + badge `déprécié`). Stats live = WS `dashboard:stats`.
- **Modules** (`routes/Modules.tsx`, route `/nodefony/modules`) : page RÉELLE (remplace le stub) inspirée du legacy `Bundle.vue` — **grille de cartes cliquables** (icône app/package, nom, version, type, chemin, deps en chips). Clic carte → `navigate(/nodefony/modules/{key})`.
- **ModuleDetail** (`routes/ModuleDetail.tsx`, route `/nodefony/modules/:name`) : page détail à **onglets Mantine** (comme le détail legacy `Bundle.vue`), TOUS réels : Vue d'ensemble · **Docs** · **API** · Dépendances · **Routes** (table filtrée `route.module === key`) · **Services** (table name DI + classe) · **Config** (JSON `module.options` sérialisé). Détail via `/api/module/{key}` (enrichi `services`+`config`). Jointure routes : `route.module` = **clé courte** (app/framework/studio/test/test-frontend-react), pas le nom `@nodefony/*`.
  - **Onglet Docs** (`DocsPanel`) : TOC (`/nodefony/kernel/api/module/{key}/docs`) + rendu markdown `react-markdown`+`remark-gfm` dans `TypographyStylesProvider`. Corps chargé à la demande par doc (`/docs/{slug}`). Badges version (`package.json`) + statut (frontmatter) + fraîcheur git. Emplacement HYBRIDE : prose dans `<module>/docs/*.md` (cf ADR-0001).
    - **UX lecture** : largeur de lecture bornée (~820px), interligne 1.75 ; TOC + lecture en **pleine hauteur** (`READER_HEIGHT = calc(100vh - 250px)`, scroll indépendants) ; liens internes `.md` cliquables (switch de doc sans reload).
    - **Bouton plein écran** (`IconMaximize`) → `Modal fullScreen` (lecture large 1100px) pour les schémas.
    - **Mermaid** (`MermaidDiagram`) : les blocks ` ```mermaid ` → diagrammes vectoriels. `mermaid` **lazy** (`import("mermaid")`, chunk séparé, tiré seulement si un schéma est présent), thème suivi du `colorScheme`, `securityLevel:"strict"`, fallback Alert+code brut si syntaxe invalide. Les 7 docs core ont été converties ASCII→mermaid (flowchart). Edge labels = forme `|"..."|` (parenthèses sûres).
  - **Onglet Coverage** (`CoveragePanel`) : 4 `RingProgress` (lines/statements/functions/branches) + table par fichier (`Progress` bars, seuils covColor ≥80 teal/≥50 jaune/<50 rouge) via `/nodefony/kernel/api/module/{key}/coverage` (`readCoverage` : `coverage-summary.json` vitest OU `lcov.info` monocart/core). Studio AFFICHE le dernier rapport (généré par `npm run coverage`), ne lance pas. Masqué si `available:false`. Coverage = tests **unit** (intégration = process serveur séparé). Marche framework/http (vitest) + core (lcov monocart 84.82%).
  - **Onglet Tests** (`TestsPanel`) : liste les fichiers de test unit (`GET /module/{key}/tests`) + bouton **Run** par fichier (`POST /module/{key}/test/run {file}` → `npx vitest run <file>`) + **Lancer tous** (→ `npm run coverage`, refresh coverage). Résultat = `ResultBadge` (✓/✗ passed/failed + durée), sortie sur échec (Code block). `ApiClient.postAbsolute`. ⚠️ **Backend gardé DEV-ONLY** (403 hors `development`) car ça EXÉCUTE un process ; `devMode` exposé par l'endpoint → si false, Alert "désactivé". Masqué si 0 fichier.
  - **Onglets vides masqués** : Routes/Config/Services/Deps/Docs/API/Coverage ne s'affichent QUE s'ils ont du contenu → le pseudo-module `core` (pas de routes/config/services) montre seulement Vue d'ensemble · Docs · API · Dépendances. `defaultValue` = Docs si présent, sinon Vue d'ensemble.
  - **Onglet API** (`ApiPanel`) : table kind/nom/description, 100 % auto via `/nodefony/kernel/api/module/{key}/symbols` (lit `.ai/symbols.json`, filtre `module === packageName` + `exported`). Tab maigre = couverture TSDoc faible (volontaire, pousse à documenter).
- **Logs** : page = SegmentedControl **[Live | Fichiers]** (shell `Logs`, observers `LiveLogs`+`LogFiles`). **Live** (`LiveLogs`) : amorçage **snapshot** réel `/nodefony/syslog/api/logs?limit=200` (ring buffer) PUIS stream live WS `syslog:stream`. Backend `SyslogAdminApi` sérialise avec les noms de champs Pdu (`moduleName`, pas `module`) → hydratation `Object.assign(new Pdu(), data)` identique REST+WS. **Fichiers** (`LogFiles.tsx`, DEV — remplace `tail -f`) : `getAbsolute("/nodefony/syslog/api/files")` (liste, prod→`enabled:false`) + `…/files/{name}?from=&raw=` ; **Follow** = polling offset (`POLL_MS=1500`, `from=to`), gère `reset` (rotation) ; toggle **secrets masqués/brut** (`raw=1`) ; `parsePdu` rend JSON en Pdu coloré sinon `ansiToReact` (le `nodefony-core.log` est en texte ANSI, pas JSON).
- **Section Sécurité (P6.15)** : `routes/Audit.tsx` (+ `routes/audit/*` — journal d'audit, live `security:audit` PRÊT/OFF) ; **`routes/Firewall.tsx` (+ `routes/firewall/*`) = page Firewall FULL-STACK** consommant `GET /nodefony/security/api/{firewall,roleHierarchy}` (introspection runtime, **types miroir** locaux, jamais d'import `@nodefony/security`). 5 onglets : Zones (`DataGrid`+Modal détail) · Authenticators (registre∪montés) · Défenses (CSRF/CORS/en-têtes/throttle) · Rôles (hiérarchie transitive) · Statistiques (3 counts `audit/events?category=auth&outcome=…` = **boucle audit→firewall**). `wip` retiré (navConfig), stub supprimé. ⚠️ `KeyValue` rend `v` dans `<Text>`(=`<p>`) → valeur RICHE (Badge/div) = `Field` local (`Group`+`Box`), pas `KeyValue`. Erreurs data plane mappées FR DANS le fetcher (`useResource` ne renvoie que `e.message` → `throw new Error(describeFirewallError(e))`).

## Config

- `nodefony/config/config.ts` : `{ "module-frontend": { https: true } }` (Vite HTTPS certs Nodefony, anti mixed-content sur 5152).
- `package.json` : `main` OK ; ⚠️ **pas de `types` ni `exports`** (TODO). peerDeps : nodefony, @nodefony/{http,framework,frontend}, vite, @vitejs/plugin-react. deps frontend dans CE package.json (pas de frontend/package.json). Onglet Docs : **`react-markdown`+`remark-gfm`** (compat React 19 OK, ≠ recharts cf [[feedback_recharts_react19]]) + **`mermaid`** (gros, mais **lazy** `import()` → chunk à part, 0 impact bundle initial).
- **Docs colocalisées** : `studio/docs/index.md` (+ http/framework/frontend idem). Frontmatter `title/module/since/updated/status/order` lu par le backend (`docsReader`). `frontend/docs/index.md` = ex-`docs/packages/frontend.md` (git mv, ADR-0001).

## Tests

- **Harness vitest** (scaffold 2026-05-21, miroir `@nodefony/frontend`) : `vitest.config.ts` + `nodefony/tests/{vitest.setup.ts,vitest-mocha-shim.mjs}`. Scripts `test`=`vitest run`, `coverage`=`vitest run --coverage`. devDeps `vitest`+`@vitest/coverage-v8` 4.1.7, `@types/chai`. `expect` importé de **chai** (≠ vitest expect), `vi` de vitest (fake timers + mocks).
- **`nodefony/tests/unit/providers.test.ts`** (11 tests, 98.55% stmts / 100% lines providers.ts) : verrouille le **coalescing** `createSyslogBridge` (lazy, 1 frame `{logs,dropped}`/flushMs, ring borné `maxBatch` écrase + ancien + compte `dropped`, dispose détache listener+timer = 0 fuite) + `createStatsTicker` (heartbeat `dashboard:stats`/intervalMs, forme payload, dispose stoppe). Fake timers `vi.useFakeTimers()` → déterministe sans serveur.
- **Split** (cf vitest.config) : `StudioRealtimeController` (WS endpoint) = **intégration live-server** (subscribe/unsubscribe → frame JSON-RPC ; couvert par la suite WS de @nodefony/http, à ajouter). Frontend React (stores MobX, ConnectionDrawer) = instrumentation séparée (non scaffoldée).
- Coverage : `npm run coverage` → `.coverage/` (gitignored), affiché par l'onglet Coverage Studio.

## Behaviors

- Boot : studio chargé APRÈS @nodefony/frontend (sinon service Vite absent au boot). Coexiste avec test-frontend-react (multibundle résolu).
- `renderStudio()` override header CSP via `frontendService.getCspDirectives()` (hack POC cross-origin).
- SSE logs : SUPPRIMÉ 2026-05-23 (back `apiLogsStream` + front `subscribeSSE`, tous deux morts ; cassé HTTP/2 : `flushHeaders` absent sur `Http2ServerResponse`). Leçon conservée mémoire `feedback_sse_http2_request_close` : tout futur SSE écoute `rawRes.once("close")` sur la RESPONSE (pas request — HTTP/2 fire trop tôt).
- WS realtime : push serveur→client sur `ctx.connection.send()` (raw ws, garde `readyState===1`), PAS `ctx.send()` (rejette après handshake car `requestEnded=true`). Cleanup par canal au `unsubscribe` + global `ctx.once("onFinish")`. Validé : 0 push avant subscribe, stop net après unsubscribe, 0 fuite.
- **WS actions (req→rép, 2026-05-23)** : frame avec `id` = requête RPC → `dispatchRequest()` : `kernel:ping` (liveness+RTT) / `kernel:gc` (force GC si `--expose-gc`, sinon `{available:false}`) ; inconnue `-32601` ; throw `-32603` (msg générique, détail loggé serveur). Chemin chaud subscribe reste sync. Client = `RealtimeClient.request()` (déjà là) + helper réutilisable `client.ping()` (RTT) **dans la lib core**, pas dupliqué front. Test unit core `RealtimeClientPing.test.ts` (3). Serveur = vérif live (intégration WS). Migrera dans `RealtimeService` P13.4.
- Front WS : `RootStore` → `RealtimeClient({url: wss://host/nodefony/studio/api/realtime})`. **`AdminLayout` ouvre le WS au montage** (pas seulement Login — sinon reload avec token saute Login → WS jamais ouvert, widgets loading + "disconnected"). `ConnectionStore.subscribe/unsubscribe` émettent au serveur ; re-subscribe des canaux actifs sur `__state__ connected` (reconnect + course 1er connect). `connect()` ne fait plus `disconnect()` au timeout (garde l'autoReconnect).
- **Centre de notifications** : `NotificationStore` (MobX, `useNotifications()`) branché sur `realtime.onNotice` au constructeur → toast Mantine (`<Notifications/>` déjà dans `App.tsx`) + ring `recent`. Source NORMALISÉE Core = `NodefonyNotice` (close codes RFC 6455 → `closeCodeToNotice`, erreur serveur poussée, reconnexion). `ApiClient.onError` toaste les **mutations** échouées (pas GET = `<DataState>`, pas 401 = logout). Erreurs `autoClose:false`. Hub `RealtimeHubContent` : bloc « incidents temps réel » = `notifications.realtimeIncidents`. Dette : `NotificationStore` non testé (harness React Studio non scaffoldé).
- **Theme : palette de marque togglable** (`theme.ts` `buildStudioTheme(palette)`). Couleur `brand` = **alias dynamique** (= `nodefonyBlue` `#0067ba` en palette `nodefony`, `nodefonyOrange` en `orange`) ; `primaryColor:"brand"` → tous les accents en dur réécrits `color="brand"` suivent le toggle d'un coup. **Défaut = nodefony** (`ui.palette`, persisté ; toggle 🎨 header `IconPalette`). Couleurs marque extraites du logo (blue `#0067ba` / green `#448438` / cyan `#00a0f2`, échelles 10-shades générées). Dark-safe : `primaryShade.dark=4` (bleu base trop sombre sinon). Restent volontairement `color="orange"` : warnings (Alert erreur, schéma invalide, outdated), badges DEBUG, palettes décoratives distinctes (`KIND_COLORS`, StatCards — collision si mappées). Dark défaut + toggle scheme localStorage.

## Gotchas

- ✅ **Routing TRANCHÉ 2026-05-20** : UI = `/nodefony` + `/nodefony/{page}` (mono-segment, porté par Studio, dispo si module chargé) ; data plane = `/nodefony/<module>/api/*` (≥3 seg, porté par chaque module, indép. de Studio). `/studio` REJETÉ (collision app user — `/nodefony` est réservé framework). Règle : jamais de route admin mono-segment `/nodefony/<module>` (collision page SPA). Validé runtime curl + proxy Vite.
- ⚠️ **Fallback SPA deep-link = préfixe LITTÉRAL** (`/modules/{name}`), JAMAIS générique `/{section}/{page}`. Régression 2026-05-20 : le générique matchait `/nodefony/test/index` (le module test monte sous `/nodefony/test/*`) → masquait TOUT le module test → 21 échecs http. Toute nouvelle page SPA ≥2 seg → ajouter son fallback littéral. Cf [[feedback_spa_fallback_literal]].
- `apiProxyPaths` = **clé RegExp Vite `["^/nodefony/[^/]+/api"]`** (depuis 2026-05-20) : proxifie TOUT le data plane `/nodefony/<module>/api/*` (studio + kernel + http + framework + syslog + futurs) vers le backend. `^…` → Vite traite la clé comme RegExp ; ≥3 segments avec `/api/` → les pages SPA mono-segment `/nodefony/{page}` et la racine `/nodefony` restent servies par Vite. `ws:true` garde le WS realtime proxifié. (Avant : `["/nodefony/studio/api"]` prefix-match, ne couvrait que studio.) Sans proxy, `fetch` depuis la page servie par Vite tombe sur le SPA-fallback → erreur JSON.
- **Mono-supervisor multi-bundle** : la `vite.config.generated.mjs` ACTIVE est celle du 1er module frontend chargé (ici `src/modules/test-frontend-react/frontend/`), PAS celle de studio (leftover possible). Le proxy y agrège les `apiProxyPaths` de tous les bundles (studio + poc). Vérifier la fraîcheur via mtime.
- **Auth = RÉELLE** (firewall `@nodefony/security` P6 branché : session BFF cookie, RBAC `ROLE_NODEFONY_ADMIN`). Mocks `/auth/*` SUPPRIMÉS — la sécurité EST en place (zone `nodefony-admin` au handshake + `isAdminGranted` fail-closed sur le data plane).
- `IAdminApi`/`AdminBroker` **LIVRÉS** : chaque module s'auto-expose via le broker (`/nodefony/<module>/api/*`). Restent en dur dans `StudioController` : `health`/`info` (bypass, pré-login) + `stats` (gardé `ROLE_SUPERVISOR`) ; `DocumentationController` gardé `[ROLE_DEV,ROLE_SUPERVISOR]`.
- ✅ CSP hack POC **RETIRÉ** : la CSP est posée par `@nodefony/security` (`securityHeaders.ts`, nonce par requête) ; `getCspDirectives` n'existe plus.
- Realtime forward-compat P13.4 : providers transport-agnostiques (`publish(channel,payload)`), canaux figés. Migration = supprimer le controller + brancher `realtimeService.publish`, front inchangé. Cf [[project_studio_realtime_ws]].
- Dashboard stats = **per-instance** (process qui tient le WS), PAS cluster-aware. CPU% = % d'UN cœur (pas /cores). En multi-process (reusePort) le WS tombe sur 1 worker → 1 instance affichée. Vue cluster = ajouter `instanceId` au payload + Redis pub/sub fan-out (P13). Cloud-native OK (chaque pod se rapporte). Cf [[project_multiprocess_scaling]].
- ⚠️ **Jauge Heap V8 = `heapUsed / heap_size_limit`, PAS `/heapTotal`**. `heapUsed/heapTotal` vaut ~99% en permanence (V8 garde `heapTotal` collé au-dessus de `heapUsed`) → trompeur, faux signal d'alerte. `heapLimit` (`v8.getHeapStatistics().heap_size_limit`, **constant** → lu 1× hors tick dans `providers.ts`) ajouté au payload `dashboard:stats.memory`. Dashboard : jauge contre le plafond (~12% normal), ligne « Remplissage » montre l'ancien ratio à titre info. La fuite se lit sur la **courbe heapUsed** absolue, pas sur le %.
- Build : warning `rollup-sourcemap-path-transform` types — bénin.
- 📋 **Backlog UX page Logs** (idées 2026-05-20) dans `CLAUDE.md` → section "TODO connus". Combo reco : autoscroll intelligent + lignes ERROR surlignées + compteurs sévérité cliquables + détail Pdu au clic.

## Routes API admin (convention `/nodefony/<module>/api/*`)

Studio est le consommateur. Chaque module expose son `IAdminApi` (P10.2 ✅ contrat figé — cf framework MEMORY.md). **Kernel migré ✅ 2026-05-20** : `GET /nodefony/kernel/api/{health,info,modules}` réels via `AdminBroker` (validé runtime). Les mocks `/nodefony/studio/api/{health,info}` du StudioController **restent** (front non migré) → TODO : pointer le front vers `/nodefony/kernel/api/*`. Reste producteurs : http/security/etc.

- **Docs+API modules ✅ 2026-05-20** (kit `project_studio_docs_integration_kit`) : producteur kernel enrichi → `module/{name}/docs`, `module/{name}/docs/{slug}`, `module/{name}/symbols` (helper `framework/nodefony/src/docsReader.ts`). Onglets Docs+API dans `ModuleDetail`. Docs colocalisées (`<module>/docs/*.md`, hybride ADR-0001) sur core/http/framework/frontend/studio. Validé runtime curl.
- **Carte Core ✅ 2026-05-20** : le core (`@nodefony/core`, package `nodefony`) n'est PAS dans `getModules()` → injecté comme **pseudo-module** `key:"core"` en tête de `/kernel/api/modules` (`readCoreInfo`/`resolveCorePath` dans `docsReader.ts`). Front INCHANGÉ (Modules.tsx itère la liste, ModuleDetail appelle `module/core/{docs,symbols}`). Docs `src/nodefony/docs/*.md` (ex-`docs/architecture/*`, git mv), symbols filtre `@nodefony/core` (131). `resolveTarget(key)` dans `KernelAdminApi` gère core vs module chargé. Détail core : services/config vides, deps depuis package.json.

## Debug bar (Core isomorphe `nodefony/debugbar`)

Studio = 2ᵉ consommateur du backbone realtime + **pilote** la debug bar.

- **`dashboard:stats` enrichi** : `createStatsTicker(publish, 1000, meta)` ajoute un bloc `app` = `{name,version,env,debug,branch}`. `branch` via `readGitBranch()` (lit `.git/HEAD`, **pas de spawn git**, caché 1×) ; env/version/debug depuis le kernel (`StudioRealtimeController.appMeta()`). → la barre affiche env (badge couleur) + branche git sur toute page.
- **Interrupteur Studio** : `UiStore.debugBar` + `toggleDebugBar()`, clé localStorage **partagée** `nf.debugbar.visible` (synchro avec le widget). Pilote la barre auto-injectée via `window.__NODEFONY_DEBUGBAR__.setVisible()`. Bouton topbar `AdminLayout` (icône bottombar).
- La barre s'auto-injecte aussi sur la page Studio (montage @nodefony/frontend) — redondant mais OK.

### Page Profiler + sync (2026-05-21)

- **Page** `routes/Profiler.tsx` + `ProfilerStore` (consomme data-plane `/nodefony/profiler/api/*` via `ApiClient.getAbsolute`/`deleteAbsolute`). Route `/nodefony/profiling` (ex-stub — câblée dans `App.tsx`, lazy). Liste recent + waterfall, **réutilise `computeWaterfall`** importé de `nodefony/debugbar` (core isomorphe, 0 dup). Auto-refresh poll 3s opt-in.
- ⚠️ **`ProfilerStore.ts` n'importe PAS `nodefony/debugbar`** : le plugin `@analogjs/vite-plugin-angular` (Vite multi-framework partagé) trébuche sur ce subpath dans un `.ts` (« contains Angular decorators »). → types **miroir locaux** dans le store. Les `.tsx` (Profiler.tsx, pris par le plugin React) peuvent importer du subpath.
- **Sync barre→Studio** : la debug bar dispatch `window` CustomEvent `nodefony:debugbar:select {requestId}` au clic ; `AdminLayout` écoute → `navigate("/nodefony/profiling?req="+id)` ; Profiler lit `?req=` (useSearchParams) → `store.select` (deep-linkable, robuste au timing).
- ⚠️ Pré-requis corrélation : le serveur doit poser `x-request-id` sur **HTTP/2** (cf http MEMORY — fix 2026-05-21). Studio = HTTP/2 (5152).

## Liens

`project_studio_prep_kit` (reprise) · `project_studio_module` · `project_ia_studio_final` · `project_realtime_vision_studio_beta` · `project_frontend_architecture_decision` · `project_csp_vite_security_todo` · `feedback_sse_http2_request_close` · `project_studio_debugbar`
