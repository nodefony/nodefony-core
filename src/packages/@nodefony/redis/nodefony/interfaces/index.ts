/**
 * Barrel des interfaces publiques de `@nodefony/redis`.
 * Réexporté par `index.ts` racine via `export type { ... }`.
 */
export type {
  IRedisConfig,
  IRedisConfigInput,
  IRedisConnectionConfig,
} from "./IRedisConfig";
