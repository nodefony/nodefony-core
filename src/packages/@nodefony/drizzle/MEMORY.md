# MEMORY.md — @nodefony/drizzle

Purpose: 3e adapter orm-core + module bootable. Drizzle + better-sqlite3. Type-safe-first.

## Entités D'APPLICATION — `nodefony create entity`, pas le colKit

- **La voie utilisateur = la commande** : `nodefony create entity Post title:string! views:int` pose table
  Drizzle **native du dialecte** + `XRow` + schémas Zod + service CRUD + controller REST/WS + tests, et câble
  `@entities([...])` (orm-core). Champs `nom:type[?|!][:index]` · `ref:<Entité>` · **non-null par défaut**.
  Traduction type→colonne : `nodefony/src/cli/scaffold/entityFields.ts` (core, module PUR, 3 dialectes).
- **`ref:` ⇒ colonne INDEXÉE d'office** (sauf `!`, déjà indexé) : c'est la colonne de jointure
  (`?include=` = `IN (…)`). L'index ≠ la FK — un `JOIN` n'exige aucune contrainte. **FK jamais émises**
  par le DDL dev (elles vivent dans le `CREATE TABLE`, donc hors d'atteinte d'une base existante).
- **Noms d'entités du framework REFUSÉS** par `create entity` (`User`, `session`, `access_token`,
  `audit_event`… — casse/séparateurs ignorés ; table `scaffold/reservedEntities.ts`, tenue honnête par
  un gate `NF_RUN_CLI_BOOT=1` contre `nodefony inspect entities`). Registre plat : un homonyme dépossède
  l'entité du module, l'app ne démarre plus sur un message de « colonne inconnue ».
- **`drizzle-orm` = dep de l'APP** (le code généré l'importe en direct) : gabarit `complete` + rattrapage
  par `create entity`. Sans elle, seul le hissage npm sauvait la résolution — absent en `--link`.
- 🔴 **Le `colKit` reste INTERNE** (jamais dans `index.ts`) : il sert aux **entités FRAMEWORK** (portées sur
  3 dialectes, table fabriquée à partir du dialecte lu au runtime). Une entité d'app écrit du **Drizzle natif**
  — décision d'audit : exposer le kit serait une sur-promesse d'API. Le scaffold écrit le code que
  l'utilisateur aurait écrit.
- Entités framework (User/Session/Token/Audit/Idempotency…) : `registerX(orm, dialect)` impératif via `wire()`
  (filtre les dialectes portés) — **pas** `@entities`, qui suppose un schéma statique.
- DDL dev = `CREATE TABLE IF NOT EXISTS` dérivé → **modifier une entité n'altère RIEN** (aucun `ALTER`).
  En sortent : les contraintes colonne (PK/NOT NULL/UNIQUE), les **index** (`CREATE INDEX` SÉPARÉ, donc
  il atteint aussi une base déjà créée) et les **`CHECK` des colonnes `enum`** (clause DE TABLE, donc
  seulement à la création — une base de dev antérieure ne l'a pas). N'en sortent PAS : les `DEFAULT` SQL
  (défauts **toujours** `$defaultFn`) ni les clés étrangères — drizzle-kit seul les émet.
- Kinds colKit : `text` · `json` · `bool` · `epochMs` · `int` (32 bits SIGNÉ sur les 3 dialectes — réservé
  aux valeurs bornées par le domaine) · **`int64`** (bigint pg/mysql — tout compteur sans borne connue) ·
  `dateMs` · **`enum`** (`values: [...]`, texte + `CHECK`). Un `CHECK` se grave à vie dans la migration
  `0000` : ne le poser que sur un ensemble **fermé par construction** (états d'automate), jamais sur un
  catalogue extensible (algorithmes, catégories) — dont l'union reste sur le type `Row`.

## Module bootable

- `index.ts` default export = `Drizzle extends Module` + `@services([DrizzleService])`. Ajouté à `@modules()` app. ORM SQL par défaut.
- `nodefony/service/DrizzleService.ts` : ctor `super(name, module.container, module.notificationsCenter, module.options)` ; `kernel.once("onBoot")` → `connectAll()` lit la config VALIDÉE `this.module.get("drizzleConfig")` (1 DrizzleOrm/connecteur) ; `#defaultFilename(name)` résout le chemin SQLite **AU BOOT** (kernel présent) si `filename` omis : `<root>/var/databases/nodefony-<x>.db` (sous `kernel.varDir` ; default → `nodefony-drizzle.db`) ; `onTerminate` → disconnectAll. `getOrm(name="default")`.
- **Config Zod (alignement famille ORM)** : `nodefony/config/config.ts` = source de vérité (`filename` **optionnel SANS défaut** — schéma pur, pas de deref kernel) → `defineDrizzleConfig` (parse + infra database `NF_DATABASE_URL`/`DATABASE_URL` : dialecte déduit du scheme, `sqlite:`→`filename`, `postgres://`/`mysql://`→`url`, `mongodb://` ignorée → freeze) → validée au `onKernelRegister`, exposée `this.set("drizzleConfig")`. Augmente `NodefonyModuleConfig` → `use("@nodefony/drizzle", …)` typé. Même pattern que `@nodefony/mongoose`. Cf audit mémoire IA `core-dev/audits/orm-config-pattern-2026-06.md`.
- `DrizzleService.#connectOne` propage `dialect`/`url` à `DrizzleOrm` ; `#defaultFilename`+`mkdirSync` UNIQUEMENT en sqlite ; log de boot = URL rédigée (`redactUrl`, jamais de password). **Échec de connexion d'un connecteur configuré → `BootConfigurationError`** (core) = boot FATAL dev ET prod (une infra déclarée non honorable ne se répare pas en continuant — jamais de serveur « vivant » aux stores morts). `#connectPostgres`/`#connectMysql` font un **ping réel `SELECT 1`** au connect (pool pg/mysql2 LAZY : sans ping, une base morte « connecterait » et n'échouerait qu'à la 1ʳᵉ requête métier) + `pool.end()` si le ping échoue (0 handle fuité).
- better-sqlite3 = **dependencies** (runtime), pas devDeps.
- Boot vérifié : `MODULE ADD : drizzle` + `Drizzle ORM "default" connected` + db créée, 4 serveurs UP, health 200.

## Adapter User (ORM par défaut)

- `entity/userTable.ts` : spec colKit (JSON/bool/dateMs) + `createUserTable(dialect)` mémoïsée + export `userTable` (variante sqlite), `createUserEntity(orm, dialect)`/`registerUserEntity(orm, dialect)` (binding ORM dynamique, **avant** connect — auto-register par le wire), `DrizzleUserRepository implements IUserRepository` (`from(orm)` lit `orm.dialect`).
- Mappe ligne ↔ `BaseUser` (comportement). `findByIdentifier` + `findBySocialProvider` **routé queryKit** (json_each sqlite / `@>` jsonb pg, bindé, Shadow User). `listPage(IUserListQuery)`/`countActiveAdmins` = **routés queryKit** (SELECT ids paginé + COUNT natif) puis recharge typée `find({id:$in})` ré-ordonnée (parsing JSON/booléens cohérent). peerDep `@nodefony/user` ajoutée + externalisée rolldown (sinon bundle → casse sur @node-rs/bcrypt natif).
- ⚠️ **Défauts via `$defaultFn` (JS), PAS `.default()` SQL** : le DDL dérivé n'émet pas les DEFAULT → NOT NULL casserait.
- ⚠️ Test cleanup : `entityRegistry.unregister("User", ORM)` **scopé** (sans orm = efface le bucket entier = contamine les autres bancs).

## Store idempotence Drizzle

- `nodefony/entity/idempotencyEntity.ts` : `idempotencyKeyTable` (sqlite : `key` PK, `fingerprint`, `state` `if|done`, `response` text-json nullable, `expiresAt` int + index) + **factory `createIdempotencyTable(dialect)`** (sqlite|postgres ; variante PG = `jsonb` + `bigint mode:number` car `integer` PG 32-bit déborde sur epoch ms ; MÊMES noms de colonnes) + `createIdempotencyEntities(orm, dialect)`/`registerIdempotencyEntities(orm, dialect)` (binding dynamique, **avant** connect) + `IdempotencyKeyRow` + `IDEMPOTENCY_ENTITY_NAME`. `module:"framework"` (ERD).
- `nodefony/src/DrizzleIdempotencyStore.ts implements IIdempotencyStore` (contrat CORE `nodefony`, `import type` → 0 cycle). **Dialect-agnostique** (référence `table.key`/`.expiresAt` via `eq`/`lt`) : `#table` injectable (5e arg, défaut sqlite) ; `.from(orm)` lit `orm.dialect` → injecte `createIdempotencyTable(orm.dialect)`.
- **begin = réservation ATOMIQUE = `SET NX PX` SQL** : `insert(...).onConflictDoUpdate({ target:key, set:{…if…}, setWhere: lt(expiresAt, now) }).returning({key})`. `returning.length>0` ⇒ **fresh** (INSERT clé neuve OU **vol** d'une entrée morte via le DO UPDATE WHERE expiré) ; `===0` ⇒ contention (clé vivante) → SELECT → in-flight/replayed/mismatch. **JAMAIS fresh hors réservation** = anti double-effet (l'invariant). Cf le `RETURNING` SQLite/PG ne rend une ligne QUE si insert/update a eu lieu.
- complete = UPDATE `set done,response,expiresAt=now+ttl` WHERE `key AND state='if'` (**fingerprint NON touché** = préservé → mismatch 422 post-complétion). abort = DELETE WHERE `key AND state='if'`. gc(now) = DELETE `expiresAt<=now` → count (**pas de TTL natif SQL** → GC applicatif, à mutualiser GC session). size = `#pending` local best-effort (≠ cross-pod).
- **Résolution LAZY** (calqué `RedisIdempotencyStore`) : ctor prend `() => DrizzleDb|null` ; `.from` = `() => orm.isConnected() ? orm.getNativeConnection<DrizzleDb>() : null`. Résout l'**ordre de boot** (framework résout le store à onKernelBoot, orm connecte à onBoot) + **shutdown** (gotcha SessionStorage). null → begin=fresh (sans dédup), complete/abort/gc=no-op.
- **AUTO-REGISTER** (`nodefony/registerStores.ts`, appelé par `Drizzle.onKernelRegister`) : entité (variante du dialecte du connecteur `default`) + fabrique (registre @nodefony/framework) déclarées par le module. Sélection = `NF_IDEMPOTENCY_STORE=drizzle` SEUL. Framework `onKernelBoot` résout → override service `idempotencyStore` (prod fatal / dev fallback mémoire). Guards : entité `has`-guarded + fabrique `get`-guarded (l'app garde la main) ; opt-out `frameworkEntities:false`.
- **Preuves 2 niveaux** : (1) **SQLite** `tests/integration/idempotency-store.test.ts` = sémantique séquentielle (verdicts, mismatch post-complétion, vol d'expiré in-flight+done, gc, size, fail-soft) — mono-fichier ne prouve PAS le multi-pod ; (2) **Postgres** `.claude/skills/nodefony-load-test/scripts/idempotency-postgres-e2e.mjs` = atomicité cross-pod RÉELLE (2 pools × 20 rounds × 10 begin concurrents → 1 fresh + 9 in-flight/round, 0 race ; replay/mismatch/in-flight cross-pod). `docker compose --profile postgres up -d postgres`.

## Journal d'audit (persistant)

- `nodefony/entity/auditEventEntity.ts` : **spec colKit** (`id` PK text, `ts` epochMs, `category`/`action`/`outcome` text — unions sur `AuditEventRow`, décision S1 —, contexte NULLABLE `actor`/`resource`/`reason`/`ip`/`userAgent`/`requestId`, `flags`/`metadata` json) + index `ts`/`category`/`actor`/`requestId` + `createAuditEventTable(dialect)` mémoïsée + `createAuditEntities(orm, dialect)`/`registerAuditEntities(orm, dialect)` (binding dynamique, **avant** connect) + `AuditEventRow` + `AUDIT_ENTITY_NAMES`. `module:"security"` (ERD).
- `nodefony/src/DrizzleAuditStore.ts implements IAuditStore` (contrat `@nodefony/security`, `import type` pour le contrat → 0 cycle). **Append-only** : `append`=INSERT (optionnels→NULL) ; `gc(now)`=DELETE `ts<now-retention`→count (pas de TTL SQL) ; **`listPage`=trappe native** (query builder dialect-agnostique) : ordre `(ts DESC, id DESC)`, curseur **composite AUTO-PORTANT `<ts>:<id>`** → `or(lt(ts,cTs), and(eq(ts,cTs), lt(id,cId)))` **sans SELECT de résolution** (un curseur-id exigeait 1 round-trip ET rembobinait à la page 1 si l'événement avait été purgé entre deux pages), `limit+1`=garde `hasNext`, `count()`=total **refusable** (`withTotal:false` → COUNT sauté). Filtres AND `#buildFilter` (category/outcome/actor/action/requestId/since/until). Multi-dialecte : table `DrizzleTable` (union) + colonnes via vue `#c` (Record par nom logique — les specs colKit partagent les NOMS) ; exécution via `execTable` ; `gc` normalise `changes ?? rowCount` (pg).
- **Résolution LAZY** (`() => DrizzleDb|null`, garde `isConnected()`) → `null`=fail-soft (append no-op best-effort — l'audit ne bloque jamais le flux —, `listPage` page vide, gc 0). `.from(orm, now?, retentionMs?)` (défaut rétention 365j).
- **AUTO-REGISTER** (`registerStores.ts`) : entité + fabrique (registre @nodefony/security) déclarées par le module. Sélection = `security.audit.store="drizzle"` SEUL (rétention lue de `config.audit.retentionDays`). Défaut `memory` (registry `auditStoreRegistry`).
- **Preuve** : banc de contrat PARTAGÉ `audit-pagination.test.ts` (× 3 dialectes réels — le même que le store mémoire de security ; pagination, ordre total, curseur auto-portant, filtres, withTotal) + `audit-store.test.ts` SQLite = le SPÉCIFIQUE drizzle (JSON flags/metadata, collision ms, filtres AND, gc rétention, dégradation db null) + `audit-store-postgres.e2e.test.ts` PG réel (round-trip jsonb/bigint, curseur composite, gc `rowCount`).

## Multi-dialecte (chantier portabilité — les 8 entités framework portées sqlite+pg+mysql)

- **Pourquoi** : Drizzle = schema-as-code dialect-spécifique (`sqliteTable`≠`pgTable`, colonnes typées par dialecte) → ≠ Sequelize, on NE peut PAS « juste changer le dialect ». On reconstruit l'abstraction au niveau Nodefony. Choix Drizzle CONFIRMÉ par comparatif froid (mémoire IA `core-dev/audits/orm-comparatif-froid-2026-07.md`) : garde-fous G1 confinement / G2 plan B Kysely / G3 rowid d'abord.
- **`connector.dialect`** (`sqlite|postgres|mysql`, défaut sqlite) + `url` (pg/mysql) dans `config/config.ts`. Type `SqlDialect` exporté. `DrizzleOrmOptions` += `dialect`/`url`. Chemin normal app = `NF_DATABASE_URL` (dialecte déduit du scheme).
- **`colKit` (`entity/colKit.ts`, INTERNE — pas dans index.ts)** : `IFrameworkTableSpec` (kinds `text|json|bool|epochMs|int|dateMs` + pk/notNull/unique/defaultFn/onUpdateFn + indexes) → `buildFrameworkTable(dialect, spec)` · `createFrameworkTableFactory(spec)` = `createXTable(dialect)` mémoïsée (1 instance/dialecte). Types par dialecte DANS le kit : epochMs=`integer` sqlite/`bigint mode:number` pg+mysql (exposé number) · dateMs=`integer mode:timestamp_ms`/`timestamptz(3) mode:date`/`datetime(3) mode:date` mysql (exposé Date — pool mysql2 en `timezone:"Z"`) · json=`text mode:json`/`jsonb`/`mysqlJsonCompat` (customType : parse si string — MariaDB rend LONGTEXT string, MySQL un objet) · bool=`integer mode:boolean`/`boolean` · text mysql PK/UNIQUE/indexé=`varchar(512)` (TEXT non indexable InnoDB ; 512×4=2048<3072 DYNAMIC ; couvre clé idempotence ~330). Index validés EAGER (fail-loud au build — le callback extraConfig drizzle est lazy). RÈGLE : toute entité à porter passe par le colKit. Défauts JS-level uniquement (`$defaultFn` — DDL dérivé n'émet pas DEFAULT).
- **`queryKit` (`src/queryKit.ts`, INTERNE — miroir du colKit pour le SQL natif)** : les requêtes brutes des entités framework, émises ET exécutées par dialecte (l'API native diverge : `db.all()` better-sqlite3 / `db.execute().rows` pg / tuple `[rows|header, fields]` mysql2). `findUserIdBySocialProvider(db, dialect, provider, providerId)` = json_each+json_extract sqlite / containment `@>` jsonb pg / `JSON_CONTAINS` mysql (motif = UN paramètre JSON.stringify bindé). `reserveIdempotencyKeyMysql(db, args)` = réservation idempotence mysql (2 instructions, cf §idempotence). `listUserIdsPage(db, dialect, filters, window)` + `countUsers(db, dialect, filters)` = pagination user native (enabled bool routé / role containment / `LOWER LIKE` ESCAPE, `ORDER BY` allowlist + tiebreaker id, `LIMIT+1`) → `countActiveAdmins` = `countUsers({enabled:true, role})`. Budget G1 : tout `sql\`` d'entité framework vit ICI.
- **Repository PK-portable** : `#pickOne(where)` = `pk IN (SELECT pk FROM (SELECT pk … LIMIT 1) AS picked)` — PK découverte lazy (`getTableColumns().filter(primary)` puis `getTableConfig().primaryKeys` composite, branche par dialecte), mémoïsée `#pk`. Table dérivée = MySQL (LIMIT-in-IN interdit + ERROR 1093) ; composite = row-values `(a,b) IN`. Fallback `rowid` UNIQUEMENT sans PK (sqlite-only). `#affected` normalise `changes` (better-sqlite3) / `rowCount` (pg) / tuple `[ResultSetHeader{affectedRows}]` (mysql2). Le repo porte `#dialect` (5ᵉ arg ctor, propagé `withTransaction`) → hack OFFSET-sans-LIMIT **routé** : sqlite=fragment `sql\`-1\``(⚠️ drizzle 0.45 IGNORE`limit(-1)`numérique), pg=rien (OFFSET seul valide,`LIMIT -1` rejeté), mysql=`limit(MAX_SAFE_INTEGER)` (2^64-1 non représentable en double).
- **Chemins mysql du repo (pas de RETURNING)** : verbes « rendant la ligne » = SELECT-cible → mutation bornée PK **+ critère re-vérifié dans le WHERE** (course perdue → 0 affected → null) → relecture par PK (`#mysqlUpdateOneReturning`/`#mysqlDeleteOneReturning`) ; `create`/`createMany` = `$returningId()` + PK des data + relecture ordonnée (`#mysqlInsertReturning` — PK OBLIGATOIRE, throw actionnable sinon) ; `upsert` = `onDuplicateKeyUpdate({set})` (pas de target — MySQL arbitre sur toutes les uniques) + relecture par valeurs finales du critère. 2-3 round-trips, mysql only.
- **Entités portées sqlite+postgres+mysql (specs colKit + factory par dialecte)** : `session` (S1) · `User`, `access_token`+`denied_jti`+`subject_revocation`, `webauthn_credential`, `totp_secret` (S2) · `audit_event`, `webhook_endpoint` (S3) · `idempotency_key` (Slice 0 ; spec colKit depuis S4 — plus de switch 2-tables). Chaque `registerXEntity/Entities(orm, dialect)` défaut sqlite ; exports statiques (`userTable`, `accessTokenTable`…) = variante sqlite mémoïsée (compat banc/mediasoup). **`@entity` et l'enregistrement app top-level SUPPRIMÉS** (l'app posait la variante sqlite figée → bug en pg) : l'entité `User` est auto-register par le wire comme les autres.
- **Wire `registerStores.ts`** : `X_PORTED = ALL_DIALECTS` (sqlite+postgres+mysql) pour les 8 briques ; `wire()` filtre AVANT d'appeler la closure → le dialecte passe tel quel aux register. Entité `User` wired (fabrique no-op : la résolution user = `provisionUsers`/`NF_USER_STORE`, backend déclaré par `registerUserStore`). La fabrique d'un store CAPTURE le dialecte de son enregistrement → `resolveConnectedOrm` refuse un ORM d'un autre dialecte (défense en profondeur ; 1 dialecte par boot en réel).
- **Index des entités framework — ils suivent les FILTRES réels, pas les colonnes « qui ressemblent à une FK »** : `access_token`(subjectId, family) · `webauthn_credential`(userId) · `audit_event`(ts, category, actor, requestId) · `idempotency_key`(expiresAt) · `session`(**user** — filtré par `listSessions`/`countSessions` du data plane et par la révocation ; le TSDoc du storage le promettait « indexable » avant que le schéma ne le tienne). `webhook_endpoint` n'indexe PAS `tenantId`/`createdBy` : personne ne filtre dessus (`listPage` filtre `enabled`/`event`/`q`). Preuve au CATALOGUE (`sqlite_master`), pas à la spec : `tests/integration/ddl-indexes.test.ts`.
- **Session : les DEUX bancs partagés de `http` tournent sur les 3 dialectes** — comportemental (`runSessionStoreContract`) + pagination (`runSessionPaginationContract`), greffés dans `session-store-contract.ts`. L'enveloppe locale est `runDrizzleSessionStoreContract` : elle portait le MÊME nom que le banc partagé → l'import l'a fait s'appeler elle-même (`Maximum call stack`). Ce qui reste propre au SQL et n'a rien à faire dans un banc partagé : bornes NIST idle+absolue, upsert concurrent ×10, valeurs hostiles, `listAll`/`open`.
- **Gates d'infra ANNONCÉES** : `vitest.config.ts` monte `gateReporter([PG_GATE, MYSQL_GATE])` (source unique `vitest.gates.ts` à la racine). Sans les variables, la suite reste verte mais la fin de run nomme les dialectes NON exercés + leur commande docker + le compte non exécuté. ⚠️ Ne jamais conclure « drizzle est vert » sans lire ce bloc : par défaut seul sqlite est prouvé.
- **Preuves PG (e2e gatés `NF_PG_URL`, docker profile postgres)** : session 7 · token 8 (gc 3 branches dont filtre JS IS NULL, revoke idempotent, denylist fenêtre, seuil monotone) · webauthn 5 (upsert save, patch §6.1.1 borné) · totp 5 (patch PARTIEL anti-rejeu préservé) · user 7 (defaults UUID/Date, UNIQUE identifier — erreur PG dans `err.cause`, drizzle wrappe —, `@>` exact/injection-bindée, Shadow User rechargé typé) · audit 4 (curseur composite collision ms, gc `rowCount`) · webhook 5 (round-trips, patch partiel, **OFFSET-sans-LIMIT routé**) + auto-register-postgres 3 (report 8 registered / 0 unported, fabrique refuse dialecte étranger).
- **Pagination — TOUS les stores paginés de drizzle passent le banc de contrat de leur propriétaire sur les 3 dialectes** (`*-pagination.test.ts` : audit/token/user/webauthn/idempotence en fichier dédié ; session/totp/webhook via leur `*-store-contract.ts`, déroulé par `*-{sqlite,postgres,mysql}*.test.ts`). ⚠️ Le banc d'idempotence n'exerçait PAS le filtre d'échéance (`expiresAt > now` retirable sans faire rougir la suite) → capacité `expireSeeded` ajoutée au harness CORE : le cas `🔒 une clé ÉCHUE ne figure plus au listing` est **skippé et annoncé** si le backend ne sait pas vieillir son horloge, jamais silencieusement absent.
- **Preuves mysql (e2e gatés `NF_MYSQL_URL` — passés sur MySQL 8.4 ET MariaDB 11.4)** : contrat repo 14 · audit 4 (curseur composite, gc `affectedRows`) · user 6 (UUID $returningId, UNIQUE ER_DUP_ENTRY via `err.cause`, `JSON_CONTAINS` exact + injection bindée, datetime(3)↔Date onUpdateFn) · idempotence 6 (verdicts, empreinte préservée, vol d'expiré, **concurrence 2 pods × 10 rounds = 1 seul fresh**, gc) + auto-register-mysql 3. Docker : `--profile mariadb` (3306, quotidien libre) / `--profile mysql` (3307, preuve compat).
- **Banc de PARITÉ DE CONTRAT `IRepository`** (`tests/integration/repository-contract.ts` + 3 consommateurs `repository-contract-{sqlite,postgres,mysql}`) : LA même suite (create defaults, createMany ordre, round-trip types unicode/json/bool/epochMs, opérateurs riches, order/limit/offset + OFFSET-sans-LIMIT, updateOne B1 + au-plus-une, updateMany compteur, increment, upsert insertOnly préservé, deleteOne au-plus-une, count/exists, UnknownCriteriaField, order strict → `InvalidOrderOption`) sur les 3 dialectes — un écart = bug framework. C'est lui qui a attrapé le bug `limit(-1)` ignoré par drizzle.
- **Typage cross-dialecte (modèle S3)** : frontières honnêtes = `DrizzleTable = SQLiteTable | PgTable` (`DrizzleOrm.#tables`, ctor repo, `DrizzleResolvedRelation.targetTable`) + `DrizzleColumn = Column` (base, acceptée par eq/lt/asc/sql) ; conversion vers les builders = **`execTable()` SEUL point** (vue d'exécution canonique typée sqlite — surface builder structurellement identique pg, prouvée e2e ; `DrizzleDb` = même principe pour le handle). Downcast localisé restant : `upsert.target` (IndexColumn[]). PAS de générique par dialecte (mur de typage `_` drizzle = scénario G2).
- **Idempotence mysql (`reserveIdempotencyKeyMysql`, queryKit)** : ODKU sans `WHERE`/`RETURNING` ET `affectedRows` ambigu (mysql2 = flag `CLIENT_FOUND_ROWS` : matched-inchangée=1, indistinguable d'un INSERT — prouvé serveur) → 2 instructions atomiques au verdict net : `INSERT IGNORE` (dup→0 toujours) puis vol `UPDATE … WHERE key AND expiresAt<now` (change toujours expiresAt → 1⇔vol ; voleurs sérialisés par lock InnoDB). Le store détecte mysql par `is(table, MySqlTable)` ; colonnes via vue `#c` (pattern audit).
- **Garde `order` posée dans `#runSelect` SEUL** — point de passage unique des options (`find` y mène, `findOne` passe par `find`), donc en amont des TROIS constructions d'`ORDER BY` (chemin direct, chemin préparé, forme mémoïsée `#selectShape`). Ne pas la recopier sur un site : elle vit dans `assertOrderOption()` d'orm-core.
- **Migrations du framework** : `migrations/<dialecte>/` (versionné ET publié — les `.sql` + `meta/_journal.json` seulement, PAS les instantanés, qui grossissent à chaque migration). Générées par `npm run generate:migrations -- --name <nom>` (`scripts/generate-migrations.mjs`), depuis `nodefony/migrations-schema/{sqlite,postgres,mysql}.ts` + `drizzle-kit/*.config.ts`. Les 3 dialectes ENSEMBLE, même nom : un tag publié sur npm est immuable, trois journaux désalignés ne se renumérotent pas.
- 🔴 **`drizzle-kit` REND 0 QUAND IL ÉCHOUE** (constaté 2×: dossier `out` ABSOLU — il le préfixe par `./` →`.//Users/…` —, et question restée sans terminal). Tout appel passe donc par `runGenerate` (`scripts/drizzleKit.mjs`), qui exige une PREUVE POSITIVE (`No schema changes` ou `[✓]`) ; la génération vérifie en plus que le journal a gagné son tag. Sans cette garde, un contrôle qui conclut de « code 0 + aucun fichier » déclare ALIGNÉ un schéma qui a dérivé.
- 🔴 **`drizzle-kit` ne collecte que les tables exportées À PLAT** (mesuré : plate + nichée → « 1 tables »). Regrouper les 10 constantes d'un schéma matérialisé dans un objet ⇒ migration VIDE, verte.
- **Renommage = danger n°1** : le diff ne voit pas une intention. L'outil pose la question et échoue sans terminal ; le message le dit et renvoie au mode interactif. Bug amont connu : renommage + changement de type ⇒ seul le renommage est écrit (drizzle-orm#3826) — c'est le drift-check qui rattrape.
- **Trois refus à la génération** (`auditMigrationSql`) : destructeur (`DROP TABLE`/`DROP COLUMN`/changement de type/`TRUNCATE`) ⇒ REFUS sauf `--allow-destructive`, fichiers conservés pour relecture ; verrouillant (index PG non concurrent, `SET NOT NULL`) ⇒ avertissement seul ; commentaires ignorés (sinon refus fantômes).
- **`check:migrations`** (CI + local) rejoue la génération depuis les instantanés et refuse qu'un fichier apparaisse — juste quel que soit le nombre de migrations, contrairement à « régénérer 0000 et comparer ».
- **Banc de parité migré ≡ dérivé**, 3 dialectes (`tests/integration/migrations-parity*.ts`) : colonnes/types/nullabilité/PK/index. Les index se comparent par COLONNES COUVERTES, jamais par nom (une contrainte `UNIQUE` = index nommé côté migration, auto-index côté dérivé). PG : deux SCHÉMAS dédiés ; MySQL : les deux phases se succèdent dans la base partagée — l'utilisateur applicatif ne peut pas créer de base (`ERROR 1044`), d'où `fileParallelism: false`.
- **Le DDL dérivé CRÉE les index** (`#createIndexSQL`) — contrairement à ce que disaient 4 en-têtes d'entités, corrigés. Ce qu'il n'émet pas : les `DEFAULT` SQL.
- **Reste** : les commandes `nodefony orm:migrate*` (l'applicateur, lui, est livré — ci-dessous).

## Migrations — l'applicateur (`nodefony/src/migrator/`)

`DrizzleMigrator` : `status()` (lecture seule, sans verrou ni écriture — une sonde qui écrit n'est
plus une sonde) · `migrate(opts)` · `baseline(upTo?)` · `repair({source?, updateHashes?})`.
Exporté par `index.ts` (surface publiée : CLI, data plane, sonde, porte d'agent la consomment).

- **Connexion UNIQUE, jamais un pool** (`drivers/{sqlite,postgres,mysql}Driver.ts`, fabrique
  `openMigrationDriver`). `pg_advisory_lock`/`GET_LOCK` sont des verrous de SESSION : un pool
  poserait le verrou sur une connexion, le DDL sur une autre, la libération sur une troisième — il
  ne protégerait rien. C'est pourquoi l'applicateur n'utilise PAS `DrizzleOrm`, dont le pool fait
  10 connexions. Mêmes imports lazy, zéro dépendance nouvelle.
- **Identités GRAVÉES** (contrats inter-versions, testées à la chaîne près dans
  `tests/unit/migratorContracts.test.ts`) : clé pg = `sha256("nodefony:migrations")[0..8]` en entier
  signé big-endian = `9131242216657117845` (une RÈGLE, pas un choix) · nom mysql =
  `nodefony:migrations:<DATABASE()>`, **qualifié côté serveur** parce que `GET_LOCK` est global à
  l'instance ; repli haché déterministe au-delà de 44 caractères de nom de base (MySQL borne un nom
  de verrou à 64) · table `nodefony_migrations`, **jamais qualifiée d'un schéma** (sinon
  l'isolation PostgreSQL par `search_path` serait exclue à vie).
- **Attente du verrou BORNÉE** : `pg_try_advisory_lock` en boucle jusqu'à `lockTimeoutMs` —
  `pg_advisory_lock` attend sans limite. `SET lock_timeout` reste posé pour son vrai rôle : les
  verrous de TABLE que prennent les `ALTER`.
- **Amorçage de la table d'historique** (`history.ts`) : `CREATE` puis `ensureHistorySchema()`
  (introspection + `HISTORY_STEPS` ordonnés, VIDE aujourd'hui), juste après le verrou et AVANT tout
  `SELECT` — la table qui sert à migrer ne peut pas se faire migrer par une migration. **Pas de
  colonne de version : la PRÉSENCE des colonnes EST la version.** Corollaire : jamais `SELECT *`,
  jamais d'insertion positionnelle (`BASE_COLUMNS` nommées partout).
- **Empreinte `sha256:<hex>` du contenu NORMALISÉ** (CRLF→LF) : un checkout Windows sous
  `core.autocrlf` ferait sinon diverger toutes les empreintes ⇒ arrêt sur dérive permanent pour un
  non-changement. Le préfixe est la seule porte de sortie pour changer d'algorithme sans réécrire
  les bases.
- **Sources = espace de noms OUVERT** (`{name, dir, rank}`) ; `framework` rang 0, `app` rang
  1 000 000, modules entre les deux ; nom en départage = ordre déterministe. Une source vue en base
  mais **absente du registre** (module désinstallé) est **ignorée en la nommant**, jamais un arrêt
  global — sinon désinstaller un module bloquerait tout `migrate` à jamais.
- **Plan par IDENTITÉ `(source, tag)`**, pas par repère haut : la mise à jour du framework insère
  des migrations dans le passé de l'app, par construction.
- **Validation fail-loud AVANT toute écriture**, verdicts `NF_MIGRATE_*` (`FAILED_MARKER`,
  `HASH_MISMATCH`, `MISSING_FILE`, `OUT_OF_ORDER`, `BASELINE_REQUIRED`, `UNKNOWN_FORMAT`,
  `LOCK_TIMEOUT`) portés par `MigrationVerdictError` : **le verdict structuré est la source, la
  prose un rendu** (`code` + `facts` + `nextActions`) — un agent lit `code`, jamais une phrase.
- **Format de fichier validé** : première ligne `-- nodefony:migration format=1` + version de
  journal `"7"` ; un format inconnu est refusé **en nommant le fichier**, jamais lu au mieux.
- **Transaction par migration** : pg/sqlite = `BEGIN` → statements → `INSERT` succès → `COMMIT` ;
  échec ⇒ `ROLLBACK` puis marqueur d'échec **HORS** transaction (écrit dedans, il disparaîtrait avec
  elle). **mysql = DDL NON transactionnel** ⇒ marqueur de début posé AVANT, état partiel assumé,
  **jamais de reprise aveugle** : `repair()` après inspection humaine.
- **Adoption explicite** (`baseline`) : historique vide + tables déjà présentes ⇒ refus
  `BASELINE_REQUIRED`. Jamais automatique — l'auto retirerait le filet « mauvaise base ».
- Preuves : `tests/integration/migrator-sqlite.test.ts` (14, dont l'application des migrations
  RÉELLEMENT livrées) · `migrator-postgres.e2e.test.ts` (5, gate `NF_PG_URL`) ·
  `migrator-mysql.e2e.test.ts` (5, gate `NF_MYSQL_URL`, passés sur MariaDB 11.4 ET MySQL 8.4) ·
  `tests/unit/migratorContracts.test.ts` (9). **12 des 14 cas sqlite ont été vus ROUGES** par
  débranchement ciblé de leur garde ; les deux verrous idem.
- ⚠️ Ce qui n'est PAS prouvé : deux bases RÉELLES sur le même serveur mysql (l'utilisateur
  applicatif n'a pas le droit de `CREATE DATABASE`, `ERROR 1044`) — la qualification est prouvée
  par le nom composé côté serveur et par deux noms distincts obtenus simultanément.

## Core Components

- `DrizzleOrm extends Orm` : onConnect **route selon `dialect`** (sqlite better-sqlite3 sync / postgres `pg` lazy) ; `new BetterSqlite3(filename)` + `drizzle(client)` (sqlite). Schema-as-code (entity.schema = table). DDL via `getTableConfig()`. tx manuelle. **`describeEntity(name)`** : colonnes normalisées via `getTableConfig().columns` (`name/getSQLType()/primary/!notNull/isUnique`) → alimente le data plane ORM/ERD/IA (orm-core). Le **module Drizzle** (`index.ts onKernelBoot`) appelle `registerOrmAdminApi(broker)` (idempotent) → monte `/nodefony/orm/api/*` (orm-core étant lib pure).
- `DrizzleRepository<T>` : CRUD + `#where` (criteria → eq/and/gt/inArray/like) + eager-load manuel (`#populate`, 1 req IN par relation) + `withTransaction`.
- **SELECT préparés mémoïsés par FORME** (`#preparedSelect` → `#runSelect`, donc find/findOne) : forme = champs du criteria (null marqué à part — `IS NULL` non paramétrable) + order + présence limit/offset ; valeurs/limit/offset = `sql.placeholder()` re-bindés à CHAQUE exécution (cache de FORME, jamais de DONNÉES — anti-staleness prouvé par test). Payé 1×/forme : sqlite = statement natif compilé (`client.prepare`), pg = requête NOMMÉE `nf_ps_<seq>` (plan caché par connexion du pool), mysql = drizzle passe par `client.query()` (0 prepare protocole) → gain JS seul (`sqlToQuery` amorti). **Repli chemin non préparé** : `$or`, opérateurs riches (`$in` = SQL à cardinalité variable), `undefined`, transaction (6ᵉ arg ctor `transactional`, handle pg éphémère), cache plein (cap 128/repo, pas d'éviction). ⚠️ `eq(col, placeholder)` NU = piège : `bindIfParam` EXCLUT les Placeholder → pas de `mapToDriverValue` (array json brut = RangeError better-sqlite3, vu rouge) → toujours `eq(col, sql.param(sql.placeholder(…), col))`. Cache par instance de repo (jeté au `disconnect` avec ses statements). Mesuré (A/B 2 paires, c25, corpus dolibarr) : read-lean 1 077-1 090 → 2 003-2 035 RPS (**~+86 %**), cycle session+find 769-778 → 1 515-1 518 (**~+96 %**). Tests : `tests/integration/prepared-select.test.ts` (spy `Database.prototype.prepare` = compteur de compilations natives).
- `DrizzleTransaction` : BEGIN/COMMIT/ROLLBACK **3 dialectes** (managée), via `ITxDriver` (`exec`/`quoteIdent`/`release`) — la classe ne connaît AUCUN driver. Fabrique posée par le `#connectX` → `DrizzleOrm.#beginTx` (`null` = not connected). **sqlite** = connexion unique (rien à rendre) ; **pg/mysql** = connexion DÉDIÉE empruntée (`pool.connect()` / `pool.getConnection()`), `drizzle(cx)` lié à ELLE, rendue au commit/rollback — sinon `BEGIN` et écritures sur des connexions différentes = 0 atomicité + pool épuisé. `release(err)` → connexion DÉTRUITE (`cx.release(err)` pg / `cx.destroy()` mysql2), jamais recyclée à l'état inconnu. `getNative()` = db de CETTE connexion → seul `withTransaction(tx)` entre dans la tx (`getRepository()` passe par le pool = HORS tx). Savepoint : nom validé `/^[A-Za-z_]\w*$/` + `quoteIdent` **par dialecte** — `"x"` = CHAÎNE en mysql/mariadb (hors ANSI_QUOTES), backticks obligatoires.
- `probe()` **routée par dialecte** (data plane admin / panneau Studio ORM) : **sqlite** = `storage` via PRAGMA (`page_count`×`page_size`, `journal_mode`, `freelist_count`) ; **pg/mysql** = `pool` (compteurs EN MÉMOIRE, 0 requête) — pg via l'API publique (`options.max` défaut 10 / `idleCount` / `totalCount-idleCount` / `waitingCount`), mysql2 **ne publie RIEN** → internes narrowés champ par champ (`MysqlPoolInternals` : `promisePool.pool.config.connectionLimit` / `_freeConnections` / `_allConnections` / `_connectionQueue`, des Denque → `.length`) : un renommage mysql2 = sonde PARTIELLE (jamais un throw), le banc de contrat le dit. Le **stockage d'une base serveur n'est PAS sondé** (coûterait `pg_database_size` par appel ; l'admin du SGBD l'expose déjà) — ne rien promettre plutôt que promettre en silence. `{}` UNIQUEMENT hors connexion.
- `DrizzleOrmOptions { filename }` (`:memory:` par défaut).

## Behaviors

- **Profiler tap `#prof(builder)` (étendu aux eager-loads)** : chaque exécution (find/create/update/delete/count + eager-load) passe par `#prof`. Alimente **2 sondes** indépendantes, gardées toutes deux par un drapeau (coût nul si les 2 OFF — `if (!buf && !flow) return builder`) : (1) **profiler par-requête** = `RequestContext.get()?.queries` (dev/ALS) → pousse `{sql,durationMs,rows,connector:"drizzle"}` ; (2) **flux agrégé** = `queryFlowMonitor.enabled` → `record(this.#connector, durationMs, sql?)`. **Lecture ALS directe** (≠ closure d'un ORM async) : better-sqlite3 **synchrone** sans pool → ALS valide pendant `await builder`. **Sécu** : `builder.toSQL().sql` = SQL **paramétré** (`?`, jamais les valeurs) ; `redactSecrets` en plus (défense). ⚠️ les finders natifs `sql\`…\`` (`findBySocialProvider`) NE passent PAS par `#prof`(raw`db.all`).
- **Sonde flux ORM** : le repo connaît son **connecteur** via le 4ᵉ arg ctor `connector` (passé par `DrizzleOrm.getRepository(name)` = `this.name`, propagé dans `withTransaction`) → `record` tague par connecteur (≠ vendor). `toSQL()` appelé **UNIQUEMENT sur le chemin lent** (`durationMs >= queryFlowMonitor.slowMs`, défaut 50) via helper `#safeSql` → l'agrégat ne paie jamais la sérialisation au cas nominal. **Gating** : `DrizzleService.onBoot` → `queryFlowMonitor.setEnabled(env!==production)` (override `NF_ORM_FLOW=1/0`). Les bancs `test:load` instancient `DrizzleOrm` **hors kernel** → flux reste OFF → **0 régression** (vérifié : insert 16k ops/s, scan 1M ops/s inchangés). Détail moniteur → orm-core MEMORY.
- Opérateurs riches: `FieldOperators` (orm-core) `$eq $ne $gt $gte $lt $lte $in $nin $like`. `isFieldOperators()` détecte. `$like`=SQL, émis par `likeSql.likeCond` avec `ESCAPE '\'` (littéral doublé en mysql) — jamais `like()` nu.
- DDL dérivé: `col.name/getSQLType()/primary/notNull/isUnique`. CREATE TABLE IF NOT EXISTS.
- Relations: one-to-many FK=`<source>Id` sur target; many/one-to-one FK=`<target>Id` sur source. localKey/targetKey='id'.
- Native: `getNativeConnection<DrizzleDb>()` → `db.all(sql\`...\`)`.

## Gotchas

- better-sqlite3 SYNCHRONE → pas `db.transaction(asyncCb)` (committe avant await). → BEGIN/COMMIT manuel.
- **Une transaction sur un POOL exige une connexion dédiée** : sans emprunt, `BEGIN` et les écritures partent sur des connexions différentes → aucune atomicité (et `BEGIN` orphelin recyclé). Vaut pour tout futur driver poolé.
- **Divergence assumée (non testée au banc de parité)** : visibilité avant commit depuis un repo NON lié — sqlite (connexion unique) la voit, pg/mysql non. Portable = `withTransaction(tx)` seul entre dans la tx.
- **Banc de contrat : jamais trier sur du texte** (PK = UUID aléatoire → ordre physique aléatoire ; tri texte = collation du backend, MySQL 8.4 `utf8mb4_0900_ai_ci` ≠ MariaDB). Trier sur un entier.
- 🔴 **Un pool `pg` SANS `pool.on("error")` TUE le process.** `pg-pool` fait `pool.emit("error")` quand un client INACTIF tombe (serveur redémarré) ; un EventEmitter qui émet `error` sans auditeur lève, et le framework n'installe aucun `uncaughtException`. Mesuré : `docker stop` du serveur PG ⇒ exit 1. `#wirePgLifecycle` écoute `error` (perte) + `connect` ET `acquire` (retour — `connect` seul rate le cas du client idle réutilisé). `mysql2` y échappe seulement parce que `PoolConnection` pose son propre `once("error")` ; son pool n'émet aucun `error`, d'où `#wireMysqlLifecycle` qui s'abonne via `connection`.
- 🔴 **Les événements de pool ne couvrent QUE le client INACTIF** : `pg-pool` retire l'auditeur du client pendant qu'il sert (`index.js:344`). Mesuré : coupure pendant une requête en vol, ou requête émise pendant la coupure ⇒ `isConnected()` restait vrai. C'est le battement d'`orm-core` qui ferme ce trou, pas le câblage.
- 🔴 **En MySQL, l'`error` d'une `PoolConnection` n'arrive PAS quand le serveur tombe.** Mesuré (`docker stop` sous connexion établie) : le socket rend `end` puis `close`, la requête en vol est rejetée en `PROTOCOL_CONNECTION_LOST` (`fatal: true`), et la connexion n'émet rien — `mysql2` délivre l'erreur fatale au demandeur, pas à l'émetteur. L'écoute `cx.on("error")` ne couvre donc que la connexion INACTIVE. D'où la seconde écoute, sur `cx.stream` : une fermeture n'est pas une preuve (un recyclage en produit une aussi), elle appelle `beatNow()` — le battement tranche par une requête. Sans elle, MySQL restait marqué connecté jusqu'à 30 s là où `pg` bascule en millisecondes.
- 🔴 **Une transaction PG tient un client SANS auditeur `error`.** `pg-pool` retire le sien tant que le client sert (`pg-pool/index.js:344`, réattaché en tête de `_release`) — donc si le serveur tombe PENDANT la transaction, `pg` émet `error` sur un émetteur sans auditeur (`client.js:210`) et le process MEURT, en pleine transaction. Même défaut que celui fermé sur le pool, par le chemin resté ouvert. `#beginTx` (postgres) pose donc un puits sur le client emprunté et le retire au rendu, SYNCHRONE jusqu'au `release` (aucune fenêtre sans auditeur).
- **`acquire` en plus de `connect`** pour le RETOUR : `pg` ne recrée un client que si aucun n'est libre, donc après une coupure brève le pool reprend du service avec un client sain — sans jamais émettre `connect`.
- **`keepAlive: true`** (pg) / **`enableKeepAlive`** (mysql2) : `pg` le laisse à FAUX par défaut ; sans lui un socket zombie (NAT expiré, pare-feu) n'est jamais réveillé.
- **`disconnect()` : `alive=false` → `stopHeartbeat` → détacher → PUITS `error` → fermer.** L'ordre entier compte : détacher « proprement » juste avant de fermer rouvrirait le crash, car drainer un pool en fait émettre.
- **Le câblage se pose AVANT le ping `SELECT 1`** : ce ping ouvre la PREMIÈRE connexion, et `mysql2` ne signale ses connexions qu'à leur CRÉATION — câbler après laissait la première sans auditeur. Détachement par `#unwireAll()` (échec du ping ET `disconnect()`), sinon un cycle connect/disconnect empile un jeu de listeners par tour.
- **Le banc de contrat couvre `IOrm`, pas que `IRepository`** : `ping`/`probe`/`describeConnection`/`describeEntity`/`isConnected`/`getNativeConnection` n'étaient testés QUE sur sqlite → `probe()` rendait `{}` en prod (panneau Studio muet). Toute méthode d'adapter qui ne route pas par dialecte est un `transaction()` en puissance. `describeConnection` a un test **anti-fuite de credential** (le data plane l'expose).
- **Une sonde se prouve par l'ÉTAT, pas par la forme** : `typeof === "number"` passe sur des compteurs figés à 0 → le banc ouvre une vraie transaction et exige `borrowed >= 1`.
- Outillage de test = RACINE, pas de devDeps locales (sinon CJS resolve `ERR_PACKAGE_PATH_NOT_EXPORTED` sur les exports import-only d'orm-core). Runner = **vitest** ; mocha a été retiré du dépôt, ne pas réintroduire `.mocharc`.
- db typé `BetterSQLite3Database<Record<string,never>>` = vue d'exécution CANONIQUE tous dialectes (pas de `db.query`, eager-load manuel) ; tables → builders via `execTable()` uniquement.
- OFFSET sans LIMIT → routé par `#dialect` : fragment `sql\`-1\``sqlite / rien pg /`MAX_SAFE_INTEGER`mysql. ⚠️ drizzle 0.45 IGNORE silencieusement`limit(-1)` NUMÉRIQUE (émet OFFSET seul).
- JSON dialecte mysql : `mysqlJsonCompat` (customType colKit) — MariaDB rend une STRING (LONGTEXT), MySQL un objet → parse-si-string. Ne pas revenir au `json()` natif drizzle.
- mysql2 = flag `CLIENT_FOUND_ROWS` actif : `affectedRows` d'un UPDATE = lignes MATCHED (parité sqlite/pg) mais rend le verdict ODKU ambigu (cf idempotence).
- **Table STALE = schéma d'avant un fix de spec, pour toujours** (pas de migrations avant le chantier DDL drizzle-kit) : `CREATE TABLE IF NOT EXISTS` ne modifie jamais une table existante. Vécu : `session.user` en TEXT (d'avant la règle varchar-si-indexé du colKit) — **MariaDB MASQUE** (crée l'index en auto-préfixant `user(768)`, tout vert) là où **MySQL 8.4 RÉVÈLE** (`BLOB/TEXT column used in key specification` à CHAQUE connect). Un e2e mysql rouge au DDL sur base ancienne → comparer `SHOW CREATE TABLE` au spec colKit ; décor docker = DROP la table, elle renaît au schéma courant. C'est la démonstration du POURQUOI du gate MYSQL_COMMUNITY (même dialecte ≠ même serveur).
- Node 26: better-sqlite3 12.10 OK (prebuild). drizzle-orm 0.45.2. mysql2 3.22.

## Config

- peerDeps: nodefony, @nodefony/http, @nodefony/orm-core, @nodefony/user. deps: drizzle-orm, better-sqlite3, **zod** (ajouté à `external` rolldown). **optionalDeps: pg + mysql2** (drivers LAZY, externalisés rolldown) ; devDeps += @types/pg (mysql2 embarque ses types).
- Tests (vitest) : `npm test`. Aucun compte n'est écrit ici (il se périme au premier test ajouté ; et il MENT sans les variables d'infra) — le compte se demande au runner et se lit avec le bloc `gateReporter`. Les e2e PG (`NF_PG_URL`) et mysql (`NF_MYSQL_URL`) **se skippent sans leur variable**. Couverture : config Zod, banc orm-core, jointure complexe, SessionStorage IoC/CRUD, User, stores token/webauthn/totp/audit/webhook/idempotency, auto-register sqlite + postgres + mysql (3 fichiers, 1 dialecte de wire par process), **colKit (dont dateMs + variante mysql)**, **parité S2/S3 trois dialectes**, **repository `#pickOne`**, **banc contrat `IRepository` × 3 dialectes**, **e2e PG et mysql par brique**. Banc ADR-0002 User↔Room + age.
- Load: `npm run test:load` (`vitest.config.load.ts`, expose-gc) — charge, limites, mémoire. Insert 20k ~15k ops/s, scan ~1M/s, 30k cycles heapΔ 0.3MB, 300 conn heapΔ 0.1MB (0 fuite).
- Charge SESSION runtime (skill load-test, route `/nodefony/test/rest/session/set/k/v`, HTTP/2) : 3000/80 = 409 RPS p99 282ms ; 5000/150 = 408 RPS p99 562ms ; 100% 200, delta sessions EXACT (0 perte/doublon), 0 erreur. **Plafond ~408 RPS = better-sqlite3 SYNCHRONE mono-connexion** (writes sérialisés) — pas un bug, Postgres/MySQL paralléliserait. Concurrence ↑ = latence ↑, pas débit.
