{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Autres options de configuration pour les tests
  },
  "include": [
    "../*.test.ts",
    "../**/*.test.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}